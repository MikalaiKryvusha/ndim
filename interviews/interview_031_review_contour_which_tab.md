# Интервью №031 — контур вычитки: в какой вкладке Вы отвечали

> **Тема:** 2026-08-14 Вы написали «сломался контур» и приложили ответ JSON-ом. Ответ при этом
> записался правильно во все три места. Один вопрос разводит две версии причины — и от него
> зависит, чинить процедуру агента или саму страницу.
> **Статус:** 🟡 ждёт ответа
> **Адресат ответа:** `bugs/122` · `plans/27` (контур вычитки)

---

## Что известно точно

Ответ **не потерян**: в логе сервера `✅ РЕШЕНИЕ ЗАПИСАНО В ТРИ МЕСТА`, в документе стоит
`**Ответ:** **Д**` с Вашей подписью и временем 19:42, файлы решения и архива на месте.

Приложенный Вами JSON — дословно содержимое **спасательного окна** страницы (поля `answers`,
`artifacts`, `comment`, без поля `by`, которое дописывает сервер). Значит Вы видели ветку
«Записать не удалось: сервер агента не отвечает» — при том что запись состоялась.

## Чего я не знаю и знать не могу

Я поднял страницу вычитки **дважды**. Первый запуск был без явного порта: сервер взял случайный,
**сам открыл вкладку у Вас в браузере**, а свой вывод отправил в фоновый файл — адреса я не увидел.
Решив по пустому логу, что не поднялось, я **погасил тот сервер** и поднял второй, на `7788`, его
адрес и дал в чат.

### В1. В какой вкладке Вы нажали «Сохранить ответы»?

**Адресат ответа:** `bugs/122` · `plans/27`

- **А) В той, что открылась сама** (её открыл первый сервер, который я потом погасил).
  Тогда виноват я, и чинится процедура — она уже исправлена: страница поднимается ровно один раз,
  сразу с явным портом, адрес называется в том же сообщении, гасить сервер под человеком нельзя.
- **Б) На странице `127.0.0.1:7788`, адрес которой я дал в чате** — и «НЕ ЗАПИСАНО» появилось
  ПОСЛЕ зелёного «Записано».
  Тогда дефект в самой странице: сервер одного документа умирает через 2,5 с после записи, и
  повторное сохранение на незакрывшейся вкладке даёт ложную тревогу о выполненной работе.
  Лечение — спрашивать документ, не лёг ли ответ уже, прежде чем говорить «не записано».
- **В) Обе** — сначала в одной, потом в другой.
- **Г) свой ответ.**

**Ответ:**
