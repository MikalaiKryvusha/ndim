# NDim Space — МАСТЕР-ПЛАН

> Дорожная карта: как мы попадём из **текущего состояния** проекта в видение, описанное в `GOAL.md`.
> Высокоуровневая пошаговая декомпозиция — фазы и вехи, а не повседневные задачи (те живут в `plans/`).
> Выведен из `GOAL.md` агентом при развёртывании и обновляется скиллом `/revision` по мере того, как
> меняется цель или состояние.
>
> Это **живой справочник**, а не задача — никогда не помечается DONE.

**Выведен:** 2026-07-10 из `GOAL.md` · **Перевыведен:** 2026-07-10 после закрытия интервью №001

---

## Видение (одной строкой)

Превратить работающую, но кустарную реализацию честной математической идеи знакомств в современный
веб-сервис, которым захотят пользоваться миллионы людей — и который ищет им человека, а не их деньги.

## Направляющие принципы

Это применение `PHILOSOPHY.md` конкретно к NDim Space:

1. **Идея священна, реализация — нет.** Математика (`similarity = proximity × commonality`) — это то,
   ради чего проект существует. Всё остальное можно и нужно переписать.
2. **Best practice вместо велосипеда.** `GOAL.md` прямо просит: перестать изобретать, делать как
   делают гиганты индустрии. Каждый самодельный механизм в 1.x — кандидат на замену стандартным.
3. **Сначала стенд, потом код.** Версия 1.x была хрупкой, потому что проверялась глазами. Ничего
   нельзя считать работающим, пока это не проверено объективно.
4. **Добро, а не выручка.** Ни одна механика удержания, ни одна подписка за иллюзию выбора. Успех —
   это когда человек нашёл человека и ушёл.
5. **Чистый лист.** Старого кода в проекте нет: версия 1.x — в приватном `ndim-old`, а её смысл выжат
   в `researches/`. В верхнем уровне только ключевые файлы KAIF и NDim Space 2.0.
6. **Дизайн решает автор.** Перед реализацией ключевого UI/UX — 4 варианта макета на утверждение.
7. **Миграция уважительна.** Люди уже заполнили свои измерения; этот труд нельзя потерять.

## Отсюда — туда: фазовый путь

### Фаза 0 — Фундамент 🔧 в работе
- **Цель фазы:** у агента есть всё, чтобы работать автономно и проверять себя объективно.
- **Шаги:**
  - ✅ Наследие 1.x сохранено в приватном `ndim-old`; его git-история (825 коммитов) — в bundle.
  - ✅ KAIF развёрнут: канон, карты, скиллы, директории знаний.
  - 🔲 Математическое ядро извлечено (исходник — `researches/03`) в чистый, тестируемый модуль с
    настоящими именами метрик вместо обфусцированных `a…t`.
  - 🔲 Юнит-тесты ядра: тождество, пустое пересечение, крайние случаи. Проверка числами.
  - 🔲 Модель данных Firestore задокументирована; продуктовые тексты (ru/en) извлечены.
- **Статус:** 🔧

### Фаза 1 — Решения владельца ✅ закрыта 2026-07-10
- **Цель фазы:** закрыты развилки, которые агент не имеет права решать один.
- **Результат** (`interviews/interview_001_stack_hosting_backend.md`):
  **SvelteKit + TypeScript** · **домен + Firebase Hosting** · **Docker на домашнем ПК** ·
  **AGPL-3.0** · **тот же репозиторий `MikalaiKryvusha/ndim` с обнулением истории**.
- **Статус:** ✅

### Фаза 1.5 — Санация наследия 🔧 в работе
- **Цель фазы:** из проекта убрано всё, что нельзя публиковать; публичный репозиторий вычищен.
- **Шаги:**
  - ✅ Пароль SMTP изъят из кода и **отозван владельцем** (`bugs/03`, закрыт).
  - ✅ ПДн (299 почт) и ключ подписи вынесены в `.private/` вне git (`bugs/01`, `bugs/02`).
  - ✅ Новая история начата чистой; секреты в `.gitignore`; проверка в `tools/03-kaif-verify.mjs`.
  - 🔲 Вычистка старой истории из публичного GitHub (`homeworks/02`, шаг 2) — **блокирует push**.
  - 🔲 Резервная копия `.private/` вне рабочего диска (`homeworks/02`, шаг 3).
- **Статус:** 🔧 Блокирует любую публикацию. Разработку не блокирует.

### Фаза 2 — Ядро 2.0 🔧 в работе
- **Цель фазы:** математика живёт отдельно от интерфейса, покрыта тестами и переиспользуема и на
  клиенте (SvelteKit), и в вычислителе (Docker); модель данных пригодна к жизни.
- **Шаги:**
  - ✅ `calculateRelation` оформлен как самостоятельный TypeScript-модуль `src/lib/similarity/`
    с настоящими именами метрик вместо обфусцированных `a…t`.
  - ✅ Инварианты из `PROJECT_ARCHITECTURE_INTERNAL_MAP.md` зафиксированы 29 юнит-тестами
    (`npm test`); набор проверен мутациями — на сломанном ядре он падает.
  - ✅ Числовой паритет с 1.x подтверждён; декодер старых полей `a…t` готов (`legacy.ts`).
  - 🔲 Новая модель данных Firestore (владелец разрешил; текущая и её 9 проблем — `researches/02`).
  - 🔲 План уважительной миграции существующих данных на новую модель.
- **Почему это первое:** ядро не зависит ни от одного нерешённого вопроса, создаёт первый настоящий
  испытательный стенд проекта и защищает самое ценное, что есть у NDim, — саму идею (Парето).
- **Статус:** 🔧

### Фаза 3 — Фронтенд 2.0 на SvelteKit 🔲
- **Цель фазы:** продукт, которому доверяешь и которым хочется пользоваться, — наравне с гигантами.
- **Шаги:** инициализировать SvelteKit + TypeScript; компонентная архитектура вместо одного
  `index.html` на 3083 строки; дизайн-система; двуязычность (ru/en) как первый класс; SSR ради
  индексации; доступность и мобильность; перенос продуктовых текстов из архива `ndim-old`.
  **Каждый ключевой экран — через 4 варианта макета на утверждение автору.**
  После базовых экранов — **социальный слой** по идеям владельца от 2026-07-11:
  микроблог + комментарии + лайки (`ideas/03`), опциональная лента событий с гранулярной
  видимостью (`ideas/02`), личные чаты вместо Email (`ideas/04`). Порядок — за владельцем;
  ленты и видимость наследуют модель кругов (`researches/04`).
- **Статус:** 🔲 Каждое UX-решение — через `/interview`.

### Фаза 4 — Настоящий бэкенд (Docker на домашнем ПК) 🔲
- **Цель фазы:** расчёт связей уходит с телефона Николая и переживает выключение телефона.
- **Шаги:** контейнер с фоновым вычислителем, только исходящие соединения, сервисный аккаунт Firebase;
  расписание; уход от O(N²) «каждый с каждым» к масштабируемой схеме; админский доступ — не по списку
  email в `firestore.rules`; приватность ядра — не через обфускацию и динамический `import()`.
- **Статус:** 🔲 Переиспользует модуль из фазы 2 без изменений.

### Фаза 5 — Публикация 🔲
- **Цель фазы:** о NDim Space знают. Сайт индексируется, у него есть имя, он живёт.
- **Шаги:** купить домен (`ndim.space` или вернуть `ndim.app`) **с автопродлением**; привязать к
  Firebase Hosting; SEO — `sitemap.xml`, `robots.txt`, человекочитаемые URL, SSR (та самая боль из
  `GOAL.md`); лицензия AGPL-3.0 в репозитории; переиздание PWA/TWA с новым ключом подписи; аккуратный
  перенос существующих пользователей.
- **Статус:** 🔲 Требует закрытия фазы 1.5.

### Фаза N — цель достигнута
Люди заходят, честно описывают себя по осям, которые придумало сообщество, и находят тех, с кем у них
действительно сложатся отношения. Платформа не берёт с них денег и не удерживает их. Николай сделал
людям добро.

## Журнал решений

| Дата | Решение | Почему |
|------|---------|--------|
| 2026-07-10 | Новая git-история с чистого листа | Владелец: старый проект написан вручную, заслуживает уважения, но 2.0 начинается заново |
| 2026-07-10 | История 1.x вытянута в `.private/ndim-1.x-history.bundle`, а не оставлена по ссылке | Владелец однажды удалит `D:\work\ndim`; 3.86 ГБ loose objects упаковались в 39 МБ |
| 2026-07-10 | **Старый код полностью удалён из проекта**; версия 1.x — в приватном `ndim-old` | `ideas/01`: в верхнем уровне только ключевые файлы KAIF и 2.0. Смотреть старое — идти в архив |
| 2026-07-10 | Перед удалением `NDim_Space/` (14 файлов, 34.9 МБ) запушен в `ndim-old` | Проверка показала: он **никогда не коммитился** и существовал только на диске. Удаление уничтожило бы его навсегда |
| 2026-07-10 | Знание из 1.x выжато в `researches/02` (модель данных) и `researches/03` (ядро + матрица `a…t`) | Удалять код можно, терять знание — нельзя. Без матрицы `a…t` существующие `relations` не расшифровать |
| 2026-07-10 | **Новую модель данных Firestore проектировать разрешено**, миграция — уважительная | Владелец: текущая модель, возможно, неоптимальна. Но люди уже заполнили свои измерения, этот труд нельзя потерять |
| 2026-07-10 | **Правило четырёх макетов** для ключевых UI/UX и брендовых решений | `ideas/01`: дизайн 2.0 придумывается заново, но утверждает его автор. Агент рисует 4 варианта, автор выбирает |
| 2026-07-10 | Фаза 0 (стенд и тесты ядра) не ждёт ответов владельца | Математика не зависит от выбора стека — по Парето это самая ценная работа, доступная прямо сейчас |
| 2026-07-10 | Стек, хостинг и бэкенд вынесены в интервью, а не выбраны агентом | Необратимые решения уровня архитектуры и денег — это решения владельца (`AGENT_GUIDE.md`) |
| 2026-07-10 | Фронтенд 2.0 — **SvelteKit + TypeScript** | SSR/SSG из коробки → индексация решается стеком, а не костылями. Наименьший порог входа и наименьший бандл среди современных фреймворков (интервью №001, В1) |
| 2026-07-10 | **Купить домен** + оставить Firebase Hosting | Индексация упирается в домен, а не в хостинг. Добавляем одну сущность вместо смены платформы (Оккам). Автопродление обязательно — домен уже терялся однажды (интервью №001, В2) |
| 2026-07-10 | Бэкенд — **Docker на домашнем ПК**, только исходящие соединения | Расчёт связей — фоновая пакетная задача, а не публичный сервис. Входящих портов нет → опасения по безопасности не применимы. Ничего не арендуем (интервью №001, В3) |
| 2026-07-10 | Лицензия — **AGPL-3.0**, а не рекомендованная MIT | Владелец выбрал защиту цели над простотой: MIT позволил бы закрыть форк и сделать платный сервис из честной идеи. `GOAL.md` именно этому и противостоит |
| 2026-07-10 | Публикуем в **тот же репозиторий** `MikalaiKryvusha/ndim` (публичный), историю обнуляем | Сохраняется URL и след проекта; версия 1.x живёт в приватном `ndim-old` (интервью №001, В5) |
| 2026-07-10 | **Секреты и ПДн изъяты** в `.private/`, вне git | Аудит при развёртывании нашёл живой пароль SMTP (`bugs/03`, отозван владельцем), 299 почт (`bugs/01`) и ключ подписи (`bugs/02`) |
| 2026-07-10 | Коммиты подписываются **noreply-адресом GitHub** | Настройка приватности почты владельца отклоняет push, раскрывающий его настоящий email |
| 2026-07-10 | **Историю оценок не делаем** | Владелец: «история — сложно, пока не делаем». Проблема №8 модели 1.x снимается осознанно, а не по недосмотру |
| 2026-07-10 | **Видимость настраивается по каждому свойству**: всем · никому · друзьям · группе | Решение владельца. Вводит в продукт друзей и группы — сущности, которых в 1.x не было |
| 2026-07-10 | **Профиль разбивается на документы по аудиториям** (`profile/everyone`, `/friends`, `/group_{id}`, `/nobody`) | Не выбор, а следствие фактов: Firestore не умеет скрывать поля при чтении («you either retrieve the full document, or you retrieve nothing»), а бэкенд без входящих портов не может фильтровать их на лету. `researches/04` |
| 2026-07-10 | **Ось = отдельный документ** (`points/{uid}/dims/{dimId}`) | Правила Firestore не умеют перебирать значения map — отсюда и костыль 1.x «одна ось за обновление». Документ на ось снимает его и даёт валидацию каждой оценки |
| 2026-07-10 | **Группа — приватный круг владельца**, а не общая сущность; «Друзья» — дефолтный круг с взаимным согласием; своих кругов до 10 | Интервью №002, В1–В2. Круги невидимы для тех, кого в них положили: имена и состав знает только владелец |
| 2026-07-10 | **Аудитория свойства = набор кругов через ИЛИ** | Интервью №002, В3. Цена: свойство дублируется по бакетам отмеченных кругов (максимум 11 документов) |
| 2026-07-10 | **Дружба живёт вне кругов**, в `friendships/{pair}`; принять запрос может только адресат | Дружба симметрична, а круг — односторонний список. Правила обеспечивают взаимное согласие без участия бэкенда |
| 2026-07-10 | **Оценки по осям не видит никто**, кроме владельца и вычислителя | Интервью №002, В4. По оценкам составляется психологический портрет; для знакомства достаточно самой похожести |
| 2026-07-10 | При миграции всё остаётся **видимым всем**, как в 1.x | Интервью №002, В5. Статус-кво: ни одно поле не раскрывается сверх прежнего и ни одно не прячется молча |
| 2026-07-10 | **Умолчание видимости — «скрыть»** | Свойство без записи в карте видимости уходит в приватный бакет. Забытая настройка обязана означать «никто не видит», а не «видят все» |
| 2026-07-10 | Старый репозиторий **приватизирован и переименован** в `ndim-old`, а не удалён | Решение владельца оказалось лучше предложенного агентом удаления: экспозиция закрывается так же надёжно (0 форков), но история 1.x сохраняется на GitHub как приватный архив |
| 2026-07-10 | Новый публичный `MikalaiKryvusha/ndim` — чистая история, `LICENSE` (AGPL-3.0) с первого коммита | Публикуем только то, что прошло `npm run kaif:check`: без секретов и ПДн |
| 2026-07-10 | `npm run kaif:check` обязателен **перед каждым push** | Утечка уже случалась однажды (`bugs/03`); второй раз она не должна пройти молча (Мёрфи: названный риск — наполовину управляемый) |
| 2026-07-11 | **Социальный слой 2.0**: микроблог с комментариями и лайками, опциональная лента событий, личные чаты вместо Email | Идеи владельца (чат) — `ideas/02`–`04`. Чаты закрывают разрыв воронки «нашёл → напиши на Email»; лента строго opt-in и хронологическая (иначе конфликт с `GOAL.md`) |
| 2026-07-11 | **Бренд-символ — классическая N-сеть 1.x**; освежается только цвет, Ч/Б-инвариант обязателен | Правка владельца: «три точки» отвергнуты по форме; N-сеть — узнаваемый бренд. 4 цветовых варианта — `design/logo-mockups.html`, ждёт выбора |

---

> **Сопровождение:** держи план синхронным с реальностью. Когда `GOAL.md` или состояние проекта
> материально сдвинутся — запусти `/revision`, чтобы заново вывести фазы. Детальные планы, реализующие
> каждую фазу, живут в `plans/`.
