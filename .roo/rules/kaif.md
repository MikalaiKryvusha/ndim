# KAIF

Read `AGENT_GUIDE.md` before every task; keep `STATUS.md` current. Skills live in `.roo/commands/` (one `/command` per KAIF skill).

A message that opens with the word `resume` is an ORDER to run /resume in full before the rest of the message (KAIF 2.7).
