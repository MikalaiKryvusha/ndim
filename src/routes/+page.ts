/**
 * КОРЕНЬ `/` — ГЛАВНАЯ С СОДЕРЖАНИЕМ (`plans/81`, макет V5; решение владельца №058 В1 = А,
 * форма — №060). Прежняя роль «крошечный распознаватель языка под noindex» отменена им же.
 *
 * Настройки страницы объявлены в `+page.server.ts` вместе с её данными: держать `prerender` и
 * `csr` в двух файлах — верный способ развести их молча.
 */
export {};
