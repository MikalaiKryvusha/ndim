/**
 * КОРЕНЬ `/` — крошечный распознаватель языка, а НЕ страница (`plans/39` шаг 2; интервью №010,
 * Р5 = В: «сам корень перестаёт быть страницей и в поиске не участвует»). Лендинг живёт на
 * `/ru` и `/en`.
 *
 * `csr = false`: оживлять нечего — инлайн-скрипт уводит человека раньше, чем доехал бы бандл.
 * Браузер не качает и не исполняет фреймворк ради страницы, которую никто не видит.
 */
export const csr = false;
