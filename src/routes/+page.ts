/**
 * КОРЕНЬ `/` — ГЛАВНАЯ: новая V1 лендинга (`plans/106`; решение владельца №096 В6 = Б).
 *
 * Страница пререндерится (`+layout.ts`) и ГИДРИРУЕТСЯ: тест на совместимость живёт в Svelte. Прежняя
 * главная V5 держала `csr = false` в `+page.server.ts` вместе со своими данными (полоса каталога) —
 * тот файл удалён вместе с ней, данных у новой главной нет: числа — снимок `landing-metric.ts`.
 */
export {};
