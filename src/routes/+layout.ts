// Весь сайт пререндерится в статический HTML на этапе сборки (adapter-static).
// Поисковики получают готовый текст без исполнения JS — это прямой ответ на боль
// индексации из GOAL.md. Когда появятся экраны с авторизацией, флаг станет
// построчечным решением, а не глобальным.
export const prerender = true;
