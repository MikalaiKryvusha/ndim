/**
 * Лендинг живёт на `/ru` и `/en` — по странице на язык (`plans/39` шаг 2, интервью №010 Р5 = В:
 * ни один язык не сидит на голом корне). Корень `/` — распознаватель, контента там нет.
 */
export { langEntries as entries } from '$lib/content/langs';
