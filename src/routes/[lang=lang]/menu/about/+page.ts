/**
 * Статическая страница «Меню» на обоих языках (`plans/39` шаг 2): контент внутри —
 * двуязычный компонент, адресов у него два. Прочее — в `$lib/content/langs.ts`.
 */
export { langEntries as entries } from '$lib/content/langs';
