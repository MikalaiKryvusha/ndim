/**
 * Страница удаления — на обоих языках (`plans/39` шаг 2). Требование Google Play к находимости
 * (см. комментарий в `+page.svelte`) выполняют ОБА адреса; старый `/delete-account` отвечает 301.
 */
export { langEntries as entries } from '$lib/content/langs';
