# NDim Space — Текущий статус

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16. **Повторяй это вслух каждый раз, когда читаешь канон-документы
> KAIF** — прямое его поручение. Разбор повода — в шапке `AGENT_GUIDE.md`.
<!-- ВЕРА-КОНЕЦ -->

> Этот файл читается ИИ-агентом перед каждой задачей. Это ГЛАВНАЯ передача эстафеты между
> сессиями: новая сессия стартует с пустым контекстом и должна стать продуктивной, опираясь на
> один этот файл. Пиши конкретно — с путями к файлам и командами.
> 🧠 Принцип мышления — `PHILOSOPHY.md` (ПРОСТОТА). Рабочий фреймворк — `AGENT_GUIDE.md`.
>
> ⚠️ **STATUS — СВОДКА «СЕЙЧАС», а не летопись.** Мягкая цель ~200 строк. Каждая строка проходит
> два теста: *«убери её — ошибётся ли следующий агент?»* и *«читается ли файл за один присест?»*
> Закрытое ВЫНОСИТСЯ дословно в `PROJECT_HISTORY.md` (шаг «стрижка бонсая» в `/end-chat-soft`), а не
> копится здесь. Оставь файл таким, каким хотел бы его найти: что работает, что в работе, что
> дальше, какие капканы и ГДЕ ИСКАТЬ подробности — указатели, а не пересказы.

<!-- ИМЯ-ОК: строка объявляет вычистку и обязана назвать вычищенное слово -->
**Последнее обновление:** 2026-08-29, смена 10 (закрытие) — **СМЕНА 10 ЗАКРЫТА по слову владельца: №059 отвечен (В1 = А → ОКНО МЕРЖЕЙ ИСПОЛНЕНО, четыре ветки + шаг 10, ворота 7 → 13), владельцу стоят №060/№061/№062/№057; вердиктов за смену ДВЕНАДЦАТЬ+ (№14–№25 и далее), стрижка бонсая и ретроспективы 7–8 оплачены; словарь доски — 🔵 в работе / 🔴 оффлайн (слово владельца); релиз 2.2 — первой операцией смены 11 (883 коммита с 2.1).**

## 🟢 ХОД СМЕНЫ 11 (2026-08-29, вечер) — команда поднята, релиз 2.2 подан владельцу

**Роли в строю все пять** (свежие окна), инструктажи по манифесту разосланы: dev-3 — Яндекс +
корзина №20–№22 · dev-1 — охват стража импорта + дочистка researches/55 · Интегратор — гигиена
questions (3 красных) + bugs/222 + отказ close-bug без секции критериев · QA — построчный суд
мастерской dev3-shift6 (та самая «отдельная смена», №8) · Дизайнер — перезакрепление доли меты
(разблокировано мержем dev-3 окном 10).

· **Релиз 2.2 НАЧАТ первой операцией:** черновик записи «Версия 2.2» — в `researches/06` под
  маркером `[AI]`, вычитка поднята страницей — **№063** (три строки: первая оценка = вход ·
  пополнение каталога · оптимизация; счётчик и главная сознательно не упомянуты — ждут №061/№060).
  После «принято»: снять `[AI]` → `/release` (бамп 2.2.0, тег, GitHub Release, выкат) — дату в
  записи сверить с днём фактического выката.
· **Яндекс, сутки 29-го закрыты сменой 10** (dev-3 сверил сухим прогоном: квота 0, остаток 1051;
  строка в bugs/203, коммит 21842f5). 🔑 Механика в постановку впредь: смена тех же суток сперва
  СПРАШИВАЕТ квоту сухим прогоном (`yandex-recrawl.mjs` без --apply), подача — только по
  «к отправке > 0»; первая настоящая подача смены 11 — 30-го.
· **Словарь доски дочищен до инструмента:** `--busy` печатал «🔴 занят» вопреки слову владельца —
  теперь «🔵 в работе» (team-status.mjs, селфтест 23/23). Прогон открытия смены:
  `verify-copy-live --live` — витрина сходится с базой, 4 поля × разница 0, снимок 0 дн.
· Переезд каталога `ndim_dev2` → `ndim_integrator` снова отложен: окна ролей открыты, а переезд
  законен только при закрытых (сказано Интегратору в инструктаже).
· 🔄 **Перезакрепление доли меты СНЯТО замером Дизайнера (48872f8): правка, рождающая «24»
  (79881a7, действительный залог в лексиконе), лежит в `dev3-shift6-20260823` — ветке МАСТЕРСКОЙ,
  а не в слитой `ndim_dev3`.** До/после мержа main числа совпадают до сотой (ru 19,67 · en 22,80,
  слепки git archive). Уезжают ОБА числа: ru 20 → 24 И en 23 → 27 — перезакрепление «по сообщению»
  поправило бы половину. Оба числа уже сняты в юнит блоком «⏳ следующее перезакрепление» (в ветке
  Дизайнера). **Новый замок, якорь коммитом:** перезакрепление — после того, как `79881a7` станет
  предком main (`git merge-base --is-ancestor 79881a7 main`), то есть после построчного суда
  мастерской и приведения её автором. 🔑 **Правило эстафеты (класс дважды за двое суток):
  условие разблокировки называет КОММИТ, а не имя роли/ветки** — у роли веток может быть две,
  и «после мержа ветки dev-3» открывается ключом от соседней двери; проверка предком — команда,
  а не память. (EXP-NEW в ветке Дизайнера, номер при мерже.)
· ⚠️ Всем ролям после обновления до main: `npm install` в своём worktree — main принёс
  зависимость `posthog-js`, отставшие node_modules роняют `check` (нашёл Дизайнер).
· ✅ **ВЕРДИКТ №27: мастерская dev3-shift6 ПРИНЯТА** (долг №8 оплачен; 27 файлов / 11 234
  строки построчно; голова main на вынесении 9c9148c; предмет 12, мерж понесёт 11).
  🔴 **Три условия к мержу + решения Менеджера, исполняет dev-3 при приведении руками:**
  (1) резолв конфликта ворот приёмки обязан сохранить ОБЕ половины защиты bugs/183 — после
  резолва оба grep-контроля из №27 дают 1 (иначе откат вердикта №21 без единого красного);
  (2) страж tag-corpus въезжает с ОБЪЯВЛЕННОЙ границей по идиоме CORPUS_FLOOR — четыре
  известных раскола поимённо (корпус в ветке побайтно тот же; правка данных — слово
  владельца: «ролевая игра» уже в №061 В4, вопрос по трём остальным — в очередь вычитки
  ПОСЛЕ мержа, мерж не держит). 🔑 Форма границы — уточнение QA, принято: Set из ИМЁН, не
  число, красный В ОБЕ СТОРОНЫ (новый раскол вне списка → красный; имя из списка, ушедшее
  из корпуса → красный «вылечено — снять допуск, назвав кто и когда») — допуск самоубывающий,
  число стало бы индульгенцией и глушило бы стража ровно на новом расколе; обе мутации —
  в самотест dev-3, QA перепрогонит их приёмкой; (3) перезакрепка доли меты — ЗАМЕРОМ прибора на слитом
  дереве (живые числа ru 24 · en 27, сверены с независимым замером Дизайнера 48872f8;
  ⛔ числа из комментария теста 26/33 не даёт ни одно дерево — не переписывать).
  ⚠️ Порядок dev-3 переставлен: приведение мастерской — ПЕРЕД корзинными пунктами про
  ворота приёмки (иначе двойной конфликт со старым строением). Очередь судов QA дальше:
  заявка dev-1 (ждёт его новую голову после подтяжки main).
· 🔴 **ВЛАДЕЛЬЦУ ОТДЕЛЬНОЙ СТРОКОЙ (находка dev-1, подтверждена QA):** удаление аккаунта
  кнопкой продукта НЕ удаляет `users/{uid}/privat/{uid}` (перечень erase.ts коллекцию не знает);
  сеть безопасности ловит ушедшего только по живому `relations/{uid}` — удаление персональных
  данных по требованию человека выполняется не полностью. Судьба пласта — интервью **№064**
  (страница поднята); при ответе А щель закрывается вместе с пластом, при Б — отдельной порцией.
· ✅ **ШАГ 1 ОКНА СМЕНЫ 11 ВЛИТ: main = 4729b6f** (заявка dev-1 целиком: plans/79 + страж
  импорта с охватом + разведка №064; EXP-0244/0245 присвоены; test:tools 430 → 439). Механика
  плацдарма повторена: Интегратор собирает `window-shift11`, Менеджер принимает ff-only и пушит.
· ✅ **ВЕРДИКТ №29: условия мастерской закрыты, «в мерж» шаг 2 дан** (f683f48; судья вернула
  дефект bugs/183 в исходник — краснеют ДВА юнита, обе половины защиты живы; допуск tag-corpus
  оказался Map «имя → причина и когда снять» — сильнее заказа; контроль шага: после мержа
  test:tools 540/540, guards 13/13). 🔴 **Живой долг Менеджера от №29:** четыре строки допуска
  tag-corpus обязаны УМЕРЕТЬ по ответам владельца — «ролевая игра» №061 В4, по трём остальным
  расколам подать вопрос ПОСЛЕ мержа; страж напомнит красным «ВЫЛЕЧЕНО» только чинящему корпус.
  Замечание автору мастерской (не держит): поведенческий замер девяти партий на живом Wikidata
  судья не воспроизводила — остаётся за автором.
· ✅ **ВЕРДИКТ №28: заявка dev-1 ПРИНЯТА, условий ноль — «в мерж» дан** (голова 10cabd4,
  довесок досужен; доказательство «правка есть перемещение» усилено судьёй: порядок операторов
  подпоследовательностью, не счёт строк). Правило «мутанта обезвреживают в исходнике» — строкой
  в TESTING_FRAMEWORK (микро-порция dev-1 после перезапуска ветки).
· 🆕 **Постановка named, без порции (находка Дизайнера):** страж «документ объявлен неначатым,
  а работа есть» находит работу ПО ССЫЛКАМ ВСЕГО РЕПОЗИТОРИЯ, а документ читает из ДЕРЕВА
  вызывающего — отставшая копия получает красный от чужого коммита и ищет причину у себя (сегодня
  стоило бы времени пятерым вместо одного; в main при этом 13/13). Кандидаты лечения: судить
  достижимое из HEAD вызывающего ЛИБО красный называет находку чужой и ворота сдающему не
  закрывает. Замер ширины — до признака.
· **Микро-заявка dev-1 в очередь судов:** правило 9 (EXP-0244) в TESTING_FRAMEWORK, голова
  8549253, 2 коммита, docs-only; граница честности внутри правила («случай назван по источнику,
  а не наблюдался автором») — снимается, когда ветка QA войдёт в ствол.
· **Очередь судов и мержей смены 11** (мержи по одному, после вердикта и «в мерж» на каждый):
  1) заявка dev-1 — 🔄 голова **51676fd**, 7 коммитов, суд ИДЁТ (добавились резолв реестра +
  **порция plans/79 ИСПОЛНЕНА dev-1**: дверь выката под предохранителем, 32 → 3 верхнеуровневых,
  A/B побайтово, юнит двери 5/5, отсрочек в реестре НОЛЬ; его инцидент мутанта — урок
  «обезвреживай в исходнике мутанта» — записан в plans/79; живой прогон smokeGate --stage
  --gate-only — ближайшим штатным окном стейджа, т.е. релизным выкатом 2.2) → 2) мастерская dev3-shift6
  — ✅ ПРИВЕДЕНА (голова f683f48, понесёт 11, симуляция бесконфликтна; условия №27 закрыты
  проверяемым выходом: grep-контроли + ПОВЕДЕНЧЕСКИЙ замер девяти партий до единицы · Set-допуск
  в обе стороны с обеими мутациями QA · доля меты 24/27 тремя независимыми замерами; второй
  нормативный ответ sitelinks СНЯТ резолвом-разбором; ждёт перепрогона условий QA) → 2а) НОВАЯ
  заявка: корзина dev-3 (голова c27cbb6, понесёт 3, с мастерской не пересекается; stash-пункт
  границы unverified — после мержа мастерской, на строении проверитьТекст) → далее по номерам
  (приведение автором по условиям №27) → 3) порция Интегратора (e0d131f: два признака круговых
  доводов + отказ close-bug без секции + 13 поправок bugs/222 + разводка дублей EXP-0086/0087;
  его EXP-0244/0245 сняты в плейсхолдеры — номера при мерже по порядку) → 4) ветка Дизайнера
  (🔄 голова теперь **192133c**: + bugs/182 закрыт замером с обоих концов — коммит в main И слово
  фикса в боевом чанке; + 🔴 КЛАСС ДНЯ «прибор молча мерит ЧУЖОЕ дерево» — третий род ложного
  зелёного (проверка исполнилась, признак верен, ПРЕДМЕТ другой): из голой оболочки stand-addresses
  падал на слот 0 главной копии; лечение — умолчание, совпавшее с чужим слотом, КРИЧИТ (не
  отказывает: на слоте 0 дверь выката байт-в-байт); правка общего инструмента ждёт согласия
  Интегратора-хозяина; макеты №060 + съёмщик + юнит-блок доли меты + bugs/187-доказательство
  с двумя дефектами прибора + bugs/123 закрыт; 🔄 решение Менеджера: суд НЕ ждёт ответа №060 —
  правки выбранного макета поедут ОТДЕЛЬНОЙ порцией поверх; ⚠️ конфликт с мастерской в
  tools/meta-share.test.mjs: перезакрепка dev-3 против блока «⏳» Дизайнера — числа совпадают
  (24/27), резолв = блок «⏳» гасится состоявшейся перезакрепкой, Интегратору смоделировать).
  **Карта мержей смены 11 заказана Интегратору** (образец shift10,
  моделирование git merge-tree — форма QA из №27). bugs/222 намеренно открыт до вердикта —
  закрытие close-bug'ом станет первым живым прогоном новых признаков.

## 🔴🔴 ЭСТАФЕТА СМЕНЕ 11 — с чего начинать (смена 10 закрыта 2026-08-29 вечером по слову владельца «закрывай смену»)

**ОКНО МЕРЖЕЙ СМЕНЫ 10 ИСПОЛНЕНО:** четыре ветки (dev-3 21 · Интегратор 16 · dev-1 27+2 ·
QA 21 коммит) + шаг 10 (починка claimsGate) слиты в main; ворота выросли **7 → 13**; номера
присвоены (EXP-0230…0243+, bugs/219–223, bugs/KAIF/04, researches/61–63). 🔑 **Механика
окна узаконена опытом:** main живёт в копии Менеджера (git не даёт ветку в два worktree) —
мержи Интегратор исполняет на ПЛАЦДАРМЕ (`window-shift10`), Менеджер принимает `ff-only`
и пушит; ствол заморожен от чужих коммитов на всё окно. Карта окна и «что вышло на
исполнении» — `reports/TEAM/2026-08-29_merge_map_shift10.md`.

### 1. 🔴 РЕЛИЗ 2.2 — первая операция смены (вопрос владельца: «может уже накопилось?» — ДА)

С бампа 2.1 (`da7027c`, 2026-08-14): **883 коммита · 990 файлов · +204 749 строк**. В бою с
тех пор: дверь карточки + воронка v2 (выкат `b81u35`) · счётчик посещений · машинный переобход
Яндекса · вся машинерия смен 4–10. Канон релиза — по прецеденту 2.1 (№027): **черновик
истории версий → вычитка владельца страницей → `/release`**. Не начат сменой 10 намеренно:
история версий требует его вычитки, а смена закрывалась.

### 2. Очередь владельца (страницы подняты/в очереди, `npm run questions`)

· **№060 «Главная с содержанием: выбор макета из четырёх»** — ГЛАВНОЕ: лечение Яндекса ждёт
  выката корня, корень ждёт выбора. После выбора: правки Дизайнера → суд его ветки
  (`2fac212`: макеты + съёмщик + прибор кропов + поправка замера карточек + набор знака) →
  мерж → **посев «Вот это драма!»** (Менеджер, из main) → перезакрепление доли меты.
· **№061 «Пачка текстов лица»** (5 вопросов, включая «править ли одобренное») ·
  **№062 «Три ответа на встречные вопросы»** (x-default по слову Google · четыре возможности
  счётчика · знак с кадрами) · **№057** (инструмент развёрток, с прошлых смен).
· Ответы №059 разнесены: В1 = А исполнен окном; В2–В4 проработаны в №062.

### 3. Пост-оконные порции (розданы вердиктами смены 10, исполнять после релиза)

· **Менеджер:** порция двери выката `plans/79` (приоритет поднят замером: fetch на верхнем
  уровне) · решение по чекбоксовому перекосу — отказ close-bug при документе ВООБЩЕ без
  секции критериев (63 из 83) — Интегратору.
· **Интегратор:** `bugs/222` — перепроверка 13 закрытий с уликой-самоссылкой · переезд
  каталога `ndim_dev2` → `ndim_integrator` (только при ЗАКРЫТЫХ окнах ролей) · дубли
  EXP-0086/0087 в летописи (наследство, летопись правится датированной поправкой).
· **dev-3:** корзина №20–№22 (охват в выводе переписи · граница unverified в воротах приёмки
  идиомой CORPUS_FLOOR · предупреждение сверхширокого запроса where-cured · вторая строка
  честности --branches) · класс «маркер про будущее устаревает молча» — усилен QA (№26):
  устаревает даже при ИСПРАВНОМ приборе; правило — **маркер описывает СОСТОЯНИЕ, а не
  намерение**, план живёт в реестре отсрочек со сроком годности, не в поле-факте.
· **dev-1:** охват в выводе стража импорта (№20) · сниппеты хабов (`researches/57`) — по «го»
  владельца · лестница PostHog Ш5+ после ответа №062 В2.
· **QA:** построчный суд мастерской `dev3-shift6-20260823` (20 файлов) — ОТДЕЛЬНОЙ сменой,
  свежим окном (решение №8 и смены 10).
· Плейсхолдеры `[[EXP-NEW-…]]` в стволе ждут мержа ветки Дизайнера (номера не занимать).

### 4. Тикает само

🔴 **Подача 150 адресов в Яндекс — ЕЖЕДНЕВНО** (остаток очереди 1051): первое действие dev-3.
**Прогон `verify-copy-live --live`** — Менеджер при открытии смены (прибор смотровой, в
воротах не стоит — решение смены 10). Хвосты выката `b81u35`: чтение `door_click`/
`guest_start` ≈ 05.09 · дата шва ряда в `bugs/204`.

### 5. 🆕 Словарь статус-доски — слово владельца 2026-08-29 (разнесено: манифест + инструмент)

Рабочий — **🔵 в работе** · свободен — 🟢 · прощальный при закрытии смены — **🔴 оффлайн**
(ставит каждая роль и Менеджер; `team-status.mjs set --offline`).

## 📜 ЭСТАФЕТА СМЕНЕ 10 — ИСПОЛНЕНА сменой 10 (оставлена до стрижки; смена 9 закрыта 2026-08-29 11:00)

**Ретроспектива смены 9 написана:** `reports/TEAM/2026-08-29_shift9_retrospective.md` — прочитай
её ПЕРЕД раздачей задач, там шесть классов и таблица шести опровержений Менеджера замером.
✅ **Долг ретроспектив 7 и 8 ОПЛАЧЕН сменой 10** — написаны вдогонку с честной рамкой
«чего не знаем»: `reports/TEAM/2026-08-28_shift7_retrospective.md` · `…_shift8_…md`.

**ГОЛОВЫ ВЕТОК НА ЗАКРЫТИИ** (сверь `git branch -v`, роли могли дослать):
`ndim_designer` bca1a05 (приведена к main) · `ndim_qa` e9a0c2d · `ndim_dev1` af4dcd0 (запушена) ·
`ndim_integrator` 57ab8db · `ndim_dev3` dfcb2ba · `dev3-shift6-20260823` 2415e52.

### 1. 🔴 ПЕРВЫМ ДЕЙСТВИЕМ — МЕРЖИ, до любой другой работы

Класс `EXP-0227` доказал цену очереди: страж, лежащий в неслитой ветке, не защищает, но
числится работающим. Порядок — **сначала порция А dev-3, потом Интегратор** (тогда первый прогон
ворот после второго мержа закроет переходящее обязательство одним заходом).

⚠️ **Мержи упираются в право сессии Интегратора на `git merge` — оно закрыто классификатором,
вопрос у владельца (интервью №059 В1).** Ни он, ни Менеджер запрет не обходили. Без ответа
владельца мержи не идут.

🔴 **РЕШЕНИЕ, ПРИНЯТОЕ ЗАРАНЕЕ, ЧТОБЫ ЕГО НЕ СЛОЖИЛИ ПО ИНЕРЦИИ (находка QA):** после мержа
порции А в воротах окажутся ДВА прибора одного класса — `verify-role-env-scope.mjs` (main) и
`verify-role-env.mjs` (порция А). Первый строго поглощает второй и сверх того краснеет на
НЕКЛАССИФИЦИРОВАННОМ имени. **При мерже более узкий `verify-role-env.mjs` СНИМАЕТСЯ, а не
складывается** — два нормативных ответа на один вопрос это будущий разъезд.

### 2. Что ждёт владельца — интервью №059 (страница поднята)

Четыре развилки: право мержа Интегратору · язык по умолчанию для двуязычной главной · выключить
четыре опции приватности на стороне проекта PostHog · знак в тёмной теме.
Страница: `test-results/owner-reviews/interview_059_shift9_four_forks.html`.
Плюс не отвеченное с прошлой смены — **№057** (инструмент развёрток).

### 3. Названные порции, готовые к раздаче

· 🔄 **Карточки будущего времени — замер ОПРОВЕРГНУТ перемером Дизайнера (смена 10):** из
  прежних «10 из 10» четыре обвинения ложные (будущее время про сюжет или порт), три настоящих
  замер пропустил (`\b` слеп к кириллице). Верно: осмотрено 130 · настоящих **9** · точность
  признака 35 % — судьёй в ворота НЕ ставить, класс держится ситом с уликами
  (`candidates/owner-critical-notes-2026-08-22.md`, верная таблица; прежняя рядом зачёркнута).
  🔴 **Все девять — в ПАРТИИ 1, одобренной и запрещённой к правке** («партию 1 уже не
  трогаем») → вопрос владельцу изменился: «править ли одобренное» — подан в **№061 В5**.
  Вне партии 1 класс закрыт: «Вот это драма!» переписана в ветке Дизайнера (`e6e81d2`).
· **Посев «Вот это драма!» в бой** — ⚠️ УТОЧНЕНИЕ СМЕНЫ 10 (2026-08-29): правка готова
  В ВЕТКЕ ДИЗАЙНЕРА (e6e81d2, «текст переведён в прошедшее время»), в main карточка ВСЁ ЕЩЁ
  в будущем времени (`выход которой назначен`). Посев из main пересеял бы неисправленный текст —
  класс EXP-0227. Порядок: мерж ветки Дизайнера → посев Менеджером из main (боевая база),
  замечание владельца при посеве уцелеет.
· **Критерий 2 `bugs/209`** — объявленная нижняя граница корпуса в страже метки + случай в
  самотест. ✅ Поправка 2026-08-29 (смена 11): критерий исполнен и `bugs/209` ЗАКРЫТ сменой 10
  (`209_DONE_`, шапка «закрыт 2026-08-29») — прежняя строка «оставлен открытым намеренно»
  пережила своё исполнение в тот же день (нашёл dev-3 при перемере трёх строк).
· **Проверка чекбоксов в `close-bug.mjs`** — «есть неотмеченные критерии → отказ с перечнем».
  Случай 209 показал, что доказательства, `PENDING:` и имени мало.
· **Вписать `verify-bug-doc-state.mjs` в `npm run guards`** — но сперва убедиться, что красных
  ноль. ⚠️ Страж судит РАБОЧЕЕ ДЕРЕВО запустившего: один документ бывает сиротой в одной ветке
  и не сиротой в другой. И ещё: после ближайшей стрижки бонсая часть ссылок переедет в летопись,
  и ноль сирот перестанет быть нулём — перемерить.
· ✅ **Перепись приборов ПО МАРКЕРУ В ШАПКЕ — СДАНА dev-3 сменой 10** (голова 5c5b5ed, ждёт
  суда): `verify-tool-markers.mjs` девятым в guards, 49 шапок размечены писарем, EXP-0194
  ловится механически В ОБЕ СТОРОНЫ (заявил ворота и не стоишь — красный; стоишь и молчишь —
  красный). ⚠️ Числа прежней редакции этой строки («приборов 80, ворот 8») были смесью
  контуров — поправка dev-3 замером: в main ворот СЕМЬ, приборов 80; после всех мержей
  состав ворот — ДЕВЯТЬ. Отсрочка verify-role-env — со сроком годности (строка сама краснеет
  при уходе файла из состава при мерже).
· **Предохранитель «запущен или подключён» у `tools/team-status.mjs`** — доски, которую зовут все
  пять ролей. Плюс вынести `stampNow` в `tools/lib/`.
· **Адреса «этот класс у нас уже вылечен вот здесь» в проекте не существует** (находка dev-3):
  лечение живёт в шапке чужого модуля, греп журнала по тегам его не находит. Это постановка, а
  не урок.
· **Замер Ш2 дал тройной ответ** (dev-1): объект НЕ потерян, он лежит там, куда человека не
  привели — разрыв навигационный, лечится показом или дорогой, а не сохранением.
· 🔄 **Утверждений об одном факте на первом экране гостя не три, а ПЯТЬ** (уточнение QA,
  вердикт №14; кадр `test-results/live-door-object/3-first-screen.png`): спорят не экраны, а
  ОДНА карточка сама с собой — profile:720 «не сохранены» против её же :728 «останется Вашим»
  и :748 «удалим эти данные»; пятое — delete-account:230 противоречит себе в одном
  предложении. ✅ ПОДАНО владельцу: кандидаты №061 В1/В2 правят ровно эту карточку.
· **Четвёртый скол корпуса тегов** («ролевая игра», 2 против 1) — числами не решается, владельцу.
· **Перезакрепление доли меты** — за Дизайнером, СРАЗУ ПОСЛЕ мержа ветки dev-3, не раньше.

### 4. Тикает само

🔴 **Подача 150 адресов в Яндекс — ЕЖЕДНЕВНО.** Остаток очереди 1051, ~7 дней; пропуск дня
очередь останавливает. Первое действие dev-3 каждой смены.

### 🏁 К ОКНУ МЕРЖЕЙ ГОТОВО ВСЁ (вердикты №14–№25, очередь судов ПУСТА)

**Покрытие трёх веток ЗАКРЫТО ПОЛНОСТЬЮ — ни одного непокрытого предка, ни одного условия:**
`ndim_dev1` 9d7ba2b (понесёт 27, вердикты №14/№16/№18/№24) · `ndim_dev3` 014202c (понесёт 21,
№10/№12/№15/№19/№20/№21/№22) · `ndim_integrator` 285c3b2 (понесёт 16, №11/№13/№17/№19/№23/№25).
Карта исполнения — `reports/TEAM/2026-08-29_merge_map_shift10.md` (ветка Интегратора).
✅ **СЛОВО ВЛАДЕЛЬЦА ПОЛУЧЕНО — №059 отвечен страницей 2026-08-29 14:11, все четыре:**
**В1 = А** — право мержа Интегратору ОТКРЫТО (разнесено в манифест, окно ИСПОЛНЯЕТСЯ по
карте) · **В2 = В** («Как делают топы?») и **В3 = В** («может это полезные фичи? что дают?»)
— встречные вопросы, проработка с данными подана в **№062** (x-default: дословная строка
документации Google — «designed for language selector pages»; четыре возможности счётчика —
разбор пользы и цены по инварианту №002 В4) · **В4 = В** («могли бы скриншот приложить») —
упрёк принят, вопрос переподан С КАДРАМИ знака в №062; урок формы: визуальный вопрос без
картинки не задаётся (класс памяти «экран записывает сам агент»).

**ОБЯЗАТЕЛЬСТВА ОКНА (из вердиктов, исполнителю окна читать вместе с картой):**
1. При мерже порции А — снять строку verify-role-env из состава ворот; отсрочка в переписи
   dev-3 обязана ПОКРАСНЕТЬ и быть снятой тем же движением (бесплатная проверка её честности).
2. Первый прогон ворот после мержа порции А закрывает ДВА стража (verify-role-env-scope +
   console-snapshot --check-age), не три (поправка QA к №13).
3. После мержей вписать в guards стражей правила «ноль красных»: спящие объявления +
   состояние bugs/ (станет 9) и verify-import-safety.
4. 11 записей EXP-NEW нумеровать ПОДРЯД по порядку мержей; предзанятому EXP-0228 не доверять.
5. **Головы ВСЕХ ролей КУМУЛЯТИВНЫ** («смержить последнюю заявку роли» = смержить ВСЮ её
   ветку: dev-3 21 коммит · dev-1 27 · Интегратор 16). 🔊 Просьба QA: прочитать этот пункт
   ВСЛУХ перед первым мержем — единственное место, где уход от карты стоит дороже всего.
   Для досуда в моменте QA держит наготове помощник стенда и формулу охвата — присылать
   голову и «дерево чисто», начнёт сразу.
После окна: посев «Вот это драма!» (из main) · перезакрепление доли меты (Дизайнер) ·
пост-оконные корзины dev-3 (4 пункта) и dev-1 (охват) · порция двери `plans/79` (Менеджер) ·
отказ close-bug без секции критериев (Интегратор) · построчный суд мастерской — отдельной
сменой (№8).

### 🔥 ПОЖАР MAIN — ПОТУШЕН ЗА ОДИН ТАКТ (2026-08-29, день)

Страж «строка пережила своё исполнение» покраснел на `plans/79`: Менеджер завёл план ОДНИМ
коммитом с правкой STATUS.md, а сводка в корне не входила в «планирование» — запись в сводку
о плане считалась исполнением плана. Нашла QA (эскалация по манифесту: красный main — прежде
любой работы), с честным «мой первый замер был ложным» — плоский слепок без подтянутой
истории дал ЛОЖНОЗЕЛЁНЫЙ на страже, спрашивающем git (четвёртый класс её стенда за смену,
самый ядовитый: перевернул ответ, а не зашумел). Настоящая причина оказалась третьей — не
«коммит создания» (он отсекается условием (2) и так): класс «СВОДКА — учёт работы, не
работа». Правка закона, не исключения (`a9c9b25`): корневые .md приравнены к планированию,
предикат `исполнение()` вынесен и покрыт 8 случаями; живая мутация — красный до правки на
том же дереве, зелёный после; ворота 7/7.

### 🟢 ХОД СМЕНЫ 10 (2026-08-29, полдень) — команда поднята, владельцу поданы три страницы

**Роли в строю все пять** (свежие окна), инструктажи по манифесту; ветки НЕ сброшены — во
всех несмерженная работа (впереди main на 5–21 коммит), дельта канона названа поимённо.

· **Владельцу:** №059 ОТКРЫТ страницей с сигналом (мержи всей смены стоят на его В1) ·
  **№060 «Главная с содержанием: выбор макета из четырёх»** и **№061 «Пачка текстов лица
  продукта»** — в очереди вычитки (артефакты макетов — 26 файлов со sha256-сверкой — в
  `test-results/owner-reviews/root-minilanding/`). Макеты поданы БЕЗ вердикта QA осознанно:
  выбор варианта — не мерж; суд ветки Дизайнера остаётся перед влитием.
· **QA — ВЕРДИКТ №14: порция dev-1 (PostHog Ш1–Ш4 + Ш2, судилась на 90cf7c1) ПРИНЯТА с
  замечаниями, мерж отперт.** 🔴 Находка, державшая ВЫКАТ: SDK счётчика въезжает в сборку
  двери карточки (+261 КБ) в момент ОЦЕНКИ — граница «каталог не получает трекер» опровергнута
  собственной сборкой в трёх местах; на ПОКАЗЕ не грузится (script-тегов 0), утечки предмета
  оценки нет (событие без свойств). **Решение Менеджера: границу привести К ПРАВДЕ** («на
  показе не получает; оценка с двери считается») — врёт текст, не код; правит dev-1 вставкой,
  тексты Политики/витрины поедут владельцу с Ш8 уже честными. Пять дефектов вдогонку — беклог
  dev-1 (главный: `capture()` не покрыта юнитом — три мутации в теле зелёные). Переходящее
  обязательство: первый прогон guards после мержа — на дереве с qa-journeys-lint И стражами
  порции А. **ВЕРДИКТ №17: порция стражей Интегратора (a503f85) ПРИНЯТА, ДЕФЕКТОВ НОЛЬ** —
  слепое пятно П1 замерено НЕЗАВИСИМО двумя инструментами (QA и автор) с полным совпадением:
  44 ключа, 2 мертвеца, оба в gateCopyPhase4; слово владельца после вычистки цело сплошной
  сверкой (из account удалён мёртвый ДУБЛЬ, живые строки в DeleteAccount.svelte); довод за
  вычистку усилен: мёртвая копия звала вкладку, которой нет, — текст был не спящим, а
  УСТАРЕВШИМ. З1 Менеджеру: вычистка унесла два комментария-УРОКА (намерение №007 В10 +
  урок «пустота молчит») — лечение микро-порцией Интегратора (опт-аут-маркер с причиной по
  образцу LIVE_BY_NAME + переселение уроков в живые дома). 🆕 Правило в манифест (практика
  Интегратора, поддержана QA): заявка на мерж — с ЧИСТЫМ деревом и головой, названной
  автором; в шапке вердиктов теперь печатается голова main на момент вынесения.
  **ВЕРДИКТ №15: критерий 2 bugs/209 ПРИНЯТ, дефектов нет, условий ноль** — довод
  «граница, а не равенство» подтверждён СИМУЛЯЦИЕЙ ПОСТ-МЕРЖА (подложены приборы dev-1:
  корпус 15 → 16, граница смолчала; равенство покраснело бы на следующей же порции очереди);
  паритет перемерен на текущем main 15/15. Хвост №15 (не держит мерж): слово «переименование»
  в 4 местах шире факта — точная форма улики правится dev-3 датированной поправкой до мержа.
  🔑 Процессная находка QA (третье попадание EXP-0226 за смену, теперь в судью): канон
  ДВИЖЕТСЯ под сессией, а не только увядает в ней — норма перечитывается перед ВЫНЕСЕНИЕМ,
  не перед началом; оформляется её уроком. Очередь судов дальше: предохранитель доски dev-1.
· **dev-3 — критерий 2 `bugs/209` ЗАКРЫТ и документ закрыт целиком** (голова 7aa5972 поверх
  нетронутой порции А): поимённый список стража метки закрывал 3 прибора из 15 — заменён
  объявленной нижней границей корпуса (CORPUS_FLOOR = 15, довод «граница против равенства»
  в шапке константы), 4 живые мутации красные, самотест 16 → 20, граница замерена на обоих
  деревьях (15/15 поимённо). ⚠️ Закрывал инструментом close-bug ИЗ ВЕТКИ ИНТЕГРАТОРА по
  абсолютному пути — в main и остальных деревьях инструмента НЕТ (ещё один случай EXP-0227
  и довод за приоритет его мержа); чекбоксы отмечены рукой, порция «проверка чекбоксов» —
  у Интегратора. Обновление его ветки до main — ПОСЛЕ мержа порции А (слово Менеджера).
  Хвост №15 закрыт (`707d4c0`: точная форма улики «переименование» — датированной поправкой
  в закрытом bugs/209, история не переписана). **Пересверка партий по основанию bugs/183
  (заказ Менеджера, только замер): настоящих новых заимствований НОЛЬ** — 247 половин, порог
  10 слов; партии 1–4 чисты (3/100 сверялись не с тем, находок 0) → №061 В5 нового материала
  НЕ получает; единственное совпадение (Reanimal, 14 слов) разобрано до не-находки — перечень
  трёх названий игр, имена собственные. Побочная: третья форма класса «служебные слова из
  английских названий» (looksLikeList) — заводится багом БЕЗ порции (1 случай на 247, правило
  требует своего замера). Риск сидел в свежих партиях 05–06 (26 из 34 неверных сверок). **Заявка №2 — перепись приборов по маркеру —
  СДАНА** (`5c5b5ed`, в очереди судов; подробности в §3 эстафеты выше); попутно EXP-0229
  (мутационный помощник откатывал КАТАЛОГ вместо файла — отравленные доказательства пойманы
  по числу до сдачи). Следующее у него: `bugs/181` + `bugs/183` (ворота мастерской — жадная
  регулярка кавычек · сверка с чужой статьёй), с замером ширины до фикса.
· **Дизайнер:** артефакты скопированы · съёмщик ЗАКРЫТ (`604bdba`: третья ширина штатно,
  условно по наличию `#width-mid`; попутно вылечен молчаливый класс «клик без проверки
  результата», мутация доказана) · порция «девять карточек» СНЯТА его же перемером — все
  девять в запретной партии 1, вопрос владельцу в №061 В5 (подробности строкой выше в §3).
  Стоит свободен, правки макетов — вперёд всего.
· **dev-1 — ВЕРДИКТ №16: предохранитель доски + stampNow ПРИНЯТ, условий ноль** (фабрика
  ложнозелёного воспроизведена судьёй дословно: assert.equal(1,2) → pass на старом приборе;
  м2 ловится только его юнитом; форм пути прогнано восемь — все живы). Один дефект в его
  беклог: смещение пояса в юните закреплено только ФОРМОЙ ([+-]\d{2}:\d{2} — «знак всегда +»
  зелёный); гипотеза «виноват материал» опровергнута тремя поясами. Долг класса числом: форму
  «CLI на верхнем уровне» несут 14 файлов tools/, предохранителя нет у 8, вылечен один —
  покрыто его постановкой ideas/43.
· **dev-1 — ideas/43 СДАНА ЦЕЛИКОМ** (голова 6a778f1, запушена, в очереди судов третьей):
  страж `verify-import-safety` (282 осмотрено · 60 с экспортом · красных 14 → 4) + 10 лечений
  мелкими коммитами + обе вставки доводов к правде (граница трекера №14 + `bugs/214`).
  Его прежний греп ошибся в 7 из 13 — поимённый разбор в заявке (шаблонные строки генераторов ·
  «предохранитель есть, работа снаружи»). 🔴 Находка крупнее прочих: `verify-stage-clean` при
  ИМПОРТЕ поднимал соединения к боевой и стейдж-базам — вылечен, живой прогон Менеджера 14/0.
  Решения Менеджера по 4 оставшимся красным: deploy.mjs — отсрочка до ОТДЕЛЬНОЙ порции двери
  (selftest + gate-only + суд) · contours.mjs и sync-server/index.mjs — законные исключения
  (несущий инвариант на импорте · входная точка контейнера) · verify-role-env — отсрочка до
  мержа. EXP-0188 переведён в mechanized. ⚠️ **ideas/43 реализована ДО одобрения владельца** —
  расхождение с каноном идей названо честно в самом документе и доложено владельцу в чате;
  его «нет» откатывает порцию до мержа дёшево. Старый довод __data.json дочищается в
  researches/55 (датированной поправкой) и сноской в отчёте аудита — микро-задача dev-1.
· **ВЕРДИКТ №18: ideas/43 dev-1 ПРИНЯТА, дефектов ноль** — лечения подтверждены всеми
  десятью CLI и семью безопасными импортами. 🔴 Суд вскрыл ВТОРОЕ ИЗМЕРЕНИЕ класса EXP-0188:
  verify-stage-clean до лечения не «поднимал соединения», а ЧИТАЛ БОЕВУЮ БАЗУ на верхнем
  уровне каждым импортом — класс «непреднамеренная дверь к боевым данным владельца из
  инструментария любой роли»; дописка в журнал + замер цены (сеть/ключи против просто
  работы) — в порцию dev-1. Живой обход формы исключения ({причина: 'так надо'} глушит
  красного) — признак содержательности причины в порцию dev-1 с замеренной ценой. Правило
  канона маркеров ([TESTED] называет, ЧЕЙ прогон и из какого контура) — оформляет QA своей
  веткой. 🔴 Порция ПРЕДОХРАНИТЕЛЯ ДВЕРИ ВЫКАТА — за Менеджером, первой после окна мержей:
  доводом суда самая опасная дверь осталась единственным непочиненным носителем класса.
· **Разведка dev-3 «адрес вылеченного класса» — механизм ЕСТЬ, но направлен в обратную
  сторону:** код называет уроки (79 из 232, 286 упоминаний), уроки код — почти никогда
  (6 из 232), а канон шлёт грепать журнал — пустое направление. Пять живых случаев с ценами.
  Решение Менеджера: Ш0 (строка маршрутизации в AGENT_GUIDE: «журнал — „что за класс",
  код — „где лечение", шапка весит больше тела») + Ш1 (прибор where-cured: ранжированный
  греп с --branches для неслитых веток) — обе его веткой; обратной засыпки mechanized НЕТ.
  Баг третьей формы looksLikeList заведён (0786666). ✅ **Ш0+Ш1 СДАНЫ** (заявка №4, голова
  014202c): правило встало шагом 2а — ТАМ, ГДЕ ошибка совершается (его поправка к постановке,
  принята), с вшитым замером против снятия «как мнения»; у прибора честность ПЕРВОЙ строкой
  вывода — форма из ПОСТАНОВКИ Менеджера, исполненная точно (поправка авторства от dev-3:
  принцип «назван ≠ вылечен» его, место первой строки — постановки; форма себя оправдала,
  повторять); «нигде» — ответ с толкованием; проверен на тех же пяти случаях разведки
  (страж границы слова всплыл первым — тот, что утром не находился). Автономный пул dev-3 исчерпан:
  три заявки в судах (№2 5c5b5ed · №3 e7a7b83 · №4 014202c), порция А ждёт окна, завтра
  первым действием — 150 в Яндекс.
· **ВЕРДИКТ №19 — ДОСУД девяти непокрытых предков: ПРИНЯТО, дефектов нет.** Эскалация
  процесса целила в форму СУДА: вердикт не различал «что я прочитала» и «что поедет» —
  лечение обеими половинами: судья печатает в шапке ДВА числа (в предмете / понесёт мерж),
  заявка называет голову и число коммитов против НАЗВАННОЙ базы — второе внесено в манифест
  (п. 4 git-процесса). Досуд Интегратора шёл КАК КОД (два прибора ~700 строк — поправка к
  ожиданию Менеджера «в основном docs»); четыре урока dev-3 — по форме. 🔴 Новый класс от
  QA: чекбоксовый страж достаёт 20 документов из 83 — документ ВООБЩЕ БЕЗ секции критериев
  (63 из 83, нарушение канона) получает МЕНЬШЕ проверки, чем соблюдающий; лечение (отказ
  close-bug без секции) — named-порция Интегратора ПОСЛЕ окна. ⚠️ Ветка Интегратора за
  время досуда выросла на три непокрытых коммита (a503f85..f1d3db7) — он ОСТАНОВИЛСЯ САМ
  до окна (git-процесс п. 2 своей волей против своего темпа); диапазон 2 судится до его
  шага. Правило маркеров внесено в TESTING_FRAMEWORK (правило 8: чей прогон и контур —
  часть улики; чужой прогон не запрещён — обязан быть назван). К окну: 11 записей EXP-NEW
  по четырём веткам, слаги не сталкиваются; предзанятому EXP-0228 не доверять — Интегратор
  нумерует все подряд.
· **ВЕРДИКТ №20: перепись приборов по маркеру (5c5b5ed + поправка 707d4c0) ПРИНЯТА, дефектов
  нет** — первый вердикт с двумя числами в шапке (предмет 3 / мерж понесёт 17, покрытие
  предков названо); судья заявила ОТВОД в самом вердикте (форма переписи — её предложение)
  и компенсировала усиленными мутациями — образец судейской формы. Поправка dev-3 исполнена
  образцово: улика не переписана, датированная секция, независимый повтор замера на ДРУГОМ
  файле. 🔴 Замечание к окну — класс EXP-0226 про ОХВАТ: после мержей два прибора в одних
  воротах напечатают РАЗНЫЕ размеры «корпуса приборов» (перепись 210 по tools/ · страж
  импорта 282 по tools+sync-server) — оба верны, оба без названного охвата; лечение —
  **пост-оконные микро-фиксы dev-3 и dev-1** (охват в строку вывода + случай в самотест),
  сейчас не ставятся намеренно (не удлинять отсуженные хвосты).
  **ВЕРДИКТ №22: маршрутизация (014202c) ПРИНЯТА, дефектов нет.** ПОСТ-ОКОННАЯ КОРЗИНА
  dev-3 собрана вердиктами №20–№22: охват в строке вывода переписи · объявленная граница
  unverified в воротах приёмки (идиома CORPUS_FLOOR, не плоский отказ) · предупреждение
  сверхширокого запроса в where-cured (запрос «.» — 1198 строк кодом 0; лечение долей
  корпуса — называет опасность, а не длину) · вторая строка честности для --branches
  («найдено в неслитой ветке — в вашем дереве этого лечения ЕЩЁ НЕТ»). 🔑 Новый класс из
  №22: **заявка, ЗАНИЖАЮЩАЯ своё поведение, — тоже цена** (ложная точка удара постановщику,
  холостой заход судье) — авторы заявляют себя точно В ОБЕ стороны. Стенд судьи получил
  двухшаговый помощник с контролем — три класса ложного красного закрыты механизмом.
  Осталось судов: диапазон 2 Интегратора (a503f85..f1d3db7) → девять пунктов dev-1 (9d7ba2b).
· **dev-1 — ДЕВЯТЬ ПУНКТОВ ЗАКРЫТЫ** (голова 5090009, запушена, в очереди судов последней):
  шесть дефектов №14/№16 с ловящей мутацией у каждого (TZ — четырьмя поясами дочерними
  процессами; capture() — семь случаев; белый список с боевым доменом; escape сырых байтов)
  + три добавки №18 (признак причины — ширина совпала с замером судьи до строки; EXP-0188
  дописан вторым измерением и ПОПРАВЛЯЕТ автора: строки ЧИТАЛИ боевые коллекции, сверено
  git show; **замер цены 14 красных: 🔴 боевые данные/ключи 2 · 🟠 сеть 2 · 🟡 запись 3 ·
  ⚪ 7**). 🔑 **Правило раздачи, выведенное его честным выводом: ЦЕНУ СНИМАТЬ ДО РАЗДАЧИ** —
  Менеджер ставил порядок лечений до существования замера, и ⚪ чинился первым при двух 🔴
  в очереди. Его урок «признак мельче предмета» (три случая; главный — `TZ=… node` в Git
  Bash НЕ закрепляет пояс: проверка выглядела бы закреплённой и не проверяла бы ничего).
  🆕 **plans/79 заведён** — порция предохранителя двери выката (Менеджер, ПОСЛЕ окна; повод
  поднят замером: deploy.mjs — 🟠 fetch на верхнем уровне); номер в строку отсрочки
  подставит Интегратор при мерже. Кода dev-1 (голова заявки → 9d7ba2b, дописка уроков
  поверх): оба класса в журнале с Repro; формула урока — «контрольный случай, дающий ОДИН
  ответ при исправном и сломанном признаке, — не контроль, а украшение»; ворота пропуска
  каталожных юнитов — ОДИН артефакт dims-build.json (уточнил себя чтением кода, обе половины
  замерены 5 ↔ 0). ⚖️ Ограничение правила цены (его же): замер окупается, только когда
  население названо — на постановке из одной задачи не окупится.
· **ВЕРДИКТ №21: ворота приёмки (e7a7b83) ПРИНЯТЫ, дефектов нет.** 🔑 Строка смены, дословно
  от судьи: «документ бага — ИСТОЧНИК ТРЕБОВАНИЯ: лечение, записанное в нём, следующий агент
  читает как решённое — взять его на веру стоило бы втрое худших ворот, и никто бы не
  узнал». Замер dev-3 трёх стратегий ДО установки предотвратил ровно это. Замечание с
  лечением: «НЕ ПРОВЕРЕНО» ворота не задерживает — лазейка СЕГОДНЯ ПУСТА (замер судьи:
  129 карточек, без Q-ида НОЛЬ), но её ничто не держит; лечение идиомой автора (объявленная
  граница а-ля CORPUS_FLOOR, не плоский отказ — тот краснел бы ложно на законно новом) —
  **пост-оконная корзина dev-3** вместе с охватом из №20. Три класса ложного КРАСНОГО от
  стенда судьи за день (код возврата после конвейера · негодный мутант · неполный слепок) —
  все пойманы ДО вердиктов, в её чек-лист. Осталось судов: маршрутизация (взяла) →
  диапазон 2 Интегратора → девять пунктов dev-1.
· 🔑 **Гигиена журнала опыта — образец от QA:** обновляя ветку до main, нашла СВОЙ дубль
  урока (EXP-NEW о числе в чужом контуре ≈ EXP-0226), сняла его ДО влития и перенесла
  неповторённую половину абзацем в EXP-0226 с автором — ровно тот класс «второго нормативного
  ответа», за который сама держит условие к мержу порции А. Ветка QA обновлена: 0 позади
  main. В чек-лист судьи входит «голова main печатается в шапке вердикта».
· **Интегратор — дев-очередь СДАНА** (f6f84e5 + c69c616, в очереди судов): страж спящих
  объявлений — ширина 397 → 42 четырьмя снятыми ложными семействами (⚠️ граница: экспортированные
  карты текстов НЕ судятся — там намеренно неподключённый gateCopyPhase4 с текстами первого
  экрана), вычищено 42 спящих текста / 107 строк / 6 файлов приговором стража с независимыми
  вторыми доказательствами (в т.ч. блок dimsEmpty из bugs/211 — мутацией доказано) · прибор
  verify-copy-live (bugs/170): асимметричный приговор, живая дверь прогнана МЕНЕДЖЕРОМ —
  4 поля × разница 0, снимок 0 дн., витрина боя: людей 95 · измерений 5144 · оценок 4071 ·
  связей 2368. Слово Менеджера по трём красным П2 исполнено: два ключа bugs/78 дочищены
  ГЛАСНО (датированные поправки + шапка к правде имени), peekRelationsSummary удалён ПОСЛЕ
  археологии (единственный предок 8e91895, неподключённый близнец живого peekRelations;
  восстановление — одна команда). **Финальная голова порции a503f85, суд идёт.** Предпросмотр
  слитого дерева: оба стража 0 красных — «ветки лечат друг друга», три строки ворот войдут
  первым действием после мержа. 🆕 Замер попутно: документов с шапкой «ОТКРЫТ» при имени
  `_DONE_` — СЕМЬ (75, 76, 77, 79, 82, 86, 87 — июльская волна ideas/21, закрывали пачкой) —
  порция шапок за Интегратором после карты мержей. ✅ **КАРТА МЕРЖЕЙ ГОТОВА** —
  `reports/TEAM/2026-08-29_merge_map_shift10.md` (в его ветке): очередь смоделирована ЦЕЛИКОМ
  и ПО ПОРЯДКУ; 🔴 главная находка — **голова, названная вердиктом, тянет ВСЕХ предков,
  включая неотсуженные коммиты** (шаг 4: из 11 судили 2, пять не покрыты ничем) → досуд
  предков поставлен QA пятым пунктом, ни один шаг окна не исполняется до досуда его предков.
  Конфликты поимённо (EXPERIENCE.md на шести шагах — план решения один, аддитивный), эволюция
  guards 7 → 12 как ПРИЁМКА каждого шага, номера детерминированы (EXP-0228…0244, bugs/219–221,
  researches/61–63, резервы за ветками QA/Дизайнера). Решение Менеджера по возражению
  Интегратора: **verify-copy-live в ворота НЕ идёт** («страж, который нельзя погасить
  действием того, на кого он краснеет, учит обходить ворота») — прибор СМОТРОВОЙ, прогон
  Менеджера при открытии смены. ✅ **Семь шапок «ОТКРЫТ» при `_DONE_` приведены к правде**
  (замер после: 0 из 7) + страж bugs/ получил зеркальный П3 ТЕМ ЖЕ КОММИТОМ — осознанное
  расхождение с «не срочно» Менеджера по канону «фикс без стража — фикс в кредит», принято
  без отката. ✅ Микро-порция З1: опт-аут «СПЯЩЕЕ НАМЕРЕННО: причина» (маркер без довода
  краснеет) · урок «пустота молчит» восстановлен в живом доме (в наследнике его НЕ БЫЛО —
  память Менеджера проиграла грепу) · намерение acctSince закрыто ГЛАСНО в ideas/21 —
  «намерение агента, спорящее со словом владельца, обязано умереть гласно, а не пережить его
  в комментарии». Интегратор СТОИТ ПО КАРТЕ (голова 4b8171d), исполнит окно по слову владельца.
· 🆕 **Находка смены: блок `dimsEmpty` (profile:922–931) МЁРТВ** — объявлен и не используется
  нигде, живые тексты пустых списков в `dims/+page.svelte`. Строка «Здесь пока пусто.» из
  пачки текстов снята: вычистка кода без слова владельца (в №061 — констатацией); живой
  контрольный случай для стража спящих экспортов Интегратора. Дописано в `bugs/211`.

## 🔴🔴 РАМКА СМЕНЫ 9 — ПЛАНКА ТОПОВ, А НЕ ЗАПЛАТКА (слово владельца 2026-08-29, дословно)

> «*важно: мы думаем не "нужно вылечить яндекс, поэтому срочно на коленке соберём заглушку
> фикс". Мы думаем: "вот так делают топы, линкедин, тикток, инстаграм — мы у них учимся,
> понимаем КАКИЕ их решения приводят их к успеху — мы сделаем так же, но ещё лучше, возьмём
> лучшее от всех топов, но не берём их неудачные решения"*»

Сказано в ответ на постановку «мини-лендинг корня лечит инцидент Яндекса». **Это рамка НАД
всеми задачами смены, а не комментарий к одной.** Разнесено в канон: `AGENT_GUIDE.md` →
«КАК МЫ ДУМАЕМ О РЕШЕНИЯХ: ПЛАНКА ТОПОВ, А НЕ ЗАПЛАТКА» (шесть пунктов операционной формы).

**Что это меняет практически, для каждой роли:**
· **Симптом задаёт срочность, но не задаёт решение.** Яндекс — повод заняться корнем сейчас,
  а не разрешение построить корень «чтобы Яндекс отстал».
· **Заплатка запрещена как класс:** «пока заглушка», «временно, потом переделаем» — сигнал,
  что задача подменена. Временное на лице продукта живёт годами.
· **Порядок неизменен: разведка топов → 4 макета → код.** Разведка отвечает «какие их решения
  приводят их к успеху», а не «как у них выглядит», и ОБЯЗАНА нести раздел «худшие решения —
  не берём, с причиной поимённо». Образец — `researches/60`.
· **«Но ещё лучше» — требование к результату:** назови место, где мы лучше них, и довод.
· **Взвешивай топливом:** кусок гиганта держится на его трафике; наш вход органический
  (`researches/30`). Форма на чужом топливе не переносится.
· **Граница:** планка топов — планка РЕМЕСЛА. Механики удержания и подписки отвергнуты
  `GOAL.md` и остаются отвергнутыми. Учимся тому, КАК сделано, не тому, РАДИ ЧЕГО.

## 🔴🔴 ЗАПУСК СМЕНЫ 9 — с чего начинать НОВЫМ ОКНАМ (мягкая парковка 2026-08-28 ~19:40, слово владельца «перегружены, мягко кончай все чаты»)

Смена 8 отработана целиком за один вечер: 8 вердиктов QA (№1–№8 в `qa/team-verdicts.md`),
всё отсуженное влито в main с воротами (417+357 юнитов · check 517/0 · guards 8 · kaif),
ветки ролей несут только несуждённые порции (поимённо ниже). Инструктажи — по манифесту.
⚠️ Финальные отчёты ролей при закрытии смены 8 могли прийти ПОСЛЕ этой парковки — сверь
головы веток `git branch -v` с названными здесь.

**ОЧЕРЕДЬ СУДОВ QA (новая смена, по убыванию срочности):**
0. ✅ Экспресс dev-1 ЗАКРЫТ ещё этой сменой — **ВЕРДИКТ №9: принято, дефектов нет, ВЫКАТ
   ОТПЕРТ**; порция влита в main (EXP-0225 присвоен). Хвост №9 без спешки: живой раздел
   «разъезд разметки» — одной строкой в порции текстов гостя (суждение судьи).
1. **Порция А dev-3 — диапазон 826b9c5..d7f98ca** — суд НАЧАТ и не досужен (прочитаны шапка
   и вход сверки authMatches, прогонов НЕ было). **Начинать с мутации «--auth-owner "x"
   обязан быть отбит»**, дальше: bugs/215 третьим путём · Я-10 · стражи З1/З3 в воротах
   (8/8) · числа 416/416 · 357/357. Подачу переобхода НЕ блокирует (условия №7 выполнены).
2. 🔴 **Интегратор (бывший dev-2) ЖДЁТ ДОЛЬШЕ ВСЕХ: страж метки (5ddddcb) + диапазон
   f8815bc…73c6f51** (удаление мёртвого блока витрины + разведдоки ивентов 1.x и спящих
   текстов + микропорция `bugs/218`: фикс \\s + страж regexp-escaping через глоб ворот).
3. dev3-shift6-20260823: приводит к main АВТОР (dev-3) руками после порции Б (3 опасности
   поимённо в вердикте №8); построчный суд мастерской (20 файлов) — отдельной сменой.

**🔴 ВЫКАТ ОДНИМ СОСТАВОМ — ПЕРВОЕ ДЕЙСТВИЕ НОВОЙ СМЕНЫ МЕНЕДЖЕРА (вердикт №9 отпер, ВСЁ
уже в main):** дверь карточки + Ф1 воронки + `bugs/201` + фикс `bugs/212`. Правка витрины ИЗ СОСТАВА СНЯТА — блок «Ни одной
рекламы…» оказался мёртвым экспортом (никогда не рендерился) и удалён словом владельца
(№056). После выката: повторный замер Ш2 на стейдже (объект виден?) · чтение
door_click/guest_start через неделю · дата шва ряда в `bugs/204`. ⚠️ Переобход главной
Яндексом — теперь ПОСЛЕ выката мини-лендинга корня (№058), не этого состава.

**РЕШЕНИЯ ВЛАДЕЛЬЦА ЗА СМЕНУ 8 (все разнесены, дословные цитаты в местах):**
· 🔴 **Канон «ЛИЦО ПРОДУКТА: никаких обещаний и оправданий»** — `AGENT_GUIDE.md` перед
  «Словарём продукта», три цитаты; развилки «текст обещает vs работа» владельцу не носятся.
· **№056 В1** — блок витрины УДАЛЁН целиком (он и не рендерился); PostHog ставим; доку
  ивентов ведём по образцу 1.x («форма — не состав», ПДн 1.x не переносим).
· **№058 В1 = А** — корень = ГЛАВНАЯ С СОДЕРЖАНИЕМ (мини-лендинг по «супер-миксу»
  `researches/60`), одной ступенью; №055 СНЯТО (кандидат строки В2 жив — в брифе);
  №052-дверь переросла в это решение; тонкой двери не будет.
· **№053 В1/В2 = А** — тексты гостя классом (6 строк 3 файла), отдельной порцией после выката.
· **AUTH машинной подачи Яндекса** — «агент будет сам отправлять правки в яндекс», цитата и
  граница в `bugs/203` § Разрешение владельца.
· **Ключи все в `.env`:** Яндекс-токен (`YANDEX_WEBMASTER`, со второго захода — scopes) ·
  PostHog phc/id/регион + phx (`POSTHOG_NDIM_SPACE_PERSONAL_API_KEY` — имя владельца,
  НЕ переименовывать, решение с dev-2) · **MCP PostHog подключён user-scope** (роли получат
  при перезапуске окон). Bing — открыт (`homeworks/15`), можно в любой день.

**ПОТОКИ РОЛЕЙ СМЕНЫ 9:**
· **dev-3 ПЕРВЫМ ДЕЙСТВИЕМ:** машинная подача переобхода (прибор читает
  `reports/YANDEX_RECRAWL/recrawl-queue.txt`, 1201 адрес, тождество доказано sha256 — ⚠️ ЧИСЕЛ ДВА И ОБА ВЕРНЫ: git хранит файл с LF, рабочая копия при core.autocrlf=true несёт CRLF, поэтому sha256 по диску и sha256 содержимого git РАЗНЫЕ (нашёл dev-3, класс EXP-0047). Проверяющий, взявший sha256sum по диску, получит другое число и прочитает это как подмену очереди — доказательство обязано называть, ЧТО именно хеширует. Уточнение приедет с мержем ветки dev-3; сверка
  слова — из `bugs/203`) → строка в журнал `bugs/203` → три числа Менеджеру → порция Б
  (З1/З2/З3 №7 + П-03/П-15/Я-12 живым прогоном) → приведение dev3-shift6 руками.
· **dev-1:** находки экспресса (если будут) → после мержа и выката — замер Ш2 стейджа;
  сниппеты хабов (`researches/57`) — по «го» владельца.
· 🆕 **ИНТЕГРАТОР (бывший dev-2 — ПЕРЕКВАЛИФИЦИРОВАН словом владельца 2026-08-28 «ок,
  переквалифицируй его и всё везде переименуй»):** первый шаг новой смены — Менеджер
  закрывает старое окно и выполняет `git worktree move D:\work\ai_sandbox\ndim-team\ndim_dev2
  D:\work\ai_sandbox\ndim-team\ndim_integrator` (при переименовании каталог был заперт
  окном; ветка/инструменты/манифест/доска УЖЕ переименованы, наследие-опознание старого
  каталога вшито в stand-slot). Зона роли — в манифесте (секция «Интегратор»): мержи с
  номерами после вердикта QA и решения Менеджера · прогоны ворот · пуши веток · перезапуски
  рабочих мест · гигиена доски · подготовка выкат-состава (сам deploy — Менеджер). Его
  дев-очередь после судов: приборы verify-copy-live + страж спящих экспортов (воротная
  зона — остаётся ему); лестница PostHog Ш1–Ш4 ПЕРЕДАНА dev-1 (после выкат-хвостов; плечо
  А ключа: константа по прецеденту Firebase; MCP сжимает Ш7).
· **Дизайнер: 4 МАКЕТА МИНИ-ЛЕНДИНГА КОРНЯ — главный приоритет** (постановка в его чате
  ушла 19:3x; доноры поимённо в `researches/60` § СУПЕР-МИКС; строка — №055 В2; числа —
  форма V4; тексты-констатации без плашек; 2 темы × 3 ширины + интерактивный html).
· **Менеджер:** выкат-состав после вердикта → подача пачки текстов владельцу (bugs/211 +
  profile:931 + тексты гостя №053 + склонение «дней»; «Только объективная математика» НЕ
  трогать — текст владельца, EXP-0222) → №057 (инструмент развёрток) в очереди вычитки →
  решение по `bugs/214` (51,6 МБ data.json в выкате) → Bing после 31.08.

**ДОЛГИ МЕНЕДЖЕРА:** ✅ ретроспективы 7 и 8 — ОПЛАЧЕНО сменой 10 · ✅ стрижка бонсая —
ОПЛАЧЕНО сменой 10 (1623 → ~790 строк) · ⏳ очередь вычитки несёт 11 накопленных документов
(`review batch`) — гасится ответами владельца на №059/№060/№061 и разбором старой девятки.

> 🔴 **AUTH ВЛАДЕЛЬЦА НА БОЕВОЙ ВЫКАТ, 2026-08-29, дословно: «катите».** Дано в ответ на доклад
> «стейдж пройден (24/0 под сессией, 81/81 гостем, 11/0 noindex), состав: дверь карточки
> `plans/75` + Ф1 воронки + `bugs/201` + фикс `bugs/212`; жду одного слова на боевой выкат».
> По канону разрешение даётся НА ВЫКАТ ЦЕЛИКОМ, а не на отдельную правку внутри него
> (`AGENT_GUIDE.md` → «Формулировка „ждёт AUTH выката“»). Порядок окна: мерж Интегратора
> придержан → стейдж начисто → бой → смоук под сессией.

### ✅ ВЫКАТ В БОЙ СОСТОЯЛСЯ — 2026-08-29, сборка `b81u35`, коммит `f72459a`

**AUTH владельца дословно: «катите».** Состав: дверь карточки (`plans/75`) + Ф1 воронки
(`plans/74`) + `bugs/201` + фикс `bugs/212`. Разрыв конверсии, найденный аудитом 28.08
(`bugs/200` — органика приходит на карточку каталога и упирается в стену входа), закрыт в бою.

**Чем проверено, числами:** ворота smoke по собранному артефакту · один `__sveltekit_<хеш>` ·
замер попадания в контур (сосед не тронут) · переадресации 16/0 · **смоук ПОД СЕССИЕЙ 24/0**
(обе темы, 390 и 1440, консоль чиста, анонимные учётки убраны за собой) · **гостевой 81/81**.
Кадры: `test-results/prod-signed-in/` и `test-results/prod-b4/`.

🔴 **ПРАВИЛО, ВЫВЕДЕННОЕ ЭТИМ ОКНОМ И ОБЯЗАТЕЛЬНОЕ ВПРЕДЬ: сначала ВСЕ мержи, потом стейдж,
потом бой.** Расписка стейджа привязана к коммиту вызывающего дерева — каждый мерж делает её
негодной. Второй раз за одно окно платить не будем.

**Хвосты выката:** повторный замер Ш2 на стейдже (объект виден?) — dev-1 · чтение
`door_click`/`guest_start` через неделю · дата шва ряда в `bugs/204`.

### ✅ ЧЕТЫРЕ РАЗВИЛКИ, СТОЯВШИЕ ЗДЕСЬ, — ОТВЕЧЕНЫ (интервью №059, страницей 2026-08-29 14:11)

Раздел был очередью к владельцу; очередь закрыта его ответами и оставлена ЗАПИСЬЮ О СУДЬБЕ
каждого пункта — три ответа из четырёх встречные, и без этой записи следующая смена прочла бы
«не отвечено» и спросила бы второй раз. Разнос по документам-получателям сделан смены 11.

1. ✅ **Право сессии Интегратора на `git merge` в main — ОТКРЫТО** (интервью №059, В1 = А).
   Держало две отсуженные порции (А dev-3, вердикт №10 · Интегратор, вердикт №13) всю смену 9;
   окно мержей смены 10 по нему исполнено (карта — `reports/TEAM/2026-08-29_merge_map_shift10.md`).
   Разнесено: `NDIM_WORKTREE_DEV_TEAM_MANIFEST.md` (git-процесс).
2. 🔄 **`x-default` для самого корня** — канон №010 Р5 говорит «на английский», но двуязычная
   главная сама сильный кандидат. **Интервью №059, В2 = В** — встречный вопрос «*Как делают
   топы?*». Прежнее решение №010 Р5 ДЕЙСТВУЕТ до ответа; разбор с дословной строкой
   документации Google подан **№062 В1**. Разнесено: `plans/39` («Что уже сделано»).
3. 🔄 **Знак в тёмной теме БЕЗ подложки вовсе** — 🔄 факт поправлен Дизайнером при съёмке
   кадров (смена 10, EXP-0226 по собственному следу): цвет плитки и фона СОВПАДАЮТ до байта
   (`#060b14` оба; «три единицы» были сравнением с фоном МАКЕТОВ — чужой контур), в светлой
   теме плитка видна → у знака две разные формы по темам, решённые совпадением чисел.
   **Интервью №059, В4 = В** — «*могли бы к вопросу и скриншот приложить*»; упрёк принят,
   вопрос переподан С КАДРАМИ (**№062 В3**: сейчас + «Кант» · «Панель» · «Скос» · «Ореол»).
   🔑 Урок формы, действующий на все роли: **визуальный вопрос без картинки не задаётся.**
   Дом стиля (тёмная тема) до ответа №062 НЕ трогаем.
4. 🔄 **PostHog: на стороне ПРОЕКТА включены `session_recording` · `capture_console_log` ·
   `heatmaps` · `capture_dead_clicks`** (нашёл dev-1 при Ш1–Ш4). Наш клиентский конфиг их глушит
   и это покрыто юнитом, но инвариант №002 В4 («предмет оценки третьей стороне не уходит»)
   стоит тогда на ОДНОЙ ноге. **Замер Менеджера, снятый до эскалации: записей сессий в проекте
   НЕТ НИ ОДНОЙ** — сегодня инвариант держится и фактом, не только конфигом. `anonymize_ips`
   уже `true`. **Интервью №059, В3 = В** — встречный вопрос «*может это полезные и ценные фичи?
   Что оно нам полезного может дать?*»: рекомендация «выключить четыре» НЕ утверждена, четыре
   настройки остаются включёнными, разбор пользы и цены подан **№062 В2**. Настройки аккаунта
   владельца агент сам не менял и не меняет — это его боевой ресурс.
   ⚠️ Срочность вырастет: бой выкачен, трафик пошёл. Разнесено: `plans/78` (секция ответа).

### 🔴 ШЕСТЬ ОТКРЫТЫХ БАГОВ-СИРОТ — усыновлены этой строкой (2026-08-29)

Замер Интегратора: из 97 открытых документов `bugs/` **шесть не имели ни одной входящей ссылки
из живого документа** (ссылка из `_DONE_` не считается — закрытый документ это история, а не
указатель). Сирота невидим для беклога и приходит к следующей смене как «новая находка», то есть
оплачивается второй раз — сегодня мы это уже заплатили на `bugs/171`. Пока класс не
механизирован, указатель живёт здесь:

· **`bugs/168`** — `team-workplaces.mjs reset` молча уничтожает несмерженную работу роли.
  🔴 Прямо касается командного режима: Менеджер зовёт `reset` при запуске каждой смены.
· **`bugs/170`** — витрина лендинга может молча разойтись с базой. Касается чисел на лице
  продукта, то есть того, что видит владелец и посторонний.
· **`bugs/181`** — жадная регулярка кавычек извиняла 19 живых заимствований (ворота мастерской).
· **`bugs/183`** — ворота приёмки описаний сверяют текст с ЧУЖОЙ статьёй (ворота мастерской).
· **`bugs/187`** — загрузочный щит не поднимается на языковом лендинге. 🔴 Касается корня и
  лендингов, то есть зоны мини-лендинга `researches/60` — читать ДО работы над корнем.
· **`bugs/191`** — самотест доски команды не мог упасть (шаблонная строка съела признак).
  Родня `bugs/218` и `EXP-0227`.

Классовое лечение — за Интегратором: `tools/verify-bug-doc-state.mjs` (готов, в ворота встанет
после уборки). Эта строка — разовая часть, снимающая сиротство сегодня.

### 🟢 ХОД СМЕНЫ 9 (2026-08-29, утро) — команда поднята, стейдж пройден

**Роли в строю все пять**, инструктажи разосланы по манифесту (ветки Дизайнера, QA и dev-1
сброшены от свежего main ДО инструктажа; dev-3 и Интегратору дельта канона названа поимённо —
у них лежит несуждённая работа).

· **QA — ВЕРДИКТ №10:** порция А dev-3 (826b9c5..d7f98ca) ПРИНЯТА с замечаниями (3 дефекта,
  4 замечания, мержа не держит ни один); блокирующее условие вердикта №7 снято. 🔑 Круг
  `bugs/217` разомкнут ПО ПОСТРОЕНИЮ: раздел «Разрешение владельца» ввёл коммит `9233191` в
  ПЕРВОЙ ЛИНИИ main — исполнитель физически не мог выписать разрешение себе.
· **dev-3 — первая машинная подача переобхода Яндекса состоялась:** отправлено 150 · отбито 0 ·
  остаток очереди **1051**. Все 150 ответов — код 202, молчаливых приёмов нет (класс `bugs/215`
  закрыт наблюдением, а не верой). Следующие 150 — завтра, очереди ~7 дней.
· **Дизайнер — 4 макета мини-лендинга корня готовы** (`design/root-minilanding-mockups.html`:
  V1 «Ступень» · V2 «Разворот» · V3 «Две речи» · V4 «Витрина») + записка «где мы лучше»
  (`design/root-minilanding-better-note.md`). Шестой кусок супер-микса — полоса живого
  каталога из `src/lib/content/dims-slice.json` — ВОШЁЛ В СДАЧУ (поправка Дизайнера смены 10:
  коммит `0704884` его ветки; прежняя строка «достраивается» пережила своё исполнение —
  класс EXP-0227). ⚠️ Числа среза от 2026-08-03 — к выкату пересматриваются.
· **СТЕЙДЖ ПРОЙДЕН** на `6fc8646`: смоук под сессией 24/0 (обе темы, 390 и 1440), гостевой
  81/81, стейдж невидим роботам 11/0. Кадры — `test-results/prod-signed-in-stage/` и
  `test-results/prod-b4-stage/`. ⚠️ Расписка того прогона НЕГОДНА для боя (дерево было грязным —
  промах Менеджера, писал стража во время прогона); перед боем стейдж гоняется заново.
· **Оплачен долг `EXP-0216`** — `tools/verify-role-env-scope.mjs` в воротах: копия роли не
  держит ключ Менеджера. Первый живой прогон нашёл настоящее: один секрет Яндекса жил под
  ДВУМЯ именами (`YANDEX_WEBMASTER` в главной копии против `NDIM_YANDEX_WEBMASTER_TOKEN`,
  который читает код) — прибор Яндекса из главной копии падал с «нет токена», имея секрет в
  соседней строке. Имя приведено к каноническому.

🔴 **ПОРЯДОК ОКНА ВЫКАТА, выведенный сегодня и обязательный впредь: сначала ВСЕ мержи, потом
стейдж, потом бой.** Расписка стейджа привязана к коммиту — каждый мерж делает её негодной.

· 🔴 **`bugs/171` (замок стейджа против доски) ПОДТВЕРЖДЁН ВТОРЫМ НАБЛЮДЕНИЕМ** — Интегратор,
  2026-08-29. Класс шире, чем был записан: замер в ОБЫЧНОМ режиме (главная копия грязна одним
  файлом — доской; 4 записи за три минуты запуска смены; 43 коммита трогали доску, 34 за семь
  дней) показал, что окно «дерево чисто» редко ВСЕГДА, пока смена жива, а не только на парковке.
  Дубля не заведено — усилен существующий документ (цена давления в сторону `--auth-owner`
  с честной границей: `.kaif/stage-bypass.log` пуст в обеих копиях, обход не применялся ни разу;
  частичное знание двери `deploy.mjs:281-292`; третья дверь лечения — гипотеза Менеджера, с ценой).
  🔴 **Находка дороже самого бага: ссылок на `bugs/171` в main НЕТ НИ ОДНОЙ** — документ пролежал
  открытым семь дней невидимым, поэтому класс пришёл как «новый». Эта строка его сиротство и
  снимает; классовое лечение (страж «открытый баг без единой входящей ссылки») — за Интегратором,
  с обязательным замером ширины ДО объявления признака.

### 🟠 СЕССИЯ 42 (2026-08-28, вечер) — СМЕНА 7 КОМАНДЫ + ИНЦИДЕНТ ЯНДЕКСА (ЗАКРЫТА парковкой, итоги ниже)

**ИНЦИДЕНТ ЯНДЕКСА — `bugs/203` (заказ владельца в чате, дословно в баге).** Вебмастер:
«Страницы в поиске — 3 (−100%)», 1352 страницы удалены как «малоценные». Причина найдена и
подтверждена формулировкой самого Яндекса: **корень `/` под `noindex`** (решение №010 Р5 —
корень-распознаватель, `plans/39`) → хостовый штраф; Google по sitemap растёт, Яндексу нужна
главная. Сделано под сессией владельца (CDP 9222, отдельный отладочный профиль):
диагностика прочитана · CSV исключённых снят (`test-results/ya-incident/`) · **переобход:
150/150 дневного лимита отправлено** (лог `recrawl-log.txt`), остаток очереди 1201
(`recrawl-rest.txt`) — слать по 150/день ЛИБО машинным каналом (разведка у dev-3).
✅ **ЛЕЧЕНИЕ ВЫБРАНО (интервью №052, В1 = А, страницей):** корень — видимая страница-дверь
RU/EN без noindex; лендинги/Google не трогаются; тексты двери — через вычитку владельца.
Разнесено: `bugs/203` + `plans/39`. Дизайнер рисует 4 макета (постановка дана). Повторная
отправка главной в переобход — после выката двери. Остаток очереди 1201 — по 150/день, либо
машинным каналом (прибор Яндекса у dev-3; OAuth-токен владельца — домашка готовится).

**СМЕНА 7 (все ветки от e48c50a, спасательные ветки смены 6 на origin):**
· **QA** — суд №1 (Дизайнер) ✅ ПРИНЯТО → мерж влит в main (`fdc90d5`, развязка C по классу 16
  — прибор фразами + приставка ПОЖЕЛАНИЕ; 386/386, 07: 16/0/0; EXP-0212 = урок Дизайнера,
  дубль 0206 снят). Сейчас: суд №2 — `dev2-shift6-20260823` (Н1–Н9 + bugs/194, предмет —
  сторона ветки; решающую проверку 194 снять живым прогоном). Дальше: суд dev-3 порциями
  (хвост смены 5 первым). Два NEW_-бага QA ждут номеров при мерже его ветки.
· **dev-1** — дверь карточки `plans/75` Ш1–Ш3 + `bugs/201`; контракт счётчика передан:
  `void track('door_click')` из чанка движка, в момент касания.
· **dev-2** — воронка Ф1: Ш0+Ш1 ПРИНЯТЫ ревью (ключ суток по Europe/Moscow — одобрено;
  правило affectedKeys — одобрено; `/admin/funnel` списком без 4 макетов — одобрено;
  замечание: страж verify-probe-mark в комментарии есть, файла нет — к сдаче обязан быть).
  Ш2–Ш5 в работе.
· **dev-3** — консоли Ф2: **первый снимок Search Console снят сегодняшней датой**
  (`reports/CONSOLES/2026-08-28_google_search_console.json`; факт: десктоп CTR 0,19 % против
  мобильного 3,62 % — ×19, кандидат в первую гипотезу). Ш2 (API Bing/Яндекс + машинный
  переобход) → Ш3 → Ш4. `.env` в его worktree вычистить до консольного ключа (моё указание).
· **Дизайнер** — 13 живых возвращённых (выгрузка `test-results/returned-prod-2026-08-28.txt`,
  два новых дословных замечания владельца: «вопрос картины» и «играют от третьего лица»);
  второй точечный файл группы Б (11 шт.) + Requiem в прошедшее время. Посев — мой.

**ПРОЦЕССЫ:** git-процесс формализован — `researches/53` (TBD/GitHub flow/DORA) + раздел
манифеста: роль обновляется до main и решает конфликты САМА, заявка/PR, мерж Менеджером после
QA, перезапуск ветки только после спасательной. Страж вопросов чинён (candidates+reports в
корпусе, ложные красные №048 сняты, №047 В2 доразнесён, два ЗАМОК-ОК): **провалов 0**.
Селфтест доски чинён (пустая альтернация — нашёл страж dev-3). Ретроспектива §6.3 поправлена
(мёртвые адреса судов → живой долг).
⏳ **Force-push origin-веток ролей ждёт права от владельца** (классификатор; строка
предложена в чате) — до тех пор роли работают локально, пуши отбиты non-fast-forward.

### ▶️ ЖИВОЕ ИЗ ВЫСТРИЖЕННОГО — сессии 38–41, смена 4/35, разделы эпохи 08-14…17 (стрижка смены 10, 2026-08-29)

Полные тексты — `PROJECT_HISTORY.md`, раздел «Стрижка смены 10». Здесь только то, без чего
следующий агент ошибётся:

· ✅ **`bugs/134` ВЫЛЕЧЕН 2026-08-22** (`134_DONE_`; поправка смены 11 — прежняя строка «диагноз
  не поставлен» пережила исполнение): виновник — вотчер `vite dev` на файлах сборки, лечение
  `vite.config.ts:111` (ignored: build/, .svelte-kit/output/). Живое подтверждение 2026-08-29:
  полная сборка (21 034 файла) при живом стенде — 853 МБ спустя 6,4 часа, стенд отвечает.
  **Релизное окно 2.2 стенд по этому классу НЕ блокирует**; обход `--max-old-space-size` —
  страховка, не условие.
· 🔴 **`probe-test-guest-live` ПИШЕТ В БОЕВУЮ БАЗУ** — читай его шапку до запуска. Класс:
  стендовый прогон может утечь в бой через данные, которые отдаёт продукт (личная ссылка пары
  строится из боевого canonical).
· **`bugs/127`** открыт — `users/{uid}/privat/{uid}`: 331 документ с живым `user_secret_key`,
  схема и правила о них не знают (держит только финальный deny-all).
· **`bugs/121`** открыт — 19 ПУБЛИЧНЫХ страниц недостижимы от языковых главных (семейство
  ТЕСТ, английские документы, обе `delete-account`); лечения правят лендинг — за владельцем.
  Остаток `plans/48`: шаги 5 (сегментация sitemap) и 7 (приёмка с AUTH).
· **`bugs/123`** открыт — довод про `100vw` у загрузочного щита живёт комментарием, страж
  стережёт только число; главный вопрос замера — жива ли полоса прокрутки при щите.
· **`bugs/182`** открыт — страница ТЕСТ обстригает карточку (близнец `bugs/172`; сливать их
  запрещено вердиктом QA; сверить состояние с ветками dev-1 прошлых смен).
· **Хвосты решений владельца по карте сайта:** `lastmod` не пишем вовсе · IndexNow отвергнут
  посылкой «страницы меняются раз в неделю», ночная сборка посылку сняла — разведку перемерить.
· **Идея, которую владелец не решал:** форма заявки обязана ИСКАТЬ за человека — три
  предложенных им объекта уже были в каталоге («забыл, что он есть»).
· **Режим усилий:** по умолчанию **max**; ultracode/workflows агент обязан ПОПРОСИТЬ сам,
  назвав пользу фан-аута. · **NDIM-AUTH-003/005** на живых контурах честно пропускаются
  (почтовые коды — только у эмулятора); не «чинить» подкладыванием сессии (`EXP-0087`).
· **«План починки — гипотеза»:** счёт замера против планов `plans/49` — 4:0; меряй прежде,
  чем поверишь. · **Реестр стражей** уехал в летопись датированным снимком (числа устарели);
  живой источник — `AGENT_GUIDE.md` → «Испытательный стенд» и `npm run guards`.

## 🔴🔴 РЕЛЬСЫ ПОСЛЕ АУДИТА — заказ владельца следующим сессиям (2026-08-28, дословно)

> «*доделывай аудит, обновляй мастер план, статус - перенаправляй рельсы твоих коллег ИИ
> агентов туда, куда требует твой аудит, по твоим находкам, чтобы следующие сессии эти эпики
> начинали планировать, эти операционные планы. и делали*»

Это слово СНИМАЕТ прежнюю границу «только документы» для СЛЕДУЮЩИХ сессий: очередь ниже —
планировать И ДЕЛАТЬ. Порядок утверждён его доверием (№049 В4), менять — только его словом.

1. 🔴 **ДВЕРЬ КАРТОЧКИ (`plans/75`) + `bugs/201` — одним выкатом.** Всё готово к коду:
   эталон Д3 утверждён (три захода правок), «Разведка исполнения» в плане обязательна к
   прочтению ДО первой строки (⛔ `csr = true` не включать — остров; движок —
   `saveTestRating`; кнопка ждёт запись; кэш чанка — класс `bugs/124`). Ш1 → Ш4 по плану,
   выкат: стейдж → кадры обеих тем → бой дверью → смоук под сессией.
2. **«ЗРЕНИЕ» Ф1 (`plans/74`) — тем же заходом или сразу следом:** +2 счётчика воронки
   (`door_click`, `signin_wall_view` — «открыл карточку» клиентом НЕ считаем, довод в плане)
   + метка своих прогонов (`ndim-probe`) + правила + тесты правил + экран воронки в Панели
   администратора (`ideas/06`). Без Ф1 дверь нечем принять (критерий 3 `plans/75`).
   ⚠️ Витрина лендинга «считаем четыре числа в день» (`landing-copy.ts:224`) устареет —
   правка текста ТОЛЬКО через вычитку владельца, тем же выкатом.
3. **«ЗРЕНИЕ» Ф2 — миссия консолей (`ideas/37`):** снимок Search Console в
   `reports/CONSOLES/` (по дням/страницам/запросам), Bing/Яндекс — разведка API, формат
   гипотез. Прибор доступа живой: `node tools/probe-search-console.mjs` (siteFullUser).
   Ряд копить с 2026-08-28 — каждый пропущенный день невосстановим (16 месяцев хранения).
4. **Обогащение карточек (`plans/56`)** — главная SEO-работа по лагу, план готов, не начат.
   Материал аудита: хабы `/en/catalog/movie/N` получают показы на позициях 2–10 с CTR 0 —
   сниппеты хабов проверить внутри шага 0.
5. **«ЗРЕНИЕ» Ф3–Ф4 — лестницы планирования:** обзор рынка аналитики («похожее на
   Амплитуду»; 🔑 критерий владельца — удобство ИИ-агенту: читающий API/экспорт) и
   мониторинга ошибок (класс Sentry) по первоисточникам → кандидаты владельцу на
   утверждение. ⚠️ Внедрение стороннего инструмента тянет правку «Политики»
   (`researches/06`) и строки витрины «ни одного стороннего трекера» — только через
   вычитку, тем же выкатом.
6. **Параллельно порциями (их ждёт ВЛАДЕЛЕЦ):** 12 возвращённых карточек
   (`node tools/read-returned-candidates.mjs --contour prod`) и партия 6 — 33 из 60.

⛔ **Инварианты исполнителям:** карточку каталога НЕ обеднять (слово владельца, `plans/75`) ·
тексты лица продукта — только через вычитку владельца · новое UI — 4 макета до кода · выкат
только `npm run deploy` через стейдж · оценки/похожесть наружу не отдаём (№002 В4 — действует
и при стороннем трекере: предмет оценки третьей стороне не уходит).

### 📜 СМЕНЫ 5 и 6 (2026-08-22/23) — ПЕРЕНЕСЕНЫ В ЛЕТОПИСЬ ДОСЛОВНО (стрижка сессии 38)

Обе закрыты владельцем, их разборы полностью в `PROJECT_HISTORY.md`. Живое из них поднято в
сводку сессии 38 выше: мерило партий (`candidates/owner-critical-notes-2026-08-22.md`), имя
«Менеджер измерений» и его два стража, заказ шестидесяти карточек.

## 🆕🆕 КОМАНДА NDIM_WORKTREE_DEV_TEAM — прочитай, если этот чат зовут Менеджером

Владелец решил (слово в чате 2026-08-21): запуск команды — В НОВОМ ЧАТЕ. Если владелец назвал
этот чат Менеджером (`/rename ndim_manager`) и велел запускать команду:

1. Прочитай **`NDIM_WORKTREE_DEV_TEAM_MANIFEST.md`** (роли, регламент общения, git-режим) и
   **`homeworks/14`** (сценарий запуска — владелец открывает 5 окон ролей по ней).
2. Твоя роль — **Менеджер**: `main`, статус-доска `NDIM_WORKTREE_DEV_TEAM_STATUS.md`
   (`node tools/team-status.mjs set --busy --doing "…"`), рабочие места ролей —
   `node tools/team-workplaces.mjs list` (5 worktree в `ndim-team/`, каталоги и ветки
   `ndim_*`). 🔄 Правки владельца на первом запуске: ролевой ИНСТРУКТАЖ высылает Менеджер
   (владелец промпты не диктует) + каталоги с припиской `ndim_` (переезд —
   `team-workplaces.mjs relocate`, только при закрытых окнах ролей). Подробно — `plans/66` фаза 4.
3. Дождись докладов ролей сообщениями, затем раздавай задачи ПО ФОРМЕ манифеста (правило 2:
   …когда и КУДА отчитаться — адрес в постановке!). Мержишь только ты, после вердикта QA;
   после мержа — `node tools/team-workplaces.mjs reset <роль>`.
4. Репетиция уже прожита: протокол — `qa/team-rehearsal.md`; дефект «отчёт ушёл чужому
   проекту» вылечен правилами 2 и 9 манифеста. ⚠️ Терминальный `claude` в PATH — 2.1.232, без
   связи сессий на Windows; окна — только через расширение VS Code (2.1.238).

## ▶️ ЖИВОЕ ИЗ ПЕРЕНЕСЁННОГО — эстафеты сессий 32–35 ушли в летопись (стрижка сессии 36)

Полные тексты — `PROJECT_HISTORY.md`, раздел «Сессии 33–35». Здесь остаётся только то, без чего
следующий агент ошибётся:

🔵🔵 **ОЧЕРЕДЬ РАБОТ ВЛАДЕЛЬЦА (№041 В3 = А), она же порядок:** мост с лендинга внутрь ✅ закрыт и
смержен 2026-08-22 → **остаток SEO-фазы каталога (`plans/56` шаги 3, 5, 6)** → эпик 60 (шаги 3–7
фазы 2 ждут за воротами).

🐞 **`bugs/158`** (слово владельца: карточка не уезжает после отсчёта) — открыт, локализован его
ответом (№042 В6 = А: **бой, вкладка «Все»**). Стенд жест держит; бой держит другую версию файла.
Дальше по плану бага: осторожная проба боя органами продукта ЛИБО повторная проба после выката.
⚠️ Починка `bugs/172` его НЕ закрывает — документ прямо запрещает их сливать (вердикт QA).

🔴 **Развилка эпика 60, ждёт решения: ГДЕ ЖИВЁТ СОСТОЯНИЕ ЭСКОРТА** (`plans/62` шаг 1, второй
пункт) — серверная коллекция против полей на точке; критерий «клиент не дотянется» и цена в
операциях. Первый пункт шага закрыт, дорога свободна.

🔬 **`bugs/162`** — закрыт (`162_DONE_`; поправка смены 11: строка «Открыт» пережила исполнение).

⚠️ **Хвост окружения** (мелочь, но копится): удалить `D:\work\ai_sandbox\_locked_rolldown_binding.trash`
и пустой `judge-wt` в Temp; пустой каталог `.claude/worktrees/candidates-batch2` можно убрать
(ветка влита `c6bd7bf`, `node_modules` цел).

🔑 **Замер парка стендов (не перемерять):** три стенда живут одновременно, минимум свободной
памяти за заход **5 198 МБ** при пороге остановки 500, подкачка не шевельнулась. ⚠️ Это ХОЛОДНЫЕ
стенды; прогретые не мерены — замер у dev-2 в фазе 2 парка.

## ЭПИК 60 — ритм сервера синхронизации (⏸ уступил рельсам аудита 2026-08-28; решения владельца №040 внутри — живые)

📍 Метаплан `plans/60_EPIC_sync_server_rhythm.md` · разведка `researches/43` · фаза 1 `plans/61`
📍 Поводы: `bugs/152` (2880 холостых операций) и `bugs/146` (индекс каталога голодает) — лечатся
вместе, одним тактом.

**Что замерено и не подлежит пересмотру** (числа сняты с кода и боевого контейнера):

| Факт | Значение |
|---|---|
| Такт боевого цикла | **60 с** (`docker inspect` + метки лога, ровно 60 с) |
| Холостой цикл стоит | **1 чтение + 1 запись** (`index.mjs:1035` и `reportServer`) |
| В сутки при нуле событий | **1440 чтений (2,9 %) + 1440 записей (7,2 % нормы)** |
| Людей в бою | 342 точки · **246 без единой оценки** · жителей 94 |

🔑 **Канон 1.x НАЙДЕН В КОДЕ** — `ndim-old/public/scripts/app.js:6250`. Сердцебиения в 1.x **не
было вовсе**: лампочка ВЫВОДИЛАСЬ из отметок настоящей работы сверкой с расписанием, состояний было
**ТРИ** (`Работает` · `Пропустил частичную синхронизацию` · `Не в сети`). Наши `fullSync`/`partialSync`
— прямые наследники и уже пишутся; минутный `lastRunAt` был надстройкой сверху.
🔑 **Отрасль на нашей стороне:** Firebase сам называет опрос анти-паттерном («*one-shot queries
re-read data that may not have changed*») и берёт деньги «*за возвращённые документы, а не за
открытое соединение*». Ловушка серверного SDK: офлайн-персистентности нет, **каждый обрыв подписки
= новый запрос** → выборка подписки обязана быть УЗКОЙ.

### 🔵 РЕШЕНИЯ ВЛАДЕЛЬЦА ПО ЭПИКУ — исполнять дословно (интервью №040)

**Эскорт новичка — ТРИ пересчёта, механику он задал сам:**

| Пересчёт | Когда | Обрыв эскорта |
|---|---|---|
| 1-й | ближайшая минутная пульсация после ПЕРВЫХ оценок | — |
| 2-й | изменил NDim ID в течение **30 мин** после 1-го → расчёт **через 10 мин** от изменения | 30 мин тишины → в общую очередь |
| 3-й | те же правила, окно от **2-го исполненного** пересчёта | 30 мин тишины → в общую очередь |
| — | — | **после трёх — всегда в общую очередь** |

⛔ **Минутная пульсация ЖИВЁТ НА ПОДПИСКАХ**: «*есть кого считать — считает, нет никого в эту
минуту — не делает холостых чтений и записей*». Два ритма, не путать: минутная пульсация — только
новички; часовой такт — все остальные + пульс + индекс каталога; раз в сутки — полный проход.

**Лампочка:** отчёт в БД раз в час, опоздание на такт = красный СРАЗУ (допуск **один такт**, как в
1.x). ⚠️ Сравнивать с ОЖИДАЕМЫМ временем следующего такта, а не «ровно 3600 с назад», иначе живой
сервер мигает красным каждый час.

**Старожил после эскорта ждёт до часа** — принято им явно: «*в порядке общего процесса
синхронизаций — в частичных раз в час, и в полных раз в сутки, как было в старом NDIM*».

### 🔴 Порядок фаз — и довод, который важнее порядка

**Сначала строим быстрый путь, ПОТОМ убираем дорогой.** Обратный порядок оставил бы окно, где
новичок ждёт связи час, — то есть эпик про экономию временно сломал бы то самое требование, ради
которого затевался. Фазы: 0 разведка ✅ · **1 прибор** · 2 эскорт · 3 часовой такт (+`bugs/146`) ·
4 лампочка · 5 выкат и суточное наблюдение.

## ✅ ПЯТЬ ПРАВОК ВИДА — В БОЮ И ПРИНЯТЫ (строка исправлена стрижкой сессии 36)

Здесь стояло «🟡 ЖДЁТ ВЫКАТА — пять правок вида, все `[NOT-TESTED]`». **Обе половины строки были
ложью на момент чтения:** правки приняты живой приёмкой QA (`b3b90c2` — переведены в `[TESTED]`)
и уехали в бой двумя выкатами. Тени карточек связей · «мёртвая душа» (имя есть, английский его
прятал) · кружок без имени как фирменное N · курсор-палец вместо лупы · термин «обновлённые
NDim ID» вместо вульгарного. Разбор — летопись.

⛔ **Поле `dirty` в базе НЕ переименовано** намеренно: это миграция 342 документов, правил и
тестов. Названо в `plans/60` → «Что эпик НЕ делает».

## 🗓 `plans/58` шаг 1 — ПОЛОВИНА УЖЕ СДЕЛАНА (строка исправлена стрижкой сессии 36)

Разметка **всех 5121** записи тегами одобрения (интервью №039, В2 = A). Его слова: «*A —
размечаем всё. но не сегодня, планируем в четверг это сделать*». Четверг был **2026-08-21**,
и он прошёл.

🔴 **Здесь стояло «назначено на четверг, не начат» — и это уже неправда.** В `main` лежат два
смерженных коммита шага 1: `ca88607` (технический тег переживает правку измерения — иначе
разметка стиралась бы молча) и `0e972bb` (новорождённое измерение штампуется `owner-approved`,
несёт `[NOT-TESTED: правила]`). Шапка самого `plans/58` тоже отстала: «Статус: 🔲 написан, не
начат» при этих же двух коммитах.

⛔ **Что из запрета ОСТАЛОСЬ в силе:** массовый прогон по БОЮ (5121 запись) не запускался и
раньше слова владельца не запускается даже сухим прогоном. Сделано другое — обвязка, которая
обязана быть до массового прогона.
⏭ **Что делать дальше:** сверить чекбоксы плана с тем, что реально легло, поправить его шапку и
назвать оставшееся. Место в очереди не назначено — вопрос владельцу.

## `plans/59` — виджет «Сегодня»: развилка закрыта

Владелец выбрал **A + B** (затухание у кромки И видимая полоса). 🔴 Отсюда работа, которой в плане
не было: **полосу придётся рисовать свою** — на телефоне система прячет системную до момента
прокрутки. Проверять на 390 обязательно.

---

## 🟢 ЧТО ТЕПЕРЬ РАБОТАЕТ — контур пополнения каталога целиком

Агент находит новинку → пишет карточку по правилам мастерской → **двое ворот** не пускают её
дальше без обязательных тегов → владелец судит в комнате (одобрить · правка · **вернуть на
доработку** · отклонить) → одобренное рождается измерением → сервер синхронизации вносит в индекс →
сборка выкатывает публичную страницу. Обратное плечо тоже живое: агент читает возвращённое
прибором и кладёт исправленное обратно.

📍 Мастерская — `candidates/` (README несёт ПРАВИЛА стиля описаний и соглашение о тегах).

🔴 **ДВЕ ПРАВКИ ВЛАДЕЛЬЦА ПО ТЕКСТАМ КАРТОЧЕК, 2026-08-22** (дословно: «*там они текст очень
чопорный пишут, и много пишут про создание объекта культуры и мало про его само содержание,
сюжет — это важнее*»). Разные претензии, путать нельзя:
· **ПРОПОРЦИЯ, и это главное:** содержание — сюжет, о чём это, что происходит — идёт ВПЕРЁД и
  занимает основную часть; история создания (кто снял, кто написал, студия, год, награды) —
  короткой строкой или вовсе. **Сюжет важнее** — его расстановка приоритета, не наша.
· **ТОН:** «чопорный» — это церемонность и канцелярская дистанция. ⚠️ Это НЕ отмена «мини
  википедии» (его слово 17-го в силе): энциклопедия бывает живой. ⛔ И НЕ повод качнуться в
  «рассказ» — за него ругали 17-го. Мишень узкая: живой ясный энциклопедический текст про
  содержание.
📍 Слова разнесены: `candidates/README.md` (канон мастерской, пишет Дизайнер) · воротная
проверка «доля меты» — у неё теперь не наш вкус, а слово владельца, и она стала главной из
четырёх (dev-3). Порог доли назначается ЗАМЕРОМ живых карточек, а не из головы.
Закрытие девяти тикетов вычитки (`bugs/139…148`) и вычистки каталога с откатами — в летописи
(сессии 22–23). Из них живо здесь: ⛔ **неразрывный пробел в каталоге НЕ трогать** (типографика
владельца) · ⛔ **лор видеоигр НЕ трогать** (Fallout, Skyrim, Vault: имя мира — суть объекта) ·
🔑 владелец называет ОДИН случай — замер находит КЛАСС; **спрашивай данные, а не память.**

## 🔴🔴 ГЛАВНОЕ ОТКРЫТОЕ — `bugs/146`, и оно бьёт в цель эпика

**Индекс каталога обслуживается только в цикле, где есть грязная точка** — то есть когда кто-то
поставил оценку (`sync-server/index.mjs:1054`: ранний выход стоит ВЫШЕ `ensureDimsIndex`). Активных
за 7 дней ноль, значит новое измерение ждёт ночного прохода — **до суток**. То же и с ПРАВКАМИ:
`ensureDimsIndex` выходит по `built.docs === dimsCount`, к смене содержимого слеп вовсе.

За вечер симптом снимался **вручную дважды**: `docker restart ndim-server-prod` (при старте идёт
прогрев, а он полный проход), а для смены содержимого — ещё и снятие отметки `built` у `dims_list`.
Это костыль, а не починка.

📌 **Три пути с ценой каждого — в тикете. Рекомендация: путь B** (флаг «каталог изменился»,
1440 чтений в сутки = 2,9 % квоты против ~17 % у пути A). **Решение за владельцем.**
⛔ **Чего НЕ делать:** не писать строку индекса панелью — предзаписанная строка обнуляет прирост
дельты, и сервер уходит в полную пересборку (`plans/44` шаг 4).



## 📜 Порядок работ 2026-08-17 и разборы сессий 18–21 — перенесены в летопись (стрижка сессии 32)

Живое из них, одной строкой каждое: **менеджер измерений — приоритет «как можно скорее»**
(слово владельца 2026-08-17, территория `plans/44`; разблокирует и `bugs/135`) · **`plans/58`**
(теги жизненного цикла, шаг 1 ждёт очереди — см. «НЕ ПОТЕРЯТЬ» выше) · **`plans/59`** (виджет
«Сегодня», A+B: своя полоса прокрутки, проверять на 390) · **английские 533 заимствования — в
беклоге словом владельца; 🟢 **ФАН-АУТ РАЗРЕШЁН 2026-08-22** — на вопрос «трое разработчиков,
распространяется ли запрет» владелец ответил дословно: «**Три разработчика — запрет был про
другое**». Замер dev-1: работы **510** записей (533 минус 23 совпадения внутри кавычек — цитаты
рецензентов, править запрещено). Режем на троих, однородность почерка держат ВОРОТА, а не
дисциплина — владельцу это сказано прямо. 📍 Нарезка написана: **`plans/70`** (партии, а не
диапазоны · ворота фазы: партия 1 dev-1 эталон, двое остальных входят после её вердикта ·
🔴 план ЗАПЕРТ четырьмя предусловиями конвейера, П1–П4)** (вход: `node tools/build-rewrite-worklist.mjs
--lang en --min-run 10`) · **`ideas/37`** (консоли: BigQuery-выгрузку включать первой) ·
⛔⛔ **откат описаний к тексту Википедии запрещён стоячим правилом** (шапка
`tools/rewrite-catalog-descriptions.mjs`) · ⛔ **ultracode отключён владельцем** («работать на
max в одиночку») · заказ **автопересборки для краулеров** разведан (`researches/39`), план не
написан. Полные тексты — `PROJECT_HISTORY.md` и история git этого файла.


*(Разбор сессии 18 — в летописи. Его строка про `bugs/134` устарела: вылечен 2026-08-22,
см. поправку смены 11 выше.)*

🧪 **QA-МИССИЯ (`plans/54`) В РАБОТЕ. Фазы 0 и 2 (Smoke) сделаны, следующее — К7 и фаза 1.**

🔴 **SMOKE ЖИВОЙ: `node tools/smoke.mjs` — 19 кейсов, стенд 19/0 за 1.1 мин** при окне 10
(`qa/suites/smoke.md`). **Скоуп продиктовал владелец**, и он глубже «экраны открылись»: вход,
регистрация гостя с сохранением UID и труда, логин в существующий, логаут, данные, правка данных,
измерения, добавить оценку, пересчёт связей, удалить, все двери «Меню». Доказан 2 мутациями.
🔑 **Мутация «оценка не помечает точку грязной» оставляет ЭКРАН ЗЕЛЁНЫМ** — звёзды горят, карточка
на месте, а сервер синхронизации об оценке не узнает. Ловит только чтение базы: **вердикт записи
снимается с базы И с экрана**.
✅ **К7 ЗАКРЫТ: набор стоит В ДВЕРИ** — `npm run deploy` гоняет его **по собранному артефакту ДО
выката** (шаг «ВОРОТА SMOKE»). Спросить, ничего не выкатывая: `npm run deploy -- --stage --gate-only`.
Доказано мутацией двери: красный набор закрыл её кодом 1 и до `firebase deploy` не дошёл.
🔴 **ЦЕНА, КОТОРУЮ НАДО ЗНАТЬ: ВЫКАТ ТЕПЕРЬ ТРЕБУЕТ ПОДНЯТОГО СТЕНДА** (`npm run stand`) — набор
пишет в базу и ждёт цикла синхронизации, а писать этим в бой нельзя. Нет эмуляторов → дверь
ОСТАНАВЛИВАЕТСЯ, а не пропускает шаг: ворота, которые молчат, красят зелёным непроверенное.
⚠️ И это смыкается с `bugs/134` (стенд умирает от нехватки памяти): падение стенда теперь блокирует
выкат. Обход на время расследования — `NODE_OPTIONS=--max-old-space-size=6144` перед `npm run stand`.
⏭️ **Не сделано и названо:** на живых контурах `NDIM-AUTH-003`/`005` честно пропускаются (почтовые
коды берутся у эмулятора Auth).

**Фаза 0 закрыта картой путей.**
Единица покрытия названа: **путь человека** с номером `NDIM-<ОБЛАСТЬ>-<NNN>`. Карта —
`qa/JOURNEYS.md` (план фазы — `plans/55`): **139 путей** в 13 областях, Smoke **14** при потолке 15,
позитивных 98 против 41 негативного. Сторожит `node tools/qa-journeys-lint.mjs` (доказан 3
мутациями): поля, уникальность номеров, сводка против строк и **ворота фазы** — маршруты снимаются
с дерева `src/routes/`, и каждый обязан быть назван путём.
🔴 **Карта показана владельцу и ждёт его правок** — он профессиональный QA, и приёмка фазы это он.
Правки вносятся в карту, а не в спор с ней.
**Фаза 1:** тест-план, схема кейса, **разметка существующих 579 проверок** (`// @covers NDIM-…`) и
страж `verify-qa-coverage` — первое в истории проекта число покрытия. Разметка идёт ПО ОБЛАСТЯМ
карты, а не по файлам: область закрывается целиком и сразу даёт число.
⛔ **59 стражей не переписывать** — их размечают.

🧾 **Расписка стейджа живёт НА КОММИТ** и сейчас ПРОСРОЧЕНА (выписана на `8560966`, HEAD ушёл
далеко вперёд). Это не поломка, а сам механизм: **сначала `npm run deploy -- --stage`, потом бой.**
Спросить, не выкатывая: `npm run deploy -- --gate-only`. Разбор эпика стейджа — в летописи,
сессия 16.

## 🔴🔴 КАНОН-ПРАВИЛО, ЗАВЕДЁННОЕ 2026-08-15 — читать до любой работы с кодом

**«СЕРВЕР СИНХРОНИЗАЦИИ» — строгое бренд-имя, ВЕЗДЕ и на всех языках** (слово владельца):
код, переменные, модули, папки, образы, комментарии, документы, README. **Любое применение
иного термина — запрет, баг, срочное исправление.** ⛔ Запрещены как имя компонента:
~~вычислитель~~ · ~~calculator~~ · ~~calc~~. <!-- ИМЯ-ОК: STATUS объявляет запрет и обязан назвать запрещённые слова -->
Причина названа владельцем: «вычислитель» — **отсебятина, придуманная ИИ при переносе 1.x → 2.0**.
🟡 Не подпадают: «Калькулятор любви» (утверждённое имя страницы) и чужие интернет-калькуляторы.
🔒 Стережёт `node tools/verify-sync-server-name.mjs` (893 файла, доказан 2 мутациями) —
**прогоняй перед коммитом**, он же ловит маркер `ИМЯ-ОК:` без причины.
Исполнено: `calculator/` → **`sync-server/`**, `CALC_*` → `SYNC_*`, образ `ndim-sync-server`,
контейнер **`ndim-server-prod`**, лог `[sync]`, `npm run test:sync` / `npm run sync:image`.

**КЛЮЧИ ЖИВУТ ТОЛЬКО В `.env`** (слово владельца: «никогда ключи в коде и других файлах не
храним»). Файлов с ключами в проекте НЕТ. Значение — **base64** от JSON (одна строка ASCII).
Приборы берут ключ через `tools/lib/credentials.mjs` (контур в имени переменной + сверка по
`project_id`); контейнер — через `NDIM_SA_B64`. Образец — `.env.example`.
🔑 Повод был не вкусовой: боевой контейнер монтировал ПАПКУ `secrets`, и ключ стейджа оказался
внутри боевого контейнера — смешение контуров, запрещённое `plans/53`.

## 🔐 `bugs/113` — ПРИВАТНОСТЬ: оценку можно привязать к личности (разобран, НЕ закрыт)

Канал — **дифференциальный съём по `commonSpaceSize`**: снять число общих осей по своему топу,
оценить измерение X, дождаться цикла — у кого выросло, тот и оценил. Счётчик в атаке не участвует.
✅ Правила Firestore исправны (`firestore.rules:234`), список общих осей ядро наружу не отдаёт.
Направление защиты выбрано владельцем (ловить и банить · делать съём нерациональным ПО ВРЕМЕНИ).
Работа не начата. ⚠️ Атака НЕ воспроизводилась в бою — разбор чтением кода.

> 🔴 **Ловушка, стоившая бы дорого:** соблазн прочитать чужие оценки ключом сервисного аккаунта
> (`sync-server/secrets/sa.json`, Admin SDK правила обходит). **Нельзя.** `firestore.rules:219` —
> «АДМИН — НЕ ИСКЛЮЧЕНИЕ», запрет восстановлен аудитом `bugs/100`.

## ⚠️ Операционные капканы стенда (каждый стоил ложного диагноза)

1. **`npm run e2e` требует ПОГАШЕННОГО стенда** (`EXP-0027`). 🔴 Признаку `stand.log` НЕ ВЕРЬ —
   сверяй ПРОЦЕССАМИ:
   `Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -match 'sync-server|stand\.mjs|emulators' }`,
   гаси дерево от ВЕРХНЕГО pid (`taskkill /PID <корень> /T /F`).
2. **Свой `vite preview` на 4173 подменяет сервер e2e** — поднимай с `--strictPort`. Осиротевший
   preview отдавал СТАРУЮ сборку и дал 21 ложное падение. Проверять портом:
   `Get-NetTCPConnection -LocalPort 4173 -State Listen`. Симптом класса: падения РАЗНОРОДНЫЕ и
   задевают экраны, которых правка не касалась.
3. **`npm run test:sync` сразу после гашения стенда врёт.** Воспроизведено снова 2026-08-14:
   первый прогон — 66 тестов и 1 провал, в логе «каталог 0 измерений» и чужие точки `anna, boris`;
   второй, когда порты освободились, — **71/0**. Это порты, а не продукт. Дай стенду догаснуть.
4. ✅ **`npm run e2e` больше НЕ выжигает чужие кадры** — `playwright.config.ts` получил
   `outputDir: 'test-results/e2e'` (`EXP-0157`). Порядок «сначала e2e, потом съёмка» помнить не надо.

## ⛔ Чего НЕ делать

- **Блок соседства:** разрешён «Похожие по каталогу» ПО КЛАССИФИКАЦИИ (№025 В7 = А); НЕ называть
  его «Рядом в Пространстве». 🔴 Инвариант «цифр метрик похожести не раскрываем» (включая порядок
  близости) в силе; IBCF — «до лучших времён».
- **Не «чинить» отсутствие строки «Обновить данные» в «Меню»** — убрана словом владельца (В1=Б).
- **Не возвращать множитель витрины лендинга** (×30) — отменён 2026-08-01 (№010 Р7).
- **Не проверять гостя без стендовых дверей** `?as=guest` / `?as=none`.
- **Не «чинить» отсутствие приглашения «Установить приложение»** — service worker сознательно НЕТ
  (`researches/19` §5), это `bugs/62`, решение владельца.
- **Не «чинить» `storage.cors.json` при загрузке фото** — SDK ходит на другой хост (`EXP-0112`).
- **Не ужесточать ЧТЕНИЕ аватаров сгоряча** — паритет с 1.x держит показ лиц (`EXP-0043`);
  тест «ПАРИТЕТ С 1.x» ОБЯЗАН уронить такую правку, и это его работа.
- **Не трогать `--bar-h-fallback` «одной константой»** — 61/55 сняты замером (`bugs/67`).
- 🔴 **Не «чинить» `inset: 0` у полноэкранных слоёв на `100vw`** — проверено пикселями, не
  работает (`bugs/65`). Разделяющий признак класса — СКОЛЛ-ЛОК, а не `inset: 0`.
