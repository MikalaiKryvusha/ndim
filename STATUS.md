# NDim Space — Текущий статус

> Этот файл читается ИИ-агентом перед каждой задачей. Обновляй его при каждом значимом изменении
> состояния. Это ГЛАВНАЯ передача эстафеты между сессиями: новая сессия агента стартует с пустым
> контекстом и должна суметь стать продуктивной, опираясь на один этот файл. Пиши соответственно —
> конкретно, с путями к файлам и командами.
> 🧠 Главный принцип мышления — `PHILOSOPHY.md` (ПРОСТОТА: KISS + Оккам). Рабочий фреймворк —
> `AGENT_GUIDE.md`.
>
> ⚠️ **STATUS — это СВОДКА «СЕЙЧАС», а не летопись.** Статусный файл, который только растёт,
> превращается в историческую книгу проекта, и агент, пришедший за быстрым «где мы», в ней тонет
> (поле: STATUS на 2 300 строк — «бездна, а не сводка»; наш дорос до 2 900 и подстрижен
> 2026-07-31). Правила, которые держат его сводкой:
>
> - **Каждая строка проходит два теста:** *«убери эту строку — ошибётся ли следующий агент?»* и
>   *«читает ли новичок весь файл за один присест?»* Мягкая цель: **~200 строк** (один-два экрана
>   сути) — страж предупреждает, а не запрещает, но пересечение значит, что стрижка назрела.
> - **Закрытое ВЫНОСИТСЯ, а не копится:** когда запись фазы или сессии перестала быть «сейчас»,
>   перенеси её ДОСЛОВНО в `PROJECT_HISTORY.md` (летопись — она для этого). Шаг «стрижка бонсая»
>   в `/end-chat` — ровно об этом (`/pause` по замыслу остаётся без церемоний).
> - **Оставь файл таким, каким хотел бы его найти:** свежая сводка того, что работает, что в
>   работе, что дальше, какие капканы, и ГДЕ ИСКАТЬ подробности (планы, баги, летопись) —
>   указатели, а не пересказы.

**Последнее обновление:** 2026-08-01, чат закрыт — 🚪 **ПРОЕКТ РАЗВЁРНУТ НА ВОРОТА.** Владелец
ответил на два интервью (№009 — 11 ответов, №010 — 14 из 15) и заказал фундаментальную работу:
лендинг-как-приложение, бесшовный вход анонимом, гость наравне с полноценным человеком,
**английский как основной язык продукта**, максимальное SEO. Проведена **ревизия мастер-плана**:
заведена **фаза 7 «Ворота»**, соцслой стал фазой 8, принципов стало девять. Видение зафиксировано в
`GOAL.md`, `MASTER_PLAN.md` и `AGENT_GUIDE.md` — **читай их до любой задачи по воротам**.

**Что этот чат СДЕЛАЛ в коде** (не только спланировал):
| Что | Где | Проверено |
|---|---|---|
| Правила Storage выкачены в бою (`bugs/04` закрыт) | Rules API | чтением боя: `/admin` и `/public` исчезли, 403 |
| Публичная дверь удаления была СЛОМАНА — три дефекта | `delete-account`, `account.ts` | `verify-delete-door` **16/16**, 2 мутации |
| Документы открыты поиску: 2 страницы → **11**, текста ×13.5 | `DocShell`, `sitemap` | `e2e/seo` **10/10**, мутация |
| Класс «один отказ гасит весь экран» | `relations.ts`, `dims.ts` | живьём: гость с 4 связями, экран жив |
| Множитель витрины ×30 удалён — настоящее число | `metrics.ts` | `verify-bug81` **28/0**, в кадре «94 человека» |
| Предохранитель воронки снят — считает в бою | `funnel.ts` | сборка: firebase остался ленивым |
| Мёртвый UID при протухшей ссылке | `profile.ts` | типы; путь редкий |

🔴 **Три отмены прежних решений, которые ломают старые ожидания:**
1. **Гость больше не невидим** — отменено интервью №004 В3=А. Гостю можно всё, срок жизни
   анонимного аккаунта **7 дней** (было 30). Правила Firestore, тесты отказов и вычислитель
   меняются (`plans/22`).
2. **Английский — основной язык ПРОДУКТА**, русский опциональный (`plans/24`).
   ⚠️ **Документы проекта остаются РУССКИМИ** — врезка в `AGENT_GUIDE` → «Языки». Не перепутай:
   это два противоположных решения, живущих рядом.
3. **Отдельного онбординга не будет** — «лендинг и онбординг в одном лице» (`plans/21`).

⚠️ **Три границы, которые агент держит вопреки просьбе владельца** (записаны в канон, не в чат):
отзывы на лендинге только НАСТОЯЩИЕ, с согласия · предупреждение при уходе только когда труд
реально есть, один раз, с явным выходом · английские тексты вычитывает человек, бренд агент не
сочиняет.

**Предыдущее:** 2026-08-01 — 🔐 **ПРАВИЛА STORAGE ВЫКАЧЕНЫ В БОЙ** по слову владельца
«кати» (`bugs/04` ЗАКРЫТ, четыре года прожив с правилами 1.x). В бою больше нет ни списка
админских почт, ни открытого всему свету `public/`; появился потолок **5 МБ + `webp`/`jpeg`**.
Файлы 1.x забэкаплены в `.private/storage-1x-backup/` (вне git), прежний ruleset цел — откат
одним PATCH. **Фаза 6 эпика (аватар) разблокирована полностью.** Подробности — п. 3 эстафеты.

**Ранее в тот же день:** эпик «Управление аккаунтом» прошёл фазы 2–5 и 8 (см. п. 3): экран
`/account`, смена почты сквозным путём, удаление аккаунта с каскадом и чисткой следов в
вычислителе, пароли отменены целиком.

**Предыдущее:** 2026-07-31, ночь — 📐 заведён сам эпик: лестница `/plan-epic` до третьей ступени —
`researches/24` + `researches/23` → `plans/15` (метаплан, 8 фаз) → `plans/16` →
`interviews/interview_008` (девять развилок).

**Предыдущее обновление:** 2026-07-31, вечер (чат закрыт) — ✅ **ЭПИК `ideas/24` (ФАЗЫ 1–2) В БОЮ
+ ПЯТЬ ВЫКАТОВ.** Все сводные экраны переведены на виджетную сетку с ШАГОМ (модель владельца);
«Аккаунт» слит с карточкой-шапкой «Профиля»; «Меню» приведено к двум колоннам; манифест удалён
целиком как отсебятина агента. Заведены `bugs/104` и `plans/13`. Уроки — `EXP-0104…0107`.

**Днём того же дня:** 2026-07-31 — 🧰 **KAIF ОБНОВЛЁН 2.0 → 2.1 «Strong KAIF»**
(по слову владельца) маршрутом bootstrap: свежий `KAIF.md` → `KAIF-LOADER.mjs` → машинерия 2.1;
сначала прогон в песочнице, предсказание совпало с боем байт в байт. Новое: **`PROJECT_HISTORY.md`**
(летопись; туда дословно мигрирована история этого файла — 2 900 → ~260 строк), 6 скиллов
(`/plan-task`, `/plan-epic`, `/guarded-loop`, `/code-revision`, `/owner-voice`, `/owner-reviews`),
канон-вставки в `AGENT_GUIDE.md` (лестница планирования · гигиена документов и текста + реестр пар
«истина ↔ зеркало» · место вопросов · класс вкуса · разрешение ≠ авторство идентичности). Полевой
отчёт разработчику фреймворка **забран владельцем лично 2026-07-31** — в репозитории его нет и
заводить заново не надо.

**Ранее в тот же день** (аудит слабых моделей, тройной выкат, README-руководство, `ideas/19`) —
перенесено в `PROJECT_HISTORY.md`.

**Свежее:** 2026-08-01, вечер — 🗣 **РАЗВЁРНУТ КОНТУР ВЫЧИТКИ ВЛАДЕЛЬЦА** (`plans/27`, регламент
KAIF `/owner-reviews`): всё, что агент хочет ОТ владельца, открывается **HTML-страницей с ответом
в один клик**, ответ механически возвращается в md с `by`/`at`. Владелец уже ответил через неё на
интервью №011 — **без единого уточняющего вопроса в чате**, а это и есть критерий приёмки
регламента. Голос сигнала взят **взаймы у соседнего проекта KLAS** (Silero v5, локально) — командой,
не копией; голос `eugene` выбран владельцем вслепую из пяти образцов.

| Команда | Зачем |
|---|---|
| `npm run questions` | 🔴 **прогоняй в начале сессии и перед сдачей.** Вопросы владельцу вне `interviews/` + все интервью, ждущие ответа |
| `npm run review -- open interviews/<файл>.md` | открыть страницу, позвать владельца голосом |
| `node tools/verify-owner-reviews.mjs` | QA-прогон контура — **97 проверок**, стенд не нужен |

🔑 **«Сохранить» БУДИТ агента** — слово владельца: агент узнаёт о событии по завершению процесса,
поэтому любое сохранение закрывает контур. Осталось неотвеченное — поднять страницу заново обязан
АГЕНТ, а не владелец держать вкладку. Вид карточек — его же выбор (интервью №012, V1 «Полоса
слева», цветом состояния).

**Что страж нашёл в первый же день:** интервью №007 два дня числилось «🔴 ЖДЁТ ОТВЕТОВ» при
двенадцати заполненных ответах · разбор считал `---` после пустого поля «Ответ:» **ответом**, из-за
чего по всем десяти живым интервью выходило «0 без ответа» · В10 интервью №008 вечно числился
открытым, хотя снят отменой паролей. Всё исправлено. **Унаследованный долг — 26 очередей вопросов
вне `interviews/`** (`tools/questions-baseline.json`): страж краснеет только на НОВЫХ, а это число
обязано убывать. Уроки — `EXP-0116…0118`.

### 🏁 ЭСТАФЕТА (для сессии с пустым контекстом — где продолжить)

0. 🚪 **ГЛАВНОЕ СЕЙЧАС — ФАЗА 7 «ВОРОТА».** Пять эпиков `plans/21–25`.

   **ЧИТАТЬ В ЭТОМ ПОРЯДКЕ, иначе построишь чужую фазу:**
   `plans/26` (**СШИВКА — первой!** порядок работ и кто чем владеет) →
   `interviews/interview_009` (11 решений, сводная таблица в шапке) →
   `interviews/interview_010` (14 развилок с ответами; Р5 БЕЗ ОТВЕТА) →
   `MASTER_PLAN.md` фаза 7 → нужный эпик.

   **Почему это важнее всего:** продукт работает и никому не виден. Гостевой режим — рабочий, в
   бою с 12 июля — спрятан так, что с лендинга в него не ведёт ни одна из трёх кнопок; человек
   упирается в стену входа ровно на пике интереса. Это боль `ideas/09` — она в бою жива.

   **Входы (не переоткрывать, ссылаться):** `researches/25` (онбординг, 128 первоисточников;
   §14 — числа, которыми пользоваться нельзя) · `researches/26` (SEO, §13 — порядок действий и
   список «⛔ не делать вовсе») · `researches/27` (боли в приложениях знакомств; §5.3 ОПРОВЕРГНУТ,
   читать §5.4).

   **⛔ ЕДИНСТВЕННЫЙ ОТКРЫТЫЙ ВОПРОС ВЛАДЕЛЬЦА — Р5: что стоит на корне сайта.** Три варианта в
   `interviews/interview_010`; рекомендация агента — **В**: ни один язык на корне, оба живут
   своими адресами, `/` только распознаёт и отправляет. Без ответа не начинать `plans/24` фазы
   6–8 (языковые адреса).

   **Следующая работа, НЕ ждущая владельца** (по `plans/26`, в этом порядке):
   1. **Замер «сколько ждать первого топа» начисто** — `node tools/probe-guest-journey.mjs` на
      СВЕЖЕМ стенде. Этот чат намерил: при 5 оценках топ не приехал за 90 с, при 10 — есть,
      4 связи, лучшая похожесть **22%**. Но «за 0.0 с» на 10 и 20 означает лишь, что документ
      лежал с прошлого круга — точное время первого топа НЕ поймано.
      🔴 От этого числа зависит вся кульминация петли владельца.
   2. **Тексты ворот** (`plans/21` фаза 2) — как только придёт ответ Р4 о формулировке «точности»
      (он уже есть: владелец настоял на «точно подбираем», см. интервью №010).
   3. **Макеты** — ОДИН раунд на первый экран, а не три (решение сшивки).

   ⚠️ **App Check агент включить НЕ МОЖЕТ** — нужен ключ reCAPTCHA из консоли Google, руки
   владельца. `plans/26` считала это агентской работой; это неверно.

1. ✅ **`ideas/24` (виджетная сетка, фазы 1–2) — ЗАКРЫТ И В БОЮ с 2026-07-31.** Подробности
   перенесены в `PROJECT_HISTORY.md` (стрижка 2026-08-01). Что важно помнить здесь:
   **утверждённая модель сетки** — 4 шага по горизонтали, малый виджет 1 шаг, большой 2, при
   нужде 4; шаг задаёт ТОЛЬКО горизонт, по вертикали колонка пакуется сама, растягивать
   ничего нельзя (прямое слово владельца). Эталон — `design/widget-layout-mockups.html`, V4.
   Страж `node tools/verify-ideas24.mjs` (60 проверок, `--mutant` обязан дать 32 провала).
   ⚠️ Капкан, оплаченный дважды: метрика пустоты считает пустоту МЕЖДУ карточками и НЕ ВИДИТ
   пустоту ВНУТРИ них — замер верен только там, где снят.
   **Фаза 3 (настройки человека, `ideas/16`) не начата**, план `plans/14` не написан.
2. 🔴 **`bugs/104` — карточка связи раскрывается только тапом ПО ИМЕНИ** (слово владельца
   2026-07-31: «чтобы карточку можно было раскрывать тапом, кликом по всему верху, а не только
   целится в поле имени»). Задачи такой в беклоге не было — заведена.
   Разведка уже сделана и меняет постановку: **в 1.x весь верх БЫЛ целью** (аватар жил в
   `<summary>`, `researches/12` строка 102) — это паритет, а не улучшение. И 🔴 **обернуть
   верх в `<button>` НЕЛЬЗЯ**: аватар сам кнопка (портрет во весь экран, `bugs/14`/`36`), а
   кнопка внутри кнопки невалидна. Пригодный путь — растянуть `.who` на всю строку справа от
   аватара. В документе висит вопрос владельцу: входит ли САМО фото в «весь верх».

3. 🔴 **ЭПИК «УПРАВЛЕНИЕ АККАУНТОМ» — `plans/15`. Заказ владельца 2026-07-31:** смена Email ·
   сброс пароля · смена пароля · аватар · **удаление аккаунта** («тоже надо» — его слово в ответ
   на вопрос о границах). Поглощает `bugs/45`, `bugs/44`+`plans/13` и часть `bugs/32`.

   **Ступень 1 лестницы закрыта:** `researches/24` (Firebase + OWASP ASVS 5.0 + NIST 800-63B-4 +
   GDPR/сторы + форма экрана у шести продуктов + разведка архива 1.x **по коду**) и
   `researches/23` (фото в браузере). 306 фактов, каждый с URL и цитатой; 24 рискованных прошли
   состязательную перепроверку (20 подтверждено, 4 уточнено).

   🔴 **Пять находок, которые меняют постановку** (не переоткрывать):
   - `updateEmail` и `linkWithCredential` с паролем **закрыты** защитой от перечисления почт;
     смена почты только `verifyBeforeUpdateEmail`, «завести пароль» — только REST `accounts:signUp`.
   - **«Письмо отправлено» может быть неправдой:** при занятом адресе promise резолвится молча ⇒
     тексты обязаны быть нейтральными («если адрес свободен, письмо придёт»).
   - **Реавторизация ветвится на ТРИ пути** (`providerData`); popup для человека, вошедшего по
     ссылке, **бросит ошибку**.
   - **Порядок удаления обратен 1.x:** сначала данные, потом `deleteUser`. В 1.x аватар оставался
     сиротой, а ошибки удаления глушились — это дефект, не образец.
   - 🔴 **Зелёный стенд НЕ доказывает смену почты:** Auth-эмулятор не применяет запрет EEP в
     `setAccountInfo`, писем не шлёт. Проверяется только боем.
   - Бонус фазы «аватар»: **Safari не кодирует WebP** из canvas, молча отдаёт PNG (323 КБ против
     12 КБ), а путь `avatar.webp` менять нельзя.

   ⚠️ **Разведка опровергла наши же документы:** в 1.x у смены пароля было **два поля, а не три**,
   а на смене Email **пароль стоял выше нового адреса**. `researches/12` и `bugs/45` исправлены.

   ✅ **ФАЗА 2 ПРОЙДЕНА 2026-08-01** (`plans/16`, полностью — `researches/24` §7). Владелец ответил
   на все девять вопросов интервью №008 (все — вариант **А**; уточнения: **В8** тип файла уже —
   только `webp/jpeg`; **В9** iPhone откладывается, приоритет Android и десктоп). Разведка боевой
   конфигурации сделана **только чтением**, замеры — прибором `tools/probe-account-phase2.mjs`
   (лёгкий эмулятор Auth, тяжёлый стенд не нужен, сироты не остаётся).

   🔴 **Четыре находки фазы 2, две из которых меняют план:**
   - **EEP в бою ВКЛЮЧЕНА** (`enableImprovedEmailPrivacy: true`) — главная развилка закрыта фактом.
   - 🔴 **`providerData` НЕ отличает «есть пароль» от «входит по ссылке»** — у обоих `password`
     (типы SDK: *«Always set to ProviderId.PASSWORD, even for email link»*). **Механизм под решение
     В1 неисполним;** фаза 7 начинается с вопроса «чем отличить», а не с вёрстки.
   - 🔴 **Код отмены смены почты на эмуляторе не рождается** (2 из 2), хотя почта менялась. В бою
     шаблон отката настроен и по-русски — проверяется только боем; фазу 5 планировать от худшего.
   - ✅ **Блок «CORS бакета только GET» ложный:** замер заголовками дал `POST, GET, HEAD, DELETE,
     PATCH`. Загрузка и удаление не блокированы (`PUT` в списке нет — довод за `uploadBytes`).

   Наследие в конфигурации: **Action URL смотрит на `ndim-space.firebaseapp.com`**, а не на наш
   дом; у шаблона подтверждения почты **английская тема при русском теле** (правится в консоли).
   ⚠️ Замер «сброс пароля для аккаунта без пароля» **честно провален** (EEP на эмуляторе не
   включилась, HTTP 404) — остаётся `[NOT-TESTED]`.

   ✅ **ФАЗА 3 ЗАКРЫТА 2026-08-01 — МАКЕТ УТВЕРЖДЁН** (`plans/17`). Владелец выбрал **V2 «Одна
   страница»** с правкой **«с виджетами»**. Форма эпика зафиксирована: отдельный маршрут ·
   подэкранов нет, секции раскрываются на месте · страница собрана **виджетной сеткой** по модели
   «шага» (`ideas/24`): содержательные виджеты по 2 шага, **«Удаление аккаунта» — во всю панель
   (4 шага), последним** · виджет «Пароль» видят только те, у кого пароль есть · меню фото из
   четырёх действий открывается тапом по самому фото.
   Артефакты: `design/account-mockups.html` · 16 кадров `test-results/account-mockups/` ·
   **новый общий съёмщик `tools/shoot-mockups.mjs`** (правило четырёх макетов — одной командой).

   ⚠️ **Кадры поймали ЧЕТЫРЕ дефекта в самих макетах, все до реализации:** меню фото закрывало
   сравниваемое (дважды) · «десктоп» был колонкой 430px вместо оболочки 1440 · диалог V4 уезжал
   за край (два конфликтующих правила) · **в утверждённом V2 кнопка «Удалить аккаунт»
   растягивалась во всю панель** и становилась самой широкой мишенью экрана (`EXP-0110`).

   ✅ **ФАЗА 4 ЗАКРЫТА 2026-08-01 — ЭКРАН ЕСТЬ В КОДЕ** (`plans/18`). Заведён маршрут
   **`/account`** («Управлять аккаунтом» — инфинитив, канон 1.x), виджет **«Мой аккаунт»**
   (Email + признак подтверждения · «Создан» в формате 1.x · «Способы входа»), а строка
   **«скоро» в «Профиле» заменена живой ссылкой**. Данные берутся из Auth **без единого запроса
   к базе**. Страж `tools/verify-account.mjs` — **84 проверки, доказан 2 мутациями**; смоук
   `e2e/account.spec.ts`. Полный прогон: check/typecheck чисто, `npm test` 192/192,
   `npm run e2e` **74** (было 70). 🔴 **В бой НЕ деплоилось** — это решение владельца.

   ⚠️ **Строку «Пароль не задан» с макета написать оказалось НЕЛЬЗЯ** — `providerData` этого не
   знает (`EXP-0109`). Экран честно молчит про пароль, и **страж стережёт именно молчание**:
   мутация «дописать про пароль» его роняет. Формулировка ждёт ответа владельца на **В10**.

   ⚠️ **Граница фазы поправлена при исполнении:** реавторизация и словарь ошибок перенесены в
   фазу 5 — у них в фазе 4 нет ни одного вызывающего, а значит ворота «проверено живым
   браузером» неисполнимы. Приедут вместе со своим первым вызывающим — сменой почты.

   🟡 **ФАЗА 5 (`plans/19`) — В КОДЕ, ЖДЁТ ПРИЁМКИ БОЕМ.** Смена почты работает сквозным путём:
   виджет «Поменять Email» (тексты и порядок 1.x) → подтверждение личности → письмо на новый
   адрес → наша страница-приёмник `/auth/action` → **адрес реально меняется**. Реавторизация
   тремя путями и словарь ошибок словами 1.x — там же. Страж `tools/verify-account-email.mjs`
   **29/29**, доказан мутацией с названными адресатами (утвердительное «Письмо отправлено» →
   покраснели **ровно** 2 проверки нейтральности). Страж ядра 84/84 без регрессий,
   `npm test` 192/192, `npm run e2e` 74.

   💡 **Находка, улучшившая замысел:** ссылка подтверждения уходит на ТЕКУЩИЙ адрес — значит она
   и есть то подтверждение со старого адреса, которого требует OWASP без второго фактора. Это
   снимает зависимость от письма-отката Firebase, которое в замере фазы 2 не приехало ни разу.

   🔴 **Что честно НЕ проверено и требует боя:** режим `recover-email` (эмулятор такого кода не
   выдаёт) · поведение при ЗАНЯТОМ новом адресе (эмулятор не применяет запрет EEP) · Google-путь
   реавторизации (popup в headless не воспроизвести).

   ⚠️ **Поправка:** Action URL переключается **после фазы 7**, а не сейчас — приёмник умеет три
   режима из четырёх, сброс пароля приедет вместе с самим паролем. Ничего не блокирует: сегодня
   письма обрабатывает страница Firebase, и смена почты работает через неё.

   ⛔ **ПАРОЛИ ОТМЕНЕНЫ ЦЕЛИКОМ — решение владельца 2026-08-01:** «Убираем пароли полностью,
   оставляем только ссылки по почте и Google авторизацию». Возврат к его же канону (интервью
   №004, В2=A+). `bugs/32` закрыт как отменённый, **фаза 7 эпика вычеркнута**, вопросы В1 и В10
   сняты. Побочно: страница `/auth/action` стала ПОЛНОЙ (режим сброса пароля невозможен), и
   переключение Action URL больше ничего не ждёт.

   🔧 **Новая работа, которую породило решение: погасить пароли у 331 человека в бою.**
   Переключателем проекта не делается — email/password и email-link это ОДИН провайдер
   (`passwordRequired`: «If false, a user may sign in via either email/password or email link»);
   выключить его значит убить и вход по ссылке. Гасится по данным: `accounts:update` с
   `deleteAttribute: ["PASSWORD"]`. 🔴 `deleteProvider: ["password"]` НЕ ИСПОЛЬЗОВАТЬ — он снимет
   саму почтовую личность. Способ `[NOT-TESTED]`, операция необратима, руки владельца.

   ✅ **ФАЗА 8 ЗАКРЫТА 2026-08-01 — АККАУНТ УДАЛЯЕТСЯ** (`plans/20`). Каскад `src/lib/data/erase.ts`
   идёт **Storage → Firestore → `deleteUser`** и **останавливается на первой неудаче**: не
   удалилось — учётную запись не трогаем (обратно дефекту 1.x, где ошибки глушились). Подтверждение
   — ввод своей почты + повторный вход. Виджет во всю панель, красная кнопка только на финальном
   шаге. **Публичная страница `/delete-account`** доступна без входа и БЕЗ `noindex` (требование
   Google Play) — это **дверь, а не инструкция** (поправка владельца: «Почему "Как" удалить
   аккаунт? Зачем КАК?»). Стражи: `verify-account-delete.mjs` **22/22** (мутация «`deleteUser`
   первым» → краснеют ровно каскад и все пять проверок «ПОСЛЕ») + 5 тестов правил на «чужое не
   удалить» (**102** теста правил).

   🔴 **Разведка нашла следы шире, чем предполагал ответ В3:** свой `relations`, членство в ЧУЖИХ
   группах, подсказки аудитории у других, свои предложения измерений. Владелец ответил **В11 = А**:
   суточный проход вычислителя вычищает всё, а **предложения ОБЕЗЛИЧИВАЕТ** (`authorUid` стирается,
   текст остаётся — предложение полезно Пространству и после ухода автора).
   `cleanupDeletedPeople()` в `calculator/index.mjs`, **9 тестов**, доказан мутацией.

   ✅ **`bugs/04` ЗАКРЫТ 2026-08-01 — ПРАВИЛА STORAGE В БОЮ** (слово владельца «кати»).
   ⚠️ Транспорт не тот, что в плане: креды Firebase CLI протухли, перелогин интерактивный —
   выкат сделан **Rules API** (ruleset `f656a24d…`, релиз переключён). Это то же, что делает
   `firebase deploy --only storage` внутри. **Проверено чтением боя:** `match /admin` и
   `match /public` исчезли · `public/um.html` **403** (был открыт ВСЕМ) · `admin/adminFunctions.js`
   **403** · аватар без входа **403**, как и до выката (правило чтения не менялось).
   Откат готов: `.private/storage-1x-backup/storage.rules.PROD-BEFORE.txt` + прежний ruleset цел.

   **Следующее — фаза 6 (аватар): загрузить · заменить · удалить.** Блоков не осталось ни одного —
   ни правил, ни CORS (замер `researches/24` §7.4). «Открыть на весь экран» уже готово
   (`Avatar.svelte`), переделывать не надо. Приёмка на живом Safari отложена самим владельцем (В9).

4. **Взгляд владельца под логином** (гостевые смоуки не видят): виджет «Аккаунт» внизу
   «Профиля» (выкачен сегодня) · ореол/уезд карточки «Измерений» (файлы `D:\ndim-ореол-до-после.png`,
   `D:\ndim-уезд-карточки-700мс.mp4` — нить с 30.07).
3. **Правила Firestore в бою обновлены сегодня** (bugs/99, 100): дружба с hasOnly+created,
   админ больше НЕ читает оценки. Если владельцу понадобится админ-чтение points — это
   осознанно снятый доступ (интервью №002 В4, ideas/13), не дефект.
5. **Сервер синхронизации — билд 20** (фиксы устойчивости bugs/91–93 + страж эмулятора
   bugs/94). Разовая конвергенция «237 топов» на прогреве объяснена и закрыта (STATUS выше);
   рестарты снова бесплатны. Дифф боевого ruleset/сервера с репо — нулевой.
6. **Прочий беклог не двигался**: тиры `plans/07` (PWA на телефон `bugs/58`/`83` — нужен
   телефон владельца; `bugs/44`/`45` — макеты/решения), `bugs/74` часть А — контракт кэша.
7. **Уроки сессии** — `EXP-0096…0102` (греп перед задачей): аудит свежим взглядом · TWINS по
   механизму · флаг после работы · двусторонние инварианты · outro-снапшот Svelte ·
   комментарии-каноны · изоляция базы стенда. Отчёты: `researches/20` (KAIF, усиление слабых
   моделей) · `researches/21` (стилометристу Unliminium, копия у них).
8. **Полная регрессия прогнана сегодня дважды** (42/42 стража после фикса 103) — не перегоняй
   без причины; точечные стражи см. в таблице AGENT_GUIDE.

## 🧭 РАБОЧИЕ РАЗДЕЛЫ (актуальны на 30.07, вечер)

### Первым делом — что запустить свежей сессии

```bash
npm run stand                     # эмуляторы + сервер синхронизации + vite (нужна Java)
node tools/verify-plan08.mjs      # 60 — память вида экранов, самое свежее и самое сквозное
node tools/verify-tierA.mjs       # досье связи целиком, включая новую строку bugs/46
cat interviews/interview_007_ideas21_forks.md   # ответы владельца по волне
```

⚠️ **Три операционных капкана, на которых этот чат обжёгся** (каждый стоил ложного диагноза):

1. **`npm run e2e` требует ПОГАШЕННОГО стенда** (EXP-0027) — иначе 6 проверок «без данных —
   честная ошибка» краснеют на исправном коде. Гасить по дереву: найти `node tools/stand.mjs`
   и `taskkill /PID <pid> /T /F`. 🔴 **Признаку `stand.log` НЕ ВЕРЬ** (проверено 2026-07-31:
   файл исчез, а три процесса выжили) — сверяй ПРОЦЕССАМИ:
   `Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -match 'calculator|stand\.mjs|emulators' }`.
2. **Свой `vite preview` на 4173 подменяет сервер e2e.** Гаси preview перед `npm run e2e`, а сам
   поднимай с `--strictPort`: без него он молча уезжает на 4174, и стражи собранного сайта судят
   не о том.
   🔴 **Обожглись ВТОРОЙ раз 2026-07-31, и дороже:** осиротевший preview отдавал СТАРУЮ сборку,
   и полный прогон дал **21 падение** — в лендинге, языке, теме, «Меню», профиле. Всё выглядело
   как разнородная регрессия продукта; после гашения порта — **72 passed**, продукт не при чём.
   **Остановка фоновой задачи может отчитаться успехом, а процесс — выжить.** Проверять только
   портом: `Get-NetTCPConnection -LocalPort 4173 -State Listen`. Пусто — можно гнать e2e.
   Симптом класса: падения РАЗНОРОДНЫЕ и задевают экраны, которых правка не касалась.
3. **`npm run test:calc` сразу после гашения стенда** может дать 26 тестов вместо 34 и один
   провал — порт эмулятора ещё занят. Дай портам освободиться.

### ⛔ ЧТО ЖДЁТ ВЛАДЕЛЬЦА (полный список на 30.07 — агент это НЕ решает)

Отсортировано по цене вопроса, а не по номеру документа:

0. ✅ **`interviews/interview_008` — ОТВЕТЫ ПОЛУЧЕНЫ 2026-08-01** (включая В11), разнесены по
   документам. Вопрос «чем отличать пароль от ссылки» **снят вместе с паролями**.
   От эпика за владельцем осталось три вещи, и все три — руки человека, не агента:
   - 🔧 **погасить пароли у 331 человека в бою** (`deleteAttribute: ["PASSWORD"]`, см. п. 3
     эстафеты) — необратимо, `[NOT-TESTED]`;
   - 🔧 **приёмка фазы 5 в бою** — смена почты под EEP: письмо на новый адрес, письмо-возврат на
     старый, поведение при занятом адресе. На стенде не проверяется по построению;
   - 🔧 **приёмка фазы 6 на живом Safari** — отложена им же (В9), не забыта.
1. **Выкат в бой** — вся работа 29–30 июля (список наверху). Плюс пересборка образа сервера.
2. **`bugs/74` часть А, контракт кэша** — гасить ли собранный экран «Измерений» своей же
   оценкой (вопросы 1 и 2 в документе). Это единственное, что мешает памяти вида работать
   ПОСЛЕ оценки: экран честно не восстанавливает позицию, когда лента пересобрана.
3. **`bugs/80` подпункты (а) звёзды и (в) адаптивный `clamp()`** — вопросы 1 и 3, замер шага 0
   не снят.
4. **`bugs/84`: текст ошибки** для ссылки БЕЗ намерения и расширенная фраза врезки (`[AI]`).
5. **`plans/08` вопросы Б, В, Г** — агент взял их по своей рекомендации и пометил это честно;
   нужна вычитка (что помнить кроме прокрутки · сброс при pull-to-refresh · на всех экранах).
6. **`bugs/83` / PWA** — обзор индустрии готов (`researches/19`), развилки владельца названы
   там же §5: что именно установлено у него на телефоне (1.x? TWA? ярлык?), один манифест или
   два (RU/EN), нужна ли нативная обёртка при изъятом ключе подписи (`bugs/02`).
7. ~~**`bugs/04` шаг 4** — выкат правил Storage~~ ✅ **СДЕЛАНО 2026-08-01.** «Взгляд в консоль»
   оказался чтением бакета через `gcloud storage ls`: по путям было не пусто, шесть файлов 1.x
   забэкаплены, владелец сказал «отвязываемся» — и выкат перестал быть необратимым.
8. **`bugs/46`: «Был онлайн»** — новое публичное поле, родня `ideas/23` / `plans/09`.
9. **`bugs/62`** — кнопка «Установить приложение» и service worker: цена SW названа в
   `researches/19` §5 (кеш пререндеренной статики может запереть человека на старой сборке).
10. **Мелочь:** строка «В Пространстве с …» осталась в «Меню» (владелец просил убрать из
    ПРОФИЛЯ) — спросить.

## 🧭 РАБОЧИЕ РАЗДЕЛЫ (продолжение) — читать всем, кто берёт задачу

### Чего НЕ делать

- **Не «чинить» отсутствие строки «Обновить данные» в «Меню».** Она убрана по прямому слову
  владельца (В1=Б), цена названа ему и принята. Это следствие, а не регрессия.
- ~~**Не «чинить» множитель витрины лендинга** (×30)~~ — 🔴 **ПРАВИЛО ОТМЕНЕНО 2026-08-01**
  (интервью №010, Р7): «Делаем живую настоящую цифру. И анонимов и полноценных считаем и
  показываем». Множитель удалён из `metrics.ts`. **Не возвращать.** Причина отмены — рядом с
  витриной встанет секция настоящих отзывов от настоящих людей, и умноженное число обесценило бы
  и её, и себя.
- **Не проверять гостя без стендовых дверей** `?as=guest` / `?as=none` — иначе проверка пройдёт
  мимо продукта.
- **Не «чинить» отсутствие приглашения «Установить приложение».** Манифест есть, service worker
  сознательно НЕТ (`researches/19` §5): без него приложение ставится из меню браузера, а
  автоприглашение требует SW, и цена SW — кеш пререндеренной статики. Это `bugs/62`, решение
  владельца.
- **Не «чинить» `storage.cors.json`, если речь о загрузке фото.** Он управляет доменом
  `storage.googleapis.com`, а Firebase SDK ходит на `firebasestorage.googleapis.com` со своим
  открытым CORS — замерено заголовками (`researches/24` §7.4). Планируешь править CORS — сначала
  preflight на ТОТ хост, куда ходит код (`EXP-0112`: агент уже перемерил это зря).
- **Не ужесточать ЧТЕНИЕ аватаров сгоряча.** Правила Storage выкачены (`bugs/04` закрыт), но
  чтение сознательно оставлено паритетом с 1.x: любой вошедший. На нём держится показ лиц
  (`EXP-0043`). Перевод на модель видимости 2.0 — отдельное решение; его ОБЯЗАН уронить тест
  «ПАРИТЕТ С 1.x» в `tests/rules/storage.test.ts`, и это его работа, а не поломка.
- **Не трогать `--bar-h-fallback` «одной константой».** Значения 61/55 сняты замером на двух
  ширинах (`bugs/67`); одна константа врёт на одной из них, а ноль загоняет рельс под шапку.

### Стражи, которые надо держать зелёными

`verify-plan08` (**60**, память вида — новый) · `verify-bug81` (**28**, витрина лендинга по
СОБРАННОМУ сайту — новый) · `verify-bug80` (44, смайлики и улёт) · `verify-back-twins` (48,
«Назад» закрывает слой) · `verify-bug69` (графика, две скорости) · `verify-bug75` (44, панель) ·
`verify-bug76` (64, история) · `verify-bug84` (58 — вход гостя, две двери, врезки) ·
`verify-bug77` (64, **настоящий Chrome**) · `verify-bug87` (60, три состояния) ·
`verify-ptr` (65…66, число НЕ детерминировано — судить по «0 провалов») ·
`verify-manual-v1` (руководство, СОБРАННЫЙ сайт) · `verify-bug42` (виджет сервера, по блокам) ·
`verify-bugs08` (пачка 1 + личность новичка) · `probe-bug67` (геометрия оболочки, 3 блока) ·
`verify-pwa` (**24**, манифест и иконки, СОБРАННЫЙ сайт) · `verify-tierA` (досье связи) ·
`verify-prod-b4` (81, бой) · `npm test` (**192**) · `npm run test:calc` (**37**) · `verify-tierA` (досье связи) · `verify-bugs08`.

## 🤖 Пул автономных задач (без человека и без особого железа)

> Задачи, которые агент может делать ПОЛНОСТЬЮ автономно: код → сборка → стенд → фикс → коммит.
> Цикловые скиллы (`/autoloop`, `/dayloop`, `/nightloop`, `/guarded-loop`) мелют этот пул.

- [x] **`ideas/24`, фаза 1** — лестница построена, замер снят, макет **V4 утверждён владельцем**.
- [x] **`ideas/24`, фаза 2 — «Шаг виджета» в бою** (выкачено 2026-07-31).
- [ ] **`bugs/104` — раскрытие карточки связи по всему верху.** Самая дешёвая из открытых и
      ничем не заблокирована: разведка сделана (в 1.x весь верх БЫЛ целью — аватар жил в
      `<summary>`), путь назван, предположение по чужому фото помечено `PENDING`. Шаг 0 —
      снять площадь цели ЧИСЛОМ до кода.
- [x] **`plans/13`, фаза 1 — обзор индустрии по загрузке фото** → `researches/23` (2026-07-31).
- [x] **Эпик `plans/15`, фазы 2–5 и 8** — конфигурация, макеты, экран `/account`, смена почты,
      удаление аккаунта. Закрыты 2026-08-01.
- [ ] 🚪 **Фаза 6.5 «Ворота», первый шаг — снять `noindex` с документов** (`plans/24`, ответ
      владельца В11 «Открываем всё!»). Одна строка в `src/lib/ui/DocShell.svelte`, поиску
      открывается в 13 раз больше текста. Полностью автономно, решений владельца не требует —
      он их уже дал. Стеречь: e2e должен требовать, чтобы `noindex` остался на ЛИЧНЫХ экранах.
- [ ] **Эпик `plans/15`, фаза 6 — АВАТАР: загрузить · заменить · удалить.** Разблокирован
      2026-08-01 (правила Storage в бою, CORS проверен), но по ценности теперь ПОСЛЕ ворот:
      фотография украшает продукт, о котором никто не знает.
      Операционный план ещё не написан — он третья ступень лестницы, пишется сейчас (`plans/13`
      поглощён фазой). Готовые решения не переоткрывать: `uploadBytes` (не resumable — `PUT` в
      CORS нет) · на Safari писать **JPEG** под именем `avatar.webp` (WebP оттуда не кодируется) ·
      однопальцевая альтернатива перетаскиванию обязательна (WCAG 2.5.7 AA) · инвалидация кэша
      лиц ПО UID (`avatar.ts:35` этого ждёт).
- [ ] **`ideas/24`, фаза 3 — подсистема настроек человека (`ideas/16`).** Автономна до выката
      правил Firestore: поле настроек в документе человека, правила, тесты правил, дебаунс.
      ⚠️ Внутри висит вопрос владельцу (что главнее при расхождении — браузер или профиль) и
      выкат правил требует его слова. Сначала написать `plans/14`.
- Остальной беклог ждёт владельца или железа (эстафета, п. 5, и раздел «⛔ ЧТО ЖДЁТ ВЛАДЕЛЬЦА»
  выше): тиры `plans/07` (PWA — нужен телефон владельца, `bugs/58`/`83`; `bugs/44`/`45` —
  макеты/решения) · `bugs/74` часть А — контракт кэша (вопрос владельцу).

---

## Прочти первым: перед каждым push — `npm run kaif:check`

Скрипт падает, если в git попал файл с секретом или ПДн. Утечка здесь уже случалась (`bugs/03`) — второй
раз она не должна пройти молча.

**Состояние репозиториев:** новый публичный `MikalaiKryvusha/ndim` (чистая история) ·
приватный архив 1.x `MikalaiKryvusha/ndim-old` (0 форков).

---

## Окружение (проверено 2026-07-10)

| Инструмент | Состояние |
|-----------|-----------|
| Node.js 24.15.0, npm, git 2.43 | ✅ |
| `gh` | ✅ авторизован как `MikalaiKryvusha` (scopes: `gist`, `read:org`, `repo`) |
| `firebase-tools` 15.23.0 | ✅ установлен (переустановлен: прежний 13.18.0 был битый — не хватало `lib/templates/`) |
| Токен Firebase | ✅ владелец перелогинился 2026-07-10; `firebase projects:list` → exit 0, проект `ndim` (`ndim-space`) виден. **Агент сам залогиниться не может** — CLI требует терминал |
| `gcloud` | ✅ авторизован (`kotkrinik@gmail.com`), проект `ndim-space` активен, Firestore `FIRESTORE_NATIVE` в `eur3` |
| Java (OpenJDK 21.0.11) | ✅ установлена; в PATH попадает **в новом сеансе** оболочки. Нужна эмулятору Firestore |
| Эмулятор Firestore | ✅ порт **8181** (дефолтный 8080 занят `llama-swap`). Адрес тесты берут из `FIRESTORE_EMULATOR_HOST` |
| Docker | ✅ демон запущен (сервер 29.3.1) — понадобится в фазе 4 |
| TypeScript | ✅ исполняется Node 24 нативно; `tsc` и `@types/node` — единственные dev-зависимости |
| Playwright | ✅ 1.61.1 + Chromium установлены 2026-07-11; `npm run e2e` (сам собирает сайт и поднимает preview на :4173) |

---


**Летопись проекта** — `PROJECT_HISTORY.md`: закрытые сессии, фазы и выкаты, перенесённые отсюда
ДОСЛОВНО, свежее сверху (первая миграция — 2026-07-31, обновление KAIF 2.1). В обязательный набор
чтения не входит — открывай, когда нужна археология решения или старой фазы.
