# NDim Space — Текущий статус

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16. **Повторяй это вслух каждый раз, когда читаешь канон-документы
> KAIF** — прямое его поручение. Разбор повода — в шапке `AGENT_GUIDE.md`.
<!-- ВЕРА-КОНЕЦ -->

> Этот файл читается ИИ-агентом перед каждой задачей. Это ГЛАВНАЯ передача эстафеты между
> сессиями: новая сессия стартует с пустым контекстом и должна стать продуктивной, опираясь на
> один этот файл. Пиши конкретно — с путями к файлам и командами.
> 🧠 Принцип мышления — `PHILOSOPHY.md` (ПРОСТОТА). Рабочий фреймворк — `AGENT_GUIDE.md`.
>
> ⚠️ **STATUS — СВОДКА «СЕЙЧАС», а не летопись.** Мягкая цель ~200 строк. Каждая строка проходит
> два теста: *«убери её — ошибётся ли следующий агент?»* и *«читается ли файл за один присест?»*
> Закрытое ВЫНОСИТСЯ дословно в `PROJECT_HISTORY.md` (шаг «стрижка бонсая» в `/end-chat-soft`), а не
> копится здесь. Оставь файл таким, каким хотел бы его найти: что работает, что в работе, что
> дальше, какие капканы и ГДЕ ИСКАТЬ подробности — указатели, а не пересказы.

## ▶️▶️ ЭСТАФЕТА 2026-09-25 00:xx +03:00 — АУДИТ ОРГАНИКИ СДЕЛАН, ЧАТ ЗАКРЫТ СЛОВОМ ВЛАДЕЛЬЦА — ЧИТАТЬ ПЕРВЫМ

Слово владельца: «*давай ты аудит отчёт напишешь, обновишь планы, и этот чат закроешь, закомитишься, и продолжим в
новом чате*». Чат шёл в ОБЛАЧНОМ контейнере: бой, Search Console, PostHog и боевая база отсюда недостижимы (прокси
403), поэтому числа о бое ниже — замеры 18–21.09, свежих нет. Ветка чата — `claude/loving-allen-d5r2ik`.

**Что сделано в этом чате:**
- Аудит «куда двигаться за органикой» — `reports/KAIF_AUDIT/2026-09-24_organic_traffic_audit.md`. Главное: техника
  здорова; поиск строился под функцию продукта (10 222 карточки под ~2 000 запросов/мес), люди ищут свою ситуацию
  (тесты 2,2 млн/мес, «найти друзей / познакомиться» 10,7 млн/мес — `researches/30` прил. C; суммы Вордстата
  завышены пересечением сегментов, читать как порядок); тесты — «главное
  содержание» по Google (`researches/73` §3) — отрезаны от сайта: проверено по коду 24.09, ссылки на `/tests` и
  `/test/*` есть только в карте сайта и в перекрёстных ссылках тестов.
- Развилки владельцу: **интервью №095** (`interviews/interview_095_organic_inflow_order.md` — В1 порядок работ ·
  В2 карточки без оценок вне индекса с автовозвратом · В3 тест для двоих) и **домашка 21**
  (`homeworks/21_search_console_manual_actions_check.md` — меры, принятые вручную, минута владельца).
- Планы: `MASTER_PLAN.md` фаза 7 — строка ревизии 2026-09-24; `plans/56` и `plans/93` — ⏸ до ответа №095.
- Стоячая ложь поправлена: «работа по ролику 002 так и НЕ закоммичена» — она закоммичена в `1712c21` (21.09).

**Где мы — живое из разделов 18–21.09, перенесённых в летопись дословно этой стрижкой:**
- 🔴 **Приток из Google оборвался 14.09** (показы 2 456 → 54 в неделю, клики и до этого 4–10 в неделю, каталог за
  жизнь дал 34 клика). Техника здорова. Две версии причины — «Google свернул пробу нового домена» и «понизил за
  малую ценность» — механически неразличимы; различает кабинет → домашка 21. Разбор — `bugs/NEW_google_impressions_collapsed_14_sept.md`.
- 🔴 **Рамка владельца, дословно:** «*спасаем НЕ КАТАЛОГ. а приложение. мне нужен трафик, люди, пользователи*».
  ⛔ Не называть работу «спасением каталога».
- **Эпик притока людей — лестница `/plan-epic`, ступень 1.** Локальная разведка есть: аудит 24.09 + `researches/30`,
  `72`, `73`. НЕ сделана отраслевая веб-разведка механики распространения (как у топов вирусятся тесты для двоих и
  карточки результата, K-фактор) → `researches/74`, затем метаплан `plans/102`. Направление — после ответа №095 В1.
- **Контрольный опыт судьи 21.09** (в аудите — шаг 1): после ссылок на тесты три недели смотреть их показы; ноль —
  домену пока не дают места в выдаче, ресурс — в распространение.
- **Вход через Google** в бою с 19.09 (три бага, `signin_failed`, окно Google называет `ndimspace.app`). Обратный путь
  настоящего Google агентом НЕ пройден (нет тестового аккаунта). Документы багов 19.09 (три входа + четыре экранных)
  не закрыты DONE — ждут взгляда владельца на его телефоне и в его аккаунте.
- **Ролик 002** закоммичен (`1712c21`), ждёт трёх слов владельца: 6-я секунда «нити» или «не те» · вариант плашки
  со ссылкой (стоит V1) · регистр подписи («ты» из его черновика). Готовый файл — `ndim-studio\out\002-street-take-1\`.
- **KAIF:** дверь бюджетов `node .kaif/kaif-core.mjs check --gate-budgets` КРАСНАЯ на `MASTER_PLAN` / `TESTING_FRAMEWORK`
  / `AGENT_GUIDE` (STATUS после этой стрижки в бюджете) — перенос `plans/101` не начат, это уже второе закрытие чата
  мимо красной двери. Переход на страницу вопросов KAIF 2.7 — `plans/100`, не начат. Живая проверка хука
  `prompt-resume-word` не состоялась: этот чат открыт словом «выполни», а не `resume`.

**Очередь владельца** (`npm run questions`, 2026-09-24): интервью №095 — новое, не показано · №085 «хвосты входа по
ссылке» — ждёт с 13.09, ни разу не показано · домашка 21 · домашка 20 (ссылки с меткой в профилях площадок).

⏭ **СЛЕДУЮЩЕМУ ЧАТУ, по порядку:**
1. Правило месяца «сначала люди» (до 2026-10-18). Ответил ли владелец №095 и домашку 21? Да → разнести ответы
   (`MASTER_PLAN.md` фаза 7, `plans/56`, `plans/93`, эта эстафета) и исполнять В1. Нет → поднять страницей:
   `npm run review -- open interviews/interview_095_organic_inflow_order.md` (и №085).
2. При В1 = А: ступень 1 эпика — отраслевая разведка распространения → `researches/74`; метаплан `plans/102`; шаг 1
   аудита (ссылки на тесты с лендинга, карточки, хабов) — сначала четыре макета блока, тексты по портрету голоса.
3. `plans/101` — до следующего `/end-chat-soft`, иначе дверь бюджетов снова остановит закрытие.
4. Метрика месяца в этом чате не снята (облако): `node tools/probe-prod-stats.mjs` → `activeIn7Days`, точка отсчёта 1.

## 📜 Сервер синхронизации на PM2 (переезд 2026-09-08) — разбор перенесён в летопись дословно (стрижка 2026-09-18)

Живое: **жив ли он** — `node tools/read-server-pulse.mjs --contour prod` (0 — жив) · служба `pm2 list` →
`ndim-server-prod`, конфиг `sync-server/ecosystem.config.cjs`, лог `pm2 logs ndim-server-prod` · код боя — 🔒
замороженная копия `D:/work/ai_sandbox/ndim-server-prod`, прибита к коммиту (⛔ не переводить на рабочее дерево
«чтобы проще») · ⛔ Hyper-V и Docker выключены владельцем ради игр — обратно не включать · **порция 2 выката
(уборка гостей — УДАЛЯЕТ аккаунты) не катилась:** `plans/85`, перед ней полный проход на стенде
`SYNC_FULL_EVERY_CYCLE=1`; замер 2026-09-18 — 35 анонимных аккаунтов старше 8 дней живут в Auth · пульс никто не
смотрит регулярно — расписания нет.

## 📜 Воронка на главной прозрела (в бою с 2026-09-12) — перенесено в летопись дословно (стрижка 2026-09-18)

Капканы прибора живые: `vite preview` отказывает чужому `Host` (`allowedHosts`) — сборка под боевым именем
отдаётся ПЕРЕХВАТОМ МАРШРУТА (`e2e/root-landing-view.spec.ts`) · предохранитель `new URL(argv[1], 'file:')` на
Windows молчит с кодом 0 — только `pathToFileURL`.

## 📜 Рельсы после аудита 2026-08-28 — перенесены в летопись дословно (стрижка 2026-09-18)

Порядок работ сменён решениями владельца 2026-09-18 (интервью №092, эстафета выше; правило месяца «сначала люди»).
Из рельсов живо: **обогащение карточек `plans/56`** — 🔧 в работе, живой статус в шапке плана (шаги 3 и 6 в
очереди; хабы — `researches/57`; ⛔ откат описаний к Википедии запрещён; кликов за починку правды хабов не обещать —
`EXP-0218`) · мастерская: партия 6 — 33 из 60, возвращённые — очередь пуста на 2026-09-05
(`node tools/read-returned-candidates.mjs --contour prod`) · ⛔ инварианты исполнителям: карточку не обеднять ·
тексты лица — только через вычитку владельца · новое UI — 4 макета · выкат только `npm run deploy` через стейдж ·
оценки и похожесть наружу не отдаём (№002 В4).

## 📜 Эпик 60 — ритм сервера синхронизации (⏸ уступил рельсам 2026-08-28) — перенесён в летопись дословно (стрижка 2026-09-18)

Метаплан `plans/60`, разведка `researches/43`, фаза 1 `plans/61`; решения владельца №040 (три пересчёта эскорта
новичка · минутная пульсация ТОЛЬКО на подписках · лампочка с допуском в один такт · старожил ждёт до часа) — в
плане и в летописи. Поводы живы: `bugs/152` (2880 холостых операций), `bugs/146` (ниже). Порядок фаз: сначала
быстрый путь, потом убрать дорогой.

## 📜 Контур пополнения каталога и две правки владельца по текстам карточек (2026-08-22) — в летописи дословно (стрижка 2026-09-18)

Канон мастерской — `candidates/README.md` (сюжет вперёд, тон живой энциклопедии, доля меты — главные из четырёх
ворот, порог назначается замером). Живые запреты: ⛔ неразрывный пробел в каталоге не трогать (типографика владельца)
· ⛔ лор видеоигр не трогать (Fallout, Skyrim, Vault) · 🔑 владелец называет ОДИН случай — замер находит КЛАСС.

## 🔴🔴 ГЛАВНОЕ ОТКРЫТОЕ — `bugs/146`, и оно бьёт в цель эпика

**Индекс каталога обслуживается только в цикле, где есть грязная точка** — то есть когда кто-то
поставил оценку (`sync-server/index.mjs:1054`: ранний выход стоит ВЫШЕ `ensureDimsIndex`). Активных
за 7 дней ноль, значит новое измерение ждёт ночного прохода — **до суток**. То же и с ПРАВКАМИ:
`ensureDimsIndex` выходит по `built.docs === dimsCount`, к смене содержимого слеп вовсе.

За вечер симптом снимался **вручную дважды**: `docker restart ndim-server-prod` (при старте идёт
прогрев, а он полный проход), а для смены содержимого — ещё и снятие отметки `built` у `dims_list`.
Это костыль, а не починка.

📌 **Три пути с ценой каждого — в тикете. Рекомендация: путь B** (флаг «каталог изменился»,
1440 чтений в сутки = 2,9 % квоты против ~17 % у пути A). **Решение за владельцем.**
⛔ **Чего НЕ делать:** не писать строку индекса панелью — предзаписанная строка обнуляет прирост
дельты, и сервер уходит в полную пересборку (`plans/44` шаг 4).



## 📜 Порядок работ 2026-08-17, QA-миссия `plans/54` и расписка стейджа — перенесены в летопись дословно (стрижка 2026-09-18)

Живое одной строкой каждое: менеджер измерений — приоритет «как можно скорее» (`plans/44`, разблокирует `bugs/135`)
· `plans/58` теги жизненного цикла · `plans/70` — 510 английских заимствований, фан-аут разрешён, план заперт
предусловиями П1–П4 · ⛔⛔ откат описаний к тексту Википедии запрещён стоячим правилом · ⛔ ultracode отключён
владельцем · **QA-миссия `plans/54`:** фаза 0 (карта 139 путей `qa/JOURNEYS.md`, страж `qa-journeys-lint`, карта
ждёт правок владельца-QA) и фаза 2 (Smoke 19 кейсов, стоит В ДВЕРИ выката, К7 закрыт) сделаны; следующее — фаза 1
(тест-план, разметка `@covers`, страж покрытия); 🔴 **выкат требует поднятого стенда** (`npm run stand`) — набор
пишет в базу; **расписка стейджа живёт НА КОММИТ** (`npm run deploy -- --gate-only` — спросить, не выкатывая).

## 📜 Канон-правила 2026-08-15 — «сервер синхронизации» строгое бренд-имя · ключи ТОЛЬКО в `.env` — живут в `AGENT_GUIDE.md`; разбор перенесён в летопись дословно (стрижка 2026-09-18)

Стражи: `node tools/verify-sync-server-name.mjs` перед коммитом (ловит запрещённые имена компонента и маркер
`ИМЯ-ОК:` без причины) · ключи через `tools/lib/credentials.mjs`, контейнер — `NDIM_SA_B64` (base64 JSON, образец
`.env.example`).

## 🔐 `bugs/113` — ПРИВАТНОСТЬ: оценку можно привязать к личности (разобран, НЕ закрыт)

Канал — **дифференциальный съём по `commonSpaceSize`**: снять число общих осей по своему топу,
оценить измерение X, дождаться цикла — у кого выросло, тот и оценил. Счётчик в атаке не участвует.
✅ Правила Firestore исправны (`firestore.rules:234`), список общих осей ядро наружу не отдаёт.
Направление защиты выбрано владельцем (ловить и банить · делать съём нерациональным ПО ВРЕМЕНИ).
Работа не начата. ⚠️ Атака НЕ воспроизводилась в бою — разбор чтением кода.

> 🔴 **Ловушка, стоившая бы дорого:** соблазн прочитать чужие оценки ключом сервисного аккаунта
> (`sync-server/secrets/sa.json`, Admin SDK правила обходит). **Нельзя.** `firestore.rules:219` —
> «АДМИН — НЕ ИСКЛЮЧЕНИЕ», запрет восстановлен аудитом `bugs/100`.

## ⚠️ Операционные капканы стенда (каждый стоил ложного диагноза)

1. **`npm run e2e` требует ПОГАШЕННОГО стенда** (`EXP-0027`). 🔴 Признаку `stand.log` НЕ ВЕРЬ —
   сверяй ПРОЦЕССАМИ:
   `Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -match 'sync-server|stand\.mjs|emulators' }`,
   гаси дерево от ВЕРХНЕГО pid (`taskkill /PID <корень> /T /F`).
2. **Свой `vite preview` на 4173 подменяет сервер e2e** — поднимай с `--strictPort`. Осиротевший
   preview отдавал СТАРУЮ сборку и дал 21 ложное падение. Проверять портом:
   `Get-NetTCPConnection -LocalPort 4173 -State Listen`. Симптом класса: падения РАЗНОРОДНЫЕ и
   задевают экраны, которых правка не касалась.
3. **`npm run test:sync` сразу после гашения стенда врёт.** Воспроизведено снова 2026-08-14:
   первый прогон — 66 тестов и 1 провал, в логе «каталог 0 измерений» и чужие точки `anna, boris`;
   второй, когда порты освободились, — **71/0**. Это порты, а не продукт. Дай стенду догаснуть.
5. 🔴 **Стенд старше ~10 часов даёт ЛОЖНЫЕ КРАСНЫЕ Smoke** — Auth и Firestore расходятся
   (поднято сюда стрижкой смены 14 из закрытого раздела релиза 2.2). Занятость стенда судится по
   **КОНФИГУ** (`-c D:\worki_sandbox
dimirebase.json`) и по `OwningProcess`, а НЕ по имени
   процесса и не по диапазону портов. Долгоживущий стенд гаси и подними заново, прежде чем верить
   красному.
4. ✅ **`npm run e2e` больше НЕ выжигает чужие кадры** — `playwright.config.ts` получил
   `outputDir: 'test-results/e2e'` (`EXP-0157`). Порядок «сначала e2e, потом съёмка» помнить не надо.

## ⛔ Чего НЕ делать

- **Блок соседства:** разрешён «Похожие по каталогу» ПО КЛАССИФИКАЦИИ (№025 В7 = А); НЕ называть
  его «Рядом в Пространстве». 🔴 Инвариант «цифр метрик похожести не раскрываем» (включая порядок
  близости) в силе; IBCF — «до лучших времён».
- **Не «чинить» отсутствие строки «Обновить данные» в «Меню»** — убрана словом владельца (В1=Б).
- **Не возвращать множитель витрины лендинга** (×30) — отменён 2026-08-01 (№010 Р7).
- **Не проверять гостя без стендовых дверей** `?as=guest` / `?as=none`.
- **Не «чинить» отсутствие приглашения «Установить приложение»** — service worker сознательно НЕТ
  (`researches/19` §5), это `bugs/62`, решение владельца.
- **Не «чинить» `storage.cors.json` при загрузке фото** — SDK ходит на другой хост (`EXP-0112`).
- **Не ужесточать ЧТЕНИЕ аватаров сгоряча** — паритет с 1.x держит показ лиц (`EXP-0043`);
  тест «ПАРИТЕТ С 1.x» ОБЯЗАН уронить такую правку, и это его работа.
- **Не трогать `--bar-h-fallback` «одной константой»** — 61/55 сняты замером (`bugs/67`).
- 🔴 **Не «чинить» `inset: 0` у полноэкранных слоёв на `100vw`** — проверено пикселями, не
  работает (`bugs/65`). Разделяющий признак класса — СКОЛЛ-ЛОК, а не `inset: 0`.
