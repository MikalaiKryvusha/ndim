# NDim Space — Текущий статус

> Этот файл читается ИИ-агентом перед каждой задачей. Это ГЛАВНАЯ передача эстафеты между
> сессиями: новая сессия стартует с пустым контекстом и должна стать продуктивной, опираясь на
> один этот файл. Пиши конкретно — с путями к файлам и командами.
> 🧠 Принцип мышления — `PHILOSOPHY.md` (ПРОСТОТА). Рабочий фреймворк — `AGENT_GUIDE.md`.
>
> ⚠️ **STATUS — СВОДКА «СЕЙЧАС», а не летопись.** Мягкая цель ~200 строк. Каждая строка проходит
> два теста: *«убери её — ошибётся ли следующий агент?»* и *«читается ли файл за один присест?»*
> Закрытое ВЫНОСИТСЯ дословно в `PROJECT_HISTORY.md` (шаг «стрижка бонсая» в `/end-chat`), а не
> копится здесь. Оставь файл таким, каким хотел бы его найти: что работает, что в работе, что
> дальше, какие капканы и ГДЕ ИСКАТЬ подробности — указатели, а не пересказы.

<!-- ИМЯ-ОК: строка объявляет вычистку и обязана назвать вычищенное слово -->
**Последнее обновление:** 2026-08-16 — **сессия 17: QA-МИССИЯ НАЧАТА. Фаза 0 закрыта картой путей
человека `qa/JOURNEYS.md` — 139 путей, 13 областей, ни одного маршрута продукта без пути. Карта
ждёт взгляда владельца.**

## ▶️ С ЧЕГО НАЧАТЬ НОВОМУ ЧАТУ

🧪 **QA-МИССИЯ (`plans/54`) В РАБОТЕ. Фазы 0 и 2 (Smoke) сделаны, следующее — К7 и фаза 1.**

🔴 **SMOKE ЖИВОЙ: `node tools/smoke.mjs` — 19 кейсов, стенд 19/0 за 1.1 мин** при окне 10
(`qa/suites/smoke.md`). **Скоуп продиктовал владелец**, и он глубже «экраны открылись»: вход,
регистрация гостя с сохранением UID и труда, логин в существующий, логаут, данные, правка данных,
измерения, добавить оценку, пересчёт связей, удалить, все двери «Меню». Доказан 2 мутациями.
🔑 **Мутация «оценка не помечает точку грязной» оставляет ЭКРАН ЗЕЛЁНЫМ** — звёзды горят, карточка
на месте, а сервер синхронизации об оценке не узнает. Ловит только чтение базы: **вердикт записи
снимается с базы И с экрана**.
✅ **К7 ЗАКРЫТ: набор стоит В ДВЕРИ** — `npm run deploy` гоняет его **по собранному артефакту ДО
выката** (шаг «ВОРОТА SMOKE»). Спросить, ничего не выкатывая: `npm run deploy -- --stage --gate-only`.
Доказано мутацией двери: красный набор закрыл её кодом 1 и до `firebase deploy` не дошёл.
🔴 **ЦЕНА, КОТОРУЮ НАДО ЗНАТЬ: ВЫКАТ ТЕПЕРЬ ТРЕБУЕТ ПОДНЯТОГО СТЕНДА** (`npm run stand`) — набор
пишет в базу и ждёт цикла синхронизации, а писать этим в бой нельзя. Нет эмуляторов → дверь
ОСТАНАВЛИВАЕТСЯ, а не пропускает шаг: ворота, которые молчат, красят зелёным непроверенное.
⚠️ И это смыкается с `bugs/134` (стенд умирает от нехватки памяти): падение стенда теперь блокирует
выкат. Обход на время расследования — `NODE_OPTIONS=--max-old-space-size=6144` перед `npm run stand`.
⏭️ **Не сделано и названо:** на живых контурах `NDIM-AUTH-003`/`005` честно пропускаются (почтовые
коды берутся у эмулятора Auth).

**Фаза 0 закрыта картой путей.**
Единица покрытия названа: **путь человека** с номером `NDIM-<ОБЛАСТЬ>-<NNN>`. Карта —
`qa/JOURNEYS.md` (план фазы — `plans/55`): **139 путей** в 13 областях, Smoke **14** при потолке 15,
позитивных 98 против 41 негативного. Сторожит `node tools/qa-journeys-lint.mjs` (доказан 3
мутациями): поля, уникальность номеров, сводка против строк и **ворота фазы** — маршруты снимаются
с дерева `src/routes/`, и каждый обязан быть назван путём.
🔴 **Карта показана владельцу и ждёт его правок** — он профессиональный QA, и приёмка фазы это он.
Правки вносятся в карту, а не в спор с ней.
**Фаза 1:** тест-план, схема кейса, **разметка существующих 579 проверок** (`// @covers NDIM-…`) и
страж `verify-qa-coverage` — первое в истории проекта число покрытия. Разметка идёт ПО ОБЛАСТЯМ
карты, а не по файлам: область закрывается целиком и сразу даёт число.
⛔ **59 стражей не переписывать** — их размечают.

**Что закрыто сессией 16** (эпик `plans/53` — все девять критериев П1–П9 измерены):

1. ✅ **Стейдж больше не отстаёт** — выкачен ДВЕРЬЮ, а не руками. Замер попадания: цель отдала хеш
   собранной сборки, соседний контур (бой) остался прежним.
2. ✅ **Фаза 4** — одна дверь с параметром контура, шесть приборов `verify-prod-*` приняли `--base`,
   стражи П6 (11/0), П7 (14/0), П8 (стоп-правило, 8 случаев самопроверки).
3. ✅ **Фаза 5** — ответ №033 В5 = А стал ЗАМКОМ в двери (см. раздел ниже).
4. ⚠️ **Данные стейджа — слепок ДО миграции**, на несколько часов старше боевых. Для тестирования
   безразлично; нужен свежий — один прогон
   `node tools/migrate/seed-stage-from-prod.mjs --apply --wipe` (сухой прогон по умолчанию).
   🆕 Умолчания прибора починены (`bugs/132`): он читал базу `(default)`, которой нет с переезда.
5. 🧾 **Расписка стейджа живёт на коммит.** Она выписана на `8560966`; любой следующий коммит
   делает её просроченной, и дверь боя честно откажет со словами «на стейдже проверялся ДРУГОЙ
   коммит». Это не поломка, а сам механизм: **сначала `npm run deploy -- --stage`, потом бой.**
   Спросить, не выкатывая: `npm run deploy -- --gate-only`.
6. 🔴 **Ждёт слова владельца, но НЕ блокирует:** у замка стейджа есть аварийный обход
   `--auth-owner "<его дословные слова>"` — одноразовый, с баннером и журналом. Решение принял
   агент; доводы «за» и «против» записаны в `plans/53` → «Решения, принятые агентом без владельца».
   Одно слово владельца его отменяет.

## 🔴🔴 КАНОН-ПРАВИЛО, ЗАВЕДЁННОЕ 2026-08-15 — читать до любой работы с кодом

**«СЕРВЕР СИНХРОНИЗАЦИИ» — строгое бренд-имя, ВЕЗДЕ и на всех языках** (слово владельца):
код, переменные, модули, папки, образы, комментарии, документы, README. **Любое применение
иного термина — запрет, баг, срочное исправление.** ⛔ Запрещены как имя компонента:
~~вычислитель~~ · ~~calculator~~ · ~~calc~~. <!-- ИМЯ-ОК: STATUS объявляет запрет и обязан назвать запрещённые слова -->
Причина названа владельцем: «вычислитель» — **отсебятина, придуманная ИИ при переносе 1.x → 2.0**.
🟡 Не подпадают: «Калькулятор любви» (утверждённое имя страницы) и чужие интернет-калькуляторы.
🔒 Стережёт `node tools/verify-sync-server-name.mjs` (893 файла, доказан 2 мутациями) —
**прогоняй перед коммитом**, он же ловит маркер `ИМЯ-ОК:` без причины.
Исполнено: `calculator/` → **`sync-server/`**, `CALC_*` → `SYNC_*`, образ `ndim-sync-server`,
контейнер **`ndim-server-prod`**, лог `[sync]`, `npm run test:sync` / `npm run sync:image`.

**КЛЮЧИ ЖИВУТ ТОЛЬКО В `.env`** (слово владельца: «никогда ключи в коде и других файлах не
храним»). Файлов с ключами в проекте НЕТ. Значение — **base64** от JSON (одна строка ASCII).
Приборы берут ключ через `tools/lib/credentials.mjs` (контур в имени переменной + сверка по
`project_id`); контейнер — через `NDIM_SA_B64`. Образец — `.env.example`.
🔑 Повод был не вкусовой: боевой контейнер монтировал ПАПКУ `secrets`, и ключ стейджа оказался
внутри боевого контейнера — смешение контуров, запрещённое `plans/53`.

## 🔴 ГЛАВНОЕ ДЛЯ СЛЕДУЮЩЕГО ЧАТА — восемь строк, даже если больше ничего не прочтёшь

0. 🧪 **НОВОЕ ГЛАВНОЕ ДЕЛО — QA-МИССИЯ, заказ владельца 2026-08-15 ночью (`plans/54`).**
   Его слова: «*Наше приложение страдает от нехватки тестирования… а меня не хватает на то, чтобы
   тестировать. Нужно, чтобы ты покрывал тестами пути пользователей*». Три уровня глубины
   (**Smoke · Critical Path · Extended**), две группы (**позитив — всегда первым**, негатив),
   артефакты: тест-план, наборы, сценарии, чек-листы. Лестница пройдена: `researches/38`
   (ISTQB · ISO 29119-3 · пирамида Фаулера) → `plans/54` (8 критериев, 7 фаз).
   🔴 **Диагноз, ради которого всё затевается:** у нас **579 проверок и 59 стражей**, но они
   зовутся `verify-bug40`, `verify-bug65`… — тестирование организовано **ПО ДЕФЕКТАМ, а не по
   путям человека**, поэтому покрытие невозможно назвать, новый путь рождается непокрытым, а дыры
   находит владелец. Улика: `bugs/128` и `bugs/129` нашёл ОН, мимо них прошли все 579.
   ⛔ **59 стражей НЕ переписывать** — они доказаны мутациями; их размечают (`@covers`) и
   подключают к машинной трассировке.
   ✅ **Фаза 0 ЗАКРЫТА 2026-08-16 (сессия 17):** карта путей `qa/JOURNEYS.md` — 139 путей,
   13 областей, границы («пути, которых ещё нет» — фото, круги, дружба, чаты, отзывы) и решения,
   принятые без владельца. План фазы — `plans/55`, страж — `tools/qa-journeys-lint.mjs`.
   🔑 **Колонки «чем проверяется сегодня» в карте НЕТ намеренно:** это ручной реестр, запрещённый
   критерием К4 эпика. Ответ даёт машина в фазе 1, разбирая `@covers`.

0б. 🏗 **СТЕЙДЖ ЖИВОЙ: https://ndim-stage.web.app** (`plans/53`, заказ `ideas/36`). Отдельный проект
   `ndim-stage` на **Spark**, база `ndim-db-stage` (`freeTier: true`), правила выкачены, вход
   настроен владельцем и сверен с боем поле в поле. Приложение и сервер синхронизации умеют именованные
   базы; выбор контура делает **ХОСТ**, артефакт сборки один.
   ✅ **СТЕЙДЖ НАПОЛНЕН 2026-08-15 и СВЕРЕН ПО СЧЁТЧИКАМ КОЛЛЕКЦИЙ — расхождений 0.**
   `tools/migrate/seed-stage-from-prod.mjs` перенёс **10 605 документов** = 11 266 − 331 (`privat`,
   `bugs/127`) − 330 (`ndimids`, наследие 1.x); число в каждой коллекции сошлось с боевым.
   Личный слой обезличен (имя → синтетическое, «о себе» → пусто, фото → нет, день рождения → null
   при живых годе и месяце), **UID новые** — по решению №033 В1 = А как оно записано.
   🔴 **НЕ ПРОГОНЯЙ по стейджу `verify-db-parity` и не считай его результат поломкой.** Прибор
   судит ПУТИ и ХЕШИ, а у обезличенной копии с новыми uid они обязаны разойтись: прогон даёт
   **10 893 расхождения**, и все они объяснены по построению — 5443 «потерянных» + 5443 «лишних»
   это ровно документы, у которых uid в КЛЮЧЕ (`users` 362 · `points` 342 · `points/*/dims` 4066 ·
   `relations` 341 · `profile` 331 · `admins` 1), плюс 7 «искажённых» = 5 `suggestions` с
   перенумерованным автором и 2 документа, которые пишет сам сервер синхронизации стейджа.
   Поймано судьёй при закрытии сессии 15: прежняя формулировка «сверен, расхождений 0» провоцировала
   принять ожидаемое за аварию. **Document-level parity к обезличенной копии неприменим по
   построению** — это сказано и в шапке самого наполнителя.
   🔑 **Зарубка прошлой сессии была неправа, и это снято ЗАМЕРОМ:** она предлагала сохранить UID
   ради ссылочной целостности `friendships`/`audience`/`testPairs` — **все три коллекции в бою
   ПУСТЫ**. Ссылки на uid живут только в ключах и `relations.top[].uid`.
   ⚠️ **Наполнение стейджа — НЕ репетиция Р2** (вопреки той же зарубке): Р2 определена как копия
   ВНУТРИ боевого проекта со сверкой 100 % документов, а здесь данные обезличены и хеши заведомо
   не совпадут. Засчитать его за репетицию значило бы получить ДВЕ вместо ТРЁХ.
   ✅ **ТРИ РЕПЕТИЦИИ ПРОЙДЕНЫ** (`plans/53` фаза 2, протоколы там же): Р1 — процедура и переход
   бесплатного тира; Р2 — на боевых данных, **расхождений 0**; Р3 — откат из точки возврата,
   расхождение одно (`space/server`) и объяснено арифметикой. Прибор — `verify-db-parity.mjs`,
   доказан 3 мутациями.
   ✅ **БОЕВАЯ МИГРАЦИЯ ВЫПОЛНЕНА 2026-08-16** (AUTH: «*Да, начинай, мигрируем, даю добро*»).
   Бой живёт в **`ndim-db-prod`** (eur3, `freeTier: True`, защита от удаления возвращена).
   Сверка **11 266 = 11 266, расхождений 0** · смоук под сессией **24/0** · гостевой **81/0** ·
   людей 94 · измерений 5111 · связей 2344 — как до переезда. Простой — окно 31 минута при
   потолке 60. Необратимый шаг выполнял ВЛАДЕЛЕЦ скриптом `tools/migrate/run-prod-migration.ps1`:
   удаление боевой базы блокирует классификатор безопасности, и это законный рубеж.
   ✅ **Второй контейнер `ndim-server-stage` работает** — стейдж считает связи на своих данных.
   🔴 **ИМЯ БАЗЫ ЖИВЁТ В ДВУХ МЕСТАХ И ОНИ СВЕРЯЮТСЯ САМИ:** `src/lib/firebase.ts` (приложение) и
   `tools/lib/contours.mjs` (приборы). Второй читает первый и ПАДАЕТ при расхождении — правишь
   одно, правь другое.
   🔴 **Правила Firestore катает ТОЛЬКО дверь** (`npm run deploy`, первым шагом). Голый
   `firebase deploy` запрещён хуком, а до 2026-08-16 дверь правила не катала вовсе — выкатить их
   в бой было нечем. ⚠️ `--only firestore` ЦЕЛИКОМ: подтаргет `firestore:rules` при массивной
   форме завершается успехом и не катит НИЧЕГО (`firebase-tools#10447`).
   ✅ **ЭПИК ЗАКРЫТ 2026-08-16 (сессия 16):** фазы 4 и 5 пройдены, все девять критериев П1–П9
   измерены. Названный долг ровно один: не замерено, шлёт ли preview-канал `X-Robots-Tag` сам —
   каналы в работе не используются, и до первого их использования ответ ничего не меняет.

1. 🚪 **ВОРОТА ОТКРЫТЫ И ЖДУТ РУК.** Тексты обоих языков приняты и лежат в
   `src/lib/content/landing-copy.ts`; композиция выбрана владельцем — **V4 «Игра сама вносит
   внутрь»**. 🔴 **Следующий шаг ворот — НЕ вёрстка в одиночку, а мост эпика `plans/23`:** выбрав
   V4, владелец подтвердил №009 В3 — большой кнопки входа гостем нет, человека вносит внутрь сама
   панель, появляющаяся после звёзд. Дверь гостя в бою РАБОТАЕТ (`/profile?guest=1`), спрятана
   только тропинка к ней. Подробности — раздел 0 ниже.
   ⚠️ **Класс «замок пережил своё снятие» теперь ловится машиной:** `npm run questions` →
   «ПРОСРОЧЕННЫЕ ЗАМКИ». Прежде чем поверить строке «⛔ заблокировано» — прогони страж.

1. 🔴🔴 **ВЫКАТ ТОЛЬКО ЧЕРЕЗ `npm run deploy`. `firebase deploy` РУКАМИ ЗАПРЕЩЁН** (хук
   `.claude/hooks/deploy-guard.mjs` его блокирует). Дверь сама: замок стейджа → стоп-правило
   контура → правила Firestore → чистая сборка → **проверка, что в бандле ОДИН
   `__sveltekit_<хеш>`** → выкат → **замер попадания в контур** → заголовки → 🔑 **смоук ПОД
   СЕССИЕЙ** → гостевой смоук; красный шаг останавливает выкат. Канон — `AGENT_GUIDE.md` →
   «ВЫКАТ В БОЙ».

   🆕 **ДВА КОНТУРА — ОДНА ДВЕРЬ (сессия 16):** `npm run deploy` — бой, `npm run deploy -- --stage` —
   стейдж, `npm run deploy -- --gate-only` — спросить замок, ничего не выкатывая.
   🔴 **В БОЙ — ТОЛЬКО ЧЕРЕЗ СТЕЙДЖ** (интервью №033 В5 = А). Дверь боя требует расписку
   `.kaif/stage-receipt.json` с ТЕМ ЖЕ коммитом и чистым деревом. Незакоммиченная правка — это по
   определению код, которого на стейдже не было, и дверь так и скажет. Обход — только
   `--auth-owner "<дословные слова владельца>"`, ОДНОРАЗОВЫЙ.
   ⚠️ Честная граница: бой собирается со свежим снимком витрины лендинга, стейдж — нет (П9). Замок
   стережёт КОД, а не число на витрине.

   **Почему так, цена урока (ночь 2026-08-15).** Агент выкатил прод ТРИЖДЫ, проверяя только
   гостевые публичные смоуки. В бою всё это время лежало приложение, которое **не стартовало
   вовсе** (`TypeError … reading 'data'`), и нашёл это ВЛАДЕЛЕЦ. Причин было две: `build/` не
   очищалась и в бой уехала СМЕСЬ двух сборок (два разных хеша рантайма) · в `firebase.json` не
   было `headers`, хостинг кешировал час всё подряд. Оба вылечены (`bugs/124`), лечение
   механическое: `prebuild` сносит `build/`, дверь проверяет целостность ДО выката.
   Слово владельца: «**деплой без тестирования это пердёж в лужу, а не работа**».
2. 🆕 **Дефект, заведённый ЧТЕНИЕМ, живёт до тех пор, пока его не начнут НАБЛЮДАТЬ** — `bugs/120`
   простоял так сутки и вскрылся, лишь когда строка выросла. Разбор такта Г и `bugs/125`/`126`
   перенесён в летопись (сессия 15).
3. 🆕 **Смоук боя под сессией `verify-prod-signed-in` (24/0) — обязателен после КАЖДОГО выката**
   и встроен в дверь. Его первый прогон нашёл `bugs/125`, которого не видел ни один прибор.
4. 🔧 **Очередь починки `plans/49`: 8 из 13 закрыто** (`bugs/65` · `plans/12` · `plans/28` ·
   `bugs/117` · `plans/20` · 🆕 **`plans/47`** · 🆕 **`plans/42`** · 🆕 **`plans/46`**) плюс
   `bugs/122`, `bugs/120`, `bugs/126` вне очереди. **Осталось 5.**
   🆕 **ГОЛОВА ОЧЕРЕДИ ЗАКРЫТА 2026-08-16 (сессия 18):** построен страж `verify-test-pages`
   (**89** проверок по собранному сайту, 3 мутации: 2 · 6 · 1), `/fable-judge` по фазе 5 дал
   **VERIFIED WITH CAVEATS**, владелец принял фазу (интервью №034, В2 = Б) с условием про перенос
   длинных названий — условие исполнено замером и кадрами. `plans/42` помечен `DONE`.
5. 🔴 **План починки из `plans/49` — ГИПОТЕЗА, а не инструкция, и счёт уже 3:0 в пользу замера.**
   У `bugs/65` ложными были 2 шага из 4; у `bugs/117` ложным оказался ВЕСЬ диагноз (вину
   возлагали на восстановление прокрутки, а `scrollY` — ноль все 60 кадров); у `plans/42` план
   требовал проверить гостя в бою дверью `?as=none` — **в бою этот параметр не читается вовсе**
   (`profile.ts:97`, дверь только на стенде), состоянием «аккаунта нет» служит свежий контекст.
   Форензика снималась ЧТЕНИЕМ КОДА. **Меряй прежде, чем поверишь.**
6. 🆕 **Чтобы наблюдать сбой посреди операции — ломай КОД, а не окружение** (`EXP-0159`). Обрыв
   сети роняет вместе со шагом и саму страницу: так хвост `plans/20` простоял две недели
   «структурной гарантией». Временный `throw` в нужном шаге снимается одной правкой и не
   оставляет в продукте флага «для стенда». И проверку пиши ИНВАРИАНТОМ («не бывает, чтобы вход
   отобрали при живых данных»), а не описанием одного исхода: инвариант зелен в обоих исправных
   исходах, поэтому его не приходится отключать.
7. 🔴 **У ВИДИМОГО дефекта приёмка ПИКСЕЛЬНАЯ, а не габаритная.** `boundingBox`, ставший равным
   ширине окна, — это габарит; спор о том, что человек ВИДИТ. Я успел написать габаритный страж и
   поверить ему (`EXP-0155`). Контрольный кадр обязателен (`EXP-0082`).
8. 🔴 **Журнал опыта — ПОСЛЕДНЕЕ средство.** Владелец поймал: «пишешь опыт и это впустую — ты им не
   пользуешься». Порядок: **убрать ловушку → страж → и только потом запись** (`AGENT_GUIDE.md` →
   «Журнал опыта», `EXP-0157`). Урок про ДЕЙСТВИЕ живёт в строке этого действия в реестре
   инструментов: греп по тегам ЗАДАЧИ его не достаёт никогда.
9. 🔴🆕 **`probe-test-guest-live` ПИШЕТ В БОЕВУЮ БАЗУ — прочти его шапку ПРЕЖДЕ, чем запускать.**
   Оценка гостя настоящая и двигает NDSR; прибор убирает за собой органами продукта, и уборка
   живёт в `finally`. 🔑 **Ловушка, уже стоившая трёх настоящих гостей в проде:** продукт строит
   личную ссылку пары из БОЕВОГО `canonical`, поэтому стендовый прогон, пойдя по ней буквально,
   уводит второго гостя в бой. Лечение вшито в прибор (перенос ссылки на адрес стенда), но
   помни класс: **стендовый прогон может утечь в бой через данные, которые продукт отдаёт**.

## 🏁 ЭСТАФЕТА — с чего начать

**Первым делом:** `AGENT_GUIDE.md` + этот файл + `npm run questions`.
Снято 2026-08-16 (сессия 17): интервью **33**, **ждут владельца 0** · новых нарушений **0** ·
унаследованный долг: вопросов вне `interviews/` **25**, неразнесённых ответов **25** ·
просроченных замков **0**. Долг за сессию не вырос и не убыл.

🆕 **СДЕЛАНО СЕССИЕЙ 18 (2026-08-16):** голова очереди починки закрыта целиком — страж
`verify-test-pages` (89/0, 3 мутации), `/fable-judge` по фазе 5 (VERIFIED WITH CAVEATS, все
перепрогоняемые числа воспроизведены: правила 118/118 · сервер синхронизации 77/77 · бой 240/0),
приёмка владельца (№034) и закрытие `plans/42`. Заведён замер `tools/measure-bug120-wrap.mjs`.
В `bugs/134` внесён замер по шести прогонам: **растёт только `vite dev`, и прирост затухает** —
гипотеза «копит стенд» опровергнута, падение не воспроизведено, виновник НЕ назван.
🔴 **Урок сессии, оплаченный ошибкой судьи:** «нет записи» — утверждение о ПОИСКЕ, а не о мире.
Судья обвинил выкат в самоуправстве, поискав AUTH только в телах коммитов; разрешение лежало в
`bugs/120` и `bugs/126` («в бою с 2026-08-15 ≈11:25, AUTH: „деплой“»). Прежде чем обвинять — назови,
где искал.

🎯 **ГДЕ ПРОДОЛЖИТЬ (сессия 17 → 18):**

0. 🔴🔴🔴 **ГЛАВНЫЙ ПРИОРИТЕТ СМЕНЁН ВЛАДЕЛЬЦЕМ 2026-08-16 11:01 — РОСТ ЧЕРЕЗ SEO, по ЛАГУ
   СОЗРЕВАНИЯ** (интервью **№035, В1 = А**; заказ и три уточнения — в шапке интервью).
   Его слова, снимающие половину прежнего порядка: «*в день выката пока неактуально — у нас нет
   живой аудитории, приложение сейчас мертво без органического трафика и SEO*»; и критерий
   сортировки: «*что нужно быстрее брать в работу, что долго „зреет“ в поисковых движках*».
   🔑 **Правило груминга, выведенное из этого:** при пустой аудитории ценность работы =
   **масса поверхности × лаг созревания**. Работа с нулевым лагом (ворота, онбординг, конверсия)
   от ожидания НЕ дешевеет — SEO-работа дешевеет каждый день.
   **Порядок работ, утверждённый владельцем (В1 = А):**
   · **1) фаза 3 эпика 40** — обогащение каталога (`plans/40` §Фаза 3): одним заходом влияет на
     **10 222 уже проиндексированные** страницы; разблокирована, решений владельца не требует
     (№025 В1 = А, В6 = А). **Это работа №1 сессии 19.**
   · **2) фаза 4 эпика 40** — опорные страницы: черновики пишет агент по `AUTHOR_STYLOMETRY.md`,
     владелец вычитывает страницей ОДИН раз (№025 В4 = А). Идёт ПАРАЛЛЕЛЬНО фазе 3.
   · **3) `ideas/37`** — миссия чтения консолей: Search Console хранит 16 месяцев и прошлое не
     восстанавливает, а фаза 8 требует ≥3 датированных замеров — без прибора каталог нечем
     принять. · **4) `ideas/33`** (язык оригинала) · **5) фаза 6** (распространение, разведка).
   ⏸ **Отложено словом владельца:** ворота и онбординг (`plans/21`/`22`/`23`), сегментация
   sitemap (замер: 10 432 адреса из 50 000 и 0,84 МБ из 50 — потолок далеко), `plans/38`,
   `ideas/38`. `bugs/121` — попутно внутри фазы 3, отдельным заходом не стоит.

0б. 🆕 **ДВА НОВЫХ ПОРУЧЕНИЯ ВЛАДЕЛЬЦА, отданные вместе с ответом №035** (дословно там же):
   · ✅ **СДЕЛАНО в сессии 18:** «*там сейчас "менеджер измерений", а я хотел Панель
     Администратора - общую. И через нее открывается панель, и там внутри разные инструменты
     администратора, один из них - Менеджер измерений*». Иерархия исправлена: дверь в «Меню» и
     заголовок раздела — **«Панель администратора»**, внутри — список инструментов, первый и пока
     единственный «Менеджер измерений» с честной пометкой «скоро». Страж `verify-admin-home`
     обновлён и проверяет ОБА имени. ⚠️ **`[NOT-TESTED]`** — страж требует живого стенда, а он
     умирает (`bugs/134`); перепрогнать первым делом, когда стенд поднимется.
   · ⏭ **«Менеджер измерений» — В РАННИЕ ПРИОРИТЕТЫ, но НИЖЕ роста:** «*Нужно тоже взять в ранних
     приоритетах менеджер измерений, чтобы я мог пополнять измерениями пространство. Но названные
     тобою вещи - выше по приоритету*». Это комната «Измерения» — план `plans/44` (28 шагов, не
     начат), фаза 4 эпика `plans/30`. Ставится сразу ПОСЛЕ пунктов 1–2 порядка выше.
     🔑 Цена ожидания названа владельцем прямо: пока комнаты нет, **пополнять каталог измерениями
     он не может вовсе** — а каталог это и есть та масса, которая зреет в поиске.

0в. 🕮 **История: слово владельца 2026-08-16 утром — сессия 18 вернулась к починке багов**, дословно:
   «*в следующем чате можем вернуться к фиксам багов, на чем остановились сильно раньше? и уже
   будем фиксить в стейдже, тестить на стейдже, и только потом катить в прод, как настоящие
   профессионалы*». **Это приоритет №1 сессии 18** — вперёд фазы 1 QA-миссии.
   · **Точка возврата — очередь починки `plans/49`** (раздел 1 ниже): 5 из 13 закрыто, **осталось
     8**; у головы очереди `plans/42` один хвост — страж `verify-test-pages` (`plans/47`).
   · **Порядок работ теперь такой и он МЕХАНИЧЕСКИЙ, а не на честном слове:** правка → коммит →
     `npm run deploy -- --stage` → проверка на стейдже → `npm run deploy` в бой. Дверь боя требует
     расписку стейджа с ТЕМ ЖЕ коммитом и чистым деревом; ворота Smoke гоняют набор по собранному
     артефакту ДО выката. Обойти можно только словом владельца, и обход одноразовый.
   · ⚠️ **Расписка стейджа сейчас ПРОСРОЧЕНА** (выписана на `8560966`, HEAD `1a18ece`) — первый же
     выкат в бой честно откажет со словами «на стейдже проверялся ДРУГОЙ коммит». Это не поломка, а
     сам механизм: сначала стейдж.
   · 🔴 **Предусловие, которое встанет поперёк в первый же день: `bugs/134`.** Ворота Smoke требуют
     живого стенда, а он умирает от нехватки памяти. Пока причина не найдена — поднимать стенд с
     `NODE_OPTIONS=--max-old-space-size=6144`.
   · ⚠️ Планы починки в `plans/49` — **гипотезы, а не инструкции**, счёт 3:0 в пользу замера
     (см. пункт 5 раздела «ГЛАВНОЕ» выше). Меряй прежде, чем поверишь.

1. **Фаза 1 QA-миссии — разметка `@covers` и страж покрытия.** ⏸ Уступает пункту 0 по слову
   владельца, но остаётся следующей по ценности работой QA-миссии. Единица покрытия названа и принята
   владельцем, набор Smoke работает и стоит в двери. Следующее — пройти 59 стражей, 13 e2e-спек,
   284 юнита, 118 правил и 77 тестов сервера синхронизации **по областям карты** (не по файлам!) и
   проставить `// @covers NDIM-…`, затем написать `tools/verify-qa-coverage.mjs` — он напечатает
   первое в истории проекта число покрытия. ⛔ 59 стражей НЕ переписывать.
2. 🔴 **`bugs/134` — стенд умирает от нехватки памяти, и теперь это блокирует ВЫКАТ** (ворота Smoke
   требуют живого стенда). Причина не установлена; в баге записана процедура замера: гонять смоук в
   цикле и смотреть `WorkingSetSize` по процессам — виновник тот, чей растёт. Обход на время
   расследования: `NODE_OPTIONS=--max-old-space-size=6144` перед `npm run stand`.
3. **Живые контуры для Smoke.** На стейдже и в бою почтовых кодов эмулятора нет, поэтому
   `NDIM-AUTH-003` и `NDIM-AUTH-005` там честно пропускаются. Решается замером: либо почтовый ящик
   прогона, либо явное разделение «стендовый полный / живой сокращённый».
4. **За владельцем (не блокирует):** `ideas/37` (миссия исследования поисковых консолей) и
   `ideas/38` (продуктовая аналитика вместо Amplitude) — оба его заказы, оба ждут очереди. У
   `ideas/38` внутри развилка: сторонний трекер отменяет его же решение №004 В4 «воронка своя, без
   трекеров».

⚠️ **Чего НЕ делать следующей сессии:** не «чинить» пропуск `NDIM-AUTH-003`/`005` на живых контурах
подкладыванием сессии снаружи — набор входит НАСТОЯЩИМ путём человека, и подмена сделала бы его
вакуумным (`EXP-0087`).

🆕 **Дефекты, заведённые в сессиях 14–16:**
· **`bugs/133`** ✅ (сессия 16) — стейдж не повторял боевых переадресаций: 10 адресов отдавали 404
  вместо 301. Нашлось ПЕРВЫМ ЖЕ прогоном прибора, научившегося `--base`. Цена расхождения выросла в
  тот же день: с фазы 5 прогон по стейджу обязателен перед выкатом, то есть правка переадресаций
  уехала бы в бой неотрепетированной. Лечение — копия ВМЕСТЕ со стражем расхождения.
· **`bugs/132`** ✅ (сессия 16) — пять приборов `tools/migrate/` ссылались на УДАЛЁННЫЙ файл ключа
  (`sync-server/secrets/sa.json`) и базу `(default)`, которой нет с переезда фазы 3. `STATUS` звал
  запускать два из них как есть. Класс — «истина ↔ зеркало»: модуль контуров завели, а приборы
  `migrate/` к нему не подключили. Проверено наблюдением: зонд коллекций читает `ndim-db-prod`.
· **`bugs/130`** ✅ и **`bugs/131`** ✅ (сессия 15) — отпечаток базы считал каталог ДВАЖДЫ, и ложное
  число уже уехало в `plans/53` как ожидание для критерия П1; второй объясняет, почему первый
  прожил незамеченным — сырые байты NUL делали исходник невидимым для `git diff` и `grep`.
· **`bugs/127`** (открыт) — в боевой базе живут `users/{uid}/privat/{uid}`: **331 документ**, поле
  `user_secret_key` непусто у всех. Ни `firestore.rules`, ни `schema.ts` о них не знают; закрыты
  лишь финальным deny-all. `researches/02` звал это «мёртвой заглушкой» — замер уточнил: заглушка
  пуста, а секреты живые. Класс `bugs/108`/`109`. **Миграция их переносит как есть — она ничего
  не чистит**, иначе сверка «ни один документ не потерян» не может дать 100 %.
· **`bugs/128`** (починен) — дверь вычитки умирала сразу после открытия браузера: в `process.exit()`
  уезжал адрес страницы. Владелец прислал кадр `ERR_CONNECTION_REFUSED`.
· **`bugs/129`** (починен) — на стейдже не войти: ссылка в письме строилась из БОЕВОГО домена.
  🔑 Урок класса: **условие «всё, что не X, — это Y» живёт ровно до появления Z**. При добавлении
  третьего чего-либо (контура, языка, роли) обход идёт не по новому коду, а по СТАРЫМ бинарным
  развилкам — их формулировка и есть список мест, где третье сломает продукт.
⚠️ **Считай «неразнесённых 25» счётчиком долга, а не доказательством разноса** — страж мерит
цитату рядом с номером интервью, и реестр-таблица гасит его, не сдвинув ни одного документа
(поймано на себе, `plans/50`).

🧱 **ДВА КОНТУРА С 2026-08-15.** Бой — проект `ndim-space` (**Blaze**, замер `billingEnabled: true`;
предпосылка «мы на бесплатном плане» неверна, но бесплатные квоты действуют и считаются **НА
ПРОЕКТ**). Стейдж — `ndim-stage` (**Spark**, бесплатен буквально). Выкат на стейдж:
`firebase deploy --config firebase.stage.json --project ndim-stage --only hosting` — хук
`deploy-guard` пропускает ТОЛЬКО явно названный стейдж, бой по-прежнему заперт за `npm run deploy`.
⚠️ Удалены: учебный проект `sandbox-ee18a` и мусорная база `sandbox2` (обе — словом владельца).

🔧 **Окружение на момент закрытия сессии 15:** стенд НЕ поднимался (эмуляторы гасились сами через
`emulators:exec`), сирот нет. **Оба контейнера сервера синхронизации РАБОТАЮТ:**
`ndim-server-prod` (проект `ndim-space`, база `ndim-db-prod`) и `ndim-server-stage`
(`ndim-stage` / `ndim-db-stage`), оба на образе `ndim-sync-server` билд 31, версия 2.1.0.
Ключи оба берут из `.env` переменной `NDIM_SA_B64`.

🚀 **Бой на 2026-08-16, ~00:53 — выкачен ПОСЛЕ миграции и проверен дверью:** правила и индексы ·
целостность сборки · заголовки · **смоук под сессией 24/0** · гостевой **81/0**.
Боевая база — **`ndim-db-prod`** (eur3, `freeTier: True`, защита от удаления ВКЛЮЧЕНА).
Точки возврата в `gs://ndim-space-backup/`: `2026-08-15-pre-migration` и `2026-08-16-migration`
(последняя снята при замороженной записи непосредственно перед переездом).

⚙️ **РЕЖИМ УСИЛИЙ (слово владельца, сессия 8):** по умолчанию **max**. **ultracode + workflows**
владелец заранее НЕ включает — **агент обязан САМ попросить**, назвав, что даст фан-аут. Не
попросил — работаешь на max в одиночку; молчаливое «я бы сделал лучше многоагентно» не считается.
📌 Ревизия беклога и аудит кода прошли многоагентно с его прямого разрешения — прецедент есть.

**Что делать, по убыванию ценности:**

### 0. 🚪 ВОРОТА — фазы 2 и 3 ЗАКРЫТЫ, фаза 4 ждёт моста эпика `plans/23`

Тексты обоих языков приняты (`src/lib/content/landing-copy.ts`), композиция выбрана владельцем —
**V4 «Игра сама вносит внутрь»** (`plans/52`, В1 = Г). Разбор решений и цена уроков перенесены в
летопись (сессия 15); здесь остаётся только действенное:

🔴 **Фаза 4 ЗАВИСИТ от моста эпика `plans/23`.** Выбрав V4, владелец подтвердил №009 В3: большой
кнопки входа гостем нет вовсе — человека вносит внутрь панель, появляющаяся после звёзд. Это мост
`plans/23`, а не украшение лендинга; строки `gateCopyPhase4.cta` ждут там же. Дверь гостя в бою
РАБОТАЕТ (`/profile?guest=1`), спрятана только тропинка к ней.

⚠️ **В3 и В4 интервью №033 остались без ответа** (владелец ответил 2 из 4); оба выведены из выбора
V4 и помечены `вид=выведено-из-решения` в `plans/52` — строка нуля СКРЫТА, дверь ПОЯВЛЕНИЕМ.
Ждут одного слова, но НЕ блокируют.

🔴 **Хвост, принятый с открытыми глазами:** активных за 7 дней сегодня **ноль**, и блок живых
метрик скажет это вслух в самом уязвимом месте воронки. Как выглядит строка при нуле — вопрос
макетов фазы 4.

Фаза 7 (отзывы) отложена владельцем (№010 Р9) — приёмка ворот её не ждёт.

### 1. 🔧 Очередь починки `plans/49` — 8 позиций (главное дело этого этапа)

Слово владельца: «нужно будет грумить баги и фиксить. **бесит тех долг**». Очередь отсортирована от
дешёвого к дорогому, у каждой позиции форензика и план. ⚠️ Планы читай как гипотезы (см. п. 3 выше).
🆕 **`bugs/123`** (заведён, не чинен): довод про `100vw` живёт комментарием в `src/app.html` у
загрузочного щита, а `verify-bug40` стережёт там только ЧИСЛО. Ключевой вопрос замера — жива ли
полоса прокрутки в момент показа щита.

### 2. Хвост фазы 2 эпика 40 «Архитектура веса» (`plans/48`)

Главное число фазы взято: **недостижимых страниц каталога от `/ru` и `/en` — 0** (было 10 222),
карточка в **3 клика**, минимум входящих у карточки **3**. Осталось: шаг 5 (сегментация
`sitemap.xml`; JSON-LD сделан) · шаг 6 (страж `verify-catalog-hubs` + мутации) · шаг 7 (приёмка и
выкат — только с AUTH). Подробности шагов 2–4 и три находки на боевых данных — в летописи.
🟡 **`bugs/121`**: недостижимых от языковых главных осталось 23, каталога среди них 0, но **19 —
публичные** (всё семейство «Тест», английские документы, обе `delete-account`). Все три лечения
правят ЛЕНДИНГ (`plans/21`) — за владельцем.

### 3. ✅ Фаза 5 «ТЕСТ» — ЗАКРЫТА 2026-08-16 (сессия 18), `plans/42_DONE_…`

Страж `verify-test-pages` построен (89 проверок, 3 мутации), `/fable-judge` пройден, владелец
принял фазу (№034 В2 = Б) с условием про перенос длинных названий — исполнено замером
`tools/measure-bug120-wrap.mjs` (390/1440 × обе темы, предел каталога 113 и 160 знаков: обрезки
нет, 4–5 строк, кадры смотрены). Осталось за владельцем и НЕ блокирует: живая проба пути гостя
его руками. Ниже — прежний текст раздела как история.

### 3б. Фаза 5 «ТЕСТ» в бою — как выглядел хвост до закрытия

4 страницы (`/{lang}/tests` · `/test/compatibility` · `/test/personality` · `/test/love`), один
движок, один гостевой NDim ID. План — `plans/42`. ✅ Такт Г закрыт 2026-08-15 (приёмка боя,
2 найденных дефекта починены и выкачены), ✅ PENDING гигиены пар снят (`cleanupStalePairs`).
Осталось:
· **слабая модель** (пул `plans/45`, по порядку): `plans/47` → `plans/44` → `plans/46`.
  🔑 `plans/47` — единственное, что держит фазу открытой; если пул не двигается, сильная модель
  берёт его сама (он же позиция №3 очереди `plans/49`);
· **владелец** (не блокирует): живая проба — пройти тест и позвать второго;
· **сильная модель:** `/fable-judge` по такту Г и закрытие шага 5 `plans/42`.
📊 Индексация: `/ru/test/love` в индексе Google уже на вторые сутки. Позиции — другое и дольше
(первые НЧ — зима 2026/27); индекс это вход в лотерею, а не выигрыш.

### 4. За владельцем, НЕ блокирует

`plans/39`: боевая приёмка (ru-память на `/en` → остаётся английский) + вычитка EN-обвязки
(условие В2 = А) · текст п. 2.2 «Политики» (правится ИСТОЧНИК `researches/06`, не `docs.ts`) ·
развилка `ideas/31` · `homeworks/08` шаг 3.
**Полный список ждущего владельца** — очередь владельца в `plans/49` (63 позиции) плюс
перенесённый дословно список от 30.07 в `PROJECT_HISTORY.md` (ничего не потеряно).

### 5. Два эпика спланированы и НЕ начаты

`plans/37` (соседство измерений, IBCF — «делаем позже»; фаза 1 — замер плотности, до её чисел кода
не писать) · `plans/38` (оживить теги и вид). ⛔ Предусловие `plans/38` — прополка `plans/30`.
🔴 Замер: теги строками в индекс **не влезают** (1073 КБ при потолке 1024).

---

## 📊 Боевые цифры 2026-08-14 (чтобы не перемерять)

Людей **94** · оценок **4064** · измерений **5111** · связей **2344** · `dims_list` 566 КБ ·
активных за 7 дней **0** · новых за 30 дней **1**. Прибор — `node tools/probe-prod-stats.mjs`
(только читает, анонимную учётку удаляет за собой; AUTH — интервью №013 В2 = Б).

🆕 **Агрегат ВЕРШИН топов** — главное число для калибровки текстов (`plans/28` §9):

| Что видит человек, открыв «Связи» | Значение |
|---|---|
| **Медиана верхней строки** | **7 %** |
| Средняя верхней строки | 11 % |
| Максимум по Пространству | **68 %** |
| Людей с непустым топом | 93 из 94 |
| Распределение вершин | low **95 %** · middle 2 % · high 0 % · upper 3 % |

🔴 **Как НЕ пользоваться:** не путать с `avgSimilarity` **3 %** — это среднее по ВСЕМ строкам всех
топов вместе с хвостом, другой вопрос (`EXP-0124`); и не обобщать **24 %** владельца — он выше
медианы, а не типичен. ⚠️ Наружу не показываем (интервью №014, В3 = А).

## 🔐 `bugs/113` — ПРИВАТНОСТЬ: оценку можно привязать к личности (разобран, НЕ закрыт)

Канал — **дифференциальный съём по `commonSpaceSize`**: снять число общих осей по своему топу,
оценить измерение X, дождаться цикла — у кого выросло, тот и оценил. Счётчик в атаке не участвует.
✅ Правила Firestore исправны (`firestore.rules:234`), список общих осей ядро наружу не отдаёт.
Направление защиты выбрано владельцем (ловить и банить · делать съём нерациональным ПО ВРЕМЕНИ).
Работа не начата. ⚠️ Атака НЕ воспроизводилась в бою — разбор чтением кода.

> 🔴 **Ловушка, стоившая бы дорого:** соблазн прочитать чужие оценки ключом сервисного аккаунта
> (`sync-server/secrets/sa.json`, Admin SDK правила обходит). **Нельзя.** `firestore.rules:219` —
> «АДМИН — НЕ ИСКЛЮЧЕНИЕ», запрет восстановлен аудитом `bugs/100`.

## ⚠️ Операционные капканы стенда (каждый стоил ложного диагноза)

1. **`npm run e2e` требует ПОГАШЕННОГО стенда** (`EXP-0027`). 🔴 Признаку `stand.log` НЕ ВЕРЬ —
   сверяй ПРОЦЕССАМИ:
   `Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -match 'sync-server|stand\.mjs|emulators' }`,
   гаси дерево от ВЕРХНЕГО pid (`taskkill /PID <корень> /T /F`).
2. **Свой `vite preview` на 4173 подменяет сервер e2e** — поднимай с `--strictPort`. Осиротевший
   preview отдавал СТАРУЮ сборку и дал 21 ложное падение. Проверять портом:
   `Get-NetTCPConnection -LocalPort 4173 -State Listen`. Симптом класса: падения РАЗНОРОДНЫЕ и
   задевают экраны, которых правка не касалась.
3. **`npm run test:sync` сразу после гашения стенда врёт.** Воспроизведено снова 2026-08-14:
   первый прогон — 66 тестов и 1 провал, в логе «каталог 0 измерений» и чужие точки `anna, boris`;
   второй, когда порты освободились, — **71/0**. Это порты, а не продукт. Дай стенду догаснуть.
4. ✅ **`npm run e2e` больше НЕ выжигает чужие кадры** — `playwright.config.ts` получил
   `outputDir: 'test-results/e2e'` (`EXP-0157`). Порядок «сначала e2e, потом съёмка» помнить не надо.

## ⛔ Чего НЕ делать

- **Блок соседства:** разрешён «Похожие по каталогу» ПО КЛАССИФИКАЦИИ (№025 В7 = А); НЕ называть
  его «Рядом в Пространстве». 🔴 Инвариант «цифр метрик похожести не раскрываем» (включая порядок
  близости) в силе; IBCF — «до лучших времён».
- **Не «чинить» отсутствие строки «Обновить данные» в «Меню»** — убрана словом владельца (В1=Б).
- **Не возвращать множитель витрины лендинга** (×30) — отменён 2026-08-01 (№010 Р7).
- **Не проверять гостя без стендовых дверей** `?as=guest` / `?as=none`.
- **Не «чинить» отсутствие приглашения «Установить приложение»** — service worker сознательно НЕТ
  (`researches/19` §5), это `bugs/62`, решение владельца.
- **Не «чинить» `storage.cors.json` при загрузке фото** — SDK ходит на другой хост (`EXP-0112`).
- **Не ужесточать ЧТЕНИЕ аватаров сгоряча** — паритет с 1.x держит показ лиц (`EXP-0043`);
  тест «ПАРИТЕТ С 1.x» ОБЯЗАН уронить такую правку, и это его работа.
- **Не трогать `--bar-h-fallback` «одной константой»** — 61/55 сняты замером (`bugs/67`).
- 🔴 **Не «чинить» `inset: 0` у полноэкранных слоёв на `100vw`** — проверено пикселями, не
  работает (`bugs/65`). Разделяющий признак класса — СКОЛЛ-ЛОК, а не `inset: 0`.

## Стражи, которые надо держать зелёными

`npm test` (**284**) · `npm run test:sync` (**77**) · `npm run check` · `npm run typecheck` ·
🆕 **ЯРУС 2 ГРАФИКИ** (`plans/46`): `node tools/verify-tier2-prefetch.mjs` (**7**, по собранному
сайту + preview: нет префетча в сыром HTML И есть 3 ссылки в живом head, `saveData` — ноль;
2 мутации) ·
🆕 **СЕМЕЙСТВО ТЕСТ** (`plans/47`, голова очереди `plans/49`): `node tools/verify-test-pages.mjs`
(**89** по собранному сайту — 8 страниц, содержание в сыром HTML, без noindex, canonical, hreflang
с самоссылкой, ПДн, словарь, sitemap ровно 8; сервера не поднимает; 3 мутации) ·
🆕 **КАРТА ПУТЕЙ** (`plans/54` фаза 0): `node tools/qa-journeys-lint.mjs` (139 путей, 24 маршрута
продукта, Smoke 14/15; сводка пересчитывается по строкам, маршруты снимаются с дерева `src/routes/`;
3 мутации) ·
🆕 **БРЕНД-ИМЯ** (канон 2026-08-15): `node tools/verify-sync-server-name.mjs` (893 файла, 76
объявленных исключений; маркер `ИМЯ-ОК: причина`, маркер без причины сам нарушение; 2 мутации) ·
🆕 **ПРИБОР ОТПЕЧАТКА**: `npx firebase emulators:exec --only firestore --project demo-ndim-fp
"node tools/migrate/verify-fingerprint.mjs"` (**6**, боевых чтений НОЛЬ; стережёт `bugs/130` —
`collectionGroup('dims')` матчит и КОРНЕВОЙ каталог; 2 мутации) ·
🆕 `node tools/migrate/seed-stage-from-prod.mjs` (**сухой прогон по умолчанию**, `--apply` пишет;
три предохранителя: цель обязана быть `ndim-stage` с именованной базой, источник ≠ цель, и отказ
при `DocumentReference` в данных — такие ссылки утащили бы стейдж обратно в бой) ·
🆕 **СТЕЙДЖ** (`plans/53`): `node tools/verify-stage-noindex.mjs` (**11**, контур закрыт от
роботов; несёт КОНТРОЛЬ ПРИБОРА — в бою заголовка быть не должно) ·
🆕 `node tools/verify-stage-clean.mjs` (**14** на живом контуре + `--selftest` на **10** случаях,
П7: в стейдже нет ПДн живых людей — мера это ПЕРЕСЕЧЕНИЕ КЛЮЧЕЙ С БОЕМ; ПДн не печатает; доказан
мутацией) ·
🆕 `node tools/deploy.mjs --selftest` (**8**, стоп-правило контура П8) и `--gate-only` (опросить
замок стейджа, ничего не выкатывая) ·
🆕 `node tools/verify-contour-parity.mjs` (**8** + `--selftest` на **5** случаях, `bugs/133`:
переадресации, `cleanUrls` и кеш обоих конфигов совпадают; законные различия перечислены в нём
поимённо — гоняется дверью ДО выката, в обоих контурах) ·
`node tools/probe-stage-contour.mjs` (стейдж не ходит в бой и наоборот, живым браузером) ·
`node tools/probe-stage-signin.mjs` (путь входа на стейдже) ·
🆕 **МИГРАЦИЯ**: `node tools/migrate/db-fingerprint.mjs --project P --database D` (отпечаток базы
«путь → хеш»; им судится «ни один документ не потерян». ⚠️ ~16 000 чтений за прогон при квоте
50 000/сут) · `node tools/migrate/probe-collection-group.mjs <имя>` (что за неизвестная коллекция:
пути, поля, ФОРМА значений — никогда сами значения) ·
🆕 `npm run questions` — **проверка 4 «ПРОСРОЧЕННЫЕ ЗАМКИ»** (блокада, ссылающаяся на отвеченное
интервью; исключение — `<!-- ЗАМОК-ОК: причина -->`; доказана мутацией) ·
🆕 `probe-test-pages-live` (**240**, БОЙ: 8 страниц ТЕСТ × обе темы × 390/1440, читающий) ·
🆕 `probe-test-guest-live` (**15** в бою, путь гостя до общего результата; ПИШЕТ в бой и убирает
за собой органами продукта — читай его шапку прежде, чем запускать) ·
`npm run e2e` (**100 passed**) · `npm run kaif:check` · `npm run questions` ·
🆕 `verify-prod-signed-in` (**24**, БОЙ под сессией — гнать после КАЖДОГО выката) ·
🆕 `verify-prod-cache` (**12**, заголовки кеширования боя, `bugs/124`) ·
🆕 `verify-bug117` (**28**, возврат из «Как меня видят» не дёргает «Профиль»; стережёт ещё и то,
что анимация ЖИВА и что слой ПЛОТНЫЙ, — иначе «починка» удалением анимации была бы зелёной;
2 мутации) ·
🆕 `verify-batch4` (обе темы × 390/1440; `bugs/65` стережётся ЦВЕТОМ холста, не габаритом слоя;
2 мутации) · 🆕 `verify-ideas24` (**60**, виджетная сетка; внесён в реестр только 2026-08-14) ·
`verify-bug110` (**22**, контур вычитки; `bugs/122` — приговор «не записано» запрещён) ·
`verify-owner-reviews` (**86**) · `verify-dimension-pages` (**30**) ·
`verify-prod-dimension-pages` (**44**, бой) · `verify-prod-lang-redirects` (**16**, бой) ·
`verify-prod-b4` (**81**, бой) · `verify-prod-dim-ratings` (**7**, бой) ·
`verify-product-vocabulary` (**7**) · `verify-lang-module` (**17**) · `verify-admin-home` (**17**) ·
`verify-bug104` (**22**) · `verify-bug105` (**56**) · `verify-plan08` (**60**) ·
`verify-bug81` (**36**) · `verify-bug40` (**71**) · `verify-bug69` · `verify-pwa` (**24**) ·
`verify-account` (**84**) ·
🆕 `verify-account-delete` (**31**, 🔴 УНИЧТОЖАЕТ dev-пользователя — последним и в одиночку;
несёт ИНВАРИАНТ «без входа при живых данных не остаются», 2 мутации) · `verify-ptr` (65…66, число НЕ детерминировано — судить по «0 провалов») ·
`verify-manual-v1` · `probe-bug67` · `verify-bug80` · `verify-bug84` · `verify-bug87` ·
`verify-back-twins` · `verify-bug75` · `verify-bug76` · `verify-bug77` · `verify-bugs08` ·
`verify-tierA` · `verify-bug42`.
Полное описание каждого — таблица «Испытательный стенд» в `AGENT_GUIDE.md`.

## Перед каждым push — `npm run kaif:check`

Падает, если в git попал секрет или ПДн. Утечка уже случалась (`bugs/03`).
**Репозитории:** публичный `MikalaiKryvusha/ndim` (чистая история) · приватный архив 1.x
`MikalaiKryvusha/ndim-old` (0 форков) · фреймворк `MikalaiKryvusha/KAIF` (сюда идут issues о
дефектах KAIF — заводит агент сам).

## Окружение (проверено 2026-07-10, досье — `AGENT_GUIDE.md`)

Node 24.15.0 · git 2.43 · `gh` авторизован · firebase-tools 15.23.0 · gcloud (`ndim-space`) ·
Java OpenJDK 21 (в PATH — в НОВОМ сеансе) · эмулятор Firestore на **8181** (8080 занят
`llama-swap`) · Docker · Playwright 1.61.1.
🔴 **Деплой требует явного проекта:** `.firebaserc` без default —
`firebase deploy --only hosting --project ndim-space` (таргеты `landing` + `app`, оба из `build`).

---

**Летопись** — `PROJECT_HISTORY.md`: закрытые сессии, фазы и выкаты, перенесённые отсюда ДОСЛОВНО,
свежее сверху. В обязательный набор чтения НЕ входит — открывай для археологии.
