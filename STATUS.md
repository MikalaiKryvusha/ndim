# NDim Space — Текущий статус

> Этот файл читается ИИ-агентом перед каждой задачей. Обновляй его при каждом значимом изменении
> состояния. Это ГЛАВНАЯ передача эстафеты между сессиями: новая сессия агента стартует с пустым
> контекстом и должна суметь стать продуктивной, опираясь на один этот файл. Пиши соответственно —
> конкретно, с путями к файлам и командами.
> 🧠 Главный принцип мышления — `PHILOSOPHY.md` (ПРОСТОТА: KISS + Оккам). Рабочий фреймворк —
> `AGENT_GUIDE.md`.
>
> ⚠️ **STATUS — это СВОДКА «СЕЙЧАС», а не летопись.** Статусный файл, который только растёт,
> превращается в историческую книгу проекта, и агент, пришедший за быстрым «где мы», в ней тонет
> (поле: STATUS на 2 300 строк — «бездна, а не сводка»; наш дорос до 2 900 и подстрижен
> 2026-07-31). Правила, которые держат его сводкой:
>
> - **Каждая строка проходит два теста:** *«убери эту строку — ошибётся ли следующий агент?»* и
>   *«читает ли новичок весь файл за один присест?»* Мягкая цель: **~200 строк** (один-два экрана
>   сути) — страж предупреждает, а не запрещает, но пересечение значит, что стрижка назрела.
> - **Закрытое ВЫНОСИТСЯ, а не копится:** когда запись фазы или сессии перестала быть «сейчас»,
>   перенеси её ДОСЛОВНО в `PROJECT_HISTORY.md` (летопись — она для этого). Шаг «стрижка бонсая»
>   в `/end-chat` — ровно об этом (`/pause` по замыслу остаётся без церемоний).
> - **Оставь файл таким, каким хотел бы его найти:** свежая сводка того, что работает, что в
>   работе, что дальше, какие капканы, и ГДЕ ИСКАТЬ подробности (планы, баги, летопись) —
>   указатели, а не пересказы.

**Последнее обновление:** 2026-08-14 вечер (восьмая сессия закрыта `/end-chat`) —
🔵 **ВЗЯТА ФАЗА 2 ЭПИКА 40 «АРХИТЕКТУРА ВЕСА»** (`plans/48`) — работа, у которой цена
промедления календарная: страница набирает вес месяцами, а теряет за неделю. За сессию:
**шаг 0** — замер поля «вид» (грязных 74 из 5111, главный класс — невидимый `U+200B` — 44) ·
**шаг 1** — `dim-kind.ts`: нормализация вида НА ЧТЕНИИ, чем СНЯТ блокер фазы (метаплан ждал
боевой прополки; без нормализации вышло бы два одинаковых хаба — «TV Series» 329 и «TV Series​»
41), 5 тестов, **доказан 3 мутациями** · **шаг 2** — 4 макета хаба показаны, владелец выбрал
**V3 «Рейтинг»** (интервью №030, В1 = В) и произнёс ЗАКОН о названиях · **шаг 5** — JSON-LD
каталога (Movie/Book/VideoGame/TVSeries/MusicGroup/CreativeWork + AggregateRating по общему
правилу показа) и `Organization`+`WebSite` на лендинге — давний долг `researches/26` §7.6.
[TESTED: 250/250 unit · сборка зелёная · разметка проверена в СОБРАННОМ HTML: у «Матрицы»
9,3 из 11 голосов с границами 0…10, у страницы без голосов рейтинга нет, `inLanguage` не
поставлен].

🔴 **ЗАКОН ВЛАДЕЛЬЦА 2026-08-14, шире этой фазы:** «названия не должны обрезаться троеточием,
всегда полностью влазят — **это закон**. если длинное — карточка растет по высоте». Записан в
`AGENT_GUIDE.md` → «Названия не обрезаются НИКОГДА». В день произнесения вскрыл нарушение
**в бою** — `bugs/120` (названия измерений в результате теста, выкат 2026-08-14).

## ЗАКРЫТЫЕ ЧАТЫ 2026-08-14 (сессии 2–7) — записи перенесены в `PROJECT_HISTORY.md`
Коротко, что действует: `plans/39` и релиз 2.1 в бою · семейство ТЕСТ в бою (8 страниц,
`plans/42`; хвост — такт Г) · приборы фазы 1 (`plans/41`) · дом админ-панели (`plans/33`) ·
пул слабой модели `plans/45` · капкан «hosting-эмулятор не применяет redirects» жив (`EXP-0144`).

## 🔴 ГЛАВНОЕ ДЛЯ СЛЕДУЮЩЕГО ЧАТА — прочитай эти семь строк, даже если больше ничего не прочтёшь

0. 🆕 **KAIF обновлён до 2.2 «Yolden KAIF» 2026-08-14 03:05 +03:00** (судья VERIFIED, update-verify
   зелёный, отчёт — `reports/KAIF_UPDATES/`, сигнал — issue #13 истока). Три вещи, меняющие работу
   СРАЗУ: (а) **модуль хуков ПОДКЛЮЧЁН** — после сжатия контекста и раз в час придёт приказ
   перечитать канон; свидетель = `.kaif/refresh-marker.json` + цитата в чате, отметка без цитаты —
   фрод; (б) новый ключевой док `REQUIREMENTS_FRAMEWORK.md` — планы/баги/идеи открываются вектором
   цели и критериями приёмки; (в) портрет голоса теперь `AUTHOR_STYLOMETRY.md` (принятое ядро 1.1,
   слепок KAIF байт-в-байт; старое имя OWNER_VOICE.md умерло, ссылки перенацелены).
1. 🚀 **Фаза 5 `ideas/30` В БОЮ.** 10 222 страницы каталога на `/ru/…` и `/en/…`, английский
   впервые в отдаваемом HTML. Бой проверяется прибором: `node tools/verify-prod-dimension-pages.mjs`
   (42 проверки, ничего не меняет). Если он покраснел — начинай оттуда, а не с кода.
2. 🚀 **`plans/39` ВЫКАЧЕН 2026-08-14 (сессия 5), бой проверен: 16/0 + 44/0 + 81/0; query при
   301 сохраняется** (старые письма с `oobCode` не деградируют). ⛔ Осталось фазе: боевая
   приёмка ЧЕЛОВЕКОМ (ru-память на `https://ndimspace.app/en` → остаётся английский → внутрь
   на английском) + вычитка владельцем EN-обвязки (его условие В2 = А). 🔴 Капкан: hosting-эмулятор
   firebase-tools 15.23 redirects МОЛЧА не применяет — не «чинить» правила по нему, судья — бой.
3. 🔴 **САМОЕ ДОРОГОЕ, ЧТО МОЖНО ЗАБЫТЬ в этой работе:** одиннадцать публичных адресов **уже
   проиндексированы**. Переезд без **301-переадресации** выбросит всё, что они заработали.
   Список адресов поимённо — `plans/39`, шаг 0. Маску `/menu/:rest*` писать НЕЛЬЗЯ: заденет
   личный экран `/menu`.
4. 🆕 **SEO-переосмысление по `ideas/34` СПЛАНИРОВАНО (2026-08-14): работы ведутся из
   `plans/40_EPIC_semantic_core_v2.md`**, непройденные фазы `plans/34` поглощены им. Поверхность
   ТЕСТ (RU «тест личности» **219 453**, EN «personality test» **39 707**, выдачу держит
   16personalities) — теперь фаза 5 нового эпика. ✅ **Интервью №025 В1–В6 отвечены в тот же
   день (все — А): каталог держим целиком и судим по данным · хабы по виду · критики-кластера
   нет · опоры пишет агент по портрету · ТЕСТ сразу после приборов · EN-фикс с журналом.**
   Фазы 1–5 разблокированы; ✅ В7 тоже = А (06:59): блок «Похожие по каталогу» по классификации
   разрешён (в фазу 2), IBCF — «до лучших времён». **Интервью №025 закрыто целиком, 7/7.**
   ✅ **Фаза 1 «Приборы» ЗАКРЫТА 2026-08-14 (ночь)** — покрытие/граф/спрос сняты числом
   (`plans/41`, приложения §14–§16 `researches/34`). **Фаза 5 «ТЕСТ»: механика РЕШЕНА
   владельцем** (интервью №026, В2 = Г, 2026-08-14): все жанры — обёртки поверх движка;
   следующий шаг — состав семейства страниц-обёрток (`plans/42` шаг 1а → предложение владельцу
   → 4 макета).
   Повторный замер покрытия — `node tools/console-coverage.mjs` через 2–4 недели — вторая
   точка тренда для вердикта фазы 8. 🆕 **Фаза 2 «Архитектура веса» ВЗЯТА В РАБОТУ** (`plans/48`,
   сессия 8): её ⛔-зависимость от прополки `plans/30` СНЯТА нормализацией вида на чтении;
   прополка осталась гигиеной данных (пишет в бой — слово владельца).
   📌 Приоритет **Менеджера измерений** (эпик `ideas/29`) отработан: макеты выбраны ещё
   2026-08-02 (V1 «Стопка», `plans/32`), дом панели построен ночью 2026-08-14 (`plans/33` ✅).
   Следующая ступень эпика — **фаза 4 «комната Измерений»**: операционный план пишется при
   заходе, с уроками фазы 3 (`plans/30` — карта эпика).
5. **Два эпика спланированы и НЕ начаты:** `plans/37` (соседство измерений, IBCF — «делаем позже»)
   и `plans/38` (оживить теги и вид: поиск, фасеты, булева логика).
6. ✅ **Интервью №026 ЗАКРЫТО 2026-08-14** (шейпинг ≈09:06 → переработка в В2 → ответ В2 = Г,
   11:15): все жанры теста — обёртки поверх движка NDim Space. Разнесено в `plans/42`,
   `researches/35` §6, журнал `MASTER_PLAN`. ✅ **Интервью №028 ЗАКРЫТО 12:49** (состав
   семейства: 4 страницы). Интервью, ждущих владельца, — **0**. Числа «до»
   `homeworks/09` (Google — 1 страница) теперь история: первый полный замер покрытия —
   `researches/34_coverage_2026-08-14.json` (Google 994 · Bing 2 · Яндекс −641).
7. 🔴 **Новый страж, который нельзя игнорировать:** `npm run questions` теперь докладывает ДВА
   плеча. «Неразнесённых ответов **35**» — унаследованный долг, и он обязан убывать. Разносишь
   ответ владельца — **цитируй «интервью №NNN, ВN»** в документе, который на нём стоял.

## 🏁 ЭСТАФЕТА — с чего начать следующему чату

**Первым делом:** прочитать `AGENT_GUIDE.md` и этот файл, прогнать `npm run questions`.
Снято при закрытии сессии 6 (2026-08-14): «Новых вопросов владельцу вне interviews/ не
появилось» · «интервью **29**, ждут владельца **0**» (№028 и №029 закрыты в тот же день) ·
унаследованный долг: вопросов вне interviews/ **26**, неразнесённых ответов **35** — оба
числа обязаны убывать.

⚙️ **РЕЖИМ УСИЛИЙ — правило для КАЖДОЙ сессии (слово владельца 2026-08-14, сессия 8):**
по умолчанию агент работает на **max**. Режимы **ultracode + workflows** (многоагентная
оркестровка: веерная разведка, состязательная проверка, широкий аудит) владелец заранее НЕ
включает — **агент обязан САМ попросить его об этом**, когда задача того стоит, и назвать,
что именно даст фан-аут. Не попросил — значит работаешь на max в одиночку; молчаливое
«я бы сделал лучше многоагентно» не считается.

🤖 **КТО РАБОТАЕТ ДАЛЬШЕ (слово владельца 2026-08-14 ≈17:20, седьмая сессия):** «кредиты
сильной модели закончились… дальше я буду работать на слабом Opus». Итого: **следующий
чат — слабая модель**, задания СТРОГО из реестра **`plans/45`** (готовые, по порядку:
`plans/47` → `plans/44` → `plans/46`; правила формата и граница «чего ей не дают» — там же).
Сильной модели по возвращении — такт Г `plans/42` (приёмка семейства ТЕСТ) и §0 ниже.

**Что делать, по убыванию ценности:**

### 0. 🆕 ФАЗА 2 ЭПИКА 40 «АРХИТЕКТУРА ВЕСА» — ВЗЯТА В РАБОТУ 2026-08-14 (`plans/48`)

**Почему она первая — ответ на вопрос владельца «чему нужно много времени, чтобы созреть».**
Не рассуждение, а наши замеры фазы 1: Google взял **994 из 10 222**, а **9 237** висят в
«Обнаружена, не проиндексирована» — робот знает адрес из sitemap и не приходит. Причина названа
числом: **все 10 222 страницы недостижимы от корня** (пары `ru↔en` замкнуты сами на себя,
`researches/34` §14). Яндекс за ту же неделю **выбросил 641 страницу** как «малоценные».
Страница набирает вес месяцами, а теряет за неделю — это единственная работа в беклоге, где
цена промедления измеряется календарём, а не объёмом.

✅ **Шаг 0** — замер поля «вид»: грязных **74 из 5111**, главный класс — невидимый `U+200B` (44).
✅ **Шаг 1** — `src/lib/content/dim-kind.ts` + 5 тестов, **доказан 3 мутациями** (2 · 3 · 2
падения). `npm test` **250/250**.

🔑 **Блокер фазы СНЯТ инженерным решением, а не ожиданием:** метаплан требовал сперва прополоть
поле «вид» в бою. Хабы группируются по нормализации **на чтении** — боевая прополка осталась
гигиеной данных и больше никого не задерживает. Без неё было бы два визуально одинаковых хаба:
«TV Series» (329) и «TV Series​» с невидимым знаком (41).

✅ **Шаг 2 — ФОРМА ВЫБРАНА ВЛАДЕЛЬЦЕМ: V3 «Рейтинг»** (интервью №030, В1 = В). Хаб — ранжированный
список, «топ по версии NDim Space»: номер места · название · год и автор · звёзды · оценка ·
«оценено N людьми». Макеты — `design/catalog-hub-mockups.html`, кадры — `test-results/catalog-hub-mockups/`.
✅ **Шаг 5 (сделан раньше очереди — он не зависел от макета)** — JSON-LD каталога
(`dim-jsonld.ts`: Movie/Book/VideoGame/TVSeries/MusicGroup/CreativeWork + `AggregateRating` строго
по общему правилу показа) и `Organization`+`WebSite` на обеих языковых главных.

✅ **Шаг 3 ЗАКРЫТ 2026-08-14 — ХАБЫ ПОСТРОЕНЫ, ГЛАВНОЕ ЧИСЛО ФАЗЫ ВЗЯТО.** 180 страниц в сборке
(индекс каталога + 89 страниц семи хабов) × 2 языка, форма V3 «Рейтинг», ссылки на семь хабов и на
индекс — с лендинга. **Недостижимых страниц каталога от `/ru` и `/en` — 0** (было 10 222); самая
дальняя карточка — **3 клика**. Прибор — `node tools/measure-link-graph.mjs` (он же научился мерить
глубину от языковых главных, а не только от корня). Кадры приёмки — `node tools/shoot-catalog-hubs.mjs`
(48 кадров, обе темы × 390/1440). [TESTED: 263/263 unit · e2e 100 passed · verify-dimension-pages 27/0 ·
verify-product-vocabulary 7/0 · клиентский бандл 1,3 МБ, каталога в нём нет · **тесты порядка
доказаны 5 мутациями** — 2 · 2 · 1 · 1 · 2 падения, контроль после отката 17/0].

🔴 **ДВА СТРАЖА КРАСНЕЛИ ДО ЭТОЙ РАБОТЫ — оба на исправном продукте, оба починены:**
· `npm run e2e` стоял красным СУТКИ: вчерашний шаг 5 положил описание фильма ещё и в JSON-LD, а
  вырез стража жаргона знал только про блок описания. **В той сессии e2e не гоняли** — вот цена;
· `verify-product-vocabulary` краснел на четырёх страницах «Теста» из-за слова «калькулятор» —
  а это утверждённое владельцем название страницы (интервью №028). Заведено узкое исключение
  ПО ТЕРМИНУ (не по странице), записано в канон словаря `AGENT_GUIDE.md` и доказано контролем.

✅ **ИНТЕРВЬЮ №030 ЗАКРЫТО ЦЕЛИКОМ 2026-08-14 (19:42): В2 = Д** — «по оценке, но С УЧЁТОМ ЧИСЛА
ГОЛОСОВ». Разнесено в тот же вечер: `catalog-hub.ts` (`makeComparator` · `catalogPrior` ·
`weighted`), тексты хаба, тесты, план. Формула `weighted = (v·R + m·C)/(v + m)`, **оба числа опоры
посчитаны, ни одно не придумано:** `C = 8,2405` (средняя по 1877 оценённым) и `m = 4` (90-й
процентиль числа голосов). Опора считается на сборке, печатается в лог, закреплена тестом.
🔴 **Наружу идёт настоящая средняя** — взвешенная участвует ТОЛЬКО в сортировке; это условие,
на котором владелец выбрал Д. 🔴 **Поправка, которую следующая сессия не должна «починить»
обратно:** иллюстрация агента при постановке варианта Д («9,3 от одиннадцати обгонит десятку от
четверых») боевыми числами НЕ воспроизводится ни при каком весе опоры — «Матрица» при общей
средней каталога 8,24 просто не высокая оценка. Настоящий эффект мягче и в ту же сторону.
Хочется жёстче — это одно число `m`, и поднимать его можно только словом владельца.

🟡 **`bugs/121` — найден тем же прибором:** недостижимых от языковых главных осталось 23, каталога
среди них 0, но **19 — публичные страницы**: всё семейство «Тест» (8, выкачено накануне ради самого
крупного спроса проекта), английские документы (9) и обе `delete-account`. Тот же дефект, что
лечила фаза, на другой поверхности. Все три лечения правят ЛЕНДИНГ (`plans/21`) — за владельцем.

✅ **Шаг 4 ЗАКРЫТ 2026-08-14 — КРИТЕРИЙ 2 ВЗЯТ.** Карточка получила ссылку «вверх» (в свой хаб; у
хвостовых видов — в индекс каталога) и блок **«Похожие по каталогу»** — восемь соседей по
КЛАССИФИКАЦИИ (интервью №025, В7 = А), без единого числа похожести, порядок показа нейтральный
(по слагу — инвариант покрывает и порядок близости). **Минимум входящих у страницы каталога — 3**
(было 1). [TESTED: 278/278 unit · e2e 100 passed · verify-dimension-pages 30/0 ·
verify-product-vocabulary 7/0 · check 0 ошибок · кадры блока в обеих темах].

🔑 Три вещи, найденные ТОЛЬКО на боевых данных, — не переоткрывать: вес общего тега = обратная
частота, иначе четыре тега вида делают всех равными и соседи выходят по алфавиту · десятилетие
усиливает признак, но само признаком не является (та же болезнь через другое поле) · **764 объекта
из 5111 не выбраны соседом ни разу** — их возвращает проход `returnLostNeighbours`, после него
объектов без единой входящей 0, ценой списков в 9–11 у части карточек.

## 🧹 РЕВИЗИЯ БЕКЛОГА ПРОВЕДЕНА 2026-08-14 — очередь починки готова (`plans/49`)

**Слово владельца:** «нужно будет грумить баги и фиксить. бесит тех долг». Ревизия прошла
многоагентно **с его прямого разрешения** (25 агентов, 90 вердиктов, 0 сбоев).

Из **93** документов беклога: **6 закрыто** и помечено `DONE` · **8 — живые справочники**
(`DONE` не ставится никогда) · **76 открыто**, из них **13 чинит агент** и **63 ждут владельца**.

🔑 **Состязательная проверка окупилась сразу:** заявлено закрытыми было 9 документов, скептик
**опроверг 3** — треть. `plans/18` выглядел закрытым, но: критерий «`/fable-judge` пройден» не
исполнен · `PENDING:` висит на вопросе В10, который СНЯТ вместе с паролями (обещанная правка
макета не приедет никогда) · внутри жива секция «Осталось владельцу».

🔴 **Отдельная находка ревизии, шире её самой:** обязательной секции «Решения, принятые без
владельца» не было **ни у одного** из пяти закрываемых документов, кроме `plans/27`. Канон её
требует (`AGENT_GUIDE.md` → «Беклог и DONE-тег»), и так закрывались документы месяцами. Дописана
всем шести по фактам из самих документов.

🟡 **ЧТО ДЕЛАТЬ ПЕРВЫМ СЛЕДУЮЩЕЙ СЕССИИ — решение владельца: «сначала список, потом по каждому
форензика и план починки, затем этот чат завершим и в новом будем чинить».** Список и планы
готовы — **`plans/49`**, очередь агента отсортирована от дешёвого к дорогому (S: `bugs/65`
жёлоб лайтбокса · `plans/12` страж не внесён в реестр · `plans/28` непрочитанный агрегат вершин).
Хвост фазы 2 идёт ПОСЛЕ починки: шаг 5 (сегментация `sitemap.xml`; JSON-LD сделан) · шаг 6
(страж `verify-catalog-hubs` + мутации) · шаг 7 (приёмка и выкат — только с AUTH владельца).

### 1. 🚀 Фаза 5 «ТЕСТ» В БОЮ с 2026-08-14 ≈16:54 — остались такт Г (приёмка) и страж (пул)

Каркас — **V5** (№029 В2 = А); состав — №028 (хаб `/tests` + совместимость + личность +
любовь). ✅ Такты А+Б+В построены, проверены и **ВЫКАЧЕНЫ В БОЙ** (AUTH владельца «Выкат»;
подробности и [TESTED]-маркеры — `plans/42`). Правила `testPairs` выпущены тем же деплоем
(`firebase deploy --only firestore:rules,hosting`). Бой проверен: страницы семейства 200 без
noindex, H1 живые · боевой sitemap несёт 8 адресов · `verify-prod-dimension-pages` 44/0.
✅ **Тексты приняты владельцем без личной вычитки** с условием сверки со стилометрией и
философией — сверка проведена и записана в шапке `test-copy.ts` (журнал решений
`MASTER_PLAN`, до ≈16:54). Что осталось фазе:
· **слабая модель (пул `plans/45`, по порядку):** страж `verify-test-pages` — `plans/47`
  (строгая форма, 3 мутации; ⚠️ план писан до тактов Б/В — все его проверки сырого HTML
  живы, гидратация им не мешает; строку-цель мутации №2 такты не тронули) → затем
  `plans/44` → `plans/46`;
· **владелец (по желанию, не блокирует):** живая проба в бою — пройти тест и позвать
  второго (`https://ndimspace.app/ru/test/compatibility`); агент боевых гостей сознательно
  не создавал — механика доказана на стенде (пара двух гостей 15/15);
· **сильная модель (когда вернётся):** такт Г — приёмка живым Chrome (обе темы × 390/1440,
  `?as=none`, критерий 3 фазы) + `/fable-judge` + закрытие шага 5 `plans/42`; PENDING
  такта В — гигиена старых пар вычислителем (названа в `plans/42`);
· 📊 не-происшествие, о котором знать: страницы теста появятся в индексе не сразу —
  честные сроки органики прежние (первые НЧ-позиции — зима 2026/27, `plans/40`).

План — `plans/42` (шаги 0–1а закрыты; состав в таблице шага 1а). Утверждено интервью №028
(2026-08-14 12:49): **4 страницы** — хаб «Тесты» `/{lang}/tests` (В4 = Б) · «Тест на
совместимость» `/{lang}/test/compatibility` (H1 по В2 = А, подзаголовок «тест для двоих») ·
«Тест личности» `/{lang}/test/personality` · «Калькулятор любви» `/{lang}/test/love`
(результат по В3 = А: счёт совпадений-фактов + искры, БЕЗ процентов похожести). Один движок,
один гостевой NDim ID на все обёртки; публичны только хаб и 3 обёртки (результат пары —
непубличная ссылка, noindex). **Шаг 2:** 4 варианта ФОРМЫ (токены продукта, обе темы,
390/1440), кадры `tools/shoot-mockups.mjs`, смотреть глазами ДО показа; оболочка — как у
публичных страниц каталога (рельса нет сознательно), образец — `design/dimension-page-mockups.html`.
Помнить шейпинг ≈09:06: обёртки сеют один и тот же живой NDim ID-паспорт.

### 2. 🚀 `plans/39` ВЫКАЧЕН 2026-08-14 — остались руки владельца

Выкат состоялся (AUTH в чате), бой проверен приборами: `verify-prod-lang-redirects` 16/0
(📊 query при 301 сохраняется — старые письма входа целы) · `verify-prod-dimension-pages` 44/0 ·
`verify-prod-b4` 81/0. Осталось фазе — только человек:
1. Боевая приёмка: `ru`-память на `https://ndimspace.app/en` → остаётся английский →
   внутрь приложения на английском. Плюс вычитка владельцем EN-обвязки (его условие В2 = А).
2. 📊 Ожидаемое НЕ-происшествие: `https://ndimspace.app/` уйдёт из индекса в «переадресация/
   noindex» — это ЦЕНА решения Р5 = В, принятая владельцем; «проиндексировано 0» через две
   недели — не авария.

🔴 **Капканы (оплачены, не переоткрывать):**
· hosting-эмулятор firebase-tools 15.23 **молча не применяет redirects** — правила по нему не
  судить и не «чинить»; судья — бой (`EXP-0144`);
· маска `/menu/:rest*` — по-прежнему под запретом (личный `/menu`);
· `locale: 'ru-RU'` в `playwright.config.ts` — не подгонка (решение №024 В1=А, `EXP-0135`);
· приёмочный прибор: память сидировать `evaluate`, НЕ `addInitScript` (тот сеет при каждой
  навигации и сам затирает мост).

🔑 Решено и не переоткрывается: схема адресов (интервью №010, Р5 = В) · `x-default` на английский ·
умолчание для пустой памяти — спрашиваем браузер (интервью №024, В1 = А) · корень не участвует
в поиске и НЕ отвечает 301 (он распознаватель) · переключатель языка на публичном адресе меняет
АДРЕС, а не текст под старым адресом.

### 3. ✅ Фаза 5 `ideas/30` — В БОЮ. Что осталось ПОСЛЕ неё

· 🔴 **вычитка владельцем английской ОБВЯЗКИ страницы** (кнопки, блок-дверь, подпись) — его
  собственное условие при ответе В2 = А. Описания — его текст, к ним вопросов нет.
  Файл: `src/routes/[lang=lang]/dimension/[slug]/+page.svelte`, объект `T`;
· 🔴 **регламент выката пополнить чисткой старых релизов:** **194 МБ на релиз** ⇒ ~52 релиза из
  бесплатных 10 ГБ. Шаг 0 обещал 126 при 79 МБ — ошибался вчетверо;
· ✅ закрытые хвосты фазы (домашка-консоли · число «до» = 1 страница · `bugs/111` разрыв 0) —
  перенесены в `PROJECT_HISTORY.md`; действующие числа — `researches/34` §3;
· ⚠️ прополка поля «вид» — работа `plans/30` (`фильм` строчной 21, `Movie` 3, год в поле вида,
  и **невидимый U+200B**). ⛔ Теперь предусловие хабов фазы 2 `plans/40`.

🔑 Не переоткрывать: рельса на публичной странице НЕТ сознательно · блока соседства нет по решению
владельца · полный каталог в git не кладётся (`node tools/fetch-dims-slice.mjs --all` — шаг выката).

### 4. ✅ Поверхность «ТЕСТ» вписана 2026-08-14 — фаза 5 `plans/40` (старт после приборов, В5 = А)

### 5. 🆕 Два эпика спланированы 2026-08-03 и НЕ начаты

· **`plans/37`** — соседство измерений по людям (IBCF), «делаем позже». Фаза 1 — замер плотности
  вычислителем; до её чисел кода не писать. Разбор — `researches/32`.
· **`plans/38`** — оживить теги и вид: поиск, фасеты, булева логика, фильтры по оценкам
  (идея `ideas/32`, исследование `researches/33`). ⛔ Предусловие — прополка `plans/30`.
  🔴 Замер вскрыл жёсткое: теги строками в индекс **не влезают** (1073 КБ при потолке 1024).

### 6. ⛔ За владельцем, но НЕ блокирует

Текст пункта 2.2 «Политики конфиденциальности» (разбор готов — `researches/31`; правится ИСТОЧНИК
`researches/06`, а не сгенерированный `docs.ts`) · развилка `ideas/31` («повод против человека») ·
домашка `homeworks/08` шаг 3.

### 7. Прочий беклог

Раздел «Пул автономных задач» ниже; ближайшее автономное — `ideas/27` шаги 2–5 (⚠️ шаг 2
требует замера Lighthouse до/после: лендинг держит 100, прелоад лиц может ей навредить) и
фаза 4 эпика `ideas/29` (план при заходе).

🔑 **Как повторить замеры** (машинерия готова, нужен один шаг владельца):
```powershell
& "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222 --user-data-dir="C:\Users\krinik\chrome-debug"
# владелец входит в Яндекс (Вордстат) и/или в Bing Webmaster под kotkrinik@gmail.com
node tools/wordstat-harvest.mjs            # русский спрос (+ --file tools/demand-*.txt --out …)
node tools/bing-keywords.mjs               # английский спрос (+ те же флаги)
node tools/bing-keywords.mjs --only "запрос" --rich   # кто ранжируется по запросу
node tools/console-coverage.mjs            # 🆕 покрытие индекса GSC/Bing/Яндекс, только чтение
```

---

### 📊 Боевые цифры на 2026-08-02 (чтобы не перемерять)

Людей **94** · оценок **4089** (43,5 на человека) · измерений **5111** · связей **2346** ·
`avgSimilarity` **3 %** · распределение **low 100 %** · активных за 7 дней — **2** ·
`dims_list` **566 КБ**. Прибор — `node tools/probe-prod-stats.mjs` (только читает, анонимную
запись удаляет за собой; разрешение — интервью №013 В2 = Б + правило в `.claude/settings.local.json`).

🔴 **ГЛАВНОЕ ОБ ЭТИХ ЧИСЛАХ — не повтори ошибку, которую я чуть не совершил** (`EXP-0124`):
`avgSimilarity` **НЕ отвечает** на вопрос «что видит новичок». Это среднее по всем строкам всех
топов вместе с хвостом, гость в него не входит ПО КОДУ (`calculator/index.mjs:709`), а у новичка
знаменатель Дайса МЕНЬШЕ — то есть проценты ВЫШЕ. Живая проверка владельцем: его верхняя строка —
**24 %**, в восемь раз больше средней (и она сходится с формулой ядра: 0,61 × 0,40 = 0,244).
Что числа доказывают честно: ≥84 человека из 94 не видят ничего выше 39 % · типичная связь стоит
на 1–2 общих осях из 43,5 · Пространство не мертво, а **очень тихое** (+1 человек за 21 день).


### 🔐 `bugs/113` — ПРИВАТНОСТЬ: оценку можно привязать к личности (разобран, НЕ закрыт)

Найден вопросом владельца 2026-08-03. **Порог показа счётчика защитой не был**; настоящий канал —
**дифференциальный съём по `commonSpaceSize`**: злоумышленник снимает число общих осей по своему
топу, ставит оценку измерению X, ждёт цикл и смотрит, у кого оно выросло на единицу. Тот и оценил.
Счётчик в атаке не участвует — он лишь удешевлял прицеливание.

✅ Проверено кодом: личные оценки правилами закрыты (`firestore.rules:234`), список общих осей
ядро наружу не отдаёт НИКОГДА. Дыра не в них.

**Направление защиты выбрано владельцем** (ловить и банить · делать съём нерациональным по
времени — снятие оценки откладывает попадание в пул синхронизации на дни). Работа не начата,
варианты и границы — в `bugs/113`. ⚠️ Атака НЕ воспроизводилась в бою: разбор чтением кода.

### 📋 ОСТАЛЬНОЙ БЕКЛОГ (главное — в «ЭСТАФЕТЕ» наверху файла)

> ⚠️ Порядок работ задаёт **эстафета в шапке**. Здесь — то, что было актуально раньше и остаётся
> открытым. Не начинай отсюда, не прочитав шапку.
>
> 🔴 **Ловушка, стоившая бы дорого, если её забыть:** соблазн прочитать чужие оценки ключом
> сервисного аккаунта (`calculator/secrets/sa.json` лежит локально, Admin SDK правила обходит).
> **Нельзя.** `firestore.rules:219` — «АДМИН — НЕ ИСКЛЮЧЕНИЕ», запрет восстановлен аудитом
> `bugs/100`. Быстрее — не значит можно. (Замер каталога это подтвердил на практике: нужные числа
> нашлись в ПУБЛИЧНОМ документе `dims/{dimId}`, лезть в приватное не понадобилось вовсе.)

**1. 🔵 ЭПИК `ideas/29` «Admin Panel и пополнение каталога», ФАЗА 3.**
   Всё готово к исполнению, вопросов владельцу нет ни одного.
   · Карта эпика — **`plans/30`** (7 фаз, ворота, 11 критериев приёмки). Читать её ПЕРВОЙ.
   · Доказательная база — `researches/29` (обзор индустрии + разведка кода 2.0 и 1.x).
   · **Операционный план фазы 3 — `plans/33`**, не начат. Дом панели: адрес, оболочка, доступ.
   🔴 Единственная ловушка фазы названа в плане: у прав **ТРИ состояния, а не два**. «Ещё не знаю»
   это не «не админ», иначе наивный редирект выбросит из панели самого владельца, пока едет claim.
   · Утверждённый макет — **V1 «Стопка»** (`plans/32`, `design/dims-manager-mockups.html`).
   ⚠️ Вместе с V1 взята его названная цена: пачка одинаковых карточек усыпляет внимание. Лечение —
   механизмом, а не бдительностью; что именно — в `plans/32` и в фазе 6 метаплана.

**2. 🔧 Хвост фазы 1, который ждёт РУК ВЛАДЕЛЬЦА** (агент не выкатывает):
   снимки дней до 2026-08-02 несут завышенное `dims: 5112`. Их правит разовый инструмент, ещё не
   написанный, — решение владельца интервью №014, В2 = В: **переписать И оставить след правки**.
   Инструмент пишет в БОЙ, запускается только с его слова (`bugs/106` шаг 3).

**3. 📝 Тексты ворот `plans/29` — ВТОРОЙ ЗАХОД.** Первый состоялся, вердикт «принять с правками»,
   разбор — `plans/29` §3а. Что переделывать: подзаголовок 1.x возвращается целиком · термины на
   витрине раскрывать, а не бросать голыми · полоса чисел становится БЛОКОМ с зелёным статусом ·
   якорь меняет слово на «точный, честный и быстрый беспристрастный суперкомпьютер» (из четырёх
   эпитетов под замером нет только «быстрый») · английский — В3=А, перевод мой, вычитка его.
   🔴 **Переписывать ТОЛЬКО по `AUTHOR_STYLOMETRY.md`.**

**4. 🎙 `AUTHOR_STYLOMETRY.md` — портрет голоса владельца, ✅ ПРИНЯТОЕ ЯДРО.** С 2026-08-14 файл —
   байт-в-байт копия публичного слепка ядра `krinik-stylometry` **1.1** (статус `accepted`,
   решение владельца №63 от 2026-08-08 22:05 +03:00; 151 правило, анти-портрет АП1–АП37, чек-лист
   §7). Истина — приватное ядро `d:\work\krinik_voice`; слепок собирает KAIF
   (`tools/stylometry-snapshot.mjs`), наш файл = его копия с ndim-преамбулой (пара в реестре
   зеркал `AGENT_GUIDE.md`). Прежний локальный черновик (слепой тест №017, 7/8 по содержанию) —
   в истории git. Реестр корпуса, подтверждённый владельцем (интервью №016, В1 · интервью №016,
   В2 · интервью №016, В3 · интервью №016, В4), впитан ПРИВАТНЫМ ядром — его таблица корпусов и
   есть ответ; локальной копии реестра больше нет вместе с черновиком. 🔴 Шапка документа
   обязательна к прочтению перед любой строкой текста для людей.

**5. Прочий беклог** — раздел «Пул автономных задач» ниже. Ближайшее автономное:
   `bugs/104` (раскрытие карточки связи по всему верху), `ideas/25`, `ideas/26`, `ideas/27`.

**6. ⛔ Что ждёт владельца** — раздел ниже; ничего из этого агент не решает.

---

## 🧭 РАБОЧИЕ РАЗДЕЛЫ — живые капканы стенда и списки ожидания


⚠️ **Три операционных капкана, на которых этот чат обжёгся** (каждый стоил ложного диагноза):

1. **`npm run e2e` требует ПОГАШЕННОГО стенда** (EXP-0027) — иначе 6 проверок «без данных —
   честная ошибка» краснеют на исправном коде. Гасить по дереву: найти `node tools/stand.mjs`
   и `taskkill /PID <pid> /T /F`. 🔴 **Признаку `stand.log` НЕ ВЕРЬ** (проверено 2026-07-31:
   файл исчез, а три процесса выжили) — сверяй ПРОЦЕССАМИ:
   `Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -match 'calculator|stand\.mjs|emulators' }`.
2. **Свой `vite preview` на 4173 подменяет сервер e2e.** Гаси preview перед `npm run e2e`, а сам
   поднимай с `--strictPort`: без него он молча уезжает на 4174, и стражи собранного сайта судят
   не о том.
   🔴 **Обожглись ВТОРОЙ раз 2026-07-31, и дороже:** осиротевший preview отдавал СТАРУЮ сборку,
   и полный прогон дал **21 падение** — в лендинге, языке, теме, «Меню», профиле. Всё выглядело
   как разнородная регрессия продукта; после гашения порта — **72 passed**, продукт не при чём.
   **Остановка фоновой задачи может отчитаться успехом, а процесс — выжить.** Проверять только
   портом: `Get-NetTCPConnection -LocalPort 4173 -State Listen`. Пусто — можно гнать e2e.
   Симптом класса: падения РАЗНОРОДНЫЕ и задевают экраны, которых правка не касалась.
3. **`npm run test:calc` сразу после гашения стенда** может дать 26 тестов вместо 34 и один
   провал — порт эмулятора ещё занят. Дай портам освободиться.

### ⛔ ЧТО ЖДЁТ ВЛАДЕЛЬЦА (полный список на 30.07 — агент это НЕ решает)

Отсортировано по цене вопроса, а не по номеру документа:

0. ✅ **`interviews/interview_008` — ОТВЕТЫ ПОЛУЧЕНЫ 2026-08-01** (включая В11), разнесены по
   документам. Вопрос «чем отличать пароль от ссылки» **снят вместе с паролями**.
   От эпика за владельцем осталось три вещи, и все три — руки человека, не агента:
   - 🔧 **погасить пароли у 331 человека в бою** (`deleteAttribute: ["PASSWORD"]`, см. п. 3
     эстафеты) — необратимо, `[NOT-TESTED]`;
   - 🔧 **приёмка фазы 5 в бою** — смена почты под EEP: письмо на новый адрес, письмо-возврат на
     старый, поведение при занятом адресе. На стенде не проверяется по построению;
   - 🔧 **приёмка фазы 6 на живом Safari** — отложена им же (В9), не забыта.
1. **Выкат в бой** — вся работа 29–30 июля (список наверху). Плюс пересборка образа сервера.
2. **`bugs/74` часть А, контракт кэша** — гасить ли собранный экран «Измерений» своей же
   оценкой (вопросы 1 и 2 в документе). Это единственное, что мешает памяти вида работать
   ПОСЛЕ оценки: экран честно не восстанавливает позицию, когда лента пересобрана.
3. **`bugs/80` подпункты (а) звёзды и (в) адаптивный `clamp()`** — вопросы 1 и 3, замер шага 0
   не снят.
4. **`bugs/84`: текст ошибки** для ссылки БЕЗ намерения и расширенная фраза врезки (`[AI]`).
5. **`plans/08` вопросы Б, В, Г** — агент взял их по своей рекомендации и пометил это честно;
   нужна вычитка (что помнить кроме прокрутки · сброс при pull-to-refresh · на всех экранах).
6. **`bugs/83` / PWA** — обзор индустрии готов (`researches/19`), развилки владельца названы
   там же §5: что именно установлено у него на телефоне (1.x? TWA? ярлык?), один манифест или
   два (RU/EN), нужна ли нативная обёртка при изъятом ключе подписи (`bugs/02`).
7. ✅ `bugs/04` шаг 4 — сделан 2026-08-01, запись в `PROJECT_HISTORY.md`.
8. **`bugs/46`: «Был онлайн»** — новое публичное поле, родня `ideas/23` / `plans/09`.
9. **`bugs/62`** — кнопка «Установить приложение» и service worker: цена SW названа в
   `researches/19` §5 (кеш пререндеренной статики может запереть человека на старой сборке).
10. **Мелочь:** строка «В Пространстве с …» осталась в «Меню» (владелец просил убрать из
    ПРОФИЛЯ) — спросить.

## 🧭 РАБОЧИЕ РАЗДЕЛЫ (продолжение) — читать всем, кто берёт задачу

### Чего НЕ делать

- 🔄 **Блок соседства: правило ОБНОВЛЕНО 2026-08-14 (интервью №025 В7 = А).** Разрешён блок
  «Похожие по каталогу» ПО КЛАССИФИКАЦИИ (теги/вид/год/автор, без математики похожести) — живёт
  в `plans/40` фаза 2; в формулировках НЕ называть его «Рядом в Пространстве». Что НЕ изменилось:
  🔴 инвариант «цифр метрик похожести не раскрываем» (включая порядок близости) в силе; IBCF —
  «до лучших времён» (слово владельца: когда оценок и людей будет много) — триггер в `plans/37`.
  Разбор математики — `researches/32`.
- **Не «чинить» отсутствие строки «Обновить данные» в «Меню».** Она убрана по прямому слову
  владельца (В1=Б), цена названа ему и принята. Это следствие, а не регрессия.
- ~~**Не «чинить» множитель витрины лендинга** (×30)~~ — 🔴 **ПРАВИЛО ОТМЕНЕНО 2026-08-01**
  (интервью №010, Р7): «Делаем живую настоящую цифру. И анонимов и полноценных считаем и
  показываем». Множитель удалён из `metrics.ts`. **Не возвращать.** Причина отмены — рядом с
  витриной встанет секция настоящих отзывов от настоящих людей, и умноженное число обесценило бы
  и её, и себя.
- **Не проверять гостя без стендовых дверей** `?as=guest` / `?as=none` — иначе проверка пройдёт
  мимо продукта.
- **Не «чинить» отсутствие приглашения «Установить приложение».** Манифест есть, service worker
  сознательно НЕТ (`researches/19` §5): без него приложение ставится из меню браузера, а
  автоприглашение требует SW, и цена SW — кеш пререндеренной статики. Это `bugs/62`, решение
  владельца.
- **Не «чинить» `storage.cors.json`, если речь о загрузке фото.** Он управляет доменом
  `storage.googleapis.com`, а Firebase SDK ходит на `firebasestorage.googleapis.com` со своим
  открытым CORS — замерено заголовками (`researches/24` §7.4). Планируешь править CORS — сначала
  preflight на ТОТ хост, куда ходит код (`EXP-0112`: агент уже перемерил это зря).
- **Не ужесточать ЧТЕНИЕ аватаров сгоряча.** Правила Storage выкачены (`bugs/04` закрыт), но
  чтение сознательно оставлено паритетом с 1.x: любой вошедший. На нём держится показ лиц
  (`EXP-0043`). Перевод на модель видимости 2.0 — отдельное решение; его ОБЯЗАН уронить тест
  «ПАРИТЕТ С 1.x» в `tests/rules/storage.test.ts`, и это его работа, а не поломка.
- **Не трогать `--bar-h-fallback` «одной константой».** Значения 61/55 сняты замером на двух
  ширинах (`bugs/67`); одна константа врёт на одной из них, а ноль загоняет рельс под шапку.

### Стражи, которые надо держать зелёными

🆕 `verify-product-vocabulary` (словарь продукта на лице собранного сайта; данные каталога и
утверждённые владельцем термины вырезаны, вырез доказан контролем — `ALLOW_TERMS`) ·
🆕 `node tools/shoot-catalog-hubs.mjs` (прибор приёмки хабов: 48 кадров — индекс · голова топа ·
страница-ГРАНИЦА, где кончаются голоса · последняя страница с полной пагинацией · самый маленький
хаб · самое длинное название каталога; отказывается снимать со старого preview) ·
🆕 `verify-admin-home` (**17**, дом админ-панели: редирект не-админа · ожидание при медленных
правах · noindex/sitemap · пустой пререндер · дверь без мигания rAF-трассой; сам поднимает
preview и гасит его ДЕРЕВОМ — `EXP-0142`; нужны эмуляторы стенда) ·
🆕 `verify-bug104` (**22**, цель раскрытия карточки связи — весь верх; настоящий тап `hasTouch`,
лайтбокс из раскрытой, клавиатура; нужен `npm run stand`) ·
🆕 `verify-prod-dim-ratings` (**7**, сводка оценок каталога по ЖИВОМУ бою, только читает:
`rating` = `stars`/`rates` у всех · сходимость суммы `rates` с `space/stats.ratings`) ·
🆕 `verify-lang-module` (**17**, язык читается из ОДНОГО места; отдельно стережёт совпадение
порядка «адрес → память → браузер» в инлайн-скрипте `app.html` с модулем — это единственная
неустранимая копия правила; доказан 3 мутациями, сборка не нужна) ·
🆕 `e2e/lang-default.spec.ts` (**9** × 2 ширины, кто побеждает кого; каждое «побеждает» проверено
в ОБЕ стороны — доказан мутацией, 2 падения) ·
🆕 `verify-dimension-pages` (**24**, публичные страницы каталога по СОБРАННОМУ сайту: правило оценок, приватность, hreflang, вес бандла — 7 мутаций) · 🆕 `verify-prod-dimension-pages` (**42**, те же проверки по ЖИВОМУ ndimspace.app, только читает) · 🆕 `shoot-dimension-pages` (прибор приёмки: 32 кадра, отказывается снимать со старого сервера) · `verify-bug110` (**20**, контур вычитки не съедает ответы владельца — живой Chromium, стенд не
нужен, ~90 с) · 🆕 `npm run test:calc` (**59**, внутри — `dims_index.test.mjs`: обслуживание индекса
каталога дельтой; пара «истина ↔ зеркало» `dims/{id}` ↔ `dims_list` ↔ `space/stats.dims`) ·
`verify-bug105` (**56**, «Как меня видят» в рабочей области; видимость оболочки меряется
`elementFromPoint`, а не селектором — **новый**) ·
`verify-plan08` (**60**, память вида) · `verify-bug81` (**36**, витрина лендинга по
СОБРАННОМУ сайту — полоса из четырёх чисел) · `verify-bug80` (44, смайлики и улёт) · `verify-back-twins` (48,
«Назад» закрывает слой) · `verify-bug69` (графика, две скорости) · `verify-bug75` (44, панель) ·
`verify-bug76` (64, история) · `verify-bug84` (58 — вход гостя, две двери, врезки) ·
`verify-bug77` (64, **настоящий Chrome**) · `verify-bug87` (60, три состояния) ·
`verify-ptr` (65…66, число НЕ детерминировано — судить по «0 провалов») ·
`verify-manual-v1` (руководство, СОБРАННЫЙ сайт) · `verify-bug42` (виджет сервера, по блокам) ·
`verify-bugs08` (пачка 1 + личность новичка) · `probe-bug67` (геометрия оболочки, 3 блока) ·
`verify-pwa` (**24**, манифест и иконки, СОБРАННЫЙ сайт) · `verify-tierA` (досье связи) ·
`verify-prod-b4` (81, бой) · `npm test` (**263**) · `npm run test:calc` (**59**) · `verify-tierA` (досье связи) · `verify-bugs08`.

## 🤖 Пул автономных задач (без человека и без особого железа)

> Задачи, которые агент может делать ПОЛНОСТЬЮ автономно: код → сборка → стенд → фикс → коммит.
> Цикловые скиллы (`/autoloop`, `/dayloop`, `/nightloop`, `/guarded-loop`) мелют этот пул.
>
> 🆕 **Пул для СЛАБОЙ модели-исполнителя (Opus) — `plans/45`** (заказ владельца 2026-08-14):
> реестр заданий со строгими пошаговыми планами (`plans/44` — фаза 4 админ-эпика,
> `plans/46` — ярус 2 графики). Слабой модели — только задания из реестра; правила формата
> и жёсткая граница «чего ей не дают» — в самом `plans/45`.

> 🗂 Стрижка 2026-08-14: закрытые пункты (bugs/104 · ideas/25 · ideas/26 · макеты и дом панели
> ideas/29 · замер ideas/30) сняты — правда живёт в их документах и летописи. Урок о гниении
> пула — `EXP-0143`: перед пунктом пула проверяй статус документа и код.

- [ ] 🖼 **`ideas/27` — три яруса готовности графики. Шаги 0–2 ✅ 2026-08-14** (инвентарь; канон
      трёх ярусов в `AGENT_GUIDE`; **шаг 2 закрыт ЗАМЕРОМ с решением «прелоад не ставить»** —
      демо ниже фолда, первый вьюпорт — инлайн-SVG; ✅ посылка «лендинг держит 100» СНОВА ВЕРНА:
      `bugs/119` закрыт 2026-08-14, бой `/ru` Perf **100** × 2). Осталось: шаг 3 — ярус 2
      (фоновая догрузка; кандидаты — полные персоны 3×1,2 МБ И кропы лиц; нужен обзор индустрии
      по канону) · шаг 4 — «объект после картинки» с потолком · шаг 5 — расширить
      `verify-bug69`, мутации.
- [ ] 🔝 **`ideas/28` — ЭПИК владельца: вверху ленты всегда 10 популярных измерений.** Слово
      владельца 2026-08-01, основание — `plans/28`; шаг 0 снят: боевой `dims_list` **566 КБ**
      из 1 МиБ, добавка счётчика ~+41 КБ влезает. Плана ещё нет — писать при заходе
      (`/plan-task`). ⚠️ Инвариант, который нельзя «упростить»: 10 штук, СЛУЧАЙНЫЕ внутри пула
      популярных, остальная лента остаётся честно случайной — иначе хвост каталога не получит
      оценок никогда («бред!!!» об алфавитном списке).
- [ ] **Эпик `plans/15`, фаза 6 — АВАТАР: загрузить · заменить · удалить.** Разблокирован
      2026-08-01 (правила Storage в бою, CORS проверен), но по ценности теперь ПОСЛЕ ворот:
      фотография украшает продукт, о котором никто не знает.
      Операционный план ещё не написан — он третья ступень лестницы, пишется сейчас (`plans/13`
      поглощён фазой). Готовые решения не переоткрывать: `uploadBytes` (не resumable — `PUT` в
      CORS нет) · на Safari писать **JPEG** под именем `avatar.webp` (WebP оттуда не кодируется) ·
      однопальцевая альтернатива перетаскиванию обязательна (WCAG 2.5.7 AA) · инвалидация кэша
      лиц ПО UID (`avatar.ts:35` этого ждёт).
- [ ] **`ideas/24`, фаза 3 — подсистема настроек человека (`ideas/16`).** Автономна до выката
      правил Firestore: поле настроек в документе человека, правила, тесты правил, дебаунс.
      ⚠️ Внутри висит вопрос владельцу (что главнее при расхождении — браузер или профиль) и
      выкат правил требует его слова. Сначала написать `plans/14`.
- Остальной беклог ждёт владельца или железа (эстафета, п. 5, и раздел «⛔ ЧТО ЖДЁТ ВЛАДЕЛЬЦА»
  выше): тиры `plans/07` (PWA — нужен телефон владельца, `bugs/58`/`83`; `bugs/44`/`45` —
  макеты/решения) · `bugs/74` часть А — контракт кэша (вопрос владельцу).

---

## Прочти первым: перед каждым push — `npm run kaif:check`

Скрипт падает, если в git попал файл с секретом или ПДн. Утечка здесь уже случалась (`bugs/03`) — второй
раз она не должна пройти молча.

**Состояние репозиториев:** новый публичный `MikalaiKryvusha/ndim` (чистая история) ·
приватный архив 1.x `MikalaiKryvusha/ndim-old` (0 форков).

---

## Окружение (проверено 2026-07-10)

| Инструмент | Состояние |
|-----------|-----------|
| Node.js 24.15.0, npm, git 2.43 | ✅ |
| `gh` | ✅ авторизован как `MikalaiKryvusha` (scopes: `gist`, `read:org`, `repo`) |
| `firebase-tools` 15.23.0 | ✅ установлен (переустановлен: прежний 13.18.0 был битый — не хватало `lib/templates/`) |
| Токен Firebase | ✅ владелец перелогинился 2026-07-10; `firebase projects:list` → exit 0, проект `ndim` (`ndim-space`) виден. **Агент сам залогиниться не может** — CLI требует терминал |
| `gcloud` | ✅ авторизован (`kotkrinik@gmail.com`), проект `ndim-space` активен, Firestore `FIRESTORE_NATIVE` в `eur3` |
| Java (OpenJDK 21.0.11) | ✅ установлена; в PATH попадает **в новом сеансе** оболочки. Нужна эмулятору Firestore |
| Эмулятор Firestore | ✅ порт **8181** (дефолтный 8080 занят `llama-swap`). Адрес тесты берут из `FIRESTORE_EMULATOR_HOST` |
| Docker | ✅ демон запущен (сервер 29.3.1) — понадобится в фазе 4 |
| TypeScript | ✅ исполняется Node 24 нативно; `tsc` и `@types/node` — единственные dev-зависимости |
| Playwright | ✅ 1.61.1 + Chromium установлены 2026-07-11; `npm run e2e` (сам собирает сайт и поднимает preview на :4173) |

---


**Летопись проекта** — `PROJECT_HISTORY.md`: закрытые сессии, фазы и выкаты, перенесённые отсюда
ДОСЛОВНО, свежее сверху (первая миграция — 2026-07-31, обновление KAIF 2.1). В обязательный набор
чтения не входит — открывай, когда нужна археология решения или старой фазы.
