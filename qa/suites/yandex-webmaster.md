# Набор ЯНДЕКС.ВЕБМАСТЕР — наблюдение и подача переобхода

> **Создан:** 2026-08-28, смена 8, dev-3 · **Родитель:** `plans/74` Ф2 «Зрение» + `bugs/203` ·
> **Статус:** 🟡 частично исполнен живьём 2026-08-28 (день выдачи токена); подача переобхода —
> первый живой прогон 2026-08-29 · **Исходящее:** маркер `[TESTED]` приборов Ф2 опирается на
> ЭТОТ набор, а не на одно наблюдение

**Вопрос набора:** отдаёт ли машинный канал Яндекса правду о нашем хосте — и подаёт ли переобход
ровно столько, сколько ему разрешено человеком и квотой.

## 🔴 Зачем этот документ существует — он исправляет мою собственную ошибку

`TESTING_FRAMEWORK.md`, правило маркеров 7: «*A FEATURE marker requires a designed set… a single
observation flips the marker of a single CASE, never of the feature*». Я снял `[NOT-TESTED]` с
прибора наблюдения по ОДНОМУ живому прогону, назвав границы словами, — границы были честные, но
написанного набора за ними не стояло. Одно наблюдение переворачивает маркер СЛУЧАЯ. Набор ниже —
то, чего маркеру не хватало.

---

## Измерения покрытия — что именно разведено (и что сознательно нет)

Техники по `TESTING_FRAMEWORK.md` (шаг 2 цепочки): классы эквивалентности · граничные значения ·
таблица решений · переходы состояний · угадывание ошибок.

| Измерение | Значения, разведённые набором | Чем закрыто |
|---|---|---|
| **Доступ** | токена нет · токен есть · хост не подтверждён | Я-01, Я-02, Я-11 |
| **Разрешение человека** | без слова · подделка · почти верная цитата · точная цитата · сухой прогон | П-01, П-02, П-03, П-17, П-18, П-19 |
| **Остаток квоты** | 0 · 0 < остаток < очередь · остаток ≥ очередь · отрицательный | П-04…П-07 |
| **Ответ на подачу** | 202 принято · 409 уже в очереди · 429 квота · 400 адрес · 403/404 доступ · незнакомый код | П-08…П-13 |
| **Состояние очереди** | пуста · частично отправлена · нетронута | П-14, П-15 |
| **День ряда** | первый снимок дня · повтор того же дня | Я-08, Я-09, Я-10 |
| **Полнота снимка** | обе половины отданы · отказ половины при существующем файле · отказ половины без файла | Я-14, Я-15, Я-16 |
| **Словарь причин** | известный код · незнакомый код · пустая причина | Я-05, Я-06, Я-07 |

⛔ **Сознательно НЕ покрыто, и это названо, а не умолчано** (принцип 2 — приоритет по риску):
· **отказы самого Вебмастера** (5xx, обрыв связи, протухший токен) — уронить чужой сервис нарочно
  мы не вправе, а ждать его отказа неделями дороже риска; ветка держится юнитами на фикстурах;
· **весь словарь кодов причин** — живой прогон принёс ДВА кода из неизвестно скольких; правило
  ядра «складывать как пришли» именно поэтому и написано;
· **поведение на границе суток** (сброс квоты в полночь по Москве) — наблюдается один раз в сутки,
  проверяется первым прогоном 29.08; до тех пор правило суток держится разбором, а не замером.

---

## Случаи

Статусы: `pass` — наблюдалось · `fail` — наблюдалось иначе · `blocked` — нечем проверить ·
`skipped` — сознательно не гнали.

### Наблюдение — `tools/yandex-snapshot.mjs`

| # | Случай | Ожидание | Статус |
|---|---|---|---|
| **Я-01** | Токена в `.env` нет | Отказ называет переменную и домашку `homeworks/15`; код возврата **1** | ✅ `pass` 2026-08-28 — текст и код сняты прогоном |
| **Я-02** | Токен есть, права подтверждены | Печатает пользователя и хост; код 0 | ✅ `pass` — пользователь `130280754`, хост `ndimspace.app` |
| **Я-03** | Квота при выбранном дневном лимите | Суточная и остаток названы числами | ✅ `pass` — суточная 150, остаток 0 |
| **Я-04** | Вердикт пула при `--sent-by-hand 150` в тот же день | «общий», довод числами | ✅ `pass` — «израсходовано 150 при 150» |
| **Я-05** | Ряд событий поиска | Итог сходится: выпало + появилось + без типа = всего | ✅ `pass` — 1379 + 1388 + 0 = 2767 |
| **Я-06** | Разбор причин выпадения | Коды складываются как пришли, без перевода | ✅ `pass` — `LOW_QUALITY` 1378 · `NO_INDEX` 1 |
| **Я-07** | Разрез по разделам сайта | Сумма по разделам сходится с числом выпавших | ✅ `pass` — 1376 + 2 + 1 = 1379 |
| **Я-08** | Первый снимок дня | Файл создан именем дня | ✅ `pass` — `2026-08-28_yandex_webmaster.json` |
| **Я-09** | Повтор того же дня (идемпотентность) | Файл ОДИН, а не два | ✅ `pass` — файлов ряда 1 после двух прогонов |
| **Я-10** | Повтор того же дня — честность вывода | Прибор говорит, что ПЕРЕЗАПИСАЛ | ✅ `pass` 2026-08-28 (было `fail`) — печатает «ПЕРЕЗАПИСАН» + отсылку к истории git; снято контрольным прогоном |
| **Я-14** | 🔴 `bugs/215`: отказ ПОЛОВИНЫ статистики при уже лежащем файле дня | Код **1** · файл дня НЕ тронут · названо «СТАТИСТИКА НЕ ОТДАНА» и «ФАЙЛ ДНЯ НЕ ТРОНУТ» | ✅ `pass` — мутация пути статистики: код 1, хеш файла до и после совпал (`10c0850079`) |
| **Я-15** | Контроль к Я-14: исправный ответ | Код 0, день перезаписан | ✅ `pass` — код 0, «ПЕРЕЗАПИСАН» |
| **Я-16** | Отказ половины, когда файла дня ЕЩЁ НЕТ | Файл пишется (день без файла хуже), код 1 | ✅ `pass` (юнит) — таблица решений покрыта целиком |
| **Я-11** | Хост не подтверждён | Отказ называет хост | ⛔ `blocked` — наш хост подтверждён; ронять права нарочно нельзя |
| **Я-13** | Полный поимённый список в снимке | `searchEvents.samples` несёт ВСЕ события; удалённых в нём столько же, сколько насчитала сводка | ✅ `pass` 2026-08-28 — 2767 событий, удалённых 1379 = сводка 1379, у всех 1379 причина названа |
| **Я-12** | Вердикт пула на следующий день после ручной отправки | «неизвестно» либо честное «сопоставлять не с чем», НЕ «раздельный» | ⏳ `skipped` до 29.08 — это и есть проверка правила суток |

### Подача — `tools/yandex-recrawl.mjs`

| # | Случай | Ожидание | Статус |
|---|---|---|---|
| **П-01** | `--apply` без `--auth-owner` | Отказ, код **1**, БЕЗ единого сетевого вызова | ✅ `pass` 2026-08-28 |
| **П-02** | Сухой прогон (без `--apply`) | План и числа, не отправлено ничего, код 0 | ✅ `pass` |
| **П-03** | `--apply` со словом владельца | Подача идёт, журнал пополняется | ⏳ `skipped` до 29.08 — сегодня квота 0 |
| **П-04** | Остаток квоты 0 | К отправке 0, ограничитель «квота исчерпана», ничего не послано | ✅ `pass` — живой прогон при остатке 0 |
| **П-05** | 0 < остаток < очередь | Шлём ровно остаток, ограничитель «квота» | ✅ `pass` (юнит) — 110 при очереди 1201 |
| **П-06** | Остаток ≥ очередь | Шлём очередь, ограничитель «очередь» | ✅ `pass` (юнит) |
| **П-07** | Остаток отрицательный | Порция 0, а не отрицательная | ✅ `pass` (юнит) |
| **П-08** | Ответ 202 | Принято, остаток квоты берётся из ответа | ✅ `pass` (юнит) |
| **П-09** | Ответ 409 | ПРОПУСК, не провал; адрес в журнал, квота цела | ✅ `pass` (юнит) |
| **П-10** | Ответ 429 | ОСТАНОВ прогона, а не пропуск адреса | ✅ `pass` (юнит) |
| **П-11** | Ответ 400 | Отказ одного адреса, прогон продолжается | ✅ `pass` (юнит) |
| **П-12** | Ответ 403/404 | ОСТАНОВ: сломан доступ, а не адрес | ✅ `pass` (юнит) |
| **П-13** | Незнакомый код | Не выдаётся за успех, называет себя числом | ✅ `pass` (юнит) |
| **П-14** | Очередь разобрана целиком | «ОЧЕРЕДЬ РАЗОБРАНА ЦЕЛИКОМ», а не «квота исчерпана» | ✅ `pass` (юнит) — причины различены |
| **П-15** | Повторный запуск в тот же день | Уже отправленное не подаётся снова | ⏳ `skipped` до 29.08 — журнал отправленного пуст, проверяется первой подачей |
| **П-17** | 🔴 `--auth-owner "x"` — подделка, которую показал судья | Отказ, код **1**, без сети | ✅ `pass` 2026-08-28 — сверка ТОЧНАЯ с первоисточником |
| **П-18** | Разрешение читается из `bugs/203`, а не из шапки прибора | Прибор называет документ и раздел; нет раздела/цитаты — отказ | ✅ `pass` — прочитано «агент будет сам отправлять правки в яндекс»; юниты на отсутствие раздела и цитаты |
| **П-19** | Почти верная цитата (регистр, пропущенное слово) | НЕ проходит | ✅ `pass` (юнит) |
| **П-16** | Файла очереди нет | Отказ объясняет, что `test-results/` вне git и в worktree не едет | ✅ `pass` — текст прочитан; путь пройден при переносе очереди |

---

## Итог покрытия на 2026-08-28

**`pass` 30 · `fail` 0 · `blocked` 1 · `skipped` 4.**
Из 30 пройденных **14 сняты живым прогоном** на боевом токене, остальные — юнитами на фикстурах
задокументированных ответов (`researches/56` §3.5). Единственный `fail` набора (Я-10) закрыт
починкой в тот же день; статус переписан вместе с наблюдением, а не молча.

🔴 **Что этот итог ЧЕСТНО означает.** Маркер `[TESTED]` у прибора наблюдения опирается на девять
живых случаев и названные границы — не на «прогнал разок». Маркер прибора подачи феечного уровня
**ещё не выставлен и не будет**, пока не отработают П-03 и П-15: подача, ни разу не подавшая ни
одного адреса, фичей проверенной не является, сколько бы юнитов под ней ни лежало.

**Что закрывает набор 29.08:** П-03 · П-15 · Я-12 — и тогда же снимается вопрос о поведении на
границе суток.
