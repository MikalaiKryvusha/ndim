# Эстафета: суд пары «набор Аналитики + код `env`» — свежему окну QA

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16. **Повторяй это вслух каждый раз, когда читаешь канон-документы
> KAIF** — прямое его поручение. Разбор повода — в шапке `AGENT_GUIDE.md`.
<!-- ВЕРА-КОНЕЦ -->

> **Написана:** 2026-08-29, смена 12, окно QA, при закрытии · **Кому:** свежему окну QA ·
> **Предмет:** порция Интегратора `f31c85d` (свойство контура `env`) + мой набор
> `qa/suites/analytics.md`. Судятся ПАРОЙ: кейсы без кода гонять не на чем, код без кейсов не
> проходит ворота владельца.
>
> 🔴 **ЧЕГО ЗДЕСЬ НЕТ НАМЕРЕННО: суждений о предмете.** Порцию Интегратора я не судила. Всё
> ниже — либо снято МОЕЙ командой (и команда приведена), либо названо чужим заявлением с именем
> заявившего. Вердикт пишется с нуля.
>
> ⚠️ **Почему это правило стоит первым.** Эстафета, которую получила Я, несла строку «по словам
> Менеджера, чинился `verify-bug40.mjs`». Файл был не тот, неправда доехала через два документа
> до самого суда и стоила мне отвода. **Утверждение о предмете называет КОМАНДУ, которой
> проверяется, а не того, кто его произнёс.**

---

## 1. Числа — сняты мной, ПЕРЕСНИМИ ВСЕ

| Что | Значение при закрытии | Команда |
|---|---|---|
| моя ветка `ndim_qa` | **`7b787d7`** | — |
| позади ствола | 2 | `git rev-list --count HEAD..main` |
| мерж моей ветки | конфликтов нет | `git merge-tree --write-tree main HEAD` → код 0 |
| ветка Интегратора | **`f31c85d`** (его слово: база `main = 62bf69d`, позади 0) | — |
| `main` при закрытии | **`2b13e07`** | — |

⚠️ **Ствол двигался под судьёй всю смену — за моё окно он ушёл на 30+ коммитов.** Все четыре
числа переснимай сам, и **всякое утверждение о ПАРЕ (ветка ↔ ствол) неси с обоими sha внутри
формулировки** — правило уже в манифесте, оплачено дважды за эту смену.

## 2. Что уже сделано и лежит готовым

- **`qa/suites/analytics.md`** — тест-план, кейсы техниками (переходы А1–А6 · границы Б1–Б9 ·
  негативные Н1–Н3), чек-лист из одиннадцати шагов, пять классов провала, контрольный случай.
  Маркер **`[NOT-TESTED]`**: прогонов нет ни одного.
- **`qa/JOURNEYS.md`** — восемь путей `NDIM-SYSTEM-010…017`, сводка 149 → **157**, линтер зелёный.
- **Замер слоя перехвата (мой, 2026-08-29):** локальная сборка боя под боевым именем
  (`--host-resolver-rules="MAP ndimspace.app 127.0.0.1:<порт>"`) даёт `location.hostname =
  ndimspace.app`, обращений к PostHog **3**, наружу **0**; контроль под `localhost` — **0**.

## 3. 🔴 Объём суда — назван мной Менеджеру, планируй окно от него

1. **Перепрогнать все четыре мутации своими руками.** Его числа: стейдж из белого списка → 4
   красных · ветка `env` из `propertyValueIsSafe` → 1 · **отцепить супер-свойство → 8** ·
   переименование контура в `contours.mjs` → красная ровно пара. Числа его, перепрогон твой.
2. **`env` приходит на ВСЕХ ДЕСЯТИ именах белого списка, а не на выборке.** Он сам назвал, что без
   специально написанного случая мутация уронила бы только случайные попадания.
3. 🔴 **Стенд по-прежнему МОЛЧИТ.** Порция трогает белый список хостов. Молчание стенда —
   **единственный отрицательный контроль всей фичи**; умрёт он — набор останется без контроля, и
   любое зелёное в нём станет бессодержательным. Это главная проверка суда.
4. **Одиннадцать шагов чек-листа на стейдже** — нужен замок стенда.

## 4. Капканы, оплаченные этой сменой, — читать ДО первой команды

- **Метка прибора глушит отправку целиком** (`analytics.ts:318`): помеченный прогон не отправит
  ничего. Пометиться и посмотреть приём НЕВОЗМОЖНО.
- **Сборка запекает порты эмуляторов по слоту КАТАЛОГА** (`vite.config.ts:77–79`, `slotOf(WORKPLACE)`).
  Артефакт, собранный не там, где поднят стенд, даёт `ERR_CONNECTION_REFUSED` — это поломка
  ПРОГОНА, а не фичи. Нашёл Интегратор ценой захода.
- **Возраст стенда.** Класс «Auth и Firestore расходятся за десять часов» оплачен: красное вида
  «документа `users/{uid}` нет в базе» — это состояние стенда, а не твои кейсы. Скажи Менеджеру,
  он перезапустит начисто.
- **Якорь мутации бери коротким и ASCII-ным.** Мой длинный кириллический якорь не лёг, а набор
  напечатал честные 570/0 — зелёное означало «мутации не было». Спас контроль применения,
  встроенный в скрипт ДО прогона. Урок dev-3 той же смены, сработавший на судье в тот же день.

## 5. Мой собственный класс ошибок за смену — назван, чтобы ты его не повторил

**Три раза за смену я строила СЛИШКОМ ШИРОКОЕ правило из верного наблюдения:**

| # | Наблюдение (верное) | Правило (слишком широкое) | Кто снял |
|---|---|---|---|
| 1 | хост решает и аналитику, и базу | «варианта настоящие события + ненастоящие данные НЕ существует» | владелец, решением про `env` |
| 2 | пара с `contours.mjs` нужна | «выведи союз ИЗ `contours.mjs`» — затащило бы `node:fs` в бандл | Интегратор |
| 3 | юнит может импортировать `.mjs` | «сверяй пару импортом» — при `checkJs` даёт `TS7006` на нетипизированном модуле | Интегратор |

🔑 **Проверка на этот класс, стоившая мне трёх случаев:** назвав правило, ищи **ТРЕТИЙ случай,
который оно обязано объяснить**. Для №1 им был стейдж, для №3 — нетипизированный модуль. Ни разу
неверным не было НАБЛЮДЕНИЕ; каждый раз — правило между наблюдениями.

## 6. Мои открытые хвосты — ничего не блокируют

- **Условия вердиктов №33 и №34 закрыты все** (`qa/team-verdicts.md` → «Закрытие условий»).
  Дизайнер `291cfdf` — семь из семи, dev-3 `2dc6106` — половина У2.
- **Заявка моей ветки идёт в ту же пару** (решение Менеджера): голова `7b787d7`.
- Замков не держу, деревьев суда не оставила, `git status` чист.
