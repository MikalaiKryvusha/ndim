# Отчёт по тестированию — экран Б3 «Спросить до профиля» и вкладка «письмо отправлено» (порция 1)

> **Создан:** 2026-09-25 17:40 +03:00 (dev-1, постановка №2 Менеджера) · **Родитель:** `plans/NEW_signin_link_forks_b3_g2.md`
> порция 1 · `bugs/233` · набор `qa/suites/signin-link-forks.md` · **Статус:** ✅ pass · **Исходящее:** EN-строки экрана Б3 —
> на вычитку владельцу (список ниже).

## Карточка прогона

- **Работа:** решение владельца №096 В11 = В — экран Б3: ссылка из письма для адреса А открыта там, где вошёл аккаунт Б →
  прежде профиля экран называет оба адреса и две дороги (`linkFork` в `src/lib/data/account.ts`, шаг `fork-account` в
  `src/lib/ui/SigninScreen.svelte`, развилка в `finishEmailLink` `src/routes/profile/+page.svelte`). Плюс хвост `bugs/233`:
  вкладка «Мы отправили Вам письмо» уходит в профиль, когда ссылку открыли в соседней вкладке того же браузера
  (`onSignedInElsewhere`, `onIdTokenChanged`).
- **Контур:** стенд слота 3 рабочего места dev-1 (`npm run stand`: vite `localhost:5203`, эмулятор Auth `127.0.0.1:9129`,
  проект `demo-ndim-dev`), Chromium Playwright; «другой браузер» — отдельный контекст (своё хранилище), «соседняя вкладка» —
  вторая страница того же контекста. Бой и стейдж не тронуты.
- **Прогонов:** 2026-09-25 +03:00, команда каждого — `node qa/reports/2026-09-25_signin-link-forks.driver.mjs` из корня
  рабочего места:
  · 17:09:58 — ВС-01…ВС-05 pass, ВС-06 упал на ДРАЙВЕРЕ (в английском контексте искал русскую кнопку двери) → драйвер
    исправлен (подготовка русской дверью, язык — перед ссылкой);
  · 17:12:29 — полный набор, провалов 0;
  · 17:15:36 · 17:17:51 · 17:27:09 — мутант (`node <скретчпад>/mutate-forks.mjs`: `linkFork` всегда `null` + подписка
    вкладки снята; файлы восстанавливаются побайтово — «restored byte-equal: true» в каждом заходе). Первые два захода
    не дошли до кейсов вкладки из-за хрупкости драйвера (клик и `innerText` отсутствующей кнопки ждали по 30 с) — драйвер
    сделан терпимым; третий заход полный;
  · 17:32:05 — финальный чистый прогон на исправленном драйвере, провалов 0.
- **Проверок:** функциональных кейсов 9 (ВС-01…ВС-09), пройдено 9 · провалено 0; мутант — провалов 13, ровно в названных
  адресатах, контроли зелёные.
  - Гигиена: юниты `account.test.ts` 17/17 (из них 4 новых на `linkFork`) · `npm test` 535/544 — 9 красных не от порции:
    проверки по выгрузке боевого каталога вне git (`src/lib/content/dims-build.json` в рабочем месте dev-1 устарел: 5125
    записей против 5153) · `npm run test:tools` 845/845 · `npm run check` 0 ошибок · `npm run typecheck` 0 ·
    `npm run guards` 22/22 · `npm run e2e` при погашенном стенде 112 passed, 4 skipped, 0 failed (17:35–17:37) —
    *поправка по суду порции 1: этот e2e шёл на рабочем дереве ДО коммита `7ac33ec` и до слияния с main, а не на голове
    `b1ab255`, как говорила заявка; на голове e2e не перепрогонялся.*
  - Функциональный прогон: стенд · живой браузер, кадры `test-results/signin-link-forks/` прочитаны глазами:
    · **ВС-01** pass — экран Б3 дословно по кадру `Vb3.png`: заголовок «Ссылка из письма для другого адреса», строка «Сейчас
      Вы в аккаунте Б. Ссылка из письма открывает аккаунт А.», кнопки «Остаться в аккаунте Б» / «Войти в аккаунт А»; пока
      вопрос открыт, в сессии Б; 390 светлая и тёмная, 1440 светлая и тёмная — длинный адрес А переносится внутри строки и
      кнопки, горизонтального переполнения нет (`scrollWidth ≤ innerWidth`).
    · **ВС-02** pass — «Остаться»: профиль Б, гостевой карточки нет, `email=` стёрт из строки браузера, консоль чиста.
    · **ВС-03** pass — «Войти»: шаг «идёт вход» называет адрес А (кадр `vs03-switch-spinner.png`), затем профиль А, без «Не
      удалось загрузить», консоль чиста.
    · **ВС-04** pass (контроль) — ссылка для Б там, где вошёл Б: экрана Б3 нет, в сессии Б.
    · **ВС-05** pass (контроль) — чистый браузер: экрана Б3 нет, шаг «идёт вход», в сессии А.
    · **ВС-06** pass — английская половина: «The link in the email is for a different address», кнопки «Stay signed in to the
      Б account» / «Sign in to the А account», русских строк нет.
    · **ВС-07** pass — гость на шаге «Мы отправили Вам письмо», ссылка во второй вкладке → вкладка 2 «Профиль сохранён»,
      вкладка 1 сама ушла на `/profile` (кадр `vs07-tab1-after.png`: профиль с адресом А, пилюли «гость» нет), тот же uid.
    · **ВС-08** pass — без сессии, шаг «Письмо отправлено», ссылка во второй вкладке → вкладка 1 в профиле А.
    · **ВС-09** pass (контроль) — никто не вошёл: вкладка стоит на шаге «Мы отправили Вам письмо» тот же срок.
    · **Мутант** (контроль прибора): ВС-01 ×3 и три клетки, ВС-03 ×2, ВС-06, ВС-07 «вкладка ушла», ВС-08 — красные; ВС-02 и
      ВС-03 «консоль чиста» красные адресом `accounts:signInWithEmailLink` 400 — это и есть прежнее поведение Б1 (адрес
      вошедшего предъявлялся Firebase и не подходил); контроли ВС-04, ВС-05, ВС-09 зелёные. ⚠️ Проверка ВС-07 «А, не гость,
      тот же uid» зелёная и на мутанте — она читает общее хранилище Firebase браузера, а не экран; различает кейс проверка
      «вкладка ушла».
- **Найдено:** дефектов продукта — 0 новых; закрыт хвост `bugs/233` «вкладка „письмо отправлено“ не открывает профиль сама» в
  том же браузере (подтверждён 09:53, теперь ВС-07/ВС-08 pass). Дефекты драйвера — 3, вылечены до финального прогона (русская
  кнопка в английском контексте; клик и `innerText` отсутствующей кнопки ждали по 30 с). Независимый проход §7Б (субагент)
  нашёл кальки в английских строках Б3 — исправлены до прогона.
- **Следы:** на эмуляторе стенда слота 3 — учётки прогонов `vs0N-…@ndim.space` и гости ВС-07/ВС-09 (стенд погашен, база
  эмулятора живёт до следующего засева); кадры `test-results/signin-link-forks/` (вне git); бой и стейдж — ничего.
- **Вердикт:** **pass** — экран Б3 исполняет №096 В11 = В на стенде, обе дороги ведут в нужный аккаунт, контроли без
  регресса, вкладка «письмо отправлено» теперь исполняет свою строку в том же браузере. Границы: два разных движка браузера
  не доказаны (контекст = «другой браузер» по хранилищу); письмо без адреса проверено юнитом, живьём не выпускается; бой и
  стейдж — при выкате.

## Строки на вычитку владельцу (EN — черновик агента, RU — дословно с кадра `Vb3.png`)

| Место | RU (утверждено) | EN (черновик после §7Б) |
|---|---|---|
| заголовок | Ссылка из письма для другого адреса | The link in the email is for a different address |
| строка | Сейчас Вы в аккаунте {Б}. Ссылка из письма открывает аккаунт {А}. | You are currently signed in to the {Б} account. The link in the email signs you in to the {А} account. |
| кнопка 1 | Остаться в аккаунте {Б} | Stay signed in to the {Б} account |
| кнопка 2 | Войти в аккаунт {А} | Sign in to the {А} account |

Проверка голоса: `node .kaif/tools/kaif-voice-lint.mjs load` (свидетель 17:03) → `check src/lib/ui/SigninScreen.svelte` —
**SKIPPED** (§8 портрета без таблицы образцов, код 3) → проход §7Б чистым субагентом: нарушений З1–З6 нет, кальки «from the
email», «opens the account», порядок «the account X», «Stay in the account» — заменены.
