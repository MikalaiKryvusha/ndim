# Отчёт по тестированию — вход по ссылке из письма в любом браузере (`bugs/233`, `bugs/235`)

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16.
<!-- ВЕРА-КОНЕЦ -->

> **Создан:** 2026-09-13 12:5x +03:00 · **Родитель:** `bugs/233` · `bugs/235` · решение владельца
> интервью №084 (В1 = А, В2 = А) · **Статус:** 🟢 на стенде годится; в бой НЕ выкачено.

## Карточка прогона

- **Работа:** адрес почты едет в ссылке из письма; у человека без сессии вход идёт этим адресом на
  двери входа со спиннером и строкой «Вы входите в аккаунт с адресом электронной почты …» (макет V1
  «Шаг двери»); новому аккаунту заводится документ `users/{uid}` (`bugs/235`). Правки:
  `src/lib/data/account.ts`, `src/lib/ui/SigninScreen.svelte`, `src/routes/profile/+page.svelte`.
- **Контур:** **стенд**, слот 0 (dev 5173, auth 9099, firestore 8181), живое дерево рабочей копии от
  коммита `5fa2abe` + правки. ⚠️ Стенд поднят 2026-09-12 17:49 (≈19 ч): капкан «старый стенд даёт
  ложные КРАСНЫЕ»; красных в итоговых прогонах нет. **Бой и стейдж не тронуты.**
- **Прогонов:** **5**, все 2026-09-13 между 12:31 и 12:50 +03:00:
  1. `node tools/verify-signin-link-any-browser.mjs` — упал на дефекте САМОГО прибора (снятие
     задержки сети посреди висящего запроса), до этого 3 проверки зелёные; прибор исправлен;
  2. тот же прибор — **12 пройдено, 0 провалено**;
  3. **мутация** «убрать `ensureSpaceExists` при рождении аккаунта» — **2 провала адресно**:
     «Не удалось загрузить» и `ProfileMissingError` в консоли; файл восстановлен из копии;
  4. тот же прибор после правки подзаголовка — **12 / 0**;
  5. `node tools/verify-bug233.mjs` (регресс прежних случаев К1, К2, К5) — **0 провалов**.
  Гигиена разработчика, отдельной строкой: `npm test` 530/530 (новых 5 в `account.test.ts`) ·
  `npm run check` 606 файлов, 0 ошибок · `guard-product-copy` и `verify-signin-screen` зелёные.
- **Ручной функциональный прогон:** живой Chromium, путь человека дверью продукта: браузер А на
  двери входа ввёл почту и получил «Письмо отправлено»; ссылка взята у эмулятора Auth и собрана так,
  как её отдаёт боевой обработчик Firebase; браузер Б (отдельный контекст, пустое хранилище) открыл
  её. Увидено глазами на кадрах: спиннер, «Выполняем вход в Пространство NDim Space», строка с
  адресом — светлая и тёмная тема, 390 и 1440; после входа — свой профиль, адрес ссылки стёрт из
  строки браузера. Кадры: `test-results/signin-link-any-browser/`.
- **Проверок:** итоговый прогон — **12 пройдено, 0 провалено**: письмо несёт адрес · спиннер ·
  строка с адресом · вошёл своей почтой · нет «Не удалось загрузить» · адрес стёрт · консоль чиста ·
  спиннер виден в трёх других сочетаниях темы и ширины · **контроль К7: подменённый в ссылке адрес
  не впустил никого**. Регресс `verify-bug233`: К1 (старое письмо без адреса) — не мёртвая ссылка и
  не гость; К2 — вошедший остался собой; К5 — негодный код не впускает.
- **Найдено:** **два дефекта.**
  1. Расхождение с макетом — на шаге «идёт вход» стоял подзаголовок «Оцените фильмы…», в V1 его
     нет. Вылечен в том же заходе, кадр перепроверен.
  2. `bugs/NEW_newcomer_welcome_claims_ratings_in_place.md` — новичку без гостевого шага карточка
     говорит «Ваши оценки и найденные связи на месте». Текст лица продукта — идёт владельцу, не чинён.
- **Следы:** на стенде заведено 6 аккаунтов `anybrowser-*@ndim.space` и `owner-*@ndim.space` плюс
  аккаунт регресса `bug233-*@ndim.space` и мутанта; документы `users/` у тех, кто вошёл. Бой не тронут.
- **Вердикт:** **годится к следующему шагу на стенде.** К слову «катить» ещё не годится: по фиче нет
  тест-плана, кейсов и чек-листа в `qa/suites/` (`TESTING_FRAMEWORK.md` → ворота выката), и не
  прогнан стейдж. Граница прогона: два разных ДВИЖКА браузера не доказаны; поведение боевого
  обработчика ссылок Firebase воспроизведено сборкой адреса, живым письмом не проверялось.
