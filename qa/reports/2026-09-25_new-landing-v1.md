# Отчёт по тестированию — новая V1 главной на `/`, `/ru`, `/en`: тест на совместимость, мост в гостя, Связи

> **Создан:** 2026-09-25 16:35 +03:00 · **Родитель:** `plans/106_epic104_phase2_new_v1_on_main.md` (Д10–Д11) · набор
> `qa/suites/new-landing-v1.md` · **Статус:** ✅ годно к выкату в бой после ответов владельца (интервью №097) ·
> **Исходящее:** интервью №097 — список теста, язык главной, новые строки RU, английская версия, раскладка на телефоне.

## Карточка прогона

- **Работа:** новая V1 главной из утверждённого макета `design/new-landing-v1.html` (№096 В5, В6 = Б, В15, В16) на трёх
  адресах — `LandingV1.svelte`; тест на совместимость на десяти объектах каталога (№096 В14); перенос звёзд теста в NDim ID
  гостя по «Смотреть больше» (№096 В8 = А); ранний тап до гидратации; счёт прихода главной через SDK; строка мостика на
  «Связях»; английская версия.
- **Контур:** стенд (`npm run stand`: эмуляторы, сервер синхронизации, `vite dev` :5173; стендовая дверь `?as=guest` —
  свежий гость) и стейдж `ndim-stage.web.app` — сборка коммита `b042058` (метка `build-stamp.json`: sha `b0420589…`,
  `dirty: true` от неотслеживаемого файла интервью; код сборки совпадает с коммитом — проверено по живому поведению:
  английский процент на стейдже ещё «27 %», правка «27%» сделана после сборки и в неё не попала). Сервер синхронизации
  стейджа поднят на время прогона (`node tools/run-sync-server.mjs --contour stage`, 16:25–16:30) и погашен.
- **Прогонов:** 2026-09-25 +03:00:
  - 15:47–15:48 (метки кадров) — `npm run build` → `npx vite preview --port 4173 --strictPort` →
    `node tools/shoot-new-landing.mjs` (кадры 12 видов + сценарий теста, `test-results/new-landing-v1/`); повтор после
    правок — до коммита `306ff36` (16:03);
  - ≈16:07 (метка кадра `asguest-before-bridge.png`) — стенд: `MSYS_NO_PATHCONV=1 node qa/reports/2026-09-25_new-landing-v1.driver.mjs --path '/ru?as=guest'`,
    `--path '/?as=guest'`, `--path '/ru?as=guest' --stars 0` (первый заход без `?as=guest` записал оценки dev-пользователю
    стенда — прогон признан недействительным, след убран, см. «Следы»);
  - 16:11–16:20 и 16:21–16:29 — `npm run deploy -- --stage` дважды (коммиты `977a6a5` и `b042058`), ворота смоука по
    собранному артефакту на стенде и смоук под сессией после выката;
  - 16:22 — `MSYS_NO_PATHCONV=1 node qa/reports/2026-09-25_new-landing-v1.stage-driver.mjs --path / --wait 180` (первый
    заход: Связей нет, событий в PostHog нет — см. «Найдено»);
  - 16:25–16:28 — тот же драйвер человеком (маскировка робота снята) с поднятым сервером стейджа: `--path /` и
    `--path /ru` (вывод — `test-results/new-landing-v1-stage/run-ru.json`);
  - 16:29 — `node tools/shoot-new-landing.mjs --base https://ndim-stage.web.app --out test-results/new-landing-v1-stage-frames`;
  - 16:30 — чтение PostHog: `hogql` «события `landing_view` · `demo_touch` · `guest_start`, `env = stage`, 15 минут».
    *Поправка 2026-09-25 16:46: окно «с начала прогона» драйвер передавал строкой UTC без пояса, PostHog читал её в
    +03:00 — фактическое окно было на три часа шире названного (класс `bugs/NEW_week_number_window_labelled_utc_is_moscow.md`).
    Перечитано с явным поясом — окно 16:00–16:45 +03:00 (`toDateTime('2026-09-25 13:00:00', 'UTC')` …
    `'13:45:00'`): ровно 6 событий — `demo_touch` → `landing_view` → `guest_start` с `/` (16:25:52–55, `entry = root`)
    и с `/ru` (16:28:11–14, `entry = landing`). Вывод НЛ-19 не меняется.*
- **Проверок:** 20 кейсов набора — пройдено 20, провалено 0. Руками на стенде и стейдже — 16 (НЛ-01, 02, 03, 06, 07,
  08, 09, 10, 12, 13, 14, 15, 16, 17, 19, 20); только e2e на собранном артефакте — 4 (НЛ-04 тема, НЛ-05 числа, НЛ-11
  ранний тап, НЛ-18 ссылка из письма).
  - Гигиена: `npm test` 538/538 · `npm run test:tools` 832/832 · `npm run guards` 22/22 · `npm run check` 0 ошибок ·
    `npm run e2e` 112 прошли / 4 пропущены (проверки одной ширины) / 0 провалов — до коммита `306ff36`; после правки
    текстов `b042058`: `npm test` 538/538, guards 22/22, check 0. Дверь выката: смоук по артефакту 21 кейс 0 провалов,
    после выката 16 + 11 проверок 0 провалов, «приложение работает под сессией» — оба выката.
  - Функциональный прогон: стенд и стейдж · живой Chromium, 390 и 1440, светлая и тёмная темы, RU и EN · путь человека
    главная → звёзды → «Смотреть больше» → профиль гостя → «Связи» → «Назад» · ПРОЧИТАНО:
    - НЛ-01…НЛ-03: на `/`, `/ru`, `/en` × 2 ширины × 2 темы (12 видов на стейдже) — заголовок А, трио портретов с
      пузырями, `html[lang]` ru/ru/en, горизонтальной прокрутки нет, консоль стейджа 0 ошибок; кадры `root-390-dark`,
      `en-1440-light`, `ru-1440-dark`, `ru-390-rated-*` просмотрены глазами;
    - НЛ-08…НЛ-10: тест стартует пустым (у персонажей «—»); «Матрица» 9 → поп-ап «Макс — Ваша самая сильная связь:
      40 %» (три оценки), карточки 40 % · 28 % · 22 %, карточка для сторис называет Макса; повторное касание снимает;
    - НЛ-12: стейдж, мост с `/` — в `points/{uid}/dims` гостя ровно три документа, значения совпали (10 · 9 · 8), мост
      3,1 с; с `/ru` — то же, 3,1 с; стенд — три документа, 0,2 с; `/profile?guest=root` и `?guest=landing`;
    - НЛ-13: мост без оценок — гость с пустым NDim ID, строка мостика «Это была демонстрация…»;
    - НЛ-14: «Назад» после моста — вкладка уходит в пустоту (`blank`), лендинга в истории нет;
    - НЛ-15, НЛ-16: стейдж, сервер синхронизации поднят — документ Связей гостя через 89 с (с `/`) и 7 с (с `/ru`), на
      экране «Связи» 23 человека стейджа; строка «Ваши оценки из теста на совместимость сохранены в Вашем профиле — вот
      Ваши Связи: люди с наибольшей Похожестью на Вас.»; кадр `03-relations.png` просмотрен;
    - НЛ-19: PostHog, `env = stage` — `landing_view` с `/` и `/ru`, браузер Chrome; `demo_touch`; `guest_start` ровно по
      одному на вход, `entry = root` с `/` и `entry = landing` с `/ru`;
    - НЛ-06, НЛ-07 (стейдж, ≈16:33): вопрос второй группы раскрывается; девять ссылок подвала — все 200;
    - НЛ-17 (стейдж, ≈16:33): живая сессия гостя — `/ru` уводит в `/profile` за 658 мс, `/` — за 232 мс. Поддельный маркер
      без сессии: `/` уводит (инлайн-дверь по маркеру), `/ru` остаётся — `hasSession` видит, что маркер соврал, так задумано
      и так было у прежнего `/ru`; формулировка кейса в наборе уточнена.
- **Найдено:** один дефект контура (сервер синхронизации стейджа не работает), два дефекта приборов, наблюдение о
  времени моста; дефекты кода найдены и вылечены до прогона:
  1. 🔴 **На стейдже не работает сервер синхронизации с 2026-09-13** (журнал запуска: «последний ночной проход был
     2026-09-13T07:00:12Z»): Связи на стейдже не считаются вовсе, и любой прогон «Связей» на стейдже слеп — первый заход
     дал «Связей нет» при исправном продукте. Документ — `bugs/NEW_stage_sync_server_not_running.md`.
  2. Прибор, а не продукт: драйвер без снятых признаков робота пришёл headless-роботом, SDK PostHog его отсеял — «событий
     0». Лечение в драйвере (обычный user agent, `AutomationControlled`, `userAgentData`), урок уже записан у снятой пробы
     главной.
  3. Прибор, а не продукт: на стенде голый адрес входит как `dev@ndim.space`; первый прогон моста записал оценки ему.
  4. Наблюдение, не дефект: мост на настоящей сети — 3,1 с (гость рождается, профиль заводится, три записи); кнопка
     приглушается на это время. Ускорить можно параллельной записью после первой — не делалось.
  5. Дефекты кода, найденные и вылеченные до прогона (кадрами и независимой проверкой текстов): лицо Алисы срезалось
     верхом карты в пустом старте; пропал пробел перед годом; на телефоне карта уходила под список из 10 строк; в
     английском стояли ёлочки «…» и «40 %»; «увлечения» против подписи «Практика», противоречие ответов FAQ, скрытый
     контраст «настоящие связи» и обещание «здесь будут» в строке мостика, кальки английского.
- **Следы:** четыре гостевые учётки стейджа удалены, документы профиля и Связей гостей остались до уборки сервера;
  события стейджа в PostHog; гости в эмуляторе стенда; кадры вне git:
  - стейдж: 2 гостевые учётки заведены и удалены их же токеном (оценки, точка, учётка — ответы 200); документы
    `users/{uid}` и `relations/{uid}` двух гостей остались — Связи пишет только сервер синхронизации, осиротевших гостей
    чистит его уборка; прогон первого захода — третья учётка удалена так же; НЛ-17 — четвёртая учётка гостя (без
    оценок) удалена её токеном (200); счётчики воронки стейджа
    `space/funnel/days/2026-09-25` выросли на заходы кадров и драйверов; 6 событий в PostHog с `env = stage`;
    сервер синхронизации стейджа пересчитал Связи пользователей стейджа за время работы (16:25–16:30);
  - стенд: 3 гостя в эмуляторе; три оценки, ошибочно записанные dev-пользователю, удалены (DELETE 200 ×3, остались его
    4 оценки сида);
  - кадры — `test-results/new-landing-v1/`, `test-results/new-landing-v1-run/`, `test-results/new-landing-v1-stage/`,
    `test-results/new-landing-v1-stage-frames/` (вне git).
- **Вердикт:** **pass** — новая главная работает на стенде и стейдже по всем 20 кейсам набора; к бою годна после ответов
  владельца в интервью №097 (список теста, язык главной, новые строки RU, английская версия, раскладка на телефоне) и
  повторного выката на стейдж того коммита, что уедет в бой (замок стейджа). Не проверено: настоящий встроенный браузер
  Instagram, телефон владельца, поведение в бою.

РЕАЛЬНЫЙ МИР: накоплено — у вошедших людей маркер `ndim-session`: на `/` их уводит инлайн-дверь (e2e `root.spec` и
НЛ-17 — проверено с маркером, засеянным как у вернувшегося человека); данные и техника — живые люди и каталог боя не
тронуты, прогон шёл на стейдже с обезличенной копией; путь — ролик ведёт на `ndimspace.app`: проверено на главной
стейджа тем же адресом `/`. В бою не проверялось — выката в бой не было.
