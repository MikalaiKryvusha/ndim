# Отчёт по тестированию — два ролика пилота: по первому и по второму дублю

> **Создан:** 2026-09-15 09:0x +03:00 · **Родитель:** `plans/91_epic90_phase1_pilot_one_video.md` Ш7 ·
> набор `qa/suites/video-pipeline.md` · **Статус:** ✅ два ролика открыты владельцу страницей ·
> **Исходящее:** его решение по загрузке

## Карточка прогона

- **Работа:** заказ владельца 2026-09-15, дословно: «*Теперь сделай видео со вторым исходным роликом, где я в
  камеру смотрю. Музыку и в первой пятерке и в новой - ещё чуть-чуть громче, на 2 дБ. Плашку Ndim с моего лица
  убери, опусти ниже и в первых пяти и во вторых пяти*»; уточнения по ходу: «*плашку делаем только в одном
  варианте 1 - черная в одну строку - но ниже опустить ее нужно, чтобы не перекрывала бороду мне*» и «*итого два
  ролика будет - по первому исходнику, и по второму исходнику*».
- **Контур:** рабочая машина агента, студия вне git `D:\work\ai_sandbox\ndim-studio`. Продукт, стенд, стейдж и бой
  НЕ затронуты — правился только монтажный конвейер `tools/video/`.
- **Прогонов:**
  1. 08:5x — `node tools/video/takes.mjs render …\out\pilot-002\cut\cutlist.json` (первый запуск нашёл дефект
     стража стыков, см. «Найдено»; второй — ALL GREEN, склейка 50,4 с из 11 кусков ТОЛЬКО второго дубля);
  2. 09:0x — `node tools/video/edit.mjs …\pilot-001\cut\assembled.mp4 --name pilot-001-take-1 …` → ALL GREEN;
  3. 09:0x — `node tools/video/edit.mjs …\pilot-002\cut\assembled.mp4 --name pilot-002-take-2 …` → RED в одном
     пункте из восьми (сверка субтитров, см. «Найдено»), семь зелёных;
  4. 09:0x — замер разброса двух распознаваний на обоих роликах (скрипт разовый, в scratchpad);
  5. 09:0x — `silencedetect -40dB` по дорожке голоса второго ролика, отрезок 8–14,5 с — третий свидетель;
  6. 09:0x — страница пакета открыта живым Chromium (`playwright`), затем открыта владельцу браузером по умолчанию.
- **Проверок:** **пройдено 60, провалено 1.**
  - Юниты `node --test "tools/video/*.test.mjs"` — 45 из 45 (новые утверждения: место плашки по умолчанию;
    написания имени бренда `Endin`, `Ending`, `Andim` и контроль «ключ бренда не глотает обычные слова»).
  - Проверка стыков склейки второго дубля — 11 из 11: первое и последнее слово услышаны, совпадение 0,93–1,0;
    длина склейки 50,4 с против расчётных 50,37 с.
  - Самопроверка `pilot-001-take-1` — **8 из 8**: 1080×1920 · звук · −14 LUFS · пауз > 1 с нет · высокие частоты
    речи −0,1 дБ (порог 3) · чёрных 0 · застывших 0 · субтитры 42 из 42, наибольший сдвиг 230 мс.
  - Самопроверка `pilot-002-take-2` — **7 из 8**: те же семь зелёных; 🔴 сверка субтитров 41 из 42, сдвиг 530 мс.
  - Подписи `caption.mjs check` — 4 из 4 ALL GREEN (две прежние и две новые с меткой `pilot-002`).
  - Страница пакета живым Chromium: 2 ролика 1080×1920 по 49,47 и 49,07 с, перемотка на 12 с работает,
    ошибок консоли 0, горизонтальной прокрутки нет.
- **Найдено: 2 дефекта.**
  1. **Вылечен.** Страж стыков покраснел на ВЕРНОМ куске: «*последнее слово не услышано*» на фразе
     «…я создал Пространство NDim» — модель услышала «Endin», а список написаний бренда знал только
     `ndim|endim|эндим|ндим`. Лечение — `BRAND_HEARD` в `tools/video/takes.mjs`, список снят С ПРОГОНОВ
     (добавлены `endin`, `ending`, `andim`), юнит с контролем «ключ бренда не глотает обычные слова».
     После лечения — 11 стыков из 11.
  2. **НЕ лечен, заведён документ** — `bugs/NEW_subtitle_sync_guard_measures_its_own_noise.md`: сверка
     субтитров краснеет на разбросе собственного измерителя. Замер: два распознавания одного и того же
     звука расходятся у `take-1` до 310 мс, у `take-2` до 530 мс, медиана 0–20 мс; третий свидетель
     (`silencedetect`) показывает, что ОБА прогона ошибаются в разные стороны. Порог 500 мс НЕ тронут.
- **Правки конвейера по слову владельца:**
  - `MUSIC_BED_LUFS` −28 → **−26** (ровно 2 дБ). Проверено журналом: усиление музыки было −16,9 дБ, стало
    −14,9 дБ в обоих роликах.
  - `LOGO` → `{ width: 0, x: '(W-w)/2', y: '1460' }`. Место выведено ЗАМЕРОМ живых кадров, а не на глаз:
    борода кончается около y = 1090, субтитры занимают 1240…1402 (`SUB_MARGIN` 0,27), плашка высотой 108 px
    между ними не помещается с запасом — ушла ПОД субтитры (низ 1568, до низа кадра 352 px).
- **Следы:** `ndim-studio\out\pilot-002\cut\` (лист выбора, склейка, spoken.txt), `out\pilot-001-take-1`,
  `out\pilot-002-take-2`; папка загрузки `ndim-studio\upload\pilot-001\` пересобрана — четыре ролика вариантов
  плашки УБРАНЫ из неё (остаются в `out\pilot-001-plate-1…4`), лежат два новых, четыре подписи, лицензия и
  страница `index.html`. Учёток и записей в базах нет.
- **Вердикт:** годится к загрузке. Один красный пункт у второго ролика — ложный, измерен и заведён документом;
  владельцу он назван на самой странице пакета, а не спрятан.
  **Гигиена разработчика:** юниты 45 из 45; мутация — контроль «ключ бренда не глотает обычные слова».
  **Ручной функциональный прогон:** оба ролика просмотрены кадрами (плашка на 7, 9, 11, 14,5 с обоих дублей —
  проверялось именно то, что она не касается бороды; спорная строка субтитров на 10,7 с); пробные композиции
  плашки на двух высотах (1100 и 1500) собраны ffmpeg и просмотрены ДО сборки; страница пакета открыта живым
  Chromium и затем браузером владельца. ⚠️ На слух агент ролики не слушал — громкость музыки судит ухо владельца.

## Прогон 2 — правки владельца по звучанию (2026-09-15 09:3x)

- **Работа:** его слово, дословно: «*во втором ролике слишком резкие склейки, ты не даёшь фразе предыдущей
  полноценно закончиться - и уже режешь и начинаешь новую фразую В первом ролике это сильно лучше у тебя
  получилось, а во втором плохо - резкие склейки, обрывы фраз. Музыку еще громче на 2 дБ в обоих роликах*».
- **Причина, установленная ЧТЕНИЕМ КОДА и подтверждённая замером:** `cutRanges` в `tools/video/takes.mjs` даёт
  запас `padIn` 0,08 с / `padOut` 0,15 с только кускам, заданным ИМЕНАМИ фраз; моему листу, заданному временем,
  запаса не досталось — все одиннадцать фраз обрывались на выдохе. Замер сырого дубля: на старой границе речь
  −48,2 дБ (первая фраза) и −38,8 дБ (фраза про NDim ID), отрезанные 0,15 с — затухание до −64,3 и −60,2 дБ.
- **Что изменено:** лист выбора переписан именами фраз (`ndim-studio\out\pilot-002\cut\cutlist.json`, версия 2);
  соседние фразы, сказанные без паузы, слиты в один кусок — стыков 6 вместо 10, первые 23 с идут без единой
  склейки; фраза про сравнение оценок больше не режется посередине. `MUSIC_BED_LUFS` −26 → **−24**
  (усиление музыки −14,9 → −12,9 дБ; за день суммарно +4 дБ).
- **Проверок:** **пройдено 24, провалено 0.**
  - Проверка стыков — 7 из 7, совпадение 0,92–1,0; склейка 60,5 с.
  - Самопроверка обоих роликов — **8 из 8 у каждого** (первый ролик 49,47 с, второй 57,53 с). Вчерашний красный
    сверки субтитров у второго ролика ушёл САМ после пересборки: 47 строк, 45 сопоставлено, сдвиг 230 мс —
    свидетельство в пользу диагноза `bugs/NEW_subtitle_sync_guard_measures_its_own_noise.md`.
  - **Прямая проверка жалобы прибором:** энергия последних 60 мс перед каждой из шести склеек — −66,0 · −69,0 ·
    −71,5 · −72,3 · −72,3 · −68,6 дБ, то есть тишина. Ни одна склейка не приходится на звучащее слово.
  - Страница пакета живым Chromium: 2 ролика 49,47 и 57,53 с, ошибок консоли 0.
- **Найдено:** дефект стражей — ни один не видит склейку на звучащем слове; заведён
  `bugs/NEW_no_guard_for_cut_on_sounding_word.md` с порогом, выведенным из замера, и мутантом. Урок — `EXP-0309`.
- **Названная владельцу цена:** второй ролик стал 57,5 с вместо 49,1 с, и «Ваши Связи» звучат в нём дважды —
  это плата за целые фразы. Сказано ему на странице пакета вместе с предложением укоротить.
- **Вердикт:** оба ролика ALL GREEN, жалоба закрыта замером, страница открыта владельцу.
  **Ручной функциональный прогон:** оба ролика просмотрены кадрами; склейки проверены замером энергии; страница
  открыта живым Chromium и браузером владельца. ⚠️ На слух агент ролики не слушал — судит ухо владельца.

## Прогон 3 — плашка между подбородком и субтитрами, музыка −20 LUFS (2026-09-15 10:2x)

- **Работа:** слово владельца: «*во втором ролике субтитры немного ниже, плашку ввершу над субтитрами, между
  сабами и подбородком. музыку минут 20 луфс в обоих роликах*». Правки применены к ОБОИМ роликам: место плашки
  и отступ субтитров — пара, в одном ролике их менять нельзя (иначе ролики разъедутся и в первом плашке не
  хватит полосы).
- **Что изменено:** `SUB_MARGIN` 0,27 → **0,21** · `SUB_MARGIN_SCREEN` 0,22 → **0,19** (осталась ниже, чем на
  лице) · `LOGO.y` 1460 → **1220** · `MUSIC_BED_LUFS` −24 → **−20** (усиление музыки −12,9 → −8,9 дБ).
- **Замеры до сборки (композиции кадров ffmpeg, а не догадки):** бородка на отрезке показа плашки опускается до
  y ≈ 1180 при наклоне головы — проверено композициями на 7, 9, 11, 13, 15 с у второго дубля и 8, 12, 16 с у
  первого. Автоматический поиск бородки по темноте ОТВЕРГНУТ замером: профиль яркости центральной полосы
  показал, что тень под подбородком (строки 1150…1380) такая же тёмная, как борода.
- **Найдено и вылечено в этом же прогоне:** при `SUB_MARGIN` 0,23 ДВУХСТРОЧНАЯ реплика начиналась на 1330 и
  касалась низа плашки (1328). Арифметика по кеглю обещала 1373 — ошиблась на 43 px. Поймано кропом готового
  кадра, отступ опущен до 0,21, зазор ≈ 35 px, перепроверено кропом. Урок — `EXP-0310`.
- **Проверок:** юниты 45 из 45 (три обновлены под новые числа) · самопроверка ролика 1 — **8 из 8**
  (сдвиг субтитров 170 мс) · ролика 2 — **7 из 8** · кадр записи экрана обоих роликов: строка стоит ниже
  карточек «Связей» и выше нижней панели · страница пакета живым Chromium, ошибок консоли 0.
- 🔴 **Красный у ролика 2 — тот же страж, и теперь у него есть ЧИСТЫЙ ОПЫТ.** Тот же монтаж и те же субтитры
  при музыке −24 LUFS давали 230 мс, при −20 LUFS дают 600 мс. Субтитры строятся по распознаванию ГОЛОСА, на
  которое громкость музыки не влияет; значит сдвиг вносит второе распознавание, слушающее смесь. Порог 500 мс
  снова НЕ тронут; свидетельство дописано в `bugs/NEW_subtitle_sync_guard_measures_its_own_noise.md`.
- **Вердикт:** ролики отданы владельцу страницей, красный назван ему там же числами.
  **Ручной функциональный прогон:** композиции ДО сборки (8 кадров), кропы полосы наложений ПОСЛЕ сборки,
  кадры записей экрана, страница в живом Chromium. ⚠️ На слух агент не слушал — громкость судит ухо владельца.

## Прогон 4 — ускорение речи, равное время двум экранам, концовка со знаком (2026-09-15 11:4x)

- **Работа:** «*Ролик 2 нравится. Делаем пару улучшений: 1) Ускорить видео со мной говорящим в 1.10 раза… Это не
  должно ускорить музыку и анимации… 2) …первому кадру очень мало времени ты выделил, а второму больше - выдели
  обоим кадрам поровну… 3) В конце ролика на 1...2 секунды на черном фоне… квадратный логотип черный с синим
  ареолом… и под ним подпись Пространство NDim - и затем фейд в черное*». Правки сделаны в ОБОИХ роликах.
- **Что изменено в приборах:** `SPEECH_SPEED` и шаг «ускорение речи» (`setpts`+`atempo`) МЕЖДУ вырезанием пауз и
  очисткой звука — музыка, наезды и концовка кладутся на уже ускоренный сырец · `OUTRO_SEC` 2,0 / `OUTRO_FADE` 0,45
  и шаг «концовка со знаком» ДО музыки (музыка играет под концовкой и там же уходит), дорожка ГОЛОСА остаётся без
  концовки, чтобы паузы и частоты судились по речи · `outroHtml`/`haloSvg` в `lockup.mjs` — кадр концовки ·
  `assertSegmentsInOrder` — новый страж (см. «Найдено»).
- **Разведка ДО правки (причина жалобы 2 установлена замером, а не догадкой):** запись `001a_film_card.mp4` не
  прокручивается, а ПРЫГАЕТ — покадровый разбор движения нашёл единственный подвижный кадр на 1,97 с. То есть
  шапка карточки жила 1,97 с, прокрученный текст 2,57 с плюс 1,3 с удержания последнего кадра. Запись разрезана
  на два файла (`001a1_card_top`, `001a2_card_scrolled`), каждый показывается своим отрезком со своим наездом.
- **Проверок:** **пройдено 60, провалено 0.**
  - Юниты 48 из 48 (три новых: ускорение, концовка, наложение вставок).
  - Самопроверка обоих роликов — **8 из 8 у каждого**. Застывший кадр 1,6 с — это кадр концовки, порог 5 с;
    чёрных отрезков от 0,5 с нет (уход 0,45 с держится короче порога НАМЕРЕННО); сверка субтитров зелёная у обоих.
  - **Ускорение проверено отношением длительностей:** ролик 2 был 57,53 с речи, стал 52,40 → **1,098**;
    ролик 1 — 49,47 → 45,00 → **1,099**. Заказано 1,10.
  - **Равное время экранов проверено журналом и полоской кадров:** ролик 2 — 2,79 с и 2,54 с встык;
    ролик 1 — 2,63 с и 2,28 с. Раньше было 1,97 с против 3,87 с.
  - Концовка просмотрена полоской кадров: знак с ореолом, подпись, уход в чёрное.
  - Страница пакета живым Chromium: 47,0 с и 54,4 с, ошибок консоли 0.
- **Найдено: 1 дефект, вылечен стражем.** Якорь `--segment "…|по|десяти"` нашёл «по» РАНЬШЕ — в хвосте длинной
  фразы, — и вторая вставка (18,05…26,50 с) накрыла первую (21,17…23,90) вместе с речью. Все стражи были зелёные:
  наложение вставок не видит ни один, потому что каждый судит объекты поодиночке. Лечение — `assertSegmentsInOrder`
  с юнитом, воспроизводящим ровно этот случай. Урок — `EXP-0311`.
- **Вердикт:** оба ролика ALL GREEN 8/8, страница открыта владельцу.
  **Ручной функциональный прогон:** покадровый разбор движения записи экрана ДО правки; полоски кадров обеих
  вставок и концовки ПОСЛЕ; кадр концовки просмотрен отдельным изображением до сборки; страница в живом Chromium.
  ⚠️ На слух агент не слушал — темп речи и громкость музыки судит ухо владельца.
