# Отчёт по тестированию — улёт карточки со своего места, независимые отсчёты, единая шапка экранов

> **Создан:** 2026-09-19 19:1x +03:00 · **Родитель:** `bugs/NEW_dims_card_flight_jumps_to_feed_top.md` ·
> `bugs/NEW_rating_countdown_one_per_screen.md` · `bugs/NEW_screen_heads_differ_between_screens.md` ·
> `bugs/NEW_dims_feed_loadmore_resurrects_flying_card.md` · набор `qa/suites/dims-rating-and-screen-heads.md` ·
> **Статус:** ✅ В БОЮ с 2026-09-19 20:45 +03:00 (коммит `0409fd4`, дверь `npm run deploy` через стейдж) ·
> **Исходящее:** —

## Карточка прогона

- **Работа:** три бага из слова владельца в чате 2026-09-19. (1) «*Я в мобилке выставлял оценки измерениям,
  которые не в топе списка, а где-то ниже — анимация прыгает куда-то вверх, а не выполняется на том месте, где
  находится карточка в списке*». (2) «*Если я поставил звёзды одному измерению и ставлю звезды другому измерению — это
  не должно снимать звезды с того первого измерения, а оно просто должно запускать в очередном измерении очередной
  таймер сохранения*». (3) «*на разных страницах верх страницы оформлен по разному… В Пространстве подпись… оформлена
  не как хелп карточка… На профиле нет заголовка "Профиль". Пока страницы грузятся… везде должна быть*». Затем его
  ответ «*А (советую)*» — оценки с идущим отсчётом сохраняются при уходе с экрана — и слово «*дальше кати в прод, и
  тестируй в проде смоук проверками юзерских сценариев*». Правки — `src/routes/dims/+page.svelte` (улёт, отсчёты, свой
  FLIP, уход с экрана, догрузка ленты), `src/lib/ui/ScreenHead.svelte` (новый) и пять экранов.
- **Контур:** серия 1 — стенд (`npm run stand`, живой Chromium Playwright). Серия 2 — стенд, затем **стейдж**
  (`https://ndim-stage.web.app`) и **бой** (`https://ndimspace.app`) дверью `npm run deploy`; на стейдже и в бою — гость
  через продукт (`tools/verify-live-dims-rating.mjs`).
- **Прогонов:** серия 1 — 2026-09-19, 18:25…19:05 +03:00:
  · `node tools/verify-dims-flight.mjs --width 390 --trace` на СТАРОМ коде — красный (снятие «до»);
  · `node tools/verify-dims-flight.mjs --width 390 [--only one|series] [--debug|--trace]` — 7 разборных прогонов;
  · `node tools/verify-dims-flight.mjs --width W --theme T` для 390/1440 × light/dark — 4 итоговых;
  · 4 мутанта `verify-dims-flight` (сдвиг в `transform`; CSS-переход не снят; свой вынос отключён; `flip` Svelte);
  · `node tools/verify-screen-heads.mjs --quick` на СТАРОМ коде — красный; `node tools/verify-screen-heads.mjs` — 3
    прогона + 1 мутант `--quick`;
  · соседние стражи: `verify-bug96` · `verify-bug54 --quick` · `verify-bug172` (390 light, 1440 dark) · `verify-bug80` ·
    `verify-ideas24` · `verify-bug226-gnote --quick` · `verify-header-v3a --quick` · `verify-bug117 --quick` ·
    `verify-bug105 --quick` · `verify-bug70`;
  · кадры: `node tools/verify-dims-flight.mjs --width 390 --shots` и `--width 1440 --theme dark --only one --shots`;
  · `npm run check` · `npm test` · `npm run test:tools`.
  Серия 2 — 19:38…20:49 +03:00:
  · 19:4x `node tools/verify-dims-flight.mjs --width 390` (сценарий «УХОД») · 19:43 мутант «уход без сохранения» —
    предсказание записано в 19:43:36 · `--width 1440 --theme dark` · `verify-screen-heads` · `verify-bug54/96/172/80`;
  · 19:53 `npm run e2e` — 121 прошло, 1 упал с аварией процесса (`code=3221226505`), 4 пропущено; повтор
    `npx playwright test e2e/root-landing-view.spec.ts` — 16/16;
  · 20:03 `npm run deploy -- --stage` — ворота smoke КРАСНЫЕ (15/4, «Профиль молчит»); `node tools/smoke.mjs` ×2 на
    стенде после правки прибора — 17/1, затем 19/0;
  · 20:12 `npm run deploy -- --stage` — выкат лёг, шаг заголовков оборвался без строки провала; отдельный прогон
    `node tools/verify-prod-cache.mjs --base https://ndim-stage.web.app` — 12/0; 20:15 `npm run deploy -- --stage
    --skip-build` — зелёный целиком;
  · ≈20:17 и 20:20 `node tools/verify-live-dims-rating.mjs --stage [--trace]` — КРАСНЫЕ (3 провала; трасса дала гонку
    догрузки ленты и шапку гостя; уборка в обоих — 0 из 6, учётка удалена);
  · 20:24 опыт `verify-dims-flight --only series --slow-db 800` (390) — зелёный; мутант старого `loadMore` —
    предсказание записано в 20:24:14 — тоже ЗЕЛЁНЫЙ (стенд гонку не воспроизводит; режим удалён);
  · `verify-bug226-gnote --quick` · `verify-screen-heads` · `verify-dims-flight --width 390` · `npm run check` ·
    `npm test` · `npm run guards`;
  · 20:33 `npm run deploy -- --stage` — выкат лёг, шаг «стейдж закрыт от роботов» оборвался без строки провала;
    20:37 `npm run deploy -- --stage --skip-build` — зелёный целиком, расписка на `0409fd4`;
  · 20:39 `node tools/verify-live-dims-rating.mjs --stage --trace` · 20:40 `--stage --width 1440 --theme dark`;
  · 20:45 `npm run deploy` — БОЙ, зелёный целиком;
  · 20:47 `node tools/verify-live-dims-rating.mjs --contour prod` · 20:49 `--contour prod --width 1440 --theme dark`.
- **Проверок:** **провалено 0 в итоговом состоянии** — две строки ниже раздельно.
  - **Гигиена:** `svelte-check` 607 файлов, 0 ошибок · `npm test` 535/535 · `npm run test:tools` 832/832 · `npm run
    guards` 22/22 · e2e 121 + повтор 16/16 · мутации: серия 1 — 5 из 5 красные; серия 2 — «уход без сохранения»
    ровно по записанному предсказанию (контроль ✅, К5 ❌×2); мутант старого `loadMore` на стенде НЕ покраснел
    (граница стенда — ниже в «Найдено»).
  - **Функциональный прогон:** ПРОЧИТАНО — **стенд:** страж улёта зелёный на 390/1440 × light/dark (отход 0px на
    телефоне, до 3px на десктопе при допуске 3, рывков 0, серия 7/5/3 и уход 4/2 в базе), страж шапок 117/0, кадры
    полёта и шапок прочитаны глазами (`test-results/dims-flight/*/shots/`, `test-results/screen-heads/`);
    **стейдж:** дверь — smoke по артефакту 19/0, попадание в контур, заголовки 12/0, переадресации 16/0, стейдж закрыт
    от роботов 11/0, смоук под сессией 24/0, публичный смоук гостем; сценарии гостем через продукт — 56/0 на 390 light
    и 56/0 на 1440 dark (шапки пяти экранов в загрузке и после, заголовок на одной высоте 14/20px, улёт четвёртой
    карточки, серия из трёх со своими отсчётами, ни одна улетевшая не вернулась, уход на «Профиль» при двух
    отсчётах, итог — шесть карточек со своими звёздами в «Мой NDim ID» после перезагрузки, уборка 0 из 6, учётка
    удалена); кадры гостя прочитаны глазами (`test-results/live-dims-rating/stage-390-light/`: шапка выше плашки
    гостя, три отсчёта серии); **бой:** дверь зелёная целиком (smoke 19/0, попадание — бой `al7dw8`, стейдж не
    тронут, заголовки 12/0, переадресации 16/0, под сессией 24/0, гостем зелёный); сценарии гостем — **56/0 на 390
    light и 56/0 на 1440 dark**; кадры прочитаны глазами (`test-results/live-dims-rating/prod-390-light/`:
    «Сохранено» на месте, пока запись идёт по сети; «Мой NDim ID» после перезагрузки).
- **Найдено:** три бага владельца — `bugs/NEW_dims_card_flight_jumps_to_feed_top.md`,
  `bugs/NEW_rating_countdown_one_per_screen.md`, `bugs/NEW_screen_heads_differ_between_screens.md`; сверх жалобы
  вылечены там же: CSS-переход `.dim` растягивал поправку места · `fix()` Svelte пропускал карточку с анимацией · `flip`
  Svelte копил дрожь 4 → 17px · заголовок «Измерений» на 4/6px ниже. **Серия 2, смоук стейджа:**
  `bugs/NEW_dims_feed_loadmore_resurrects_flying_card.md` — догрузка ленты возвращала улетающую карточку (на стенде не
  воспроизводится); шапка у гостя стояла на разной высоте (14 против 173–185px — плашка гостя над заголовком на
  четырёх экранах из пяти) — шапка поставлена выше плашки. **Приборы:** набор smoke и смоук под сессией судили
  готовность экрана числом знаков — с шапкой, видной в загрузке, признак срабатывал на «Загрузке» (ворота стейджа
  красные на исправном «Профиле»); вылечено ожиданием ухода экранной «Загрузки» (коммит `49baa55`). Живой прибор
  ждал улёта серии секундомером и обрывал трассу на живом контуре — переведён на ожидание по состоянию. Нестабильные
  без дефекта продукта: одна авария процесса e2e, один провал «продающий интерактив» набора smoke на `vite dev` (повтор
  зелёный), два обрыва шагов двери без строки провала (повтор зелёный) — лендинг и конфигурация хостинга в работе не
  менялись. Стенд НЕ умеет воспроизвести гонку с сетью (Firestore ходит долгим каналом WebChannel, придержка
  HTTP-запросов её не замедляет) — класс стережёт только живой прибор (К6).
- **Суд (`/fable-judge`, независимый экземпляр, 2026-09-19 ≈19:25, по серии 1):** **VERIFIED WITH CAVEATS**; оговорки
  исполнены коммитом `4629cd1` (описание мутанта, поле механизации EXP-0317, пометка авторства).
- **Уроки учтены:** EXP-0094 (одна жалоба на движение — не одна причина) · EXP-0100 (отменяя outro, Svelte
  восстанавливает inline-стили) · EXP-0115 (экран показывает намерение, база хранит факт) · EXP-0158 (уходящий
  элемент держит место в потоке) · EXP-0317 (прибор на первом элементе списка слеп к месту).
- **Следы:** стенд — оценки dev-пользователя удалены, уборка проверена. Стейдж — 4 прогона гостем, бой — 2 прогона
  гостем: в каждом 6 оценок записаны и сняты «Убрать мою оценку» (проверено: 0 из 6), анонимная учётка удалена своим
  `idToken` (1 из 1); остаются документы `points/{uid}` удалённых гостей с флагом пересчёта — их вычищает уборка
  гостей сервера синхронизации; сервер синхронизации мог учесть оценки гостя в NDSR этих объектов на время прогона —
  ближайший цикл после уборки пересчитывает сводку. Метка прибора глушит воронку и аналитику. Дверь боя обновила снимок
  чисел витрины (только дата) — коммит `adcf3fd`. Кадры — `test-results/live-dims-rating/`, `test-results/smoke/`,
  `test-results/prod-signed-in/`, `test-results/prod-b4/` (вне git).
- **РЕАЛЬНЫЙ МИР:** накоплено — возвращающийся человек с сессией: проверен дверью (`verify-prod-b4` с маркером сессии и
  памятью, смоук под сессией) · аккаунт владельца с его оценками — **не проверен**: оценка в его живом профиле меняет
  его состояние, такая проверка ждёт владельца; данные и техника — боевая база и боевая сборка, Chromium 390 и 1440 с
  касанием; его телефон — **не проверен**; путь — дверь «гостем без регистрации», экран «Измерения», навигация на
  «Профиль», «Мой NDim ID» после перезагрузки — проверен в бою.
- **Вердикт:** **pass в бою** — на боевом контуре пользовательские сценарии всех трёх багов и ответа «А» прошли гостем
  (56/0 × 2). Не проверено и названо: аккаунт владельца и его телефон; вид движения судит его глаз.
