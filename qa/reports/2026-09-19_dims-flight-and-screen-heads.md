# Отчёт по тестированию — улёт карточки со своего места, независимые отсчёты, единая шапка экранов

> **Создан:** 2026-09-19 19:1x +03:00 · **Родитель:** `bugs/NEW_dims_card_flight_jumps_to_feed_top.md` ·
> `bugs/NEW_rating_countdown_one_per_screen.md` · `bugs/NEW_screen_heads_differ_between_screens.md` ·
> **Статус:** стенд пройден; в бой не выкачено — уедет с ближайшим выкатом ·
> **Исходящее:** один вопрос владельцу в чате (что делать с идущими отсчётами при уходе с экрана)

## Карточка прогона

- **Работа:** три бага из слова владельца в чате 2026-09-19. (1) «*Я в мобилке выставлял оценки измерениям,
  которые не в топе списка, а где-то ниже — анимация прыгает куда-то вверх, а не выполняется на том месте, где
  находится карточка в списке*». (2) «*Если я поставил звёзды одному измерению и ставлю звезды другому измерению — это
  не должно снимать звезды с того первого измерения, а оно просто должно запускать в очередном измерении очередной
  таймер сохранения*». (3) «*на разных страницах верх страницы оформлен по разному… В Пространстве подпись… оформлена
  не как хелп карточка… На профиле нет заголовка "Профиль". Пока страницы грузятся… везде должна быть*». Правки —
  `src/routes/dims/+page.svelte` (улёт, отсчёты, свой FLIP), `src/lib/ui/ScreenHead.svelte` (новый) и пять экранов,
  `src/lib/ui/motion.ts` (комментарий).
- **Контур:** стенд (`npm run stand`: эмуляторы Firestore/Auth/Storage, сервер синхронизации службой, `vite dev`),
  живой Chromium Playwright. Бой и стейдж не затронуты.
- **Прогонов:** 2026-09-19, 18:25…19:05 +03:00 —
  · `node tools/verify-dims-flight.mjs --width 390 --trace` на СТАРОМ коде — красный (снятие «до»);
  · `node tools/verify-dims-flight.mjs --width 390 [--only one|series] [--debug|--trace]` — 7 разборных прогонов
    по ходу починки (три причины бага 1 и дрожь соседей найдены ими);
  · `node tools/verify-dims-flight.mjs --width W --theme T` для 390/1440 × light/dark — 4 итоговых прогона;
  · 4 мутанта `verify-dims-flight` (сдвиг обратно в `transform`; CSS-переход не снят; свой вынос отключён; обратно
    `flip` Svelte), каждый — прогон и возврат;
  · `node tools/verify-screen-heads.mjs --quick` на СТАРОМ коде — красный (снятие «до»);
  · `node tools/verify-screen-heads.mjs` — 3 прогона (последний — итоговый) + 1 мутант `--quick`;
  · `node tools/verify-bug96.mjs` · `node tools/verify-bug54.mjs --quick` · `node tools/verify-bug172.mjs --width 390
    --theme light` и `--width 1440 --theme dark` · `node tools/verify-bug80.mjs` (дважды: до и после поправки метода
    замера) · `node tools/verify-ideas24.mjs` · `node tools/verify-bug226-gnote.mjs --quick` ·
    `node tools/verify-header-v3a.mjs --quick` · `node tools/verify-bug117.mjs --quick` ·
    `node tools/verify-bug105.mjs --quick` · `node tools/verify-bug70.mjs`;
  · `node tools/verify-dims-flight.mjs --width 390 --shots` и `--width 1440 --theme dark --only one --shots` — кадры
    для глаз;
  · `npm run check` (дважды) · `npm test` · `npm run test:tools`.
- **Проверок:** **провалено 0 в итоговом состоянии** — две строки ниже раздельно.
  - **Гигиена:** `svelte-check` 607 файлов, 0 ошибок · `npm test` 535/535 · `npm run test:tools` 832/832 · мутации
    5 из 5 краснеют адресно (4 у стража улёта, 1 у стража шапок); прогоны стражей на старом коде красные — баг 1:
    К1 552px и рывок 260 → −292; баг 2: К3 ❌×2, К4 ❌×2 (из трёх оценок в базе одна); баг 3: 9 провалов.
  - **Функциональный прогон:** стенд, живой Chromium, путь человека на экране «Измерения» и пяти экранах
    приложения; ПРОЧИТАНО: (а) страж улёта зелёный на 390/1440 × light/dark: четвёртая карточка и серия из трёх
    улетают со своего места (отход 0px на телефоне, 1–3px на десктопе при допуске 3), рывков 0, три оценки серии
    легли в базу со своими значениями 7/5/3, след убран; (б) кадры полёта прочитаны глазами —
    `test-results/dims-flight/390-light/shots/` (одна карточка: «Проба 8» уезжает вправо со своего места, соседи
    ждут; серия: у трёх карточек одновременно «Сохраню через 4 с…», «через 5 с…» и своя звезда, затем каждая
    улетает со своего места) и `1440-dark/shots/` (карточка правой колонки уезжает со своего места); (в) страж шапок
    **117/0** на 390/1440 × light/dark, заголовок на одной высоте на пяти экранах (14px телефон / 20px десктоп),
    контроль прибора «Загрузка на экране» зелёный у четырёх загружающихся экранов; (г) кадры шапок прочитаны
    глазами — `test-results/screen-heads/` против `test-results/screen-heads-before/`: «Профиль» получил заголовок,
    подсказка «Пространства» стала плашкой, в загрузке на всех экранах заголовок и плашка над «Загрузкой», на
    десктопе шапка во всю строку сетки; (д) соседние стражи зелёные: `verify-bug96` 10/0, `verify-bug54`,
    `verify-bug172` (390 light, 1440 dark), `verify-bug80`, `verify-ideas24` 60/0, `verify-bug226-gnote`,
    `verify-header-v3a`, `verify-bug117` 7/0, `verify-bug105` 14/0, `verify-bug70`.
- **Найдено:** три бага владельца — `bugs/NEW_dims_card_flight_jumps_to_feed_top.md`,
  `bugs/NEW_rating_countdown_one_per_screen.md`, `bugs/NEW_screen_heads_differ_between_screens.md`. Прогоны вскрыли
  сверх жалобы и вылечили в тех же документах: CSS-переход `.dim` растягивал поправку места на 180 мс · Svelte
  `fix()` пропускал карточку с идущей анимацией (серия оценок) · `flip` Svelte копил дрожь соседей 4 → 6 → 17px
  (`clientHeight` без рамки) · заголовок «Измерений» на 4/6px ниже остальных. Дефекты приборов, найденные по ходу и
  исправленные до сдачи: К1 брал точку отсчёта после прыжка (печатал 0 на дефекте) · К2 «шаг > 60px» краснел на
  здоровом подтягивании и был слеп к дрожи · `verify-bug80` мерил сдвиг по `transform`, где теперь живёт поправка
  места.
- **Следы:** оценки прогонов пишутся dev-пользователю стенда и удаляются приборами; уборка проверена в каждом
  прогоне («оценок прогона в базе осталось 0»). Кадры и видео — `test-results/dims-flight/`,
  `test-results/screen-heads/`, `test-results/screen-heads-before/` (вне git). Стенд поднят этой сессией.
- **Вердикт:** **pass на стенде** — годится к ближайшему выкату. Ограничения, названные честно: на живых устройствах
  (телефон владельца) не проверено; бой не тронут; вид движения судит глаз владельца.
