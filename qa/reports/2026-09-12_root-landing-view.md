# Отчёт по тестированию — главная считает приход (одна строка, только PostHog)

> **Создан:** 2026-09-12 01:1x +03:00 · **Родитель:** `qa/suites/root-landing-view.md` ·
> `bugs/NEW_funnel_blind_on_v5_root.md` · интервью №078 В1 = Г · **Статус:** ✅ все три слоя
> исполнены 2026-09-12 (Ю · П · С — стейдж и ряд PostHog), вердикт — раздел 5 ·
> **Исходящее:** вопрос владельцу о выкате в бой — страницей интервью, со ссылками на этот отчёт.

## Карточка прогона

- **Работа:** приход человека на главную `ndimspace.app/` считается одним событием
  `landing_view` в PostHog без SDK и без Firestore; вошедший, гость, ссылка из письма, прибор
  под меткой и стенд не считаются (`bugs/NEW_funnel_blind_on_v5_root.md`, решение владельца №078
  В1 = Г, набор `qa/suites/root-landing-view.md`).
- **Контур:** **стенд** — юниты в стенде-заглушке и e2e по СОБРАННОМУ артефакту рабочего дерева
  (сборка 1872 от `ea31d77` + правки этой смены, дерево грязное до коммита), под боевым и
  стейджевым именами через перехват маршрута. Стейдж — раздел 4.
- **Прогонов:** 5 —
  · 2026-09-12 00:4x `node --test src/lib/data/analytics.test.ts` (до и после мутации, 2 прогона);
  · 2026-09-12 00:5x `npm run e2e -- e2e/root-landing-view.spec.ts e2e/root.spec.ts` (первая
    редакция прибора — 10 красных, причина в приборе, не в продукте: `vite preview` отказал
    чужому имени хоста; прибор переписан на перехват маршрута);
  · 2026-09-12 01:0x `npm run e2e -- e2e/root-landing-view.spec.ts` — **12/12**;
  · 2026-09-12 01:1x тот же спек `--project=desktop` на МУТИРОВАННОМ продукте (снята врезка
    `{@html LANDING_VIEW}`) — 2 красных адресно, 4 зелёных по замыслу; продукт возвращён.
- **Проверок:** юниты **515/515** (`npm test`; из них 10 новых — слой Ю, кейсы ГЛ-01…ГЛ-12) ·
  e2e **12/12** (6 кейсов × 2 ширины — слой П, кейсы ГЛ-01…ГЛ-07) · `svelte-check` 604 файла,
  0 ошибок · ворота проекта **22/22**. Провалов в продукте: **0**. Провал прибора: 1 (первая
  редакция e2e, разобран выше).
- **Найдено:** ноль дефектов продукта. Один дефект ПРИБОРА (первая редакция e2e), вылечен в
  самом приборе до объявления зелёного; в `bugs/` не заводился — дефект прожил 10 минут и
  урок записан в шапке спека.
- **Следы:** в контурах ничего — PostHog в обоих слоях заглушен, Firestore не трогается по
  замыслу. Одна проба транспорта ДО кода (Node, 00:3x): событие `landing_view` с `env = stage`,
  `$lib = ndim-root-inline`, `transport_probe: true`, `$current_url = https://ndim-stage.web.app/`
  — лежит в общем ряду PostHog как стейджевое. Кадры e2e — `test-results/e2e/`.
- **Вердикт:** 🟢 **к стейджу годится.** Слои Ю и П доказали решение строки и её исполнение
  браузером из отдаваемого HTML; каждая проверка доказана мутацией. К бою — только после слоя С
  (раздел 4) и слова владельца.

---

## 1. Что доказано слоем Ю (юниты, стенд-заглушка)

| Кейс | О чём | Итог |
|---|---|---|
| ГЛ-01 | бой: одна отправка, адрес `/i/v0/e/`, POST, `text/plain`, `keepalive`; тело: токен, `landing_view`, `distinct_id`, `env = prod`, `$process_person_profile = false`, `$lib`, `$current_url` без query | ✅ |
| ГЛ-02 | стейдж: `env = stage` | ✅ |
| ГЛ-06 | `localhost` · `127.0.0.1` · `ndimspace.app.evil.example` → 0 | ✅ |
| ГЛ-03 | маркер сессии → 0 | ✅ |
| ГЛ-04 | метка прибора → 0 | ✅ |
| ГЛ-05 · ГЛ-12 | письмо → 0; `mode=signIn` без кода → 1, query наружу не едет | ✅ |
| ГЛ-08 | хранилище бросает → не падает, 0 | ✅ |
| ГЛ-09 | ключи свойств закрыты, `env` из союза, значения не пахнут предметом оценки | ✅ |
| ГЛ-10 | пары: `SESSION_MARK` из `session.ts` · `PROBE_MARK` · таблица хостов = `contourOf`, `localhost` отсутствует | ✅ |
| ГЛ-11 | строка не закрывает тег, не импортирует | ✅ |

**Мутация** (снят возврат по маркеру сессии и по метке прибора): красных **4** — ГЛ-03 и ГЛ-04
адресно, плюс ГЛ-08 и ГЛ-10 как зависимые (строка перестала читать хранилища). Файл возвращён,
повторный прогон 49/49.

## 2. Что доказано слоем П (e2e, собранный артефакт под боевым именем)

Команда: `npm run e2e -- e2e/root-landing-view.spec.ts` · проекты mobile 430 и desktop 1440.

| Кейс | О чём | Итог |
|---|---|---|
| ГЛ-01 · ГЛ-07 | `http://ndimspace.app/` из свежего контекста: `h1 = NDim Space`, `location.hostname = ndimspace.app`, `script[src]` = **0**, ровно **1** POST в PostHog с телом по контракту, ключи свойств ровно шесть | ✅ ×2 |
| ГЛ-02 | `http://ndim-stage.web.app/` → `env = stage` | ✅ ×2 |
| ГЛ-03 | маркер сессии → увод в `/profile`, отправок 0 | ✅ ×2 |
| ГЛ-04 | метка прибора → главная отрисована, отправок 0 | ✅ ×2 |
| ГЛ-05 | письмо → `/profile?mode=signIn&oobCode=…` нетронутым, отправок 0 | ✅ ×2 |
| ГЛ-06 | контроль прибора: `http://localhost:<порт>/` → 0 | ✅ ×2 |

**Мутация продукта** (снята строка `{@html LANDING_VIEW}` в `+page.svelte`, desktop): красных
**2** — ГЛ-01 и ГЛ-02 («Received length: 0»), негативные ГЛ-03…ГЛ-06 зелёные по замыслу.
Продукт возвращён, `grep -c "html LANDING_VIEW"` = 1.

**Что этот слой НЕ доказывает:** приём события самим PostHog. Это слой С.

## 3. Ворота проекта после правки

`npm run guards` → **22/22** (в том числе «карта путей: сводка сошлась» с двумя новыми путями
`NDIM-PUBLIC-014/015`, «воронка: шаги ↔ правила ↔ метка ↔ места вызова»). Красная строка ворот
была одна — «неотслеживаемых файлов 2» — и закрывается `git add` явных путей.

## 4. Слой С — стейдж и ряд PostHog

**Выкат:** `npm run deploy -- --stage` 2026-09-12 00:2x–00:3x +03:00 на коммите `63b0ae8`
(расписка стейджа выписана на него; ⚠️ дерево на момент выката было грязным — новый прибор
слоя С ещё не лежал в индексе, — поэтому **перед боем стейдж катится заново на финальном
коммите**). Дверь: правила и индексы ✅ · сборка целая, хеш один · попадание в контур ✅ ·
ворота Smoke на стенде **11/0** · `verify-stage-noindex` ✅ · смоук под сессией **24/0** ·
гостевой смоук **83/0**. Итог двери: «✅ ВЫКАТ В СТЕЙДЖ ЗАВЕРШЁН И ПРОВЕРЕН».

**ГЛ-13, отправка.** `node tools/probe-root-landing-view-live.mjs --base https://ndim-stage.web.app`
(свежий браузер, БЕЗ метки прибора — намеренно, см. шапку прибора):

```
контур: https://ndim-stage.web.app · начато 2026-09-11T21:38:17.252Z
h1: NDim Space · файлов кода на странице: 0
отправок в PostHog: 1 · ответы: 200
  event=landing_view distinct_id=22e39bb6-0464-40b4-860f-b69c01b19c21 env=stage $lib=ndim-root-inline $pathname=/
✅ событие ушло и принято
```

⚠️ **Первый запуск прибора напечатал НИЧЕГО и вышел кодом 0** — предохранитель «запущен или
подключён» сравнивал с `new URL(argv[1], 'file:')`, а на Windows это не совпадает никогда.
Ложный зелёный первого вопроса лестницы, пойман потому, что пустой вывод не похож на замер.
Форма исправлена на каноническую `pathToFileURL`, прибор теперь печатает контур первой строкой.

**ГЛ-13, приём.** Ряд прочитан через MCP PostHog (`execute-sql`, окно 2 часа,
`event = 'landing_view' AND properties.$lib = 'ndim-root-inline'`):

| timestamp (Europe/Minsk) | distinct_id | env | $lib | $pathname | $current_url |
|---|---|---|---|---|---|
| 2026-09-12 00:38:17 | 22e39bb6-…-b69c01b19c21 | stage | ndim-root-inline | / | https://ndim-stage.web.app/ |
| 2026-09-12 00:16:02 | probe-1789161361851 | stage | ndim-root-inline | (null) | https://ndim-stage.web.app/ |

Первая строка — событие живого стейджа: `distinct_id` совпадает с напечатанным прибором,
контур `stage`, путь `/`. Вторая — проба транспорта из Node до кода (раздел «Следы»). Ряд
`space/funnel/days` не проверялся и не будет: по слову владельца главная Firestore не трогает.

**Следы слоя С:** два события `env = stage` в общем ряду PostHog (названы выше); в базе стейджа
— следы двери выката, убранные ею самой («анонимных учёток заведено 2, удалено 2»).

## 5. Вердикт по трём слоям

🟢 **Фича доказана целиком: приход → событие → имя → контур → ряд PostHog.** К бою годится
при двух условиях: (1) стейдж перекатывается на финальном коммите (расписка на `63b0ae8`
выписана при грязном дереве, и дверь в бой её не примет); (2) слово владельца на выкат.
**ГЛ-14 (сутки боя)** меряется временем после выката и в этот отчёт не входит.
