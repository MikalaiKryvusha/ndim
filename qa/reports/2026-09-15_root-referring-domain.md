# Отчёт по тестированию — главная пишет домен источника захода

> **Создан:** 2026-09-15 21:3x +03:00 · **Родитель:** `qa/suites/root-landing-view.md` (ГЛ-18…ГЛ-22) ·
> `bugs/NEW_root_landing_view_drops_utm.md` · **Статус:** ✅ В БОЮ с 2026-09-15 ≈21:2x ·
> **Исходящее:** через сутки — ряд `landing_view` по `$referring_domain` (как ролик пилота приводит людей)

## Карточка прогона

- **Работа:** слово владельца 2026-09-15, дословно: «*Счётчик главной будет записывать только домен сайта, откуда
  пришёл человек (youtube.com, instagram.com, tiktok.com), без полного адреса. Тогда станет видно, какая площадка
  приводит людей. давай сделаем, если это возможно*» и «*и в постхог будешь это слать?*» — да, свойством того же
  события. Строка корня `src/lib/data/root-landing-view.ts` получила `$referring_domain` = хост
  `document.referrer`, без источника — `$direct`; полный адрес (`$referrer`) не шлётся. Имя и смысл поля сверены
  по исходнику `posthog-js` (`update_referrer_info`). Заодно погашены унаследованные красные ворота:
  `bugs/NEW_DONE_signin_link_probe_sessions_unmarked_gate_red.md` и форма отчёта `2026-09-15_pilot-two-takes`.
- **Контур:** стенд (юниты, мутации, e2e по собранному артефакту под боевым именем, прибор входа по ссылке) ·
  **стейдж** (дверь `npm run deploy -- --stage`, живой прибор человеком, событие прочитано из PostHog) ·
  **бой** (дверь `npm run deploy`, отданный HTML главной прочитан без записи событий).
- **Прогонов:** 9 за вечер 2026-09-15 —
  · 20:5x `npx tsx --test src/lib/data/analytics.test.ts` — 56/56; затем 2 мутанта и возврат (sha исходника совпал);
  · 21:0x `npm run build` + `npm run e2e -- e2e/root-landing-view.spec.ts e2e/root.spec.ts` — 22/22;
  · 21:0x `npm run check` · `npm test` 535/535 · `npm run guards` (сначала 21/22 и 20/22 — см. «Найдено», затем 22/22);
  · 21:0x `node tools/verify-signin-link-any-browser.mjs` на поднятом стенде — 9/9;
  · 21:1x `npm run deploy -- --stage` — дверь зелёная целиком (смоук по сборке, попадание, смоук под сессией);
  · 21:13 `node tools/probe-root-landing-view-live.mjs --base https://ndim-stage.web.app --walk` — 26/26;
  · 21:15 запрос HogQL к PostHog по `$lib = ndim-root-inline` за 30 минут;
  · 21:2x `npm run deploy` — дверь зелёная целиком;
  · 21:2x `curl https://ndimspace.app/` — разбор отданного HTML.
- **Проверок:** **пройдено 140, провалено 0 в итоговом состоянии.**
  - Юниты слоя Ю: 56 из 56 в `analytics.test.ts` (5 новых: ГЛ-18 три площадки · ГЛ-19 `$direct` · ГЛ-20 путь и
    query не уходят, судится всё тело · ГЛ-22 битый referrer не роняет счёт · внутренний переход); всего `npm test`
    535 из 535.
  - Мутации: «адрес вместо домена» → 4 красных адресно (ГЛ-18, ГЛ-20, ГЛ-22, внутренний переход); «`$direct` снят» →
    1 красный (ГЛ-19). Исходник возвращён, sha-256 до и после совпал.
  - E2e слоя П: 22 из 22 (11 кейсов × 2 ширины), новый кейс — настоящий `document.referrer` через `referer` у `goto`,
    контроль прибора «страница видит источник» зелёный.
  - Стейдж, слой С: прибор 26 из 26 — 4 прямых захода с `$direct`, заход с YouTube с `www.youtube.com` и без пути и
    query, переход на `/ru`, гостевой вход и возврат; гость удалён.
  - **PostHog, не только браузер:** событие `36129e93…` лежит с `env = stage`, `$referring_domain = www.youtube.com`,
    `$referrer` пустой; шесть соседних — `$direct`.
  - Бой: дверь 16 проверок попадания + смоуки под сессией на 390-light и 1440-dark — провалов 0; отданный HTML главной
    содержит строку `var rd='$direct'…` и `$referring_domain:rd`, `script[src]` 0, `$referrer:` 0.
  - Ворота проекта 22 из 22; `svelte-check` 606 файлов, 0 ошибок.
- **Найдено:** **ноль дефектов продукта.** Два красных стража, оба не от этой правки и оба вылечены до выката:
  (1) страж формы отчётов — у отчёта `2026-09-15_pilot-two-takes.md` поле «Прогонов» было пустым заголовком и строки
  в реестре не было (долг утренней работы, уже лежал в стволе); (2) страж воронки красный с 13.09 — прибор входа по
  ссылке метил сессии динамическим импортом и не метил сессию стенда. ⚠️ Известное, не чинено: метка сборки боя
  снова `dirty` (снимок витрины лендинга пишется перед сборкой — капкан STATUS от 13.09).
- **Следы:** на стейдже — 7 событий `landing_view` с `env = stage` в 21:13–21:14 и одна гостевая учётка, удалённая
  прибором. В бою прибор НЕ запускался (без слова владельца он записал бы ложный приход) — боевых событий этот прогон
  не оставил. Стенд поднят в 21:0x и погашен в 21:3x (процессы проверены по портам).
- **Вердикт:** в бою и работает. Слои Ю · П · С исполнены, событие прочитано из PostHog.
  **Гигиена разработчика:** юниты 535 из 535, мутации 2 из 2 красные адресно.
  **Ручной функциональный прогон:** живой Chromium-человек на стейдже прошёл главную с источником YouTube и без него,
  переход на лендинг, гостевой вход; ряд прочитан в PostHog глазами; боевая главная прочитана HTML.
  ⚠️ Граница, названная владельцу: адрес, набранный руками (подписи Instagram и TikTok не кликаются), честно пишется
  `$direct` — источник виден только тем, кто кликнул.
