# Отчёт по тестированию — страница вычитки: варианты абзацами получают кнопки выбора (S1)

> **Создан:** 2026-09-25 23:32 +03:00 (dev-1, срочная постановка Менеджера `ndim-61`) · **Родитель:**
> `bugs/NEW_review_page_options_without_radio.md` (S1) · **Статус:** ✅ pass · **Исходящее:** —

## Карточка прогона

- **Работа:** владелец ответил на №098 текстом: «*какого хуя ты опять радиокнопки забыл сделать?*». Варианты там стояли
  абзацами «`**А) V1 …».** текст`», а разбор `tools/lib/review-core.mjs` знал только пункт списка «`- **А) …**`» и вернул 0
  вариантов. Счётная сверка «разобрано N из N» считала кандидатов той же строгой регуляркой и промолчала («0 из 0»),
  предполёт не отказал. Лечение:
  · разбор узнаёт обе формы; у вопроса со списком кнопки строятся по списку, без списка — по абзацам;
  · счёт кандидатов широкий (любая строка «X) …»);
  · предполёт `lintOptionsLost` отказывает, если строк-вариантов ≥ 2, а разобрано меньше.
- **Контур:** контур вычитки на рабочем месте dev-1 (автономен, стенд не нужен). Фикстура — копия живого интервью с пустыми
  полями ответа, `interviews/interview_997_radio_fixture.md`. Страница поднимается `--no-open --no-signal` на постоянном
  порту документа (20360), браузер владельцу не открывался.
- **Прогонов:** 2026-09-25 +03:00, из корня рабочего места `D:\work\ai_sandbox\ndim-team\ndim_dev1`:
  · 23:25:36 — мутанты юнитов `node <скретчпад сессии>/mut-radio.mjs`;
  · 23:25:49–23:27:46 — регрессы `node tools/verify-owner-reviews.mjs` и `node tools/verify-bug110.mjs`;
  · 23:29:32–23:29:44 — `node qa/reports/2026-09-25_review-radio.driver.mjs`, «Голова: db15f5a · дерево чистое» → 11
    прошло, 0 провалов, фикстура и замок удалены;
  · 23:30:57 — замер по `interviews/`: форма «- **A. …**» (буква с точкой) — 21 строка в №092, разбор её не знал;
    лечение `80623a1`;
  · 23:32:14 — мутанты M1–M3; 23:32:29–23:34:25 — регрессы `verify-owner-reviews` и `verify-bug110` заново;
  · 23:34:25–23:34:38 — драйвер заново на `80623a1` → 11 прошло, 0 провалов. Вывод «дерево ГРЯЗНОЕ»: в дереве лежали
    незакоммиченные документ бага и этот отчёт, код продукта и драйвер — закоммичены;
  · **после суда радиокнопок (`d5a8c87`, CONFIRMED с оговорками):** 23:55:29–23:55:53 — драйвер с новыми РК-04 и РК-05 на
    `df08caf` → 14 из 15: РК-04 «queue отказал кодом 1» — ❌, отказ напечатан, но точка входа выходила кодом 0 (найдено
    прогоном, вылечено `351fbc5`); **23:56:12–23:56:35 — на `351fbc5` → 15 прошло, 0 провалов** — итог;
  · ворота — строка «Гигиена» ниже.
- **Проверок:** итог — 15 из 15 (РК-01…РК-05), провалено 0; до суда — 11 из 11 (РК-01…РК-03, оба прогона).
  - Суд радиокнопок, две находки, обе вылечены:
    · **Н1** — «- **A.** Впишу числа ниже.» (точка сразу перед «**») не узнавалась ни разбором, ни счётом: у №092 В6 была
      одна кнопка из четырёх, а я написал «все 21 узнаются» — неверно для №092:146–148. Шаблоны принимают «.» перед
      пробелом и перед «**»; корпус — 1196 строк-кандидатов, 0 расхождений; юнит — форма «**C.**» и живой №092 (В1–В6 по 4);
    · **Н2** — моё ON-REAL-PATH «open и batch зовут preflight» было ложным: пачка строит страницу через `/doc?p=` мимо
      `preflight`, `queue` ставил без проверки (у судьи — 200 и 0 кнопок). Теперь `queue` отказывает кодом 1, `/doc` отдаёт
      409 «Этот документ агент ещё чинит» с вопросом и строкой; юнит `tools/review-options-gate.test.mjs` 2/2 (мутант
      «проверка очереди снята» — красный адресно); живьём РК-04 и РК-05.
    · Мелкое: в отказе называется первая НЕУЗНАННАЯ строка (при смеси форм первая похожая могла быть узнанной).
  - Гигиена: юниты `node --test tools/lib/review-core.test.mjs` 9/9, новых 6 (шестой — форма №092 с точкой и контроль
    «инициал в прозе — не вариант»; мутант M3 «точка не узнаётся» — красный ровно он):
    · абзацы формы №098 разобраны все — с текстом и ценой;
    · форма №097 (абзацы плюс список) — кнопки по списку, без дублей;
    · предполёт отказывает на неузнанной форме «А) …»;
    · контроль: обе узнанные формы и отвеченный вопрос — не отказ;
    · живые №098 и №097 — кнопки для всех вариантов.
    Мутанты: M1 «абзац не узнаётся» — 4 красных (абзацы, №097, контроль предполёта, живые); M2 «счёт кандидатов строгий» —
    красный «отказ предполёта». Файл восстановлен побайтово в обоих. Регрессы: `verify-owner-reviews` 137/0 (блок 5в — все
    живые интервью новым широким счётом), `verify-bug110` 25/0; `interviews/decisions/queue.json` возвращён `git checkout`
    (капкан прибора). Ворота 23:36:35–23:37:29 (`npm run -s <имя>` по очереди): `test:tools` 866/866, `guards` 22/22
    «ВОРОТА ЧИСТЫ», `typecheck` 0, `check` 0 ошибок (614 файлов), `questions` и `kaif:check` зелёные, `npm test` 557/566 —
    те же 9 красных выгрузки каталога вне git (5125 против 5153), построчно.
  - Функциональный прогон: контур выше, живой Chromium, кадры `test-results/review-radio/` прочитаны глазами:
    · **РК-01** pass — копия №098: страница поднята; у В1 пять кнопок А–Д, у В2 четыре А–Г, на 1440 и на 390. Подпись кнопки
      А — полный текст варианта с ценой: «А) V1 «Дело сверху, руководство под делом». Под тестом — около 7 000 знаков…
      *Цена:* человек, которому нужен только тест, текст пролистывает» (кадры `rk01-098-v1-1440`, `rk01-098-v1-390`).
    · **РК-02** pass — копия №097: у В1 три кнопки «А) русский · Б) английский · В) свой ответ», дублей от абзацев-описаний
      нет (кадр `rk02-097-v1-1440`).
    · **РК-03** pass (контроль) — варианты №098 без «**»: `open` не поднял страницу, код 1, «⛔ СТРАНИЦА НЕ ПОДНЯТА: у вопроса
      есть варианты…» и «В1 — interviews\interview_997_radio_fixture.md:48 · строк-вариантов 5, разобрано 0».
- **Найдено:** близнец того же класса — форма «- **A. …**» (буква с точкой): 21 строка в №092 (замер 23:30:57), вопросы
  №092 тоже остались бы без кнопок. Найдено тем, что я проверил своё же утверждение «такой формы в корпусе нет» до
  отправки; вылечено `80623a1`, корпус — 1193 строки-кандидата, 0 расхождений. Других дефектов — 0. Наблюдение, не дефект: текст варианта стоит на странице дважды — абзацем в
  теле вопроса и подписью кнопки. Так же было и у формы-списка, это прежнее устройство страницы. Для длинных вариантов
  №098 это вдвое больше чтения — вопрос устройства страницы Менеджеру, не правлю.
- **Следы:** фикстура `interviews/interview_997_radio_fixture.md`, её замок и решение удалены драйвером; страница погашена
  своим процессом. Живые №097 и №098 не тронуты. Кадры — `test-results/review-radio/` (вне git).
- **Вердикт:** **pass** — вопрос с вариантами абзацами получает кнопки выбора; вопрос с неузнанной формой страницу не
  поднимает и называет место. Годится к суду и мержу.

## Решения, принятые без владельца

- `[AI]` У вопроса, где есть и список, и абзацы, кнопки строятся по списку. Так уже устроен №097, и так агенты обходили
  дефект: абзацы — описание, список — выбор.
- `[AI]` Буква варианта — любая заглавная, как у пункта списка. Постановка называла А–Е и A–F; узкий набор потерял бы
  вариант «Ж)», если он когда-нибудь появится.

## Границы (GAP)

- **Реальный мир владельца** — первой страницей после мержа (Менеджер); dev-1 страницу владельцу не поднимает.
- **Вариант без «)» и без жирной точки** («**А —** …», «А: …») — не узнаётся и счётом не видится; замер 23:30:57 форм
  «**X —**» в корпусе не нашёл. *(Поправка суда Н1: прежде здесь стояло «все 21 попадание — «**X.**» №092, теперь
  узнаются» — неверно для «**A.**» с точкой перед «**»; вылечено `df08caf`.)*
- **Вопрос с одной строкой-вариантом** порог «≥ 2» не судит — №070 В2 (варианты картинками).
