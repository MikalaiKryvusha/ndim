# Отчёт по тестированию — прибор снимка консолей сверяет годность разрезов (`bugs/232`)

> **Создан:** 2026-09-12 15:3x +03:00 · **Родитель:** `bugs/232` · `plans/84_DONE` (разведка,
> породившая находку) · **Статус:** ✅ ЛЕЧЕНИЕ ПРОВЕРЕНО — юниты, мутация и ручной
> функциональный прогон исполнены, дефектов не найдено ·
> **Исходящее:** ничего — прибор читающий, в бой ничего не уезжает.

## Карточка прогона

- **Работа:** `tools/console-snapshot.mjs` печатал раздел «ПОЛОСЫ ПОЗИЦИЙ ПО УСТРОЙСТВАМ» из
  разреза `device × page`, удерживающего **5 кликов из 40**, ничем об этом не предупреждая, и
  выдал правдоподобное ложное число («полоса 1–10 — 762 показа, 0 кликов»), которое за час дошло
  до владельца и до `MASTER_PLAN.md`. Лечение — сверка суммы каждого разреза с эталоном
  (`sliceIntegrity`), честный отказ печатать клики негодного разреза, флаг годности в снимке.
- **Контур:** **бой** — прибор читающий, ходит в живой Google Search Console по ключу из `.env`
  с областью `webmasters.readonly`. Другого контура у него нет и быть не может: сверка годности
  проверяема только на настоящих данных, где потеря кликов и происходит.
- **Прогонов:** **6**, все 2026-09-12 между 14:45 и 15:30 +03:00 —
  1. `node tools/_probe-agg.mjs` (временный зонд, удалён) — семь разрезов одним окном, сверка итогов;
  2. `node tools/_probe-filter.mjs` (временный зонд, удалён) — спасает ли фильтр вместо группировки;
  3. `node tools/console-snapshot.mjs --dry` — прибор после лечения, вывод прочитан глазами;
  4. `node tools/console-snapshot.mjs --dry` **на мутанте** (порог `0.9` → `0.0`);
  5. `node --test tools/console-snapshot.test.mjs` — 32 юнита;
  6. `node tools/console-snapshot.mjs` — настоящий снимок, поля снимка прочитаны.
- **Проверок:** **пройдено 36, провалено 0.** Юнитов 32/32 (из них 4 новых на годность разреза),
  мутация 1 (красная адресно), ручной функциональный прогон 3 (вывод прибора, флаг в снимке,
  сверка чисел с прямым опросом API).
- **Найдено:** **ноль дефектов лечения.** Сам дефект, ради которого всё делалось, — `bugs/232`,
  измерен и вылечен. Попутно снята вторая его жертва: «необъяснённая» строка `MASTER_PLAN.md`
  про десктоп с одним кликом (`researches/67`) — артефакт того же разреза.
- **Следы:** перезаписан `reports/CONSOLES/2026-09-12_google_search_console.json` (повтор дня —
  штатное поведение прибора, прежнее содержимое в истории git). Два временных зонда
  (`tools/_probe-agg.mjs`, `tools/_probe-filter.mjs`) созданы и **удалены**. В бой не записано
  ничего: область доступа только на чтение.
- **Вердикт:** **ГОДИТСЯ.** Прибор больше не может напечатать клики негодного разреза молча, и это
  доказано мутацией, а не утверждением.

## 1. Замер, вскрывший дефект (прогон 1)

Один окно (01.07–11.09), `type: web`, `dataState: all`, семь разрезов подряд:

| Размерности запроса | Строк | Показов | Кликов | Годен |
|---|---|---|---|---|
| (без размерностей) | 1 | 9 514 | 40 | эталон |
| `date` | 64 | 9 514 | 40 | ✅ |
| `device` | 3 | 9 514 | 40 | ✅ |
| `date × device` | 96 | 9 514 | 40 | ✅ |
| `page` | 2 629 | 11 370 | 41 | ✅ |
| `query` | 2 873 | 5 342 | **5** | 🔴 |
| **`device × page`** | 1 670 | 6 810 | **5** | 🔴 |

🔑 Показы по `page` выше эталона — законно: постраничная агрегация считает показ на каждую
показанную страницу, эталон — один на появление сайта. Дефект не здесь.

## 2. Проверка лечения, которое НЕ помогло (прогон 2) — записано, чтобы не пробовали снова

Гипотеза: клики теряет ГРУППИРОВКА, а ФИЛЬТР по устройству их сохранит. **Опровергнута замером:**

| Способ | DESKTOP | MOBILE |
|---|---|---|
| группировка `device` (эталон) | 15 кликов | 25 кликов |
| группировка `device × page` | — | — (вместе 5) |
| **фильтр `device` + группировка `page`** | **1 клик** | **4 клика** |

⛔ **Вывод, который стоит знать до следующей попытки:** клики теряются от САМОГО СОЧЕТАНИЯ
`page` с `device`, а не от способа его запросить. Гипотеза Г-2 («разрыв CTR по устройствам — это
устройство или глубина выдачи?») этими данными **непроверяема в принципе**. Записано в шапке
`bandsByDevice`.

## 3. Ручной функциональный прогон (прогоны 3 и 6)

🔴 По слову владельца 2026-09-12 юнит и мутация маркер не переворачивают — ниже именно ручной
прогон: прибор запущен руками, вывод прочитан глазами, числа сверены с прямым опросом API.

**Вывод прибора после лечения (прочитан глазами):**

```
ПОЛОСЫ ПОЗИЦИЙ (разрез `page` — держит все клики):
  поз 1–10    страниц  496 показов  2345 кликов  24 CTR 1.02 %
  поз 11–20   страниц  183 показов   840 кликов   1 CTR 0.12 %
  поз 21–50   страниц  607 показов  2695 кликов   9 CTR 0.33 %
  поз 51–100  страниц 1341 показов  5498 кликов   6 CTR 0.11 %

УСТРОЙСТВА (разрез `device` — держит все клики):
  MOBILE   показов  1767 кликов  25 CTR 1.41 % позиция 18.4
  DESKTOP  показов  7721 кликов  15 CTR 0.19 % позиция 50.6

ПОЛОСЫ ПОЗИЦИЙ ПО УСТРОЙСТВАМ (гипотеза Г-2):
  ⛔ РАЗРЕЗ НЕ ГОДЕН: срез держит 5 кликов из 40 — НЕ годен для CTR и выводов о кликах.
     Удержано кликов 13 %, показов 72 % …
  DESKTOP: показов 5972 (кликов не читать)

ГОДНОСТЬ СРЕЗОВ (сверка с эталоном: показов 9525 · кликов 40):
  ✅ byPage  41 (102 %) · 🔴 byQuery 5 (13 %) · ✅ byDevice 40 (100 %)
  ✅ byCountry 40 (100 %) · 🔴 byDevicePage 5 (13 %)
```

**Сверка с прямым опросом API** (прогон 1): полоса 1–10 по `page` — прибор 2 345/24, прямой
опрос 2 155/23. Расхождение объяснено и ожидаемо: у прибора окно 16 месяцев, у зонда — с 01.07;
знак и порядок совпадают. Устройства совпали точно (1 767/25 и 7 721/15).

**Поля снимка** (прогон 6): `deviceBandsFit: "impressions-only"`, `sliceFitness` несёт годность
всех пяти разрезов. Читатель снимка больше не гадает, какому числу верить.

## 4. Мутация (прогон 4) — доказательство, что проверка нагружена

Порог годности `clicksKept >= 0.9` → `0.0` (резервная копия файла, по правилу `EXP-0190`;
`git checkout` не применялся).

**Результат — прибор немедленно напечатал ровно то ложное число, которым обманулась утренняя
сессия:**

```
  DESKTOP: показов 5972 · кликов 1 · CTR 0.02 % · позиция 50.4
      поз 1–10    показов   543 кликов   0  CTR 0.00 %
```

Файл восстановлен из резервной копии, порог на месте (`console-snapshot-core.mjs:362`).
🔑 Мутант доказал не «код исполняется», а что **именно порог стоит между потерянными кликами и
выводом о продукте**.

## 5. Юниты (прогон 5) — 32/32, из них 4 новых

- срез, потерявший клики, объявляется НЕ годным для CTR;
- срез, удержавший клики, объявляется годным;
- постраничный разрез ВЫШЕ эталона годен, а не подозрителен (граница, на которой наивная
  проверка «сумма равна эталону» покраснела бы ложно);
- без эталона срез НЕ объявляется годным по умолчанию — молчаливое «годен» вернуло бы ровно тот
  дефект, ради которого всё это.

## 6. Названная граница этой работы

⚠️ **Порог 0,9 назначен суждением, а не замером.** Наблюдаемые доли — 100 %, 102 %, 13 % — лежат
далеко от него с обеих сторон, поэтому его точное значение сегодня ни на что не влияет. Если
появится разрез с долей 0,85–0,95, порог придётся обосновывать, и это честно записано здесь, а
не выдано за вычисленное число.

⚠️ **Общего стража «производное число сверено с эталоном» у проекта нет.** Вылечен один прибор.
Другие приборы, считающие производные величины, этой проверки не имеют.

📌 **Гипотеза Г-2 осталась открытой** — не потому, что её не проверили, а потому, что данные
Search Console на неё ответить не могут. Кандидат: `date × device` удерживает все клики и даёт
устройство со средней позицией по дням, но не CTR внутри полосы позиций.
