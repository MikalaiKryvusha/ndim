# Отчёт по тестированию — главная быстрее: портреты без ожидания оживления, мост одним пакетом записи

> **Создан:** 2026-09-25 19:27 +03:00 · **Родитель:** наблюдения отчётов `qa/reports/2026-09-25_new-landing-v1-release.md`
> (портреты проявляются после оживления) и `qa/reports/2026-09-25_bridge-timing.md` (мост ≈3,06 с) · набор
> `qa/suites/new-landing-v1.md` (НЛ-01…НЛ-03, НЛ-12, НЛ-15, НЛ-16) · **Статус:** ✅ pass на стенде и стейдже · **Исходящее:**
> выкат в бой — по слову владельца.

## Карточка прогона

- **Работа:** две правки главной. (1) `38c244d` — портреты героя проявляются, как только загружены, не дожидаясь оживления
  страницы: `readyEarlyScript(id)` в `src/lib/ui/ready.ts`, встроенная строка сразу за трио. (2) `2fcd523` — мост «Смотреть
  больше» пишет оценки теста одним пакетом: для только что рождённого гостя корень `users/{uid}`, все оценки и отметка
  точки уходят одним `writeBatch` (`saveRatingsBatch` · `saveTestRatings` · `carryAll`) без чтения корня и цепочки записей.
- **Контур:** стенд (`npm run stand`), локальная сборка (`npm run build` + `npx vite preview --port 4173 --strictPort`),
  стейдж `ndim-stage.web.app` (сборка `2fcd523`, выкат 19:15:28–19:21:59, расписка `dirty: false`), бой — только как
  контроль «до» (сборка `a3e3125`), проходы меткой прибора.
- **Прогонов:** 2026-09-25 +03:00:
  · ≈19:03 — замер «до» на бою: `node <скретчпад>/hero-timing.mjs` (сеть 1,6 Мбит/с, задержка 150 мс, процессор ×4, метка
    прибора), два захода;
  · ≈19:07 — `npm run build` → `npx vite preview --port 4173 --strictPort`; `node <скретчпад>/hero-timing.mjs http://localhost:4173/`;
    `node <скретчпад>/hero-nojs.mjs http://localhost:4173/` и `… https://ndimspace.app/` (JS приложения отрезан — контроль);
    `node <скретчпад>/hero-normal.mjs` (обычный путь 390 светлая, 1440 тёмная EN); `node <скретчпад>/stars-check.mjs`;
  · 19:11:42 — стенд: `node qa/reports/2026-09-25_root-posthog-only.repeat.driver.mjs` (мост с пакетом) + чтение
    `users/{uid}` и `points/{uid}` из эмулятора (`Bearer owner`);
  · 19:12–19:14 — `npm run e2e` (стенд погашен) · 19:15:28–19:21:59 — `npm run deploy -- --stage`;
  · 19:22:23 — `MSYS_NO_PATHCONV=1 node qa/reports/2026-09-25_bridge-timing.stage-driver.mjs --path /ru --stars 3` × 2;
  · ≈19:22:50 — `node <скретчпад>/hero-nojs.mjs https://ndim-stage.web.app/` и `hero-timing.mjs https://ndim-stage.web.app/`;
  · 19:23:23 — `MSYS_NO_PATHCONV=1 node qa/reports/2026-09-25_new-landing-v1.stage-driver-a3e3125.mjs --path / --wait 150`
    (сервер синхронизации стейджа поднят на время прогона `node tools/run-sync-server.mjs --contour stage`, погашен по pid).
  Разовые сценарии `hero-*.mjs`, `stars-check.mjs` — скретчпад сессии, вне git.
- **Проверок:** функциональных пройдено 8, провалено 0.
  - Гигиена: `npm test` 549/549 · `npm run test:tools` 845/845 · `npm run guards` 22/22 · `npm run check` 0 ·
    `npm run typecheck` 0 · `npm run questions` чисто · `npm run kaif:check` ✅ · `npm run e2e` 112/4/0 · юниты
    `ready.test.ts` (3) и `compat-demo.test.ts` (тесты переноса переписаны под пакет); мутация «писать по одной» — красные
    «одним вызовом» (названный) и «атомарен» (следствие: он тоже требует один вызов); дверь стейджа — заголовки 12/0,
    смоук под сессией 24/0, публичный 83/0.
  - Функциональный прогон: стенд, локальная сборка, стейдж · живой Chromium · ПРОЧИТАНО:
    · портреты «до», бой: загружены к 1,4–1,6 с, страница ожила к 1,8–1,9 с, видны к 2,0–2,2 с; с отрезанным JS —
      загружены, но `ok=false`, прозрачность 0 (навсегда);
    · портреты «после»: с отрезанным JS — `ok=true`, прозрачность 1 у всех трёх (локальная сборка и стейдж); стейдж,
      заход 2 — страница ожила к 3,35 с, портреты видны к 2,50 с (раньше оживления);
    · обычный путь: страница оживает, портреты видны, звезда «Матрице» 9 → девять звёзд золотые, поп-ап «Макс — 27 %»,
      ошибок консоли 0 (`/` 390 светлая, `/en` 1440 тёмная);
    · мост на стенде: «Настольные игры» 9, «Велоспорт» 7, «Матрица» 10 → в базе корень `users/{uid}` (язык ru, время
      создания), три оценки, точка `guest: true`, сервер синхронизации её обработал (`escortRuns: 1`);
    · мост на стейдже, уход документа `/profile` после «Смотреть больше»: 1 937 и 2 031 мс (было 3 056 мс,
      `qa/reports/2026-09-25_bridge-timing.md`); в базе по три оценки; уборка — 200;
    · стейдж, гость с корня: мост → три оценки в NDim ID (`dbMatches: true`), Связи через 88 с, на экране 24 человека,
      строка мостика верная, `guest_start` с `entry = root` в PostHog.
- **Найдено:** дефектов ноль. Кадр с «серыми» звёздами при цифре 9 оказался снятым посреди перехода цвета — через
  1,2 с все девять золотые (`stars-check.mjs`, стенд и бой одинаково).
- **Следы:** стейдж — четыре гостевые учётки (три замера моста и прогон с корня) удалены своими токенами (оценки, точка,
  учётка — 200), документы `users/{uid}` и Связей остаются до уборки сервера синхронизации; события `env = stage` в
  PostHog; счётчики воронки стейджа +1 от заходов на `/ru`. Бой — проходы меткой прибора, в базу и метрику людей не
  пишут. Стенд — гость в эмуляторе. Кадры — вне git.
- **Вердикт:** **pass** — портреты первого экрана видны без ожидания скрипта, мост быстрее примерно на секунду; к бою
  годно по слову владельца. Не проверено: встроенный браузер Instagram, телефон владельца, бой.
