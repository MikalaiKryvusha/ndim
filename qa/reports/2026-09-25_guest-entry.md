# Отчёт по тестированию — место входа гостя (`entry` у `guest_start`)

> **Создан:** 2026-09-25 09:08 +03:00 (коммит `839255a`; *поправка 09:25 — прежде стояло выдуманное «09:1x»*) · **Родитель:** `plans/105_epic104_phase1_texts_faq_eyes.md` шаг Б2 · набор
> `qa/suites/analytics.md` §12 · **Статус:** ✅ годно к выкату вместе с ближайшей сборкой · **Исходящее:** —

## Карточка прогона

- **Работа:** событие `guest_start` несёт место входа — через какую дверь человек стал гостем: `root` (кнопка главной),
  `landing` (мост демо `/ru` · `/en`), `catalog_card` (звезда и дверь карточки каталога), `signin` («гостем» на экране
  входа), `restart` («начать заново»), `test` (страница теста), `direct` (адрес без слова двери). Слово едет в адресе
  двери `?guest=<слово>`; союз и разбор — `src/lib/data/funnel.ts`. Повод — неделя ролика: 9 гостей без единого
  признака, откуда они пришли.
- **Контур:** свежая локальная сборка, отданная браузеру ПОД ИМЕНЕМ СТЕЙДЖА `https://ndim-stage.web.app` перехватом
  маршрута (страницы — из `vite preview` :4173). Продукт сам выбирает контур по имени хоста, поэтому работал с настоящим
  Firebase стейджа (`ndim-stage`, база `ndim-db-stage`) и контуром аналитики `stage`. Все запросы к `*.posthog.com`
  перехвачены: тело прочитано, ответ — заглушка 200, наружу не ушло ни одного события. Выкат не делался.
- **Прогонов:** 2026-09-25 +03:00:
  · до 08:56 `npm run build` (сборка 2064, дерево грязное — правки не закоммичены; минута старта не снята) →
    08:56:52 `npx vite preview --port 4173 --strictPort` ответил 200;
  · между 08:56:52 и 08:58:53 прогон 1 — `node qa/reports/2026-09-25_guest-entry.driver.mjs` из корня проекта (драйвер
    лежит рядом с отчётом; прибором в `tools/` не заведён по решению владельца №096 В2 = А), кейсы ЕВ-01…ЕВ-07 → 7
    провалов, разбор ниже;
  · 08:58:53–09:00:08 прогон 2 — тот же драйвер, перехват PostHog отвечает 200 вместо обрыва, повторы считаются по `uuid`
    → **0 провалов**, уборка 7 из 7;
  · ≈09:10–09:24 прогон 3 — независимый судья (субагент, `/fable-judge`) на HEAD `839255a`: `npm run build` (сборка 2065) →
    превью :4173 → тот же драйвер → **0 провалов**, уборка 7 из 7.
- **Проверок:** прогон 2 — 52 проверки в 7 кейсах + уборка: пройдено 53, провалено 0 (прогон 3 — те же 53 из 53).
  *(поправка 09:25: прежде здесь стояло «56 проверок … пройдено 57» — числа не были посчитаны по выводу; найдено судьёй.)*
  - Гигиена: `npm test` 542/0 · `npm run typecheck` 0 ошибок · `npm run check` 0 ошибок · юниты новых проверок доказаны
    мутациями: опечатка слова двери `catalogcard` → красный адресно (файл и строка) · `entryOf` без сверки с союзом →
    красный «слово вне союза» · страж воронки `verify-probe-mark` (признак пересылки после суда ТРЕБУЕТ форму
    `capture(step, props)`) краснеет на снятой пересылке и на выброшенных свойствах (М4) в живом дереве · `npm run guards` 22/22 после правки стража · e2e `demo.spec.ts` 9/10:
    провал — нестабильность появления кнопки моста на mobile, не связанная с правкой (ниже, «Найдено»).
  - Функциональный прогон: сборка под именем стейджа · живой Chromium 390×860 с приметами человека · в каждом кейсе
    ПРОЧИТАНО тело перехваченного `guest_start`: ЕВ-01 `root` · ЕВ-02 `landing` · ЕВ-03 `catalog_card` (гость рождён
    звездой карточки, событие одно) · ЕВ-04 `signin` · ЕВ-05 `test` · ЕВ-06 `/profile?guest=1` → `direct` · ЕВ-07
    `/profile?guest=matrix-1999` → `direct`; везде `env: 'stage'`, ключи только `entry` и `env`, ни одного `@`, в адресе
    после входа нет `guest=`; кадры ЕВ-03 и ЕВ-04 просмотрены глазами — гостевой профиль, у ЕВ-03 блок «Вы оценили»
    с оценкой 7 из 10.
- **Найдено:** один дефект своей правки (вылечен до прогона), два дефекта своего драйвера, один баг вне задачи:
  · 🔑 **дефект моей первой редакции, пойманный чтением кода до прогона:** звезда карточки каталога рождает гостя сама
    (`door/engine.ts` → `saveTestRating`), и место входа там стояло бы `test`; профиль по адресу двери после касания
    видит живую сессию и молчит. Лечение — `saveTestRating(…, entry = 'test')`, дверь карточки передаёт `catalog_card`;
    ЕВ-03 это и подтвердил;
  · прогон 1: «`guest_start` пришёл дважды» в 5 кейсах — повторная отправка той же пачки SDK после обрыва запроса моим
    перехватом; при ответе 200 у каждой двери ровно одна отправка (прогон 2). Дефект драйвера, не продукта;
  · прогон 1: проверка «канал живой — есть и другие события» красная у ЕВ-06 и ЕВ-07 — у прямого адреса других событий
    не бывает; проверка снята как неверная, сам `guest_start` доказывает живой канал;
  · вне задачи: `bugs/NEW_DONE_e2e_demo_bridge_flaky_on_mobile.md` — e2e «мост в гостя» на mobile падает 1 раз из 3 на
    появлении кнопки моста (строка 84), до проверки адреса;
  · суд `/fable-judge` (09:24): дыра покрытия — мутация «свойства молча выброшены из пересылки» (`capture(step)` вместо
    `capture(step, props)`) была зелёной везде; лечение — признак пересылки в `tools/verify-probe-mark.mjs` требует форму
    `capture(step, props)` (коммит поправок того же утра).
- **Следы:** 7 гостевых учёток стейджа на прогон (прогоны 2 и 3 — 14; прогон 1 — ещё 7) — удалены своим `idToken`, каждый
  прогон 7 из 7; их документы в базе стейджа (`points/…`)
  остаются до уборки сиротских гостей сервером синхронизации; счётчики своей воронки стейджа (`space/funnel/days`)
  получили шаги этих гостей; кадры — `test-results/guest-entry/` (вне git).
- **Вердикт:** **pass** — годно к выкату вместе с ближайшей сборкой. Границы: дверь `restart` живьём не проходилась
  (истёкший гость не воспроизводился), её адрес стережёт юнит-сверка; приём событий самим PostHog и ряд в нём — после
  выката; порядок работ нарушен в одном месте и назван: тест-кейсы §12 набора записаны ПОСЛЕ прогона, а не до.

РЕАЛЬНЫЙ МИР: накоплено — в бою правка ещё не стоит; старые ссылки и закладки `/profile?guest=1` проверены (ЕВ-06 →
`direct`) · данные и техника — Firebase стейджа, не боя; телефонная ширина в эмуляции Chromium, не телефон человека ·
путь — пять дверей продукта из шести (без `restart`) и прямой адрес, как ходит человек; вход по ссылке из ролика ведёт
на главную (ЕВ-01).
