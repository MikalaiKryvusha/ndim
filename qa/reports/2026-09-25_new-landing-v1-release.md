# Отчёт по тестированию — выкат новой V1 главной в бой по ответам владельца №097

> **Создан:** 2026-09-25 18:36 +03:00 · **Родитель:** `plans/106_epic104_phase2_new_v1_on_main.md` (Д11–Д12, «Ответы
> владельца №097») · набор `qa/suites/new-landing-v1.md` · **Статус:** ✅ pass — в бою · **Исходящее:** —.

## Карточка прогона

- **Работа:** коммит `a3e3125` — новая V1 главной на `/`, `/ru`, `/en` с правками по ответам владельца (интервью №097,
  ≈17:52): практики теста без сенситивных тем («Настольные игры», «Чтение», «Велоспорт», «Медитация»), абзац над тестом и
  ответ FAQ «Как работает тест на совместимость?» языком сильных запросов, корень считает приход только в PostHog
  (№078 В1 = Г); вместе с ним в бой уехало всё накопленное с 2026-09-19 (место входа гостя `entry`, строка «Письмо
  отправлено» без «на этом устройстве»). AUTH выката — слово владельца: «*чиним, тестим стейдж, выкатываем*».
- **Контур:** стенд (гигиена и e2e) · стейдж `ndim-stage.web.app` (сборка `a3e3125`, расписка `dirty: false`) · бой
  `ndimspace.app` (сборка `a3e3125`, `build-stamp` = хеш `2y3jw7`, прежняя — `001480c` от 2026-09-19).
- **Прогонов:** 2026-09-25 +03:00:
  · 18:04–18:06 — `npm test` 542/542 · `npm run test:tools` 845/845 · `npm run guards` 22/22 · `npm run typecheck` 0 ·
    `npm run questions` чисто · `npm run kaif:check` ✅ · `npm run e2e` (стенд погашен) 112 / 4 пропущены / 0;
  · 18:07:08–18:12:52 — `npm run deploy -- --stage` (журнал `$TEMP/ndim-deploy-stage3.log`): смоук по артефакту на стенде,
    смоук под сессией 24/0, публичный смоук 83/0, `deploy-exit=0`;
  · 18:13:25–18:16:14 — `MSYS_NO_PATHCONV=1 node qa/reports/2026-09-25_new-landing-v1.stage-driver-a3e3125.mjs --path / --wait 150`
    и `--path /ru` (сервер синхронизации стейджа поднят на время прогона: `node tools/run-sync-server.mjs --contour stage`,
    погашен адресно по pid после прогона; вывод — `test-results/stage-a3e3125-root.json`, `-ru.json`);
  · 18:16:36 — `node qa/reports/2026-09-25_root-posthog-only.stage.driver.mjs` (счётчик воронки стейджа до и после заходов);
  · 18:17:04 — `node tools/shoot-new-landing.mjs --base https://ndim-stage.web.app --out test-results/stage-a3e3125-frames`;
  · ≈18:19 — `npm run deploy -- --gate-only`: замок стейджа принял расписку, Smoke 19/0;
  · 18:21:16–18:27:44 — `npm run deploy` (журнал `$TEMP/ndim-deploy-prod-0925.log`): сборка легла в бой («цель БОЙ:
    сборка 2y3jw7 · сайт отдаёт 2y3jw7; сосед СТЕЙДЖ не тронут»), затем шаг «заголовки кеширования» упал — `deploy-exit=1`;
  · ≈18:28 — `node tools/verify-prod-cache.mjs --base https://ndimspace.app` вручную: 12/0;
  · 18:28–18:29 — оставшиеся шаги двери вручную теми же командами (`tools/deploy.mjs:636–641`), журнал
    `$TEMP/ndim-deploy-prod-0925-tail.log`: `node tools/verify-prod-lang-redirects.mjs --base https://ndimspace.app` (код 0) ·
    `node tools/verify-prod-signed-in.mjs --base https://ndimspace.app` 24/0 · `node tools/verify-prod-b4.mjs --base https://ndimspace.app` 83/0;
  · 18:30:05 — разовый проход боевой главной меткой прибора (в метрику людей не попадает): четыре вида и вернувшийся
    человек; ≈18:32 — повторный кадр с паузой 2,5 с (сценарии исполнялись из скретчпада; тела без изменений — `qa/reports/2026-09-25_new-landing-v1-release.prod-walk.driver.mjs`, `…prod-walk2.driver.mjs`).
- **Проверок:** функциональных пройдено 9, провалено 0; шаг двери провален 1 раз и пройден повтором.
  - Гигиена: юниты 542/542 и 845/845, стражи 22/22, типы 0, `check` 0, e2e 112/4/0; дверь стейджа и дверь боя — числа выше.
  - Функциональный прогон: стейдж и бой · живой Chromium · ПРОЧИТАНО:
    · стейдж, гость с `/` и с `/ru` (человеком): звёзды «Гарри Поттеру» 10, «Матрице» 9, «Чтению» 8 → в NDim ID гостя
      ровно эти три (`dbMatches: true`); мост 2,9 с; Связи через 79 с и 27 с, на экране «Связи» 24 человека; строка
      мостика «Ваши оценки из теста на совместимость сохранены в Вашем профиле — вот Ваши Связи…»; «Назад» — пустая
      вкладка; уборка — все коды 200;
    · стейдж, PostHog `env = stage`: `demo_touch` → `landing_view` → `guest_start` с `/` (18:13:27–29, `entry = root`) и с
      `/ru` (18:15:18–20, `entry = landing`) — приход корня доходит до PostHog;
    · стейдж, счётчик воронки в базе: заход на корень — `landing_view` 20 → 20, `demo_touch` 10 → 10; заход на `/ru` — 21
      и 11 (+1 и +1): корень в базу не пишет, `/ru` пишет;
    · стейдж, двенадцать видов (`/`, `/ru`, `/en` × 390/1440 × светлая/тёмная) — все двенадцать первых экранов
      просмотрены глазами; сценарий с оценками на телефоне: карта над списком, поп-ап «Макс — Ваша самая сильная связь:
      40 %», три карточки 40 % · 28 % · 22 %;
    · бой, меткой прибора: `/` 390 светлая и 1440 тёмная, `/en` 390 тёмная, `/ru` 1440 светлая — `html[lang]` ru/ru/en/ru,
      заголовок новой V1, практики «Настольные игры», «Чтение», «Велоспорт», «Медитация» (EN — Board Gaming, Reading,
      Cycling, Meditation), ошибок страницы 0; кадры `test-results/prod-0925-walk-*.png` просмотрены;
    · бой, вернувшийся человек (маркер `ndim-session`): корень уводит в `/profile`.
- **Найдено:** дефектов продукта ноль; один сбой двери выката и одно наблюдение:
  1. Шаг двери «заголовки кеширования» упал сразу после выпуска версии, не напечатав ни одной проверки; повтор через
     ≈1 минуту — 12/0. Вероятная причина — адрес в первые секунды после выпуска; причина не наблюдалась. Следствие для
     двери: она остановилась ДО смоука под сессией — выкат стоял в бою непроверенным, пока шаги не прогнаны руками.
     Лечение (повтор проверки заголовков с паузой либо ожидание, пока адрес отдаст новую сборку) — задачей, не здесь.
  2. Наблюдение, не дефект этой работы: портреты героя проявляются только после оживления страницы — сразу после
     оживления прозрачность 0, через 2,5 с — 1 (замер `getComputedStyle`); на первом кадре без паузы они бледные. Было так
     и в сборке `b042058`.
- **Следы:** стейдж — две гостевые учётки заведены и удалены своими токенами (оценки, точка, учётка — 200), документы
  Связей гостей остались до уборки сервера синхронизации; счётчики воронки стейджа +1 от `/ru`; события `env = stage` в
  PostHog. Бой — смоук под сессией завёл и удалил 2 анонимные учётки («следа в бою не осталось»); публичный смоук и
  проход меткой прибора в метрику людей не попадают. Дверь пересняла числа витрины (`landing-metric.ts`: людей 95,
  оценок 4 077, Связей 2 368 на 2026-09-25). Кадры — вне git.
- **Вердикт:** **pass** — новая главная в бою работает на трёх адресах, ведёт гостя в NDim ID, считает приход корня только
  в PostHog; все шаги двери пройдены (один — повтором руками). Не проверено: встроенный браузер Instagram, телефон и
  аккаунт владельца, отправка `landing_view` с боевого корня живым человеком (проход меткой её не шлёт по построению).

РЕАЛЬНЫЙ МИР: накоплено — у вошедших людей маркер `ndim-session`: проверено в бою (маркер → корень уводит в
`/profile`), смоук под сессией вошёл настоящим входом; данные и техника — каталог и люди боя, проход только читающий
(меткой прибора), телефон владельца — не проверено, ждёт его взгляда; путь — ролик ведёт на `ndimspace.app`: проверено
в бою тем же адресом `/`.
