# Отчёт по тестированию — «Начать заново» отпускает шаг `guest_start`: новый гость в той же вкладке засчитан

> **Создан:** 2026-09-25 21:57 +03:00 (dev-1, очередь Менеджера `ndim-61`, пункт 2: «выбираю А: restartAsGuest освобождает
> шаг guest_start (одна строка) + юнит + прогон ЕВ-08н») · **Родитель:** `bugs/NEW_restart_guest_start_swallowed_in_same_tab.md`
> (S3) · набор `qa/suites/analytics.md` §12 (ЕВ-08, ЕВ-08н) · отчёт находки `qa/reports/2026-09-25_guest-entry-restart.md` ·
> **Статус:** ✅ pass · **Исходящее:** —

## Карточка прогона

- **Работа:** лечение находки. `track()` занимает шаг на визит вкладки (`claimStep`, `sessionStorage`), а «Начать
  заново» рождает нового гостя той же вкладкой. Во вкладке, прожившей 7+ суток, `guest_start` нового гостя
  (`entry: 'restart'`) молча съедался. Лечение А: `releaseStep(step)` в `src/lib/data/funnel.ts`; `restartAsGuest()` зовёт
  `releaseStep('guest_start')` после `signOutUser()` и до ухода на `/profile?guest=restart`. «Политика» перечитана
  (№082 В3): п. 2.6 говорит о PostHog в общем, состав данных не меняется — то же событие, те же свойства, тот же
  получатель. Правка текста не нужна.
- **Контур:** свежая локальная сборка, отданная браузеру ПОД ИМЕНЕМ СТЕЙДЖА `https://ndim-stage.web.app` перехватом
  маршрута (превью :4183, не 4173). Продукт работал с настоящим Firebase стейджа (`ndim-stage`, база `ndim-db-stage`) и
  контуром аналитики `stage`. Всё к `*.posthog.com` перехвачено, наружу не ушло ничего. Истёкший гость воспроизведён,
  как в отчёте находки: документ удалён токеном самого гостя, часы браузера +8 суток. Выкат не делался.
- **Прогонов:** 2026-09-25 +03:00, все из корня рабочего места `D:\work\ai_sandbox\ndim-team\ndim_dev1`:
  · 21:48:57 — мутанты юнитов `node <скретчпад сессии>/mut-release.mjs` (М1: из `restartAsGuest` снят `releaseStep` ·
    М2: тело `releaseStep` пустое; адресаты названы в скрипте до прогона);
  · 21:52:01–21:53:15 — `npm run build`, метка «385e376 · сборка 2173» (дерево чистое, коммит лечения);
  · 21:53:26 — `npx vite preview --port 4183 --strictPort`, ответ 200;
  · **21:53:26–21:54:18 — `node qa/reports/2026-09-25_guest-entry-restart.driver.mjs`, вывод «Голова: 385e376 · дерево
    чистое» → ЕВ-08 12 проверок, ЕВ-08н 12 проверок, уборка 1 — провалов 0**;
  · ворота — строка «Гигиена» ниже.
  Сравнение «до»: тот же драйвер на `14a2383` (без лечения) 21:35:58–21:37:06 — в той же вкладке после касания 0
  событий (отчёт находки).
- **Проверок:** 25 из 25, провалено 0.
  - Гигиена: юниты `node --test src/lib/data/funnel.test.ts` 28/28 (новых 4: отпущенный шаг занимается снова · соседний
    шаг не трогается · недоступное хранилище не роняет · проводка «после выхода и до ухода на дверь»); мутанты М1 —
    красные «проводка…» и группа «Отпущенный шаг», М2 — красные «отпущенный guest_start занимается снова» и группа; файлы
    восстановлены побайтово в обоих. Ворота 21:56:23–21:57:25 (`npm run -s <имя>` по очереди): `test:tools` 860/860,
    `guards` 22/22 «ВОРОТА ЧИСТЫ», `typecheck` 0, `check` 0 ошибок (613 файлов), `questions` и `kaif:check` зелёные,
    `npm test` 553/562 — те же 9 красных выгрузки каталога вне git (5125 против 5153), построчно.
  - Функциональный прогон: контур выше, вывод драйвера прочитан построчно, кадры `test-results/guest-entry/`
    прочитаны глазами:
    · **ЕВ-08н** pass (кейс лечения) — гость родился и истёк в той же вкладке; предусловие: шаг `guest_start` прежнего
      гостя занят ДО касания (`sessionStorage` → `"1"`, до касания ушло `guest_start:direct`); «Гостевая сессия истекла» →
      «Начать заново» → новый гость (uid `CZydh0…` против `TSb0jK…`), адрес без `guest=`; ровно одно событие `guest_start`
      (отправок 1): `entry: "restart"`, `env: "stage"`, ключи только `entry, env`, ни одного `@`. Кадр
      `ЕВ-08н-after-restart.png` — профиль нового гостя с карточкой «Сейчас Вы гость…».
    · **ЕВ-08** pass (регресс, новая вкладка) — шаг до касания не занят (`null`); после касания одно событие, `entry:
      "restart"`, `env: "stage"`.
- **Найдено:** дефектов продукта — 0 новых. Свой: в TWINS документа бага я сначала назвал места с `signOutUser()` по
  памяти, до грепа. Греп нашёл ещё `leave()` — близнец по форме, но это тот же визит, лечение не нужно; число мест
  поправлено тоже по грепу: 5, а не 6.
- **Следы:** на стейдже — 4 гостевые учётки прогона удалены своим idToken («заведено 4, удалено 4»); корни `users/{uid}`
  всех четырёх гостей удалены их токенами (`DELETE 200 · GET 404`). В PostHog не ушло ничего. Кадры —
  `test-results/guest-entry/` (вне git). Превью :4183 погашено своим процессом (PID 48408 — `vite preview` из
  `ndim_dev1\node_modules`), порт свободен.
- **Вердикт:** **pass** — истёкший гость, вернувшийся в той же вкладке, засчитан `guest_start` с `entry: 'restart'`; в новой
  вкладке — как прежде. Повтор шага без выхода из сессии по-прежнему молчит (юниты «Один визит — один шаг»). Годится к
  суду и мержу.

## Решения, принятые без владельца

- `[AI]` по выбору Менеджера (А) — отпускается ровно `guest_start` и только в «Начать заново». `leave()` («Выйти» гостя)
  не трогается: новый гость той же вкладкой там — тот же визит тех же минут.

## Границы (GAP)

- **Настоящая вкладка, прожившая 7+ суток** (телефон, восстановленная сессия браузера) — не воспроизводилась: та же
  вкладка с часами +8 суток.
- **Своя воронка** `space/funnel/days` — прогон на Firebase стейджа, счётчик не сверялся (ряд PostHog перехвачен и
  прочитан; +1 своего счётчика за рождение гостя — тот же путь `track()`, что у ЕВ-01…ЕВ-08).
- **РЕАЛЬНЫЙ МИР:** не проверено — бой не трогался; ряд `restart` в PostHog после выката — слой С.
