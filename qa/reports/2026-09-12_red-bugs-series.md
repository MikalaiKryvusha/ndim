# Отчёт по тестированию — серия починок красных багов (слово владельца 2026-09-12)

> **Создан:** 2026-09-12 10:07 +03:00 · **Родитель:** поручение владельца в чате 2026-09-12
> («*Чиним последовательно 47 красных багов… меняешь код · собираешь, запускаешь · тестируешь ·
> чинишь код…*») · `BUG_FIXING_FRAMEWORK.md` · **Статус:** 🔧 В РАБОТЕ — один документ на серию,
> раздел на каждый закрытый баг; карточка прогона копит числа по мере серии ·
> **Исходящее:** —

## Карточка прогона

- **Работа:** серия починок по пулу из 47 документов `bugs/` со статусом 🔴 (сортировка
  2026-09-12: 13 чинятся автономно · 17 уже вылечены в коде и ждут сверки с тегом DONE · 12 ждут
  владельца · 5 отложены каноном). Цикл по каждому: код → сборка/запуск → тест → починка → тест.
  Разделы ниже — по одному на баг, в порядке исполнения.
- **Контур:** стенд (локальные приборы и юниты; временный git-репозиторий для разрушительных
  операций; живые рабочие места ролей в `ndim-team/` — только безвредные ветви). Сборка `build/`
  от `34020ae` + правки серии, стенд эмуляторов поднимается там, где это назовёт раздел бага.
- **Прогонов:** базовая линия 2026-09-12 09:4x — `npm test` 517/517 · `npm run guards` 22/22.
  Далее — по разделам багов (команды и метки времени внутри).
- **Проверок:** копится по разделам. На 10:07 — bugs/168: юнит 5/5 · самотест 14/14 · мутация
  К4 (2 красных адресно, возврат 5/5) · живой путь 2/2 · страж импорта чист. Провалов продукта: 0.
- **Найдено:** ноль новых дефектов продукта на 10:07 (сортировка нашла 17 документов с отставшей
  строкой статуса — это дефект документов, лечится сверкой и тегом DONE в этой же серии).
- **Следы:** временные репозитории в `%TEMP%\ndim-reset-*` (удаляются юнитом сами) · ветка
  `ndim_qa` переставлена на `main` (`34020ae`, влитая, работа не потеряна) · страховочная ветка
  `ndim_dev3-safety-20260912` ставилась на время живого отказа и снята · `ndim_dev3` не тронута.
- **Вердикт:** bugs/168 годится к закрытию: К1–К4 доказаны приборами, живой путь пройден,
  разрушительная ветвь (`--force`) живьём сознательно не гонялась (граница названа в документе бага).

## 1. bugs/168 — `reset` рабочего места молча уничтожает невлитую работу

| Что | Команда | Когда | Итог |
|---|---|---|---|
| Самотест прибора | `node tools/team-workplaces.mjs --selftest` | 10:0x | 14/14 (+4 случая reset) |
| Юнит К1–К3 + грязное дерево | `node --test tools/team-workplaces.test.mjs` | 10:0x | 5/5 |
| Мутация К4 | `sed` снял `if (ahead > 0 && !force)` → прогон → возврат | 10:0x | на мутанте 2 красных адресно (К1 + чистая функция), К2/К3 зелёные; после возврата 5/5, следов мутации 0 |
| Живой К1 | `node tools/team-workplaces.mjs reset dev-3` | 10:07 | код 1, 3 заголовка, голова `09203c9` цела |
| Живой К2 | `node tools/team-workplaces.mjs reset qa` | 10:07 | код 0, `ndim_qa == main == 34020ae` |
| Страж импорта | `node tools/verify-import-safety.mjs` | 10:0x | чисто |

Найдено: дефектов продукта 0. Следы: см. карточку. Документ: `bugs/168_DONE_reset_molcha_unichtozhaet_nesmerzhennoe.md`.

## 2. Пакет «лечение уже в коде» — 17 документов сверены прогонами, 15 закрыты, 2 переведены в 🟡

Сортировка (четыре читающих агента, улики с адресами `файл:строка`) назвала 17 документов, чьё
лечение лежит в коде при красной строке статуса (класс `bugs/177`). Каждый сверен ПРОГОНОМ, не
чтением; улики и команды — в разделе «✅ СТАТУС: DONE» каждого документа.

| Баг | Прогон 2026-09-12 (10:0x–10:14) | Итог |
|---|---|---|
| 170 витрина против базы | `verify-copy-live.mjs --selftest` · `--live` | 14/0 · витрина 5148 = база 5148, снимок 1 дн. |
| 184 страж словаря слеп к h1 | `verify-product-vocabulary.mjs` по `build/` | проверки h1 ✅; **найден соседний класс** (список соседей) — раздел 3 |
| 191 самотест доски | `node --test tools/team-status.test.mjs` · `regexp-escaping.test.mjs` | 11/0 · 2/0 |
| 193 страж статуса слеп | `node tools/verify-status-outlived.mjs` | 329 документов, нарушений 0 |
| 196 смоук на пустом body | греп: `document.body.innerText` 0 · `bodyText(page)` 5 | код на месте |
| 197 дверь не читает .env | `debugToken()` из чистой оболочки | `ЕСТЬ` |
| 204 ключ дня в UTC | `node --test src/lib/data/funnel.test.ts` | 19/0 |
| 214 `__data.json` не исчезли | `find build … __data.json` | 10 288 файлов, документы называют число с датой |
| 217 разрешение живёт в приборе | `node --test tools/yandex-recrawl.test.mjs` | 43/0 |
| 218 `\s` в шаблонной строке | `node --test tools/regexp-escaping.test.mjs` | 2/0 |
| NEW guest_ttl текст врёт | `node --test src/routes/profile/guest-facts.test.ts` | 5/0 |
| NEW пачка вычитки умирает | `node --test tools/review-batch.test.mjs` (живой сервер) | 1/0 |
| NEW съёмщик видит 13 из 23 | `node tools/census-mockup-sets.mjs` | долг = храповик (9) |
| 200 дверь карточки в стену | `verify-probe-mark.mjs` + код `DOOR_HREF`, `track('door_click')` | ✅ 4 пары; в бою с 2026-08-29 |
| 202 воронка слепа | `verify-probe-mark.mjs` + код счётчиков/правил/экрана | ✅; `plans/76` все шаги |
| 189 приёмка не воспроизводима | `node --test tools/measure-batch-residual.test.mjs` | 10/0 — **🟡**: `jsonl` и добор кэша не исполнены |
| 106 служебный документ в счёте | `verify-copy-live.mjs --live` → `dims` 5148/5148 | **🟡**: шаг 3 (история снимков) пишет в бой — слово владельца |

Найдено: 1 дефект прибора (страж словаря судит названия соседей на карточке — раздел 3);
дефектов продукта 0. Следы: `--live` читал бой только чтением; ничего не записано.
Страж состояния bugs/: `node tools/verify-bug-doc-state.mjs` → рассогласований нет.

## 3. Страж словаря продукта слеп к списку соседей на карточке (найден сверкой bugs/184)

Тяжесть S3 → без документа бага, лечение в самом приборе. `tools/verify-product-vocabulary.mjs`
красил `en/dimension/the-astronaut-farmer-*.html` на слове «axis» — оно в блоке «Похожие по
каталогу», в названии соседнего фильма «American Traitor: The Trial of Axis Sally» (данные
владельца, тот же класс, что h1 в bugs/184). Лечение: вырез `<ul>` внутри `nav.similar` только у
карточки; заголовок `h2` блока (наш текст) под судом остаётся. Две проверки в обе стороны.

| Что | Команда | Итог |
|---|---|---|
| После починки | `node tools/verify-product-vocabulary.mjs` (сборка от `34020ae`) | 14/0, страниц с нарушениями 0 |
| Мутация (вырез снят, через резервную копию файла) | тот же прогон на мутанте | **2 красных**: новая проверка списка соседей + живая карточка |
| Возврат из копии | тот же прогон | 14/0, следов мутанта 0 |

Найдено попутно: **промах агента** — первая постановка мутации завершалась `git checkout -- <файл>`
и стёрла незакоммиченную починку (класс `EXP-0190`, второй раз в проекте); восстановлена из
транскрипта дословно, мутация переставлена на резервную копию. Дефектов продукта 0.

## 4. bugs/190 — раздутый знаменатель журнал называл улучшением

| Что | Команда | Когда | Итог |
|---|---|---|---|
| Классификация образцов ДО теста | `пропорцияПравки` на 4 английских парах | 10:1x | дробление 1/2→1/3 при 21→21 словах; прирост 21→27; слияние доля↑; мета 1→0 |
| Юнит ярлыка | `node --test tools/rewrite-journal-label.test.mjs` | 10:2x | 5/5 |
| Самотест прибора (дверь не тронута) | `node tools/rewrite-catalog-descriptions.mjs --selftest` | 10:2x | 17/17 |
| К4 мутация («лучше» снова из доли, резервная копия) | тот же юнит на мутанте | 10:23 | **2 красных адресно** (дробление · прирост), 3 зелёных; возврат 5/5 |

Найдено: дефектов продукта 0. Следы: нет. Документ: `bugs/190_DONE_…`.

## 5. bugs/192 — страж границы слова слеп к `\b` за классом с квантификатором

| Что | Команда | Когда | Итог |
|---|---|---|---|
| Самотест после починки | `node tools/verify-cyrillic-word-boundary.mjs --selftest` | 10:1x | 25/25 (+7: 4 красных случая, 3 контроля) |
| Живой корпус (страж в воротах) | `node tools/verify-cyrillic-word-boundary.mjs` | 10:1x | нарушений 0 (образец `bugs/193` вылечен ранее) |
| К4 мутация (признак «вплотную», резервная копия) | самотест на мутанте | 10:2x | **3 из 25 красные адресно**; возврат 25/25 |

Найдено: дефектов продукта 0. Следы: нет. Документ: `bugs/192_DONE_…`.

## 6. bugs/NEW — очередь вычитки копит уже отвеченные интервью

| Что | Команда | Когда | Итог |
|---|---|---|---|
| Юнит + два живых прогона CLI на подложных документах | `node --test tools/review-queue-prune.test.mjs` | 10:3x | 3/3 |
| Регрессии контура | `review-core.test` · `review.mjs --selftest` · `review-batch.test` (живой сервер) | 10:3x | 3/3 · чист · 1/1 |
| К4 мутация (очистка снята) — первая постановка | тот же юнит | 10:3x | **тест ВИС** (>120 с): мутант держит отвеченный «живым», пачка ждёт владельца — снят вручную, файл возвращён из копии, сирота сервера убита |
| Тест получил таймаут 15 с; К4 повторно | тот же юнит на мутанте | 10:4x | **2 из 3 красные адресно**; возврат 3/3; сирот 0 |

Найдено: дефект ТЕСТА (зависание вместо красного) — вылечен таймаутом в том же коммите; дефектов
продукта 0. Следы: `test-results/owner-reviews/selftest-queue-prune/` (убирается тестом). Документ:
`bugs/NEW_DONE_review_queue_keeps_answered.md`.

## 7. bugs/206 — число в отчёте о немашинной работе не сводится с диффом

| Что | Команда | Когда | Итог |
|---|---|---|---|
| Юнит сводки (3 случая) | `node --test tools/owner-classes-summary.test.mjs` | 10:3x | 3/3 |
| Регрессия прибора | `node --test tools/check-owner-classes.test.mjs` | 10:3x | 49/49 |
| Живой случай бага: партия 04 `c071766` → `8a983a5` | `check-owner-classes.mjs --summary <ДО> <ПОСЛЕ> --today 2026-08-28` | 10:3x | карточек 20/20 · находок 56 → 24 · отказов 7 · пожеланий 17 · чистых 15; код 0 |
| К4 мутация («где» всегда «обе», резервная копия) | юнит на мутанте | 10:4x | **2 из 3 красные адресно**; возврат 3/3 |

Найдено: дефектов продукта 0. Следы: временные JSON в `%TEMP%` (сняты из git, вне репозитория).
Документ: `bugs/206_DONE_…`.

## 8. bugs/207 — зонд щита не исполняет собственный критерий (стенд)

Контур: **стенд** `npm run stand`, слот главной копии (`localhost:5173`, эмуляторы 8181/9099),
сборка от `b411761` + правки серии. ⚠️ Первый запуск упал не по делу: Git Bash подменил аргумент
`/ru` путём `C:/Program Files/Git/ru` (капкан MSYS из досье окружения) — прогон повторён с
`MSYS_NO_PATHCONV=1`.

| Что | Команда | Когда | Итог |
|---|---|---|---|
| А+Б: настоящая сессия под замедлением | `probe-bug40-live.mjs http://localhost:5173 /ru --real-session --slow` | 10:3x | щит 38 · ЛЕНДИНГ 0 · оба раунда; код 0 |
| В + контроль: гость | `probe-bug40-live.mjs http://localhost:5173 /ru --guest` | 10:3x | лендинг 40 · щит 0 · «✅ КОНТРОЛЬ ПРИБОРА»; код 0 |
| Уборка | `taskkill /T` от корня стенда | 10:3x | сирот 0, Java 0, боевой PM2 не задет (`read-server-pulse --contour prod` ЖИВ) |

Найдено: дефектов продукта 0. Следы: кадры `test-results/bug40-live/` (вне git); стенд погашен.
Документ: `bugs/207_DONE_…`.

## 9. bugs/208 — строка реестра приборов стареет внутри своей же ветки

| Что | Команда | Когда | Итог |
|---|---|---|---|
| Паспорт прибора | `node tools/measure-grammar-agreement.mjs --passport` | 10:5x | 52 лемм → 253 словоформ · мутаций 7 · N 12 файлов / 206 текстов, Н3 2 пары на 2 карточках · K 50, Н3 12 на 11 · R 536 |
| Строка канона переписана | скрипт `rewrite-guide-line.mjs` (числа → команда, замер датирован) | 10:3x | строка 2054 `AGENT_GUIDE.md` |
| Страж пары «паспорт ↔ строка» | `node --test tools/grammar-passport.test.mjs` | 10:3x | 2/2 |
| К4 мутация («47 лемм → 228 словоформ» возвращены в строку) | страж на мутанте | 10:3x | **1 из 2 красный адресно**; возврат 2/2, следов 0 |

Найдено: дефектов продукта 0. Попутно замечено (не судится здесь): паспорт показывает на корпусе
партий подозрения Н2 — 2 («вселенная студии» ×2) и Н1+Н2 — 1 («британского премьера»); на 08-28
их было 1 (фраза владельца). Похоже на омонимию («премьера»/«премьер», «студии»), читать глазами —
кандидат в `EXPERIENCE`/самотест прибора, вне этой серии. Документ: `bugs/208_DONE_…`.

## 10. bugs/154 — удаление аккаунта оставляет ответы человека в парах теста (эмулятор)

Контур: **стенд** — эмулятор Firestore через `firebase emulators:exec`, свой проект
`demo-ndim-pairhalf`; сборка не участвует. ⚠️ В бою правка сервера синхронизации появится
только с его выкатом (двери нет — `bugs/NEW_sync_server_has_no_deploy_door`).

| Что | Команда | Когда | Итог |
|---|---|---|---|
| Набор сервера целиком с новым файлом | `npm run test:sync` | 10:4x | **118/118** (`pair_half_anonymize.test.mjs` — 6 случаев зелёные) |
| К4 мутация (обезличивание отцеплено, резервная копия) | `npx firebase emulators:exec --only firestore --project demo-ndim-pairhalf "node --test sync-server/pair_half_anonymize.test.mjs"` | 10:5x | **2 из 6 красные адресно** (умер создатель · умер присоединившийся), 4 зелёные; возврат — следов мутанта 0 |
| Клиент при `null`-половине | чтение `test-pair.ts:52`, `test/[slug]/+page.svelte:286–302` | 10:4x | `sanitizeAnswers(null)` → пустая Map, сравнения uid строгие; падений нет |

Найдено: дефектов продукта 0. Следы: эмуляторы погашены самим `emulators:exec`. Документ:
`bugs/154_DONE_…`.


## 11. РУЧНОЙ ФУНКЦИОНАЛЬНЫЙ ПРОГОН — bugs/200 (дверь карточки) и bugs/202 (воронка) в БОЮ

> Слово владельца 2026-09-12 (`TESTING_FRAMEWORK.md` → «„ТЕСТ“ у владельца»): протестировано =
> руками, живой браузер, глазами. Разделы 1–10 выше — гигиена (юниты, самотесты, мутации), НЕ тест.
> Этот раздел — первый ручной прогон серии; строки «гигиена» и «ручной прогон» ниже раздельны.

**Контур:** бой `https://ndimspace.app`, сборка боя на 11:22 (`/build-stamp.json` не снимался —
дверь в бою с 2026-09-12 ≈01:20 по эстафете). Браузер: Playwright Chromium **с окном**, без метки
`ndim-probe` — то есть для воронки я человек из поиска. Скрипты: `test-results/manual/manual-door-prod.mjs`,
`manual-dark.mjs`, `probe-403*.mjs` (вне git). Кадры: `test-results/manual/2026-09-12_door/*.png`.

**Гигиена (до серии):** `verify-probe-mark.mjs` 4 пары ✅ · код `DOOR_HREF`, `track('door_click')` на месте.

**Ручной прогон — путь незнакомца из поиска, 4 сессии (1280 светлая · 390 тёмная-системная ·
1280 тёмная продукта · 390 тёмная продукта), 11:22–11:26:**

| Шаг | Что сделал руками | Что увидел глазами | Итог |
|---|---|---|---|
| 1 | Открыл карточку `/ru/dimension/the-queen-s-gambit-01tckmb5` | Блок «NDim Space Rating…», 11 звёзд 0–10, запасная дверь скрыта при живом JS, `href=/profile?guest=1` | ✅ |
| 2 | Коснулся звезды 8 (в тёмной — 9) | Звезда зажглась, подпись «Ваша оценка: 8 из 10», панель «Ваша оценка засчитана!» с картой узлов раскрылась | ✅ обе темы, обе ширины |
| 3 | Нажал большую кнопку панели | Адрес `/profile?guest=1`; экран «Профиль» гостя: «ВЫ ОЦЕНИЛИ · Телесериал · Ход королевы (2020) · 8 из 10», карточка гостя с «Сохранить мои результаты» | ✅ стены входа НЕТ ни разу |
| 4 | Прочитал сессию из IndexedDB | `isAnonymous=true`, uid `Vu2bSzct…` (1280) и `NTA2iyRe…` (390) | ✅ гость родился касанием |
| 5 | Той же вкладкой снова открыл `/profile` | Гость на месте, стены нет | ✅ сессия переживает заход |
| 6 | Новой вкладкой без сессии открыл `/profile` (1280 тёмная · 390 светлая · 390 тёмная) | Стена «Войдите в Пространство NDim Space»: Google, почта, «Смотреть гостем без регистрации» | ✅ стена там, где ей место |
| 7 | Воронка ДО/ПОСЛЕ, `read-funnel-days.mjs --contour prod` 11:21 → 11:27 | ДО: колонки `door_click` в ряду боя НЕТ. ПОСЛЕ: `door_click` **5** · `guest_start` **5** · `signin_wall_view` **7** — ровно мои 5 касаний, 5 гостей, 7 открытий стены (4 сессии + 4 зонда 403) | ✅ считает, сходится до единицы, ключ дня локальный |
| 8 | Консоль | 2 ошибки на сессию: `403` `exchangeRecaptchaV3Token` → тело `App attestation failed. PERMISSION_DENIED` — во всех трёх браузерах (Chromium, Chrome, Edge) | ⚠️ см. «Найдено» |
| 9 | Тёмная тема | `colorScheme: dark` контекста продукт НЕ слушает (дефолт «Бумага» по решению владельца); включается ключом `ndim-theme` — прогон повторён им, `data-theme=dark` подтверждён | ✅ (капкан прибора, не продукта) |

**Найдено:**
1. 🔑 **Первый `door_click` в бою за две недели — мой.** Дверь в бою с 08-29 по эстафете (по кадрам
   и коду — точно с ≈01:20 сегодня), а колонки не было вовсе до моего касания: счётчик исправен,
   **людей у двери не было**. Это факт о притоке, не о дефекте (`plans/74` Ф1 Ш1 закрыт живым касанием).
2. ⚠️ **App Check отвечает 403 «App attestation failed» живому браузеру без пропуска прибора.**
   `bugs/169` закрыт пропуском для ПРИБОРОВ; консоль Firebase тогда показала «1–2 % проверенных
   запросов». Из автоматизированного браузера низкий балл reCAPTCHA v3 ожидаем, и отличить «403 у
   ботов» от «403 у людей» агент не может — нужен взгляд владельца в страницу App Check консоли
   Firebase (доля проверенных запросов). Продукт при этом работает: App Check в наблюдении, запись
   оценки и воронки прошла. Документ бага не заведён до его взгляда — иначе это гадание.
3. Дефектов продукта на пути: **0**.

**Следы:** 5 гостевых аккаунтов в бою (uid `Vu2bSzctbkSiD9Cg8l7iG1cX2Tc2`, `NTA2iyReWVcpTbhilmGjwz6Obqg1`,
ещё 3 не сняты — тёмный прогон и зонд 403), у каждого одна оценка «Ход королевы» (8 или 9);
NDSR карточки сдвинут на 5 голосов; счётчики воронки за 2026-09-12 содержат мои 5/5/7 —
**при чтении притока за сегодня вычесть.** Уборка гостей в бою не выкачена (`plans/63`), TTL 7 дней —
текст экрана сам это обещает.

**Вердикт:** bugs/200 и bugs/202 — **ПРОТЕСТИРОВАНЫ РУКАМИ в бою**, путь незнакомца работает от
поиска до гостя без стены; воронка считает все три шага. Открытый вопрос владельцу — доля
проверенных App Check у живых людей.


## 12. РУЧНОЙ ФУНКЦИОНАЛЬНЫЙ ПРОГОН — приборы и стражи (11:30–11:45) + смоук под сессией по бою

Правило то же: прибор считается протестированным, когда он запущен руками на живом материале
И когда воспроизведённый руками исходный симптом бага пойман на моих глазах. Гигиена (юниты,
самотесты, мутации) — в разделах 1–10, здесь её нет.

| Баг | Что сделал руками | Что увидел | Итог |
|---|---|---|---|
| 192 страж границы слова | Прогнал по дереву; положил `tools/_manual192_probe.mjs` с образцом `[аоы]?`; прогнал; убрал | Дерево чисто → на подложном файле `❌ tools/_manual192_probe.mjs:2`, «НАРУШЕНИЙ 1» → после уборки чисто | ✅ |
| 193 страж «не начат при живой работе» | Первая проба (документ без коммитов) прошла зелёной — по замыслу прибора «живая работа» = коммит с кодом, ссылающийся на документ. Вторая: `bugs/999…` «🔲 написан, не начат.» + коммит `fix(bugs/999)` с `tools/_probe999.mjs`; прогнал; `git reset --hard HEAD~1` при чистом дереве | `❌ bugs/999_manual_probe_193.md · 007e5cf fix(bugs/999)…`, код 1; после отката HEAD `a2ee559`, следов 0 | ✅ |
| 184 страж словаря в h1 | Прогнал по сборке; внедрил «Ось и юзер:» в `h1` страницы «О проекте» (копия → правка → возврат) | Чисто 14/0 → `❌ ru\menubout.html — ось/оси — говорим «измерение»`, код 1 → после возврата 15 ✅ | ✅ |
| 217 разрешение владельца | `node tools/yandex-recrawl.mjs --apply` без `--auth-owner` | `🔴 ОТКАЗ … Первоисточник: bugs/203 § «Разрешение владельца»`, код 1; цитата живёт в `bugs/203`, не в приборе | ✅ |
| 218 `\s` в шаблонной строке | Живой вызов `пунктуацияПояснения` на примере из кода: «Пётр , известный инженер , приехал» / без пробелов / без закрывающей запятой | 1 подозрение · 1 подозрение · 0 — пробел перед запятой больше не слепит правило | ✅ |
| 191 самотест доски | `node tools/team-status.mjs --selftest` | 35/35, случай миграции формы «слот 0…2» присутствует и зелёный | ✅ |
| 208 паспорт прибора | `measure-grammar-agreement.mjs --passport`; строку канона прочитал глазами | Паспорт: 52 лемм → 253 словоформ, мутаций 7, корпуса N/K/R с числами; строка канона называет КОМАНДУ и дату замера, чисел прибора не несёт | ✅ |
| 214 `__data.json` | `curl` боевой карточки `…/__data.json` | `200`, 4100 байт — файлы отдаются, как и записано в документе («существуют, число с датой») | ✅ факт совпал с документом |
| 204 ключ дня | Прочитал код: `OWNER_TIMEZONE = 'Europe/Moscow'`; воронка сегодня легла в `2026-09-12` при касаниях 11:22 МСК | Ключ по часам владельца | ✅ (граница полуночи руками не проверялась — не то время) |
| NEW перепись макетов | `node tools/census-mockup-sets.mjs` | 4 набора без сцены/ширины названы поимённо; долг 9 = храповик | ✅ |
| NEW guest_ttl текст | Кадры раздела 11 и смоука: карточка гостя в бою | «Гостевой аккаунт живёт 7 дней от дня создания. Гостевой аккаунт, не ставший полноценным за эти 7 дней, удаляется вместе со всеми оценками и связями» — правило владельца, без «если не вернётесь» | ✅ |
| 196 + 197 смоук под сессией | `node tools/verify-prod-signed-in.mjs` по бою из чистой оболочки, 11:30 | 24/24, обе ширины и темы, пропуск App Check дочитан из `.env`, `bodyText` на месте, консоль чиста | ✅ |
| 207 зонд щита | Кадры `test-results/bug40-live/cold-10.png`, `warm-10.png` открыты глазами | Лендинг отрисован, щита нет | ✅ |
| 206 / 168 / 170 | Ручные прогоны выполнены в разделах 1, 2, 7 (живые команды на живых данных) | — | ✅ (учтены ранее) |

**Наблюдение по дороге (не дефект продукта):** кадр смоука «Связи» 1440-dark снят в момент
въезда баннера гостя и спиннера «Загрузка»; ручной заход тем же гостем показал экран целиком
через 1,5 с (`test-results/manual/2026-09-12_door/1440-dark-relations-*.png`, 390 тоже). Смоук
судит текстом, а кадр снимает раньше — читать его кадры как «экран сломан» нельзя.

**Найдено:** дефектов продукта 0 · дефектов приборов 0. **Следы:** смоук завёл и убрал своих
гостей (метка прибора, уборка проверяется им самим); мои два гостя «Связи» — с меткой `ndim-probe`,
в воронку не попали; пробный коммит стража 193 откачен, дерево чистое.

**Ещё не протестированы руками:** 190 (ярлык журнала на настоящей партии), 154 (удаление аккаунта
при живой паре — стенд с эмуляторами), очередь и пачка вычитки (настоящая очередь владельца).

## Итог серии на закрытие чата (2026-09-12 ≈11:00)

Красных документов было **47** → осталось **22**: закрыто 25 (7 починок кодом: 168 · 190 · 192 ·
206 · 207 · 208 · 154 · очередь вычитки; 15 сверкой «лечение уже в коде»; 1 попутный дефект стража
словаря без документа), 2 переведены в 🟡 частично (189, 106). Из 22 оставшихся: **12 ждут
владельца** (127 · 152 · 158 · 16 · 186 · 19 · 203 · 220 · 28 · 31 · 45 · 85), **5 отложены
каноном** (146 · 221 · 229 · 230 · 62), **5 автономных не начаты** (180 · NEW mockups_quote ·
NEW sync_server_no_deploy_door · 44 · 205). Дефектов продукта серия не нашла; дефектов приборов и
документов — 25, все закрыты прогонами и мутациями.
