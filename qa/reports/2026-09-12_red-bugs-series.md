# Отчёт по тестированию — серия починок красных багов (слово владельца 2026-09-12)

> **Создан:** 2026-09-12 10:07 +03:00 · **Родитель:** поручение владельца в чате 2026-09-12
> («*Чиним последовательно 47 красных багов… меняешь код · собираешь, запускаешь · тестируешь ·
> чинишь код…*») · `BUG_FIXING_FRAMEWORK.md` · **Статус:** 🔧 В РАБОТЕ — один документ на серию,
> раздел на каждый закрытый баг; карточка прогона копит числа по мере серии ·
> **Исходящее:** —

## Карточка прогона

- **Работа:** серия починок по пулу из 47 документов `bugs/` со статусом 🔴 (сортировка
  2026-09-12: 13 чинятся автономно · 17 уже вылечены в коде и ждут сверки с тегом DONE · 12 ждут
  владельца · 5 отложены каноном). Цикл по каждому: код → сборка/запуск → тест → починка → тест.
  Разделы ниже — по одному на баг, в порядке исполнения.
- **Контур:** стенд (локальные приборы и юниты; временный git-репозиторий для разрушительных
  операций; живые рабочие места ролей в `ndim-team/` — только безвредные ветви). Сборка `build/`
  от `34020ae` + правки серии, стенд эмуляторов поднимается там, где это назовёт раздел бага.
- **Прогонов:** базовая линия 2026-09-12 09:4x — `npm test` 517/517 · `npm run guards` 22/22.
  Далее — по разделам багов (команды и метки времени внутри).
- **Проверок:** копится по разделам. На 10:07 — bugs/168: юнит 5/5 · самотест 14/14 · мутация
  К4 (2 красных адресно, возврат 5/5) · живой путь 2/2 · страж импорта чист. Провалов продукта: 0.
- **Найдено:** ноль новых дефектов продукта на 10:07 (сортировка нашла 17 документов с отставшей
  строкой статуса — это дефект документов, лечится сверкой и тегом DONE в этой же серии).
- **Следы:** временные репозитории в `%TEMP%\ndim-reset-*` (удаляются юнитом сами) · ветка
  `ndim_qa` переставлена на `main` (`34020ae`, влитая, работа не потеряна) · страховочная ветка
  `ndim_dev3-safety-20260912` ставилась на время живого отказа и снята · `ndim_dev3` не тронута.
- **Вердикт:** bugs/168 годится к закрытию: К1–К4 доказаны приборами, живой путь пройден,
  разрушительная ветвь (`--force`) живьём сознательно не гонялась (граница названа в документе бага).

## 1. bugs/168 — `reset` рабочего места молча уничтожает невлитую работу

| Что | Команда | Когда | Итог |
|---|---|---|---|
| Самотест прибора | `node tools/team-workplaces.mjs --selftest` | 10:0x | 14/14 (+4 случая reset) |
| Юнит К1–К3 + грязное дерево | `node --test tools/team-workplaces.test.mjs` | 10:0x | 5/5 |
| Мутация К4 | `sed` снял `if (ahead > 0 && !force)` → прогон → возврат | 10:0x | на мутанте 2 красных адресно (К1 + чистая функция), К2/К3 зелёные; после возврата 5/5, следов мутации 0 |
| Живой К1 | `node tools/team-workplaces.mjs reset dev-3` | 10:07 | код 1, 3 заголовка, голова `09203c9` цела |
| Живой К2 | `node tools/team-workplaces.mjs reset qa` | 10:07 | код 0, `ndim_qa == main == 34020ae` |
| Страж импорта | `node tools/verify-import-safety.mjs` | 10:0x | чисто |

Найдено: дефектов продукта 0. Следы: см. карточку. Документ: `bugs/168_DONE_reset_molcha_unichtozhaet_nesmerzhennoe.md`.

## 2. Пакет «лечение уже в коде» — 17 документов сверены прогонами, 15 закрыты, 2 переведены в 🟡

Сортировка (четыре читающих агента, улики с адресами `файл:строка`) назвала 17 документов, чьё
лечение лежит в коде при красной строке статуса (класс `bugs/177`). Каждый сверен ПРОГОНОМ, не
чтением; улики и команды — в разделе «✅ СТАТУС: DONE» каждого документа.

| Баг | Прогон 2026-09-12 (10:0x–10:14) | Итог |
|---|---|---|
| 170 витрина против базы | `verify-copy-live.mjs --selftest` · `--live` | 14/0 · витрина 5148 = база 5148, снимок 1 дн. |
| 184 страж словаря слеп к h1 | `verify-product-vocabulary.mjs` по `build/` | проверки h1 ✅; **найден соседний класс** (список соседей) — раздел 3 |
| 191 самотест доски | `node --test tools/team-status.test.mjs` · `regexp-escaping.test.mjs` | 11/0 · 2/0 |
| 193 страж статуса слеп | `node tools/verify-status-outlived.mjs` | 329 документов, нарушений 0 |
| 196 смоук на пустом body | греп: `document.body.innerText` 0 · `bodyText(page)` 5 | код на месте |
| 197 дверь не читает .env | `debugToken()` из чистой оболочки | `ЕСТЬ` |
| 204 ключ дня в UTC | `node --test src/lib/data/funnel.test.ts` | 19/0 |
| 214 `__data.json` не исчезли | `find build … __data.json` | 10 288 файлов, документы называют число с датой |
| 217 разрешение живёт в приборе | `node --test tools/yandex-recrawl.test.mjs` | 43/0 |
| 218 `\s` в шаблонной строке | `node --test tools/regexp-escaping.test.mjs` | 2/0 |
| NEW guest_ttl текст врёт | `node --test src/routes/profile/guest-facts.test.ts` | 5/0 |
| NEW пачка вычитки умирает | `node --test tools/review-batch.test.mjs` (живой сервер) | 1/0 |
| NEW съёмщик видит 13 из 23 | `node tools/census-mockup-sets.mjs` | долг = храповик (9) |
| 200 дверь карточки в стену | `verify-probe-mark.mjs` + код `DOOR_HREF`, `track('door_click')` | ✅ 4 пары; в бою с 2026-08-29 |
| 202 воронка слепа | `verify-probe-mark.mjs` + код счётчиков/правил/экрана | ✅; `plans/76` все шаги |
| 189 приёмка не воспроизводима | `node --test tools/measure-batch-residual.test.mjs` | 10/0 — **🟡**: `jsonl` и добор кэша не исполнены |
| 106 служебный документ в счёте | `verify-copy-live.mjs --live` → `dims` 5148/5148 | **🟡**: шаг 3 (история снимков) пишет в бой — слово владельца |

Найдено: 1 дефект прибора (страж словаря судит названия соседей на карточке — раздел 3);
дефектов продукта 0. Следы: `--live` читал бой только чтением; ничего не записано.
Страж состояния bugs/: `node tools/verify-bug-doc-state.mjs` → рассогласований нет.

## 3. Страж словаря продукта слеп к списку соседей на карточке (найден сверкой bugs/184)

Тяжесть S3 → без документа бага, лечение в самом приборе. `tools/verify-product-vocabulary.mjs`
красил `en/dimension/the-astronaut-farmer-*.html` на слове «axis» — оно в блоке «Похожие по
каталогу», в названии соседнего фильма «American Traitor: The Trial of Axis Sally» (данные
владельца, тот же класс, что h1 в bugs/184). Лечение: вырез `<ul>` внутри `nav.similar` только у
карточки; заголовок `h2` блока (наш текст) под судом остаётся. Две проверки в обе стороны.

| Что | Команда | Итог |
|---|---|---|
| После починки | `node tools/verify-product-vocabulary.mjs` (сборка от `34020ae`) | 14/0, страниц с нарушениями 0 |
| Мутация (вырез снят, через резервную копию файла) | тот же прогон на мутанте | **2 красных**: новая проверка списка соседей + живая карточка |
| Возврат из копии | тот же прогон | 14/0, следов мутанта 0 |

Найдено попутно: **промах агента** — первая постановка мутации завершалась `git checkout -- <файл>`
и стёрла незакоммиченную починку (класс `EXP-0190`, второй раз в проекте); восстановлена из
транскрипта дословно, мутация переставлена на резервную копию. Дефектов продукта 0.

## 4. bugs/190 — раздутый знаменатель журнал называл улучшением

| Что | Команда | Когда | Итог |
|---|---|---|---|
| Классификация образцов ДО теста | `пропорцияПравки` на 4 английских парах | 10:1x | дробление 1/2→1/3 при 21→21 словах; прирост 21→27; слияние доля↑; мета 1→0 |
| Юнит ярлыка | `node --test tools/rewrite-journal-label.test.mjs` | 10:2x | 5/5 |
| Самотест прибора (дверь не тронута) | `node tools/rewrite-catalog-descriptions.mjs --selftest` | 10:2x | 17/17 |
| К4 мутация («лучше» снова из доли, резервная копия) | тот же юнит на мутанте | 10:23 | **2 красных адресно** (дробление · прирост), 3 зелёных; возврат 5/5 |

Найдено: дефектов продукта 0. Следы: нет. Документ: `bugs/190_DONE_…`.

## 5. bugs/192 — страж границы слова слеп к `\b` за классом с квантификатором

| Что | Команда | Когда | Итог |
|---|---|---|---|
| Самотест после починки | `node tools/verify-cyrillic-word-boundary.mjs --selftest` | 10:1x | 25/25 (+7: 4 красных случая, 3 контроля) |
| Живой корпус (страж в воротах) | `node tools/verify-cyrillic-word-boundary.mjs` | 10:1x | нарушений 0 (образец `bugs/193` вылечен ранее) |
| К4 мутация (признак «вплотную», резервная копия) | самотест на мутанте | 10:2x | **3 из 25 красные адресно**; возврат 25/25 |

Найдено: дефектов продукта 0. Следы: нет. Документ: `bugs/192_DONE_…`.
