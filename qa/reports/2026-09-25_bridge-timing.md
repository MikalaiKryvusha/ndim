# Отчёт по тестированию — замер моста главной на стейдже: на что уходят три секунды «Смотреть больше»

> **Создан:** 2026-09-25 17:12 +03:00 · **Родитель:** наблюдение 4 отчёта `qa/reports/2026-09-25_new-landing-v1.md`
> («мост 3,1 с») · `plans/106` (Д5) · **Статус:** ✅ замер снят · **Исходящее:** решение об ускорении — постановкой
> (отрезки ниже).

## Карточка прогона

- **Работа:** прежнее число «мост 3,1 с» мерило касание «Смотреть больше» → событие `load` страницы профиля, то есть
  вместе с загрузкой следующей страницы. Этот прогон делит мост на отрезки по сетевым шагам: подгрузка модулей ·
  рождение гостя в Auth · записи Firestore · уход документа `/profile`.
- **Контур:** стейдж `ndim-stage.web.app`, сборка `b042058`; вход человеком (обычный user agent, признаки робота сняты);
  390 px, мобильный контекст.
- **Прогонов:** 1 — 2026-09-25 16:47:48 +03:00:
  `MSYS_NO_PATHCONV=1 node qa/reports/2026-09-25_bridge-timing.stage-driver.mjs --path /ru --stars 3`
  (вывод — `test-results/bridge-timing-run1.json`).
- **Проверок:** 1 замер, прочитан глазами построчно.
  - Гигиена: не применялась — прибор замера, продукт не менялся.
  - Функциональный прогон: стейдж · касание трёх звёзд → «Смотреть больше» · ПРОЧИТАНО по шкале от касания:
    · +0,04…0,12 с — модули маршрута профиля и Firebase (двадцать чанков, все из кэша/сети за 0,1 с);
    · +0,11…0,98 с — `accounts:signUp`, рождение гостя: **0,87 с**;
    · +0,98…1,43 с — `accounts:lookup` сразу следом: **0,45 с** (последовательно, SDK сам);
    · +1,43…1,56 с — открытие каналов Firestore;
    · +1,69…2,94 с — цепочка записей: четыре последовательных запроса канала записи (≈0,11–0,17 с каждый, с паузами
      0,15–0,3 с между), затем три параллельных; всего ≈1,4 с;
    · +3,06 с — уходит запрос документа `/profile` (уход моста), +3,33 с — навигация.
- **Найдено:** дефектов ноль. Наблюдение для решения: мост ≈3,06 с = Auth ≈1,3 с + Firestore ≈1,5 с + модули 0,1 с.
  Цепочка Firestore идёт последовательно: `ensureSpaceExists` читает документ человека и пишет его (`getDoc` +
  `setDoc`), затем первая оценка отдельным пакетом, затем остальные. Кандидаты ускорения — объединить заведение
  профиля и первую оценку в один пакет записи; параллелить остальное. Решение — постановкой, не этим прогоном.
- **Следы:** на стейдже — одна гостевая учётка, драйвер удаляет её токеном (оценки, точка, учётка). ⚠️ Вывод прогона
  записан не целиком: он шёл через `tee` в `head -120`, конвейер оборвался, и строки с кодами уборки в файл не попали —
  уборку прогон не показал. Проверено отдельно в 17:10 командой Admin SDK стейджа (`listUsers`, разовая проба вне git):
  учёток стейджа 9, заведённых после 16:40 — **0 из 9**, то есть учётка удалена. Удаление её оценок и точки не
  наблюдалось (uid в выводе не печатался); `users/{uid}` и возможные остатки подчищает уборка сервера синхронизации.
  События `env = stage` в PostHog (приход, касание, гость).
- **Вердикт:** **pass** как замер — отрезки моста сняты; к бою не относится.
