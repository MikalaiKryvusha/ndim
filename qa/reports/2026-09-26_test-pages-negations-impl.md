# Отчёт по тестированию — строки интервью №102 и №103 на страницах тестов и в подвале каталога

> **Создан:** 2026-09-26 10:16 +03:00 (dev-1, Opus 5.5) · **Родитель:** постановки Менеджера `ndim-b3` 2026-09-26 —
> внедрение ответов владельца №102 В1 = А (страница, 09:58) и №103 В1 = А (страница, 10:01) ·
> `interviews/interview_102_test_pages_negations.md` · `interviews/interview_103_test_pages_nearby.md` ·
> `bugs/NEW_DONE_personality_faq_privacy_claim_false.md` · `bugs/NEW_DONE_personality_result_example_wider_than_product.md` ·
> **Статус:** ✅ сборка — pass; стейдж и бой — за Менеджером · **Исходящее:** —

## Карточка прогона

- **Работа:** все строки таблиц №102 (строки 1–29, строка 3 — «для сведения», уже в main) и №103 (строки 1–14, строка 8 —
  «без правки») внесены дословно в `src/lib/content/test-copy.ts` (подвал `TEST_FOOT`; «Тест личности»; «Калькулятор
  любви»; хаб `HUB`), `src/lib/content/catalog-copy.ts` (подвал хабов каталога) и
  `src/routes/[lang=lang]/dimension/[slug]/+page.svelte` (подвал карточки объекта). У внесённых строк — пометка «Принято
  владельцем — интервью №102/№103, В1 = А»; прежние `[AI]` у описаний для поиска «Теста личности» заменены ею. Ветка
  `ndim_dev1_negations_impl` от main `a65832b`.
- **Контур:** рабочее место dev-1 (слот 3): сборка `npm run build` (метка сборки `a65832b` · дерево грязное · сборка 2306),
  `vite preview --port 4203 --strictPort`, Chromium Playwright. Стейдж и бой не тронуты — выкат за Менеджером.
- **Прогонов:** 2026-09-26 +03:00:
  · ≈10:05 — `node <скретчпад>/apply-102-103.mjs` (35 замен, каждая ровно один раз, иначе файлы не пишутся) и
    `node <скретчпад>/verify-102-103.mjs <скретчпад>` — код против колонки «Новая строка» обеих таблиц;
  · ≈10:06 — `npm test`: 9 красных юнитов каталога (`catalog-hub.test.ts`, `catalog-copy.test.ts`: ждали 5153 записи, пришло
    5125) — у рабочего места снимок `dims-build.json` от 2026-08-23; скопирован снимок главной копии
    (`D:\work\ai_sandbox\ndim`, 2026-09-25 15:21, 5153 записи; файл вне git), сборка на старом снимке остановлена;
  · ≈10:08 — `npm test`, `npm run test:tools`, `npm run build`;
  · ≈10:10 — `node tools/verify-test-pages.mjs`, `node tools/verify-product-vocabulary.mjs`, `npm run guards`, `npm run check`,
    `npm run typecheck`;
  · 10:12:22–10:12:52 — `node qa/reports/2026-09-26_test-pages-negations-impl.driver.mjs` (первый проход: раскрывал все
    `<details>`, включая выпадашку языка в шапке, — промах драйвера, исправлен);
  · ≈10:13 — мутант драйвера; 10:14:03–10:14:32 — повтор драйвера;
  · ≈10:15 — `grep` по всей `build/`: прежний и новый подвал.
- **Проверок:** провалов 0.
  - Гигиена: `npm test` 591/591 · `npm run test:tools` 901/901 · `npm run check` 0 ошибок, 0 предупреждений ·
    `npm run typecheck` 0 · `npm run guards` 22/22 · `verify-test-pages` 89/0 · `verify-product-vocabulary` 14/0
    (303 страницы).
  - Дословность: код против таблиц — 47 из 47 сравнений (пары RU / EN склеены « · », как в таблице); мутант таблицы
    («Сравнили вещей: 8.») — 1 красный адресно. Вещи примеров — все шесть в пуле из 23 (`poolQueue`), живые подписи
    `rowLabel` дословно как в таблицах: «Побег из Шоушенка · Фильм, 1994», «Матрица · Фильм, 1999», «Доктор Хаус ·
    Телесериал, 2004», «Титаник · Фильм, 1997», «Игра Престолов · Телесериал, 2011», «Аватар · Фильм, 2009» и их EN.
  - Ручной функциональный прогон (сборка · Chromium · ПРОЧИТАНО): восемь страниц — `/{ru,en}/test/personality`,
    `/{ru,en}/test/love`, `/{ru,en}/tests`, `/{ru,en}/dimension/the-shawshank-redemption-bqz7orsq` — × 390/1440 × светлая и
    тёмная: 1352 проверки, 0 провалов (новые строки стоят — текст берётся из того же источника, что у страницы; 29
    прежних строк нет ни на одной; описание для поиска новое; тема применена; переполнения нет; консоль без ошибок).
    Мутант драйвера (новый подвал объявлен «прежним» и строка «Калькулятора любви» подменена) — 20 красных адресно:
    подвал четырёх русских страниц × 4 сочетания и строка под примером «Калькулятора любви» × 4. Кадры прочитаны
    глазами: «Тест личности» RU 390 светлая, «Калькулятор любви» RU 390 тёмная, «Тесты» RU 390 светлая и EN 1440
    тёмная, карточка каталога RU 390 тёмная — строки на местах, значки примеров ⭐ · 🤝 · 💬 как у живого результата,
    переносы без наложений.
  - Вся сборка: прежний подвал («честный поиск похожих людей» / «honest search for similar people») — 0 из 10 528 HTML;
    новый — на 5 247 русских и 5 247 английских страницах.
- **Найдено:** дефектов внедрения — ноль. Замечено рядом, вне таблиц, не правилось:
  · строка карточки «Теста на совместимость» на странице «Тесты» — «Вы оба оцениваете одни и те же 12 вещей — совпадения
    говорят сами за себя.» (`test-copy.ts`, `compatibility` → `hubLine`) — того же рода, что снятое «она проходит те же
    7» (№103, строка 13); с кодом пары не сверялась;
  · у всех пяти рабочих мест команды (`ndim_designer`, `ndim_dev1`, `ndim_dev3`, `ndim_integrator`, `ndim_qa`) снимок
    `dims-build.json` от 2026-08-23 (5125 записей) — `npm test` там краснеет девятью юнитами каталога без правки кода;
  · подвал стоит в видимом тексте 10 494 страниц, а видимый текст входит в отпечаток `lastmod` карты сайта
    (`tools/lib/sitemap-lastmod.mjs`) — ближайший выкат даст новую дату всем карточкам каталога, хотя главное
    содержание карточек не менялось.
- **Следы:** записей в базу нет: страницы только открывались, гость рождается первой оценкой, а оценок не ставилось;
  аналитика на `localhost` не шлёт (`analytics.ts:152–163`). Кадры — `test-results/negations-impl/` (32) и
  `test-results/negations-impl-mutant/`, вне git. `src/lib/content/dims-build.json` рабочего места заменён снимком
  главной копии (вне git). Свой `vite preview` на 4203 погашен, порт свободен.
- **РЕАЛЬНЫЙ МИР:** в бою до выката стоят прежние строки. Накоплено у людей: пререндеренные страницы в кеше браузера и
  CDN; ссылки пары, созданные до выката, работают как прежде (строки результата не менялись — менялся только пример).
  Выкат трогает 10 494 страницы каталога и тестов одним подвалом — это объём выката Менеджера, и `lastmod` названных
  страниц сменится (раздел «Найдено»). Страницы глазами владельца не пройдены.
- **Вердикт:** **pass** для сборки — строки обеих таблиц стоят дословно, гигиена и ручной прогон зелёные; стейдж, бой и
  взгляд владельца — за Менеджером.
