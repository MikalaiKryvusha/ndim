# Отчёт по тестированию — главная `/` считает только в PostHog; «Секс» в тесте на совместимость

> **Создан:** 2026-09-25 17:10 +03:00 · **Родитель:** `plans/106_epic104_phase2_new_v1_on_main.md` (поправки по суду) ·
> коммит `f8acf3c` (+ английская строка над тестом — `ca911f8`) · набор `qa/suites/new-landing-v1.md` (НЛ-08…НЛ-12, НЛ-19)
> · **Статус:** ✅ pass на стенде · **Исходящее:** стейдж — вместе с ответами интервью №097.

## Карточка прогона

- **Работа:** две находки независимого суда 2026-09-25. (1) Корень `/` звал `track()`, а он пишет ещё и свой счётчик
  `space/funnel/days` в Firestore — вопреки слову владельца (интервью №078, В1): «*Г — только аналитикой постхог, мы свою
  БД фаерстор не грузим запросами*». Теперь на корне `track(…, { ownCounter: false })` — только PostHog; `/ru` и `/en`
  пишут и свой счётчик, как до новой V1. (2) В тесте седьмой строкой «Секс» (11 голосов, самая оцениваемая практика) на
  месте «Путешествий» (3 голоса) — по стоячему слову владельца о витрине «*НУ И ЧТО, ЧТО СЕКС — люди любят, вот и
  показываем честно*»; строка над тестом (RU/EN) перечисляет практики по новому списку.
- **Контур:** стенд (`npm run stand`: эмуляторы Firestore :8181 / Auth :9099 / Storage :9199, сервер синхронизации,
  `vite dev` :5173). PostHog на стенде молчит по построению (ворота хоста) — отправку туда этот прогон не судит.
- **Прогонов:** 2026-09-25 +03:00:
  · 16:56:55 — `node qa/reports/2026-09-25_root-posthog-only.driver.mjs` (корень и `/ru` в свежих контекстах, звезда, 6 с
    тишины, чтение счётчика дня из эмулятора; вывод — `test-results/root-posthog-only.json`);
  · 16:57:20 — разовый сценарий кадров списка теста: `node qa/reports/2026-09-25_root-posthog-only.rows.driver.mjs` (`/ru?as=none` 390 светлая,
    `/en?as=none` 1440 тёмная, звезда 9 «Сексу»; кадры `test-results/root-posthog-only/rows-*.png`);
  · 16:57:41–16:57:48 — разовый сценарий моста: `node qa/reports/2026-09-25_root-posthog-only.bridge.driver.mjs` (`/?as=guest`: «Секс» 9 и «Матрица» 10 →
    «Смотреть больше» → профиль гостя; чтение `points/{uid}/dims` из эмулятора с `Bearer owner`; кадр
    `test-results/root-posthog-only/profile-after-bridge.png`);
  · 16:59–17:00:46 — `npm run e2e` (стенд погашен, замок стенда у Менеджера).
  *Поправка 2026-09-25 17:29 (находка суда): оба разовых сценария исполнялись из скретчпада сессии (`rows.mjs`,
  `bridge-sex.mjs`); в отчёт они приложены файлами с телами без изменений — команды выше их повторяют. Английская строка
  над тестом правлена в 17:01:55 (`ca911f8`), после этих прогонов, а список практик сменён между 17:10 и 17:29 по второму суду —
  оба изменения этим прогоном не покрыты; их прогон — раздел «Повтор» ниже.*
- **Проверок:** функциональных пройдено 5, провалено 0.
  - Гигиена: `npm test` 540/540 · `npm run check` 0 ошибок · `npm run guards` 22/22 · `npm run e2e` 112 прошли / 4
    пропущены / 0 провалов · два новых юнита в `funnel.test.ts` доказаны мутациями с адресатами до прогона (М1
    «`ownCounter: true` на корне» → красный ровно первый; М2 «один вызов голым `track`» → ровно второй).
  - Функциональный прогон: стенд · живой Chromium · ПРОЧИТАНО:
    · корень `/`: h1 «Знакомства по интересам в Пространстве NDim Space»; шаги `landing_view` и `demo_touch` засчитаны
      (отметки визита `ndim-funnel-landing_view`, `ndim-funnel-demo_touch` в `sessionStorage` — `track()` вызван);
      документ счётчика дня в Firestore — 404 до и 404 после: в базу не записано ничего;
    · `/ru` (положительный контроль): те же два шага засчитаны, счётчик дня появился — `landing_view` 1, `demo_touch` 1;
    · список теста: RU — седьмая строка «ПРАКТИКА · Секс», EN — «PRACTICE · Sex»; звезда 9 горит; кадры 390 светлая и
      1440 тёмная просмотрены глазами;
    · мост: за 1,25 с до `/profile?guest=root`; в NDim ID гостя ровно две оценки — `BDsOxBipIv04OwFlWuRv` («Секс») 9 и
      `Z3jmK1IXmsKEneI72RVH` («Матрица») 10; кадр профиля гостя просмотрен.
  - **Повтор после второго суда** (практики по голосам: «Секс», «Настольные игры», «Употребление алкогольных напитков»,
    «Чтение»; новый абзац над тестом; строка Макса с кавычками в английском; Максу за алкоголь 8), стенд, 2026-09-25:
    · 17:34:10 — `node qa/reports/2026-09-25_root-posthog-only.driver.mjs` → корень 404 → 404, `/ru` +1 и +1, все четыре
      признака вердикта `true` (вывод — `test-results/root-posthog-only-repeat-counter.json`);
    · 17:34:24 — `node qa/reports/2026-09-25_root-posthog-only.repeat.driver.mjs` → ПРОЧИТАНО: десять строк RU и EN в
      новом порядке («ПРАКТИКА · Употребление алкогольных напитков» / «PRACTICE · Alcohol Consumption»); абзац над тестом
      и строка Макса — новые редакции на обоих языках; «Любит:» у Макса — «Матрица» первой; мост «Секс» 9 +
      «Употребление алкогольных напитков» 7 + «Матрица» 10 → в NDim ID гостя ровно эти три (вывод —
      `test-results/root-posthog-only-repeat.json`); кадры `test-results/root-posthog-only-repeat/rows-390-light.png` и
      `rows-1440-dark.png` просмотрены глазами — длинное название переносится, не обрезается;
    · 17:35–17:37 — `npm run e2e` (стенд погашен): 112 прошли, 4 пропущены, 0 провалов · `npm test` 540/540 · `check` 0.
- **Найдено:** дефектов продукта ноль. Правки — исправление находок двух судов, заведённых в `plans/106` поправками на
  месте. Дефект процесса при гашении стенда: фильтр по командной строке «`npm-cli.js run stand`» нашёл и погасил ДВА
  стенда — свой (слот 0) и стенд dev-1 (слот 3, замок у него с 17:08); dev-1 извещён. Лечение — адресная остановка стенда
  своего рабочего места (очередь задач, `STATUS.md`).
- **Следы:** эмулятор стенда — счётчики дня от `/ru` (дважды по 1 — стенд между прогонами поднимался заново), два гостя
  с оценками, заведённые мостом; стенды погашены, эмуляторы свою базу на диск не сохраняют. Кадры — вне git.
- **Вердикт:** **pass** на стенде — корень считает только в PostHog, `/ru` пишет свой счётчик, практики теста стоят по
  голосам и уезжают в NDim ID гостя. Не проверено: отправка `landing_view` корня в PostHog на стейдже (стенд её не
  шлёт) — проверяется повторным прогоном на стейдже вместе с ответами №097.
