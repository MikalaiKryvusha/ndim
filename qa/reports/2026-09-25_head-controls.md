# Отчёт по тестированию — одна пара «тема + язык» во всех шапках продукта (`HeadControls`)

> **Создан:** 2026-09-25 21:17 +03:00 · **Родитель:** слово владельца 2026-09-25 «*какого хуя в разных местах переключатель темы
> и языка разный? почему дизайн схема и элементы не переиспользуете. а нахуй изобретаете велосипед каждый раз???*» ·
> `bugs/NEW_theme_lang_switchers_differ.md` · **Статус:** ✅ вторая партия в бою 20:58, третья — стейдж · **Исходящее:** —

## Карточка прогона

- **Работа:** пять копий пары «тема + язык» (шапка приложения `AppBar`, шапка публичных страниц `PublicBar`, копия шапки
  страницы теста, экран входа `SigninScreen`, главная `LandingV1`) заменены одним компонентом `src/lib/ui/HeadControls.svelte`
  — вид шапки приложения по слову владельца 2026-07-27 «*квадратные и без стрелочки*» (партия 2: `0d87980`, `47847af`,
  `ea6b95b`, слияние `7bcf7e3`). Партия 3 (`2d4c374`): касание темы до оживления страницы, закрытие выпадашки касанием мимо
  и Esc на страницах без JS, тема на «Калькуляторе любви» после клиентского перехода — делегированный слушатель `app.html`.
- **Контур:** ветка помощника (сборка, `vite preview` :4191), стенд главной копии (вошедший `dev@`), стейдж
  `ndim-stage.web.app` (`a9f4e63`, затем `deff64c`), бой — дверью выката (`a9f4e63`).
- **Прогонов:** 2026-09-25 +03:00:
  · ≈19:45–20:05 — помощник: Playwright-прибор 7 страниц × 390/1440 × обе темы (28 прогонов), кадры
    `.claude/worktrees/agent-a7a0a309b15bfbc60/test-results/head-controls/`;
  · ≈20:08 — `node <скретчпад>/appbar-check.mjs http://localhost:4191` (шапка приложения под вошедшим человеком стенда);
  · ≈20:12 и ≈20:20 — `node <скретчпад>/heads.mjs http://localhost:4191 <build> heads-rel2` (шапки пяти поверхностей × обе темы);
  · 20:27:31–20:29:07 — `npm run e2e`; 20:29:27–20:34:34 — `npm run deploy -- --stage` (`a9f4e63`);
  · 20:35:04 — `node qa/reports/2026-09-25_landing-owner-fixes.walk.driver.mjs https://ndim-stage.web.app stage2` и `heads.mjs` на стейдже;
  · 20:37:09 — `MSYS_NO_PATHCONV=1 node qa/reports/2026-09-25_landing-owner-fixes.stage-guest.driver.mjs --path /ru --theme light`;
  · 20:52:29–20:58:54 — `npm run deploy` (бой, `a9f4e63`, AUTH — слово владельца «*катите*»);
  · 21:02:58–21:04:57 — `npm run e2e` (партия 3); 21:05:14–21:06:54 — `npx playwright test e2e/head-controls.spec.ts e2e/demo.spec.ts`;
  · 21:08:50–21:13:54 — `npm run deploy -- --stage` (`deff64c`);
  · 21:14:26 — `node qa/reports/2026-09-25_head-controls.stage.driver.mjs --base https://ndim-stage.web.app`;
  · 21:15:01 — `…walk.driver.mjs https://ndim-stage.web.app stage3` и `…stage-guest.driver.mjs --path /ru --theme dark`.
- **Проверок:** функциональных пройдено все, провалено 0 (после исправления промаха теста).
  - Гигиена: `npm test` 552/552 · `npm run test:tools` 848/848 (860/860 после слияния контура вычитки) · check 0 ·
    typecheck 0 · guards 22/22 · e2e партии 2 — 112/4/0; партии 3 — 115 пройдено + 1 провал промахом теста (касание в центр
    заголовка на 430 попадало в пункт выпадашки), исправлено, повтор двух файлов 14/14 · двери стейджа и боя — заголовки
    12/0, под сессией 24/0, публичный 83/0.
  - Функциональный прогон: сборка · стенд · стейдж · живой Chromium · ПРОЧИТАНО:
    · пять поверхностей × обе темы: одна пара, кнопки 34×34 на непрозрачной панели, одинаковый вид (фон кнопки светлая
      `rgb(255,255,255)`, тёмная `rgb(9,19,34)` на всех пяти);
    · шапка приложения под вошедшим (стенд) и гостем (стейдж): тема — одна запись `data-theme` за касание, «English» переводит
      экран и закрывает выпадашку, «Меню» видит ту же тему и язык, бейдж «гость» стоит перед парой; консоль 0;
    · касание темы ДО оживления главной (стейдж, эмуляция телефона): касание в 1 489 мс, страница ожила к 1 836 мс → одна
      смена темы; после оживления — снова одна;
    · каталог без JS (стейдж): выпадашка открывается, закрывается касанием мимо и Esc; тема — одна смена;
    · клиентский переход `/ru` → «Калькулятор любви»: тема — одна смена (до партии 3 кнопка была мёртвой);
    · главная целиком — 8 сочетаний RU/EN × 390/1440 × обе темы: «системного персонажа» нет, переполнения нет, консоль 0, FAQ
      13–14 кадров раскрытия.
- **Найдено:** касание темы до оживления терялось (находка dev-1, ВС-17) · выпадашка на страницах без JS не закрывалась
  касанием мимо и Esc (суд) · тема на «Калькуляторе любви» после клиентского перехода была мёртвой (суд; было и до партии 2) —
  все три закрыты партией 3, документ `bugs/NEW_theme_lang_switchers_differ.md`.
- **Следы:** стейдж — две гостевые учётки проходов гостя удалены своим токеном (точка и учётка — 200); проходы главной и
  каталога — меткой прибора, в метрику людей не пишут; кадры — `test-results/`, вне git.
- **РЕАЛЬНЫЙ МИР:** накоплено — у владельца прежние сборки в кеше браузера и сохранённая тема/язык в `localStorage`; данные и
  техника — его телефон; путь — `ndimspace.app`, каталог из поиска, приложение под его входом. Дверь боя прошла вход под
  сессией и публичный смоук (партия 2); ключи `ndim-theme` и `ndim-lang` не менялись. Главная в бою агентом глазами не пройдена —
  прогон отказан системой разрешений сессии; ждёт взгляда владельца.
- **Вердикт:** **partial** до выката партии 3 в бой и взгляда владельца; сборка, стенд и стейдж — pass.
