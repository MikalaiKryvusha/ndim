# Отчёт по тестированию — черновик страницы вычитки переживает смерть процесса на закреплённом порту (`bugs/234`)

> **Создан:** 2026-09-13 00:2x +03:00 · **Родитель:** `bugs/234` · `STATUS.md` (мера была
> `[NOT-TESTED]`) · **Статус:** ✅ мера доказана ручным прогоном · **Исходящее:** —

## Карточка прогона

- **Работа:** операционная мера против потери набранного ответа владельца (`bugs/234`): поднимать
  страницу вычитки `node tools/review.mjs open … --port <закреплённый>`. Утверждение под проверкой —
  черновик живёт в `localStorage` своего web-origin, порт входит в origin, значит подъём на ТОМ ЖЕ
  порту после смерти процесса возвращает набранное, а на ДРУГОМ — нет.
- **Контур:** рабочее место агента, локальный контур вычитки (`tools/review.mjs`); временная копия
  документа `interviews/interview_999_tmp_draft_port_check.md` (копия №083), порты 47991 и 47992,
  отдельный браузер Playwright (Chromium) — хранилище владельца не задевается.
- **Прогонов:** **1**, 2026-09-13 ≈00:07 +03:00, прибор `check-draft-port.mjs` из скретчпада сессии
  (запускался копией `tools/.tmp-check-draft-port.mjs`, удалена после прогона).
- **Проверок:** **пройдено 6, провалено 0.**
  1. набранный в поле комментария В1 текст записан в хранилище порта A (ключ
     `ndim-review-draft:interviews/interview_999_tmp_draft_port_check.md`);
  2. **контроль:** процесс убит `taskkill /PID … /T /F`, страница поднята на порту B — поле пусто
     (проверка способна покраснеть);
  3. страница поднята заново на порту A — текст вернулся побайтово;
  4. страница показала плашку «Восстановлен черновик»;
  5. временный документ удалён;
  6. новых файлов решений в `interviews/decisions/` нет (75 → 75).
- **Найдено:** **ноль дефектов меры.** Отдельно и не провалом этой проверки: в 00:13 окно живой страницы
  №083 владельцу закрыла соседняя сессия KAIF, приняв за свою пробную (`bugs/234`); процесс
  страницы при этом жил, мера не задета.
- **Следы:** ничего — временный документ и прибор удалены, процессы погашены прибором, решений не
  записано.
- **Вердикт:** годится — мера работает как утверждалось; маркер `[NOT-TESTED]` в `STATUS.md`
  снимается. Гигиена разработчика здесь не применялась (кода не менялось); **ручной функциональный
  прогон:** живой контур вычитки, браузер, смерть процесса, повторный подъём — что увидено перечислено
  в проверках 1–4.
