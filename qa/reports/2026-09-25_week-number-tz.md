# Отчёт по тестированию — прибор «число недели»: окно с явным поясом +03:00

> **Создан:** 2026-09-25 16:55 +03:00 (dev-1, постановка №1 Менеджера) · **Родитель:**
> `bugs/NEW_DONE_week_number_window_labelled_utc_is_moscow.md` · набор `qa/suites/week-number.md` (НВ-07, НВ-08) ·
> прежний отчёт прибора `qa/reports/2026-09-25_week-number.md` · **Статус:** ✅ pass · **Исходящее:** —

## Карточка прогона

- **Работа:** прибор числа недели (`tools/week-number.mjs`) печатал окно «(UTC)», а HogQL читал его границы без пояса
  в поясе проекта (+03:00). Правка: каждая граница и каждый календарный день — с явным `Europe/Moscow`, подпись
  «+03:00 (Москва)», контроль пояса самим запросом (HogQL возвращает начало окна в UTC, прибор сверяет момент); общая
  дорога HogQL (`tools/lib/posthog.mjs`) отказывает границе без пояса до сети. Ветка `ndim_dev1`, головы `06726b0` →
  `d6bdf3e`.
- **Контур:** бой — PostHog проекта NDim Space, события `properties.env = 'prod'`; прибор только читает. Живые прогоны
  исполнил **Менеджер из главной копии** `D:\work\ai_sandbox\ndim` (ключ чтения PostHog есть только там; ключи никуда не
  копировались): прибор ДО — его ствол `ff145a8`, прибор ПОСЛЕ — файл ветки dev-1 по абсолютному пути (`.env` читается
  из текущего каталога, импорты — рядом с файлом). Отказ дороги HogQL — прогон dev-1 в своём рабочем месте, без сети.
- **Прогонов:** 2026-09-25 +03:00:
  · 16:48:55 (dev-1) — отказ: `node <скретчпад>/refusal-run.mjs` (дословный запрос драйвера
    `qa/reports/2026-09-25_new-landing-v1.stage-driver.mjs:150` через `hogql()` с `fetch`, считающим вызовы) ·
    `node tools/week-number.mjs --week 2026-09-15` · `node tools/week-number.mjs --week 2026-09-21` (без ключа);
  · 16:50:10 (Менеджер, голова `06726b0`) — четыре команды одним заходом:
    `node tools/week-number.mjs --week 2026-09-21` ·
    `node D:\work\ai_sandbox\ndim-team\ndim_dev1\tools\week-number.mjs --week 2026-09-21` · то же для `--week 2026-09-14`;
  · 16:52:42 (Менеджер, голова `d6bdf3e`) — те же четыре команды тем же заходом.
- **Проверок:** функциональных пройдено 2 (НВ-07 на `d6bdf3e`, НВ-08) · провалено 1 и вылечено (НВ-07 на `06726b0` —
  ложный красный контроля) · не исполним 0 из новых; НВ-06 по-прежнему ждёт первого возврата.
  - Гигиена: юниты `tools/week-number.test.mjs` 9/9 и `tools/lib/posthog.test.mjs` 4/4 (`npm run test:tools` 844/844
    на `06726b0`) · мутации с адресатами, названными до прогона: М2, М3, М3б, М4 — ровно названные красные (1 · 2 · 1 ·
    1); М1 (граница `{to}` без пояса) дал 5 красных вместо предсказанного 1 — прогноз сделан до правки общей дороги,
    теперь голую границу первым отказывает `hogql()`; М5 → 2, М6 → 3, как названо · `npm run guards` 22/22 ·
    `npm run typecheck` 0 · `node tools/verify-import-safety.mjs` чисто · `npm test` 529/538 — 9 красных НЕ от правки:
    `src/` не тронут, красные — проверки по выгрузке боевого каталога вне git (`src/lib/content/dims-build.json` в
    рабочем месте dev-1 устарела: 5125 записей против 5153, которые ждут `dim-kind.test.ts` и `catalog-hub.test.ts`).
  - Функциональный прогон: бой · прибор запущен руками на живых данных, вывод прочитан:
    · НВ-07 **pass** (заход 16:52): ПОСЛЕ печатает «окно 2026-09-21 00:00 … 2026-09-28 00:00 +03:00 (Москва)» и
      «начало окна 2026-09-21 00:00 +03:00 = 2026-09-20 21:00 UTC — сверено HogQL» (для 14.09 — 2026-09-13 21:00 UTC);
      пять чисел ДО = ПОСЛЕ: неделя 21.09 — пришли 8 · гостем 0 · ≥ 5 оценок 0 · Связи 0 · вернулись 0 (окно возврата
      открыто); неделя 14.09 (контроль, не ноль) — 160 · 9 · 3 · 6 · 0; коды выхода 0. Совпадение ожидаемо: прежний
      запрос уже читался в поясе проекта +03:00, неверной была подпись, а не окно.
    · НВ-07 **fail → вылечено** (заход 16:50 на `06726b0`): ПОСЛЕ краснел «окно названо неверно: … как 2026-09-20
      21:00:00.000000 UTC, прибор ждал 2026-09-20 21:00:00 UTC» — моменты равны, контроль сверял СТРОКИ, а HogQL
      печатает DateTime64 с дробью. Фикстура юнита повторяла ожидание кода, а не живой ответ. Лечение `d6bdf3e`:
      сверка моментов, фикстура — дословный живой ответ.
    · НВ-08 **pass** (16:48:55): «граница времени без пояса: toDateTime({since}) — HogQL прочтёт её в поясе проекта
      (+03:00)…», обращений в сеть 0. Неверный вход «2026-09-15 — не понедельник», код 1; без ключа — «нет
      POSTHOG_PROJECT_ID или POSTHOG_NDIM_SPACE_PERSONAL_API_KEY в .env», код 1.
- **Найдено:** 1 дефект собственной правки — ложный красный контроля пояса на формате DateTime64 (пойман живым
  прогоном, вылечен до сдачи, `d6bdf3e`). Близнец класса «граница без пояса» — 1:
  `qa/reports/2026-09-25_new-landing-v1.stage-driver.mjs:149–151` (нашёл Менеджер, тот прогон он перепроверил с явным
  поясом; артефакт не правится, после мержа общий `hogql()` его запрос отказывает). Дефектов продукта — ноль.
  Стоячая ложь: подписи «(UTC)» в `qa/reports/2026-09-25_week-number.md` нет (греп «UTC» по файлу пуст) — она жила только
  в выводе прибора; строка `STATUS.md:57` о баге — зона Менеджера, снимается после мержа.
- **Следы:** нет — прибор и дорога HogQL только читают; учёток, записей, событий прогоны не оставили. Скрипты мутаций и
  прогона отказа — в скретчпаде сессии dev-1, вне git.
- **Вердикт:** **pass** — окно числа недели названо тем поясом, в котором считается, и каждое чтение само сверяет это с
  HogQL; числа прежних прогонов верны (менялась подпись). Граница: НВ-06 (возврат > 0) по-прежнему ждёт первого
  возврата; `toDate('…')`, `toStartOfDay(…)` и столбцы без пояса общая дорога не судит — только границы-значения
  `toDateTime({…})` и `toDateTime('…')`.
