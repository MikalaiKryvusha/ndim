# Отчёт по тестированию — первый экран гостя В БОЮ, после выката

> **Создан:** 2026-09-10 00:2x +03:00 · **Родитель:** `plans/82` Ш5 · слово владельца:
> «*кати в прод*» и «*тестируй в проде*» · **Статус:** ✅ выкат состоялся, прогон исполнен ·
> **Исходящее:** наблюдение за воронкой через неделю.

## Карточка прогона

- **Работа:** первый экран гостя показывает объект, который человек только что оценил
  (`plans/82`, макет V5), вместе с лечением двух текстов лица продукта.
- **Контур:** **БОЙ** `https://ndimspace.app`, коммит `0f584000`, выкат дверью `npm run deploy`
  2026-09-10 ≈00:1x +03:00. AUTH владельца в чате: «*кати в прод*».
- **Прогонов:** 3 — дверь выката (смоук под сессией + гостевой смоук), затем набор
  «первый экран гостя» по живому бою.
  Команды: `npm run deploy` · `node tools/verify-guest-first-screen.mjs --base https://ndimspace.app --prod`.
- **Проверок:** дверь выката **24 + 83**, набор **25**. Итого **132**, провалов **0**.
- **Найдено:** ноль. Ни один дефект продукта этим прогоном не найден.
- **Следы:** 6 гостей и 6 оценок заведено и убрано. Проверено ТРЕМЯ независимыми способами:
  встроенная уборка (`УБ-01` точки, `УБ-02` учётки входа), замер боевого измерения до и после
  (`stars=0 rates=0 rating=0` → без изменений, точек в бою 348 → 348) и прямой пересчёт учёток
  входа (анонимных, заведённых за последний час: **0**). Кадры — `test-results/guest-first-screen/`.
- **Вердикт:** 🟢 **работа в бою и ведёт себя по замыслу.**

---

## 1. Что выкачено

| Что | Состояние в бою |
|---|---|
| Блок «Вы оценили» на первом экране гостя (макет V5) | ✅ работает |
| Строка «Вас никто не видит: в Пространстве Вы невидимы» — отменённое правило | ✅ убрана, обе половины |
| Вторая копия того же правила в редакторе аудитории свойства | ✅ убрана |
| Текст про срок жизни гостя — Ваш, из утверждённого набора макетов | ✅ стоит, обе половины |

## 2. Прогон по бою — что именно проверено

Восемь кейсов набора (`qa/suites/guest-first-screen.md`), четыре из них негативные:

| Кейс | О чём | Итог |
|---|---|---|
| ГЭ-01 | объект и оценка на первом экране; запись есть в базе | ✅ 5/5 |
| ГЭ-02 | название читается целиком на 390 | ✅ 2/2 |
| ГЭ-03 | гость без двери блока не видит | ✅ 2/2 |
| ГЭ-04 | след одноразовый | ✅ 3/3 |
| ГЭ-05 | след есть, оценки нет — продукт молчит | ✅ 1/1 |
| ГЭ-06 | отменённого правила и слова «навсегда» на экране нет | ✅ 2/2 |
| ГЭ-07 | обе темы × 390/1440 | ✅ 4/4 |
| ГЭ-08 | английская половина | ✅ 3/3 |
| УБ-01 · УБ-02 · КН-01 | уборка точек, уборка учёток, чистая консоль | ✅ 3/3 |

**Вердикт снимается с экрана И с базы.** Оценка ищется в боевом Firestore служебным ключом.

## 3. Цена прогона по бою, названная ДО прогона

Набор пишет: за проход он заводит гостя и ставит оценку настоящему объекту каталога. Шесть клеток
матрицы — шесть гостей и шесть оценок. Гость живёт 7 дней и считается точкой Пространства;
оценка участвует в фирменном рейтинге объекта.

Поэтому: снят замер объекта ДО прогона, прогон помечен меткой прибора (иначе он попал бы в
воронку — класс `bugs/202`, за который уже заплачено 77 ложными гостями), уборка проверена тремя
независимыми способами.

**Замер объекта «1.Kla$» до и после:** `stars=0 rates=0 rating=0` → `stars=0 rates=0 rating=0`.
Фирменный рейтинг не сдвинулся ни на бит.

## 4. Что дальше по этой работе

- **Через неделю** прочитать ряд воронки боя: `node tools/read-funnel-days.mjs --contour prod` —
  шаги `door_click` → `guest_start` → оценки после входа. Это критерий приёмки `plans/82` Ш5,
  и он меряется временем, а не прогоном.
- **Открытым остаётся** наблюдение за первым живым человеком, прошедшим этим путём: прибор
  проходит его безошибочно, но человек — не прибор.
