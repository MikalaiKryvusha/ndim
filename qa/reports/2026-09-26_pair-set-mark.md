# Отчёт по тестированию — метка пула в коде набора ссылки пары (находка 4 суда V4)

> **Создан:** 2026-09-26 00:46 +03:00 (Менеджер, сессия `ndim-61`) · **Родитель:** суд V4
> `qa/reports/2026-09-26_compat-test-v4.md`, находка 4 «правка `TEST_POOL` сдвигает номера в кодах `set=`» · `plans/107`
> фаза 4 · **Статус:** 🟢 pass на стенде агента; стейдж и бой заперты квотой хостинга.

## Карточка прогона

- **Работа:** `src/lib/model/test-pair.ts`. Код набора первого в ссылке стал `<метка пула>.<номера>`. Метка — три
  36-ричных знака отпечатка списка вещей пула (`poolMark`, FNV-1a). `decodePairSet` отдаёт `null` («подсказки нет»),
  если метки нет или она от другого пула. Тогда второй человек до чтения пары видит очередь пула, как по ссылке без
  `set=`, а после первой оценки очередь берётся из пары. Ссылок V4 с `set=` в мире ещё нет: V4 не выкачен. Поэтому
  формат меняется бесплатно.
- **Контур:** стенд агента — сборка `npm run build` (00:43:29–00:45:00) главной копии + `npx vite preview --port 4196
  --strictPort`, Chromium Playwright, ширина 390. Второй человек приходит без сессии: документ пары до первой оценки не
  читается, очередь строится только из кода ссылки. id пары — форма из 22 знаков, документа нет.
- **Прогонов:** 2026-09-26 +03:00, `node --experimental-strip-types qa/reports/2026-09-26_pair-set-mark.driver.mjs`:
  · 00:45:15–00:45:19 — 3 из 3, провалов 0.
  Юнит `node --test src/lib/model/test-pair.test.ts` — 10/10. Мутант M3 «декодер не сверяет метку», адресат назван в
  чате до прогона: красный ровно «🔑 код с меткой ДРУГОГО пула…» (9/1). Файл возвращён побайтово, sha256 сверен, 10/10.
- **Проверок:** 3 из 3, провалено 0 (метка пула этой сборки — `q0o`):
  · ПМ-01 — код своего пула `q0o.57`: первая карточка «Леон», это вещь 5 пула;
  · ПМ-02 — код с чужой меткой `zzz.57`: первая «Гарри Поттер и философский камень», это вещь 0 (порядок пула);
  · ПМ-03 — код без метки `57`: то же, вещь 0.
  - Гигиена: `npm test` 587/587, `npm run check` 0 ошибок (00:45:27–00:45:47).
- **Найдено:** дефектов 0.
- **Следы:** preview погашен; `build/` — сборка этой правки. В сеть ничего не писалось.
- **Вердикт:** **pass** — сменившийся пул больше не подсказывает второму человеку чужие вещи по старому коду.

## Решения, принятые без владельца

- `[AI]` Код без метки не принимается: ссылок старого вида с `set=` в мире нет, V4 не выкачен. Поддерживать чтение
  формата, который никто не выдавал, — лишняя ветка.

## Границы (GAP)

- Юнит «пул = правилу на каталоге сборки» по-прежнему краснеет на обновлении снимка каталога, если новые голоса меняют
  пул (суд: 49 из 160 смоделированных голосов). Это задумано — «подмена голосов вкусом краснеет сама». Лечение —
  пересчитать `TEST_POOL` правилом `derivePool`; метка пула в кодах после этого сменится сама.
- Ссылки, выданные ДО V4 (без `set=`), — находка 3 суда: второму достаётся пересечение старого набора с пулом.
  Не лечится этой правкой; число таких пар в бою не замерено.
- **РЕАЛЬНЫЙ МИР:** не проверено — стейдж и бой заперты квотой хостинга.
- Уроки EXPERIENCE: EXP-0333 (сменил состав — ищи всё, что на него опирается: здесь коды ссылок) · EXP-0334 (контроль
  отсутствия: ПМ-02 и ПМ-03 — случаи, где подсказки быть не должно).
