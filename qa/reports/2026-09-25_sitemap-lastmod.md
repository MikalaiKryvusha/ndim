# Отчёт по тестированию — правдивый `<lastmod>` в карте сайта (реестр отпечатков, FORK Б)

> **Создан:** 2026-09-25 22:53 +03:00 (dev-1, очередь Менеджера `ndim-61`, пункт 4) · **Родитель:**
> `plans/NEW_sitemap_truthful_lastmod.md` (FORK Б — решение Менеджера) · `researches/76` §6 строка 9 · **Статус:** 🟡 partial
> — ручной прогон на настоящей сборке pass; живой проход на стейдже двумя выкатами ждёт двери выката (запускает Менеджер) ·
> **Исходящее:** заявка на живой проход стейджа — Менеджеру.

## Карточка прогона

- **Работа:** шаг двери выката `tools/deploy.mjs` → `stampSitemapLastmod` → `tools/lib/sitemap-lastmod.mjs` `stampBuild`.
  У каждого адреса карты берётся отпечаток значимого содержания собранной страницы: заголовок, описание,
  canonical/alternate, JSON-LD, видимый текст, ссылки. Служебное исключено: скрипты сборки, стили и атрибуты, чанки,
  комментарии гидратации, числа витрины главной `ul.nums`, виджет версий `div.vers`, год копирайта. Реестр
  `sitemap-lastmod.json` читается с контура, куда катят:
  · отпечаток прежний — дата прежняя;
  · отпечаток другой или адрес новый — дата выката;
  · реестра нет — карта без `lastmod` целиком.
  Первый выкат — без засева.
- **Контур:** настоящая сборка `npm run build` на рабочем месте dev-1. Шаг штампа — тем же кодом, что в двери; реестр
  «контура» подан из памяти прогона (заглушка ответа хостинга). В сеть ничего не писалось, выката не было.
- **Прогонов:** 2026-09-25 +03:00, из корня рабочего места `D:\work\ai_sandbox\ndim-team\ndim_dev1`:
  · 22:41:06–22:45:35 — прогон 1: `node qa/reports/2026-09-25_sitemap-lastmod.driver.mjs`, «Голова: 3013e24 · дерево
    чистое» → 5 провалов: ЛМ-02 и ЛМ-03 — разбор в «Найдено»;
  · 22:47:18–22:51:45 — **прогон 2 (итог)**: тот же драйвер, «Голова: 501f552 · дерево чистое» → **провалов 0**;
  · 22:52:11–22:52:12 — мутанты `node <скретчпад сессии>/mut-lastmod.mjs` (L1–L4 модуля и S1 стража пробела Svelte;
    адресаты названы в скрипте до прогона);
  · ворота — строка «Гигиена» ниже.
- **Проверок:** прогон 2 — 9 из 9, провалено 0 (прогон 1 — 4 из 9, провалено 5: ЛМ-02 ×2, ЛМ-03 ×3).
  - Гигиена: юниты `node --test tools/lib/sitemap-lastmod.test.mjs` 11/11; `node tools/deploy.mjs --selftest` 8/0;
    `node --test tools/deploy.test.mjs` 9/9. Мутанты, все красные адресно, файлы восстановлены побайтово:
    · L1 «виджет версий не исключён» — «пересборка без правок», «виджет версий»;
    · L2 «прежняя дата не переносится» — «прежний отпечаток — дата прежняя…», «страж штампа»;
    · L3 «без реестра — всем сегодня» — «реестра нет»;
    · L4 «скрипты сборки не вырезаны» — «пересборка без правок», «части отпечатка».
    Ворота 22:54:41–22:55:35 (`npm run -s <имя>` по очереди): `test:tools` 871/871, `guards` 22/22 «ВОРОТА ЧИСТЫ»,
    `typecheck` 0, `check` 0 ошибок (614 файлов), `questions` и `kaif:check` зелёные, `npm test` 558/567 — те же 9
    красных выгрузки каталога вне git (5125 против 5153), построчно.
  - Функциональный прогон: настоящая сборка, вывод драйвера прочитан построчно, `build/sitemap.xml` и реестр прочитаны
    драйвером:
    · **ЛМ-01** pass — сборка A (2196), реестр «контура» 404: карта без `lastmod` целиком; реестр — 10461 страница
      карты, у каждой отпечаток, дат нет.
    · **ЛМ-02** pass — сборка B без правок, реестр A: сменившихся отпечатков 0, дат 0 (прежних 10461).
    · **ЛМ-03** pass — сборка C с временной правкой одной фразы «Политики» п. 2.6 (есть только на `/ru/menu/privacy`),
      реестр B: `lastmod` ровно у `/ru/menu/privacy` — `2026-09-25T22:51:27+03:00`, это момент штампа с поясом;
      изменилась 1, прежних 10460. `src/lib/content/docs.ts` восстановлен побайтово.
    · **ЛМ-04** pass — повторный штамп сборки C (как `--skip-build`) с тем же реестром: карта та же.
- **Найдено:** дефектов продукта — 0. Свой, найден прогоном 1 и вылечен до заявки: страница «О приложении»
  (`/ru|en/menu/about`) печатает виджет версий — «Приложение 2.2 (2195) Собрано 25 сентября 2026 г. в 22:44». Отпечаток
  менялся на каждой сборке, и ЛМ-03 дал лишние даты этим двум адресам. Шапка модуля говорила «метка сборки живёт не в
  страницах» — это была неправда, исправлено. `div.vers` вырезается со счётом вложенности (`stripByClass`, `501f552`).
- **Следы:** на рабочем месте — `build/` сейчас сборка C второго прогона (правка откатана в исходнике), перед выкатом её
  пересобирает дверь. На контурах — ничего: в сеть не писалось. Реестры прогона — только в памяти драйвера.
- **Вердикт:** **partial** — на настоящей сборке шаг правдив: без реестра даты не выдумываются, пересборка без правок
  ничего не сдвигает, изменённая страница получает дату выката, и только она. Живой проход по контуру (реестр,
  выложенный хостингом и прочитанный следующим выкатом) не пройден: его запускает только дверь выката Менеджера.
- **Живой проход дверью Менеджера (дописано Менеджером 2026-09-25 23:58, после слияния `7248b54`):**
  · 23:43:24–23:47:56 — `npm run deploy -- --stage`: «реестр СТЕЙДЖ: HTTP 404 · страниц 10517 · с датой 0 … без даты
    10517»; затем `curl https://ndim-stage.web.app/sitemap-lastmod.json` → реестр `sitemap-lastmod/v1` с отпечатками и
    `lastmod: null`; `curl …/sitemap.xml | grep -c "<lastmod>"` → 0 при 10 517 `<loc>` — ЛМ шаг 1 ✅ на живом контуре;
  · 23:48:18–23:51:13 — `npm run deploy -- --stage` без правок: «реестр СТЕЙДЖ: прочитан · страниц 10517 · с датой 0
    (прежних 10517, изменились 0, новых 0)» — реестр хостинга прочитан, пересборка ничего не сдвинула ✅; сам выкат
    отказан Firebase — квота хранилища хостинга (HTTP 429), `bugs/NEW_stage_hosting_storage_quota_exceeded.md`;
  · 23:52:19–23:57:04 — `npm run deploy` (бой, AUTH «*чиним беклог, тестим, катим*»): «реестр БОЙ: HTTP 404 · страниц
    10517 · с датой 0» — боевой реестр создан, дат ноль, дверь зелёная.
  Шаг 3 плана (правка одной страницы → дата ровно у неё на живом контуре) — ждёт открытия стейджа после решения по квоте.

## Решения, принятые без владельца

- `[AI]` по решению Менеджера (FORK Б) — перечень служебного в шапке модуля. Число витрины главной и виджет версий
  вырезаются по классу элемента; правка этих классов в компонентах отключит исключение. Страж — юнит «пересборка без правок».
- `[AI]` Правила отпечатка несут версию (`RULES`). Реестр другой версии читается как «не прочитался» (дат в этот выкат
  нет), а не как «всё изменилось» (всем сегодня).
- `[AI]` Стоп двери — только если страницы карты нет в сборке или страж штампа нашёл нарушение. Нечитаемый реестр выкат не
  останавливает (карта уходит без `lastmod`).

## Границы (GAP)

- **Живой контур** — не пройден: выкладка `sitemap-lastmod.json` хостингом и чтение его следующим выкатом. Предложение
  Менеджеру: стейдж три выката подряд. Первый — без дат. Второй без правок — даты не появляются. Третий с правкой одной
  страницы теста — `lastmod` ровно у неё.
- **Потеря реестра** (сеть моргнула на выкате) обнуляет даты всех страниц до их следующего изменения. Это правда, но
  свежесть теряется; названо, не лечится.
- **Яндекс** — не сверялся: опора только на Google и sitemaps.org.
- **РЕАЛЬНЫЙ МИР:** не проверено — бой не тронут; как Google прочтёт даты, увидим в консоли после выката.
