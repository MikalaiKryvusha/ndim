# NDim Space — Летопись проекта (PROJECT_HISTORY)

> ТОЛЬКО-ДОПИСЫВАЕМАЯ летопись того, как проект жил и рос: закрытые сессии, выпущенные фазы,
> выкаты, большие решения — в порядке, в котором они случались. Сюда `STATUS.md` сбрасывает своё
> прошлое: STATUS остаётся короткой живой сводкой «СЕЙЧАС», всё завершённое переезжает СЮДА
> (шаг «стрижка бонсая» скилла `/end-chat`).
>
> **Не обязательное чтение.** Файл НЕ входит в канонический набор `/resume` и в минимум «перед
> каждой задачей» — открывай его, только когда действительно нужна археология: как родилось
> решение, что было в старой фазе, когда что выкатывалось.
>
> **Правила летописи (дисциплина ADR):**
> - **Только дописывание, свежее сверху.** Записанная запись никогда не редактируется, чтобы
>   сказать другое, — история, которую можно переписать, не история. Исправления приходят НОВЫМИ
>   записями, ссылающимися на старую и заменяющими её.
> - Запись переезжает сюда ДОСЛОВНО из `STATUS.md`, когда её работа закрыта, — перенос, не
>   переписывание; запись уже несёт свои даты, числа и указатели на файлы.
> - Записи свободно упоминают версии и даты — летопись законно говорит о старых версиях, и
>   stale-claims-сканер машинерии обновлений знает, что этот файл не трогают.
> - Когда файл разрастётся — дели по эпохам: свежую эпоху держи здесь, старшие выноси в
>   `PROJECT_HISTORY_<эпоха>.md`, а сверху оставь однострочный индекс (приём больших ченджлогов).
>
> Живой документ — никогда не помечается DONE.

---


## 🗂 Перенесено из STATUS.md ДОСЛОВНО — стрижка бонсая 2026-08-14 (утро, /end-chat сессии 3)

> Контекст переноса: сессия 3 (ночная, автономная) закрыла фазу 1 «Приборы» и отработала
> автолуп; при её закрытии запись СЕССИИ 2 перестала быть «сейчас» и переезжает сюда.

## 🆕 ЧАТ 2026-08-14 (сессия 2) — переосмысление SEO по `ideas/34`

· Лестница `/plan-epic` пройдена целиком за сессию: **`researches/34`** (5 веб-агентов по
  первоисточникам + повёрстные вердикты 10 тезисов Грока: Т6-сайтмап опровергнут, Т1/Т4/Т5/Т7/Т8
  подтверждены; проверка описаний — 0 дословных заимствований из 20 фраз) → **`plans/40_EPIC`**
  (8 фаз, поглотил непройденные фазы `plans/34` — там ревизионная шапка) → **`plans/41`**
  (операционный план фазы 1 «Приборы»).
· 🔴 Жёсткие находки: **все 10 222 страницы каталога — сироты** (0 внутренних входящих ссылок,
  дверь только sitemap) · покрытие индекса НЕИЗВЕСТНО (точка «до»: 1 страница) · 63 % страниц без
  рейтинга · по букве политик Google страницы легальны, риск — взвешенный site-wide профиль.
· Сроки органики честно: 1,74 % новых страниц достигают топ-10 за год; первые НЧ-позиции — зима
  2026/27. Крупнейшая поверхность — ТЕСТ — вписана фазой 5 `plans/40` (снят долг п. 4 эстафеты).

## 🗂 Перенесено из STATUS.md ДОСЛОВНО — стрижка бонсая 2026-08-14 (вечер, сессия 2)

> Контекст переноса: вторая сессия дня — эпик «Семантическое ядро 2.0» (`ideas/34` →
> `researches/34` → `plans/40_EPIC` → `plans/41`; интервью №025 закрыто 7/7 в день создания).
> Ниже — записи STATUS, переставшие быть «сейчас».

## 🆕 ЧАТ 2026-08-14 — что сделано (обновление KAIF 2.1 → 2.2)

· Bootstrap-маршрут с базой сравнения бандлов и песочницей — бой совпал с прогнозом файл-в-файл;
  55 модулей сшито переводом в 23 файлах, кириллицы потеряно 0, удвоений 0, тесты **232/232**.
· **Хуки `.kaif/hooks/` ПОДКЛЮЧЕНЫ** (слово владельца в чате) — `.claude/settings.json`, дымовая
  проверка пройдена. ⚠️ Фрагмент поставки нёс недокументированные поля `command`+`args` — подключено
  строкой, расхождение ушло в исток.
· **`AUTHOR_STYLOMETRY.md`**: `git mv` с OWNER_VOICE.md + актуализация до принятого ядра
  `krinik-stylometry` **1.1** (байт-в-байт слепок KAIF, пара в реестре зеркал, ndim вписан
  потребителем в version.json хранилища голоса).
· Полевой отчёт: `reports/KAIF_UPDATES/NDIM_SPACE_KAIF_2.2_UPDATE_REPORT.md` + **issue #13**
  истока. Урок — `EXP-0140`. Коммиты: `c4fd2a6` (обновление), `f26d38d` (STATUS), далее стрижка.

### Из эстафеты §2 «Фаза 5 ideas/30» — закрытые пункты

· ✅ **домашка  — две трети сделаны агентом 2026-08-03** в авторизованной сессии
  владельца: Google принял карту («Файл Sitemap отправлен», 3 авг.), Bing принял (Processing).
  ✅ **Яндекс тоже сделан** по прямому указанию владельца (счётчик 10 → 9). Иконка опознана по радиусу дуг SVG: крестик 0,75, стрелка 6,5. Раньше стояло «БЕЗ подписи вплотную к ✕ «удалить
  карту»; отличить программно нечем, цена ошибки несопоставима ();
· 🔴 **ЧИСЛО «ДО» ПОЛУЧЕНО И ОНО ОТРЕЗВЛЯЕТ: в индексе Google была РОВНО 1 страница.** Все три
  консоли держали чтение карты месячной давности, когда в ней было 1–2 адреса. Это и есть
  точка отсчёта для ;
· 🚀 **`bugs/111` ПОЧИНЕН И ВЫКАЧЕН В БОЙ 2026-08-03** (образ билд 26). Причина: поля
  `stars`/`rates`/`rating` каталога в 2.0 не писал никто — механизм 1.x не перенесли. Владелец
  выбрал чинить механизм, а не снимать число со страниц. Сводка считается тем же проходом, что
  `space/stats` (это и шаг 0 `ideas/28`). `npm run test:calc` **67/67**, три решения доказаны
  мутациями. Бой проверен прибором `node tools/verify-prod-dim-ratings.mjs` — **7/0**:
  🎯 **разрыв РОВНО НОЛЬ** (4064 = 4064; было 31 и росло). ✅ **Сироты вычищены по слову
  владельца:** 15 мёртвых измерений, **25 оценок у 14 человек** удалены механизмом
  `cleanupOrphanRatings` — поиск стоит **0 чтений**, работает только в полном проходе, два
  предохранителя против «каталог прочитался пусто ⇒ мёртвым стало всё», оба доказаны мутациями.
  Топов пересчитано 89, связей 2346 → 2344: математика поправилась там, где несла мёртвые оси;

### Из эстафеты §3 — поверхность «ТЕСТ» (закрыто 2026-08-14: вписана фазой 5 `plans/40`)

### 3. 🆕 Вписать поверхность «ТЕСТ» в метаплан `plans/34`

Крупнейшая достижимая поверхность по замеру, и её в плане до сих пор нет. Дальше — **четыре
макета** (правило четырёх макетов). Наше отличие от 16personalities — честный инструмент против
эффекта Барнума.

### Из «ЧТО ЖДЁТ ВЛАДЕЛЬЦА» — закрытый пункт

7. ~~**`bugs/04` шаг 4** — выкат правил Storage~~ ✅ **СДЕЛАНО 2026-08-01.** «Взгляд в консоль»
   оказался чтением бакета через `gcloud storage ls`: по путям было не пусто, шесть файлов 1.x
   забэкаплены, владелец сказал «отвязываемся» — и выкат перестал быть необратимым.

---

## 🗂 Перенесено из STATUS.md ДОСЛОВНО — стрижка бонсая 2026-08-14, вечер (сессия 5)

> Записи сессий 3 и 4 перестали быть «сейчас»: выкат `plans/39` состоялся в сессии 5, релиз 2.1
> в бою, интервью №026 закрыто. Действующее — в STATUS-шапке сессии 5 и документах.

## 🆕 ЧАТ 2026-08-14 (сессия 4) — языковые адреса всему сайту (`plans/39` шаги 2–5)

· **Переезд:** `git mv` лендинга, `delete-account` и 6 маршрутов «Меню» под
  `src/routes/[lang=lang]/`; entries из общего `langEntries()`; 🔴 язык на переехавших
  страницах — ИЗ `params.lang` (модуль при пререндере отвечал бы «ru» всем, `/en` запёкся бы
  русским). Мост «адрес → память» — ОДНИМ местом, `[lang=lang]/+layout.svelte` ($effect →
  `adoptLangFromPath`; до этого мост был написан, но его никто не звал).
· **Корень `/`** — распознаватель: `csr=false`, noindex, порядок ПИСЬМО (oobCode → `/profile`
  с query — боевой контракт 12.07) → ПАМЯТЬ → БРАУЗЕР; вторая узаконенная до-JS копия чтения
  `ndim-lang` (страж позиционный + 2 мутации). Переключатели ведут на АДРЕС (лендинг —
  ссылки, шапка — `goto(swapLangInPath)`); ссылки личного «Меню» → `/{lang}/menu/…`.
· **Стражи:** 🆕 `verify-lang-addresses` (74, по build + firebase.json, 4 мутации) ·
  `verify-lang-module` вырос 17 → 26 · 🆕 `verify-prod-lang-redirects` (16, по бою; дым на
  до-выкатном бою красен ровно там, где надо) · 🆕 приёмочный `accept-lang-addresses`
  (живой Chromium 14/14 + 10 кадров, смотрены глазами). 13 старых стражей tools/* обновлены.
· 🔴 **Капкан, оплаченный сессией: hosting-эмулятор firebase-tools 15.23 МОЛЧА не применяет
  redirects** (минимальный конфиг из документации: запрос в логе есть, ответ 404; контрольный
  образец — боевые 301 цели `app` — в эмуляторе тоже мёртв). Живая проверка 301 — только бой.
· Прочее: юниты 232 → **234** (swapLangInPath) · e2e **100 passed** (lang-default прошёл
  сквозь распознаватель без правок) · старые письма на голый `/delete-account`: судьба query
  при 301 неизвестна платформе заранее — замер вписан в прод-страж.

## ЧАТ 2026-08-14 (сессия 3, ночь) — фаза 1 «Приборы» закрыта

· Приборы построены и прогнаны: `tools/measure-link-graph.mjs` (граф ссылок по `build/`;
  контроль доказан мутацией) · `tools/console-coverage.mjs` (GSC/Bing/Яндекс, ТОЛЬКО чтение) ·
  `tools/grep-en-boilerplate.mjs` (279 записей со справочным EN-стилем) · жнецы спроса получили
  `--file` (84 RU + 84 EN собраны, контрольная строка сошлась 4 430 ≈ 4 206).
· 🔴 Уточнение разведки: строгих сирот в каталоге 0 (двойники ru↔en ссылаются друг на друга),
  но **все 10 222 страницы недостижимы от корня** — дверь только sitemap (§14).
· 🔴 Первый живой сигнал риска §5.4: **Яндекс удалил 641 `/en/dimension/*` как «малоценные»**
  (6–12.08). Google наоборот: 1 → 994, органика началась (8 кликов, запросы каталожные).
· Вердикты о судьбе каталога НЕ выносились — критерии записаны до данных (`75b02f7`), решает
  фаза 8 после ≥3 замеров.
· 🆕 **/autoloop той же ночью: ФАЗА 3 эпика `ideas/29` ЗАКРЫТА — дом админ-панели** (`plans/33`):
  `/admin` (noindex, вне sitemap, пререндер пуст по данным) · права ТРЁХ состояний в одной точке
  `src/lib/data/admin.ts` (`unknown` ждёт, не приговаривает) · дверь в «Меню» только админу ·
  страж `tools/verify-admin-home.mjs` 17/17, 2 мутации · 232 юнита, правила, e2e 100 passed ·
  судья VERIFIED. Следующая комната эпика — фаза 4 (план пишется при заходе).
· Остальные итерации автолупа: **`bugs/104` закрыт** (карточка связи раскрывается всем верхом,
  цель 82 % → 100 %; портрет чужого — из раскрытой) · **`ideas/26` дозакрыт** (год-прочерк в
  раскрытой карточке `/dims`) · **фаза 5 «ТЕСТ» начата** (`plans/42` + `researches/35`; интервью
  №026 ждёт) · **`ideas/27` шаги 0–1** (инвентарь графики; канон трёх ярусов в `AGENT_GUIDE`;
  находка: полноразмерные персоны лендинга 1,2 МБ — единственный кандидат яруса 2).
· 🔄 **Шейпинг видения владельца (09:06, к №026, НЕ ответ развилки):** NDim ID со временем —
  «постоянная динамически живущая и обновляющаяся анкета, паспорт личности». Дословно — журнал
  `MASTER_PLAN`; поправка рамки — `researches/35` §3 (запрет на Барнум, не на паспорт).
· Ревизия пула: ЧЕТЫРЕ протухших пункта сняты (`EXP-0143`); уроки ночи — `EXP-0142`, `EXP-0143`.

## 🗂 Перенесено из STATUS.md ДОСЛОВНО — стрижка бонсая 2026-08-14 (утро)

> Записи перестали быть «сейчас»: отчёт чата 2026-08-03 (фаза 5 ideas/30 в бою, plans/39 шаг 1,
> контур разноса ответов), блок закрытых интервью №014–№022 с замером каталога, устаревший
> раздел «первым делом» от 30.07 и закрытые пункты пула автономных задач. Решения живут в
> plans/34, plans/36, plans/39, researches/30 и самих интервью; здесь — первоисточник хода событий.

## 🆕 ЧАТ 2026-08-03 — что сделано

**Фаза 5 эпика `ideas/30` (публичные страницы измерений), план `plans/36`:**

🚀 **ФАЗА ЗАКРЫТА И ВЫКАЧЕНА В БОЙ 2026-08-03** (по прямому распоряжению владельца
«перепроверь, затем деплой»). Выкат: `firebase deploy --only hosting`, **20 488 файлов**, обе цели
хостинга. Правила Firestore и Storage сознательно НЕ трогались.

✅ **Бой проверен прибором, а не доверием:** `node tools/verify-prod-dimension-pages.mjs` →
**42 проверки, 0 провалов** на живом `ndimspace.app` (оба языка × три границы правила оценок ·
описания в сыром HTML · карта сайта 10 222 адреса каталога · лендинг цел · личные экраны
по-прежнему под `noindex`).

🔁 **Откат, если понадобится:** Firebase Hosting хранит предыдущие релизы — консоль проекта →
Hosting → предыдущая версия → «Откатить». Кода это не касается.

| Что | Замер |
|---|---|
| Страниц | **10 222** = 5111 × 2 языка (`/ru/…`, `/en/…`) |
| Сборка | **~50 с** · **194 МБ** (после двух облегчений за день) |
| Клиентский бандл | **1,3 МБ** |
| Карта сайта | **10 233 адреса**, 0,83 МБ |
| Стражи | `verify-dimension-pages` **24/0** (7 мутаций) · по бою **42/0** · `e2e` **98** (было 80) · `npm test` **230** · `npm run check` **0/0** · `verify-lang-module` **17/0** |
| Приёмка | 32 кадра живым Chrome: обе темы × две ширины × 4 граничных случая |

🎯 **Главный приз эпика взят: английский ЕСТЬ в сыром HTML.** До фазы его не было ни знака
(`researches/26` §1.2) — теперь 5111 английских страниц с описаниями, `<html lang="en">`,
двусторонним `hreflang` и `x-default` на английский.

🔴 **Оценка хранилища была неверна вчетверо:** шаг 0 обещал ~126 релизов при 79 МБ. Правда — сначала
**322 МБ**, после двух облегчений **194 МБ** ⇒ **~52 релиза** из бесплатных 10 ГБ. Чистка старых
релизов переезжает в регламент выката.

⚡ **Страница каталога не грузит ни байта JavaScript** (): 25,9 → **16,2 КБ** по бою.
🔴 И здесь прогноз агента был неверен ДВАЖДЫ:  от  не исчезают вовсе
(выключателя у SvelteKit нет), а раздуты они были моим же загрузчиком — он отдавал весь
двуязычный объект каталога. Удалять их шагом сборки НЕЛЬЗЯ: клиентский переход сюда получит 404.

🔴 **Дефект, найденный и вылеченный внутри фазы — `EXP-0136`:** универсальный `+page.ts` вшил
**весь каталог (16,86 МБ) в клиентский бандл**. При этом 24 проверки стража были ЗЕЛЁНЫМИ —
дефект нашёл страж ЖАРГОНА по случайному совпадению слов. Отсюда новые проверки на ВЕС сборки.

| Шаг | Состояние | Доказательство |
|---|---|---|
| 1. Адреса-слаги | ✅ | `dim-slug.ts` + 13 тестов, **3 мутации убиты** |
| 2. Канал «каталог → сборка» | ✅ | срез в git / полный вне git; два прогона → **побайтово тот же файл** |
| 3. Страница **V2 «Досье»** | ✅ | интервью №023: **В1 = Г** (рекомендация агента была другой), **В2 = А** |
| 4. Карта сайта | ✅ | 10 233 адреса, `canonical` на каждой |
| 5. Стражи | ✅ | 24 проверки, 7 мутаций |
| 6. Приёмка живым Chrome | ✅ | 32 кадра, `test-results/dimension-pages/` |

🔴 **Дважды за сессию мутация ВЫЖИВАЛА**, и оба раза по одной причине: граничный случай был взят
наугад, а не сконструирован (разрез слага не попадал на пробел; `Infinity * 0` — это NaN, а
защиту отличает только `Infinity`). **Граничный случай надо строить, а не брать пример.**

**Дыра в KAIF — найдена, замерена, залечена, отправлена наверх** (заказ владельца):
- **Симптом:** ответ владельца Р5 интервью №010 (2026-08-01) два дня не доезжал до `plans/24`;
  тот держал вопрос открытым И риск «флип корня», которого при этом ответе нет. Свежая сессия
  собиралась спросить заново — остановил владелец: «*странно, что это не записано!*»
- **Замер по ТРЁМ проектам KAIF:** **124 из 227 отвеченных вопросов (55 %)** не имеют ни одной
  ссылки вне `interviews/` — NDim 30 %, KLAS 71 %, Unliminium 91 %. Интервью-сирот при этом **ноль**:
  единица разноса — **вопрос**, а не интервью, и на уровне файлов дефект невидим.
- **Лечение механизмом:** `npm run questions` вырос третьей секцией «РАЗНОС ОТВЕТОВ» с базовой
  линией `tools/propagation-baseline.json` (36 унаследованных) — краснеет только на новых,
  проверено мутацией. Канон — `AGENT_GUIDE.md` → «Дефект в САМОМ KAIF» и «Ответ владельца обязан
  ДОЕХАТЬ». Урок — `EXP-0134`.
- **Наверх:** `KAIF/ideas/ai_agents_reports/25_answer_propagation_gap_field_report_from_ndim.md`
  + пункты **1.9–1.11** в `KAIF/ideas/20_kaif_2.2_scope.md`.
  🔴 KLAS и Unliminium — **только читать** (прямое слово владельца).

**`bugs/112` (закрыт):** машинная метка контура протекала владельцу на страницу видимым мусором, а
время писалось в UTC. Теперь под каждым ответом видимая отметка **местным** временем, на карточке
вопроса — тоже. Контур: **132 проверки** (было 118), **4 мутации убиты**.

**Разнесено по документам:** `plans/24` — ответ Р5 = В вписан в четыре места, снят устаревший риск
«флип корня» и выведенный из него порядок фаз («флип последним» — пункт отменён).

✅ **Интервью №018 ЗАКРЫТО в тот же день — все шесть ответов, через страницу вычитки:**
· **В1 = А** — частотность запросов снимает **сам владелец** по инструкции агента;
· **В2 = А и В** — «вера» это вера **в людей** + «мир устроен по законам». 🔴 **Религиозного
  прочтения НЕТ** — развилка закрыта, конфессиональной рамки у продукта не будет;
· **В3 = «нужно обсудить»** + четыре встречных вопроса → брифинг **`interviews/interview_019`**;
· **В4** — разрешены все три формы обмена, но 🔴 «**личных данных не раскрываем, цифр метрик
  похожести не раскрываем**». Это СТРОЖЕ рекомендации агента и стало инвариантом `plans/34`;
· **В5 = В** — 🔴 **эксперимента над текстами главной НЕ БУДЕТ**: «не экспериментируем, а делаем на
  основании нашего видения и с учётом данных об реальных SEO зацепках». Фаза 7 переписана,
  прибор притока остался, сравнение версий удалено;
· **В6 = А** — записанный **заранее** ответ на опровержение гипотезы подтверждён.

🔴 **ЗАМЕР КАТАЛОГА СНЯТ — и он перевернул эпик** (`researches/30`, приложение A). Владелец поймал
крупную ошибку агента: «*ты сильно неправ… у каждого измерения есть очень большое описание*».
Он прав. Я читал `dims_list` — **сжатую проекцию** `{ru, en, year}` — и принял её за само измерение
(урок `EXP-0129`). Замер по бою, 5111 документов:
· описание — **медиана 1047 знаков RU и 1026 EN**, без описания **ноль** из выборки 120;
· корпус ≈ **5,35 млн знаков RU + 5,24 млн EN** против ~50 000 открытых сегодня — **×100**;
· 🔑 **это первый английский текст, который увидит робот** (`researches/26` §1.2: сегодня в
  отдаваемом HTML английского нет ни знака, а решение «EN основной» упиралось именно в это);
· гистограмма: **63,4 % измерений без единой оценки**, максимум **14** голосов, с ≥20 — **ноль**.
  Вопрос «какой выбрать порог N» **снят данными**: средняя оценка при нашем размере базы как
  содержание не работает вообще.
**Следствие:** фаза 5 `plans/34` из последней и заблокированной стала **крупнейшей победой эпика** и
зависимости от `plans/30` больше не имеет.
🐞 Попутно заведён **`bugs/111`** — сумма `rates` (4058) не сходится с `space/stats.ratings` (4089),
разница 31. И найдена грязь в поле «вид»: `фильм` строчной 21 шт., `Movie` 3 шт., один раз в поле
вида записан год.

✅ **Интервью №019 закрыто.** В1 — поправка владельца (см. выше). В2 — его слова о спецслужбах:
«*если ко мне придут… я буду ВЫНУЖДЕН способствовать расследованию*». Это верно, и моя рекомендация
«данные не передаются третьим лицам» была неверна как абсолют. Проблема текста 2.2 не в оговорке, а
в её ширине: сегодня он разрешает передачу «при необходимости, определяемой автором в одностороннем
порядке» — то есть что угодно, включая продажу.

✅ **Интервью №020, №021 и №022 закрыты в тот же вечер — форма страниц каталога определена целиком.**
Итоговое правило (записано в `plans/34`, фаза 5):
· **страница у ВСЕХ 5111 измерений**, весь корпус;
· **звёзды** при `rates` ≥ 1 (1873 шт.), **серый текст «ещё без голосов»** при нуле (3238 шт.) —
  то же правило, что владелец принял для экрана внутри (`ideas/26`): одно внутри и снаружи;
· **число проголосовавших** — 🔄 **при ЛЮБОМ числе оценивших** (решение владельца 2026-08-03
  ОТМЕНЯЕТ интервью №022 В3: «открываем показ оценок при любом числе оценивших везде по
  приложению»). Прежний порог ≥ 3 давал счётчик лишь у 526 из 1873 и защищал не личность, а
  впечатление от числа — разбор `bugs/113`. 🔴 **Перед следующим выкатом сайта помнить: публичные
  страницы пререндерены, число появится только после пересборки и деплоя;**
· описания — его текст, писанный с ИИ по открытым источникам ⇒ публиковать можно;
· подпись человека без имени — **номер из UID** («Участник Пространства №4718»), только как
  подпись; сквозная нумерация отвергнута: «Участник №94» сообщал бы, что нас меньше сотни.
🔴 **Путь к этому правилу стоит помнить:** первая редакция владельца («только 0 и >3 оценок») давала
3535 страниц и **мигающие адреса** — измерение, получив первый голос, обязано было исчезнуть из
поиска. Агент назвал механику до кода и вынес развилкой, а не исправил молча. Итог: **+1576 страниц
и +1,65 млн знаков при той же приватности**.
⚖️ **Юридический ответ по Email** (его вопрос): права показывать почту нет — ст. 4(1) GDPR (почта
это ПДн и без имени), ст. 25(2) (по умолчанию ПДн не становятся доступны неопределённому кругу лиц
без действия человека), ст. 6 («нечего показывать» основанием не является). ✅ **Но в 2.0 нарушения
НЕТ — проверено кодом:** почты нет в `UserRootDoc` вовсе, в строке топа связей её нет, человек без
имени показывается как «Без имени». Править надо **документ**, а не продукт: пункт 2.3 описывает 1.x.
Пункт 2.2 Политики сужается до мысли владельца о законных требованиях (его ответ А) — разбор за агентом.

📜 **Закрытое интервью №019** (брифинг по В3 + риск, найденный попутно).
Две развилки: открывать ли страницы измерений и в каком виде · что делать с пунктами **2.2 и 2.3 «Политики конфиденциальности»**.
🔴 **Про 2.2/2.3 — это находка, которая крупнее самого вопроса.** В нашей ОПУБЛИКОВАННОЙ «Политике конфиденциальности»
(текст 1.x) стоит: «*пользовательские данные могут быть переданы третьим лицам… без уведомления
Пользователя*» и «*Email адреса в Системе являются публичной общедоступной информацией*». Первое
противоречит позиционированию «Ваши данные не товар» и не является правовым основанием под GDPR;
второе просто **неверно для 2.0**. И всё это **уже открыто поиску** с 2026-08-01.
⚠️ **Работа не стоит:** замер каталога (`plans/35`, шаги 0–3) автономен.

**Прочие вопросы владельцу — ноль.** Все 17 предыдущих интервью отвечены. №015 (шесть развилок эпика
`ideas/29`) заведено и закрыто 2026-08-02, ответы разнесены — **волна эпика разблокирована целиком**:
· **В1 = Б** — панель РАЗДЕЛОМ ВНУТРИ приложения (не отдельный сайт), плюс два его условия:
  **не индексируется** и **редиректит не-админа на главную**;
· **В2 = А** — Wikidata как единственный реестр (TMDB с его атрибуцией отклонён);
· **В3 = А** — агент **НИКОГДА** не заводит измерение без вычитки. Инвариант, не настройка;
· **В4 = В** — только `title`, поле `name` **вычистить у всех 5111** («легаси не поддерживаем»);
· **В5** — первая комната панели = **кандидаты от агента**; заявки людей читает **сам агент** и
  выносит в ту же очередь с пилюлей «по заявке»; 🔴 статусы вычитки **в продукт не просачиваются**;
· **В6 = А** — запуск агента локальным скиллом KAIF.

№014 (три развилки замера боя) закрыто в тот же день:
· **В1 = В** — строка притока на лендинге станет ВЫВОДИМОЙ из снимков дней (`bugs/107`);
· **В2 = В** — снимки дней переписать И оставить след правки (`bugs/106` шаг 3);
· **В3 = А** — агрегат вершин пока не показывать (`plans/28` §8 шаг 4).
🟡 **Ждёт ответов — интервью №016 (реестр корпуса для портрета голоса).** Пять вопросов о
источниках, которых агент знать не может: тексты того же жанра вне NDim · на чём владелец учился
писать · ранние тексты · что брать из руководства 1.x · каким регистром писать лендинг.
Страница поднята. ⚠️ Работа НЕ стоит: портрет уже написан по тому, что есть в проекте.

✅ **Вычитка `plans/29` состоялась 2026-08-02** — вердикт «принять, но с правками». Разбор
ответов и что из них следует — **`plans/29` §3а**. Ворота фазы 2 эпика `plans/21` НЕ закрыты.
⚠️ Два урока этой вычитки, оплаченные дорого:
· `EXP-0125` — **назвать задачу владельцу не значит подать её.** Три вопроса в `plans/29` лежали
  ПРОЗОЙ, и ответить в один клик было физически нельзя. Строка «за владельцем» обязана идти вместе
  с ПОДНЯТОЙ страницей, а документ — быть кликабельным ДО доклада.
· 🔴 **Портрета голоса владельца в проекте не было вовсе** — он отвёл текст словами «вот это не мой
  язык, твоя стилометрия моего стиля должна была это красным цветом подсветить», а стилометрии
  здесь никогда не строилось. Теперь есть: **`AUTHOR_STYLOMETRY.md`** — с 2026-08-14 это
  публичный слепок ПРИНЯТОГО ядра `krinik-stylometry` 1.1 (151 правило; локальный черновик
  2026-08-02 — в истории git этого же файла). Это не «вопрос владельцу» в смысле стража, а
документ на построчное утверждение; внутри него три развилки, которые агент решать не вправе
(две Ваши собственные строки из 1.x и голос английского). Открывать страницей, не спрашивать в чате.
Унаследованный долг — 26 очередей вне `interviews/`, число обязано убывать.

### Устаревший раздел «Первым делом» (актуален был на 30.07)

### Первым делом — что запустить свежей сессии

```bash
npm run stand                     # эмуляторы + сервер синхронизации + vite (нужна Java)
node tools/verify-plan08.mjs      # 60 — память вида экранов, самое свежее и самое сквозное
node tools/verify-tierA.mjs       # досье связи целиком, включая новую строку bugs/46
cat interviews/interview_007_ideas21_forks.md   # ответы владельца по волне
```

### Закрытые пункты пула автономных задач (перенесены 2026-08-14)

- [x] **`ideas/24`, фаза 1** — лестница построена, замер снят, макет **V4 утверждён владельцем**.
- [x] **`ideas/24`, фаза 2 — «Шаг виджета» в бою** (выкачено 2026-07-31).
- [x] **`plans/13`, фаза 1 — обзор индустрии по загрузке фото** → `researches/23` (2026-07-31).
- [x] **Эпик `plans/15`, фазы 2–5 и 8** — конфигурация, макеты, экран `/account`, смена почты,
      удаление аккаунта. Закрыты 2026-08-01.
- [x] 🚪 **Фаза 7 «Ворота», первый шаг — снять `noindex` с документов** — ✅ **СДЕЛАНО**
      (`DocShell.svelte:44–59`, поиску открыто 2 страницы → 11, текста ×13.5; `e2e/seo` 10/10 +
      мутация). Чекбокс висел незакрытым — снят при возобновлении 2026-08-01.
- [x] 🐞 **`bugs/105` — «Как меня видят» в рабочей области** — ✅ **СДЕЛАНО 2026-08-02**
      (`bugs/105_DONE_…`, страж `verify-bug105` 56/0). Инвариант `bugs/76` жив и проверен.
- [x] 🗂 **ЭПИК `ideas/29` — ПЛАНИРОВАНИЕ ЗАКРЫТО 2026-08-02.** Лестница пройдена целиком:
      `researches/29` → `plans/30` (метаплан, 7 фаз) → `plans/31` (операционный план фазы 1) →
      `interviews/interview_015` (6 развилок, **все отвечены в тот же день**).
- [x] ✅ **ЭПИК `ideas/29`, ФАЗА 1 — СДЕЛАНА И ВЫКАЧЕНА В БОЙ 2026-08-02.** `diameter` 714,9,
      `dims` 5111, сервер билд 24. Закрыла `bugs/106`, `bugs/108`, `bugs/109` одной правкой.
- [x] ✅ **ЭПИК `ideas/29`, ФАЗА 2 — ЗАКРЫТА:** владелец выбрал **V1 «Стопка»** (`plans/32`).

## 🗂 Перенесено из STATUS.md ДОСЛОВНО — стрижка бонсая 2026-08-03

> Записи перестали быть «сейчас»: это отчёт чата 2026-08-02 (ночь) о планировании эпика
> `ideas/30` и блок того же дня о вечернем эпике `ideas/29`, включая разбор интервью №018–022.
> Решения из них живут в `plans/34`, `plans/36`, `researches/30` и в самих интервью; здесь —
> первоисточник хода событий.

## Что сделал этот чат

| Что | Где | Доказательство |
|---|---|---|
| Документ владельца закоммичен дословно | `ideas/30` | правило 16, отдельный коммит до правок |
| Зонтичный обзор написан | **`researches/30`** | 30+ первоисточников с DOI · уровень доказательности у КАЖДОГО утверждения · PICO · GRADE-таблица |
| Метаплан эпика | **`plans/34`** | 8 фаз · ворота · 10 критериев приёмки · границы с 4 соседними эпиками |
| Операционный план фазы 1 | **`plans/35`** | замер каталога (автономен) + замер спроса (ждёт владельца) |
| Развилки: **пять интервью заведены и ЗАКРЫТЫ за вечер** | `interviews/018`–`022` | 21 вопрос, все отвечены и разнесены |
| Дорожная карта внесена в мастер-план | `MASTER_PLAN.md` фаза 7 + 3 записи в журнале решений | прямой пункт заказа владельца |
| **Фаза 1 не только спланирована, а СДЕЛАНА** | `researches/30` приложение A | 5111 боевых документов прочитано |
| Список запросов + инструкция владельцу | `researches/30` приложение B · **`homeworks/08`** | ~120 запросов + 5 шаблонов, ни одной выдуманной частотности |
| Разбор Политики конфиденциальности | **`researches/31`** | построчно «что написано / что делает код» |
| **Русский спрос ЗАМЕРЕН** | `researches/30` прил. C · `30_demand_ru.json` | 82 запроса + **3 948 реальных запросов людей** из API Вордстата |
| **Английский спрос ЗАМЕРЕН** | `researches/30` прил. D · `30_demand_en.json` | 99 запросов через Bing, записей с ошибкой ноль |
| План фазы 5 + **шаг 0 выполнен** | **`plans/36`** | 5111 страниц: 17,1 с, 78,8 МБ, 15,8 КБ/стр., слаг-коллизий ноль |
| Прототип страницы измерения | `src/routes/dimension/[slug]/` | описание в СЫРОМ HTML; правило оценок в одном месте |
| Развилка «повод против человека» | **`ideas/31`** | «events near me» 14,9 млн против «people near me» **81** |

**Машинерия, которой раньше не было** (вся работает в авторизованной сессии владельца через
Chrome с портом отладки 9222, не логинится и паролей не знает):

| Инструмент | Что делает |
|---|---|
| `tools/cdp-discover.mjs` | разведка чужой страницы: есть ли у неё свой API и что он отдаёт |
| `tools/wordstat-harvest.mjs` | русский спрос из API Вордстата (`totalValue`) + попутно реальные запросы людей |
| `tools/bing-keywords.mjs` | английский спрос из Bing Webmaster; **`topsearchurls` показывает, КТО ранжируется** |
| `tools/fetch-dims-slice.mjs` | каталог → сборка, детерминированный слаг, страж коллизий |
| `tools/search-queries.json` | машиночитаемый список запросов по группам намерения |

🔑 **Четыре вывода этого захода, которые нельзя переоткрывать:**
1. **«SEO-ядра» как подсистемы не существует.** Четыре поверхности и **один двигатель —
   распространение**. Негатив кликают (+2,3 % CTR за слово, РКИ на 370 млн показов), благоговением
   **делятся**. `GOAL.md` закрывает первый путь; знание, что он работает, — **не разрешение**.
2. **Гиганты из заказа SEO-ядра не имеют** (Tinder 67,3 % прямого трафика, Letterboxd 67,8 %).
   Аналог NDim — **16personalities**: органика 55,4 %, 81 389 ключей, двигатель = бесплатный
   инструмент + **десятки** страниц с содержанием.
3. 🔑 **Поправка владельца про «ключ СЕКС» получила научное имя.** Fisher (2002): три системы мозга.
   Индустрия торгует **вожделением**; определение **влечения** — «выбирать среди партнёров и
   предпочитать конкретных, экономя время и силы» — есть описание функции NDim. Благородная форма
   оказалась **незанятой нишей**, а не жертвой. Мета-анализ 78 работ: пошлый ключ бренду не помогает.
4. ⛔ **«Страница на каждое из 5111 измерений» = doorway abuse** и не обеспечена данными
   (4089 оценок на 5111 измерений).

🔴 **И пятый вывод — самый дорогой, потому что он про мою ошибку.** Пункт 4 выше («страницам нечем
быть») **опровергнут владельцем в тот же вечер**: у каждого измерения есть описание на 1000+ знаков
на ДВУХ языках. Я читал `dims_list` — сжатую проекцию `{ru, en, year}` — и принял её за само
измерение (`EXP-0129`). Правильная картина: **корпус в базе ×100 больше того, что открыто поиску**,
и он уже двуязычный. Фаза 5 из последней и заблокированной стала крупнейшей победой эпика.

### 🎓 Пять уроков этого чата — все оплачены ошибками, все записаны

| Урок | Что стоило |
|---|---|
| `EXP-0129` **Проекция — не сущность** | три документа с выводом «публиковать нечем»; владелец поправил одной фразой |
| `EXP-0130` **Проза после «Ответ:» съедает вопрос** | контур отчитался «ждут 2» при трёх заданных — вопрос не дошёл бы до владельца |
| `EXP-0131` **Гаси по СВОЕМУ документу, не по имени инструмента** | убил свою страницу дважды; чужую (Unliminium) не убил по случайности |
| `EXP-0132` **Ноль — это ВЫВОД, а не результат обработки ошибки** | 16 записей уехали как «спроса нет», хотя это был сбой сервиса |
| `EXP-0133` **Поправка относится к КЛАССУ, а не к экземпляру** | владелец поправил про язык — я применил к русскому и повторил ошибку в английском |

---

**Предыдущее обновление:** 2026-08-02, вечер — 🗂 **ЭПИК `ideas/29`: ЛЕСТНИЦА
ПРОЙДЕНА, ФАЗА 1 В БОЮ, ФАЗА 2 ЗАКРЫТА.**

**Что этот чат сделал** (прямое распоряжение владельца — взять `ideas/29` в планирование):

| Что | Где | Доказательство |
|---|---|---|
| Лестница `/plan-epic` пройдена целиком | `researches/29` → `plans/30` → `plans/31` | 25 первоисточников · 7 фаз · план фазы 1 |
| Шесть развилок владельца — заданы и отвечены в тот же день | `interviews/interview_015` | В1=Б · В2=А · В3=А · В4=В · В5 свой · В6=А |
| 🚀 **Фаза 1 выкачена в БОЙ** | `calculator/index.mjs` | `diameter` **714,9**, `dims` **5111**; сервер билд 20 → **24** |
| Три бага закрыты одной правкой | `bugs/106`, `bugs/108`, `bugs/109` | `dims_index.test.mjs` 6/6 (до фикса 5 провалов из 6) |
| Контур вычитки больше не съедает ответы | `bugs/110` | `verify-bug110` **20/20**, две адресные мутации |
| Портрет голоса владельца снят | `OWNER_VOICE.md` | язык подтверждён им лично; документ — черновик |
| Фаза 2 закрыта: выбран **V1 «Стопка»** | `plans/32`, `design/dims-manager-mockups.html` | 16 кадров, 2 дефекта исправлены до показа |
| План фазы 3 написан | `plans/33` | не начат |

🔴 **Четыре урока этого дня, оплаченные дорого:**
1. **Назвать задачу владельцу — не значит подать её** (`EXP-0125`). Строка «за владельцем» обязана
   идти вместе с ПОДНЯТОЙ страницей, а документ — быть кликабельным ДО доклада.
2. **Дублирующий звонок дороже пропущенного** (`EXP-0126`). Страницу поднимать ОДНОЙ фоновой
   задачей; жива и без ответов — гасить и поднимать МОЛЧА (`--no-signal`).
3. **Бой простоял 5 минут по вине агента** (`EXP-0127`): Git Bash подменил `/secrets/sa.json` на
   `C:/Program Files/Git/secrets/…`. **`docker ps` показывал `Up`** — «Up» не признак работы,
   единственный признак — ЛОГ первого цикла. Команды `docker` гнать из PowerShell.
4. 🔑 **Похожий голос делает выдуманное содержание ОПАСНЕЕ** (`EXP-0128`). Владелец: «язык похож на
   мой, а содержание выдумано». Портрет даёт право писать ЕГО ГОЛОСОМ, но не ЗА НЕГО.

> 📜 Подробности этого дня и исполненный блок «НА ЗАВТРА» перенесены ДОСЛОВНО в
> `PROJECT_HISTORY.md` — стрижка бонсая 2026-08-02, вечер.

---

### 🗣 Контур вычитки владельца — им задаются ВСЕ вопросы

Всё, что агент хочет ОТ владельца, открывается **HTML-страницей с ответом в один клик**; ответ
механически возвращается в md с `by`/`at`. Критерий приёмки регламента уже выполнен — владелец
отвечает через неё **без единого уточняющего вопроса в чате** (интервью №011–№014).
Устройство и история — `plans/27` и `PROJECT_HISTORY.md`.

| Команда | Зачем |
|---|---|
| `npm run questions` | 🔴 **прогоняй в начале сессии и перед сдачей.** Вопросы владельцу вне `interviews/` + все интервью, ждущие ответа |
| `npm run review -- open interviews/<файл>.md` | открыть страницу, позвать владельца голосом |
| `node tools/verify-owner-reviews.mjs` | QA-прогон контура — **97 проверок**, стенд не нужен |

🔑 **«Сохранить» БУДИТ агента** — слово владельца: агент узнаёт о событии по завершению процесса,
поэтому любое сохранение закрывает контур. Осталось неотвеченное — поднять страницу заново обязан
АГЕНТ, а не владелец держать вкладку. Вид карточек — его же выбор (интервью №012, V1 «Полоса
слева», цветом состояния).

📤 **Отчёт наружу — `researches/28`** (заказ владельца): полевой отчёт по этой реализации для
разработчика KAIF **и** для агента на другом проекте. Часть I — что формализовать в регламенте
(главное: инварианта «сохранение будит ждущего» в нём НЕТ), часть II — исполнимый контракт «как
построить то же самое». Ссылка на него вписана в `D:\work\ai_sandbox\KAIF\ideas\20_kaif_2.2_scope.md`
п. 1.6 — там уже стоял запрос ровно на это.

**Что страж нашёл в первый же день:** интервью №007 два дня числилось «🔴 ЖДЁТ ОТВЕТОВ» при
двенадцати заполненных ответах · разбор считал `---` после пустого поля «Ответ:» **ответом**, из-за
чего по всем десяти живым интервью выходило «0 без ответа» · В10 интервью №008 вечно числился
открытым, хотя снят отменой паролей. Всё исправлено. **Унаследованный долг — 26 очередей вопросов
вне `interviews/`** (`tools/questions-baseline.json`): страж краснеет только на НОВЫХ, а это число
обязано убывать. Уроки — `EXP-0116…0118`.

### ✅ Bing Webmaster: сайт НА МЕСТЕ — тревога была ложной, и это стоит помнить

Разведка 2026-08-02 сначала показала пустой аккаунт (`kotkrinik@yandex.ru`, `UserSites: []`,
создан тем же входом) — я чуть не завёл это дефектом «`homeworks/04` врёт». **Вторая гипотеза
оказалась верной:** под `kotkrinik@gmail.com` сайт есть — `ndimspace.app`, роль **Owner**,
подтверждён как ДОМЕН, аккаунт создан **2026-07-11 13:37**, то есть ровно в дату домашки.

🔑 **Урок, который дороже находки:** у одного человека несколько аккаунтов, и «пусто» в одном из
них — это не «пусто везде». Прежде чем объявлять запись документа неправдой, **проверь другой вход**.

📌 **Полезное следствие:** Bing Webmaster даёт **бесплатный точный** замер спроса (Google без
рекламных трат отдаёт только вилки) и вдобавок отвечает на вопрос, которого у нас не было вовсе, —
`topsearchurls` показывает, **кто реально ранжируется** по запросу. Инструмент — `tools/bing-keywords.mjs`.


## 2026-08-02, вечер — ЭПИК `ideas/29`: лестница пройдена, фаза 1 в бою, фаза 2 закрыта

> Перенесено ДОСЛОВНО из `STATUS.md` стрижкой бонсая 2026-08-02, ночь.

**Что тот чат сделал** (прямое распоряжение владельца — взять `ideas/29` в планирование):

| Что | Где | Доказательство |
|---|---|---|
| Лестница `/plan-epic` пройдена целиком | `researches/29` → `plans/30` → `plans/31` | 25 первоисточников · 7 фаз · план фазы 1 |
| Шесть развилок владельца — заданы и отвечены в тот же день | `interviews/interview_015` | В1=Б · В2=А · В3=А · В4=В · В5 свой · В6=А |
| 🚀 **Фаза 1 выкачена в БОЙ** | `calculator/index.mjs` | `diameter` **714,9**, `dims` **5111**; сервер билд 20 → **24** |
| Три бага закрыты одной правкой | `bugs/106`, `bugs/108`, `bugs/109` | `dims_index.test.mjs` 6/6 (до фикса 5 провалов из 6) |
| Контур вычитки больше не съедает ответы | `bugs/110` | `verify-bug110` **20/20**, две адресные мутации |
| Портрет голоса владельца снят | `OWNER_VOICE.md` | язык подтверждён им лично; документ — черновик |
| Фаза 2 закрыта: выбран **V1 «Стопка»** | `plans/32`, `design/dims-manager-mockups.html` | 16 кадров, 2 дефекта исправлены до показа |
| План фазы 3 написан | `plans/33` | не начат |

🔴 **Четыре урока того дня, оплаченные дорого:**
1. **Назвать задачу владельцу — не значит подать её** (`EXP-0125`).
2. **Дублирующий звонок дороже пропущенного** (`EXP-0126`).
3. **Бой простоял 5 минут по вине агента** (`EXP-0127`): «Up» не признак работы, единственный признак — ЛОГ первого цикла.
4. 🔑 **Похожий голос делает выдуманное содержание ОПАСНЕЕ** (`EXP-0128`).

---
## 2026-08-02 — день эпика `ideas/29`: планирование, фаза 1 в бой, фаза 2 закрыта

> Перенесено ДОСЛОВНО из `STATUS.md` при завершении чата (стрижка бонсая). В конце записи — блок
> «НА ЗАВТРА», написанный ДО того, как эта работа была сделана: он исполнен целиком, кроме
> пунктов, уехавших в свежую эстафету.

### Что чат сделал

| Что | Где | Доказательство |
|---|---|---|
| Лестница `/plan-epic` для `ideas/29` пройдена целиком | `researches/29` → `plans/30` → `plans/31` | 25 первоисточников, 7 фаз, операционный план фазы 1 |
| Шесть развилок владельца заданы и отвечены в тот же день | `interviews/interview_015` | В1=Б · В2=А · В3=А · В4=В · В5 свой ответ · В6=А |
| Фаза 1 — экономный индекс каталога — сделана и **выкачена в бой** | `calculator/index.mjs`, `calculator/dims_index.test.mjs` | 6/6 (до фикса 5 провалов из 6) · `test:calc` 59/59 · мутация адресная |
| Три бага закрыты одной правкой и одним выкатом | `bugs/106`, `bugs/108`, `bugs/109` | в бою `diameter` 714,9 и `dims` 5111; сервер билд 20 → 24 |
| Контур вычитки перестал съедать ответы владельца | `bugs/110`, `tools/verify-bug110.mjs` | 20/20 живым Chromium, ДВЕ адресные мутации, взяты все пять мер источника |
| Портрет голоса владельца снят | `OWNER_VOICE.md`, `interviews/016`, `017` | 13 правил с цитатами, 26 оборотов, 15 маркеров анти-портрета |
| Фаза 2 — четыре макета «Менеджера измерений», выбран **V1 «Стопка»** | `design/dims-manager-mockups.html`, `plans/32` | 16 кадров сняты, 2 дефекта исправлены до показа |
| Операционный план фазы 3 | `plans/33` | три состояния прав вместо двух |

### Уроки, оплаченные этим днём

`EXP-0125` назвать задачу владельцу не значит подать её · `EXP-0126` дублирующий звонок дороже
пропущенного · `EXP-0127` бой простоял 5 минут из-за подмены пути в Git Bash, «Up» не признак
работы · `EXP-0128` похожий голос делает выдуманное содержание опаснее, а не безопаснее.

### Блок «НА ЗАВТРА», исполненный этим же днём (дословно)

### ☀️ НА ЗАВТРА (2026-08-03) — что делать первым, по порядку

> Написано для сессии с пустым контекстом. Пункт 1 — прямой заказ владельца, остальное по порядку
> волны `plans/26`.

1. ✅ **ФАЗА 1 ЭПИКА `ideas/29` СДЕЛАНА И ВЫКАЧЕНА В БОЙ 2026-08-02.**
   Подтверждено боем: `diameter` **714,9**, `dims` **5111** (было 715 и 5112). Сервер
   синхронизации — **билд 24** (был 20); приложение выкачено на обе площадки.
   ⚠️ Снимки дней ДО 2026-08-02 остались с 5112 — их правит отдельный инструмент (решение
   владельца В2=В, интервью №014), он ещё не написан.
   Закрыты одной правкой: `bugs/106` (5112 → 5111, диаметр 714,9) · `bugs/108` (вечная
   пересборка) · `bugs/109` (новые измерения в бою не находились). Плюс попутно найден и
   починен дефект, ронявший ВЕСЬ цикл записи на измерении без названия.
   Доказано: `calculator/dims_index.test.mjs` 6/6 (до фикса 5 провалов из 6) · `test:calc` 59/59 ·
   `npm test` 199/199 · мутация адресная. Подробности — `plans/31`.
   ⛔ **Следующая фаза — 2 (четыре макета «Менеджера измерений»), она уже разблокирована.**

<details><summary>Что было в этом пункте до исполнения</summary>

   🔧 **ФАЗА 1 ЭПИКА `ideas/29`: экономный индекс каталога.**
   ✅ Планирование закрыто 2026-08-02 (лестница пройдена целиком). Исполнять по
   **`plans/31_admin_epic_phase1_index_economy.md`** — шаги готовы, решений владельца НЕ требует,
   клиент не трогается.
   Что фаза делает разом: обезвреживает `bugs/108` (вечная пересборка) · переводит индекс на
   ДЕЛЬТУ вместо полной пересборки (ответ на вопрос владельца «можно ли лучше») · чинит `bugs/106`
   (5112 → 5111, диаметр 714,9) · подтверждает и чинит `bugs/109` · делает сид честным.
   🔴 **Первый шаг — ЗАМЕР, а не код**: воспроизвести мину на стенде и снять числа «до». Не
   воспроизвелась — переписать гипотезу, а не «чинить на всякий случай» (обе находки помечены
   `[NOT-TESTED]` честно).
   🔧 На выходе — **рука владельца**: `npm run calc:image` и выкат. Агент не выкатывает.
   🔗 Карта эпика — `plans/30` (7 фаз), доказательная база — `researches/29`.
</details>

2. 📝 **`plans/29` — тексты ворот: ВТОРОЙ ЗАХОД.** Первый состоялся 2026-08-02, вердикт —
   «принять, но с правками». Разбор — `plans/29` §3а. Что переделывать:
   · подзаголовок 1.x **возвращается целиком** (его слово «оставляем»);
   · **термины на витрине раскрывать, а не бросать голыми** — «5 111 измерений» нарушение,
     «5 111 объектов человеческой культуры, по которым…» норма;
   · полоса чисел становится **БЛОКОМ** с зелёным статусом и буллет-списком (форма — макеты, фаза 3);
   · якорь меняет слово: не «калькулятор», а «точный, честный и быстрый беспристрастный
     суперкомпьютер». ⚠️ Из четырёх эпитетов под замером нет только **«быстрый»**;
   · английский — В3=А: рабочий перевод мой, вычитка его.
   🔴 **Переписывать ТОЛЬКО по `OWNER_VOICE.md`** — иначе третий заход по тому же кругу.
   ⚠️ **Пока текст не утверждён — макеты не рисовать** (`plans/21` фаза 2 → 3: «макет с рыбой
   неотличим от макета с рыбой»).

3. 🔧 **За владельцем — ПЕРЕСБОРКА ОБРАЗА** (`npm run calc:image`). Без неё агрегат вершин
   (`bestMatch`) остаётся стендовым, и медиана верхних строк по 94 людям так и не станет
   известна — а именно ею калибруется обещание лендинга (`plans/26` §3 №9).

4. 🐞 **Четыре дефекта каталога и витрины:**
   · `bugs/106` (5112 вместо 5111, диаметр 715 вместо 714,9) · `bugs/108` (вечная пересборка
     индекса) · `bugs/109` (новые измерения в бою не находятся) — 🔑 **все три чинятся ФАЗОЙ 1
     эпика одним выкатом образа**, отдельными заходами их брать не надо.
     ⚠️ Хвост `bugs/106` остаётся за владельцем: снимки дней переписать И оставить след правки
     (интервью №014, В2 = В) — разовый инструмент, пишет в БОЙ, только с его слова и ПОСЛЕ выката.
   · `bugs/107` — витрина больше не врёт (полоса чисел выкачена), но **вариант В владельца ещё
     не сделан**: строка притока обязана стать ВЫВОДИМОЙ из снимков дней. Развилка закрыта
     (№014 В1=В), спрашивать нечего — исполнять.

5. **`ideas/28`** — порог решён (В3 = В, верхние 20 % каталога), шаг 0 закрыт: `dims_list` — 566 КБ
   = **54 %** потолка 1 МиБ, счётчик влезает (57–58 %).
   🔴 **Настоящая развилка эпика другая:** `ensureDimsIndex` (`calculator/index.mjs:344–370`)
   пересобирает индекс ТОЛЬКО из `{ru,en,year}` и пишет его `merge` на уровне поля — **первая же
   смена числа измерений сотрёт все 5111 счётчиков молча**, и сторож свежести этого не заметит.
   Правка `ensureDimsIndex` обязана идти ДО любого кода, который счётчики пишет.
   ⚠️ Запас каталога — **1,85×** (лопнет примерно на 9 400 измерениях); документ перечитывается
   целиком каждым циклом сервера и каждой клиентской сессией ⇒ счётчики писать ПАЧКОЙ.
   ⚠️ У **9 пар новичков из 10** общей оси по-прежнему нет даже при пуле 20 %. Решение владельца
   не отменять — знать и показать.

6. 🔧 **Прочее за владельцем (руки человека):** погасить пароли у 331 человека в бою · приёмка
   смены почты боем · приёмка аватара на живом Safari · ключ reCAPTCHA для App Check ·
   приёмка `bugs/105` боем.


---

## Сессия 2026-08-02, ночь: замер петли анонима + пять замечаний владельца (перенесено из STATUS дословно)

> Стрижка бонсая при закрытии чата 2026-08-02 (день). Работа закрыта; её результат живёт в
> `plans/28`, `bugs/105`, `ideas/25–28`. В STATUS остались только выводы, которые ещё «сейчас».

**Последнее обновление:** 2026-08-02, ночь, чат закрыт — 🔬 **ЗАМЕР ПЕТЛИ АНОНИМА + ПЯТЬ ЗАМЕЧАНИЙ
ВЛАДЕЛЬЦА + ИНТЕРВЬЮ №013.** Фаза 1 эпика `plans/23` закрыта замером (`plans/28`): время до первого
топа **1…12 с**, но главное найдено случайно — 🔴 **усердие новичка НАКАЗЫВАЕТСЯ**: похожесть падает
**51 → 33 → 26 → 14 %** по мере роста числа оценок, потому что лента даёт случайное, а Дайс растит
знаменатель. В случайной ветке при 10 оценках пересечение НУЛЕВОЕ со всеми — значит топа не будет
никогда. Отсюда родился эпик владельца `ideas/28`.

**Что этот чат сделал:**

| Что | Где | Проверено |
|---|---|---|
| Замер петли анонима начисто | `plans/28`, `tools/probe-guest-journey.mjs` | живой браузер, 2 ветки |
| Прибор починен от **4 лжей** | тот же | сверка со вторым свидетелем |
| Шапка страницы вычитки: кто спрашивает + пилюли | `tools/review.mjs` | кадры 2 темы, страж 118/0 |
| Ложное «браузер не дал закрыть» | `tools/review.mjs` | CDP-замер, 4053 мс vs 4055 мс скептика |
| Пять замечаний владельца заведены | `bugs/105`, `ideas/25–28` | — |
| Ответы интервью №013 разнесены | `bugs/105`, `ideas/28` | состязательная проверка |

🔴 **Три урока этого чата, оплаченные дорого:**
1. **Прибор соврал ЧЕТЫРЬМЯ способами** (`plans/28` §4), и самая опасная ложь **совпала с
   гипотезой** — она уже стояла в черновике как находка. Поймана только вторым свидетелем:
   вычислитель записал `ownerSpaceSize: 40`, что разошлось с прибором. Урок — `EXP-0121`.
2. **Владелец поправил вывод агента и был прав** (`EXP-0120`): я объявил ограничением математику,
   а это было ограничение ПОДАЧИ — лента выдаёт случайное. Второй такой случай за неделю.
3. **Обратные кавычки в комментарии внутри шаблонной строки** уронили `tools/review.mjs` целиком
   (`SyntaxError`), при том что предупреждение об этих граблях стоит в том же файле тридцатью
   строками выше. Поймал субагент, не я.

## Сессия 2026-08-01: разворот на ворота + контур вычитки (перенесено из STATUS дословно)

**Предыдущее:** 2026-08-01 — 🚪 **ПРОЕКТ РАЗВЁРНУТ НА ВОРОТА.** Владелец
ответил на два интервью (№009 — 11 ответов, №010 — 14 из 15) и заказал фундаментальную работу:
лендинг-как-приложение, бесшовный вход анонимом, гость наравне с полноценным человеком,
**английский как основной язык продукта**, максимальное SEO. Проведена **ревизия мастер-плана**:
заведена **фаза 7 «Ворота»**, соцслой стал фазой 8, принципов стало девять. Видение зафиксировано в
`GOAL.md`, `MASTER_PLAN.md` и `AGENT_GUIDE.md` — **читай их до любой задачи по воротам**.

**Что этот чат СДЕЛАЛ в коде** (не только спланировал):
| Что | Где | Проверено |
|---|---|---|
| Правила Storage выкачены в бою (`bugs/04` закрыт) | Rules API | чтением боя: `/admin` и `/public` исчезли, 403 |
| Публичная дверь удаления была СЛОМАНА — три дефекта | `delete-account`, `account.ts` | `verify-delete-door` **16/16**, 2 мутации |
| Документы открыты поиску: 2 страницы → **11**, текста ×13.5 | `DocShell`, `sitemap` | `e2e/seo` **10/10**, мутация |
| Класс «один отказ гасит весь экран» | `relations.ts`, `dims.ts` | живьём: гость с 4 связями, экран жив |
| Множитель витрины ×30 удалён — настоящее число | `metrics.ts` | `verify-bug81` **28/0**, в кадре «94 человека» |
| Предохранитель воронки снят — считает в бою | `funnel.ts` | сборка: firebase остался ленивым |
| Мёртвый UID при протухшей ссылке | `profile.ts` | типы; путь редкий |

**Предыдущее:** 2026-08-01 — 🔐 **ПРАВИЛА STORAGE ВЫКАЧЕНЫ В БОЙ** по слову владельца
«кати» (`bugs/04` ЗАКРЫТ, четыре года прожив с правилами 1.x). В бою больше нет ни списка
админских почт, ни открытого всему свету `public/`; появился потолок **5 МБ + `webp`/`jpeg`**.
Файлы 1.x забэкаплены в `.private/storage-1x-backup/` (вне git), прежний ruleset цел — откат
одним PATCH. **Фаза 6 эпика (аватар) разблокирована полностью.**

**Ранее в тот же день:** эпик «Управление аккаунтом» прошёл фазы 2–5 и 8: экран
`/account`, смена почты сквозным путём, удаление аккаунта с каскадом и чисткой следов в
вычислителе, пароли отменены целиком.

**Предыдущее:** 2026-07-31, ночь — 📐 заведён сам эпик: лестница `/plan-epic` до третьей ступени —
`researches/24` + `researches/23` → `plans/15` (метаплан, 8 фаз) → `plans/16` →
`interviews/interview_008` (девять развилок).

**Предыдущее обновление:** 2026-07-31, вечер (чат закрыт) — ✅ **ЭПИК `ideas/24` (ФАЗЫ 1–2) В БОЮ
+ ПЯТЬ ВЫКАТОВ.** Все сводные экраны переведены на виджетную сетку с ШАГОМ (модель владельца);
«Аккаунт» слит с карточкой-шапкой «Профиля»; «Меню» приведено к двум колоннам; манифест удалён
целиком как отсебятина агента. Заведены `bugs/104` и `plans/13`. Уроки — `EXP-0104…0107`.

**Днём того же дня:** 2026-07-31 — 🧰 **KAIF ОБНОВЛЁН 2.0 → 2.1 «Strong KAIF»**
(по слову владельца) маршрутом bootstrap: свежий `KAIF.md` → `KAIF-LOADER.mjs` → машинерия 2.1;
сначала прогон в песочнице, предсказание совпало с боем байт в байт. Новое: **`PROJECT_HISTORY.md`**
(летопись; туда дословно мигрирована история этого файла — 2 900 → ~260 строк), 6 скиллов
(`/plan-task`, `/plan-epic`, `/guarded-loop`, `/code-revision`, `/owner-voice`, `/owner-reviews`),
канон-вставки в `AGENT_GUIDE.md` (лестница планирования · гигиена документов и текста + реестр пар
«истина ↔ зеркало» · место вопросов · класс вкуса · разрешение ≠ авторство идентичности). Полевой
отчёт разработчику фреймворка **забран владельцем лично 2026-07-31** — в репозитории его нет и
заводить заново не надо.

**Свежее:** 2026-08-01, вечер — 🗣 **РАЗВЁРНУТ КОНТУР ВЫЧИТКИ ВЛАДЕЛЬЦА** (`plans/27`, регламент
KAIF `/owner-reviews`): всё, что агент хочет ОТ владельца, открывается **HTML-страницей с ответом
в один клик**, ответ механически возвращается в md с `by`/`at`. Владелец уже ответил через неё на
интервью №011 — **без единого уточняющего вопроса в чате**, а это и есть критерий приёмки
регламента. Голос сигнала взят **взаймы у соседнего проекта KLAS** (Silero v5, локально) — командой,
не копией; голос `eugene` выбран владельцем вслепую из пяти образцов.

📤 **Отчёт наружу — `researches/28`** (заказ владельца): полевой отчёт по этой реализации для
разработчика KAIF **и** для агента на другом проекте. Часть I — что формализовать в регламенте
(главное: инварианта «сохранение будит ждущего» в нём НЕТ), часть II — исполнимый контракт «как
построить то же самое». Ссылка на него вписана в `D:\work\ai_sandbox\KAIF\ideas\20_kaif_2.2_scope.md`
п. 1.6 — там уже стоял запрос ровно на это.

**Что страж места вопросов нашёл в первый же день:** интервью №007 два дня числилось «🔴 ЖДЁТ
ОТВЕТОВ» при двенадцати заполненных ответах · разбор считал `---` после пустого поля «Ответ:»
**ответом**, из-за чего по всем десяти живым интервью выходило «0 без ответа» · В10 интервью №008
вечно числился открытым, хотя снят отменой паролей. Всё исправлено. Уроки — `EXP-0116…0118`.

---

## Эпик «Управление аккаунтом», фазы 2–5 и 8 — 2026-08-01 (перенесено из STATUS дословно)

> Стрижка бонсая при закрытии чата 2026-08-01 (вечер). Работа закрыта; в STATUS остались
> указатель и открытая фаза 6 «аватар».

**ЭПИК «УПРАВЛЕНИЕ АККАУНТОМ» — `plans/15`. Заказ владельца 2026-07-31:** смена Email ·
   сброс пароля · смена пароля · аватар · **удаление аккаунта** («тоже надо» — его слово в ответ
   на вопрос о границах). Поглощает `bugs/45`, `bugs/44`+`plans/13` и часть `bugs/32`.

   **Ступень 1 лестницы закрыта:** `researches/24` (Firebase + OWASP ASVS 5.0 + NIST 800-63B-4 +
   GDPR/сторы + форма экрана у шести продуктов + разведка архива 1.x **по коду**) и
   `researches/23` (фото в браузере). 306 фактов, каждый с URL и цитатой; 24 рискованных прошли
   состязательную перепроверку (20 подтверждено, 4 уточнено).

   🔴 **Пять находок, которые меняют постановку** (не переоткрывать):
   - `updateEmail` и `linkWithCredential` с паролем **закрыты** защитой от перечисления почт;
     смена почты только `verifyBeforeUpdateEmail`, «завести пароль» — только REST `accounts:signUp`.
   - **«Письмо отправлено» может быть неправдой:** при занятом адресе promise резолвится молча ⇒
     тексты обязаны быть нейтральными («если адрес свободен, письмо придёт»).
   - **Реавторизация ветвится на ТРИ пути** (`providerData`); popup для человека, вошедшего по
     ссылке, **бросит ошибку**.
   - **Порядок удаления обратен 1.x:** сначала данные, потом `deleteUser`. В 1.x аватар оставался
     сиротой, а ошибки удаления глушились — это дефект, не образец.
   - 🔴 **Зелёный стенд НЕ доказывает смену почты:** Auth-эмулятор не применяет запрет EEP в
     `setAccountInfo`, писем не шлёт. Проверяется только боем.
   - Бонус фазы «аватар»: **Safari не кодирует WebP** из canvas, молча отдаёт PNG (323 КБ против
     12 КБ), а путь `avatar.webp` менять нельзя.

   ⚠️ **Разведка опровергла наши же документы:** в 1.x у смены пароля было **два поля, а не три**,
   а на смене Email **пароль стоял выше нового адреса**. `researches/12` и `bugs/45` исправлены.

   ✅ **ФАЗА 2 ПРОЙДЕНА 2026-08-01** (`plans/16`, полностью — `researches/24` §7). Владелец ответил
   на все девять вопросов интервью №008 (все — вариант **А**; уточнения: **В8** тип файла уже —
   только `webp/jpeg`; **В9** iPhone откладывается, приоритет Android и десктоп). Разведка боевой
   конфигурации сделана **только чтением**, замеры — прибором `tools/probe-account-phase2.mjs`
   (лёгкий эмулятор Auth, тяжёлый стенд не нужен, сироты не остаётся).

   🔴 **Четыре находки фазы 2, две из которых меняют план:**
   - **EEP в бою ВКЛЮЧЕНА** (`enableImprovedEmailPrivacy: true`) — главная развилка закрыта фактом.
   - 🔴 **`providerData` НЕ отличает «есть пароль» от «входит по ссылке»** — у обоих `password`
     (типы SDK: *«Always set to ProviderId.PASSWORD, even for email link»*). **Механизм под решение
     В1 неисполним;** фаза 7 начинается с вопроса «чем отличить», а не с вёрстки.
   - 🔴 **Код отмены смены почты на эмуляторе не рождается** (2 из 2), хотя почта менялась. В бою
     шаблон отката настроен и по-русски — проверяется только боем; фазу 5 планировать от худшего.
   - ✅ **Блок «CORS бакета только GET» ложный:** замер заголовками дал `POST, GET, HEAD, DELETE,
     PATCH`. Загрузка и удаление не блокированы (`PUT` в списке нет — довод за `uploadBytes`).

   Наследие в конфигурации: **Action URL смотрит на `ndim-space.firebaseapp.com`**, а не на наш
   дом; у шаблона подтверждения почты **английская тема при русском теле** (правится в консоли).
   ⚠️ Замер «сброс пароля для аккаунта без пароля» **честно провален** (EEP на эмуляторе не
   включилась, HTTP 404) — остаётся `[NOT-TESTED]`.

   ✅ **ФАЗА 3 ЗАКРЫТА 2026-08-01 — МАКЕТ УТВЕРЖДЁН** (`plans/17`). Владелец выбрал **V2 «Одна
   страница»** с правкой **«с виджетами»**. Форма эпика зафиксирована: отдельный маршрут ·
   подэкранов нет, секции раскрываются на месте · страница собрана **виджетной сеткой** по модели
   «шага» (`ideas/24`): содержательные виджеты по 2 шага, **«Удаление аккаунта» — во всю панель
   (4 шага), последним** · виджет «Пароль» видят только те, у кого пароль есть · меню фото из
   четырёх действий открывается тапом по самому фото.
   Артефакты: `design/account-mockups.html` · 16 кадров `test-results/account-mockups/` ·
   **новый общий съёмщик `tools/shoot-mockups.mjs`** (правило четырёх макетов — одной командой).

   ⚠️ **Кадры поймали ЧЕТЫРЕ дефекта в самих макетах, все до реализации:** меню фото закрывало
   сравниваемое (дважды) · «десктоп» был колонкой 430px вместо оболочки 1440 · диалог V4 уезжал
   за край (два конфликтующих правила) · **в утверждённом V2 кнопка «Удалить аккаунт»
   растягивалась во всю панель** и становилась самой широкой мишенью экрана (`EXP-0110`).

   ✅ **ФАЗА 4 ЗАКРЫТА 2026-08-01 — ЭКРАН ЕСТЬ В КОДЕ** (`plans/18`). Заведён маршрут
   **`/account`** («Управлять аккаунтом» — инфинитив, канон 1.x), виджет **«Мой аккаунт»**
   (Email + признак подтверждения · «Создан» в формате 1.x · «Способы входа»), а строка
   **«скоро» в «Профиле» заменена живой ссылкой**. Данные берутся из Auth **без единого запроса
   к базе**. Страж `tools/verify-account.mjs` — **84 проверки, доказан 2 мутациями**; смоук
   `e2e/account.spec.ts`. Полный прогон: check/typecheck чисто, `npm test` 192/192,
   `npm run e2e` **74** (было 70). 🔴 **В бой НЕ деплоилось** — это решение владельца.

   ⚠️ **Строку «Пароль не задан» с макета написать оказалось НЕЛЬЗЯ** — `providerData` этого не
   знает (`EXP-0109`). Экран честно молчит про пароль, и **страж стережёт именно молчание**:
   мутация «дописать про пароль» его роняет. Формулировка ждёт ответа владельца на **В10**.

   ⚠️ **Граница фазы поправлена при исполнении:** реавторизация и словарь ошибок перенесены в
   фазу 5 — у них в фазе 4 нет ни одного вызывающего, а значит ворота «проверено живым
   браузером» неисполнимы. Приедут вместе со своим первым вызывающим — сменой почты.

   🟡 **ФАЗА 5 (`plans/19`) — В КОДЕ, ЖДЁТ ПРИЁМКИ БОЕМ.** Смена почты работает сквозным путём:
   виджет «Поменять Email» (тексты и порядок 1.x) → подтверждение личности → письмо на новый
   адрес → наша страница-приёмник `/auth/action` → **адрес реально меняется**. Реавторизация
   тремя путями и словарь ошибок словами 1.x — там же. Страж `tools/verify-account-email.mjs`
   **29/29**, доказан мутацией с названными адресатами (утвердительное «Письмо отправлено» →
   покраснели **ровно** 2 проверки нейтральности). Страж ядра 84/84 без регрессий,
   `npm test` 192/192, `npm run e2e` 74.

   💡 **Находка, улучшившая замысел:** ссылка подтверждения уходит на ТЕКУЩИЙ адрес — значит она
   и есть то подтверждение со старого адреса, которого требует OWASP без второго фактора. Это
   снимает зависимость от письма-отката Firebase, которое в замере фазы 2 не приехало ни разу.

   🔴 **Что честно НЕ проверено и требует боя:** режим `recover-email` (эмулятор такого кода не
   выдаёт) · поведение при ЗАНЯТОМ новом адресе (эмулятор не применяет запрет EEP) · Google-путь
   реавторизации (popup в headless не воспроизвести).

   ⚠️ **Поправка:** Action URL переключается **после фазы 7**, а не сейчас — приёмник умеет три
   режима из четырёх, сброс пароля приедет вместе с самим паролем. Ничего не блокирует: сегодня
   письма обрабатывает страница Firebase, и смена почты работает через неё.

   ⛔ **ПАРОЛИ ОТМЕНЕНЫ ЦЕЛИКОМ — решение владельца 2026-08-01:** «Убираем пароли полностью,
   оставляем только ссылки по почте и Google авторизацию». Возврат к его же канону (интервью
   №004, В2=A+). `bugs/32` закрыт как отменённый, **фаза 7 эпика вычеркнута**, вопросы В1 и В10
   сняты. Побочно: страница `/auth/action` стала ПОЛНОЙ (режим сброса пароля невозможен), и
   переключение Action URL больше ничего не ждёт.

   🔧 **Новая работа, которую породило решение: погасить пароли у 331 человека в бою.**
   Переключателем проекта не делается — email/password и email-link это ОДИН провайдер
   (`passwordRequired`: «If false, a user may sign in via either email/password or email link»);
   выключить его значит убить и вход по ссылке. Гасится по данным: `accounts:update` с
   `deleteAttribute: ["PASSWORD"]`. 🔴 `deleteProvider: ["password"]` НЕ ИСПОЛЬЗОВАТЬ — он снимет
   саму почтовую личность. Способ `[NOT-TESTED]`, операция необратима, руки владельца.

   ✅ **ФАЗА 8 ЗАКРЫТА 2026-08-01 — АККАУНТ УДАЛЯЕТСЯ** (`plans/20`). Каскад `src/lib/data/erase.ts`
   идёт **Storage → Firestore → `deleteUser`** и **останавливается на первой неудаче**: не
   удалилось — учётную запись не трогаем (обратно дефекту 1.x, где ошибки глушились). Подтверждение
   — ввод своей почты + повторный вход. Виджет во всю панель, красная кнопка только на финальном
   шаге. **Публичная страница `/delete-account`** доступна без входа и БЕЗ `noindex` (требование
   Google Play) — это **дверь, а не инструкция** (поправка владельца: «Почему "Как" удалить
   аккаунт? Зачем КАК?»). Стражи: `verify-account-delete.mjs` **22/22** (мутация «`deleteUser`
   первым» → краснеют ровно каскад и все пять проверок «ПОСЛЕ») + 5 тестов правил на «чужое не
   удалить» (**102** теста правил).

   🔴 **Разведка нашла следы шире, чем предполагал ответ В3:** свой `relations`, членство в ЧУЖИХ
   группах, подсказки аудитории у других, свои предложения измерений. Владелец ответил **В11 = А**:
   суточный проход вычислителя вычищает всё, а **предложения ОБЕЗЛИЧИВАЕТ** (`authorUid` стирается,
   текст остаётся — предложение полезно Пространству и после ухода автора).
   `cleanupDeletedPeople()` в `calculator/index.mjs`, **9 тестов**, доказан мутацией.

   ✅ **`bugs/04` ЗАКРЫТ 2026-08-01 — ПРАВИЛА STORAGE В БОЮ** (слово владельца «кати»).
   ⚠️ Транспорт не тот, что в плане: креды Firebase CLI протухли, перелогин интерактивный —
   выкат сделан **Rules API** (ruleset `f656a24d…`, релиз переключён). Это то же, что делает
   `firebase deploy --only storage` внутри. **Проверено чтением боя:** `match /admin` и
   `match /public` исчезли · `public/um.html` **403** (был открыт ВСЕМ) · `admin/adminFunctions.js`
   **403** · аватар без входа **403**, как и до выката (правило чтения не менялось).
   Откат готов: `.private/storage-1x-backup/storage.rules.PROD-BEFORE.txt` + прежний ruleset цел.

   **Следующее — фаза 6 (аватар): загрузить · заменить · удалить.** Блоков не осталось ни одного —
   ни правил, ни CORS (замер `researches/24` §7.4). «Открыть на весь экран» уже готово
   (`Avatar.svelte`), переделывать не надо. Приёмка на живом Safari отложена самим владельцем (В9).

## Сессия 2026-07-31 — `ideas/24`, фазы 1–2 (перенесено из STATUS 2026-08-01 дословно)

1. ✅ **`ideas/24`, ФАЗЫ 1–2 ЗАКРЫТЫ, ОБА ЭКРАНА ВЫКАЧЕНЫ В БОЙ 2026-07-31** (два слова
   владельца «деплой прод»). Боль «виджеты держат место, появляются дыры» — снята.

   | Экран | пустоты 1440 | пустоты 1024 | высота |
   |---|---|---|---|
   | Пространство | 26.8% → **22.2%** | 30.9% → **27.2%** | 932 → 902px |
   | Пространство, тихий день (случай со скриншота) | → **13.1%** | → **18.2%** | → 693px |
   | Профиль | 16% → **10.9%** | → **13.9%** | 776 → **619px** |

   **Признак в боевом CSS** (снят до и после выката): `/space` — `display:contents` **0 → 2**,
   `.col.svelte-*` **0 → 2**; `/profile` — `.head-who` **0 → 1**, `.col.svelte-*` **0 → 1**.
   Бой: `verify-prod-b4` **81/81** (дважды), `verify-bug81` 28/0, `verify-pwa` 24/0,
   `probe-bug67` зелёный. Телефон не изменился ни на пиксель.

   **На «Профиле» управление аккаунтом СЛИТО с карточкой-шапкой** (слово владельца): фото ·
   имя · место будущего «Статуса» (`plans/09`) · «Управление аккаунтом» · «Выйти». Отдельной
   карточки «Аккаунт» внизу больше нет — отмена части `ideas/19` того же дня, записана в саму
   идею. Гость БЕЗ документа защищён: у него шапки нет, ряды остаются своей карточкой, иначе
   замок `bugs/84`.

   **«МЕНЮ»: ДВЕ КОЛОННЫ, МАНИФЕСТ УДАЛЁН ЦЕЛИКОМ.** Слово владельца: «и виджет и страницу
   удаляем — это была отсебятина ИИ, я этого не делал в оригинальном НДим». Удалены виджет,
   страница `/menu/manifesto`, модуль `src/lib/content/manifest.ts`, стили и все проверки,
   которые их стерегли. **Текст не потерян:** «1. Манифест» — раздел РУКОВОДСТВА пользователя
   (`tools/extract-docs.mjs`), и его по-прежнему стережёт e2e-строка
   `{ path: '/menu/manual', marker: 'Манифест' }`.
   Колонны: слева «Вид», «Поделиться», «Документы» (450px), справа «Проект», «Версии» (325px) —
   разбиение выбрано ЗАМЕРОМ (первый вариант давал 250 против 525).
   Признаки в бою: `/menu/manifesto` **200 → 404** · «Манифест» в сыром HTML `/menu` **3 → 0** ·
   секций `.col` **1 → 2** · «Манифест» в руководстве **1 → 1** (текст цел).
   🔴 **Стражи не выброшены, а ПЕРЕВЁРНУТЫ:** `verify-batch4` и e2e теперь требуют, чтобы
   виджета, ссылки и страницы НЕ БЫЛО, — иначе следующая сессия «восстановит» удалённое, и ни
   один тест не возразит. Урок класса — `EXP-0107`, документ — `bugs/38_DONE`.

   ⚠️ **«В Пространстве с …» убрана из профиля ПОВТОРНО** — первый раз её убрал владелец
   (интервью №007 В10), второй раз она вернулась по вине агента: сливая ряды аккаунта с
   шапкой, он притащил подпись вместе с ними. **Урок класса: перенося блок, проверяй не только
   что он выглядит как раньше, но и что он не тащит за собой ОТМЕНЁННОЕ.** Признак в бою:
   `/profile` «В Пространстве с» **1 → 0**. Место под именем принадлежит `plans/09` («Статус»).

   🔴 **ЖДЁТ ВЗГЛЯДА ВЛАДЕЛЬЦА:** оба экрана ПОД ЛОГИНОМ — гостевые смоуки видят их без
   данных, а сетка живёт там, где есть цифры.

   ⚠️ **Капкан этого эпика, оплаченный дважды:** метрика пустоты считает пустоту МЕЖДУ
   карточками и НЕ ВИДИТ пустоту ВНУТРИ них. По ней «Профиль» выглядел здоровым (16%), и
   первый заход оставил его как есть; владелец посмотрел глазами и оказался прав. **Замер
   верен в границах, где снят: меняется состав виджетов — замер пересдают, а не цитируют.**

   **Следующее — фаза 3:** подсистема настроек человека (`ideas/16`), она же предусловие
   ручного перетаскивания. Трогает модель данных и правила Firestore; в `ideas/16` висит
   открытый вопрос владельцу (что главнее при расхождении — настройка браузера или профиля).
   Операционный план ещё не написан (номер `plans/14` — `plans/13` занят фото профиля).

   Утверждённая модель (реализована дословно, менять только по слову владельца):

   Сетка в **4 шага** по горизонтали ·
   **малый виджет = 1 шаг** (плитки метрик становятся самостоятельными виджетами) ·
   **большой = 2 шага** (содержательные карточки), при нужде 4 — во всю панель ·
   🔴 **шаг задаёт ТОЛЬКО ГОРИЗОНТ**: по вертикали колонка пакуется сама, карточка сохраняет
   естественную высоту, **искусственно не растягивать ничего** (прямое слово владельца) ·
   расположение как в V1: слева «Сегодня», «Сейчас», «Похожесть», справа «Сервер».
   Эталон — `design/widget-layout-mockups.html`, кнопка V4.

   Лестница эпика: `researches/22` (обзор индустрии) → `plans/10` (метаплан, 6 фаз) →
   `plans/11` (фаза 1 ✅) → `plans/12` (фаза 2 ✅, выкачена). План фазы 3 — **`plans/14`**,
   ещё не написан (`plans/13` занят фото профиля).
   Страж — `node tools/verify-ideas24.mjs` (**60 проверок** на двух экранах; `--mutant`
   возвращает старую сетку инъекцией и обязан дать **32 провала** — этим он и доказан).

   **Капканы этого эпика, проверенные замером** (не переоткрывать):
   - **`grid-auto-flow: dense` сам по себе бесполезен** — 4/4 замеров без пикселя разницы,
     пока все виджеты одного размера. При РАЗНЫХ шагах он работает. Опровергнут был кандидат,
     а не идея.
   - Отсеяны замером: `grid-lanes` (не Baseline в 2026, движок не знает), `columns: 2`,
     `span 2` — обе делали «Профиль» ВЫШЕ.
   - Дыра БОЛЬШЕ на узком десктопе — правку проверять сначала на 1024, а не на 1440.
   - Тема на геометрию не влияет ни на пиксель.
   - Урок фазы: техника, полезная на одном экране, бывает вредна на соседнем. Радиус решается
     замером на КАЖДОМ экране (`EXP-0104`).

   ⚠️ **Часть 2 идеи (перетаскивание) стоит на `ideas/16`** и на WCAG 2.2 SC 2.5.7 (AA):
   перетаскиванию обязана сопутствовать однопальцевая альтернатива. Разбор — `researches/22` §3.3.

## 2026-07-31 — аудит слабых моделей, тройной выкат и страница руководства

*(перенесено дословно из `STATUS.md` при закрытии чата 2026-07-31; см. также записи ниже)*

### ✅ АУДИТ + ТРОЙНОЙ ВЫКАТ 31.07, утро (слова владельца: «перепроверить адекватность и
работоспособность» · «деплой прод, перезапуск сервера» · «правила… перепроверей и выкатывай»)

**Аудит** (механика — `researches/20`): полный прогон всех гейтов (~900 проверок) + 6 читающих
ревизий по осям + состязательное опровержение каждой находки + критик полноты. Итог: **гейты не
соврали ни разу** (всё заявленное зелёным — зелёное; флейк `demo.spec` не воспроизвёлся за 100
повторов), но нашлись **12 настоящих дефектов, невидимых гейтам** (5 major) — все починены в тот
же день, каждый фикс доказан адресной мутацией; **9 находок ревизоров опровергнуты скептиками**
(документы решений владельца защитили). Уроки — `EXP-0096…0101`; отчёт разработчику KAIF о
механизмах УСИЛЕНИЯ слабых моделей — **`researches/20`** (заказ владельца).

| Выкачено | Признак ДО → ПОСЛЕ |
|---|---|
| **Hosting** (фиксы `bugs/95` `?guest=` не затирает сессию · `96` пин карточки · `101` свечения по теме + витрина 94) | `--glow-strong` в боевом CSS **0 → 2**; смоуки УЖЕСТОЧЁННЫМ стражем (`bugs/97`/`98`): **81/81 · 28/28 · 24/24**; e2e перед выкатом 72 passed |
| **Правила Firestore** (`bugs/99` дружба hasOnly+created · `100` админ НЕ читает оценки) | боевой ruleset **от 2026-07-12 → от 2026-07-31 06:11 UTC**; выгружен Rules API и сверен: **бой == репо байт в байт** (и до выката дрейфа не было); test:rules **97/97** |
| **Сервер синхронизации** (`bugs/91` проход переживает сбой · `92` пустой топ опустевшей точки · `93` транзакция dirty · `94` страж эмулятора) | `space/server.build` **19 → 20**; лог старта: «проект ndim-space, эмулятор: нет (боевой Firestore)» — новая строка стража `bugs/94`; test:calc **43/43** |

**Ночной полный проход** (проверка из прошлой эстафеты): состоялся **31.07 в 03:00:09**, боевой
`space/server`: `fullSync.at = 31 июля 03:00`, `nextAt = 1 августа 03:00` (читано Admin SDK) —
ровно ожидание владельца. «Записано топов — 0» — экономия работает.

⚠️ **Прогрев билда 20 разово дозаписал 237 топов** — это конвергенция, а не сбой: документы,
до которых diff-экономия не дотягивалась с 30.07 (в основном гостевые топы), получили поле
`updated` (`bugs/46`). Проверено фактами: повторный рестарт → «записано топов — 0»; в базе
теперь **2159/2159 строк топов с `updated`**. Следующие рестарты бесплатны.

**Хвост аудита, закрытый после паузы:** боевые пиксели иллюстраций пересчитаны (`network.webp`
ровно 0 белых — заявление выката подтверждено) · стражи `verify-bug95` (12) и `verify-bug96`
(10) написаны и доказаны мутациями — второй сразу окупился, поймав недоделанную первую редакцию
фикса (движок Svelte восстанавливает снапшот inline-стилей при отмене outro, `EXP-0100`).

**Что осталось владельцу (прежние нити):** карточка «Измерений» под логином (ореол/уезд 700 мс,
файлы на `D:\`) · установка PWA на телефон (`bugs/58`/`83`). Беклог аудита закрыт целиком.

**✅ `ideas/19` РЕАЛИЗОВАНА И ВЫКАЧЕНА В БОЙ (2026-07-31, слова «Делай» → «деплой»):** виджет
«Аккаунт» переехал из «Меню» вниз «Профиля» целиком (возврат канона 1.x «Дом»; прежний канон
«выход — в /menu» из bugs/29 отменён словом владельца — пометка в researches/12). «Меню» больше
не читает данных профиля (экономия). Стражи переехали с блоком: bug84 58/0 · bug87 60→**72/0**
(+«в Меню карточки нет») · bug40 71/71 · bug95/96 12/0 и 10/0 · wave11/12 28/0 и 108/0 · юниты
192 · e2e 72. Гость без документа не заперт (обработка ProfileMissingError переехала тоже).
**Выкат hosting:** снимок витрины свеж (94) · чистая пересборка · признак ДО→ПОСЛЕ: заголовок
«Аккаунт» в сыром боевом /menu HTML **1 → 0** · смоуки **81/81 · 28/28 · 24/24**. ⚠️ Сам виджет
на «Профиле» живёт под логином — гостевые смоуки его не видят, нужен взгляд владельца.
Решения без владельца — в идее.

**Попутно закрыт `bugs/103`** (ложная регрессия свипа): verify-ideas18 оставлял оценки в базе
стенда, verify-bug64 ждал константу — теперь стражи убирают за собой (встроенная проверка
уборки) и считают ожидания из данных; правило класса — в AGENT_GUIDE «База стенда — общая».

**За ней — `ideas/24`** (новая идея владельца 2026-07-31): виджеты схлопывают дыры —
автораскладка + ручное перетаскивание с фиксацией, раскладка в профиле в БД. ЭПИК: сначала
обзор индустрии (шаг 7б), потом проектирование; связка с `ideas/16` (настройки с дебаунсом).

**README переработан в руководство пользователя** (2026-07-31, по слову владельца «в NDim так же
README переработать» с образцом KPOT): академический регистр владельца («N.1. Основные
положения», нумерованные нормы, безличный залог), 8 разделов × 2 языка целиком, без истории
проекта — витрина и роадмап живут в STATUS/MASTER_PLAN. Каждое утверждение перевыведено из кода
(250 = `RELATIONS_TOP_LIMIT` · 30 дней = `GUEST_TTL_DAYS` · 0.7 = `PROXIMITY_EXPONENT` · до 10
кругов · 404 = сумма четырёх сюит), все 9 относительных ссылок проверены. Образец разведан по
KPOT: STATUS-запись «USER MANUAL» + вердикт владельца об old-README («устаревший фрод») +
фирменная шапка-витрина Unliminium. Вычитка владельцем — приветствуется.

### ✅ ВТОРОЙ ВЫКАТ 31.07, ночь — страница руководства (слово владельца: «Да, выкатывай»)

Три замечания, все закрыты ЗАМЕРОМ, прибор — `tools/measure-bug80-scale.mjs` (новый; это шаг 0
документа `bugs/80`, не снимавшийся полтора месяца).

| Замечание | Причина, найденная замером | Признак в бою до → после |
|---|---|---|
| «на десктопе малые элементы в таблице» | колонка документа растёт ×2.21 (347→768px), а звезда/цифра/смайлик были КОНСТАНТАМИ на обеих ширинах. Звезда руководства = 63% от звезды продукта | **17/13/18 → 27/18/27** на 1440; телефон не изменился |
| «это слишком огромное, в оригинале было маленькое» | `researches/12:323`: финальная картинка несла класс 1.x `small_image` (потолок 100px). Размер и загрузку решал ОДИН флаг — разведены на `kind:'small'` и `eager` | **298×300 → 99×100px** |
| «белый не весь удалён, в кружках остался» | удаление фона шло снаружи и в замкнутые кольца не попало | `network.webp` **4398 → 0** непрозрачных белых |

⚠️ **Найден второй файл, о котором владелец не говорил:** `island.webp` с белой полосой во всю
ширину (18646 → 3412; остаток — законная пена на воде). Класс проверен по ВСЕМ восьми
иллюстрациям; `logo-1x` (белая «N» на чёрной плитке) и мелочи самолёта/больницы — не дефект.

**Бой проверен живым браузером:** `verify-prod-b4` **81/81** · `verify-bug69 --prod` (графика не
появляется «на горячую») **зелёный** · боевые файлы картинок скачаны и пересчитаны: сеть 0,
остров 3412, знак 5429. Перед выкатом: e2e **72 passed**, юниты **192**, `svelte-check` 418
файлов 0 ошибок, `kaif:check` чист.

**Стражи:** 4 новые проверки в `verify-manual-v1.mjs`, доказаны ТРЕМЯ мутациями разом — 16
провалов на сломанной сборке, 0 на исправной.

Кадр «до/после» шкалы для владельца — `D:\ndim-шкала-до-после.png`.


## Записи (свежее сверху)

> 📦 **Миграция 2026-07-31 (обновление KAIF 2.0 → 2.1):** всё ниже перенесено ДОСЛОВНО из
> `STATUS.md` (2 900 строк → сводка ~260). Порядок — как лежало в STATUS: сначала сессии
> 30–31.07, затем летопись «Предыдущее обновление» (29.07 → 16.07 и глубже), в конце — ранние
> фазы и разделы первой недели (10–12.07). Внутренние заголовки и «эстафеты» записей — исторические:
> задачами они больше не являются.

### 🚀 ВЫКАЧЕНО В БОЙ 2026-07-30, 17:52 (слово владельца: «деплой прод и перезапусти сервер синхронизации»)

**Хостинг:** снимок витрины (`node tools/snapshot-landing-metric.mjs` — 94 человека, 30.07) →
чистая пересборка (`build/` снесён, EXP-0023) → гейты → `npx firebase deploy --only hosting
--project ndim-space` → **оба таргета release complete**. Правила Firestore, индексы,
`firebase.json` и `storage.rules` НЕ выкатывались (`--only hosting`), и за весь цикл они не
менялись — проверено диффом, 0 файлов.

**Признак снят ДО и ПОСЛЕ** (все пять — обязаны были ПОЯВИТЬСЯ):

| Признак | до | после |
|---|---|---|
| витрина «С нами уже …» в СЫРОМ HTML (`bugs/81`) | **0** | **«2 820 человек»** |
| `/manifest.webmanifest` | **404** | **200** |
| `/img/app/icon-512.png` | **404** | **200** |
| `<link rel="manifest">` в HTML | **0** | **1** |
| `/img/app/apple-touch-icon.png` | **404** | **200** |

**Бой проверен живым браузером:** `verify-prod-b4` **81/81** · `PROBE_BASE=https://ndimspace.app
verify-bug81` **28/28** · `PROBE_BASE=… verify-pwa` **24/24**.

**Сервер синхронизации перезапущен.** `npm run calc:image` → образ **билд 19** (в бою был 15,
от 27.07) → контейнер заменён с тем же окружением (`FIREBASE_PROJECT_ID=ndim-space`,
ключ сервисного аккаунта монтируется из `calculator/secrets`, `restart: unless-stopped`).
Первый цикл на боевом Firestore прошёл: *«прогрев кэша при старте: в Пространстве точек — 334»*,
*«записано топов — 95, в Пространстве людей 94, измерений 5112, связей рассчитано 2346»*,
ночной полный проход объявлен **в 00:00 UTC** (`bugs/85`).

⚠️ **95 переписанных топов — это ожидаемая РАЗОВАЯ перезапись**, а не сбой экономии: строка топа
получила новое поле `updated` (`bugs/46`), поэтому дифф увидел изменение у каждого. Следующие
циклы вернутся к обычному «записано топов — 0», когда никто не оценивает.

⚠️ **Что агент проверить НЕ МОЖЕТ и на что нужен Ваш взгляд:** `space/server` и `relations/{uid}`
по правилам закрыты от постороннего (403 гостю — это правильно). То есть новые строки виджета
«Последний запуск» (`bugs/86`) и «Последнее обновление NDim ID» в раскрытой связи (`bugs/46`)
видны только Вам, под логином. Посмотрите их глазами — это последняя непроверенная часть выката.

⚠️ **Git Bash ломает docker-пути.** `-e GOOGLE_APPLICATION_CREDENTIALS=/secrets/sa.json` из Git
Bash превращается в `C:/Program Files/Git/secrets/sa.json`, и контейнер падает с ENOENT. Запускай
`docker run` из **PowerShell** (или `MSYS_NO_PATHCONV=1`). Стоило одного лишнего перезапуска.

### ✅ ЗАКРЫТО ЗАМЕРОМ И ВЫКАЧЕНО (30.07, поздний вечер) — обе жалобы по карточке «Измерений»

**Ниже сохранена постановка, с которой сессия начиналась. Вот чем всё кончилось.**

Владелец **снял экран на камеру** (`D:\video5431525967346573958.mp4`) — из видео сразу стало
видно то, чего не было в словах: он смотрит на **ДЕСКТОПЕ в СВЕТЛОЙ теме**, а оба прошлых захода
проверялись в тёмной. Его правка процесса дословно: *«В следующий раз ТЫ САМ записываешь
экран!»* — записано в постоянную память агента, приборы теперь пишут `recordVideo`.

| Жалоба | Настоящая причина (найдена ЗАМЕРОМ) | Замер до → после |
|---|---|---|
| «тёмное вокруг смайлика… как ореол или тень» | `drop-shadow` золотом по БЕЛОЙ карточке — это тень, а не свет: `--star` #e8a516 имеет светлоту **0.44**, `--panel` — **1.00**. В тёмной теме то же золото светлее фона, и потому два захода, проверенные там, выглядели успешными | кольцо вокруг лица **0.912 → 0.997** при фоне 1.000 (соседи 0.997…0.999 — контроль) |
| «соседние не дожидаются, пока она уедет» | ДВЕ причины: `animate:flip` стартовал вместе с улётом; и карточка УКОРАЧИВАЛАСЬ до начала движения (уходили ряд смайликов и отсчёт) | соседка проходила половину пути на **0 мс** (1% пути карточки) → трогается на **682…684 мс** при уходе на **667…672 мс** |

**`TWINS:`** искал `drop-shadow` по `src/` — 2 места, оба этого класса. Второе (`.st.peak i`,
сама звезда) было ХУЖЕ первого (−0.107 против −0.088) и стоит прямо НАД смайликом. Починены оба.

**Лечение:** токен `--glow` в блоке темы (`transparent` в светлой — на белом свечения не бывает,
`var(--star)` в тёмной); задержка `animate:flip` на время жеста; синхронный пин высоты карточки
первым действием `commit()`. Длительность улёта **1200 → 700 мс** — владелец выбрал по записи
жеста из четырёх вариантов.

**Стражи (в `verify-bug80.mjs`) доказаны 4 мутациями:** вернул золотой ореол → 2 провала
(свечение 0.441 против фона 1.000); снял выделение выбранного лица → 1; снял задержку flip →
соседка на −50 мс; снял пин высоты → на −85 мс.

**Новые приборы:** `tools/measure-bug80-halo.mjs` (кольцо пикселей вокруг лица против фона,
соседи как контроль, пишет видео) · `tools/measure-bug80-flight.mjs` (обе карточки кадр в кадр,
ось движения выбирается замером) · `tools/probe-bug80-focusring.mjs` (снял ложного подозреваемого).

**Выкачено 30.07, признак в боевом CSS до → после:** `--motion-gesture` **1.2s → .7s** ·
`--glow` **отсутствовал → transparent · var(--star)** · ореол через `--star` **1 → 0** ·
ореол через `--glow` **0 → 2**. Бой проверен живым браузером: `verify-prod-b4` **81/81**,
`verify-bug81` **28/28**, `verify-pwa` **24/24**. e2e **72 passed** (вчерашний флейк
`demo.spec` не повторился — счётчик обнулён, третьего появления не было).

⚠️ **Что владелец должен посмотреть сам:** карточка «Измерений» живёт под логином, гостем её не
увидеть. Файлы для просмотра: `D:\ndim-ореол-до-после.png` (кроп «до/после») и
`D:\ndim-уезд-карточки-700мс.mp4` (запись жеста).

**Два ложных подозреваемых, снятых замером** (чтобы к ним не возвращаться): тёмно-синяя рамка
вокруг звезды на видео — штатный `:focus-visible`, мышью не появляется ни в одном из четырёх
сценариев, убирать нельзя (доступность); плашка `:hover` `--edge-soft` (светлота 0.897), из-за
которой прибор один раз объявил починенного близнеца сломанным.

<details>
<summary>Как задача была поставлена до замера (гипотезы прошлой сессии — обе оказались не тем)</summary>

#### Жалоба 1 (новая, и владелец сам назвал причину — она похожа на правду)

> «Я понял в чём проблема с уездом карточки. Соседние слишком быстро занимают её место, не
> дожидаются, пока она уедет — баг. И стал уезд слишком медленным.»

**Гипотеза агента (НЕ проверена, замер не снят):** карточка уезжает `MOTION.gesture` = 1200 мс,
а соседи подтягиваются `animate:flip` = `MOTION.slow` = 320 мс — и стартуют они, судя по всему,
одновременно. Плюс перед улётом схлопываются ряд смайликов и строка отсчёта (`pending = null`
в `commit()`), то есть карточка ещё и УМЕНЬШАЕТСЯ до старта движения — соседи могут ехать вверх
уже от этого.

**Как мерить (прибор писать заново, `test-results/` выжигается прогоном e2e — EXP-0062):**
покадрово снять `y` ВТОРОЙ карточки ленты и `transform.x` первой одновременно, от тапа
«Сохранить сейчас» до исчезновения. Дефект = вторая карточка начала подниматься раньше, чем
первая ушла за край (или закончила движение сильно раньше).

**Что уже известно про сам улёт (замерено):** движение стартует мягко, к 1036 мс — 294px из 480,
дальше разгон; непрозрачность 1 держится до 61% пути; всего ≈1170 мс. Владелец говорит, что это
**слишком медленно** — то есть длительность надо СНИЖАТЬ (ручка `MOTION.gesture` в
`src/lib/ui/motion.ts` + CSS-зеркало `--motion-gesture` в `src/routes/+layout.svelte`), а
подтягивание соседей — ЗАДЕРЖИВАТЬ.

#### Жалоба 2 (третий раз подряд, и агент дважды промахнулся мимо причины)

> «И тёмное вокруг смайлика при выборе звезды осталось»

Что уже сделано и НЕ помогло: (а) ореол сменён с серо-синего `--faint` на золото `--star`
(первый заход); (б) база прозрачности ряда поднята `.55 → .8`, чтобы выбранный не выглядел
единственным цветным (второй заход). Оба выкачены в бой, владелец видит проблему после них.

**Чего агент НЕ проверил и с чего начинать:** снять ПИКСЕЛИ вокруг выбранного лица (кроп
`deviceScaleFactor: 6`) и сравнить светлоту кольца вокруг него со светлотой фона карточки — в
обеих темах и **на том устройстве/теме, где смотрит владелец** (это стоит у него спросить: телефон
или десктоп, светлая или тёмная). Возможные подозреваемые, ни один не проверен: тень
`drop-shadow` в браузере владельца рендерится иначе; плашка `.st:hover` (`--edge-soft`) под
звездой на десктопе с мышью; контраст самого лица (цвета шкалы 1.x на его экране).

⚠️ **Правило для третьего захода:** сначала показать владельцу КРОП экрана и спросить «это оно?»,
и только потом править. Два захода вслепую уже потрачены.

**Состояние на момент паузы:** обе правки второго захода **ВЫКАЧЕНЫ В БОЙ** (`--motion-gesture`
`.9s → 1.2s`, база ряда `.55 → .8`, ореол золотой) — проверено признаком в боевом CSS, смоук
`verify-prod-b4` 81/81. Дерево чистое, всё запушено. Стенд ПОГАШЕН.

⚠️ **e2e мигает второй раз подряд на одном тесте** (`e2e/demo.spec.ts` → «язык: EN переименовывает
персонажей»): в общем прогоне 71 passed вместо 72, в изоляции 5/5 зелёных. Это уже не случайность —
при следующем появлении заводить баг (родня по классу — `bugs/67`).

</details>

### 🚀 ВТОРОЙ ВЫКАТ 30.07, ~21:40 — правка анимаций `bugs/80` (слово владельца: «деплой прод»)

Владелец посмотрел бой и дал две правки: «странно пульсирует тёмный смайлик внизу — нужно более
медленное и светлое пульсирование» и «карточка плохо уезжает по автосейву, быстро и быстро
растворяется; по кнопке лучше, но всё равно слишком быстро». Сделано, проверено, выкачено.

**Признак снят ДО и ПОСЛЕ прямо в боевом CSS** (⚠️ значения минифицированы — искать `.64s`/`.9s`,
а не `640ms`):

| Признак | до | после |
|---|---|---|
| `--motion-gesture` (длительность улёта карточки) | **`.64s`** | **`.9s`** |
| ореол выбранного смайлика | `--faint` 45% (тёмный) | **`drop-shadow(0 0 7px color-mix(in srgb, var(--star) 60%, transparent))`** — золото звезды |

**Бой проверен живым браузером:** `verify-prod-b4` **81/81** · `verify-bug81` по бою зелёный ·
`verify-pwa` по бою **24/24**.

⚠️ **Что в бою проверить нельзя гостем:** сами анимации живут на `/dims`, а он под логином.
Числа доказаны на стенде покадрово (карточка проходит 285px из 480 при непрозрачности 1;
раньше гасла до 0.2 за 230 мс), в бою доказан выкат КОДА этих правок.

⚠️ Первый прогон `npm run e2e` перед выкатом дал один флейк (`demo.spec` → «язык: EN
переименовывает персонажей»), повторный — **72 passed, 0 провалов**. Если он замигает снова,
это кандидат в баг: у него уже есть родня по классу (`bugs/67`).

### 🔍 ПРОВЕРКА ВЛАДЕЛЬЦА ПОСЛЕ ВЫКАТА (30.07, вечер) — что он увидел

**✅ `bugs/46` подтверждён в бою.** Владелец прислал раскрытую связь: строка «Последнее обновление
NDim ID → 25 ноября 2025 г. в 12:41» на месте, все шесть блоков досье 1.x собраны.

**✅ `bugs/86` подтверждён:** «Последний запуск — 30 июля в 20:52» это время ПОДЪЁМА контейнера
(17:52 UTC), а не последнего цикла.

**⏳ ОДНА СТРОКА ПОКАЗАНА СТАРЫМ ЗНАЧЕНИЕМ, и это ожидаемо:** «Полная синхронизация →
Запланированная **31 июля в 13:03**». Должно быть **03:00** (00:00 UTC, ответ владельца В6).

Причина, по коду: блок `fullSync` (включая `nextAt`) записывается **только во время ночного
прохода** — `calculator/index.mjs`, `const syncBlock = nightly ? { fullSync: … } : { partialSync: … }`.
Последний ночной прошёл в 10:03 UTC ещё **сборкой 15**, по её старой формуле «последняя + 24 ч».
Новая сборка 19 при старте объявила в логе «ночной полный проход в 00:00 UTC», но переписать блок
ей нечем до самого прохода.

> **🔴 ЧТО ПРОВЕРИТЬ ПЕРВЫМ ДЕЛОМ СЛЕДУЮЩЕЙ СЕССИИ (31.07 после 03:00 по местному):**
> открыть «Пространство» **с перезагрузкой страницы** (цифры сервера кэшируются на сессию,
> `FRESH.server = Infinity`) и посмотреть блок «Полная синхронизация». Ожидание:
> «Последняя успешная — 31 июля в 03:00», «Запланированная — 1 августа в 03:00».
> **Если там снова 13:03 — это НАСТОЯЩИЙ дефект**, заводить баг: значит ночной проход не
> случился либо `nextScheduledFullPass` считает не то. Проверять по логу контейнера:
> `docker logs ndim-calculator | Select-String "ночной"`.

### 🎯 ГЛАВНОЕ ДЛЯ СЛЕДУЮЩЕГО ЧАТА

**Состояние на 30.07, 17:55: в бою свежее, дерево чистое, беклог агентских задач пуст.**
Прод содержит ВСЮ работу 29–30 июля (В1, В2, В5, В6, В10, `bugs/87`, В9, В12б, В3+В4, В11,
`bugs/81`, `bugs/88`, `bugs/90`, `bugs/67`, `bugs/46`, манифест PWA), сервер синхронизации —
билд 19. Доказательства выката — в разделе выше.

**✅ Пять замечаний владельца ЗАКРЫТЫ и выкачены** двумя выкатами — разделы выше. Открытых
нитей две, обе на владельце:
1. **`/dims` под логином** — ушло ли тёмное вокруг смайлика и хорош ли уезд на 700 мс.
   Гостем эту карточку не проверить; файлы для просмотра лежат на `D:\`.
2. 🔴 **31.07 после 03:00 — блок «Полная синхронизация»** на экране «Пространство»
   (детали и критерий дефекта — в разделе «🔍 ПРОВЕРКА ВЛАДЕЛЬЦА» ниже). Открывать
   С ПЕРЕЗАГРУЗКОЙ страницы: цифры сервера кэшируются на сессию.

**Урок этой сессии, который дороже самих фиксов:** три захода словами дали три промаха, а
замер дал причину с первого раза. Правило владельца — **агент сам записывает экран** — теперь
в постоянной памяти и в приборах (`recordVideo`). Подробности: `EXP-0093`, `EXP-0094`.

**Затем — досмотреть выкат.** Владелец уже проверил две вещи из трёх (см. раздел
выше): `bugs/46` и `bugs/86` в бою работают. Осталось:
1. 🔴 **31.07 после 03:00 — блок «Полная синхронизация»** (детали и критерий дефекта в разделе
   «🔍 ПРОВЕРКА ВЛАДЕЛЬЦА» выше). Это единственная незакрытая нить выката;
2. приложение ставится на телефон заново, иконка и сплэш не пикселят (`bugs/58`, `plans/07` B2);
   старое приложение 1.x обновить нельзя — ключ подписи изъят (`bugs/02`). Нужен телефон владельца.

**Дальше работа есть только по ответам владельца.** Автономных задач беклог не содержит: этот
цикл выгреб всё, что можно было сделать без него. Полный список вопросов — раздел
«⛔ ЧТО ЖДЁТ ВЛАДЕЛЬЦА» ниже; самые дорогие из них — контракт кэша (`bugs/74` часть А, без него
память вида не работает после оценки) и развилки PWA (`researches/19` §5).

⚠️ **Если владелец даст новую волну или ответы — начинай с них, а не с поиска работы в беклоге.**
Открытые документы `bugs/` без тега DONE — это либо ждущие владельца развилки, либо крупные фичи
(`bugs/16`, `19`, `31`, `32`, `44`, `45`, `62` — им нужны макеты и решения, `AGENT_GUIDE` →
«Дизайн»), а не «взял и сделал».

### 🆕 Что сделал этот чат (30.07, автономный цикл, 12 коммитов)

**Волна владельца `ideas/21` — последние пункты:**

| Что | Как проверено |
|---|---|
| **`bugs/88`** (найден по дороге) — тёплая лента «Измерений» выбрасывалась на КАЖДОМ возврате: `rebuilt = screen !== data` истинно всегда, потому что `data` — глубокий прокси `$state`, а `screen` — сырой объект из кэша. Лечение — `$state.raw`. Замер: было 44 карточки → 12, стало 44 с нулевого кадра | стражем `verify-plan08` + трассой по кадрам |
| **В11 / `plans/08`** — память ВИДА экранов: все пять открываются там, где их оставили, «Измерения» помнят вкладку и глубину каждой вкладки. Тонкий слой `src/lib/ui/view-memory.ts` поверх `beforeNavigate`/`afterNavigate`, память в кэше сессии под `view:` | `verify-plan08` **60/0** (до кода 8 провалов), **3 мутации** |
| **`bugs/81`** — витрина лендинга больше не доезжает «на горячую»: число берётся из снимка перед выкатом и лежит в ПРЕРЕНДЕРЕ. Заодно её видят поисковики, а лендинг не стоит чтения Firestore | `verify-bug81` **28/28** по собранному сайту, мутация → 5 провалов |
| **`bugs/78`** (дыра, найденная судьёй) — механизм обновления снова проверяем: юнит на ключи + дверь стенда `window.__ndimRefresh` | `verify-ptr` **0 провалов** (65…66 проверок — число НЕ детерминировано, судить по нулю), две мутации (2 и 8 провалов) |

**Долги, до которых руки не доходили месяцами:**

| Что | Как проверено |
|---|---|
| **`bugs/04` шаг 3** — у правил Storage появились ТЕСТЫ (лежали черновиком без единой проверки) | `test:rules` **85 → 94**, 3 мутации правил |
| **`plans/05` хвост** — «тест-запрет литерала» на лендинге, ненаписанный полтора месяца. Вышел строже: число витрины обязано быть КРАТНО снимку боевой метрики | юнит **192** + e2e **72**, мутации на обоих уровнях |
| **`bugs/46`** — «Последнее обновление NDim ID», последний блок досье связи. Числа не было у экрана по устройству ПРАВ; кладёт его вычислитель в строку топа | `test:calc` **34 → 37**, мутация «время владельца в каждой строке» → 3 падения |
| **`researches/19`** — обзор индустрии по PWA (канон, шаг 7б). **Разблокировал эпик `bugs/83`** | по первоисточникам, ссылки у каждого утверждения |
| **`plans/07` B2 + `bugs/58`** — приложение снова ставится на телефон: манифест + иконки 192/512/maskable/apple-touch, растятся из утверждённого знака | `verify-pwa` **24/24**, 3 мутации |

**Три дефекта, которых никто не искал, — их вскрыла РЕВИЗИЯ СТРАЖЕЙ:**

| Что | Как проверено |
|---|---|
| **`bugs/89`** — корень вакуумного КЛАССА проверок: автовход стенда **вытеснял живую сессию**, поэтому вход по почтовой ссылке был ненаблюдаем принципиально, а вход новичка молча подменялся dev-аккаунтом | мутация «вернуть безусловный автовход» → проверка личности краснеет |
| **`bugs/90`** — 🔴 регрессия от В12б, видимая человеку: **тап по главе руководства никуда не вёз**. Панель глав получила запись истории, её закрытие — popstate, а платформа на popstate возвращает прокрутку записи и отменяла переход | `verify-manual-v1` давал **12 провалов**, теперь зелёный |
| **`bugs/67`** — двухдневный флейк e2e (рельс на 1px ниже шапки) закрыт со второй попытки: запасные `56px` не соответствовали шапке ни на одной ширине (замер: 61 и 55) | **4 падения из 18 → 0 из 18, четыре круга подряд (72 прогона)** |
| Плюс оживлены два СГНИВШИХ стража (`verify-bugs08` был красным месяцами и падал исключением; `verify-bug42` защищал ОТМЕНЁННЫЕ владельцем подписи) и закрыто слепое пятно `probe-bug67` | все зелёные |

### 🆕 Что сделал автономный цикл 30.07 (после `/end-chat` прошлой сессии)

| Что | Как проверено |
|---|---|
| **`bugs/88`** (найден по дороге) — тёплая лента «Измерений» выбрасывалась на КАЖДОМ возврате: `rebuilt = screen !== data` истинно всегда, потому что `data` — глубокий прокси `$state`, а `screen` — сырой объект из кэша. Лечение — `$state.raw`. Замер: было 44 карточки → 12, стало 44 с нулевого кадра | стражем `verify-plan08` + трассой по кадрам |
| **В11 / `plans/08`** — память ВИДА экранов: все пять открываются там, где их оставили, «Измерения» помнят вкладку и глубину каждой вкладки. Тонкий слой `src/lib/ui/view-memory.ts` поверх `beforeNavigate`/`afterNavigate`, память в кэше сессии под `view:` | `verify-plan08` **60/0** (до кода 8 провалов), **3 мутации** |
| **`bugs/81`** — витрина лендинга больше не доезжает «на горячую»: число берётся из снимка перед выкатом и лежит в ПРЕРЕНДЕРЕ. Заодно её видят поисковики, а лендинг не стоит чтения Firestore | `verify-bug81` **28/28** по собранному сайту, мутация → 5 провалов |
| **`bugs/78`** (дыра, найденная судьёй) — механизм обновления снова проверяем: юнит на ключи (**189** тестов) + дверь стенда `window.__ndimRefresh` | `verify-ptr` **0 провалов** (число проверок плавает 65…66), две мутации (2 и 8 провалов) |

### 🆕 Вторая половина цикла: ревизия стражей вскрыла три дефекта

Эстафета просила поискать родню вакуумной проверки (`EXP-0087`). Нашлась — и не одна.

| Что | Как проверено |
|---|---|
| **`bugs/89`** — корень вакуумного КЛАССА был в продукте: ветка стенда `currentSession()` делала безусловный `signInDev()`, то есть автовход **вытеснял живую сессию**. Вход по почтовой ссылке был ненаблюдаем принципиально, а вход НОВИЧКА молча подменялся dev-аккаунтом. Теперь живая сессия настоящего аккаунта уважается | мутация «вернуть безусловный автовход» → проверка личности краснеет. `verify-bug84` 58/0, `verify-bug87` 60/0 |
| **`verify-bugs08` был КРАСНЫМ давно и молча** — его не гоняли месяцами: три проверки `check(имя, true)` (зелёные по написанию), ширина шапки требовала ровно 390/800px при живом `scrollbar-gutter` (−15px), селектор кнопки устарел с `bugs/84` и ронял прогон исключением | всё исправлено, страж зелёный |
| **`bugs/90`** — 🔴 РЕГРЕССИЯ ОТ В12б, видимая человеку: тап по главе руководства **никуда не везёт**. Панель глав получила запись истории, её закрытие — popstate, а SvelteKit на popstate возвращает прокрутку записи (0) и отменял переход | `verify-manual-v1` давал **12 провалов**, теперь зелёный |
| **`verify-bug42` защищал ОТМЕНЁННЫЕ владельцем подписи** («Из них обновлено», «Связей рассчитано», «Следующий цикл») и падал исключением на двух совпадениях «Запланированная» | переписан по блокам, зелёный |
| **`bugs/67`** — двухдневный флейк e2e (рельс на 1px ниже шапки) закрыт со второй попытки: запасные `56px` не соответствовали шапке ни на одной ширине (замер: 61px на узких, 55px от 1024px). Одна переменная `--bar-h-fallback` с медиазапросом | **4 падения из 18 → 0 из 18, четыре круга подряд (72 прогона)**; зонд 41/41 → 0/39 |
| **Слепое пятно зонда `probe-bug67`** («до гидратации И с прокруткой»), из-за которого провалилась попытка №1, закрыто | мутант «запасное 0px» даёт 8 кадров, где рельс на 55px ПОД шапкой. Первая редакция блока была ВАКУУМНОЙ — поймано парной проверкой «реально ли прокручено» |

### 🆕 Третья часть цикла: закрыты долги, до которых руки не доходили месяцами

| Что | Как проверено |
|---|---|
| **`bugs/04` шаг 3** — у правил Storage появились ТЕСТЫ. Они лежали черновиком без единой проверки, и выпускать их было нельзя: на чтении аватаров держится показ лиц. 8 проверок, отказы первыми: чужое фото не перезаписать (и гостю тоже) · не вошедший не читает ничего · всё вне `users/{uid}/avatar/**` закрыто УМОЛЧАНИЕМ · паритет 1.x «вошедший видит чужое фото» зафиксирован тестом, чтобы ужесточение было СОЗНАТЕЛЬНЫМ | `test:rules` **85 → 94**, три мутации правил (2, 1 и 3 падения) |
| **`plans/05` хвост** — «тест-запрет литерала» на лендинге, стоявший ненаписанным полтора месяца. Вышел строже задуманного: число витрины обязано быть КРАТНО снимку боевой метрики — выдуманный литерал этому не удовлетворяет | юнит **189 → 192** (мутация `return 2184` → 2 падения) + e2e **70 → 72** (мутация даёт внятный текст «отношение 23.23 — похоже на выдуманное число») |
| **`plans/05` пункт 3** — мутации на тесты `bugs/07`, тоже висели | снял запись `public_metrics` из вычислителя → **6 падений** `test:calc`; ослабил правило записи витрины → падает ровно «🔒 витрину нельзя переписать с клиента» |
| **`bugs/46`** — «Последнее обновление NDim ID», последний блок досье связи, который был за агентом. Числа не было у экрана по УСТРОЙСТВУ ПРАВ: чужой `points/{uid}` зрителю недоступен. Вычислитель кладёт `updated` соседа в строку топа; поле необязательное — старые боевые `relations` читаются как прежде | `test:calc` **34 → 37**; мутация «время владельца в каждой строке» → 3 падения; `verify-tierA` смотрит на ДАТУ рядом с подписью, мутация «спрятать строку» → 2 падения |

⚠️ **Два операционных капкана, найденных на себе** (записаны в стражах и `AGENT_GUIDE`):
свой `vite preview` на 4173 подменяет сервер `npm run e2e` — полный прогон дал 12 разнородных
падений против СТАРОЙ сборки · у каждого файла тестов вычислителя обязан быть СВОЙ проект
эмулятора, иначе его точки попадают в чужое Пространство (моя первая редакция уронила
`guest_filter`).

**Прогнаны и зелёные ВСЕ стражи проекта:** batch2/3/4 · bug40 (71) · bug42 · bug49 · bug50 ·
bug51 · bug52 (64) · bug54 · bug57 · bug64 (21) · bug68 · bug69 · bug70 · bug75 (44) · bug76 (64) ·
bug77 (64) · bug80 · bug81 (28) · bug84 (58) · bug87 (60) · bugs08 · header-v3a · icons (27) ·
ideas18 · manual-v1 · plan08 (60) · ptr (66) · tierA · wave11 (28) · wave12 (108) · back-twins ·
probe-bug67 · `test:rules` **87/0** · `test:calc` **34/0**.
⚠️ `test:calc` сразу после гашения стенда дал 26 тестов и 1 провал — гонка освобождения порта
эмулятора, а не код: два следующих прогона 34/34. Записано в `AGENT_GUIDE`.

**Регрессия на конец цикла:** юнит **189/189** · typecheck **0** · svelte-check **0 на 416** ·
`npm run build` зелёный · `kaif:check` зелёный · **e2e 70 passed, 2 skipped** (гонялся при
ПОГАШЕННОМ стенде — иначе часть проверок краснеет ложно) · стендовые стражи `verify-bug69`,
`verify-bug70`, `verify-bug75` (44), `verify-bug76` (64), `verify-bug80`, `verify-bug84` (58),
`verify-bug87` (60), `verify-bug54`, `verify-ideas18`, `verify-back-twins` — все зелёные.
⚠️ **Стенд ПОГАШЕН** по дереву (`stand.log` удалён — сирот нет). Поднимать: `npm run stand`.
⚠️ `npm run e2e` пересобрал `build/` — **перед выкатом пересобрать начисто** (EXP-0023).

### 📜 ИСТОРИЯ (чат 30.07 до обеда): что тогда было главным — вакуумная проверка

> ⚠️ Это НЕ текущая повестка: урок усвоен и развит, класс закрыт (`bugs/89`, EXP-0090).
> Текущая повестка — раздел «🎯 ГЛАВНОЕ ДЛЯ СЛЕДУЮЩЕГО ЧАТА» в начале файла.

**Мутация вскрыла ВАКУУМНУЮ ПРОВЕРКУ в страже, который прошлая сессия сдала как доказанный.**
`verify-bug84.mjs` проверял «вошли ИМЕННО в свой аккаунт», заходя на `/menu` и читая почту. Но
**голый `/menu` на стенде входит `dev@ndim.space` САМ** (`currentSession()` → `signInDev()`, если в
URL нет двери `?as=`) — то есть проверка печатала нужную почту независимо от того, чем кончился вход,
и **не могла покраснеть никогда**. Мутант «выбрасываем намерение» дал 54 зелёных и 2 провала, оба на
консоли. Признак перенесён на пилюлю гостя в шапке (`.badge`) на той странице, куда привела ссылка —
там автовхода нет. **Правило на будущее (EXP-0087): мутация провалена, если краснеют только
побочные проверки.** Ищи родню этой ошибки в других стражах: любое «человек вошёл как X» на
дефолтном адресе стенда подозрительно.

### Что сделано в этом чате (11:14–14:15, автономно)

| Ответ | Что сделано | Страж |
|---|---|---|
| **В3+В4** | Две двери гостевой карточки («Сохранить мои результаты» / «У меня уже есть аккаунт») и **врезка подтверждения** вместо мгновенного выхода. Корень половины «б» жалобы вылечен: намерение живёт рядом с почтой (`account.ts` → `PENDING_INTENT_KEY`) и переживает письмо; при `intent=signin` гостевая сессия отпускается ДО `completeLoginLink`. Отказ «почта занята» переводит карточку в ОТДЕЛЬНОЕ состояние «Эта почта уже Ваша», а не дописывает красную строку; гостевой заголовок над врезками снят | `verify-bug84` **28 → 58**, мутация |
| **В9** | Ряд смайликов + замедленный улёт (подробнее ниже) | `verify-bug80` **44/44**, 2 мутации |
| **В12б** | «Назад» закрывает лайтбокс и панель глав | `verify-back-twins` **48/48**, мутация |

| Ответ | Что сделано | Страж |
|---|---|---|
| **В9** | Ряд из 11 смайликов в карточке «Измерений» — появляется, когда звезда выбрана и ждёт сохранения, исчезает по снятию выбора (дословно слово владельца). Лица — общим компонентом **`src/lib/ui/GradeFace.svelte`**: сниппет из `DocBlocks.svelte` переехал в него, копии разметки в проекте нет. **Улёт карточки замедлен вдвое** — новая длительность канона `MOTION.gesture = 640 мс` (+ CSS-зеркало `--motion-gesture`): уезд ЗА КРАЙ перестал делить хронометраж с перестроением списка | `verify-bug80` **44/44**, 2 мутации |
| **В12б** | «Назад» закрывает **лайтбокс фото** и **панель глав** — тем же штатным `pushState` + `page.state`, что окно «Как меня видят». Оба близнеца объявлены в `app.d.ts` | `verify-back-twins` **48/48**, мутация |
| попутно | Лог службы сервера синхронизации **врал** «00:00 по местному времени» при сравнении по UTC (`calculator/index.mjs:661`) | — |

**🔴 Находка, которая исправляет запись выше в этом же файле:** **В12 НЕ «без ответа»** — владелец
ответил на оба пункта ещё в коммите `57d65fe`, ответы лежат в интервью строками «Ответ: - ты прав…».
Прошлая сессия написала «В12 — БЕЗ ОТВЕТА», прочитав только поле `**Ответ:**` внизу вопроса.
**(а) ящик поиска — оставить открытым, это уже текущее поведение, работы нет.**

**⚠️ Приборы соврали в эту сессию ПЯТЬ раз** (уроки — `EXP-0086` и `EXP-0087`, читай их):
1. Вакуумная проверка автовхода стенда — см. «ГЛАВНОЕ» вверху. Самая дорогая.
2. Замер улёта карточки спрашивал «есть ли `transform`» и дал **1666 мс при заведомых 640** — то есть
   был **зелёным по неверной причине**: карточка несёт `transform` и в покое (его ставит
   `animate:flip`), а в замер попадали клик и запись в Firestore. Переписан на сдвиг вправо; трасса
   печатается флагом `--trace` и показывает `994:37 … 1610:480` — ровно 480px, **633 мс**.
3. Кадр тёмной темы застал ряд смайликов **посреди перехода `slide`** — лица срезаны сверху
   (`overflow: hidden`), и на скриншоте это выглядело точь-в-точь как дефект вёрстки. Я успел так и
   решить. `waitFor({state:'visible'})` возвращается, как только высота больше нуля.
4. Страж близнецов кликал по капсуле глав **до гидратации пререндеренной страницы** и краснел на
   исправном коде: `.pager` лежит в готовом HTML и находится селектором раньше, чем Svelte навесил
   обработчики.
5. **`innerText` в Chromium отдаёт текст УЖЕ применённым `text-transform`** — регистрозависимая
   проверка заголовка не находила «Войти в свой аккаунт?», потому что `h3` в карточке рисуется
   ПРОПИСНЫМИ. Сравнивай слова без учёта регистра: начертание — забота CSS.

**Состояние дерева на конец чата:** `main` чист и **запушен** · юнит **186/186** · typecheck **0** ·
svelte-check **0 на 414** · `npm run build` зелёный · `kaif:check` зелёный.
⚠️ **СТЕНД УПАЛ, а не оставлен поднятым** (поправка 14:35). Прощальный отчёт того чата обещал живой
стенд — обещание неверно: фоновая задача вышла с кодом 1, эмуляторы Firestore/Auth/Storage погашены
по SIGINT (`Script "node tools/seed-dev.mjs && node tools/stand.mjs" exited with code 1`).
**Поднимай его сам: `npm run stand`** (нужна Java). Причина падения не выяснена — упало уже после
всех прогонов, все стражи в тот день были зелёными на живом стенде. Если не поднимется — сначала
поищи сироту `node calculator/index.mjs` (`bugs/73`, `AGENT_GUIDE` → «Испытательный стенд»); признак
сироты — не удаляется `stand.log`, гасить ПО ИМЕНИ процесса, а не по площади (EXP-0081).

> 🔍 **Поправка к поправке — факт, снятый наблюдением (автономный цикл, 14:24).** Ко́д выхода 1
> получила ФОНОВАЯ ЗАДАЧА, а сам стенд в этот момент был **жив целиком**: слушали порты 8181,
> 9099, 9199 и 5173, в процессах стояли `firebase emulators:exec`, `node tools/stand.mjs`,
> `node calculator/index.mjs` и `vite dev`, все четыре отвечали HTTP 200. На нём и прошли ВСЕ
> прогоны этого цикла. То есть «стенд упал» — про задачу харнесса, а не про процессы; призрака
> искать не надо. Погашен он позже и НАМЕРЕННО — по дереву, перед прогоном `npm run e2e`
> (`stand.log` удалён, сирот нет). Признак, по которому это различать: `npm run stand` падает с
> «Port … is not open», значит прежний стенд ЖИВ, а не мёртв.
>
> ⚠️ **В этот день в одном рабочем каталоге работали ДВА чата.** Параллельная сессия сделала
> коммит `6b66523` (эту самую поправку) поверх коммитов автономного цикла и запушила их вместе
> со своим. Ничего не потерялось, но следующей сессии стоит знать: история `main` за 30.07
> писалась в два пера, и `git log` тут честнее любого отчёта.

### Что сделано этим утром (после ответов владельца)

Ответы закоммичены **дословно** до правок (`AGENT_GUIDE` шаг 16). Реализовано:

| Ответ | Что сделано |
|---|---|
| **В1=Б** | Блок «Данные» убран ЦЕЛИКОМ вместе с кнопкой обновления. ⚠️ Цена названа владельцу ДО решения и принята: пути обновления БЕЗ жеста больше нет (WCAG 2.2 SC 2.5.7), на десктопе остаётся перезагрузка страницы. Записано в коде и в страже — **это не регрессия, а следствие**. `verify-ptr` 47/47 |
| **В2=Б** | Витрина лендинга — живое число × множитель, как в 1.x. Взято ×30 (94 → ≈2 800), потому что множитель 1.x (862) дал бы 81 028 |
| **В5=А** | «Из них обновлено» → «Записано изменений», «Связей рассчитано» → «Связей в Пространстве» |
| **В6** | Час ночного прохода переведён на **UTC** — точный паритет с 1.x |
| **В10=В** | «В Пространстве с …» убрано из профиля; фича «Статус» расписана `plans/09`, без жалоб |
| **`bugs/87`** | Экран больше не врёт вошедшему гостю «Вы не вошли». Страж на ТРИ состояния — 60/60 |

**🔍 Две находки разведкой 1.x, которые изменили решения** (архив на диске: `d:/work/ai_sandbox/ndim_old`):

1. **Счётчик 1.x** (`app.js:2119-2122`): `userCount * 777 + * 77 + * 7 + userCount` = **×862**, и
   рядом собственный комментарий владельца **«TEST - временно, убрать»**.
2. **Час суточной сверки 1.x** (`app.js:10376-10378`): `nowUTC.getHours() === 0` с комментарием
   «00 по Лондону» — то есть фактически **00:00 UTC**. Летом Лондон = UTC+1, а проход был по UTC.
   ⚠️ Довод владельца («привязывался ко времени сброса лимитов Firebase») **фактом не
   подтверждается**: квоты Firestore сбрасываются около полуночи по ТИХООКЕАНСКОМУ времени.
   Привязка не работала и в 1.x. Поведение повторено, факт вынесен владельцу.

### Что осталось из ответов владельца (не начато)

· **В9=А с правкой** — ряд смайликов при выбранной несохранённой звезде (снимаешь выбор — ряд
  исчезает) + **замедлить анимацию улёта** сохраняемого измерения («сейчас слишком быстро, не
  понятно, что происходит»);
· **В3=А + В4=А** — врезка подтверждения выхода гостя и две двери в гостевой карточке
  (вторая половина `bugs/84`; механику намерения агент делает сам);
· **В11=А** — память прокрутки в живой вкладке (`plans/08`, `bugs/74`);
· **В12 — БЕЗ ОТВЕТА** (ящик поиска при смене вкладки; близнецы «Назад» — лайтбокс и панель глав).

### 🚀 ВЫКАЧЕНО В БОЙ (слово владельца: «деплой прод и конец чата»)

Гейты → чистая пересборка (`build/` снесён) → `npx firebase deploy --only hosting
--project ndim-space` → **оба таргета release complete**.

**Дифф выката проверен:** `firestore.rules`, `firestore.indexes.json`, `firebase.json` — **0
файлов**. В диффе есть `calculator/` и `src/lib/model/stats.ts`: модель едет с приложением
(добавлено необязательное поле), **а сервер синхронизации хостингом НЕ выкатывается** — см. ниже.

**Признак снят ДО и ПОСЛЕ, включая ОБРАТНЫЙ** (строка обязана была ИСЧЕЗНУТЬ):

| Признак | до | после |
|---|---|---|
| «Обновить данные» в чанках `/menu` (В1 — убрано) | **1** | **0** |
| «Записано изменений» в чанках `/space` (В5) | 0 | **1** |
| «Связей в Пространстве» / `startedUpAt` (В5, bugs/86) | 0 | **1** |
| «Не удалось загрузить Ваши данные» (bugs/87) | 0 | **1** |

**Сверка боя живым браузером:** `node tools/verify-prod-b4.mjs` — **81/81** · витрина лендинга
показывает **«С нами уже 2 820 человек»** (94 × 30, В2) · лица персонажей `opacity: 1,1,1`.

### 📜 ИСТОРИЯ: «сервер синхронизации в бой не поехал» — ✅ ПОЕХАЛ 30.07 в 17:52 (билд 19)

> ⚠️ Ниже — состояние на утро 30.07. Образ пересобран и контейнер заменён вечером того же дня,
> см. раздел «🚀 ВЫКАЧЕНО В БОЙ» в начале файла. Читать как историю, а не как задачу.

Правки `bugs/85`/`bugs/86` (ночной проход по UTC, `startedUpAt`, `checked` без гостей) живут в
`calculator/` и выкатываются **отдельно**, контейнером на серверном ПК владельца:

```
npm run calc:image        # соберёт образ с честным номером сборки
# затем заменить боевой контейнер
```

⚠️ **До этой пересборки** виджет «Пространства» в бою покажет «Последний запуск — **неизвестно**»
(поля `startedUpAt` в боевом `space/server` ещё нет) и прежнюю «Запланированную» от часа подъёма
контейнера. Это ожидаемо и честно, а не дефект.

### Мелочь, замеченная и НЕ тронутая

В «Меню», в карточке «Аккаунт», строка «В Пространстве с …» **осталась** — владелец просил убрать
её из ПРОФИЛЯ, про «Меню» не говорил. Спросить.

---

### ⚖️ Судейский проход перед push (что он поймал на МНЕ)

Прогнан по канону перед push. Все заявленные числа воспроизведены заново:
`verify-plan08` **60/0** · `verify-pwa` **24/0** · `verify-bug81` **28/0** · `verify-ptr`
**0 провалов** · `test:rules` **94** · `test:calc` **37** · юнит **192** · e2e **72** ·
правила Firestore, индексы и `firebase.json` за весь цикл **не тронуты** (дифф пуст).

Но две вещи он нашёл, и обе исправлены ДО push:

1. **`verify-bug81` зависел от поднятого стенда, и шапка об этом молчала.** Страж гоняется по
   собранному сайту, но `isStand()` смотрит на hostname — приложение целится в эмуляторы, и
   ВОРОНКА (`track('landing_view')`) пишет в Firestore. При погашенном стенде это
   `ERR_CONNECTION_REFUSED`, и страж краснел на исправном продукте: числа «28/28» были верны
   только в одном состоянии окружения. Теперь отказы эмуляторов прощаются **адресно** (по портам
   8181/9099/9199), любой другой упавший запрос остаётся провалом.
2. **Первая редакция этой же починки вышла СЛЕПОЙ:** фильтр выкидывал любое «Failed to load
   resource» — вместе с «responded with a status of 404», то есть с НАШИМ битым файлом. Сужен до
   сетевого отказа `net::ERR_`. Попутно выяснилось приятное: битую ссылку на свой файл ловит
   **сама сборка** — пререндер SvelteKit валится на ней раньше любого стража.

Плюс поправлена собственная неточность отчёта: `verify-ptr` цитировался числом «66», хотя его
число проверок **не детерминировано** (65…66) — это записано в этом же файле как правило, и я его
же нарушил. Судить его надо по «0 провалов».

> ## 🎯 ЧТО ОСТАЛОСЬ ПО ВОЛНЕ `ideas/21` — ТОЛЬКО ТО, ЧТО ЖДЁТ ВЛАДЕЛЬЦА
>
> Все 16 пунктов волны закрыты или разложены. Живого кода в ней больше нет; открыты ровно эти
> вопросы к человеку:
> · `bugs/80` подпункты **(а) звёзды в таблице манифеста** и **(в) адаптивный `clamp()`** —
>   вопросы 1 и 3 в разделе «⛔ Ждёт владельца», замер шага 0 не снят;
> · `bugs/84`: **текст ошибки** для ссылки БЕЗ намерения и расширенная фраза врезки (`[AI]`);
> · `bugs/74` **часть А** — контракт кэша: гасить ли собранный экран своей же оценкой
>   (вопросы 1 и 2). Это единственное, что мешает памяти вида работать после оценки;
> · `plans/08` вопросы **Б, В, Г** — агент взял их по своей рекомендации и помечает это честно,
>   нужна вычитка;
> · `bugs/83` — ссылка из письма мимо PWA: эпик, требует обзора индустрии (шаг 7б) и слова
>   владельца. PWA у 2.0 нет вовсе;
> · `ideas/23` / `plans/09` — публичный «Статус» вместо «В Пространстве с …»;
> · **мелочь:** строка «В Пространстве с …» осталась в «Меню» (владелец просил убрать из
>   ПРОФИЛЯ) — спросить.
>
> ✅ **Закрыты и проверены: В9, В12б, В3+В4, В11, `bugs/81`, `bugs/78`, `bugs/88`.**
> Новые стражи, которые надо держать зелёными: `verify-bug80` (44) · `verify-back-twins` (48) ·
> `verify-bug84` (**58**, было 28).
>
> **Что осталось открытым по уже закрытым пунктам** (владелец на это НЕ отвечал):
> · `bugs/80` подпункты **(а) звёзды** и **(в) адаптивный `clamp()`** — вопросы 1 и 3 в разделе
>   «⛔ Ждёт владельца» документа живы, замер шага 0 не снят;
> · `bugs/84`: **текст ошибки** `profile/+page.svelte` для случая, когда ссылка приходит БЕЗ
>   намерения, и расширенная фраза врезки — она помечена `[AI]` и ждёт вычитки владельца;
> · ❓ **непроверенная гипотеза**: переживает ли одноразовый oob-код неудачный
>   `linkWithCredential`. Я её обошёл — дверь «Это мой аккаунт» просит НОВОЕ письмо, а не
>   переиспользует код. Проверить можно на стенде; если код живёт, отказ можно спасать на месте.

## 📜 ИСТОРИЯ: эстафета прошлого чата (ИСПОЛНЕНА 30.07 целиком)

> ⚠️ Всё, что она просила, сделано: В11 закрыт, `bugs/81` закрыт, близнецы пройдены, родня
> вакуумной проверки найдена. Ниже оставлено ради контекста — там записаны приёмы и ловушки,
> которые пригодятся снова. **Задачами это больше не является.**
> Прошлая сессия писала: «Ты продолжаешь волну владельца `ideas/21`… закрыто 14 из 16» — сегодня
> закрыто всё, что не требует владельца. ⚠️ Один её совет по-прежнему в силе: ответы владельца в
> `interviews/interview_007_ideas21_forks.md` стоят в ДВУХ видах — полем `**Ответ:**` и строками
> `Ответ: - …` под подпунктами; читай весь текст вопроса, а не одно поле.

1. ~~**В9 — ряд смайликов и скорость анимации**~~ (`bugs/80`) — **ЗАКРЫТО 30.07.**
2. ~~**В3+В4 — вторая половина `bugs/84`**~~ — **ЗАКРЫТО 30.07.** Намерение живёт в
   `account.ts` → `PENDING_INTENT_KEY`, путь входа выбирает ОНО; выход гостя двухступенчатый.
3. **В11 — память прокрутки** (`plans/08` + `bugs/74`, ответ А: живёт в живой вкладке,
   перезагрузка обнуляет). Замер: 3 экрана из 3 забывают позицию (ушёл с 900 → вернулся на 0).
   ⚠️ Признак программной прокрутки `restoringScroll` из `bugs/75` **обязан переехать** в общую
   механику — иначе дефект панели родится заново на каждом экране.
4. **`bugs/81`** — счётчик лендинга доезжает «на горячую» (кадр 0 — строки нет, кадр 17 — есть).
   Теперь чинить есть смысл: решение о самом числе принято (В2=Б, множитель ×30).
5. **`bugs/83`** — ссылка из письма мимо PWA. PWA у 2.0 **нет вовсе** (ни манифеста, ни SW, ни
   assetlinks) — у владельца установлено приложение 1.x. Эпик, требует обзора индустрии (шаг 7б).
6. ~~**В12 остался БЕЗ ОТВЕТА**~~ — **НЕВЕРНО, ответы есть** (см. верх файла): ящик поиска оставить
   открытым (уже так), близнецов «Назад» — пройти. **Оба близнеца пройдены 30.07.**

### 📜 ИСТОРИЯ: что нашёл судья после выката 29.07 — ✅ ЗАКРЫТО 30.07 (`bugs/78`)

**Механизм обновления данных остался БЕЗ ЕДИНОЙ исполняемой проверки.** Следствие В1, которое
никто не назвал заранее. `refreshNow()` теперь достижим ровно из одного места —
`src/lib/ui/PullToRefresh.svelte:98`, то есть только жестом, а жест **намеренно не эмулируется**
(синтетический `touchmove` доказывал бы работу прибора, а не продукта). Пока строка «Обновить
данные» жила, она была единственным исполняемым входом, и через неё страж доказывал, что
обновление реально идёт в базу. Инвалидация `relations:*` / `space` / `ratings:*` / `profile`
сегодня не покрыта ничем запускаемым. Варианты лечения расписаны в `bugs/78` (рекомендация: юнит на
`invalidateAll` + стендовая дверь `?refresh=1` по образцу `?as=guest`).

> ✅ **ЗАКРЫТО 30.07 автономным циклом** — сделаны оба пункта рекомендации: список ключей уехал в
> `data/cache.ts` → `invalidateRefreshable()` (юнит-тесты, 189) и заведена дверь стенда
> `window.__ndimRefresh` (только localhost). `verify-ptr` — **0 провалов** (проверок 65…66,
> число плавает по устройству прибора), мутации дали 2 и 8 провалов.
> Необследованным остался ровно один участок — обработчики touch; их проверяет рука владельца.

**Мелочь того же разбора:** у `tools/verify-ptr.mjs` **число проверок не детерминировано** — одна
пропускается, когда экран короче окна (`verify-ptr.mjs:188`). Прогоны дают 46…**51** (51 снят
30.07 — вилка шире, чем думали 29.07). Судить его надо
по «0 провалов», а не по фиксированному числу; в отчётах фиксированное число не цитировать.

**Предыдущее обновление:** 2026-07-29, ночь — 🌊 **ВОЛНА ВЛАДЕЛЬЦА `ideas/21` (16 ПУНКТОВ) РАЗЛОЖЕНА
НА БЕКЛОГ И ЧИНИТСЯ В АВТОНОМНОМ ЦИКЛЕ. ЗАКРЫТО 7 ПУНКТОВ, РАЗБЛОКИРОВАН ВХОД ВЛАДЕЛЬЦА В СВОЙ
АККАУНТ.**

> ⚠️ **Главное, что должна знать следующая сессия.** Дороже всех фиксов — две вещи.
> **(1) На стенде существовало РОВНО ОДНО состояние человека** — вошедший `dev@ndim.space`. Значит
> гостевые ветки и карточка входа (дверь для 331 человека из 1.x) **не проверялись НИКОГДА**. Ровно
> поэтому дожил до боя дефект «гость заперт». Заведены двери **`?as=guest` и `?as=none`**
> (`AGENT_GUIDE.md` → «Испытательный стенд»). Пишешь про гостя или вход — ходи через них.
> **(2) Приборы врали в этой сессии СЕМЬ раз**, и каждый раз в сторону «зелено на дефекте»:
> `getComputedStyle` у `::-webkit-search-cancel-button` отдаёт ширину поля (249px) и `appearance:
> auto` при погашенном крестике · встроенный **Chromium вообще не рисует** нативный крестик (страж
> переведён на `channel: 'chrome'`) · страж `bug69` **придерживал графику роутом, и задержка сама
> лечила симптом** · `hasBytes` у SVG опирался на то же событие `load`, что и баг · `history.length`
> **не убывает** при возврате · сессия Firebase живёт в **IndexedDB**, не в localStorage · переход
> счётчика `null → «4»` считался «одним значением».

### Что сделано этой сессией

**0. Волна принята по канону.** Оригинал владельца закоммичен дословно (`d9801fe`) ДО правок,
затем структурирован. Разведка по коду — **18 агентов** (9 кустов + состязательная сверка каждого),
все 16 пунктов подтверждены или честно помечены «нужен живой прогон». Заведено **16 документов**:
`bugs/74-86`, `ideas/22-23`, `plans/08`. Плюс `bugs/87`, найденный по дороге.

**1. 🔧 Прибор на всю волну — `node tools/probe-ideas21.mjs`.** Один прогон снимает «до/после» по
девяти измеримым пунктам. Им сделан замер до кода.

**2. ✅ ЗАКРЫТО 7 ПУНКТОВ ВЛАДЕЛЬЦА** (каждый — страж, доказанный мутацией):
· **п. 11** `bugs/82` — лица Насти, Макса и Алисы висели прозрачными НАВСЕГДА. Лендинг пререндерен,
  `load` у SVG-картинки проходит до гидратации, а `complete` у неё не существует → спрашиваем кэш
  браузера служебной `new Image()`. Страж `bug69` расширен тремя способами, потому что **был
  зелёным на этом дефекте**.
· **п. 7** `bugs/79` — 41 правило `:hover` в 11 файлах закрыто `@media (hover: hover)`; такого
  медиазапроса в репозитории не было **ни разу**. Видно только в тёмной теме и только тапом.
· **пп. 4+5** `bugs/77` — они КОНФЛИКТОВАЛИ: нативный крестик живёт лишь при фокусе, а закрытие
  Android-клавиатуры требует фокус снять. Своя кнопка очистки + `blur()` на сабмите. **64/64**,
  три мутации.
· **п. 2** `bugs/75` — панель уезжала от НАШЕГО ЖЕ `scrollTo` при смене вкладки. Признак
  программной прокрутки + форс показа — то, что владелец делал в 1.x тремя вызовами. **44/44**,
  две мутации в разные стороны (вторая стережёт, чтобы «починка» не прибила панель насмерть).
· **п. 3** `bugs/76` — окно «Как меня видят» не имело записи в истории. Штатное неглубокое
  маршрутирование SvelteKit (`pushState` + `page.state`), первое применение `$app/*` в проекте.
  **64/64**.
· **п. 13** `bugs/84` — 🔑 **владелец снова может войти в свой аккаунт.** Гостю дана «Выйти»
  (переиспользование готовой строки). Страж **28/28** проходит ВЕСЬ путь: гость → «Выйти» →
  карточка входа → ссылка на почту (oob-код у эмулятора) → он в СВОЁМ аккаунте.

**3. ⛔ Что ЖДЁТ ВЛАДЕЛЬЦА (агент не решает):** `bugs/74` контракт кэша · `bugs/78` что именно
убрать в «Данных» (конфликт с интервью №006 и WCAG) · `bugs/80` макеты шкалы · `plans/08` срок
жизни памяти состояния · `ideas/22` честность против заманухи (отменяет `bugs/07`) · `ideas/23`
публичный «Статус» · вторая половина `bugs/84` (подтверждение выхода, две двери, текст ошибки).

**4. 📌 Ответ на прямой вопрос владельца о цифрах сервера (`bugs/85`):** цифры
**непротиворечивы — врут ПОДПИСИ.** «Пользователей проверено» = пересчитано в памяти (бесплатно),
«Из них обновлено» = реально записано в Firestore (0 = ничьи связи не изменились), «Связей
рассчитано» = стоячий итог Пространства. А вот **ночного полного прохода действительно НЕТ**:
`fullPass` считается от `startedAt` процесса, якорь — момент подъёма контейнера (13:03), и любой
рестарт гонит полное вычитывание базы. Канон 1.x иной — `daily_sync в 00:00 по Лондону`.

**5. 🚀 ВЫКАЧЕНО В БОЙ** (слово владельца: «деплой прод и дальше работаем»). Гейты → чистая
пересборка (`build/` снесён) → `npx firebase deploy --only hosting --project ndim-space` → **оба
таргета release complete**.

**Дифф выката проверен, а не заявлен:** `git diff e0248a5..HEAD -- calculator firestore.rules
firestore.indexes.json firebase.json src/lib/model` → **0 файлов**. Сервер синхронизации, правила,
модель и индексы НЕ тронуты.

**Выкат доказан признаком, снятым ДО и ПОСЛЕ** (снимок «до» сделан ДО деплоя специально):

| Признак | до | после |
|---|---|---|
| «Очистить поле поиска» (своя кнопка очистки, `bugs/77`) | 0 чанков | **1** |
| `hover:hover` в боевом CSS (`bugs/79`) | **0** | 2 |
| `seeMe` / `pushState` в чанках профиля (`bugs/76`) | 0 | **1** |

**Сверка боя живым браузером:** `node tools/verify-prod-b4.mjs` — **81/81** ·
`PROBE_BASE=https://ndimspace.app node tools/verify-bug69.mjs --quick` — **все зелёные** ·
точечно лица персонажей на боевом лендинге: `opacity: 1`, класс `ok` у всех трёх, кадр
`test-results/ideas21/prod-map.png` **смотрен глазами** — Алиса, Настя и Макс на месте.

⚠️ **Чего гость проверить НЕ МОЖЕТ:** вход в свой аккаунт (`bugs/84`), панель «Измерений»,
окно «Как меня видят» — всё это под логином. Нужен взгляд владельца.

**Состояние дерева:** `main` чист · юнит **186/186** · typecheck **0** · svelte-check **0 на 413** ·
`kaif:check` зелёный · стенд поднят.

### 🎯 Эстафета: что делать ДАЛЬШЕ

1. **`bugs/85` + `bugs/86` — сервер синхронизации.** Причина доказана (см. выше): сделать проход
   НОЧНЫМ по канону 1.x, сохранять `lastFullPassAt` вне памяти процесса, завести время подъёма
   сервера. Подписи виджета — текст, нужна вычитка владельца.
2. **`plans/08` + `bugs/74` — память прокрутки** (замер: 3 экрана из 3 забывают позицию). ⚠️ Признак
   программной прокрутки из `bugs/75` обязан переехать в общую механику, иначе дефект панели
   родится заново на каждом экране.
3. **`bugs/87`** — свежий гость без документа видит «Вы не вошли» (ветки `down` и `signedout`
   печатают один текст). Найден новыми дверями стенда.
4. **Собрать интервью по шести развилкам** (список в п. 3 выше) — без него семь пунктов волны
   стоят.
5. **Близнецы `bugs/76`** — лайтбокс фото и панель глав, приём готов, ~20 строк на оба.

**Команды:** `npm run stand` · `node tools/probe-ideas21.mjs` (замер всей волны) ·
`verify-bug75.mjs` (44) · `verify-bug76.mjs` (64) · `verify-bug77.mjs` (64, **настоящий Chrome**) ·
`verify-bug84.mjs` (28) · `verify-bug69.mjs` (графика, две скорости) · `npm test` (186).

---

**Предыдущее обновление:** 2026-07-29, вечер — 🧹 **БЕКЛОГ ПРОГРУМЛЕН + 🚀 `bugs/40` ПУНКТ 2 ЗАКРЫТ И
ВЫКАЧЕН В БОЙ: ЗАГРУЗОЧНЫЙ ЩИТ ВМЕСТО МЕЛЬКАНИЯ ЛЕНДИНГА** (просьбы владельца: «грумь беклог, что
дальше по планам бери в работу из багов и импрувов», затем «деплой прод»). Сессия закрыта `/end-chat`.

> ⚠️ **Главное, что должна знать следующая сессия:** три ловушки этой работы жили В ПРИБОРАХ, а не в
> продукте, и каждая давала уверенное ложное показание. Самая дорогая — **`vite dev` КЭШИРУЕТ
> `app.html`** (HMR обновляет только `.svelte`/`.ts`): два вывода подряд я сделал о ПРЕДЫДУЩЕЙ версии
> продукта. Правишь шаблон оболочки — **перезапускай стенд** (EXP-0083). Вторая: признак «правка
> доехала» греп нашёл в моём же комментарии рядом (EXP-0084) — маркер мутации обязан быть уникален.
> Третья: страж спрашивал **атрибут** `hidden` вместо видимости и был зелёным на дефекте, который
> **видно на скриншоте невооружённым глазом**. Канон владельца «смотреть кадры глазами» окупился.

### Что сделано этой сессией

**1. 🧹 ГРУМИНГ БЕКЛОГА — беклог не врёт.** Открыто честно: **20 багов · 10 идей · 4 плана**. Тегов
`DONE` никому не полагается: `bugs/06` и `17` — фиксы **в бою, но ждут контрольного взгляда
владельца** (закрывать своим словом нельзя); `bugs/08`/`09` — не задачи, а **индексы волн**, в них
ещё висят живые пункты (16, 17, 19, 20, 28, 31, 32). Из **Tier A** (`plans/07`) остались двое:
`bugs/46` (две строки времени) и `bugs/06` (взгляд владельца).

**2. ✅ `bugs/40`, ПУНКТ 2 — ЗАГРУЗОЧНЫЙ ЩИТ.** Разведка ДО кода (канон, шаг 7а): boot-флоу 1.x снят
с живого `ndim-old` → `researches/12` → «Boot-экран: механика целиком». Главное, чего не было в
инвентаре: 1.x держал щит **статической разметкой в `index.html`, видимой с первого кадра**, и гасил
его не таймером, а **принятым решением** — всегда ПОСЛЕ `openPage(...)`. Отсюда «красиво открывало».

**Замер тем же прибором, что снимал «до»** (`node tools/measure-bug40.mjs`): кадров лендинга
**4 · 5 → 0 · 0**, кадров кольца **0 · 0 → 4 · 4** (число кадров щита зависит от скорости отклика
стенда — на повторной проверке было 49 · 5; значение имеет НОЛЬ кадров лендинга, а не эта величина).

⚠️ **Прибор проверен на подгонку** (я правил его вместе с фиксом — уточнял, что «кадр лендинга» это
не-накрытый лендинг): прогон ЭТОГО ЖЕ прибора на коде с отключённым щитом даёт **75 · 6 кадров
лендинга**. Видеть лендинг он не разучился — значит ноль после фикса заслуга продукта.

Устройство: щит поднимает **инлайн-скрипт `app.html`** (тот же, что применяет тему) — любой щит,
который рисует Svelte, опаздывает на гидратацию, и это ровно измеренные 4–5 кадров. Знак —
**инлайн-SVG** (правило bugs/69 иначе невыполнимо). Потолок **15 с** с кнопкой «Перезагрузить» —
канон 1.x. Признак сессии `ndim-session` читается **синхронно** (Firebase хранит сессию в IndexedDB,
к первому кадру правды от SDK нет), и держит его **ОДНА точка** — постоянный слушатель в
`firebase.devAuth()`. ⚠️ Первая версия расставляла вызовы по местам входа и **пропустила стендовую
ветку** (`signInDev` в обход `waitForSession`) — щит не поднимался вовсе.

🔴 **У ГОСТЯ ЩИТА НЕТ** — корень это единственная индексируемая страница продукта.

**Страж `node tools/verify-bug40.mjs` — 71/71** (обе темы × 390/1440), трижды подряд, **доказан
двумя мутациями с подтверждённым доездом до сервера**: щит не поднимается → **28 провалов**; сужение
ширины → «1425×900 против 1440×900» (дефект класса `bugs/65` — щит не накрывал полосу
`scrollbar-gutter`).

**3. 🚀 ВЫКАТ В БОЙ** (слово владельца: «деплой прод»). Гейты → чистая пересборка (`build/` снесён) →
`npx firebase deploy --only hosting --project ndim-space` → **оба таргета release complete**.

**Дифф выката проверен, а не заявлен:** `git diff 4915c39..HEAD -- calculator firestore.rules
firestore.indexes.json firebase.json src/lib/model` → **0 файлов**.

**Выкат доказан признаком, снятым ДО и ПОСЛЕ:** `id="boot"` в боевом `index.html` было **0**, стало
**1**; `data-booting` — **0 → 6**; размер `index.html` **24 858 → 33 629** байт. Снимок «до» сделан
ДО деплоя специально, чтобы признак что-то доказывал.

**Сверка боя живым браузером:** `node tools/verify-prod-b4.mjs` — **81/81** · точечная проверка щита
на `ndimspace.app` — **10/10**: 🔴 **гость щита НЕ видит** (ни одного кадра из 25, обе ширины) и
лендинг у него виден · с признаком сессии щит поднимается, накрывает окно целиком, строка одна
(«Пространство NDim») в обеих темах. Кадры `test-results/prod-boot/` смотрены глазами.
⚠️ Щит в бою у ВОШЕДШЕГО агент проверить не может — под логином владельца нужен его взгляд.

**Состояние дерева:** `main` чист и запушен · юнит **186/186** · typecheck **0** · svelte-check
**0 на 413** · e2e **70** · `kaif:check` зелёный · стенд погашен ПО ДЕРЕВУ, сирот **0**.

### 🎯 Эстафета: что делать СЛЕДУЮЩЕМУ чату

1. **Взгляд владельца в бою под его логином — единственное, чего агент не смог проверить.** Открыть
   `ndimspace.app` по базовой ссылке вошедшим: ожидание — вместо мелькания лендинга виден щит (знак
   N-сети + «Пространство NDim» + кольцо), затем открывается профиль. Заодно проверяется **пункт 1**
   бага (см. ниже).
2. **`bugs/40` пункт 1 (редирект не срабатывает) — ОСТАЁТСЯ ОТКРЫТЫМ, только бой.** На стенде
   редирект исправен в обоих раундах; причину в коде редиректа не искать, пока не воспроизведён под
   логином владельца. ⚠️ Щит его **маскирует**: человек теперь видит кольцо вместо лендинга, поэтому
   симптом мог измениться — спросить владельца, доходит ли дело до профиля.
3. **Проверка жеста pull-to-refresh пальцем на телефоне** (висит с прошлой сессии) — синтетический
   `touchmove` не воспроизводит физику браузера.
4. **Контрольный взгляд владельца в бою, под его логином:** Профиль ↔ Связи ↔ Пространство ↔
   Измерения ↔ Меню два-три раза — со второго захода экраны появляются **сразу, без «Загрузки»**.
5. **`bugs/67`** — причина доказана, фикс НЕ найден; заходить со стороны «убрать асинхронную
   публикацию `--bar-h`». ⚠️ Судья — `npx playwright test e2e/desktop-shell.spec.ts
   --project=desktop --repeat-each=4`, а НЕ `tools/probe-bug67.mjs` (у зонда слепое пятно).
6. **Вычитки владельца:** две `[AI]`-фразы руководства (bugs/55 → «Решения…», п. 0) · черновик
   «Версия 2.0» (`researches/06`, `[AI]`).
7. `bugs/46` (две строки времени, Tier A) · `bugs/62`+`58` (PWA — эпическая фича, нужен обзор
   индустрии) · `ideas/19` · `ideas/16` (трогает модель и правила) · `bugs/65` · **пачка 4 целиком
   ждёт владельца**: `bugs/16`+`19`+`32` · `17` · `20`+`41` · `28` · `31`.

**Команды:** `npm run stand` · `node tools/verify-bug40.mjs` (щит, 71 — ⚠️ **правил `app.html` →
перезапусти стенд**) · `node tools/measure-bug40.mjs` (замер мелькания) · `node tools/verify-ptr.mjs`
(обновление, 60) · `node tools/verify-prod-b4.mjs` (смоук БОЯ, 81) · `npm test` (186) ·
`npm run e2e` (⚠️ выжигает `test-results/` — EXP-0062; гасить стенд ПО ДЕРЕВУ).

---

**Предыдущее обновление:** 2026-07-29, день — 🚀 **PULL-TO-REFRESH СДЕЛАН ЦЕЛИКОМ И ВЫКАЧЕН В БОЙ**
(слово владельца: «и деплой в конце, чтобы я потестил в проде»). Интервью №006 закрыто: владелец
ответил на все четыре развилки, реализация разблокирована и исполнена за один заход.

> ⚠️ **Главное, что должна знать следующая сессия:** новый страж показал **40 падений из 48** — и
> продукт был исправен ПОЛНОСТЬЮ. Врал прибор, тремя независимыми способами сразу (мерил до
> гидратации · счётчик чтений не находил URL-кодированные пути · ожидание конца работы возвращалось
> мгновенно). Самое опасное из трёх: проверка «повторный заход бесплатен — 0 чтений» была **зелёной
> от неисправности счётчика**. Отсюда правило, стоившее сегодня дороже всего — **EXP-0082**: у
> стража, где «ноль» это хорошая новость, обязан быть КОНТРОЛЬ САМОГО ПРИБОРА в том же прогоне.

### Что сделано этой сессией

**1. 📋 ИНТЕРВЬЮ №006 ЗАКРЫТО — владелец ответил на все четыре развилки.** Ответы В1=А, В2=А, В3=А
лежали в рабочей копии незакоммиченными (сессия их и нашла); В4=А получен в чате. Оба раза слово
владельца закоммичено ДОСЛОВНО ДО любых правок (канон, шаг 16) — коммиты `3598479` и `08ccabf`.

**2. ✅ PULL-TO-REFRESH РЕАЛИЗОВАН ПО МАКЕТУ V1** (`0794d39`):
· `src/lib/data/refresh.svelte.ts` — ядро: инвалидация `relations:*` / `space` / `ratings:*` /
`profile`, отметка времени, определение поддержки жеста (В3: `CSS.supports`, Safari<16 отсекается);
· `src/lib/ui/PullToRefresh.svelte` — жест и индикатор, форма из макета (круг 34px, порог 64px,
разворот стрелки, кольцо на обновлении). `overscroll-behavior-y: contain` — В2=А: отобрано только
обновление, отскок оставлен браузеру;
· строка `↻ Обновить данные · только что` в «Меню» (карточка «Данные») — путь БЕЗ жеста, которого
требует **WCAG 2.2 SC 2.5.7**, как только жест забран у браузера, и единственный путь на десктопе;
· загрузчики четырёх экранов вынесены в `loadScreen()` — обновление перечитывает ровно то же, что и
заход. ⚠️ В «Профиле» порядок внутри не косметика: экран объявляется готовым ДО чтения сводки связей.

**Решение, принятое без владельца и записанное в интервью:** полотно экрана НЕ едет вниз — двигается
только индикатор, как в Android (Material). На iOS содержимое пружинит само, браузером, которого мы
по В2=А не трогаем.

**3. 🛡 Страж `node tools/verify-ptr.mjs` — 60/60** (обе темы × 390/1440), **доказан мутацией**:
отключил инвалидацию → 4 падения ровно на «тап реально перечитывает». В логе видно, ЧТО прочитано
(`users/…`, `points/…`), а не только «больше нуля».

**4. 🚀 ВЫКАТ В БОЙ. Доказан признаком, снятым ДО и ПОСЛЕ:** строки «Обновить данные» в боевых
чанках было **0 из 23**, стало **1 из 24**. Дифф выката проверен, а не заявлен:
`git diff 6fb54cb..HEAD -- calculator firestore.rules firestore.indexes.json firebase.json
src/lib/model` → **0 файлов**. Смоук боя живым браузером `verify-prod-b4.mjs` — **81/81**.

⚠️ **ЧЕГО АГЕНТ ПРОВЕРИТЬ НЕ МОЖЕТ И ЖДЁТ ОТ ВЛАДЕЛЬЦА:** сам жест пальцем на телефоне.
Синтетический `touchmove` не воспроизводит физику прокрутки браузера — зелёная такая проверка
доказывала бы работу прибора, а не продукта (класс EXP-0078/0080). Ожидаемое поведение: тянете вниз
на «Связях» → из-под шапки выезжает круг → за порогом стрелка разворачивается вверх → отпустили →
кольцо крутится → данные перечитаны.

**Состояние дерева:** `main` чист · юнит **186/186** · typecheck **0** · svelte-check **0 на 413** ·
e2e **70** · `kaif:check` зелёный · стенд погашен ПО ДЕРЕВУ, сирот **0**.

### 🎯 Эстафета: что делать СЛЕДУЮЩЕМУ чату

1. **Проверка жеста владельцем на телефоне** (см. выше) — единственное, что осталось от этой работы.
2. **Контрольный взгляд владельца в бою, под его логином** (висит с прошлых сессий): Профиль ↔ Связи
   ↔ Пространство ↔ Измерения ↔ Меню два-три раза — со второго захода экраны появляются **сразу,
   без карточки «Загрузка»**.
3. **`bugs/67` — причина доказана, фикс НЕ найден.** Не крути запасное значение (обе стороны
   измерены и обе врут); заходи со стороны «убрать асинхронную публикацию `--bar-h`». ⚠️ Судья —
   `npx playwright test e2e/desktop-shell.spec.ts --project=desktop --repeat-each=4`, а НЕ
   `tools/probe-bug67.mjs` (у зонда слепое пятно, описано в его шапке).
4. **`bugs/40` (boot-экран) — лучший автономный заход, замер уже сделан.** Брать ПУНКТ 2 (boot-экран
   вместо мелькания): воспроизводим на стенде, есть прибор для «до и после». Пункт 1 (редирект) в
   автономию не годится — на стенде не воспроизводится.
5. **Вычитки владельца:** две `[AI]`-фразы руководства (bugs/55 → «Решения…», п. 0) · черновик
   «Версия 2.0» (`researches/06`, `[AI]`).
6. `bugs/62`+`58` (PWA) · `ideas/19` · `bugs/65` · **пачка 4 целиком ждёт владельца**:
   `bugs/16`+`19`+`32` · `17` · `20`+`41` · `28` · `31`.

**Команды:** `npm run stand` · `node tools/verify-ptr.mjs` (страж обновления, 60) ·
`node tools/verify-prod-b4.mjs` (смоук БОЯ, 81) · `node tools/verify-ideas18.mjs` (страж кэша, 30) ·
`npm test` (186) · `npm run e2e` (⚠️ выжигает `test-results/` — EXP-0062; гасить стенд ПО ДЕРЕВУ).

---

**Предыдущее обновление:** 2026-07-29, утро — 🚀 **В4 ВЫКАЧЕН В БОЙ** (слово владельца: «деплой
прод») **+ владелец выбрал макет pull-to-refresh: V1 «Как в системе»** («v1, как в системе»)
**+ закрыты `bugs/72` (не дефект) и `bugs/73` (оснастка), доказана причина `bugs/67`, сделан
замер `bugs/40`.** Сессия закрыта паузой в 11:00 по просьбе владельца.

> ⚠️ **Главное, что должна знать следующая сессия:** два из четырёх разобранных багов оказались
> НЕ ТЕМ, чем выглядели. `bugs/72` («каретка сбрасывается», воспроизводимость 5 из 5) —
> **вообще не дефект продукта**: реверс существует только в состоянии, недостижимом для человека.
> `bugs/67` — причина доказана, но мой фикс **опровергнут настоящим тестом уже после пуша**.
> Оба раза врал МОЙ СОБСТВЕННЫЙ прибор. Отсюда два правила, которые стоили сегодня дороже всего:
> **(1)** воспроизведя дефект, спроси — достижимо ли это состояние для живого человека
> (EXP-0080); **(2)** не объявляй готовым то, что проверено только своим прибором, когда рядом
> есть независимый судья (EXP-0078).

### Что сделано этой сессией

**1. 🚀 ВЫКАТ В БОЙ решения В4** (интервью №005: топ и статистика живут всю сессию, пульс сервера
остаётся минутным). Гейты: юнит **186/186** · typecheck **0** · svelte-check **0 ошибок на 411
файлах** · `kaif:check` **28/28**. Чистая пересборка (`build/` снесён) → `npx firebase deploy
--only hosting --project ndim-space` → **оба таргета release complete**.

**Дифф выката проверен, а не заявлен:** `git diff 53398e7..HEAD -- calculator firestore.rules
firestore.indexes.json firebase.json src/lib/model` → **0 файлов**. Сервер синхронизации, правила,
модель и индексы НЕ тронуты.

**Выкат доказан признаком, снятым ДО и ПОСЛЕ** (а не кодами ответов): минифицированный контракт
свежести в боевом чанке был `{mine:1/0,server:6e4}`, стал **`{mine:1/0,server:1/0,pulse:6e4}`**.
Снимок «до» сделан ДО деплоя специально, чтобы признак что-то доказывал. ⚠️ Первый кандидат в
признаки (`'space:server'`) был отвергнут — он лежал в бою и раньше.

**Сверка боя:** 15 маршрутов — 200, зеркало `ndim-space.web.app` — 301; **живой браузер гостем,
5 экранов × 2 темы × 390/1440 — 81/81** (`node tools/verify-prod-b4.mjs`, новый инструмент):
страницы реально ОТРИСОВАНЫ (высота + текст, а не «200 OK»), тема применена, консоль чиста.
Кадры `test-results/prod-b4/` смотрены глазами: лендинг живой («С нами уже 94 человека»),
рельс и тёмная тема на месте, гостю честно предложено войти.
⚠️ **Чего гость проверить НЕ МОЖЕТ:** сам эффект кэша — нужен взгляд владельца под логином.

**2. 🎨 ВЛАДЕЛЕЦ ВЫБРАЛ МАКЕТ pull-to-refresh — V1 «Как в системе»** (дословно: *«v1, как в
системе»*). Зафиксировано в **`interviews/interview_006_pull_to_refresh.md`**: чистый жест с
индикатором под шапкой, НОЛЬ новых элементов на экранах, путь без жеста — строка `↻ Обновить
данные · 12 минут назад` в «Меню». Цена принята осознанно и записана: самый чистый экран и самая
низкая находимость.
⚠️ **Кода НЕ писать: четыре сопутствующих развилки ЖДУТ ОТВЕТА владельца** (заданы и в документе,
и текстом в чат): В1 где живёт жест (рекомендация — все четыре главных экрана) · В2 отбирать ли
«резинку» (рекомендация — только обновление) · В3 Safari младше 16 (рекомендация — жест не
включается, строка в «Меню» работает везде) · В4 текст строки.

**3. 🎯 `bugs/67` — КОРНЕВАЯ ПРИЧИНА НАЙДЕНА** (документ бага говорил «причина НЕ найдена»).
Виновата не гонка со шрифтами и не округление, а **запасное значение, которое врёт в бОльшую
сторону, в паре с `position: sticky`**: `SideRail.svelte:80` держит `top: var(--bar-h, 56px)`,
`--bar-h` публикуется только при гидратации, настоящая шапка на 1440 — **55px**, а липкий элемент
прижимается вниз до `top` даже при нулевой прокрутке. Отсюда ровно 1px, всегда в одну сторону, на
любом маршруте и старше всей работы над данными — версия объясняет ВСЕ пять наблюдений документа.

**✅ ПОДТВЕРЖДЕНО НАБЛЮДЕНИЕМ, а не рассуждением** — новый зонд `node tools/probe-bug67.mjs`
(покадровая трасса, метод EXP-0060; отдельно от `npm run e2e`, потому что тот выжигает
`test-results/` — EXP-0062). **1080 кадров: где `--bar-h` пуста — расхождение в 41 из 41 кадра;
где заполнена — в 0 из 1039.** Та же трасса **опровергает обе прежние гипотезы**: во всех падающих
кадрах `fonts=loaded` (гонки шрифтов нет), а высоты целые `barH=55 / railY=56` (округления нет).
Окно дефекта ~3 кадра на загрузку (~50 мс) — отсюда «1 прогон из 3».

**❌ А ВОТ ФИКС НЕ ВЫШЕЛ — И ЭТО ГЛАВНЫЙ УРОК ДНЯ (`bugs/67` остаётся ОТКРЫТЫМ).** Попробовал
запасное значение `var(--bar-h, 56px)` → `0px` («ноль не может прижать»). Свой зонд подтвердил:
41/41 → **0/42**, инвариант `bugs/49` после прокрутки — 12/12 чисто. Гейты зелёные. Закоммитил и
**запушил как готовое**. Затем ради полноты запустил НАСТОЯЩИЙ тест по рецепту из самого бага:

```
desktop-shell.spec.ts:70   Expected: 55   Received: 0
```

Рельс заезжал **ПОД шапку** — буквальная регрессия `bugs/49` («ДОЛЖНО БЫТЬ ЗАФИКСИРОВАНО», слово
владельца капсом). **Фикс откачен, DONE-тег снят, 56px возвращены** как знающий компромисс с
объяснением прямо в коде.

**Почему зонд соврал (это ценнее самой попытки):** он мерил «до гидратации БЕЗ прокрутки» и «после
гидратации С прокруткой» — два условия по отдельности, — а цена нуля живёт в их **пересечении**,
куда прибор не заходил ни разу. Слабость записана в шапке `tools/probe-bug67.mjs`, чтобы следующая
сессия не поверила его зелёному так же, как поверил я. Уроки — **EXP-0078**.

**Что теперь известно и чего не было утром:** причина доказана; обе прежние гипотезы (шрифты,
округление) опровергнуты; доказано, что у запасного значения **нет безопасной стороны** (завышенное
врёт на 1px без прокрутки, заниженное прячет рельс под шапку с прокруткой) — значит крутить
константу бессмысленно, лечить надо асинхронную публикацию `--bar-h`. Кандидат следующего захода
записан в баге: объявить высоту шапки CSS-константой по брейкпойнтам (как Material и HIG), тогда
публиковать нечего и гонки нет вовсе. ⚠️ Это меняет вид шапки — возможно, нужен взгляд владельца.

**4. ✅ `bugs/72` ЗАКРЫТ — ЭТО НЕ ДЕФЕКТ ПРОДУКТА.** Кода не написано ни строки, потому что чинить
нечего.

**Строгое A/B в одном процессе** (`node tools/probe-bug72-open.mjs`) — одно поле, один способ
ввода, отличается ТОЛЬКО состояние ящика поиска: **открыт → «Кошки» 3/3, закрыт → «икшоК» 3/3.**
Закрытый ящик — это `max-height: 0; overflow: hidden` у `.toolbar`: поле схлопнуто и человеку
недоступно физически (чтобы напечатать букву, надо сначала нажать «Поиск измерения»). **Состояния,
в котором возникает реверс, у живого человека не существует** — а автоматика попадает в него легко,
потому что Playwright считает схлопнутое поле «видимым» (в точности **EXP-0071**, найденный в тот
же день на другом приборе; оба замера родились из одной ошибки оснастки).

Подтверждено и с другой стороны: ловушки на ЖИВОМ элементе поймали 7 событий за ввод и **ни одного**
`value=` / `setSelectionRange` / `setAttribute` — приложение к полю не прикасается вовсе, а каретка
на `beforeinput` **уже** 0, то есть не сбрасывается, а никогда не продвигается: браузер не двигает
её в неотрисованном элементе.

**Урок — EXP-0080: воспроизведя дефект, спроси, достижимо ли это состояние для человека.**
«5 из 5» доказывает воспроизводимость, но не существование дефекта — пять раз подряд можно надёжно
воспроизвести собственную ошибку.

**По дороге туда закрыты и остальные двери** (`node tools/probe-bug72.mjs`):
· **Дверь 1 (главная в документе): это НЕ артефакт `vite dev`.** Сравнение стало возможным, когда
нашлось, что `isStand()` смотрит на **hostname `localhost`**, а не на режим сборки: `vite preview`
с той же сборкой, что едет в бой, ходит в те же эмуляторы. Реверс «икшоК» одинаков на :5173 и
:4173 — чинить придётся по-настоящему.
· **Дверь 3 закрыта:** ни один из 8 предков поля не задаёт `rtl` или нестандартный `unicode-bidi`.
· **Дверь 2 закрыта наполовину:** два РАЗНЫХ пути ввода (клавиатурные события и `insertText` в их
обход) дают один и тот же реверс — способ ввода ни при чём.
· 🔥 **Новая улика:** голый `<input>`, вставленный в ЭТУ ЖЕ страницу, печатается ВЕРНО (прошлый
контроль ставился на `about:blank` и доказывал лишь исправность инструмента). Исключено ВСЁ
окружение — глобальные слушатели, стили, шрифты, тема, роутер. Виноват сам компонент.
· Разметка осмотрена и **обыкновенна** (`bind:value={search}` в форме, без `oninput` и `{#key}`) →
подозреваемых осталось двое: кто ещё пишет в переменную `search`, и пара `bind:value` +
`type="search"` в Svelte 5. Урок — **EXP-0079**.

**5. 📏 `bugs/40` (boot-экран) — СДЕЛАН ЗАМЕР ДО КОДА** (`node tools/measure-bug40.mjs`), и он
**развёл два пункта бага в разные стороны**:
· **Пункт 1 «редирект вошедшего не срабатывает» — НА СТЕНДЕ НЕ ВОСПРОИЗВОДИТСЯ:** редирект честно
происходит, оба раунда. Значит либо это бой (холодный старт Auth по сети медленнее localhost с
эмулятором), либо условия владельца. **Не ищите причину в коде редиректа, пока не воспроизвели** —
на стенде он исправен; нужен прогон в бою под логином владельца.
· **Пункт 2 «мелькает лендинг, boot-экрана нет» — ПОДТВЕРЖДЁН И ИЗМЕРЕН:** человек видит лендинг
**4–5 кадров** до прыжка, кадров с кольцом загрузки — **0**. Это чинится независимо от пункта 1,
на стенде, с прибором для «до и после». ⚠️ 4–5 кадров — НИЖНЯЯ оценка: на стенде Auth отвечает
мгновенно, в бою окно шире.

**6. ✅ `bugs/73` ЗАВЕДЁН И ПОЧИНЕН — `npm run stand` больше не отказывает молча.** Воспроизводился
3 раза за сессию и стоил мне ~20 минут; стоил бы столько же каждой следующей сессии.

**Причина:** `child.kill()` на Windows убивал только обёртку `cmd.exe` (стенд запускает детей с
`shell: true`, иначе не стартуют `.cmd` от npm/npx), а внук `node calculator/index.mjs` выживал,
крутил цикл каждые 15 с и мешал поднять стенд заново. Наверх при этом **не выходило ничего** —
команда выглядела работающей.

**Фикс** (`tools/stand.mjs`): убийство по ДЕРЕВУ через `taskkill /T /F` · `process.on('exit')` как
последний рубеж · **ворота громкости** — стенд ждёт, пока эмулятор реально начнёт слушать, и при
отказе падает с кодом 1 и готовой командой поиска сироты.

**Доказано тремя наблюдениями:** запуск без эмулятора — было «бесконечное капание в лог», стало
**выход 1 за 30 с** с причиной · остановка стенда — сервер синхронизации **1 → 0** · перезапуск
(сам симптом) — **2 порта из 2**.

⚠️ **Чего фикс не покрывает, названо в баге:** если убить сам `stand.mjs` ПРИНУДИТЕЛЬНО
(`Stop-Process -Force`, как делает автоматика), обработчики выхода не исполняются в принципе и
сирота остаётся — предел ОС. Поэтому ворота громкости не менее важны: остаточный случай теперь
диагностирует себя сам. Урок — **EXP-0081** (ловушки уборки: у сироты в командной строке НЕТ пути
проекта, а фильтр по `*calculator/index.mjs*` находит собственный PowerShell и убивает его).
⚠️ Дешёвый индикатор «чисто ли»: если `stand.log` не удаляется — сироты живы.

**7. 🧹 Груминг беклога — беклог не врёт.** Подозрительные документы сверены **с кодом**, а не со
своим описанием: в `src/` нет ни `beforeinstallprompt`, ни `deleteUser`/`updateEmail`, ни
`@media (min-width: 600px)`, ни boot-экрана, ни service worker → `bugs/40 · 44 · 45 · 58 · 62 · 20`
открыты честно, тегов `DONE` никому не полагается. ⚠️ Первые три грепа были сделаны
`Select-String -Recurse` (такого параметра нет), молча упали и напечатали «открыт честно» из
else-ветки — переделано нормальным инструментом. **Урок: вывод упавшей проверки — не результат.**

### 🎯 Эстафета: что делать СЛЕДУЮЩЕМУ чату

1. **📋 ЧЕТЫРЕ ОТВЕТА ВЛАДЕЛЬЦА по pull-to-refresh** — `interviews/interview_006_pull_to_refresh.md`.
   Без них кода не писать (канон). Как ответы придут — реализация: жест + строка в «Меню» +
   инвалидация ключей `relations:*`, `space`, `ratings:*` + страж `tools/verify-ptr.mjs`.
2. **Контрольный взгляд владельца в бою, под его логином** (висит с прошлой сессии, теперь
   особенно ценен — В4 в бою): Профиль ↔ Связи ↔ Пространство ↔ Измерения ↔ Меню два-три раза,
   со второго захода экраны появляются **сразу, без карточки «Загрузка»**.
3. **`bugs/67` — причина доказана, фикс НЕ найден.** Не крути запасное значение (обе стороны
   измерены и обе врут); заходи со стороны «убрать асинхронную публикацию `--bar-h`» — кандидат
   расписан в баге. ⚠️ Судья — `npx playwright test e2e/desktop-shell.spec.ts --project=desktop
   --repeat-each=4`, а НЕ `tools/probe-bug67.mjs` (у зонда слепое пятно, описано в его шапке).
4. **Вычитки владельца:** две `[AI]`-фразы руководства (bugs/55 → «Решения…», п. 0) · черновик
   «Версия 2.0» (`researches/06`, `[AI]`).
5. ~~`bugs/72`~~ — **ЗАКРЫТ** как «не дефект» (см. выше). ⚠️ Если где-то ещё страж печатает в
   поле внутри выдвижного ящика — открывай ящик ПО РАЗМЕТКЕ (`.toolbar.open`), иначе получишь
   такой же призрак (EXP-0071 + EXP-0080).
6. **`bugs/40` (boot-экран) — следующий лучший автономный заход, замер уже сделан.** Брать надо
   ПУНКТ 2 (boot-экран вместо мелькания): он воспроизводим на стенде и есть прибор для «до и
   после». Пункт 1 (редирект) в автономию не годится — на стенде не воспроизводится.
   Перед кодом — разведка boot-флоу 1.x в `ndim-old` (канон, шаг 7а).
7. `bugs/62`+`58` (PWA — люди потеряли установленное приложение) · `ideas/19` · `bugs/65` (ждёт
   слова владельца).
7. **Пачка 4 целиком ждёт владельца:** `bugs/16`+`19`+`32` · `17` · `20`+`41` · `28` · `31`.

**Состояние дерева на момент паузы:** `main` чист и запушен · юнит **186/186** · typecheck **0** ·
svelte-check **0 на 411 файлах** · `kaif:check` **28/28** · продуктовый код изменений НЕ содержит
(вся работа дня — оснастка, документы и один выкат уже готового кода) · стенд погашен, сирот нет.

**Команды:** `npm run stand` (нужна Java; теперь падает громко, если эмулятор не поднялся —
bugs/73) · `node tools/verify-prod-b4.mjs` (смоук БОЯ, 81) · `node tools/verify-ideas18.mjs`
(страж кэша, 30) · `node tools/measure-bug40.mjs` (замер мелькания лендинга) ·
`node tools/probe-bug72-open.mjs` (образец: как мерить поле в выдвижном ящике) · `npm test` (186) ·
`npm run e2e` (⚠️ выжигает `test-results/` — EXP-0062; гасить стенд ПО ДЕРЕВУ — EXP-0059/0027).

---

**Предыдущее обновление:** 2026-07-28 — ✅ **ВЛАДЕЛЕЦ ОТВЕТИЛ НА ИНТЕРВЬЮ №005 (кэширование).
Все четыре развилки закрыты: три — записью, четвёртая — кодом. Топ связей и статистика больше
не перечитываются внутри сеанса. Заведена следующая работа: pull-to-refresh — обзор индустрии
готов, 4 макета ЖДУТ ВЫБОРА ВЛАДЕЛЬЦА.**

### Что сделано этой сессией

**0. Ответы владельца закоммичены дословно ДО правок** (канон, шаг 16): В1 = A (кэш живёт только
в живой вкладке: *«Закрыл браузер — пока-пока данные»*) · В2 = A (живые подписки не сейчас) ·
В3 = A (при возврате во вкладку не освежать) · **В4 — свой ответ**: *«ТОП обновляется очень
редко. Нет смысла его обновлять. Пользователь зайдёт в приложение — получит свежие данные. Этого
хватит»* + pull-to-refresh на главных экранах, начиная со «Связей».

**1. 📌 В1/В2/В3 — закрыты записью, кода не потребовали.** Перенесены в `researches/17` **§8**
таблицей «развилка → решение владельца → что это значит в коде». Соответствие кода решениям
проверено грепом, а не на слово: персистентного кэша нет, `onSnapshot` в нашем коде **0
вхождений**, обработчика «вернулись во вкладку» не заведено. Будущая сессия прочитает решение,
а не откроет вопрос заново.

**2. ✅ В4 — сделано в коде** (`src/lib/data/cache.ts`, `space.ts`, `relations.ts`).
`FRESH.server` → **вся сессия**. ⚠️ **Главное, чего не было в вопросе:** под именем «данные
сервера синхронизации» жили ДВЕ сущности, и решение применимо только к одной. Цифры (топ,
счётчики, тренды) застывают безобидно; **сердцебиение** `space/server` — нет: из него
`syncServerState` ВЫВОДИТ лампочку «Работает» относительно «сейчас», с терпимостью в три цикла,
и заморозка показала бы **живой сервер умершим**. Поэтому пульс выделен в **`FRESH.pulse` = 60 с**
и в отдельную запись кэша `KEYS.syncServer` — общую для «Пространства», «Меню» и «О системе»
(побочно исчезло дублирующее чтение между ними). Урок — **EXP-0075**.

**Замер прибором** `node tools/measure-ideas18.mjs --rounds 4 --slow 0` (91 с реального времени):
цена повторных заходов по кругу из пяти экранов — **чтений 10 → 3**, лоадеров 0. Все три чтения
ОБЪЯСНЕНЫ наблюдением, а не списаны на шум: 1 документ пульса раз в минуту (замысел) + первый
настоящий заход на стартовый экран прогона `/profile` (EXP-0072: клик по вкладке, на которой
браузер уже стоит, навигацией не является).

**Проверка:** страж `node tools/verify-ideas18.mjs` — **30/30**, обе темы × 390/1440 · юнит
**186** (+1) · typecheck/check **0/0**. Тест контракта свежести переписан вместе с контрактом и
дополнен **вторым** тестом, стерегущим не число, а причину: запись пульса обязана устаревать
раньше, чем молчание становится поводом сказать «Не отвечает» (выражено через
`HEARTBEAT_TOLERANCE`). **Мутация** `pulse = Infinity` роняет оба. Подсказки обоих инструментов
правились вместе с кодом — они печатали «окно свежести 60 с», которого больше нет (EXP-0067).

**3. 📚 `researches/18` — обзор индустрии по pull-to-refresh** (канон, шаг 7б: новый механизм →
сначала читаем мир). По первоисточникам (MDN · Chrome for Developers · WebKit · W3C/WAI ·
Material · Apple HIG · Nielsen Norman Group). Три находки, без чтения не появившиеся бы:
· **жест в вебе НЕ свободен** — у верхней границы его слушает браузер, свой PTR не добавляется,
а **отбирается** через `overscroll-behavior`, и в WebKit это возможно только с **Safari 16.0**
(MDN до сих пор: «Limited availability… not Baseline»);
· **WCAG 2.2 SC 2.5.7 (AA)** — пока обновляет браузер, критерий не применяется («determined by
the user agent»), но как только жест наш, у него обязан быть равноценный путь БЕЗ перетаскивания
(плюс на десктопе тянуть нечем вовсе);
· **Apple HIG считает жест ДОБАВКОЙ** к автоматическому обновлению — а у нас после В4
автообновления внутри сеанса нет. Расхождение записано осознанно, чтобы следующая сессия не
«чинила» его как дефект.

**4. 🎨 `design/pull-to-refresh-mockups.html` — 4 макета, ЖДУТ ВЫБОРА** (правило четырёх макетов).
V1 «Как в системе» (чистый жест, путь без жеста — строка в «Меню») · **V2 «Жест + строка
свежести»** (рекомендация агента: «Обновлено 12 минут назад · Обновить» — закрывает разом
находимость, десктоп, доступность и честность «данные из памяти») · V3 «Жест + значок в шапке» ·
V4 «Без жеста, только кнопка». Жест в макетах **живой** — проигрывается по кругу с порогом,
сменой стрелки и кольцом обновления. Смотрено живым Edge в трёх моментах цикла
(`test-results/ptr-mockups/`); связка «классы CSS ↔ классы разметки» сходится в обе стороны.
Первая редакция рисовала в шапке пункт «⋯», которого в `AppBar` нет, — выдуманный элемент убран.

⚠️ **Что владелец должен знать про В4** (сказано ему в документе интервью): оценил измерение →
топ помечается устаревшим и перечитывается сразу, но сервер к этому моменту мог ещё не досчитать
(цикл 60 с + тихий период 120 с), и прочитанное **замрёт до конца сеанса**. Это ровно тот случай,
ради которого он назвал pull-to-refresh, — то есть жест закрывает не только «хочу свежее».

### 🎯 Эстафета: что делать СЛЕДУЮЩЕМУ чату

1. **🎨 ВЫБОР ВЛАДЕЛЬЦА ПО МАКЕТАМ pull-to-refresh** — открыть
   `design/pull-to-refresh-mockups.html` в браузере (жест играет сам). Кроме варианта нужны
   ответы на 4 вопроса внизу страницы: где живёт жест · отбирать ли «резинку» · что делать в
   Safari младше 16 · текст строки свежести. **Кода до этого не писать** (канон).
2. **Контрольный взгляд владельца в бою, под его логином** (висит с прошлой сессии): пройти
   Профиль ↔ Связи ↔ Пространство ↔ Измерения ↔ Меню два-три раза — со второго захода экраны
   появляются **сразу, без карточки «Загрузка»**. ⚠️ В4 в бой ещё **НЕ выкачен** — лежит в
   `main`, ждёт слова владельца о деплое.
3. **Вычитки владельца** (висят с прошлых сессий): две `[AI]`-фразы руководства (bugs/55 →
   «Решения…», п. 0) · черновик «Версия 2.0» (`researches/06`, `[AI]`).
4. **`bugs/72`** (каретка в поле поиска сбрасывается в начало) — три непроверенные двери в
   документе; первая: боевой это дефект или артефакт `vite dev`.
5. `ideas/19` (виджет «Аккаунт» в профиль) · `bugs/40` (boot-экран) · `bugs/62`+`58` (PWA) ·
   `bugs/65` (гуттер) · `bugs/67` (флейк e2e 1px).
6. **Пачка 4 целиком ждёт владельца** (макеты и развилки): `bugs/16`+`19`+`32` · `17` · `20`+`41`
   (планшет) · `28` · `31`.

**Команды:** `npm run stand` (эмуляторы + сервер + vite; нужна Java) · `node tools/verify-ideas18.mjs`
(страж кэша, 30 проверок) · `node tools/measure-ideas18.mjs --rounds 4 --slow 0` (прибор замера) ·
`npm test` (186) · `npm run e2e` (⚠️ гасить стенд ПО ДЕРЕВУ ПРОЦЕССОВ — EXP-0059/0027).

---

**Предыдущее обновление:** 2026-07-28, ночь — 🚀 **`ideas/18` (кэш данных экранов на сессию)
ЗАКРЫТА И ВЫКАЧЕНА В БОЙ ВМЕСТЕ СО СВЯЗКОЙ «ИЗМЕРЕНИЯ»** (слово владельца: «деплой прод тоже»).
Первой сделана разведка по новому канону (шаг 7б) — `researches/17`, обзор индустрии, — и она
опровергла кандидат решения, записанный в самой идее.

### Что сделано той сессией

**1. 📚 `researches/17` — обзор индустрии по кэшированию данных клиента** (первое применение
шага 7б: «почти на всё в индустрии есть золотые стандарты»). Прочитаны первоисточники: RFC 5861
(**stale-while-revalidate — это стандарт IETF**, с обязательным потолком на показ устаревшего),
TanStack Query и SWR (две независимые оси: свежесть ≠ жизнь в памяти; инвалидация пометкой
ключа), Workbox и bfcache (слой страниц), документация Firestore и SvelteKit.
**Главная находка — опровергнут кандидат №1 самой `ideas/18`:** встроенный кэш Firestore SDK
**не экономит онлайновые чтения**. Дословно из Google Cloud: *«By default, a `get` call will
attempt to fetch the latest document snapshot from your database… will use the offline cache
if the network is unavailable or if the request times out»*. Кэш SDK — про офлайн, а не про
навигацию, и вдобавок открывает соединение с IndexedDB (задевает bfcache). **Урок EXP-0074:
обзор индустрии окупается тем, что убивает правдоподобное неверное.**

**2. ✅ `ideas/18` — кэш данных экранов на сессию.** Ядро — один модуль
`src/lib/data/cache.ts` по модели SWR: `peek` отдаёт лежащее в памяти **синхронно** (первый
кадр экрана уже с данными, лоадера нет вовсе), `cached` решает, нужен ли поход в базу,
`invalidate` гасит ключи по началу имени, `forgetAll` забывает всё; дедупликация параллельных
чтений внутри. Контракт свежести из `ideas/14`: мои данные — вся сессия (записи гасят кэш
сами), данные сервера синхронизации — 60 с, по его же циклу, и за окном освежаются **тихо
поверх показанного экрана**. Приватность: у кэша есть хозяин, смена uid уничтожает содержимое,
выход из аккаунта зовёт `forgetAll()` + `forgetAvatars()`.

**Замер одним прибором** (`tools/measure-ideas18.mjs`; «до» снято на чистом HEAD через
`git stash`, чтобы сравнивать одно с одним): цена повторных заходов по кругу из 5 экранов —
**чтений документов 49 → 10, карточек «Загрузка» экрана 8 → 0.**

**3. 🔧 Обе оснастки пришлось чинить по ходу — они врали.** (а) Считали HTTP-запросы к
эмулятору, а у Firestore это в основном служебные кадры WebChannel: три ОДИНАКОВЫХ захода
дали 5 → 1 → 0. Теперь считаются пути документов в теле запроса — та единица, за которую берут
деньги (**EXP-0073**). (б) «Холодным» заходом был клик по вкладке, на которой браузер уже
стоял, — навигации нет, и «тёплым» оказывался первый настоящий заход (**EXP-0072**).

**Регрессия сессии:** юнит **185** (+12: `src/lib/data/cache.test.ts`) · e2e **70** (+2 skip) ·
typecheck/check **0/0** · `kaif:check` зелёный · страж `node tools/verify-ideas18.mjs` —
**30 проверок** (обе темы × 390/1440), доказан двумя мутациями (снят мгновенный кадр →
4 падения; отключена свежесть → 5). Скриншоты `test-results/ideas18/` смотрены глазами.

**4. 🚀 ВЫКАТ В БОЙ 2026-07-28, ночь** (слово владельца: «деплой прод тоже»). Повёз разом
**`ideas/18`** (кэш сессии) и **связку «Измерения»** (`bugs/54` отмена оценки повторным тапом +
`ideas/20` поиск по явному вводу), лежавшую с прошлой сессии. Гейты → чистая пересборка
(`rm -rf build` + `npm run build`) → `npx firebase deploy --only hosting --project ndim-space`,
**оба таргета release complete**.

**Дифф выката проверен, а не заявлен:** `git diff 40a2576..HEAD -- calculator firestore.rules
firestore.indexes.json firebase.json src/lib/model` → **0 файлов**. Сервер синхронизации,
правила, модель и индексы НЕ тронуты.

**Сверка боя:** 14 маршрутов — 200, зеркало `ndim-space.web.app` — 301; **боевые чанки `/dims`
реально несут выкаченное** (ключи кэша `ratings:screen` и `relations:summary`, кнопка «Искать»)
— проверено грепом по 21 скачанному чанку, а не по кодам ответов; живой браузер в обеих темах ×
1440/390 (`test-results/prod-ideas18/`): лендинг со счётчиком, «Меню», «Измерения» гостю честно
предлагают войти, консоль чиста (отказы правил гостю — ожидаемы).

⚠️ **Чего гость проверить НЕ МОЖЕТ:** сам эффект кэша виден только под логином. Нужен
контрольный взгляд владельца — см. эстафету, п. 1.

### 🎯 Эстафета: что делать СЛЕДУЮЩЕМУ чату (он не знает ничего из этого)

1. **Контрольный взгляд владельца в бою, под его логином** — это единственное, что не смог
   проверить агент. Открыть `ndimspace.app`, войти, походить **Профиль ↔ Связи ↔ Пространство
   ↔ Измерения ↔ Меню** по кругу два-три раза. Ожидание: со второго захода экраны появляются
   **сразу, без карточки «Загрузка»**. Плюс проверить, что повторный тап по звезде отменяет
   отсчёт (`bugs/54`) и что поиск измерений ищет только по нажатию «Искать»/Enter (`ideas/20`).
2. **📋 `interviews/interview_005_caching_forks.md` — ЖДЁТ ОТВЕТОВ ВЛАДЕЛЬЦА.** Четыре вопроса
   по кэшированию, каждый с рекомендацией и ценой: В1 хранить ли данные между сессиями (офлайн
   против приватности на общем компьютере) · В2 живые подписки вместо чтений по запросу ·
   В3 освежать ли при возврате во вкладку · **В4 (найден при подготовке, про деньги): срок
   свежести топа — сейчас 60 с, и повторный заход на «Связи» после него стоит до ~94 чтений.**
   Владелец отвечает прямо в документе, в полях «Ответ:». Без ответов — не реализовывать.
3. **Вычитки владельца** (висят с прошлых сессий): две `[AI]`-фразы руководства (bugs/55 →
   «Решения…», п. 0) · черновик «Версия 2.0» (`researches/06`, `[AI]`).
4. **`bugs/72`** (каретка в поле поиска сбрасывается в начало) — в документе таблица
   отвергнутых гипотез и три непроверенные двери; первая и главная: **боевой это дефект или
   артефакт `vite dev`** (теперь проверяемо — фикс поиска в бою).
5. `ideas/19` (виджет «Аккаунт» в профиль) · `bugs/40` (boot-экран) · `bugs/62`+`58` (PWA) ·
   `bugs/65` (гуттер) · `bugs/67` (флейк e2e 1px).
6. **Пачка 4 целиком ждёт владельца** (макеты и развилки): `bugs/16`+`19`+`32` · `17` · `20`+`41`
   (планшет) · `28` · `31`.

**Команды, которые понадобятся сразу:** `npm run stand` (эмуляторы + сервер + vite; нужна Java) ·
`node tools/verify-ideas18.mjs` (страж кэша, 30 проверок) · `node tools/measure-ideas18.mjs`
(прибор замера навигации) · `npm test` (185) · `npm run e2e` (⚠️ гасить стенд ПО ДЕРЕВУ
ПРОЦЕССОВ перед прогоном — EXP-0059/0027).

---

**Предыдущее обновление:** 2026-07-28, вечер — 🚀 **ВОЛНА 13 ВЫКАЧЕНА В БОЙ** (слово
владельца: «Да, выкатывай хостинг») **+ закрыта связка «Измерения»: `bugs/54` (отмена
оценки повторным тапом) и `ideas/20` (поиск только по явному вводу). Беклог прогрумлен.
Связка в бой НЕ выкачена — ждёт слова владельца.**

### Что сделано этой сессией

**1. 🧹 Груминг беклога (просьба владельца «погрумь беклог, что ценно сейчас взять»).**
Каждый открытый документ сверялся С КОДОМ, а не со своим описанием. Два оказались
фактически закрытыми и в бою, но без тега: **`bugs/49`** (рельс прибит — `sticky` в
`SideRail.svelte:79-83`, инвариант стережёт `e2e/desktop-shell.spec.ts`) и **`ideas/17`**
(десктопная шапка V3-А, 501/501; приборная строка V4 остаётся планом в разделе «Будущее»).
Обоим проставлен `DONE`. Остальные подозрительные проверены и честно открыты: `bugs/20`
(во всём `src/` нет ни одного `@media (min-width: 600px)`), `bugs/28` («Без имени» живо),
`bugs/40`/`44`/`45`/`62` (в коде нет ни boot-экрана, ни `deleteUser`, ни кропа, ни
`beforeinstallprompt`).

**2. 🚀 Выкат волны 13** (`bugs/68` подёргивания раскрывашек · `69` графика «на горячую» ·
`70` карточка «Загрузка»). Чистая пересборка → `firebase deploy --only hosting`, **оба
таргета release complete**. Сверка боя: 14 маршрутов 200, зеркало 301, **страж
`node tools/verify-bug69.mjs --prod` — 60/60 на живом `ndimspace.app`** (флаг `--prod`
добавлен этой сессией, чтобы правило готовности графики проверялось там, где его видит
владелец), кадры смотрены глазами. Дифф выката проверен: сервер синхронизации, правила и
модель — **0 файлов**.

**3. ✅ `bugs/54` — повторный тап по звезде отменяет отсчёт автосохранения.** Разведка 1.x
ДО кода (`researches/12` → «Жест звезды: повторный тап») ответила на вопрос, который баг
ставил ребром: жест звезды отменял только НЕзаписанное и **сохранённых данных не трогал
никогда** (на «Все» — снять выделение + `hideSaveButton`, `app.js:3759`; на «Мой NDim ID»
по выделенной звезде — ничего, все ветки закрыты условием `checked != "checked"`; удаление
жило отдельной операцией `deleteField`). Фикс — одна ветка в `pick()`, разметка не тронута.
Страж `node tools/verify-bug54.mjs` — **60/60**, смотрит В БАЗУ эмулятора и ждёт дольше
отсчёта. Мутация: ветка отключена → «в базе оказалось 7».

**4. ✅ `ideas/20` — поиск только по явному вводу.** Введён `submitted` (запрос, по которому
ИЩУТ) отдельно от `search` (того, что печатают); поле стало формой — кнопка «Искать» +
Enter + `enterkeyhint="search"` для Android; дебаунс 250 мс удалён. **Замер экономии: набор
запроса — 0 запросов к Firestore, отправка — 2; до фикса набор стоил 3.** Страж
`verify-bug50` получил новый предмет — счёт обращений к базе (этого просил сам `ideas/20`).

**5. 📝 Канон пополнен по слову владельца:** перед крупными эпик-фичами — **гуглёж-разведка
и research-документ** («почти на всё в индустрии есть золотые стандарты и научные работы»).
Вписано в `AGENT_GUIDE` четвёртым типом разведартефакта («Обзор индустрии»), шагом **7б**
чеклиста и явными списками «что считается эпической фичей / что нет».

**6. 🐞 Заведён `bugs/72`** — каретка в поле поиска сбрасывается в начало, посимвольный ввод
даёт «икшоК» вместо «Кошки». **Дефект СТАРШЕ этой работы** (доказано прогоном на HEAD через
`git stash`, 5 из 5). Причина за один заход не нашлась — в документе таблица отвергнутых
гипотез (подмена сеттера `value` — 0 присваиваний; ловушки на `focus`/`setSelectionRange` —
ни одного вызова; `MutationObserver` — элемент тот же; контрольный опыт на голом `<input>` —
реверса нет) и три непроверенные двери.

**Регрессия сессии:** юнит **173** · e2e **70** (+2 skip) · typecheck/check **0/0** ·
`kaif:check` зелёный · стражи `verify-bug50` (весь, обе темы + EN) и `verify-bug54` (60/60) —
зелёные, оба доказаны мутациями.

**Уроки — EXP-0068** (наткнулся на дефект: первым делом докажи прогоном на HEAD, твой он или
чужой; чужой заводи НЕ пустым) · **EXP-0069** (⚠️ `Get-Content | Set-Content` в PS 5.1
уничтожает кириллицу — кодировка теряется на ЧТЕНИИ) · **EXP-0070** (у проверки «ничего не
произошло» обязана быть парная «а теперь произошло», и ставить её надо на ХОЛОДНОМ кеше) ·
**EXP-0071** (состояние выдвижного ящика спрашивай у разметки: Playwright считает видимым
элемент, обрезанный родителем через `overflow`).

### Где продолжить (следующая сессия)

1. **🚀 Выкат связки «Измерения»** — `bugs/54` + `ideas/20` лежат в `main`, в бою их нет.
   Дифф выката: `src/routes/dims/+page.svelte` + два стража. Сервер, правила и модель НЕ
   тронуты. Ждёт слова владельца.
2. **Вычитки владельца** (висят с прошлых сессий): две `[AI]`-фразы руководства (bugs/55 →
   «Решения…», п. 0) · черновик «Версия 2.0» (`researches/06`, `[AI]`) · контрольный взгляд
   на прод после волны 13.
3. **`ideas/18` — кэш сессии** (навигация без загрузок с нуля): лучший автономный заход,
   кандидаты решения расписаны в идее. ⚠️ По новому канону (шаг 7б) это **эпическая фича** —
   сначала обзор индустрии в `researches/`, потом код.
4. **`bugs/72`** (каретка) — три двери названы в документе; первая и главная: **боевой это
   дефект или артефакт `vite dev`** (проверить под логином владельца).
5. `ideas/19` (виджет «Аккаунт» в профиль) · `bugs/40` (boot-экран) · `bugs/62`+`58` (PWA) ·
   `bugs/65` (гуттер) · `bugs/67` (флейк e2e 1px).
6. **Пачка 4 целиком ждёт владельца** (макеты и развилки): `bugs/16`+`19`+`32` (ОБ-флоу,
   вход, пароль — один набор макетов) · `17` (иконки внедрены, ждут взгляда) · `20`+`41`
   (планшет — нужно устройство) · `28` (формат почты) · `31` (документы из Firestore).

---

**Предыдущее обновление:** 2026-07-28 — 🔧 **ЗАМЕРЫ ВОЛНЫ 13 ЗАКРЫТЫ: bugs/70, 69, 68
ПОЧИНЕНЫ (слово владельца — «чиним баги и боли»). Попутно найден и починен дефект гейта
push — `bugs/71`.**

**Что сделано (три бага волны 13, каждый — замер до кода, свой страж, мутации):**

1. ✅ **`bugs/70` — карточка «Загрузка» всегда компактная.** Замер опроверг слово «иногда»:
   лоадер растягивался НА ВСЕХ четырёх экранах и обеих ширинах (**1129px** при рабочей
   области 1193px) — блочный flex занимал всю ячейку, а на «Пространстве» ячейка ещё и
   `grid-column: 1 / -1`. Компактность зависела от хозяина места (случайно спасал только
   центрирующий `.loader` «Измерений»). Лечение формой в одном компоненте
   (`src/lib/ui/Loading.svelte`): обёртка забирает растяжение, карточка — `width: max-content`.
   **После: 118px везде.** Страж `node tools/verify-bug70.mjs` — **32/32**.
2. ✅ **`bugs/69` — глобальное правило владельца «графика нигде не появляется на горячую»
   обойдено по всему проекту.** rAF-трасса с придержанной графикой намеряла нарушение везде,
   кроме руководства: `support.png` — 44 кадра «место занято, картинки нет», `donation.png` —
   44, портрет автора — 45, три лица персонажей лендинга — по 82 (0,7…1,4 с пустоты на
   глазах). Приём вынесен в **`src/lib/ui/ready.ts`** (один на проект) и подключён: support,
   donate, author, лендинг (кружки, SVG-лица карты, оверлей). Страж
   `node tools/verify-bug69.mjs` — **60/60** (5 страниц × 2 темы × 390/1440).
3. ✅ **`bugs/68` — раскрывашки истории версий ходят без подёргиваний.** Покадровая трасса
   показала ровно то, что видел владелец: кривая затухает 10.9 → 4.9 и вдруг последний кадр
   **11.2px**, а на старте отдельным кадром выскакивает 4px (внешняя карточка) и 6px
   (вложенная). Причины **разделены экспериментом**: начало — отступ `[open]` включался
   мгновенно; конец — `overflow: clip` НЕ создаёт блочного контекста, и внешние отступы
   содержимого протекали наружу. Лечение: `overflow: hidden` + `transition: padding-bottom`.
   Третье заготовленное лекарство эксперимент отверг как лишнее. Страж
   `node tools/verify-bug68.mjs` — **68/68**.
4. ✅ **`bugs/71` (найден по ходу) — `npm run kaif:check` краснел на CRLF.** Гейт, обязательный
   перед КАЖДЫМ push, выдавал 15 ошибок «нет YAML-фронтматтера» на скиллах, у которых
   фронтматтер был на месте: проверка искала `/^---\n/`, а git на Windows отдаёт `---\r\n`.
   Починено нормализацией чтения в `tools/03-kaif-verify.mjs`; мутацией доказано, что гейт
   по-прежнему стережёт содержимое. Находка отправлена вверх по течению — `researches/16`, K6.

**Регрессия сессии:** юнит **173** · rules **87** · calc **30** · e2e **70** (+2 skip) ·
typecheck/check **0/0** · `kaif:check` зелёный · смежные стражи не сломаны (`verify-manual-v1`
целиком, `verify-wave12` **108/108**, `verify-batch3`). Скриншоты — `test-results/bug68|69|70/`,
смотрены глазами. **Каждый фикс доказан мутацией** (детали в документах багов).

**Уроки — EXP-0065** (в страж, меряющий ИЗМЕНЕНИЕ, клади проверку «изменение вообще произошло»:
селектор `.ver` ловил ещё и строки виджета, и трасса молча мерила неподвижный элемент) ·
**EXP-0066** (метрика стража должна быть признаком САМОГО дефекта: «доля пути» была зелёной на
сломанной анимации) · **EXP-0067** (гейт, краснеющий не по своей теме, опаснее отсутствующего).

**Где продолжить (запись закрыта: выкат состоялся 2026-07-28 вечером):**
1. ✅ **Выкат состоялся** — три фикса в бою, сверены живым браузером (`verify-bug69 --prod`,
   60/60). Дифф выката был: `src/lib/ui/Loading.svelte`, `src/lib/ui/ready.ts` (новый),
   `SimilarityDemo`, `DocBlocks`, три страницы «Меню», `menu/about`. **Сервер синхронизации,
   правила и модель НЕ тронуты.**
2. **Вычитки владельца** (ждут с прошлой сессии): две `[AI]`-фразы руководства (bugs/55 →
   «Решения…», п. 0) · черновик «Версия 2.0» (`researches/06`, `[AI]`) · контрольный взгляд
   на прод после волны 13.
3. **`ideas/18` — кэш сессии** (навигация без загрузок с нуля): лучший автономный заход,
   кандидаты решения расписаны в идее.
4. `ideas/20` (поиск по явному вводу — экономия Firestore) · `ideas/19` (виджет «Аккаунт» в
   профиль) · `bugs/65` (гуттер) · `bugs/67` (флейк e2e 1px).
5. **Пачка 4 с поднятым приоритетом `bugs/32`**: экран входа с ТРЕМЯ опциями (Email+Pass ·
   ссылка · Google — порядок владельца) — макеты вместе с bugs/16/19.
6. Прежняя очередь: тир B `plans/07` (`bugs/40` boot-экран → PWA → `bugs/44` фото-редактор).

---

**Предыдущее обновление:** 2026-07-28, ночь — ⏸ **ПАУЗА-СРЕЗ (слово владельца). Волна 13
закрыта целиком и в бою + последним заходом сделано окно «Как меня видят» (вкладка
«Видимость» упразднена).**

### ⏸ Пауза 2026-07-28: срез для следующих агентов — ЧТО БРАТЬ В РАБОТУ

**В бою прямо сейчас** (ndimspace.app, всё сверено живым браузером): руководство V1 целиком
(иллюстрации с прозрачным фоном, таблица шкалы с границами/зеброй/звёздами/смайликами,
кнопки-образцы 80×80, пагинатор глав, без плашки) · лица «карточка ждёт фото» + страховка
(сбой fetch → сетевой URL) · сервер синхронизации **0.2 (15)** с датой сборки (контейнер
заменён) · виджет «Версии» — один объект в «Меню» и «О системе» · профиль без вкладок:
кнопка **«Как меня видят»** открывает окно предпросмотра, «Назад»/Esc возвращает.

**Первым делом следующей сессии (по убыванию ценности):**
1. **Вычитки владельца** (заведено, ждёт слова): две `[AI]`-фразы руководства (bugs/55 →
   «Решения…», п. 0) · черновик «Версия 2.0» (`researches/06`, `[AI]`) · его контрольный
   взгляд на прод после волны 13 (лица, «О системе» под логином, руководство, окно
   «Как меня видят»).
2. **`ideas/18` — кэш сессии** (навигация без загрузок с нуля): лучший автономный заход,
   кандидаты решения расписаны в идее.
3. **Замеры заведённого волной 13**: `bugs/68` (подёргивания раскрывашек — rAF-трасса,
   метод EXP-0060) · `bugs/70` (лоадер на всю ширину на «Пространстве») · `bugs/69`
   (обход «готовности графики» по инвентарю в баге) · `ideas/20` (поиск по явному вводу —
   экономия Firestore) · `ideas/19` (виджет «Аккаунт» из «Меню» в профиль).
4. **Пачка 4 с поднятым приоритетом `bugs/32`**: экран входа с ТРЕМЯ опциями
   (Email+Pass · ссылка · Google — порядок владельца) — макеты вместе с bugs/16/19.
5. Прежняя очередь: `bugs/65` (гуттер) · `bugs/67` (флейк e2e 1px) · тир B `plans/07`
   (`bugs/40` boot-экран → PWA → `bugs/44` фото-редактор).

**Уроки этой сессии, которые сберегут время** (полные тексты — EXPERIENCE):
EXP-0060 (мерцания ловит только rAF-трасса с сетью на часах страницы) · EXP-0061 (литерал
от сида в страже гниёт — ожидание из данных) · EXP-0062 (e2e выжигает `test-results/`;
промис-предикат waitForFunction не ожидается) · EXP-0063 (найденный конфиг ≠ причина;
чини класс фолбэком). ⚠️ Перед e2e гаси стенд ПО ДЕРЕВУ ПРОЦЕССОВ (EXP-0059); после
пересборки перезапускай vite preview.

---

**Предыдущее обновление:** 2026-07-27, глубокая ночь — 🌊 **ВОЛНА 13 ВЛАДЕЛЬЦА (живой
прогон после выката руководства): всё срочное починено и 🚀 В БОЮ, сервер синхронизации
заменён — виджет наконец показывает «0.2 (15), Собран …».**

**Пункты волны и их судьба:**
1. 🔥 **«Нет ни одного фото»** (регрессия bugs/57 в бою) — разобран: гипотеза CORS
   ОПРОВЕРГНУТА наблюдением (v0-эндпоинт всегда отдаёт `allow-origin: *`); точная причина
   падения fetch у владельца не установлена (кандидат — блокировщик контента); закрыто
   **страховкой на весь класс**: сбой blob-пути → сетевой URL → `<img>` качает сам.
   Фолбэк доказан живьём (fetch задушен — лицо есть). CORS бакета всё равно приведён к
   правде (`storage.cors.json`, + ndimspace.app; бэкап старого — в bugs/57).
2. ✅ **«НЕ ВИЖУ НОМЕРОВ СБОРОК СЕРВЕРА. СДЕЛАТЬ»** — образ пересобран `npm run calc:image`
   (**CALC_BUILD=15**, дата 2026-07-27T18:14Z), боевой контейнер заменён с теми же
   параметрами (inspect → stop/rm → run), первый полный проход чист (333 точки, 94
   человека, 2346 связей, записано 0 — база спокойна). «+1 за коммит кода сервера» уже
   устроено: номер = `git rev-list --count HEAD -- calculator`. Сервер виден ВОШЕДШИМ
   (гостю — честное «неизвестно», дизайн bugs/66).
3. ✅ **«Иконка на горячую — ГЛОБАЛЬНОЕ ПРАВИЛО»** — правило вписано в канон
   (`AGENT_GUIDE` → «Дизайн» → «Готовность графики»); руководство приведено: резерв места
   w/h + проявление готовой картинки (action `ready`) + preload логотипа. Обход всего
   проекта — **`bugs/69`** (инвентарь класса внутри).
4. ✅ **«Нет таблицы»** — шкала 0…10 теперь ТАБЛИЦА как в 1.x: границы ячеек + зебра
   (снято со styles.css:2790, переведено в токены).
5. ✅ **«Кнопки МАЛЕНЬКИЕ»** — образцы 80×80 с иконкой 40 (масштаб 1.x, форма ibtn 2.0).
6. ✅ **«Вырезать белый фон картинок»** — ImageMagick флуд-заливка от углов (airplane,
   hospital, network, logo; облака самолёта целы — заливка не трогает внутренности).
7. ✅ **«Убрать плашку „разделы обновляются“»** — убрана (решение о непереносе разделов
   1.x в силе, но на лице продукта об этом не пишем).
8. ✅ **«Виджет версий как объект»** — карточка с заголовком «Версии» переехала ВНУТРЬ
   `Versions.svelte`; «Меню» и «О системе» ставят один объект без своих стилей.
9. 📝 Заведено по слову: **`ideas/19`** (виджет «Аккаунт» из «Меню» → в профиль) ·
   **`ideas/20`** (поиск только по явному вводу — кнопка/Enter/Android-клавиатура) ·
   **`bugs/32`** дословно пополнен (вернуть Email+Pass ТРЕТЬЕЙ опцией: Email+Pass ·
   ссылка · Google) · **`bugs/68`** (подёргивания раскрывашек — сначала замер) ·
   **`bugs/70`** (виджет «Загрузка» растягивается на «Пространстве» — должен быть
   компактным).
10. ❓ **Вопрос владельца про вкладку «Видимость» в профиле** («зачем — видимость видна и
   настраивается на главной») — ЖДЁТ РЕШЕНИЯ ВЛАДЕЛЬЦА: вкладка была превью «как меня
   видит аудитория» (глазами Я/Все/Друзья, интервью №002). Варианты: убрать · оставить ·
   слить превью в главную. Агент сам не решает.

**Проверка волны:** стражи manual (с новыми проверками: кнопки 80, границы+зебра,
5 картинок) · wave12 **108/108** · bug57 — зелёные; юнит **173** · e2e **70** (+2 skip) ·
typecheck/check 0/0 · kaif:check. **Выкат хостинга** — оба таргета release complete,
смоук прода: маршруты 200, руководство (5/5 картинок, таблица 1px, кнопка 80, плашки нет),
консоль чиста. Сервер: логи циклов чистые.

**Где продолжить (следующая сессия):**
1. **Контрольный взгляд владельца в бою** (обновить страницу с чистым кэшем): фото на
   «Доме»/«Связях» · «О системе» под логином — «Сервер синхронизации 0.2 (15), Собран …» ·
   руководство после волны (прозрачные картинки, таблица, кнопки) · вычитка двух
   `[AI]`-фраз (bugs/55) и «Версии 2.0» (`researches/06`).
2. **Слово владельца по п. 10** (вкладка «Видимость»: убрать / оставить / слить в главную).
3. Замеры и фиксы заведённого: `bugs/68` (подёргивания — трасса) · `bugs/70` (лоадер) ·
   `bugs/69` (обход готовности графики) · `ideas/20` (поиск по явному вводу) · `ideas/19`
   (аккаунт в профиль) · `ideas/18` (кэш сессии) · далее прежняя очередь (`bugs/65`,
   `bugs/67`, тир B).

**Предыдущая запись (та же дата):** 📖 **РУКОВОДСТВО V1 «КАК БЫЛО»
РЕАЛИЗОВАНО, bugs/55 ЗАКРЫТ И ВЫКАЧЕН В БОЙ** (слово владельца: «выкатывай») **+
заведена ideas/18 (кэш сессии).** Ждёт вычитки владельцем двух адаптированных фраз.

**Выкат сверен живым браузером на `ndimspace.app`** (`test-results/prod-manual-v1/`):
пререндер `/menu/manual` несёт картинки, «Напутствие», адаптированные фразы; в обеих темах
5/5 иллюстраций отрисованы пикселями, шкала 11 оценок, пагинатор 5 точек / панель 12 глав,
консоль чиста; 7 маршрутов 200, зеркало 301. **Сервер синхронизации, правила и модель НЕ
трогались** (дифф выката: генератор, рендер документов, 2 новых UI-модуля, страж).

**Слово владельца (три решения этой сессией, дословно в bugs/55):** «Давай V1, как было,
один документ сплошняком, сбоку справа делаем floating пагинатор глав…, Звёзды, кнопки —
нужно адаптировать к текущему виду нашей нынешней реализации» · картинки в тёмной теме —
«V1 светлая карточка, только не идеально белый, а чуть-чуть в бирюзовую синеву» (выбрано по
4 вариантам `design/manual-dark-images-mockups.html`; разведка уточнила: картинки не
«белые», а 4 классов — прозрачные/белофонные/чёрный рисунок/чёрная плашка) · плюс идея
«кеширование данных и страниц» → **`ideas/18`** (одобрена по построению, ждёт своего
захода; кандидаты решения и инвалидация расписаны).

**Сделано (руководство):** генератор `extract-docs.mjs` превращает пометки исследований в
img-блоки — вернулись логотип, остров/самолёт/больница ВНУТРИ пунктов (как в 1.x),
финальная сеть, «Напутствие» главой · таблица шкалы 0…10: звезда `--star` (ноль гаснет) +
цифра + цветной смайлик (формы 1.x, цвета EXP-0057, «десятка» многоцветная —
`src/lib/ui/emojiscale.ts`) · ряд 11 смайликов · кнопки-образцы = копии `ibtn` «Измерений» ·
**в тёмной теме картинки на светлой карточке `color-mix(#fff 93%, --accent 7%)`** ·
**`src/lib/ui/ChapterNav.svelte`** — плавающий пагинатор: капсула 5 точек → панель
5 глав + 7 подглав, активная из прокрутки, у дна — последняя, Esc/тап-мимо · иерархия
заголовков (подразделы понижены до h3 только в руководстве). **Две фразы о месте кнопок
адаптированы `[AI]` под 2.0** (генератор падает, если оригинал изменился) — ждут вычитки.

**Проверка:** новый страж `node tools/verify-manual-v1.mjs` — все проверки зелёные (обе
темы × 390/1440, ~27 проверок/комбинация; иллюстрации меряются ПИКСЕЛЯМИ после прокатки
lazy, ожидание конца плавной прокрутки — циклом стабилизации), скриншоты
`test-results/manual-v1/` смотрены глазами; мутация «снята иерархия» → 2 падения.
Регрессия: юнит **173** · rules 87 · calc 30 · e2e **70** (+2 skip) · typecheck/check 0/0 ·
kaif:check. ⚠️ Дважды за день: e2e чистит `test-results/` целиком — оснастку перегонять
ПОСЛЕ e2e, если нужны её скриншоты; preview перезапускать после каждой пересборки.

**Где продолжить (следующая сессия):**
1. **Контрольный взгляд владельца в бою**: `/menu/manual` в обеих темах (картинки на
   бирюзовых карточках в тёмной, пагинатор) + «Дом»/«Связи» под логином (лица без
   мерцания, bugs/57) + **вычитка двух `[AI]`-фраз** (bugs/55 → «Решения…», п. 0) и
   черновика «Версии 2.0» (`researches/06`).
2. **Долг образа сервера**: `npm run calc:image` + замена контейнера — ждёт слова владельца
   «меняй контейнер» (рестарт боевой службы), иначе дата сборки сервера в виджете не
   появится.
3. `ideas/18` (кэш сессии — экраны перестают грузиться с нуля при навигации) — хороший
   следующий автономный заход.
4. Очередь прежняя: `bugs/65` (гуттер) · `bugs/67` (флейк e2e) · тир B `plans/07`
   (`bugs/40` boot-экран → PWA → `bugs/44` фото). Кандидат владельцу: ряд смайликов на
   карточке измерения (в руководстве теперь есть; нужен ли на карточке — его слово).

---

**Предыдущее обновление:** 2026-07-27, ночь — 📸 **bugs/57 + хвост bugs/56 ПОЧИНЕНЫ:
карточка ждёт фото, как в 1.x.** Волна 12 по-прежнему ждёт владельца (макет руководства +
текст «Версии 2.0»).

**Слово владельца (волна 11):** «рендерится страница без фотки, и затем только спустя время
появляется фотка — плохо. В оригинальном НДим делал, чтобы карточка ждала загрузки фотки» ·
«на СВЯЗИ плашки появляются резко — нужно унифицировать».

**Замер до кода (rAF-трасса по кадрам, Storage замедлен до латентности прода):** человек
видел ТРИ состояния — буква (316мс) → **пустой серый круг** (317мс) → лицо. Причина: два
сетевых раунда ПОСЛЕ вставки карточки в DOM (`avatarUrl` ставила в `src` сетевой
download-URL, байты качал уже `<img>`). «Резко на Связях» — это ОНО ЖЕ: анимация полёта
играла, а фото доезжало после неё. Канон 1.x снят с живого кода (`researches/12` → новый
раздел «Фотографии в карточках»): карточка НЕ вставлялась в DOM без скачанного фото,
`<img>` получал локальный blob (`fetchUserAvatar`, app.js:4967).

**Фикс (перенос механизма 1.x):** `data/avatar.ts` — blob целиком + `img.decode()` до
вставки + кэш сессии (дедуп параллельных, повторный заход = 0 запросов) +
`preloadAvatars(потолок 1500мс)` · `Avatar.svelte` — первый рендер берёт лицо из кэша
СИНХРОННО; не успевшее лицо доезжает атомарной заменой буквы (пустого img больше не
существует) · «Связи» — лица порции под кольцом загрузки, догрузка порций раскрывается
готовой за якорем · «Дом» — своё фото под лоадером. Люди без фото не ждут и не порождают
запросов. **Трасса после: первый кадр каждой карточки — уже с лицом.**

**Проверка:** новый страж `node tools/verify-bug57.mjs` — все проверки зелёные (обе темы ×
390/1440 + «Дом» + потолок + экономия), скриншоты смотрены глазами; **мутации: снял
предзагрузку → 5 падений, сетевой URL вместо blob → 18**. Async-бамп догрузки порций
проверен живьём (порция временно 2 → лента раскрыта до дна; на проде у людей до 250 связей).
Регрессия: юнит **173** · rules 87 · calc 30 · e2e **70** (+2 skip) · typecheck/check/
kaif:check чисто.

**🚀 ВЫКАЧЕНО В БОЙ 2026-07-27, ночь** (слово владельца: «текущее задеплой») — этот выкат
повёз разом волну 12 (виджет «Версии» в «Меню»/«О системе», портрет автора, анимация
раскрывашек) и bugs/57+56. Чистая пересборка → `npx firebase deploy --only hosting`,
**оба таргета release complete**. Сверено живым браузером на `https://ndimspace.app`
(`test-results/prod-wave12-57/`): 9 маршрутов — 200, зеркало — 301; боевые чанки несут
blob-механизм фото (`createObjectURL` + `.decode()`) и виджет версий («Собрано»); гостем в
обеих темах: «Версии» в Меню — «Приложение 2.0 (173), Собрано 27 июля 2026 г. в 19:18»,
сервер гостю честно «неизвестно»; портрет автора реально отрисован (`naturalWidth > 0`);
руководство открывается; консоль чиста. Лица на «Доме»/«Связях» гостю не видны (правила
Storage требуют входа) — контрольный взгляд владельца под своим логином.
**Сервер синхронизации, правила и модель НЕ трогались.** ⚠️ Долг остался: образ сервера
пересобрать `npm run calc:image` + заменить контейнер (по слову владельца — это рестарт
боевой службы), иначе дата сборки сервера в виджете не появится.

**Попутно:** три сгнивших стража вылечены (класс EXP-0056, все — гниль ДО этой работы:
tierA кликал кнопку, удалённую bugs/51; batch2 ждал литерал «41», сгнивший при досеве
bugs/50, и требовал у фото src эмулятора — теперь канон blob). **`bugs/67`** — новый флейк
e2e `desktop-shell` (рельс/шапка расходятся на 1px, мигает на идентичном коде, НЕ регрессия
фикса — доказано base-прогоном). Уроки: **EXP-0060** (мерцания ловит только потоковая
rAF-трасса; часы сети = часы кадров; замедляй роутом только целевой ресурс) и **EXP-0061**
(литерал от сида в страже гниёт при каждом досеве — ожидание выводи из данных).

**Где продолжить (следующая сессия):**
1. **Руководство V1 в работе** (выбор владельца 2026-07-27: «V1, один документ сплошняком,
   floating пагинатор глав справа; звёзды/кнопки адаптировать к нынешней реализации;
   картинки в тёмной теме — вопрос открыт, готовятся 4 варианта обработки»). Текст
   «Версии 2.0» (`researches/06`, `[AI]`) всё ещё ждёт приёмки владельца.
2. **Контрольный взгляд владельца в бою под логином:** «Дом»/«Связи» — лица без мерцания
   (bugs/57), виджет «Версии» с версией сервера. **Долг:** пересобрать образ сервера
   (`npm run calc:image`) и заменить контейнер по слову владельца — иначе дата сборки
   сервера в виджете не появится.
3. Очередь прежняя: `bugs/65` (полоска гуттера, ждёт взгляда владельца) · `bugs/67` (флейк
   e2e, план в баге) · тир B `plans/07` (`bugs/40` boot-экран → переиздание PWA →
   `bugs/44` фото).

---

**Предыдущее обновление:** 2026-07-27, вечер — 🎨 **ВОЛНА 12 ВЛАДЕЛЬЦА («наводим красоту и
стиль»): механическая часть сделана и проверена, 4 макета руководства ждут выбора.**

**Слово владельца (пять пунктов):** убрать виджет «Версии» с «Пространства» и перенести в
Settings с полным форматом · починить вёрстку дочерних страниц Settings, особенно
«Руководство пользователя» («было с картинками, с кнопками HTML, с таблицами») · «если был
емодзи — он был красочный. Сейчас там всё скупое и некрасивое, нет центровок» · «в Об Авторе
вообще фото потерялось» · в «О системе» сделать версию 2.0 и список версий с анимацией.

**Разведка (до кода, `researches/12` → новый раздел «Вёрстка страниц документов 1.x»):**
руководство 1.x — **66 картинок, 2 таблицы, 2 круглые кнопки-образца**; шкала 0–10 была
таблицей со звездой, цифрой и **цветным смайликом**; версии в 1.x показывались ТОЛЬКО в
«Меню» и «О системе» (один код на два контейнера, `app.js:5164`) — на «Пространстве» их не
было вовсе, то есть владелец прав по канону. **Фото автора не терялось: оно и все пять его
публичных ссылок лежали в архиве 1.x** — `homeworks/06` полтора месяца просила у владельца
то, что было на диске (EXP-0058).

**Сделано (агент сам, макетов не требовало):** ✅ **bugs/66** — виджет «Версии» уехал в «Меню»
и «О системе» одним общим компонентом `src/lib/ui/Versions.svelte`; показывает версию, сборку
**и дату со временем** по КАЖДОМУ из двух (приложение и сервер). Попутно: «О системе»
спрашивала версию сервера только на localhost — в бою её не видел никто; «Меню» ломилось за
ней гостем и сыпало отказом в консоль. · 🔧 **bugs/55** (механическая часть) — портрет автора
и пять площадок с настоящими знаками (`src/lib/ui/brands.ts`), глифы `♡ ✉ ↗ ⧉ ‹` заменены
иконками набора, `❤` вернулся ЦВЕТНЫМ `❤️` (терялся селектор U+FE0F), иллюстрации
`support.png` и `donation.png` на местах, текст коротких страниц снова по центру — это канон
1.x, а 2.0 центрировал наоборот. · 🔧 **bugs/56** — «История версий» анимируется
(`::details-content` + `interpolate-size`, токены `--motion-*`), запись «Версия 2.0» написана
черновиком `[AI]`.

**Проверка:** новая оснастка `node tools/verify-wave12.mjs` — **108/108** (обе темы ×
390/1440), скриншоты `test-results/wave12/` смотрены глазами. Оснастка нашла три вещи, две
настоящие: стенд не передавал серверу номер и дату сборки (починено в `tools/stand.mjs` —
иначе проверка была бы зелёной от пустоты) и глиф `✉` в сетке соцсетей. Регрессия: юнит
**173** · rules 87 · calc 30 · e2e **70** (+2 skip) · typecheck/check/build/kaif:check чисто.
Страж «версия вшита в сборку» **переехал** из `space.spec.ts` в `menu.spec.ts` вместе с
виджетом и проверен мутацией (2 падения).

**⚠️ Долг перед выкатом:** дата сборки СЕРВЕРА появится в бою только у образа, собранного
через `npm run calc:image` — боевой контейнер собирался до появления этого аргумента.
Пересобрать вместе с ближайшим выкатом сервера.

**Где продолжить (следующая сессия):**
1. **Владельцу — выбрать макет** `design/manual-mockups.html` (V1 «Как было» · V2
   «Главы-карточки» · V3 «Живая шкала» · V4 «Документация») и **принять или поправить текст
   «Версии 2.0»** (`researches/06`, пометка `[AI]`). Обе вещи блокируют дальнейшее.
2. После выбора — реализация руководства: ассеты уже в `static/img/docs/`, цвета шкалы
   замерены (EXP-0057), таблица и кнопки-образцы по строкам `researches/12`.
3. Хвост `bugs/56`: «на СВЯЗИ плашки появляются резко» — сначала ЗАМЕР трассой, гипотеза
   смыкается с `bugs/57` (карточка рисуется до фотографии).
4. Прежняя очередь: `bugs/65` (полоска гуттера) · тир B `plans/07` (`bugs/40` boot-экран →
   переиздание PWA → `bugs/44` фото).

**📝 Кандидат в баги (не заведён, нужно слово владельца):** в 1.x под звёздами карточки
измерения стоял ряд из **11 цветных смайликов** — в 2.0 его нет вовсе (только `★/☆`).
Спросить: нужен ли он на карточке, или достаточно шкалы в руководстве.

---

**Предыдущее обновление:** 2026-07-27 — 🚀 **ДЕСКТОПНАЯ ШАПКА V3-А ВЫКАЧЕНА В БОЙ (ideas/17).**

**Слово владельца:** «в десктоп версии оцень пустует хедер… может вспомогательные меню туда
перемещать, типа менюшка ВСЕ - МОЙ NDIM ID или может заголовок приложения Пространство NDim туда
перенести? В меню кнопок слева выше поднимится?» → 4 макета (`design/desktop-header-mockups.html`)
→ выбран **V3, геометрия А, БЕЗ имени раздела**: «пока что делаем только V3 - A, а приборную
панель с метриками — высокоуровневый план на будущее, без детализации».

**Сделано:** шапка на десктопе — первая строка сетки **во всю ширину поверх рельса**; знак и
«Пространство NDim» переехали в неё из рельса и стали **ссылкой на `/profile`** (bugs/61 переехал
вместе с ними; на телефоне знак наконец кликабелен); рельс — чистая навигация, липнет к низу шапки
(`top: var(--bar-h)`, `height: calc(100dvh - var(--bar-h))`); новый **общий замер высоты шапки**
`src/lib/ui/barheight.svelte.ts` (было бы два независимых замера — панель «Измерений» и рельс;
попутно ушёл хрупкий `document.querySelector('.bar')`). **Шесть оболочек** (/profile, /relations,
/space, /dims, /menu, DocShell) не потребовали ни строки: шапка и рельс размещают себя сами.

**Замеры живым Chrome (до → после):** десктоп шапка `x=232, w=1193, h=59` → `x=0, w=1425, h=55`;
рельс `y=0` → `y=55` (под шапкой). **Телефон 390: 61px до и после — раскладка не изменилась.**

**Проверка:** новая оснастка `node tools/verify-header-v3a.mjs` — **501/501** (6 оболочек × 2 темы ×
1024/1440 + телефон + панель + смена языка + клик по знаку), скриншоты `test-results/header-v3a/`
смотрены глазами. **Мутации:** снял полноширинность → 4 падения оснастки + 4 падения e2e · вернул
рельсу `top: 0; height: 100dvh` → 4 падения (в т.ч. «низ рельса 855 при вьюпорте 800» — буквальная
регрессия bugs/49) · вернул бренд в рельс → 6 падений. Регрессия: юнит **173** · rules 87 · calc 30 ·
e2e **70** (+2 skip) · typecheck/check/kaif:check чисто. Оснастки оболочки зелёные: bug49, bug51,
bug52 64/64, batch4, wave11 28/28, bug64 21/21.

**Попутно (не мои регрессии — проверено на базе через `git stash`):** три стража сгнили от смены
канона и приведены к правде (`verify-bug51` требовал того, что запретил bugs/52; `verify-batch4`
сравнивал оверлей с вьюпортом вместо раскладки), находка про неприкрытую полоску гуттера заведена
как **`bugs/65`** (🔬, нужен взгляд владельца на десктопе). Уроки — **EXP-0055** и **EXP-0056**.

**🚀 ВЫКАЧЕНО В БОЙ 2026-07-27** (слово владельца: «деплой конечно»): чистая пересборка (`rm -rf
build` + `npm run build`) → `npx firebase deploy --only hosting --project ndim-space`, **оба
таргета release complete**. Сверено на живом `https://ndimspace.app` **живым браузером, а не
только кодами ответов** (`test-results/prod-v3a/`): на `/profile`, `/dims`, `/menu` в обеих темах
на 1440 — шапка `x=0, w=1425` во всю ширину, рельс `y=55` под шапкой, знак ведёт на `/profile`, в
рельсе бренда нет, пять пунктов, после прокрутки шапка прибита, консоль чиста; телефон 390 —
шапка 61px, рельса нет, нижняя панель на месте. Смоук `/ · /profile · /relations · /space · /dims ·
/menu · /menu/share · /menu/manual` — все 200, зеркало `ndim-space.web.app` — 301.
**Сервер синхронизации, правила и модель данных НЕ трогались** (в диффе только `src/lib/ui`,
`src/routes/dims`, проверки и документы).

**Где продолжить (следующая сессия):**
1. **Контрольный взгляд владельца в бою** на десктопную шапку (и на телефоне — там ничего не
   должно было измениться).
2. **Приборная строка (V4)** — план крупными мазками уже в `ideas/17` («Будущее»); реализация
   требует своих 4 макетов и своего QA.
3. `bugs/65` (полоска гуттера в лайтбоксе) — решение владельца из трёх гипотез.
4. Прежняя очередь тира B (`plans/07`): `bugs/40` boot-экран → переиздание PWA → `bugs/44` фото.

---

**Предыдущее обновление:** 2026-07-26, вечер — ⏸ **ПАУЗА. ВОЛНА 10 ВЛАДЕЛЬЦА ЗАКРЫТА И В БОЮ
(bugs/50 поиск · bugs/51 «Предложить измерение»).**

**Слово владельца, два дефекта из чата:**
1. «сломался поиск измерений. В старом НДим я вводил название измерения — и оно находило его
   среди всех измерений пространства. Сейчас не находит, словно оно ищет только в том скоупе,
   который подгружен в браузере — это неприемлемо. Есть dims_list — по ним можно искать что
   угодно» → **`bugs/50`**.
2. «раньше были FAB про добавить новое измерение — теперь это запрятано вниз списка измерений,
   там пользователь никогда не найдёт» → **`bugs/51`**.

**bugs/50 — причина найдена ЧТЕНИЕМ КОДА, а не гаданием:** `searchIndex` честно искал по всему
`dims_list` (5111), но экран использовал результат как **фильтр по уже загруженным карточкам**
(первые 12 ленты + раскрытое «Мой NDim ID»). Написано это было под флагом «экономим запросы»;
разведка живого 1.x (`ndim_old/public/scripts/app.js` → `doSearchDims` 9131, `searchInJson` 9026,
`normalizeStringForSearch` 9052) показала, что канон экономил иначе: **искал по индексу и
ДОГРУЖАЛ найденное, ≤20 документов**. Сделано: догрузка найденного · `normalizeForSearch`
перенесена дословно (ё→е · дефис/тире · кавычки и скобки · римские I…XV → арабские · регистр ·
схлопывание пробелов) · порядок по релевантности (|длина имени − длина запроса|) + тай-брейк по
id · языки сравниваются ПО ОТДЕЛЬНОСТИ (склейка «ru en» давала ложные совпадения) · лимит 20 с
текстом 1.x дословно · пауза 250 мс против дребезга · метка запроса против гонки ответов ·
кольцо загрузки · «Ничего не найдено» только по ЗАВЕРШЕНИИ поиска, ошибка сети не прячется.
**Замер экономии в бою на стенде: поиск «проба» (36 совпадений) добавил 3 запроса к Firestore,
а не 5111.**

**bugs/51 — 4 макета владельцу, выбран V3 «Рядом с поиском»** (владелец называл удобным FAB
из 1.x, но, увидев варианты рядом, выбрал вход, который ничего не перекрывает). Макеты —
`design/dims-suggest-fab-mockups.html`. Сделано: 💡 в строке поиска · **строка поиска ПРИБИТА
под шапкой** (без этого V3 воспроизводил бы ту же боль) · смещение `top` = **измеренная**
высота шапки через `ResizeObserver` (живой замер: 57px на 390, 52px на 1440 — зашитая
константа врала бы на одной из ширин) · форма открывается тут же и страница поднимается к ней ·
кнопка ПОД лентой удалена · подсказка экрана RU+EN переписана · `aria-label` на кнопке.

**Проверка (ворота plans/06 пройдены):** юнит **170** (+7 стражей) · rules 87 · calc 30 ·
e2e 70 (+2 skip) · typecheck/check/kaif:check чисто · QA живым Chrome: `node tools/verify-bug50.mjs`
**31/31** и `node tools/verify-bug51.mjs` **59/59** (обе темы × 390/1440 + английский экран),
скриншоты `test-results/bug50|bug51/` смотрены глазами. **Мутации проверены на сломанной
версии:** снял нормализацию → падают 2 юнита · убрал сортировку → 2 · вернул склейку языков → 1 ·
убрал подъём к форме → краснеет QA · `static` вместо `sticky` → краснеет QA.
**Стенд досеян сором боевых данных** («Звёздные войны» с «ё», «Человек-паук» с дефисом,
«Рокки IV») — на идеальных данных нормализацию проверить нечем (урок EXP-0041). Осей на стенде
теперь 48.

**Судейский проход (`/fable-judge`) до выката нашёл три вещи:** (1) живая проверка bugs/50
гонялась ДО правок разметки для 51 — перепрогнал оба на финальном коде; (2) мёртвое состояние
`searchDone` (писалось трижды, не читалось ни разу) — вычищено; (3) осиротевший текст удалённой
кнопки `t.suggest` — убран. Заявление «сервер синхронизации, правила и модель не тронуты»
проверено диффом: 0 файлов.

**🚀 ВЫКАЧЕНО В БОЙ 2026-07-26** (слово владельца: «Выкатить вместе с FAB»): чистая пересборка →
`firebase deploy --only hosting --project ndim-space`, оба таргета release complete. Сверено на
живом `https://ndimspace.app`: боевой чанк `/dims` (`3.CBr20TAp.js`) содержит `suggest-btn`,
текст «Будут показаны только первые 20 совпадений» и таблицу римских цифр нормализации
(`VIII`/`XIII`); смоук `/ · /profile · /relations · /dims · /menu · /menu/share` — все 200.
**Сервер синхронизации и правила НЕ трогались.**

**Урок сессии — EXP-0054:** «экономия запросов» — не «не читать», а «читать мало и по делу»;
слово «осознанно» в инвентаре паритета имеет право означать только «владелец это выбрал»
(за день найдены ДВЕ ложные отметки: поиск ✅ и плавающие кнопки ✖ — обе исправлены на 🔧→✅).

**Где продолжить (следующая сессия):**
1. **Контрольный взгляд владельца в бою** на `/dims`: поиск (введите название измерения,
   которого нет на первом экране) и кнопку 💡 рядом с поиском. Плюс всё ещё висит взгляд на
   `/profile`, раскрытую связь и `bugs/06` (стиль «Измерений»).
2. **Тир B** (`plans/07`): `bugs/40` boot-экран (владелец натыкается каждый раз) → переиздание
   PWA (манифест ru/en + SW) → `bugs/44` фото (после `bugs/04` правил Storage).
3. Хвост `bugs/46` — «последнее обновление NDim ID» требует правки вычислителя.
4. Тир C ждёт ответов владельца (🔴 `bugs/45` удаление аккаунта — долг перед своим каноном).
5. Мелочь для уборки: в `src/routes/profile/+page.svelte` лежит мёртвый текст `t.suggestDim`
   (был мёртв до этой работы, чинить в чужом баге не стал).

---

**Предыдущее обновление:** 2026-07-26 — 🎯 **БОЛИ ПЕРЕЕЗДА РАЗЛОЖЕНЫ ПО ТИРАМ · ТИР A
ЗАКРЫТ (4 бага)**. Владелец спросил не «что чинить», а «что даст 80% пользы за 20%
усилий». Ответ потребовал разведки ПО КОДУ, а не по беклогу, и она перевернула оценку:
ядро считает 20 величин и вычислитель кладёт их в КАЖДУЮ запись топа — экраны показывают
три. Половина «потерянного при переезде» оказалась **вёрсткой уже посчитанных чисел**
(EXP-0052). План — **`plans/07_owner_pain_tiers.md`** (тиры A/B/C по оси «цена работы ×
нужно ли решение владельца»), сводка — в `MASTER_PLAN.md` фаза 6.
**Сделано в тире A** (5 коммитов, без единой правки сервера синхронизации, правил и
модели данных): ✅ **bugs/43** профиль вернул статистику «Дома» — виджеты «Мой NDim ID»
(эмоциональная шкала 1.x «4 (очень мало 😭)», диаметр, «Обновлен», «Синхронизирован») и
«Мои связи» (полосы 90 / 75…89 / 50…74), вводная подсказка · 🟡 **bugs/46** раскрытая
связь снова досье (персональные данные по видимости 2.0, возраст, коэффициенты
×моего/×хозяина) · ✅ **bugs/47** «Пригласить друзей» — своя страница `/menu/share` с
сеткой 13 соцсетей БЕЗ внешних скриптов · ✅ **bugs/48** «Правила оформления» дословно.
**Судейский проход поймал настоящий дефект в моей же работе до выката:** «Синхронизирован»
брал `relations.computedAt`, а он с `ideas/14` пишется ТОЛЬКО при изменении топа — у
человека со стабильным топом дата застыла бы на неделю при живом сервере. Источником стал
`points.lastSync`; страж проверен на сломанной версии (EXP-0053). **Регрессия:** юнит
**163** (+14) · rules 87 · calc 30 · e2e 70 (+2 skip) · typecheck/check/kaif:check чисто ·
QA-прогон `node tools/verify-tierA.mjs` (обе темы × 390/1440, скриншоты
`test-results/tierA/` смотрены глазами, три прогона подряд стабильны). Мутации ловятся
(шкала, полосы, возраст, коэффициенты, источник даты).
**🚀 ВЫКАЧЕНО В БОЙ 2026-07-26** (слово владельца «деплой прод»): чистая пересборка →
`firebase deploy --only hosting --project ndim-space`, оба таргета release complete.
Сверено на живом `https://ndimspace.app`: новая страница `/menu/share` отвечает 200, в её
пререндеренном HTML **13 плиток соцсетей** и текст 1.x; боевой чанк профиля
(`_app/immutable/nodes/12.*.js`) содержит «Количество установленных связей» и шкалу
«очень мало»; чанк связей — «хозяина»; смоук `/ · /profile · /relations · /dims · /menu` —
все 200. **Сервер синхронизации и правила НЕ трогались** — выкат только хостинга.
**Где продолжить:** (1) контрольный взгляд владельца на `/profile` и раскрытую связь в бою
(+ `bugs/06` — стиль «Измерений» ждёт того же слова); (2) **тир B**: `bugs/40` boot-экран ·
переиздание PWA · `bugs/44` фото (после `bugs/04`); (3) хвост `bugs/46` — «последнее
обновление NDim ID» требует правки вычислителя (формат `relations` + пересборка образа),
«был онлайн» — развилка владельца; (4) тир C ждёт ответов владельца (🔴 `bugs/45` удаление
аккаунта — долг перед собственным каноном).

**2026-07-28 — 🧰 KAIF ОБНОВЛЁН ДО РЕЛИЗА 2.0 «Excellent KAIF»** (по слову владельца). Маркер
`.kaif/kaif.json` = **2.0** (релиз 2026-07-28), `kaif:check` зелёный (28 скиллов), `update-verify`
пройден. Маршрут — **bootstrap** (свежий `KAIF.md` → `KAIF-LOADER.mjs` → машинерия 2.0), он же
рекомендованный релизом: обновление ведёт НОВОЕ ядро, а не старое. **Проход предсказан заранее на
песочной копии проекта** (`git archive HEAD` → отдельный каталог → настоящий прогон там) и совпал с
боем байт в байт. Механика: **0 файлов заменено целиком**, 161 модуль слит на месте, 5 сущностей
добавлено, 27 сохранено; **кириллицы утрачено 0**; продуктовый код и содержательные артефакты — **0
изменений**. Регрессия: юнит **173/173**, typecheck чисто, build чисто.
Что пришло: справочник **`.kaif/KAIF_REFERENCE.md`** (12-й ключевой документ, `/help-kaif` читает его
первым) · скиллы **`/end-chat`** (полное завершение с эстафетой) и **`/derive-styleguide`** ·
**`/pause` сменил смысл** — теперь мягкая парковка, а прежняя его работа уехала в `/end-chat` ·
**режимы строгости** (черновик vs канон) и правило «заметки владельца по ходу — в беклог, не в
переключение» в `AGENT_GUIDE.md` · механизированный гейт провенанса и канон-линтер в `.kaif/tools/`
(подключение опционально) · расписка `.kaif/last-update.json` и история обновлений в маркере.
⚠️ **Главный дефект прохода — удвоение переведённых документов:** 2.0 опознаёт модули по
заголовку-сигнатуре, наши русские заголовки читаются как «другие модули», и английский шаблон
вставился РЯДОМ с русским текстом (25 файлов, +6.3 тыс. строк). Ничего не потеряно, но сдавать так
нельзя — вернул из HEAD и влил настоящую дельту (фреймворк реально изменил **15 файлов из 52**)
переводом. Лечение на будущее: в маркере объявлено **`"i18n": "translated"`**. Урок — **EXP-0064**,
отчёт для агента-разработчика KAIF — **`researches/16_kaif_20_update_mechanics.md`**.
Точка отката — тег `pre-kaif-2.0`. **Где продолжить:** беклог не изменился (см. выше).

**Предыдущее обновление:** 2026-07-26 — 🧰 **KAIF ОБНОВЛЁН ДО РЕЛИЗА 1.6 «Homeostatic KAIF»**
(по слову владельца). Маркер `.kaif/kaif.json` = **1.6** (релиз 2026-07-24), `kaif:check`
зелёный, `update-verify` пройден. **Продуктовый код не тронут: 0 файлов `src/`, `calculator/`,
`e2e/`, `tools/` в диффе; содержательные артефакты (`bugs/`, `ideas/`, `interviews/`,
`researches/`, `homeworks/`, `design/`, `plans/`) — 0 изменений.** Регрессия: юнит **149/149**,
typecheck чисто, build чисто. Механика: 23 файла заменено, 29 сохранено; ручной сшивки
потребовали **24 блока правок в 12 файлах**. Что пришло в проект: **разведартефакты**
(разведдок · канон-карта · инвентарь паритета — наш `researches/12` теперь назван каноном
в `AGENT_GUIDE.md`) · **правило трёх дверей** и «наблюдение вместо додумывания» в
`PHILOSOPHY.md` · **`/fable-judge` обязателен перед КАЖДЫМ push и деплоем** + шесть
guardrail-охот · **правило одного шага** в циклах (одно изменение = один прогон гейтов =
один коммит) · неотменяемая **git-гигиена** (`git diff --stat` перед коммитом; сначала
ignore, потом инструмент; первоисточники владельца неприкосновенны) · **чеклист выката из
5 ворот** в `/release` (зеркало прода первым) · **пометки провенанса `[AI]…[/AI]`** и ворота
записи в канон-артефакты владельца · секция **«Решения, принятые без владельца»** при закрытии
любой задачи · поля **Repro / Not for** в записях опыта. ⚠️ Механика снова попыталась
заменить русские документы английскими шаблонами (`BUG_FIXING_FRAMEWORK.md` + 4 скилла) —
поймано предполётным предсказанием, локализация восстановлена, новинки 1.6 в них перенесены
переводом; урок — **EXP-0051**. Отчёт о дефектах механики обновления для агента-разработчика
KAIF — **`researches/15_kaif_16_update_mechanics.md`** (8 находок, приоритеты починки).
Точка отката — тег `pre-kaif-1.6`. **Где продолжить:** беклог не изменился — пачка 5
инвентаря паритета (bugs/43 → 46 → 44 → 47 → 48; 45 каркас) · bugs/40 boot-экран ·
пачка 4 ждёт макетов владельцу.

**Предыдущее обновление:** 2026-07-24, ночь — ⏸ **ПАУЗА. bugs/49 ПОЧИНЕН И В БОЮ: рельс
навигации прибит.** Слово владельца: «уезжает вверх и исчезает меню с кнопками в левой
панели — ДОЛЖНО БЫТЬ ЗАФИКСИРОВАНО». Причина: колонка `SideRail` в сетке `.screen` тянулась
на высоту ДОКУМЕНТА, кнопки у её верха прокручивались со страницей (макет V2 это не
проверял — превью без скролла). Фикс: `position: sticky; top: 0; align-self: start;
height: 100dvh; overflow-y: auto` (`src/lib/ui/SideRail.svelte`). Проверки: новый
e2e-инвариант в `desktop-shell.spec.ts` (скролл вниз → рельс y=0, «Меню» во вьюпорте) ·
QA-прогон `node tools/verify-bug49.mjs` **24/24** (обе темы × 1024/1440, скриншоты
`test-results/bug49/` смотрены глазами) · регрессия: юнит **149** · e2e **70** (+2 skip) ·
typecheck/kaif:check чисто. **Выкат: агентом по слову владельца** (`npm run build` +
`firebase deploy --only hosting --project ndim-space`, оба таргета release complete);
сверено в бою: живой `SideRail.*.css` на ndimspace.app содержит sticky-правило, смоук
/dims чист. ⚠️ Попутная находка: **на этой машине висел недобитый `npm run stand`**
(эмуляторы 9099/9199/8181 + vite 5173) с прошлой сессии — из-за него 6 e2e «без данных —
честная ошибка» видели живой бэкенд и падали. Стенд погашен (родитель Stop-Process; сироты
java/8181 и vite/5173 добиты), после чего e2e стал полностью зелёным. Урок кандидат в
EXPERIENCE при следующей фиксации: **перед e2e проверяй, что порты стенда свободны;
после стенда гаси всё дерево**. ⚠️ Порт 8080 на машине занят `llama-swap` (служба
владельца, НЕ трогать) — эмулятор Firestore проекта живёт на 8181 именно поэтому.
**Где продолжить (следующая сессия):** пачка 5 инвентаря паритета (bugs/43 статистика
«Дома» → 46 раскрытая связь → 44 редактор фото → 47 соцсети → 48 правила оформления;
45 каркас) · bugs/40 boot-экран · пачка 4 ждёт макетов владельцу. Полный контекст пачки 5 —
запись ниже (🗺 ИНВЕНТАРЬ) и `bugs/00_backlog_batches.md`.

**Предыдущее обновление:** 2026-07-24, ночь — 🗺 **ИНВЕНТАРЬ ПАРИТЕТА ПРОЙДЕН ЦЕЛИКОМ**
(слово владельца «делай инвентарь», без суб-агентов). `researches/12` перевыведен: карта
**всех 37 страниц** 1.x → 2.0 (источники: `d:\work\ai_sandbox\ndim_old` index.html/app.js +
25 кадров `design/reference-1x/`). Дыры → **пачка 5** (`bugs/43–48`, порядок в `bugs/00`):
**43** профиль потерял статистику «Дома» (виджеты «Мой NDim ID» с эмоц. шкалой + «Мои
связи») · **46** раскрытая связь потеряла человека и параметры пространств (×моего/×хозяина,
возраст) · **44** нет редактора фото (в 1.x FilePond+Cropper→webp) · **45** 🔴 нет
управления аккаунтом (удаление/смена почты; «человек может удалить данные» сейчас
невыполнимо) · **47** «Пригласить друзей» без сетки соцсетей · **48** форма предложения без
«Правил оформления». Почти всё — агент сам; в интервью пачки 4 добавился 1 вопрос
(формулировка подтверждения удаления, bugs/45). Также снят канон ОБ-флоу/входа целиком
(welcome→auth→verify→ob1–4) — готовая разведка для макетов 16+19+32. MASTER_PLAN фаза 6:
инвентарь ✅, выход = пачки 4–5 закрыты + волна владельца с 0 багов. Кандидат «переиздание
PWA/TWA» подтверждён инвентарём (manifest ru/en + SW были в 1.x). **Следующее:** чинить
пачку 5 (43 → 46 → 44 → 47 → 48; 45 каркас) и bugs/40.

**Предыдущее обновление:** 2026-07-24, ночь — 🧹 **РЕВИЗИЯ БЕКЛОГА + МАСТЕР-ПЛАН ПЕРЕВЫВЕДЕН**
(по слову владельца; главная боль — «сильный уход от старого NDim»). Помечены DONE: **22 бага**
(05, 07, 13–15, 18, 21–27, 29, 30, 33–39 — все фиксы в бою после выкатов 07-17 и 07-24),
**4 идеи** (06 статистика+версии, 09 трение ОБ, 10 демо, 11 гость), **3 плана** (01 модель,
02 миграция, 04 cutover). Открытый беклог сжался до: **пачка 4** (16+19+32 ОБ-флоу/вход/пароль
одним набором макетов · 17 иконки · 20 планшет · 28 имена · 31 доки) + вне пачек (40 boot-экран —
следующий, 04 Storage, 06 слово владельца, 41 устройство владельца, plans/05 e2e лендинга) +
идеи (05 UI-часть, 07, 08, 12 ⛔, 13) + homeworks/06. `bugs/00_backlog_batches.md` освежён.
**MASTER_PLAN перевыведен** (первый раз с 10 июля!): фазы 0–2 ✅, 3 ✅ базово, 4 ✅ в бою + хвосты,
5 ✅ в основном, **новая фаза 6 «ПАРИТЕТ С 1.x»** (механика волн работает: 9→14→4→3; выход —
полный инвентарь `researches/12` + волна владельца с 0 багов), фаза 7 — соцслой за ⛔ ideas/12.
Кандидаты на заведение (ждут слова владельца): полный проход инвентаря паритета · переиздание
PWA/TWA (хвост фазы 5).

**Предыдущее обновление:** 2026-07-24, вечер — 🪟 **bugs/42 ПОЧИНЕН: виджет сервера больше не
врёт.** Скриншот владельца показал: «Полная синхронизация — минуту назад, 74 пользователя»
на спокойном Пространстве. Причина — минутное сердцебиение-успех (bugs/33) склеивалось с
числами суточного прохода, а «Запланированная» показывала следующий ЦИКЛ. Починено по канону
1.x (мандат владельца «виджет перепиши со свежим видением»): вычислитель пишет **fullSync /
partialSync раздельно** (researches/13 §6), виджет — три блока: Статус (+Следующий цикл) ·
Полная (проверено · из них обновлено · Запланированная = следующая ПОЛНАЯ) · Частичная
(«ещё не было»). test:calc **30**, QA-прогон `tools/verify-bug42.mjs` 48/48 со скриншотами
обеих тем. Контейнер в бою: первый проход — «**проверено 93, обновлено 0**» (тай-брейк
доказан в бою). **Хостинг выкачен владельцем** (`firebase deploy --only hosting`, оба
target'а release complete); агент сверил боевой бандл — строки нового виджета в
`_app/immutable/nodes/13.*.js` на ndimspace.app. Осталось: контрольный взгляд владельца
на `/space`.

**Предыдущее обновление:** 2026-07-24 — ⚙️ **ideas/14 СДЕЛАНА: сервер синхронизации экономит
запросы в духе 1.x** (владелец: «в 1.x я экономил запросы, 2.0 словно не вдохновлялся старым
кодом» → «фикси»). Разведка живого `d:\work\ai_sandbox\ndim_old` → **`researches/13`** —
руководство «как всё было устроено в синхронизаторе 1.x» (кэш в памяти, дельта по
`sync_status`, ленивый контракт свежести, пустой цикл = 1 запрос). Перенесено в
`calculator/index.mjs` (v0.2.0): кэш точек процесса + дочитка только грязных, пересчёт всех
в памяти, но **запись только изменившихся топов** (diff по детерминированной сериализации),
**тихий период** `CALC_QUIET_SECONDS` (деф. 120 с; стенд — 0), **суточный полный проход**
`CALC_FULL_SYNC_HOURS` (дописывает ленивые чужие топы, лечит кэш, чистит гостей — раз в
сутки, не каждую минуту), холостой цикл = 1 запрос + 1 запись сердцебиения (bugs/33
соблюдён), батчи по 400, тики службы не накладываются. Контракт свежести (наследие 1.x,
зафиксирован в идее): свой топ — минуты, появление в чужих топах — до суток.
**test:calc 28** (16 старых нетронуты + 12 новых: `economy.test.mjs`, `full_pass.test.mjs`),
юнит 149, typecheck/kaif:check чисто, смоук службы на эмуляторе глазами. Судейская проверка
поймала паразитную зависимость `ndim-space: file:..`, добавленную `npm --prefix ... install`
в package.json вычислителя (сломала бы Docker-сборку классом EXP-0040), — вычищена до пуша
(`226aa94`). Урок — EXP-0050. **Дополнение той же сессией — ОКНО НОВИЧКА (ideas/05,
серверная часть):** пока `firstSeen` пуст или моложе `CALC_NEWCOMER_MINUTES` (деф. 30),
тихий период не действует — первый топ ≤60 с после первой оценки, всю первую сессию топ
свежий каждым циклом (владелец: «да, суппер, делаем»). UI-часть idea 05 (честный текст
ожидания в `/relations`) остаётся открытой.
**🚀 ВЫКАЧЕНО В БОЙ той же сессией** (слово владельца «комит, пуш, деплой прод»):
образ пересобран, контейнер `ndim-calculator` заменён (docker run из plans/04 шаг 6, но
**интервал теперь дефолтные 60 с** — раньше в проде стоял `CALC_INTERVAL_SECONDS=3600`;
именно часовые ПОЛНЫЕ пересчёты владелец и видел; с новой экономикой минутный цикл дешевле
старого часового, а окно новичка требует минутного). Первый боевой полный проход: 332
точки, 93 человека, 5112 измерений, 2340 связей, **записано топов 77 из 93** — diff
работает, остальные совпали байт-в-байт. Расхождения были недетерминированной ничьёй
сортировки → добит **тай-брейк по uid** в `topFor` (иначе суточный проход переписывал бы
топы «по ничьей причине»), перевыкачено, второй полный проход канонизировал порядок (74
записи). **Где продолжить:** (1) назавтра `docker logs ndim-calculator` — суточный полный
проход должен записать ~0 топов; (2) владельцу глазами `/space` — «Работает», версия
0.2.0; (3) UI-часть idea 05; (4) очередь прежняя: bugs/40 · хвост пачки 3 (29, 27) ·
макеты пачки 4 · bugs/04 · e2e счётчика (plans/05).

**Предыдущее обновление:** 2026-07-17, день — 🔑 **Вход через Google починен** (владелец
пересоздал OAuth-клиент — homeworks/07 путь Б; агент прописал его в провайдер через
Identity Toolkit API и проверил живой эндпоинт: `deleted_client` исчез; остался
контрольный клик владельца на проде) · ✅ **ideas/12: все 4 вопроса закрыты владельцем**
(формулировки — только в документах, в UI не предупреждаем; фильтр: политика/ненависть/
преследование/угрозы/оскорбления/порно/эксплуатация; уведомления: панель + суточный
дайджест-письмо от сервера синхронизации) · 🔧 **Третья волна ручного теста владельца
(bugs/34–37) — ПОЧИНЕНА той же сессией, ждёт слова на выкат**: 34 шапка sticky
(`--panel-solid`, канон 1.x) · 35 история версий карточкой + плашки версий (канон app-17;
белое-на-белом не читалось) · 36 лайтбокс фото порталом в body (hover-transform карточки
ломал `fixed` — EXP-0048) + скролл-лок · 37 сортировка «Мой NDim ID» без языка (тай-брейк
по id; юнит переписан, мутации ловятся). QA-прогон — новая оснастка
`tools/verify-batch4.mjs` (все проверки зелёные, скриншоты обеих тем смотрены глазами);
регрессия: юнит **149** · e2e **70** (+2 skip) · typecheck/check чисто.
**Пачка 34–37 ВЫКАЧЕНА В БОЙ** (слово владельца «деплой прод, мне удобно тестировать в
проде») + следом **bugs/38** (манифест: на телефоне — кнопка на новую страницу
`/menu/manifesto`, на десктопе — виджет рядом с кнопками, сетка `/menu` трёхколоночная;
текст — общий модуль `src/lib/content/manifest.ts`) — тоже в бою, смоук зелёный.
**Четвёртая волна (39–41, чат 2026-07-17):** ✅ 39 в бою — язык ВЫПАДАШКОЙ «Ru ▾» +
тема ☾/☀ рядом в шапке на всех экранах (persist в самой шапке, контракт `onLang`;
две ловушки Svelte 5 — EXP-0049) · 🔬 41 (зазор под панелью на манифесте) на стенде
НЕ воспроизводится (0px на 390/800/1023) — нужен скрин/устройство владельца, добивать
вместе с bugs/20 · 🔴 40 (boot-экран вместо мелькания лендинга) — следующий в работу.
Очередь дальше: bugs/40 · хвост пачки 3 (29, 27) · макеты пачки 4 · bugs/04 ·
e2e счётчика (plans/05).

**Предыдущее обновление:** 2026-07-17 — 🧰 **KAIF обновлён до РЕЛИЗА 1.5 «Tested KAIF»**
(release-канал вышел в тот же день; `/kaif-update` по слову владельца): механический проход
`node .kaif/kaif-core.mjs update` + ручная сшивка. Маркер `.kaif/kaif.json` = **1.5**,
`kaif:check` 28/28, `update-verify` пройден. ⚠️ Механика заменила 18 русских скиллов и
`PHILOSOPHY.md` английскими шаблонами — возвращены из git с нормализацией LF, проектные
вставки README (`bugs/`, `ideas/`) сшиты заново поверх свежих RU-шаблонов 1.5; урок —
**EXP-0047**. Новое в 1.5 (пришло ещё предрелизом): `TESTING_FRAMEWORK.md` (маркеры
`[NOT-TESTED]`/`[TESTED: …]`), скиллы `fable-*` (судейский проход обязателен в циклах и /release,
ворота INTENT + близнецы TWINS в багфиксе), `.kaif/spheres/`, обёртки 5 агентских систем,
`.kaif/kaif-core.mjs` за npm-хендлами. Продуктовый код НЕ тронут (смок после предрелиза:
build ✅ · юнит 149/149 ✅ · typecheck ✅ · check 0/0 ✅ — `test-results/kaif15-smoke-report.md`).
Коммиты предрелиза: 95bac16…a7aca4c.

Предыдущая веха: 2026-07-16, ночь — ⏸ **ПАУЗА. Пачки 2 и 3 волн 08–09 починены и
ВЫКАЧЕНЫ В БОЙ** (слово владельца «комит, пуш, деплой прод»).

### ⏸ Пауза 2026-07-16: что в бою и где продолжить

**В бою** (этой сессией, тремя выкатами): пачка 1 (лендинг-редирект · поздравление только
новым · мобильная оболочка + прибитая панель) · пачка 2 (незаметная догрузка лент ·
«Мой NDim ID» без пляски · ровные карточки · фото во весь экран · непрозрачные панель и
меню) · пачка 3 (каноничный лоадер «( )» 1.x · подсказки метрик ⓘ дословно из 1.x · тап без
обводки · история версий вложенными раскрывашками · Похожесть СПРАВА — канон 1.x по слову
владельца · зачистка мёртвой зоны /dims).

**Где продолжить (по убыванию):**
1. **Хвост пачки 3**: bugs/29 (плотность меню — разведка кадра app-08 в `researches/12`),
   bugs/27 (полнота карточек измерений — инвентарь полей из `ndim-old`).
2. **Пачка 4 — развилки владельцу** (готовить макеты): ОБ-флоу + экран входа + пароль
   (16/19/32 — ОДИН набор из 4 макетов) · иконки (17 — 4 варианта SVG-набора) ·
   **планшетная раскладка** (bugs/20 — ВОСПРОИЗВЕДЁН на HeadWolf Titan1 800×1280: пустота
   над панелью до 790px; зона 600–1023px не спроектирована; варианты в баге) ·
   имя/почта вместо «Без имени» (28 — вопрос формата) · документы из Firestore (31).
3. Хвосты plans/05 (e2e лендинга на счётчик, пересборка боевого `ndim-calculator` — он
   должен писать `space/public_metrics`) и bugs/04 (правила Storage; черновик уже лежит
   `storage.rules`, НЕ выпускать без тестов).
4. Стенд вырос: эмулятор Storage (9199, у стенда есть лица), каталог 45 осей, оснастки
   `tools/verify-bugs08|batch2|batch3.mjs`. Канон процессов — `plans/06` + `researches/12`.

### 🔴 Волна 09 (2026-07-16) — вердикт владельца и реорганизация беклога

Владелец (профессиональный QA-инженер) второй волной записал 14 багов и **вердикт о
процессах**: разведка 1.x и тестирование при создании 2.0 были неприемлемо слабы — 2.0
растерял удобства оригинала. Ответ:

- **Беклог разведён по отдельным баг-документам** `bugs/10–32` (приказ владельца), в каждом —
  причина/план/проверка. Индексы волн: `bugs/08`, `bugs/09`. Пачки — `bugs/00_backlog_batches.md`.
- **`plans/06_quality_process_overhaul.md`** — радикальное улучшение процессов: (1) разведка
  ЖИВОГО поведения 1.x до кода (`researches/12_1x_behavior_inventory.md` — завести в пачке 3),
  (2) обязательный QA-прогон живым Chrome как ворота сдачи (обе темы, 2 ширины, консоль,
  скриншоты, пиксели). Вписано в шапку чеклиста `AGENT_GUIDE.md`, урок — EXP-0046.
- **Пачка 2 починена и проверена** (QA-прогон `tools/verify-batch2.mjs`, 21/21; регрессия
  149/87/16/70 зелёная; **ждёт слова владельца на выкат**): незаметная прогрессивная
  догрузка лент (13) · «Мой NDim ID» без пляски, свой порядок и догрузка (18) · ровные
  карточки (15) · фото во весь экран + **эмулятор Storage на стенде, у стенда теперь есть
  лица** (14) · непрозрачные панель (22) и меню (23) — токен `--panel-solid`.
- **Пачка 3 частично готова** (та же сессия; QA-прогон `tools/verify-batch3.mjs` 19/19,
  регрессия зелёная, ждёт выката): ✅ 21 каноничная карточка загрузки (`Loading.svelte`,
  кольцо «( )» из `ndim-old`) · ✅ 24 подсказки метрик ⓘ (тексты 1.x дословно) · ✅ 26 тап
  без обводки, `:focus-visible` видим · ✅ 30 история версий вложенными раскрывашками ·
  попутно зачищена мёртвая зона `/dims` (96px→24px). Разведка канона — новый живой
  справочник **`researches/12_1x_behavior_inventory.md`** (по plans/06).
  **Два вопроса владельцу** (🔬 исследовано, не воспроизводится):
  - `bugs/20` — пустота под панелью: замеры на 9 экранах (стенд+прод) дают 0px зазора;
    нужен экран + устройство/браузер (лучше скрин).
  - `bugs/25` — «Похожесть справа»: в 1.x она СПРАВА (кадр app-02), в 2.0 уже слева;
    на каком экране владелец её видит справа — и какой канон желаем?
  Хвост пачки 3: 29 (плотность меню), 27 (полнота карточек измерений) — следующая сессия.
  **Пачка 4** ждёт владельца: ОБ-флоу + экран входа + пароль (16/19/32, один набор макетов),
  иконки (17, 4 варианта), имя/почта (28, вопрос формата), документы в Firestore (31).

---

**Предыдущее обновление:** 2026-07-16 — 🔧 **bugs/08 (вторая волна ручного теста владельца): пачка 1 починена.**

### 🔧 bugs/08 — 8 дефектов второй волны (2026-07-16), пачка 1 готова

Владелец прошёлся по проду руками и записал 8 дефектов; документ причёсан, по каждому — план
(`bugs/08_krinik_tests.md`). **Пачка 1 починена, проверена живьём и ВЫКАЧЕНА В БОЙ**
(слово владельца «деплой»; оснастка `node tools/verify-bugs08.mjs` на стенде 18/18, смоук
прода зелёный, скриншоты в `test-results/bugs08/`). Попутно: `/dims` со старого адреса
теперь 301 на канонический домен (дыра конфига хостинга):

1. **08.1** — вошедшего (и гостя) лендинг уводит в `/profile`; заодно `landing_view` и счётчик
   людей считаются только для тех, кто остаётся (`src/lib/data/session.ts` + onMount лендинга).
2. **08.2** — поздравление «Профиль сохранён» видит только тот, чей аккаунт создан ТОЛЬКО ЧТО
   (флаг `created` в `UpgradeResult`, `getAdditionalUserInfo().isNewUser`); 331 человек из 1.x
   входит без него, воронка `account_created` больше не считает входы.
3. **08.3** — шапка/табы/нижняя панель во всю ширину, панель `sticky` прибита к низу
   (BottomNav + оболочки 6 экранов; контент — колонной 458px по центру).

**Осталось:** пачка 2 — 08.4 прогрессивная подгрузка лент (якорь за вьюпортом вместо порций
по кнопке), 08.5 фото на весь экран, 08.6 одинаковый размер карточек измерений; пачка 3 —
08.7 онбординг-флоу (4 макета владельцу), 08.8 векторные иконки по канону 1.x (4 варианта).
Регрессия после пачки 1 вся зелёная: юнит **149** · rules **87** · calc **16** · e2e **70**
(+2 skip) · typecheck/check чисто.

---

**Предыдущее обновление:** 2026-07-12, вечер — 🚀 2.0 в бою + 🔴 **боль владельца записана в баги 05–07.**
Cutover выполнен целиком (`plans/04_cutover.md`, шаги 1–6). Версия 1.x мертва, 2.0 живёт по
адресам `ndimspace.app` и `ndim-space.web.app`. Труд 331 человека перенесён и сверен.

### 🔴 Боль владельца после запуска (2026-07-12, вечер) — ТЕКУЩИЙ приоритет

Слово владельца: «Лендинг делал Fable 5 — красиво. Внутренности делал Opus 4.8 по планам
Fable 5 — топорно и с багами». По его просьбе боль записана в баги (чинить в этом порядке):

1. **`bugs/06`** — «Измерения» свёрстаны НЕ в стиле приборной панели, как другие вкладки
   (на десктопе — одна колонка на 1100px вместо канона V2 «Рабочий стол» / виджетной сетки).
2. **`bugs/05`** — по всему приложению нет анимаций: Профиль, Связи, Пространство, Меню — ноль
   переходов (инвентаризация в баге). Планка — лендинг и демо (работа Fable 5).
3. **`bugs/07`** — лендинг врёт: «С нами уже 2 184 человека» зашито литералом, в базе 331.
   В 1.x счётчик был живым (`space/public_metrics`, читался без авторизации) — вернуть.

Аудит живого прода (Playwright, аноним, все 6 маршрутов): экраны отвечают, ошибок консоли нет,
`/menu` жив (таймаут networkidle даёт длящееся Firestore-соединение — не дефект).

**Той же сессией ПОЧИНЕНО и выкачено в бой (слово владельца: «комит, пуш, деплой прод»):**
`f657668` «Измерения» по канону V2 + анимации + дедупликация имён каталога ·
`c4acf46` анимации всех внутренних экранов (канон `src/lib/ui/motion.ts`) ·
bugs/07 — литерал «2 184» изъят, счётчик живой из `space/public_metrics`.
⏭ **Хвост — `plans/05_bugs_05-07_tail.md`**: прогнать регрессию (rules 87 · calc 16), дописать
e2e лендинга на отсутствие выдуманного числа, пересобрать боевой `ndim-calculator`
(он должен начать писать `space/public_metrics`), затем пометить баги DONE.

### 🚀 Боевой запуск состоялся (2026-07-12)

- ✅ **Точка отката:** `gs://ndim-space-backup/2026-07-12-cutover-1550` (SUCCESSFUL), снята
  непосредственно перед выкатом. Предыдущая — `gs://ndim-space-backup/2026-07-12`.
- ✅ **Данные перенесены:** 331 человек · 330 точек · **4083 оценки** · 4 заявки — **5079
  документов 2.0**. Коллекции 1.x **не тронуты** (откат возможен).
- ✅ **Сверка на боевой базе ЧИСТАЯ:** у всех 331 публичный бакет · оценки 4083→4083 · ни одного
  потерянного измерения · **паритет похожести: 2306 сверено, 0 расхождений** · чужих почт нет.
  (97 «устаревших» связей 1.x — ожидаемо, их пересчитал сервер синхронизации.)
- ✅ **Хостинг:** выкачен на **оба** сайта. Проверено живьём: оба отдают 2.0 (SvelteKit), следов
  `app.js` 1.x нет.
- ✅ **Правила и индексы** выпущены — это и был необратимый шаг. Два индекса 1.x оставлены
  нетронутыми (`--force` не применялся) — по плану «старое не удаляем».
- ✅ **Админы:** `admin: true` выдан 2 людям (claim в токене, не список email, как было в 1.x).
- ✅ **Сервер синхронизации в бою:** Docker, служба, `--restart unless-stopped`, цикл **3600 с**.
  Первый боевой цикл: **93 грязные точки → 93 топа пересчитано**; Пространство — **93 человека ·
  5112 измерений · 2340 связей**. Сердцебиение пишется каждый цикл → `/space` показывает «Работает».
  Контейнер: `ndim-calculator` (на ЭТОЙ машине — она и есть серверный ПК).

**🔴 Два дефекта, пойманных НА выкате (оба починены, оба записаны в `EXPERIENCE.md`):**
1. **EXP-0039** — `04-admin-claim.mjs` разрешал почту в uid по коллекции `users`, а миграция
   этот документ **перезаписывает** схемой 2.0, где поля `email` нет: «2 почты → 0 uid».
   Починено: разрешаем через **Firebase Auth** (`getUserByEmail`). Порядок шагов больше не важен.
2. **EXP-0040** — **Dockerfile отстал от кода**: копировал только `similarity.ts`, а `index.mjs`
   импортирует ещё и `model/stats.ts` → боевой контейнер упал в цикле перезапуска. Починено:
   копируем **директориями** (`src/lib/similarity/` + `src/lib/model/`). Стенд этого поймать
   не мог — локально весь репозиторий лежит на диске.

### 🔥 БОЕВОЙ ИНЦИДЕНТ через минуты после запуска — разобран и закрыт (2026-07-12)

Владелец вошёл и **сразу увидел сломанный продукт**. Пять причин, все найдены и починены в бою
(коммиты `c444d01`, `fc2955d`). Это самая ценная часть дня — читать перед следующей миграцией.

| Что видел человек | Настоящая причина | Урок |
|---|---|---|
| «Профиль» падал у всех: `undefined (reading 'ru')` | **Каталог измерений не мигрировали вовсе.** `researches/02` описывал `dims/` строкой «тексты названия/описания — **в правилах не описаны**» → шаг просто не написали. В бою название в `name`, экран читает `title` | **EXP-0041** |
| он же | **Один служебный документ (`dims_list`) ронял ВЕСЬ экран** — `undefined.ru` в сортировке | **EXP-0041** |
| «Меню» не кликалось | В `SideRail` остался `href: null` со времён заглушки «скоро». e2e не поймал: искал ссылку через `.first()` и попадал в мобильную панель | добавлен тест на рельс |
| ссылка из письма → лендинг | **Action URL писем в проекте Firebase** указывал на корень старого домена (наследие 1.x, где `index.html` сам разбирал `oobCode`) | **EXP-0042** |
| «нет фото, в старом NDim были фотки» | Фото живёт **не в Firestore**, а в **Storage** (`users/{uid}/avatar/avatar.webp`); в базе только ФЛАГ `avatar: bool`. Флаг перенесли — за картинкой не сходили | **EXP-0043** |

Попутно: `ndim-space.web.app` → **301** → `ndimspace.app` (у продукта один дом); починен падеж
(«В Пространстве с феврал**ь** 2025» → «февраля»); `callbackUri` возвращён на штатный обработчик.

**Три вывода, которые дороже самих фиксов:**
1. **Стенд был засеян ИДЕАЛЬНЫМИ данными** — и потому не показал ничего из этого. Стенд обязан
   быть похож на бой, **включая его сор** (`seed-dev.mjs` теперь сеет документ без названия).
2. **«В правилах не описано» в исследовании — это ДЫРА, а не примечание.** Место, где модель
   говорит «не знаю», обязано стать пунктом плана.
3. **Мигрируй ХРАНИЛИЩА, а не коллекции.** Труд людей лежит ещё и в Storage, в Auth, в настройках
   проекта. Булев флаг вместо данных (`avatar: bool`) — маячок: настоящее лежит где-то ещё.

### ✅ Шаг 7 (проверка руками) — фактически пройден

Владелец вошёл по ссылке из письма, попал **в свой профиль** (485 измерений, своё фото, свои
связи). Осталось по мелочи: вход через Google и гость с лендинга — на слово владельцу.

**Откат, если понадобится** (`plans/04_cutover.md` → «Откат»): правила 1.x из git → приложение 1.x
из архива `ndim-old` → в крайнем случае импорт `gs://ndim-space-backup/2026-07-12-cutover-1550`.

### 🆕 Долги, открытые инцидентом

- 🔧 **`bugs/04`** — **правила Storage до сих пор от 1.x** (в `firebase.json` секции `storage` нет).
  Права админа зашиты **списком email** — антипаттерн, который мы уже вычистили из Firestore.
  Не дыра, но долг. ⚠️ Не ужесточать чтение аватаров сгоряча: на нём держится показ лиц.
- 🆕 **`ideas/13`** — инсталлятор сервера синхронизации + **Admin WebUI** (идея владельца).
- Фото в боевом Storage — всего **13** на 331 человека. Остальные видят букву. Это факт данных.

### Регрессия (вся зелёная, 2026-07-16)

`npm test` **149** · `test:rules` **87** · `test:calc` **16** · `e2e` **70** (+2 skip) ·
`typecheck` чисто · `check` **0/0** · `kaif:check` чисто.
⚠️ `test:calc` сразу после гашения стенда может словить гонку портов — выждать пару секунд
и перезапустить (чистый прогон зелёный).
⚠️ **gcloud проверять в PowerShell**, а не в git-bash (в bash обёртка печатает «Python» вместо аккаунта).

### Что ещё ждёт владельца

- **`homeworks/06`** — портрет и публичные ссылки автора для `/menu/author`.
- **`ideas/12`** — личная ответственность автора контента (⛔ блокирует социальный слой).
- **15 измерений**, по которым стоят оценки, удалены из каталога 1.x: восстановить или оставить?
- Приоритизация идей 07–08.

### Что сделано в этой сессии (2026-07-12, ночь) — подготовка боевого запуска

**Владелец дал добро на боевой запуск.** Сделано всё, кроме необратимого шага.

- ✅ **Резервная копия боевой базы**: `gs://ndim-space-backup/2026-07-12` — 6444 документа.
  Это точка отката. Копия аккаунтов Auth НЕ делалась (решение владельца: 299 живых почт на
  рабочем диске — это ровно тот риск, из-за которого закрывали `bugs/01`).
- ✅ **Инвентаризация боевой базы** (`tools/migrate/01-inventory.mjs`, только чтение):
  **331 человек · 4083 оценки · 5112 измерений**; НО непустой NDim ID лишь у **93** —
  у 237 из 330 профилей оценок нет вовсе. Инвариант плана 02 держится: ни у кого не два `ndimids`.
- ✅ **Миграция и сверка прогнаны НА КОПИИ** (база `sandbox2`): 4083 оценки из 4083, ни одного
  потерянного измерения, **паритет похожести: 2306 связей сверено, 0 расхождений**, чужих почт
  в данных 2.0 нет.
- 🔴 **Песочница спасла от двух необратимых ошибок:**
  1. топ связей в 1.x лежит **JSON-строкой**, а не массивом (модель `researches/02` этого не
     знала) — перенос «как есть» записал бы всем **пустые связи**;
  2. документ `relations` 1.x лежит по **тому же пути** `relations/{uid}`, что и документ 2.0, —
     «перенос» **затёр бы оригинал**. Решение: связи не мигрируем вовсе, их пересчитает сервер
     синхронизации (они производные).
- ✅ **Настройки входа боевого проекта** (владелец разрешил явно, показан точный диф): включён
  вход по ссылке из письма, включён анонимный вход (гостевой режим), домены `ndimspace.app` и
  `www.ndimspace.app` внесены в authorized domains. Google уже был включён.
  **Находка:** 109 из 331 человека не подтвердили почту — правила 2.0 без неё не показывают
  ничего. Чинится само: вход по ссылке и через Google подтверждают почту.
- ✅ **Приложение переведено в бой**: `src/lib/firebase.ts` выбирает окружение по хосту;
  экраны показывают **настоящий вход** (Google · ссылка на почту · гостем), а не заглушку;
  человек из 1.x входит своей почтой и попадает в СВОЙ профиль (тот же UID); лендинг ведёт
  в продукт, а не в 1.x.
- ✅ **Сценарий запуска написан**: `plans/04_cutover.md` — по шагам, с откатом.

**Стенд:** `npm test` **133** (+22 на миграции) · `test:rules` **85** · `test:calc` **15** ·
`npm run e2e` **57**. Мутации ловятся (молча спрятанная видимость · потерянная оценка «0» ·
переехавший email гостя · затёртая дата регистрации).

### ⛔ Блокирующее требование владельца (2026-07-12): `ideas/12` — личная ответственность автора

Социальный слой — комментарии (`ideas/03`) и личные сообщения (`ideas/04`) — **НЕ ВЫПУСКАЕТСЯ**,
пока не сделано `ideas/12_personal_responsibility.md`: минимальный фильтр экстремального контента
+ **панель администратора** с очередью «опасного» на проверку, и правовые тексты (манифест, условия
использования, политика приватности) о личной ответственности автора контента. Это про безопасность
живого человека — Николая. Тексты пишет владелец (агент готовит черновик), панель админа — отдельный
экран (интервью + 4 макета). В идее — 4 открытых вопроса владельцу.

### Что сделано в этой сессии (2026-07-12, вечер, часть 2) — экран «Меню»

Реализован по утверждённому V1 «Список» + блок манифеста (`design/menu-mockups.html`).
Смотреть: `npm run stand` → http://localhost:5173/menu

- **Меню начинается со смысла**: манифест (текст владельца, утверждён — про то, что ЕСТЬ),
  затем аккаунт (кто я · выход; **у гостя** — пунктирная карточка и «Сохранить результаты»),
  вид (язык RU/EN, тема), «Поделиться» (системное «поделиться» или копирование ссылки),
  документы, проект, версии в подвале.
- **За меню стоят 8 настоящих страниц**, а не заглушки: `/menu/manual`, `/menu/terms`,
  `/menu/privacy`, `/menu/disclaimer`, `/menu/support`, `/menu/donate`, `/menu/about`,
  `/menu/author`. Все пререндерены в статику.
- **Тексты документов НЕ переписаны руками**: `tools/extract-docs.mjs` генерирует
  `src/lib/content/docs.ts` прямо из `researches/06` и `researches/07`, где они уже сняты
  дословно. Переписывать юридический текст руками = гарантированно наплодить опечаток.
- **Чего в руководстве НЕТ и почему**: разделы 1.x про экраны («Дом», суб-вкладки «Все» /
  «Мой NDim ID», поиск и предложение измерений) и определения «Синхронизация» / «Сервер
  синхронизации» с почасовым расписанием 1.x. В 2.0 такого интерфейса и такого расписания нет —
  дословный перенос был бы враньём о продукте. Страница честно говорит, что эти разделы
  обновляются.
- **Поддержка** — `ndimspace@yandex.ru` (адрес из живого 1.x). **Пожертвование** —
  `donationalerts.com/r/mikalai_kryvusha`, спокойная страница благодарности: ни «премиума»,
  ни «плюса», ни давления (GOAL.md); e2e это проверяет.
- **Домашка `homeworks/06`**: портрет автора и его публичные ссылки (Instagram, LinkedIn,
  YouTube, VK) — их у агента нет, выдумывать ссылки на живого человека нельзя.
  EN-версии четырёх коротких страниц — перевод агента (английских оригиналов 1.x нет).

**Стенд:** `npm run e2e` **57** (+12; 6 проверок меню × 2 раскладки), мутации ловятся
(манифест без ценностей · подменённая ссылка доната · пропавшая шкала оценок).

### Что сделано в этой сессии (2026-07-12, вечер) — экран «Пространство»

Реализован по утверждённому V1 «Приборная панель» (`design/space-mockups.html`).
Смотреть: `npm run stand` → http://localhost:5173/space.

- **Сервер синхронизации теперь производит статистику Пространства** (`calculator/index.mjs`):
  пишет `space/stats` (люди · измерения · диаметр · оценки · связи · средняя похожесть ·
  распределение · новые измерения за сутки), снимок дня `space/stats/daily/{дата}` и сердцебиение
  с версией — `space/server`. Считает всё **чистый модуль** `src/lib/model/stats.ts` — тот же,
  которым экран эти цифры читает.
- **Тренды растут из снимков** (ideas/06): «за неделю» = разность с ближайшим снимком не позже
  недели назад; виджет «Сегодня» = разность со вчерашним. **Истории нет → тренда нет** (нулевой
  рост не выдумываем). Спокойные сутки → честное «Сегодня в Пространстве ничего не изменилось».
- **Состояние сервера ВЫВОДИТСЯ из сердцебиения, а не рисуется**: молчит дольше трёх интервалов —
  «Не отвечает» (слово выбрал агент: макет этот случай не покрывал; «остановлен» было бы враньём —
  мы знаем лишь, что он не отвечает). Зелёная лампочка — рядом со **словом состояния**, как велел
  владелец.
- **Стенд стал живым**: `npm run stand` теперь гоняет сервер синхронизации **службой** (цикл 15 с,
  `tools/stand.mjs`), а не однократно. Иначе стенд врал бы о продукте («Не отвечает» через 3 минуты),
  а оценка из браузера не долетала бы до «Связей» и «Пространства» сама.
- **Версии на виду**: приложение — `__APP_VERSION__` из `package.json` (vite `define`, версия
  проекта поднята до **2.0.0**); сервер — из `space/server`.
- **Правила**: клиенту запись в `space/stats` / `space/server` / снимки **запрещена** — цифры
  витрины не должен уметь подделать никто. Читают все вошедшие, **включая гостя** (в агрегатах
  нет ничего персонального).
- **Гость не входит в счётчики Пространства** — он невидим другим (В3): иначе цифра обещала бы
  людей, которых не найти.
- Побочно: русская морфология вынесена в `src/lib/ui/format.ts` (одна на все экраны), в токены тем
  добавлены `--up` / `--down` (тренды).

**Стенд:** `npm test` **111** (+23) · `npm run test:rules` **85** (+4) · `npm run test:calc`
**15** (+6) · `npm run e2e` **45** (+8). Всё проверено мутациями (5 на агрегатах, 3 на правилах,
2 на e2e). **Эмуляторный тест поймал настоящий баг**: в агрегатор передавалась карта оценок вместо
их числа — юнит-тесты этого увидеть не могли.

### Что сделано в этой сессии (2026-07-12, первая половина)

**Утверждены владельцем:** десктопная раскладка **V2 «Рабочий стол»** · вход **V4 «Врезка»** ·
«Пространство» **V1 + виджеты** · «Меню» **V1 + манифест**. Все макеты — в `design/`, отмечены
внутри файлов.

**Реализовано:** этап 3 (аккаунт без пароля) и этап 4 (воронка) плана 03 · десктопная оболочка
(`SideRail`) · карта демо переделана (лица ×1.8, шкала на весь радиус) · починена вравшая строка
«В Пространстве с мая 2025».

**Канон, установленный владельцем** (нарушать нельзя): «Измерение», НИКОГДА «ось» · «Сервер
синхронизации», НИКОГДА «вычислитель» · состояние «Работает», НИКОГДА «жив» · обращение на «Вы» ·
слово «навсегда» НЕ пишем (человек может удалить данные) · ноль внутреннего жаргона на лице
приложения · ценностные тексты пишем УТВЕРДИТЕЛЬНО (про то, что ЕСТЬ, а не чего нет) · дашборды
собираем из виджетов.

---

**Детали (предыдущее обновление той же сессии):** план 03, этапы 3 и 4.
**Этап 4 — воронка:** `space/funnel/days/{дата}`, четыре счётчика (`landing_view`, `demo_touch`,
`guest_start`, `account_created`) и больше ничего — ни UID, ни почты, ни устройства. Правила —
самое тесное место: писать может кто угодно (первый шаг случается до входа), поэтому разрешены
только эти четыре имени, за запись меняется ровно ОДИН счётчик и ровно на +1, уменьшать нельзя,
читает только админ. **`test:rules` → 81** (+8 отказов), **2 мутации ловятся**. Firebase в
`funnel.ts` тянется **динамическим импортом** — в бандл лендинга SDK не попадает (проверено).
Сквозной прогон на стенде: лендинг → 2 клика по звёздам → гость → аккаунт = воронка `1/1/1/1`
(считаем людей, а не клики). На проде воронка — осознанный no-op до появления боевого конфига.

**Этап 3 — аккаунт без пароля:**
Утверждён макет **V4 «Врезка»** (`design/login-mockups.html`): карточка гостя разворачивается в
вход прямо на месте. `src/lib/data/account.ts` — Google (`linkWithPopup`) и ссылка на почту
(`linkWithCredential`), строго `link*`, не `signIn*`: **UID сохраняется, труд гостя остаётся**.
Критично: апгрейд ОБЯЗАН снять с точки флаг `guest` (`promoteGuestPoint`) и обновить токен
(`getIdToken(true)`) — иначе человек с аккаунтом остался бы невидимым, а автоочистка удалила бы его
данные через 30 дней. Сквозной сценарий на стенде зелёный (гость оценил измерение → письмо →
аккаунт: UID тот же, оценки на месте, `guest: false`), **мутация «флаг не снят» ловится**.
Попутно: починена вравшая строка «В Пространстве с мая 2025» (была жёстко вписана).

**⚠️ ДВА КАНОНА, установленных владельцем 2026-07-12 — читать перед любой строкой текста:**
**«Словарь продукта»** в `AGENT_GUIDE.md` (**«Измерение»**, НИКОГДА «ось» — наследие 1.x, уважаем)
и **правила тона** в шапке `design/login-mockups.html` (везде «Вы» · слово «навсегда» НЕ пишем,
человек может удалить свои данные · никакого внутреннего жаргона на лице приложения).
Механизм ошибки: агент пишет интерфейс языком, которым описывает себе код. Пойман дважды.

**Предыдущее обновление:** 2026-07-11 — **десктопная раскладка V2 «Рабочий стол» утверждена и
реализована.** Приложение больше не колонка 430px по центру на широком экране: рельс навигации
слева (`src/lib/ui/SideRail.svelte`, от 1024px), нижняя панель на десктопе прячется, мобильная
раскладка не изменилась. `/profile` и `/relations` — сетки в 2 колонки (связи от 1560px — в 3),
лендинг раскрыт до 1040px, демо: слева звёзды и соседи, справа большая карта. **Карта демо
переделана** по правкам владельца: лица ×1.8, пунктирный горизонт на всю панель, шкала расстояний
растянута на весь радиус (ядро даёт долю 0…78 % — раньше она жалась в треть радиуса).
Стенд: **e2e 37** (+`e2e/desktop-shell.spec.ts`, 2 мутации ловятся). Макеты и отметка об
утверждении — `design/desktop-layout-mockups.html`. Дальше — **этап 3 плана 03: аккаунт без
пароля** (Google + волшебная ссылка через `linkWithCredential`; экран входа = 4 макета на
утверждение).

**Предыдущее обновление:** 2026-07-11, ночь — **демо похожести задеплоено на прод**
https://ndimspace.app (владелец явно подтвердил цель; проверено: демо пререндерено в живом HTML,
аватарки отдаются, sitemap/robots/404 целы) · **домашка 04 закрыта владельцем** (sitemap подан в
GSC и Яндексе, Bing-импорт завершён) · **приоритизация владельца**: следующим — **гостевой режим**
(`plans/03`, этап 2), идеи 06–08 — после. Сертификат www всё ещё `CERT_PROPAGATING` (301 работает).

**Гостевой режим (этап 2 плана 03) — нижняя половина готова той же ночью:** правила
(`isGuest()`, честный флаг `guest` на точке, гостю каталог измерений и своё, запрещена любая
публикация себя) — 17 тестов отказов (**73** в `test:rules`), 3 мутации пойманы · вычислитель
фильтрует гостей из чужих relations (свой топ гость получает) и **вычищает осиротевших
гостей** (30 дн. по `lastSync`; труд полноценных людей неприкосновенен — охраняется тестом) —
новый стенд **`npm run test:calc`** (9 проверок, 2 мутации пойманы). **UI-флоу гостя
утверждён и РЕАЛИЗОВАН** тем же вечером: V1 «Тихий бейдж» с правками владельца
(центрированная карточка, аватар-силуэт, «Пока что ты гость…», [Сохранить результаты]) —
`/profile?guest=1` на стенде входит анонимно без форм, оценки пишутся через правила,
труд переживает перезагрузку (проверено живым Playwright-сценарием); мост из демо на
localhost (e2e **32**, мутация ловится). **Этап 2 плана 03 закрыт на стенде.**
Дальше по плану 03 — **этап 3: аккаунт без пароля** (linkWithCredential, Google +
волшебная ссылка; экран входа = 4 макета на утверждение) и этап 4 (воронка).

За длинную сессию 2026-07-11 (вечер) сделано два больших блока:

1. **SEO-хвост живого домена доведён почти до конца.** sitemap.xml + robots.txt (+`Sitemap:`)
   + 404.html **задеплоены и проверены на живом https://ndimspace.app** · Lighthouse по домену:
   **Performance 100, SEO 100** · **www.ndimspace.app работает** (владелец внёс CNAME и
   ACME-TXT в Porkbun; отвечает 301 на apex; `hostState: HOST_ACTIVE`, сертификат в статусе
   `CERT_PROPAGATING` — дораскатается сам, проверь при случае) · владелец **прописал сайт в
   GSC, Яндексе и Bing** (TXT-верификации в DNS видны; хвосты — в «Ожидает решения»).
2. **Боль онбординга (`ideas/09`) отработана от исследования до кода за один день:**
   `researches/10` (трение, lazy registration, анонимный вход) → интервью №004 закрыто
   (оба слоя «пощупать», без паролей + OAuth, гость невидим другим, своя воронка) →
   план `plans/03` → макеты, владелец утвердил **V5 «Синтез»** → **демо похожести реализовано
   на лендинге**: `src/lib/ui/SimilarityDemo.svelte`, настоящее ядро импортом, ru/en,
   **аватарки-иллюстрации** (владелец сгенерировал в ChatGPT, `homeworks/05` DONE; кроп лиц
   «общим планом», hover-рост, тап → полный портрет, пульс). ✅ **Демо задеплоено на прод**
   2026-07-11 ночью — владелец подтвердил боевую цель явно.

Стенд: **30 e2e** (лендинг + демо + смоук экранов + SEO), 88 юнит, всё зелёное, мутации ловятся.
Из прошлых сессий всё в силе: прод-заглушка `/profile` и `/relations` на публичном хосте, два
экрана продукта на модели 2.0 на localhost, вычислитель в Docker (эта машина = серверный ПК),
лого V3, идеи владельца 06–08 в беклоге.

---

## Что сделано

### Фаза 0 — Фундамент ✅

- ✅ **Наследие сохранено и вынесено из проекта.** NDim Space 1.x целиком живёт в приватном
  `MikalaiKryvusha/ndim-old`. По решению владельца новый проект начинается с чистого листа: старых
  файлов здесь нет. Ключевое знание выжато в `researches/02` (модель данных) и `researches/03` (ядро).
- ✅ **`NDim_Space/` спасён.** 14 файлов лазерной гравировки (34.9 МБ) **никогда не коммитились** и
  существовали только на диске. Запушены в `ndim-old` перед удалением — иначе исчезли бы навсегда.
- ✅ **История спасена.** Все 825 коммитов 1.x упакованы в bundle (39 МБ, `git bundle verify` →
  «complete history»). 2026-07-11 владелец скопировал `.private/` на внешний носитель и удалил
  с рабочего диска. Восстановление истории: клон приватного `ndim-old` либо копия владельца.
- ✅ **Новая история начата.** `git init -b main`, корневой `.gitignore`.
- ✅ **KAIF развёрнут** (v1.4, сверен с origin побайтово, режим standard, язык русский,
  агент Claude Code). Скрипты развёртывания — в `tools/`, перепроверка — `npm run kaif:check`.
- ✅ **Проект изучен.** Математическое ядро найдено и разобрано в
  `PROJECT_ARCHITECTURE_INTERNAL_MAP.md`.
- ✅ **Аудит секретов — все три находки закрыты.** Пароль SMTP (`bugs/03` — владелец отозвал),
  299 почт (`bugs/01`), ключ подписи (`bugs/02`, риск нулевой). Секреты вынесены в `.private/` вне git;
  старый публичный репозиторий приватизирован владельцем как `ndim-old` (0 форков) — экспозиция закрыта.
- ✅ **Лицензия AGPL-3.0** — файл `LICENSE` и поле в `package.json`.

### Фаза 1.5 — Санация наследия ✅

Закрыта 2026-07-10 вместе с `homeworks/02`. Публичной утечки больше нет.

### Фаза 1 — Решения владельца ✅

Интервью №001 закрыто 2026-07-10. Решения:
**SvelteKit + TypeScript** · **купить домен + Firebase Hosting** · **Docker на домашнем ПК** ·
**AGPL-3.0** · **тот же репозиторий, история обнуляется**.

---

## Где мы сейчас

Версия 1.x (весь фронтенд в одном `index.html` на 3083 строки плюс `app.js` на 9127 строк, Firebase, и
«бэкенд», роль которого исполнял телефон владельца с открытым Chrome) осталась в приватном архиве
`ndim-old`. Здесь — чистый лист.

**Написано ядро и механизм приватности.** Математическое ядро — `src/lib/similarity/` (29 тестов,
паритет с 1.x, декодер полей `a…t`). Логика видимости профиля — `src/lib/model/visibility.ts`
(30 тестов). Оба набора проверены **мутациями**: на сломанном коде краснеют.

Зависимостей у кода нет: **Node 24 исполняет TypeScript нативно**, тесты — встроенный `node:test`.
`tsc` и `@types/node` нужны только для `npm run typecheck`.

Модель данных 2.0 спроектирована (`researches/04`), интервью №002 закрыто. Профиль разбит на
документы-бакеты по кругам владельца — это **вынужденное** следствие двух фактов: Firestore не умеет
скрывать поля при чтении, а бэкенд без входящих портов не может отфильтровать их на лету.

**Фронтенд стартовал.** Лендинг (`src/routes/+page.svelte`) реализован по утверждённому макету
«Колонна»; 2026-07-11 владелец утвердил **светлую тему «Бумага» (дефолт)** + тёмный киберпанк +
переключатель **RU/EN** (клиентский свап, RU пререндерится). Всё это покрыто e2e-стендом:
`npm run e2e` — Playwright, 10 проверок на продакшен-сборке, включая пререндер и персистентность
темы/языка. Стенд проверен мутацией: на сломанном дефолте темы честно краснеет.

| Фаза | Статус | Что там |
|------|--------|---------|
| Фаза 0 — Фундамент | ✅ | наследие, KAIF, аудит секретов |
| Фаза 1 — Решения владельца | ✅ | интервью №001 закрыто |
| Фаза 1.5 — Санация наследия | ✅ | секреты изъяты, экспозиция закрыта, AGPL-3.0 |
| Фаза 2 — Ядро 2.0 | ✅ по сути | ядро (29 тестов) + декодер `a…t` + модель данных (`researches/04`) + видимость (30) + схема (29) + правила (56) + план миграции (`plans/02`) |
| Фаза 3 — Фронтенд 2.0 | ✅ **базовые экраны готовы** | ✅ лендинг · ✅ **«Профиль»** (3 вкладки, звёзды, правка личного, аудитории, заявки на измерения) · ✅ **«Связи» по S4** (тройка метрик, «яркость связи», общее пространство) · ✅ **онбординг**: демо → гость → аккаунт без пароля → воронка · ✅ **«Пространство»** (V1, приборная панель из виджетов) · ✅ **«Меню»** (V1 + манифест + 8 страниц: руководство, условия, политика, отказ, поддержка, пожертвование, о системе, об авторе). Дальше — социальный слой (⛔ только после `ideas/12`) |
| Фаза 4 — Бэкенд | 🔧 **в работе** | ✅ **сервер синхронизации в Docker локально**: dirty-цикл, топ-250 в `relations/`, ядро без изменений, ✅ **статистика Пространства и сердцебиение** (`space/*`), проверен на стенде и из контейнера. Осталось: боевой запуск на домашнем ПК владельца — только вместе с миграцией данных 2.0 |
| Фаза 5 — Публикация | 🔲 | домен, индексация, запуск |

Подробнее — `MASTER_PLAN.md`.

---

## 🤖 Пул автономных задач (без человека и без особого железа)

> Задачи, которые агент может делать ПОЛНОСТЬЮ автономно: написать код → собрать → протестировать на
> стенде → починить → закоммитить. Цикловые скиллы (`/autoloop`, `/dayloop`, `/nightloop`) мелют этот пул.

- [x] ~~① Извлечь математическое ядро в тестируемый TS-модуль~~ ✅ `src/lib/similarity/similarity.ts`
- [x] ~~② Юнит-тесты математического ядра~~ ✅ 29 тестов, `npm test`; мутационно проверены
- [x] ~~③ Новая модель данных Firestore~~ ✅ `researches/04` + интервью №002 закрыто.
      Профиль разбит на бакеты по кругам; логика видимости реализована и покрыта тестами:
      `src/lib/model/visibility.ts` (30 тестов, мутационно проверены четыре сценария утечки).
- [x] ~~④ Типы документов Firestore~~ ✅ `src/lib/model/schema.ts` (29 тестов, 6 мутаций).
      Границы значений — паритет с 1.x; логика дружбы — источник истины для правил.
- [x] ~~⑤ Правила безопасности~~ ✅ `firestore.rules`
- [x] ~~⑥ Тесты правил на эмуляторе~~ ✅ `tests/rules/rules.test.ts` — 56 тестов, `npm run test:rules`.
      Проверяют **отказы**; восемь мутаций правил ловятся (снятие умолчания «запрещено» — 39 падений).
- [x] ~~⑦ План уважительной миграции~~ ✅ `plans/02_migration.md`. Скрипт не пишем сейчас: мигрировать
      некуда, пока нет фронтенда. Чистые функции трансформации можно написать в любой момент.
- [x] ~~⑧ Инициализировать SvelteKit + первый экран~~ ✅ Лендинг реализован по утверждённому макету
      (3 раунда правок, итог: «Колонна» + синий киберпанк-неон, mobile-first, без градиентных кнопок).
      Пререндер в статику: тексты владельца лежат в `build/index.html` — индексация решена стеком.
      Кнопки пока ведут в живое приложение 1.x (`ndim-space.web.app`).
- [~] **⑨ Деплой лендинга** — preview-канал задеплоен 2026-07-11 (см. раздел «Ожидает решения
      человека»). **Боевой** деплой (`firebase deploy --only hosting`) — только по явному решению
      владельца: он заменит живой сайт 1.x на лендинг, а кнопки лендинга сейчас ведут на этот же
      адрес (`ndim-space.web.app`) — получится кольцо. Сначала решить, куда переедет 1.x.
- [x] ~~⑩ Playwright e2e~~ ✅ `npm run e2e` — 10 проверок (mobile 430px + desktop 1440px):
      пререндер текстов в сыром HTML, дефолт светлая/RU, переключение и персистентность темы и языка,
      кнопки в живое приложение, чистая консоль. Стенд проверен мутацией (дефолт light→dark → 2
      падения). Конфиг: `playwright.config.ts` — собирает **продакшен-артефакт** и гоняет по нему.
- [x] ~~⑤ Исследование SEO и индексации~~ ✅ `researches/08_seo_indexing.md` — по официальным
      источникам, с чеклистом фазы 5 (15 пунктов, метки [сейчас]/[домен]/[2.0]). Ключевое: EN как
      клиентский свап для поисковиков не существует (нужен `/en/` + hreflang); 301 с web.app
      конфигом невозможен — дубли гасит абсолютный canonical.
- [x] ~~⑥ Извлечь продуктовые тексты (ru/en)~~ ✅ `researches/06_product_texts_1x.md` (about =
      история версий, tou, pp, disclaimer) + `researches/07_user_manual_1x.md` (руководство:
      манифест, идея, 14 терминов, механики). Вместе с `researches/05` (онбординг) — все тексты
      владельца извлечены дословно; ПДн в них нет.
- [x] ~~⑦ PageSpeed Insights~~ ✅ Lighthouse локально по живому домену (PSI API отдаёт 429 на
      анонимной квоте; обход — `npx lighthouse` с Chromium от Playwright): Performance 100,
      SEO 100, LCP 1.0 s, CLS 0 — чинить нечего.
- [x] ~~⑧ Каркас `sitemap.xml`~~ ✅ `src/routes/sitemap.xml/+server.ts` (домен — константа
      `src/lib/site.ts`, её же использует canonical) + `Sitemap:` в robots.txt + `static/404.html`.
      e2e: 22 проверки, мутация ловится. На живой домен выкатится с боевым деплоем (слово владельца).
---

## ❓ Ожидает решения человека

- 🔑 **К фазе 5 (публикация): попросить владельца проверить вход через Google руками.**
  Договорённость 2026-07-12: агент включает провайдеров на проде → **просит владельца** войти
  через Google живьём и доложить результат. Автотестом это не закрыть — эмулятор Auth открывает
  интерактивное окно. Почтовая ссылка проверена автоматически и в проверке руками не нуждается.

- ✅ **Все три набора макетов УТВЕРЖДЕНЫ владельцем 2026-07-12**:
  - `design/login-mockups.html` → **V4 «Врезка»** (✅ реализован).
  - `design/space-mockups.html` → **V1 «Приборная панель»** + виджеты «Сегодня», «Сейчас в
    Пространстве», «Версии» (✅ **реализован** 2026-07-12; смотреть: `npm run stand` → `/space`).
  - `design/menu-mockups.html` → **V1 «Список»** + блок манифеста (**реализовать** — следующая задача).

- 💬 **Мелочь на слово владельцу:** как называется состояние сервера синхронизации, когда он молчит?
  Агент выбрал **«Не отвечает»** (макет этот случай не покрывал; «остановлен» — враньё, мы этого не
  знаем). Меняется одной строкой в `src/routes/space/+page.svelte`.

- 👀 **Посмотреть живой экран профиля**: `npm run stand` → http://localhost:5173/profile
  (вход на стенд автоматический, пользователь `dev@ndim.space`). Реализован утверждённый синтез
  S1–S3: три вкладки, лента измерений с барами и раскрытием, звёзды пишут оценку в эмулятор
  через правила безопасности, «Видимость» — предпросмотр глазами гостя. Правки — говори.
- 🧭 S4 «Связи» — превью в `design/profile-synthesis-mockups.html`, утверждается отдельно,
  когда дойдём до раздела Связи.
- 🌐 **Лендинг на preview-канале:** **https://ndim-space--landing-3jvzs4cd.web.app**
  (живёт до 2026-08-10). Правки — говори, внесу. Боевой деплой решён интервью №003 (В3 = A):
  пока не деплоим, вернуться к вопросу с первым экраном 2.0 или с покупкой домена.

### Решено 2026-07-11 (ночь, после деплоя демо)

- ✅ **Демо похожести задеплоено на прод** — владелец явно подтвердил боевую цель
  (`ndimspace.app`, проект `ndim-space`). Проверено на живом домене: демо пререндерено,
  `/img/personas/*_face.png` отдаются, sitemap/robots/404 целы.
- ✅ **Домашка 04 закрыта целиком** (`homeworks/04_DONE_...`): sitemap подан в GSC и
  Яндекс.Вебмастере, Bing-импорт завершён. Индексацию дальше отслеживает агент.
- ✅ **Приоритизация владельца**: следующим — **гостевой режим** (`plans/03`, этап 2);
  идеи 06–08 остаются в беклоге после него (владелец выбрал только гостевой режим).

### Решено 2026-07-11

- ✅ **Лого утверждено: V3 «Диагональ»** — классическая N-сеть 1.x, градиент синий→циан вдоль
  диагонали, тёмная плашка. Внедрено в `static/favicon.svg`; варианты — `design/logo-mockups.html`.
- ✅ **Интервью №003 закрыто**: первый экран — **профиль с измерениями**; данные — **модель 2.0 +
  эмулятор**; боевой деплой лендинга — **отложен** (1.x не трогаем).
- ✅ **Референс-коллекция живого 1.x собрана** — `design/reference-1x/`, 22 кадра: все разделы
  и подэкраны, публичные и залогиненные (владелец дал доступ). PNG вне git (ПДн настоящих людей),
  в git — README с инвентарём и методикой пополнения.
- ✅ **`.private/` скопирован владельцем на внешний носитель и удалён с рабочего диска** —
  это закрывает шаг 3 `homeworks/02` (резервная копия вне рабочего диска). История 1.x доступна
  в приватном `ndim-old` и в копии владельца; предупреждение `kaif:check` об отсутствии
  `.private/ndim-1.x-history.bundle` — ожидаемое и корректное.
- 🧰 **Уведомлять ли 299 участников беты** о том, что их адреса были публичны? Владелец ответил
  «подумаю» (`homeworks/02_DONE_...`, шаг 4). Если решит уведомить — агент поможет составить письмо.
- 🔑 ~~Перелогиниться в Google~~ — firebase работает (проверено 2026-07-11 деплоем канала).
  `gcloud` перелогинить, только если понадобится (фазы 2–3 в нём не нуждаются).

---

## Где продолжить в следующей сессии

> Конкретный чеклист, чтобы следующая сессия (пустой контекст) могла стартовать немедленно.

1. ~~Деплой демо~~ ✅ сделано 2026-07-11 ночью (владелец подтвердил явно; урок: классификатору
   авто-режима мало голого «деплой» — нужна названная боевая цель, см. EXPERIENCE).
2. ~~Этап 2 плана 03 — гостевой режим~~ ✅ закрыт на стенде.
   ~~Десктопная раскладка~~ ✅ V2 «Рабочий стол» утверждена и реализована 2026-07-11.
2а. ~~Этапы 3 и 4 плана 03 (аккаунт без пароля, воронка)~~ ✅ закрыты.
   ~~Экран «Пространство»~~ ✅ реализован 2026-07-12 (V1 «Приборная панель»).
2б. ~~Экран «Меню»~~ ✅ реализован 2026-07-12 (V1 «Список» + манифест + 8 страниц).
2в. ~~**БОЕВОЙ ЗАПУСК** (`plans/04_cutover.md`)~~ ✅ **СОСТОЯЛСЯ 2026-07-12, 16:05 UTC.**
   2.0 в бою на `ndimspace.app` и `ndim-space.web.app`; 1.x мёртв. Подробности — в шапке этого файла.
2г. **ТЕКУЩЕЕ — хвосты запуска (по убыванию важности):**
   - ⏳ **шаг 7 cutover: владелец проверяет вход руками** (Google · старая почта · гость). Пока не
     подтверждён — `plans/04` не помечается `DONE`, а откат держим наготове.
   - 👀 **последить за живым продуктом первые дни**: логи `docker logs ndim-calculator`, `/space`
     («Работает»), жалобы людей. 109 из 331 не подтверждали почту — правила 2.0 без подтверждения
     ничего не показывают; чинится само при входе через Google или по ссылке из письма.
   - 🆕 `ideas/13` — **инсталлятор сервера синхронизации + Admin WebUI** (идея владельца, сказана
     на выкате). Панель **поглощает** админку из `ideas/12`, а не дублирует её. Ждёт приоритизации.
   - 15 измерений, по которым стоят оценки, удалены из каталога 1.x — восстановить или оставить?
3. ~~SEO: сертификат www~~ ✅ **доехал** (проверено 2026-07-11: `https://www.ndimspace.app` отвечает
   по HTTPS без ошибок TLS и отдаёт 301 на apex; apex → 200). Домашка 04 закрыта целиком — дальше
   следить за индексацией по мере появления новых страниц. DNS проверять только через DoH —
   порт 53 наружу закрыт. `gcloud` разлогинен (токена нет) — если понадобится Hosting API,
   владельцу нужно перелогиниться; для проверки домена хватило обычного curl.
4. **Идеи 06–08 ждут приоритизации владельцем**: 06 статистика+версии (дёшево начать с
   `space/server` и vite define) · 07 автобилды из git + GitHub Actions деплой лендинга
   (проверить ключ SA `github-action-855638138@ndim-space`) · 08 ИИ-команда продвижения
   (большая; приоритет логичен после входа/онбординга). Мелочь-развилка владельцу: показывать
   ли гостю «был онлайн» (в 2.0 это новое публичное поле — решение владельца).
5. Регресс: `npm test` (**111**), `npm run test:rules` (**85**), `npm run test:calc` (**15**),
   `npm run e2e` (**57**), `npm run typecheck`, `npm run check`, `npm run kaif:check`.
   ⚠️ `npm run e2e` пересобирает `build/` — после мутационных прогонов пересобери начисто,
   прежде чем деплоить артефакт (EXP-0023).
   ⚠️ Перед общим прогоном e2e **гаси стенд** (порты 8181/9099/5173): часть проверок ждёт
   состояния «эмуляторов нет» и при живом стенде даёт ложные падения (EXP-0027).
   Живой стенд: `npm run stand` → `/profile`, `/relations`, `/space`. Сид прогоняется сам, а
   **сервер синхронизации теперь работает службой** (цикл 15 с): оценка из браузера сама долетает
   до связей и статистики, а «Пространство» показывает «Работает», а не «Не отвечает».
   ⚠️ На публичном хосте экраны показывают прод-заглушку — полная функциональность видна только
   на localhost (это осознанно, до миграции данных 2.0).
5. Запрос владельца (не срочно): проверить доступ в Firebase Console через дебаг-Chrome
   (отдельный `--user-data-dir` + его логин в Google). CLI-доступ уже есть: firebase/gcloud.
6. `npm run test:rules` и `npm run stand` требуют Java: если не находится — добавь
   `C:\Program Files\Microsoft\jdk-21.0.11.10-hotspot\bin` в `$env:PATH` на время команды.
   После остановки стенда добивай эмуляторы на портах 8181/9099 (переживают обёртку, EXP-0021).
7. Перед правкой кода — `PHILOSOPHY.md` + греп `EXPERIENCE.md` по тегам. Новым тестам не верить
   без мутации (e2e профиля проверен: «убрать noindex» роняет ровно пререндер-тест).
8. **Социальный слой 2.0** (`ideas/02`–`04`: лента, комментарии+лайки, чаты) и **первая
   синхронизация новичка** (`ideas/05`) — после базовых экранов; каждый экран — интервью +
   4 макета. НЕ брать в автономные циклы.
   ⛔ **Комментарии и чаты не выпускаются без `ideas/12`** (личная ответственность автора
   контента: фильтр + панель админа + правовые тексты). Требование владельца 2026-07-12 —
   это про его личную безопасность.

---

## Открытые баги

- 🔧 **`bugs/08_krinik_tests.md`** — вторая волна ручного теста владельца, 8 дефектов.
  Пачка 1 (лендинг-редирект, поздравление, мобильная оболочка) починена 2026-07-16;
  открыты пачка 2 (подгрузка лент, фото фуллскрин, размер карточек) и пачка 3
  (онбординг-флоу и иконки — через макеты владельцу).
- 🔴 **`bugs/05_no_motion_in_app_screens.md`** — внутренним экранам не хватает анимаций, всё
  резкое (боль владельца 2026-07-12: «топорно»; Профиль/Связи/Пространство/Меню — ноль переходов).
- 🔴 **`bugs/06_dims_not_dashboard_style.md`** — «Измерения» не в стиле приборной панели, как
  другие вкладки (десктопный канон V2 «Рабочий стол» на экран не применён вовсе).
- 🔴 **`bugs/07_landing_fake_people_counter.md`** — лендинг врёт «С нами уже 2 184 человека»
  (литерал в коде; в базе 331; в 1.x счётчик был живым из `space/public_metrics`).
- 🔧 **`bugs/04_storage_rules_still_1x.md`** — правила Firebase Storage в боевом проекте всё ещё
  от версии 1.x: секции `storage` в `firebase.json` нет вовсе, права админа зашиты **списком
  email** прямо в тексте правил (в 2.0 админ — это claim в токене). Не дыра, а долг: данные не
  утекают, но права живут не там, где должны, и не покрыты тестами. Нашли, когда искали пропавшие
  фотографии (`EXP-0043`).

Три бага безопасности закрыты 2026-07-10:

- ✅ `bugs/01_DONE_pii_email_lists_in_repo.md` — 299 почт участников беты. Изъяты из репозитория;
  старый репозиторий приватизирован (`ndim-old`, 0 форков) → публичной экспозиции нет.
- ✅ `bugs/02_DONE_android_keystore_in_repo.md` — ключ подписи. Изъят; риск оценён как **нулевой**
  (в Play не публиковалось, пароля нет).
- ✅ `bugs/03_DONE_smtp_credential_in_repo.md` — живой пароль SMTP в публичном репозитории.
  Владелец отозвал пароль, литерал изъят из кода. Это была единственная настоящая утечка.

Известные дефекты версии 1.x (чинить не надо — учесть при проектировании 2.0; подробнее в
`researches/02` → «Что в этой модели плохо»):
- Математическое ядро возвращает обфусцированные поля `a…t` — нечитаемо и непроверяемо.
  Устраняется задачей ① автономного пула.
- Бэкенда нет: связи считаются в браузере админа на телефоне (O(N²) в UI-потоке).
- Права админа зашиты списком email прямо в `firestore.rules` (~25 повторений).
- `owner_email` гостя попадает в чужой документ `relations` — лишние ПДн.

