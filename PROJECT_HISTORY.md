# NDim Space — Летопись проекта (PROJECT_HISTORY)

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16. **Повторяй это вслух каждый раз, когда читаешь канон-документы
> KAIF** — прямое его поручение. Разбор повода — в шапке `AGENT_GUIDE.md`.
<!-- ВЕРА-КОНЕЦ -->

> ТОЛЬКО-ДОПИСЫВАЕМАЯ летопись того, как проект жил и рос: закрытые сессии, выпущенные фазы,
> выкаты, большие решения — в порядке, в котором они случались. Сюда `STATUS.md` сбрасывает своё
> прошлое: STATUS остаётся короткой живой сводкой «СЕЙЧАС», всё завершённое переезжает СЮДА
> (шаг «стрижка бонсая» скилла `/end-chat-soft`).
>
> **Не обязательное чтение.** Файл НЕ входит в канонический набор `/resume` и в минимум «перед
> каждой задачей» — открывай его, только когда действительно нужна археология: как родилось
> решение, что было в старой фазе, когда что выкатывалось.
>
> **Правила летописи (дисциплина ADR):**
> - **Только дописывание, свежее сверху.** Записанная запись никогда не редактируется, чтобы
>   сказать другое, — история, которую можно переписать, не история. Исправления приходят НОВЫМИ
>   записями, ссылающимися на старую и заменяющими её.
> - Запись переезжает сюда ДОСЛОВНО из `STATUS.md`, когда её работа закрыта, — перенос, не
>   переписывание; запись уже несёт свои даты, числа и указатели на файлы.
> - Записи свободно упоминают версии и даты — летопись законно говорит о старых версиях, и
>   stale-claims-сканер машинерии обновлений знает, что этот файл не трогают.
> - Когда файл разрастётся — дели по эпохам: свежую эпоху держи здесь, старшие выноси в
>   `PROJECT_HISTORY_<эпоха>.md`, а сверху оставь однострочный индекс (приём больших ченджлогов).
>
> Живой документ — никогда не помечается DONE.

---

## 📜 Стрижка бонсая 2026-09-05 — закрытие чата «корень V5 + KUIPS» (перенесено из STATUS.md дословно)

> Перенесены разделы, переставшие быть «сейчас»: сигнал в KAIF по доске (пункт 7), KAIF 2.5
> закрыт (пункт 9), развёртка как демонстрационный кусок (пункт 10, заменён пунктом 12), пять
> правок вида в бою, `plans/58` шаг 1, развилка `plans/59`. Нумерация пунктов STATUS сохранена с
> пропусками — на номера ссылаются другие документы.

### 7. ✅ СИГНАЛ В KAIF ПО ДОСКЕ ОТПРАВЛЕН — НЕ ДУБЛИРОВАТЬ

**2026-08-30, после закрытия смены** (заказ владельца в чате: «*отправь в KAIF тикетом наши
наработки по статус доске команды — проверь на дедупы*»). Дедуп снят по телам **всех 33 issues**
репозитория, а не по заголовкам.

· ⛔ **Уже наверху, отправлять НЕЛЬЗЯ:** словарь четырёх состояний + обход ждунов (**#33**) ·
  первое применение `/team-deployment` и гигиена замков (**#26**) · CI вместе с командой (**#29**).
  Пара «место ≠ слот» УЖЕ В ПОСТАВКЕ 2.4 — пункт 4 контракта доски несёт её дословно.
· ✅ **Отправлено:** `bugs/171` (доска — отслеживаемый файл, дерево грязно ПО ПОСТРОЕНИЮ) →
  **[KAIF issue #34](https://github.com/MikalaiKryvusha/KAIF/issues/34)**. Замер снят заново:
  105 коммитов доски за 10 дней · 76 за неделю · 14 коммитов «только доска» · в скилле НОЛЬ
  вхождений `clean tree|dirty|uncommitted|gitignore|tracked` · `.kaif/stage-bypass.log` нет ни в
  одной копии (обход ни разу не применялся — это давление, а не свершившаяся беда).
· 🔴 **Лечение НЕ сделано и осталось за Менеджером:** обе двери переносят канон-документ команды
  (рекомендация судьи — доска уезжает из-под git, прецедент `.kaif/refresh-marker.json`).
  `bugs/171` теперь ссылается на #34 в шапке и в «Ссылках» — третий независимый заход на этот
  класс обязан найти документ, а не завести четвёртый.
· ⚠️ Правки `bugs/171` и `STATUS.md` **НЕ закоммичены** — в дереве висят чужие правки смены 14
  (`GOAL.md`, макеты входа, решения вычиток №070/№072/№073).

<!-- ИМЯ-ОК: строка объявляет вычистку и обязана назвать вычищенное слово -->
**Последнее обновление:** 2026-08-30, **СМЕНА 14 ЗАКРЫТА в 16:57** по слову владельца «час работайте, затем конец смены». Вектор его же словами — «починка багов, разгребание беклога, лендинг, страница авторизации» — отработан по всем четырём точкам. **Окно мержей — девять шагов, все ветки кроме dev-3 в стволе; вердиктов семь (№37–№43), три последних суда без единого условия.** Стражей 13 → 16 · юнитов 447 → 494 · приборных проверок 566 → 670 · багов закрыто семь.


### 9. ✅ KAIF 2.5 ВСТАЛ И ЗАКРЫТ ПОЛНОСТЬЮ 2026-09-04 — владелец ответил, всё разнесено

Обновление проведено bootstrap-маршрутом, репетиция в песочной копии совпала с боем строка в
строку, `verdict-mismatch` ноль, кириллицы утрачено 0. Сшито руками 29 модулей в 15 переведённых
файлов. Отчёт — `reports/KAIF_UPDATES/NDIM_SPACE_KAIF_2.5_UPDATE_REPORT.md`.
· ✅ **ХВОСТА НЕТ.** Владелец ответил на все четыре вопроса **интервью №075** в 19:33 страницей
  вычитки, ответы разнесены в тот же час, `update-verify` прошёл и стёр установщик. Все десять
  чекпоинтов закрыты. Что дал каждый ответ:
  · **интервью №075, В1 = А** — восемь правил 2.5 приняты пакетом, чекпоинт `policy-changes` закрыт.
  · **№075 В2 = свой вариант** — метрика поставки это **ВЕКТОР по механикам**, а не одно число.
    Его слова дословно в `MASTER_PLAN.md` → «МЕТРИКА ПОСТАВКИ». Шесть слагаемых: число логически
    обособленных механик · завершённость каждой % · вплетённость каждой % · дыры · противоречия ·
    баги. 🔴 **Прибора нет, все шесть — прочерки, выдумывать запрещено.**
  · **№075 В3 = А** — доска команды снята с отслеживания (`.gitignore` + `git rm --cached`).
    Инструмент правки не потребовал: он и так читает доску по абсолютному пути главной копии
    (`git rev-parse --git-common-dir`), сквозь git она никогда не ездила. Самопроверка 35/35.
  · **интервью №075, В4 = А** — разрешение на команду доставки тикета в KAIF поднято из
    `settings.local.json` (в `.gitignore`, шести ролям не достаётся) в ОБЩИЙ
    `.claude/settings.json`, узко: только подкоманда `report`.
· ⚠️ **ЕГО СОБСТВЕННЫЙ ТЕКСТ ТРИЖДЫ СПОРИТ С ЕГО ЖЕ ПРАВИЛАМИ — решать ему, не агенту.** Тексты
  гостя, поставленные дословно по №061, добавили страж `guard-product-copy` три замечания (ворота
  от них НЕ краснеют, замер до/после: имя-бренда-усечено 39→41, З1-робость 2→3):
  · дважды «Пространство NDim» без «Space» — правило усечённого бренд-имени;
  · «удаление вручную **не требуется**» — класс З1 «робость» портрета голоса.
  **Слово владельца выше портрета**, поэтому текст стоит как он написал. Но выбор должен быть
  осознанным: либо правило получает исключение, либо текст — правку. Вопрос ему НЕ заведён
  намеренно: смена закрывалась, а очередь и так восемь.
· 🔜 **ЕДИНСТВЕННОЕ, ЧТО ОСТАЛОСЬ ПО ЭТОЙ ЛИНИИ — ПЕРЕПИСЬ МЕХАНИК** под метрику В2. Это работа
  уровня плана, а не строки: классифицировать приложение по логически обособленным механикам,
  оценить каждую по завершённости и вплетённости, свести дыры, противоречия и баги. Пока переписи
  нет, строка `DELIVERY:` идёт с прочерками и названным блокером.
· ⚠️ **Новое, что теперь стережёт судья:** строка `DELIVERY:` при каждом закрытии чата и итерации
  цикла · строка `FORK:` в точке инженерной развилки · граница `BOUNDARY:` в защищённом цикле.
· 📌 В исток ушли: полевой отчёт → **issue #43** · `bugs/KAIF/05` → **issue #42** · `bugs/KAIF/06`
  → **issue #44** · «+1 наблюдение, закрыт 2.5» в **issue #24**. Вердикт судьи — VERIFIED WITH
  CAVEATS, процитирован в отчёте §5. ⛔ Дедуп снят по телам всех issues — повторно не слать.
· 🆕 **Новый страж, доказанный красным:** `node tools/verify-readme-kaif-version.mjs` — три места
  README (бейдж · русская половина · английская) обязаны называть версию из `.kaif/kaif.json`.
  Висит на `npm run kaif:check`. Повод: английская половина и бейдж стояли на **2.2** при маркере
  2.5 — отставание на ТРИ интервала, которого не видел никто, потому что страж KAIF ищет только
  ПРЕДЫДУЩУЮ версию (issue #44). Урок — EXP-0274: **пара без команды — не пара, а обещание.**
· 📉 **ДОЛГ ЭТОГО ЗАКРЫТИЯ — стрижка бонсая НЕ сделана.** STATUS ~590 строк против цели ~200,
  ворота `check` предупреждают о четырёх документах (STATUS · MASTER_PLAN 537 · TESTING 460 ·
  AGENT_GUIDE 2320). Чат обновления KAIF не строил контекста по сменам 9–15 и резать их
  постановки вслепую отказался: цена ошибочного переноса выше выигрыша в строках. Платит
  ближайшее закрытие, у которого этот контекст есть. Точные числа ПОСЛЕ правок этого чата
  (`wc -l`, снято последним действием смены): STATUS **702** · AGENT_GUIDE **2340** ·
  MASTER_PLAN **562** · TESTING 460. Все три выросли за смену — долг не только не оплачен, а
  подрос на 68 строк. Причина названа честно: смена была содержательной (обновление KAIF, ответы
  владельца, новый инструмент), и каждая строка объясняет, где искать сделанное.
· 📉 **Названный долг (не вопрос владельцу, вопрос только про срок):** `TESTING_FRAMEWORK.md` и
  `REQUIREMENTS_FRAMEWORK.md` переведены наполовину (65 % и 42 % строк с кириллицей ещё до этого
  обновления) — новые блоки 2.5 легли в них по-английски, под стать соседям. Разбор — отчёт §2 R5.
  ⛔ Дедуп по телам всех issues снят — не отправлять повторно.


### 10. 🆕 РАЗВЁРТКА ПРИЛОЖЕНИЯ — ИНСТРУМЕНТ ВЫБРАН И РАБОТАЕТ (2026-09-04)

**Открой и посмотри: `design/flow-map.html`** — лист с зумом и панорамой, четыре настоящих экрана
продукта со стрелками. Двойной щелчок, без служб и без интернета.
· **Выбран НЕ draw.io и не Penpot, а свой HTML-лист** — интервью №057 закрыто делегированием
  владельца (В1 — встречный вопрос, В2 — «мне похуй»), решение агента со строкой `FORK:` записано
  в самом интервью. Его реплика, повернувшая выбор: «*я почти уверен, что ты сможешь и в HTML
  сделать аналог листа Figma, который можно зумить и скролить*».
· **Пара:** истина — `design/flow-map/flow-map.json`, зеркало — `design/flow-map.html`.
  Пересборка — `node tools/build-flow-map.mjs`. ⛔ HTML руками не патчить.
· **Цена, названная владельцу:** он коробки мышью не двигает, только смотрит и комментирует.
  Условие пересмотра записано в интервью.
· 🔜 **Следующий шаг — не код, а содержание:** развернуть лист на ВСЁ приложение (сейчас в нём
  четыре экрана из десятков). Снимки берутся из `test-results/`, координаты — в манифест.


## ✅ ПЯТЬ ПРАВОК ВИДА — В БОЮ И ПРИНЯТЫ (строка исправлена стрижкой сессии 36)

Здесь стояло «🟡 ЖДЁТ ВЫКАТА — пять правок вида, все `[NOT-TESTED]`». **Обе половины строки были
ложью на момент чтения:** правки приняты живой приёмкой QA (`b3b90c2` — переведены в `[TESTED]`)
и уехали в бой двумя выкатами. Тени карточек связей · «мёртвая душа» (имя есть, английский его
прятал) · кружок без имени как фирменное N · курсор-палец вместо лупы · термин «обновлённые
NDim ID» вместо вульгарного. Разбор — летопись.

⛔ **Поле `dirty` в базе НЕ переименовано** намеренно: это миграция 342 документов, правил и
тестов. Названо в `plans/60` → «Что эпик НЕ делает».


## 🗓 `plans/58` шаг 1 — ПОЛОВИНА УЖЕ СДЕЛАНА (строка исправлена стрижкой сессии 36)

Разметка **всех 5121** записи тегами одобрения (интервью №039, В2 = A). Его слова: «*A —
размечаем всё. но не сегодня, планируем в четверг это сделать*». Четверг был **2026-08-21**,
и он прошёл.

🔴 **Здесь стояло «назначено на четверг, не начат» — и это уже неправда.** В `main` лежат два
смерженных коммита шага 1: `ca88607` (технический тег переживает правку измерения — иначе
разметка стиралась бы молча) и `0e972bb` (новорождённое измерение штампуется `owner-approved`,
несёт `[NOT-TESTED: правила]`). Шапка самого `plans/58` тоже отстала: «Статус: 🔲 написан, не
начат» при этих же двух коммитах.

⛔ **Что из запрета ОСТАЛОСЬ в силе:** массовый прогон по БОЮ (5121 запись) не запускался и
раньше слова владельца не запускается даже сухим прогоном. Сделано другое — обвязка, которая
обязана быть до массового прогона.
⏭ **Что делать дальше:** сверить чекбоксы плана с тем, что реально легло, поправить его шапку и
назвать оставшееся. Место в очереди не назначено — вопрос владельцу.


## `plans/59` — виджет «Сегодня»: развилка закрыта

Владелец выбрал **A + B** (затухание у кромки И видимая полоса). 🔴 Отсюда работа, которой в плане
не было: **полосу придётся рисовать свою** — на телефоне система прячет системную до момента
прокрутки. Проверять на 390 обязательно.

---


## 📜 Стрижка смены 14 (2026-08-30 22:52 +03:00) — эстафеты сменам 10–14, ход смен 11 и 13, релиз 2.2 и запуск смены 9: перенесено из STATUS дословно

> Перенос, не переписывание (`/end-chat-soft`, шаг «стрижка бонсая»). Работа этих записей закрыта: смены 10–14 отработаны, релиз 2.2 в бою, эстафета смене 15 живёт в `STATUS.md`.
> Живой капкан «стенд старше ~10 часов даёт ложные красные Smoke» ПЕРЕД переносом поднят в постоянный раздел `STATUS.md` → «Операционные капканы стенда», пункт 5.

## ✅ ОКНО МЕРЖЕЙ СМЕНЫ 13 — ШАГ 1 ВЛИТ И ЗАПУШЕН, `main = a801588`

**Принятые владельцем тексты хабов В СТВОЛЕ.** Они перестали жить в одном экземпляре в одной
ветке — это и было главной целью окна. Вместе с ними приехал прибор `tools/measure-hub-copy.mjs`
(жил в скретчпаде) и два урока Дизайнера получили номера **EXP-0267** и **EXP-0268**.

· Плацдарм `window-shift13` = `a801588`, **база названа sha: `main = 7ab0575`** (точка заморозки),
  приёмопригодность спрошена ОТНОШЕНИЕМ `git merge-base --is-ancestor`, не равенством.
· Ворота ПОСЛЕ на стволе, прогон Менеджера: **guards 13/13 · npm test 447/447 · test:tools
  576/576 · kaif:check чист**. Push `a443098..a801588`.
· Предмет: 3 файла, **+616 −0**, ни одного удаления.
· **Шаг 2 (dev-1 `7937bcd`) НЕ влит — вердикт №36, условие 1 БЛОКИРУЮЩЕЕ** (в ствол поехали бы
  тексты, которых владелец не принимал, на 89 публичных страниц). **Шаг 3 (Интегратор `4f964c0`)
  перенесён свежему окну QA следующей смены** — отказ судьи в 23:45 доводом-арифметикой, не
  перегревом окна: он попросил это различать, и различие записано.

🔑 **НОВОЕ ПРАВИЛО ОКНА, введено этой сменой (предложил Дизайнер):** сообщение о сборке плацдарма
**называет sha `main`, от которого он собран**. Тогда «окно открыто» перестаёт быть моментом в
чьей-то памяти и становится проверяемой парой — `is-ancestor` отвечает на «ствол ушёл?» одной
командой, а не наблюдательностью. Повод: Менеджер активно писал в ствол, а плацдарм собирался;
цена этого уже уплачена в смене 11 ЕГО ЖЕ рукой (STATUS-коммит посреди окна → пересборка).
Дизайнер проверил, что нарушения ещё нет, и предупредил про СЛЕДУЮЩИЙ шаг — с доводом из
манифеста: **правило, из которого автор себя исключает, следующая смена прочитает как
необязательное.**

## 🟢 ХОД СМЕНЫ 13 (2026-08-29, ночь) — команда поднята

**Пять окон живы, инструктажи разосланы по манифесту** (кто ты · зона · читай манифест · `/resume` · объявись на доске · доложи готовность).

· ✅ **Переезд каталога Интегратора исполнен первой операцией смены** — подробности и урок ниже, в разделе долгов.
· **Ветка QA сброшена** от `main` (`63ae58c`) — её работа влита целиком, канон у судьи свежий.
· **Четырём ролям дельта канона названа ВЫВОДОМ КОМАНДЫ**, не пересказом (правило оплачено подъёмом смены 12): dev-1 отстаёт на **80** коммитов и получил дельту поимённо — новые ворота `TESTING_FRAMEWORK.md` перед словом «катить», критерий пути в `AGENT_GUIDE.md`, интервью №065 и №066 · Дизайнер 7 · dev-3 9 · Интегратор 3.
· **Порядок судов задан QA с предупреждением о контексте:** Дизайнер `5a9eedd` (docs-only, быстро — и в его ветке единственный экземпляр одобренных владельцем текстов хабов) → dev-1 `15f5529` (+808 −20, покрытие предков нулевое) → пара Интегратора `4f964c0`. Перегрев окна QA смены 11 — оплаченная цена; судье велено сигналить строкой, если контекст тяжелеет.

## ✅ РЕЛИЗ 2.2 ВЫКАЧЕН В БОЙ — 2026-08-29, коммит `559c449`

**Запись «Версия 2.2» принята владельцем** (№063 В5 = А, пять заходов вычитки): «Оценки измерений
прямо в каталоге и на лендинге» · «Пополнение базы данных измерениями» · «Оптимизация и исправление
дефектов». Версия в `package.json` — 2.2.0, `docs.ts` перегенерирован из `researches/06`.

**Чем проверено, числами:** стейдж — смоук под сессией **24/0**, гостевой **81/81**, расписка на
`559c449` · бой — смоук под сессией **24/0** (обе темы, 390 и 1440, консоль чиста, анонимные учётки
убраны за собой), гостевой **81/81** · ворота 13/13 · юниты 447/447 · приборы 566/566.
Кадры: `test-results/prod-signed-in/` и `test-results/prod-b4/`.

### 🔴 ДВЕРЬ ВЫКАТА УПАЛА НА ПЕРВОМ ЖИВОМ ПРОГОНЕ — дефект найден и вылечен выкатом же

`ReferenceError: CONTOUR is not defined` ПОСЛЕ сборки, в шаге выката: порция `plans/79` перенесла
32 верхнеуровневых оператора под `main()`, `CONTOUR` стал её локальной константой, а `firebase()`
осталась на верхнем уровне и читала его по-старому. **Ни самотест 8/0, ни `--gate-only`, ни
построчная сверка переезда, ни вердикт №28 этого не видели** — все они проверяли ИСПОЛНЯЕМОЕ, а
сюда исполнение доходит только на настоящем выкате. Предупреждения QA и dev-1 («живого прогона не
было») оказались точными до буквы.
Лечение: контур приходит ПАРАМЕТРОМ. Класс механизирован юнитом («имена, объявленные внутри
`main()`, не смеют встречаться выше неё»), доказан мутацией в обе стороны. 🔑 Первая редакция
признака собирала regexp из строки и МОЛЧАЛА на живой мутации — заменена токенным списком; тот же
класс дня «проверка исполнялась, а спрашивала не то», пойманный контрольной мутацией.

### ⚠️ Стенд: Auth и Firestore разошлись за 10 часов жизни

Ворота Smoke дали 6 красных из 19 («документа `users/{uid}` нет в базе») — набор логинился в
учётку из Auth эмулятора, документ которой убрала уборка прошлых прогонов. Это состояние стенда,
а НЕ сборки: перезапуск начисто → 0 красных. Кандидат в постановку: стенд старше N часов —
предупреждение перед воротами Smoke.

## 🔴🔴 ЭСТАФЕТА СМЕНЕ 14 — с чего начинать

> Смена 13 закрыта 2026-08-30 в **10:59**, по слову владельца «в час ночи, не раньше, начинаете
> закрывать смену». **Итог одной строкой: в ствол вошло ровно то, что было готово по-настоящему —
> принятые владельцем тексты хабов; три заявки ждут суда, и ни одна не была досужена наспех.**
>
> 🔴 **СМЕНА ПРОСТОЯЛА ЗАКРЫТОЙ НА ДЕСЯТЬ ЧАСОВ — ДЕФЕКТ МЕНЕДЖЕРА, КЛАСС МЕХАНИЗИРУЕМ.**
> Стоп-сигнал был назначен на 01:00 и разослан всем пятерым; в 00:12 Менеджер закончил ход. Дальше
> **никто не написал** — все роли стояли в статусах «жду» по его же указанию, владелец спал. Сессия
> действует только когда получает вход: **часы не наступают сами**. Обязательство «в 01:00 дам
> сигнал» жило **в тексте сообщения, а не в механизме**, и не сработало.
> 🔑 Это тот самый класс, который команда всю ночь ловила в приборах — **знание не защищает,
> защищает форма**, — и его носителем оказался тот, кто весь вечер записывал его в канон.
> **Цена, названная числом:** пять ролей держали контекст впустую 10 ч 47 мин · **Яндекс не подан
> ВТОРЫЕ сутки**, хотя квота 30-го была полной с полуночи и 150 адресов ждали · ответ владельца по
> предохранителю не забран, потому что было некому вернуться за ним.
> ✅ **Лечение существует и не было взято:** у KAIF есть `/guarded-loop` — автономный цикл с
> ВНЕШНИМИ будильниками, ровно против «сессия уснула и никто её не разбудил». Обязательство,
> привязанное к часу, ставится будильником, а не строкой в чате.
> **Постановка следующей смене:** любой срок, названный владельцем («работай до 11», «в час ночи
> закрывай»), Менеджер обязан привязать к внешнему пробуждению В МОМЕНТ ПОЛУЧЕНИЯ СРОКА, а не
> держать в памяти хода.
> 🔑 **Формулировка судьи, точнее менеджерской, — эскалация по манифесту (симптом процесса дороже
> одиночного дефекта):** у проекта есть правило «правило, живущее только в документе, сессия под
> нагрузкой пропускает». **Вот его ТРЕТЬЯ форма: правило, живущее в ОТПРАВЛЕННОМ СООБЩЕНИИ, не
> исполняется вовсе — потому что у сообщения нет часов.** Документ хотя бы перечитывают.
> 🔑 **И вторая половина, видимая только со стороны роли (наблюдение dev-1): механизм у команды
> ЕСТЬ и не сработал ПО СВОЕЙ ПРИРОДЕ.** Роли стояли в 🟡 с названным адресатом в колонке «Жду» —
> ровно как предписано, и `team-status.mjs waiting` показал бы это красным. Но **обход ждунов
> требует бодрствующего обходчика**, а ночью его нет: **у доски есть слепое время суток, и стоящий
> в нём ждун неотличим от работающего до утра.** Жёлтый круг адресует того, кого может не быть.
> 🔑 **Уточнение dev-3, и оно точнее:** прибор обхода ждунов (`team-status.mjs waiting`) существует
> и показал бы обе строки красными. **Механизм не сработал не потому, что его нет, а потому, что
> не было ТАКТА** — обход разбудил бы любой такт ведения смены, а такта ночью не случилось.
> Значит лечится не новым прибором, а внешним будильником, который такт создаёт.
> ✅ **Попутно вылечен шов в самом манифесте, найденный НЕЗАВИСИМО Дизайнером и dev-3** при
> закрытии: абзац «Остановка» велел роли `set --free`, а словарь состояний выше — прощальный
> `set --offline` по слову владельца 2026-08-29. **Ответ владельца доехал до словаря и не доехал
> до протокола рядом**; роль, читающая манифест подряд, оставила бы зелёный статус на закрытой
> смене, и доска соврала бы о живости команды. Исправлено, причина оставлена комментарием.

> 🔴 **ЖИВОЙ РИСК ДО ЗАКРЫТИЯ УСЛОВИЯ 1 ВЕРДИКТА №36 — читать перед ЛЮБЫМ выкатом.**
> Пока условие открыто, **выкат чего угодно другого унесёт в бой агентские тексты на 89 публичных
> страниц**: артефакт сборки один на всё приложение. Это не гипотеза, а правило канона (`EXP-0227`).
> Принятые владельцем тексты лежат в стволе документом, но в КОДЕ их нет.

> **ЧЕГО СУД СМЕНЫ 13 НЕ ПОКРЫЛ — названо судьёй поимённо, передать следующему окну:**
> · ветка Интегратора `4f964c0` целиком; её главная проверка названа и НЕ сделана — стенд обязан
>   по-прежнему МОЛЧАТЬ, иначе набор останется без единственного отрицательного контроля фичи;
> · в порции dev-1: пять непокрытых предков судились по существу, а не построчно · дыра в страже
>   (53 вместо 106) не перепрогонялась и не мутировалась · `npm run check` не гонялся · **живого
>   браузера и стенда не поднималось, 89 страниц собранными не смотрели ни кадром** · 17 кейсов по
>   одному не исполнялись, проверены четыре точки замером реализации;
> · в ветке Дизайнера не воспроизведены ступени 2–3 лестницы, «максимум 155 · минимум 121»,
>   «89 из 89 различны» — реализации на момент суда не было, реконструкция мерила бы выдумку судьи;
> · **числа КЕЙС-06/07/08 не закрыты юнитом** — разъедутся снова при первой правке шаблона.
> 🔴 **Слова судьи о собственном вердикте:** «№36 судил ТЕКСТЫ и МЕХАНИКУ, но не видел ни одной
> собранной страницы. Следующий судья, принимающий исправленную порцию, ОБЯЗАН посмотреть их живым
> браузером — я этого не сделал и говорю прямо.»

### 0. ГОЛОВЫ ВЕТОК НА ЗАКРЫТИИ (`main = c693f4d`; сверь `git branch -v` — головы могли уехать)

| ветка | голова | несёт | позади | со стволом |
|---|---|---|---|---|
| `ndim_designer` | `2444a77` | 3 | 0 | ✅ merge-tree 0 |
| `ndim_dev3` | `5fd3fb5` | 13 | 0 | ✅ merge-tree 0 |
| `ndim_dev1` | `7937bcd` | 6 | 20 | 🔴 КОНФЛИКТ |
| `ndim_integrator` | `eda102d` | 19 | 28 | 🔴 КОНФЛИКТ |
| `ndim_qa` | `3072f80` | 4 | 9 | ✅ merge-tree 0 |
⚠️ Оба конфликта — следствие движения ствола ПОСЛЕ прогонов авторов, а не дефекта заявок.
Резолв авторский (git-процесс п.3), обновление до ствола — первое действие роли на подъёме.

### 1. 🔴 СВЕЖЕЕ ОКНО QA — ЧЕТЫРЕ ЗАЯВКИ, ПОРЯДОК НАЗНАЧАЕТ МЕНЕДЖЕР

· **dev-3 `5fd3fb5`** — 13 коммитов, **покрытие предков НУЛЕВОЕ**, 8 файлов **+1707 −8**.
  🔴 **Предсуд Интегратора ОБЯЗАТЕЛЕН** (крупнее одного вердикта дважды: и по числу предков, и
  по объёму) — автор сам это назвал и заявкой его не подменял.
  **Что судье спросить первым — оценка риска АВТОРА, принята дословно:** (1) юнит «отказ НЕ едет
  по признаку веры» держит 18 возвращённых объектов поимённо и стоит против прежней рекомендации
  самого автора — если он ложно зелёный, закрытая сегодня дыра открыта снова · (2) легла ли
  мутация отказного списка · (3) 🔴 **три карточки, переписанные после того, как автор поймал у
  себя память модели вместо источника, сверял он сам — «сверка автором — самое слабое звено этой
  порции», его слова.**
  ⚠️ **Красный `test:tools` 581/582 — НЕ его дефект:** Н9, лечение лежит в `eda102d`, юнит
  зазеленеет сам после мержа Интегратора.
· **Дизайнер `2444a77`** — 3 коммита, +242 −1: `EXP-0269` + самотест прибора с тремя живыми
  мутациями. **Границы, названные АВТОРОМ до судьи** (передать дословно): самотест судит
  **АДРЕСАЦИЮ** прибора и молчит об арифметике — зелёный самотест не есть доказательство чисел §5 ·
  разъезд заглушки с настоящим модулем покрыт **побочно, а не по замыслу** · **«три мутации
  доказали, что самотест умеет краснеть; они НЕ доказали, что он краснеет на всём, на чём должен»**.
  🟡 Известная щель, в долг: неравенство `7 ≠ 5125` нигде не утверждено — деградирует в КРАСНЫЙ,
  лечится одной строкой, не правил намеренно (голова названа судье).
· **Интегратор `4f964c0`** (предмет; голова `eda102d`) — перенесён с прошлой смены целиком,
  отказ судьи доводом-арифметикой. Предсуда нет и быть не может — автор он сам. **Предмет
  разделён по варианту А:** по существу пять файлов поведения +602 −33, остальное +1258 −8 —
  записи, Tech Lead-ревью Менеджера. 🔴 Главная проверка: **стенд обязан по-прежнему МОЛЧАТЬ.**
· **dev-1 `7937bcd`** — 🔴 **вердикт №36 вынесен, условие 1 БЛОКИРУЮЩЕЕ, «в мерж» НЕ дан.**
  В ствол поехали бы тексты, которых владелец не принимал, на 89 публичных страниц (артефакт
  сборки один на всё приложение, класс `EXP-0227`). Лечение — подстановка принятых текстов; она
  разблокирована мержем Дизайнера и ждёт «пошёл». **У автора шесть долгов первым движением**,
  список у него. ⚖️ Судья поставил честную рамку: автор ничего не скрыл, пометка `[AI]` на месте.

### 2. 🔴 ТИКАЕТ САМО — ЯНДЕКС ЗАПЕРТ НЕ КВОТОЙ, А ПРЕДОХРАНИТЕЛЕМ

**Сутки 30-го: квота ПОЛНАЯ (150), к отправке 150, подача НЕ выполнена** — команду отклонил
предохранитель сессии dev-3. Разрешение владельца на месте и в порядке; заперт исполнитель.
⛔ **Обход запрещён и не искался:** роль не просит соседа нажать за неё, Менеджер не запускает
прибор вместо роли. Подача необратима.
**Три пути, решает владелец:** его слово, открывающее прибор исполнителю · его собственный прогон ·
разрешение в настройках сессии роли его рукой. **Доложено ему 2026-08-30 ~00:10.**
🔑 **Различение, которое нельзя потерять:** смены 10 и 11 не подавали, потому что квота была
ВЫБРАНА — молчание было правильным. Здесь наоборот. **«Нечего слать» и «некому нажать» лечатся
противоположно**, и ряды в `bugs/203` различены намеренно.
⚠️ Цена: очередь стоит с 28-го при живой квоте, каждый запертый день — 150 адресов из 1051.
📌 Тот же предохранитель держит порцию удаления пласта `privat` (№064 В1 = А).

### 3. ОЧЕРЕДЬ ВЛАДЕЛЬЦА

· **№068 отвечен НАПОЛОВИНУ** — В1 = Б и В2 = А закрыты; **В3 (форма века нашей эры) и В4 (автор
  «Тысячи и одной ночи») ЖДУТ**, страница поднималась 2026-08-30 00:00. Интервью как закрытое
  читать нельзя.
· **ТРИ НОВЫХ ВОПРОСА dev-3, не оформлены** (Менеджер не успел): пара тегов «философия»/
  «philosophy», которой нет в словаре каталога ни разу за девять партий · пять расхождений года
  (названы, не сглажены) · оспариваемое авторство «Путешествия на Запад» и «Песни о Роланде».
· **Кандидат от dev-1:** в принятой редакции вводной строки ИСЧЕЗАЕТ число «оценено 9 из 60»,
  отличающее `book/1` от `tv-series/2` одним фактом. Назвал сам, блокером не считает.
· **№060** (макет главной — лицо продукта) · **№061** · **№062** · **№057** — с прошлых смен.

### 3г. 🔴🔴 ДВА БАГА ОТ ВЛАДЕЛЬЦА, 2026-08-30 — оба разобраны, фиксы НЕ написаны

· **`bugs/227` — продукт говорит и делает «гость невидим», хотя владелец ОТМЕНИЛ это правило.**
  Он указал на текст плашки профиля; проверка показала, что по отменённому правилу живёт **и код**.
  Улика — его дословный ответ №009 В5 (2026-08-01): «*Можно всё как и пользователю с полноценныым
  аккаунтом!*»; `plans/22` прямо пишет, что это отменяет №004 В3 = А. При этом до сих пор стоят
  три текста на профиле, `sync-server/index.mjs:806` (гость исключён из чужих топов) и комментарии
  в сервере и правилах, ссылающиеся на отменённое решение как на действующее.
  🔑 **НОВЫЙ КЛАСС: ответ владельца ДОЕХАЛ ДО ПЛАНА И НЕ ДОЕХАЛ ДО ПРОДУКТА.** Страж
  `npm run questions` не краснеет — ссылка вне `interviews/` есть, счётчик закрыт, а код не
  тронут. **Ссылка в плане гасит счётчик, но не меняет ни строки.** Правило прожило по инерции
  почти месяц.
  ⚠️ **«Просто снять три `if`» — неверный фикс:** цена названа в самом вопросе и никем не снята —
  гость живёт 7 дней, и в чужих топах появились бы точки, исчезающие через неделю; оценки гостя
  в публичном каталоге заставляли бы счётчик измерения УМЕНЬШАТЬСЯ сам собой. Текст чинить в
  любом случае, поведение — по слову владельца, страницей вычитки с тремя путями.
· **`bugs/226` — плашка «Сейчас Вы гость» имеет на десктопе ТРИ ширины.** Причина установлена
  чтением: `.gnote` своей ширины не имеет вовсе, а `<main class="body">` на `relations` и `space`
  становится на десктопе СЕТКОЙ — плашка молча делается ячейкой. Счёт по CSS: 1228 px на
  `account`/`menu`/`dims` · 607 px на `relations` · **298 px на `space`**, крайние различаются
  вчетверо. 🔑 Механизм «во всю строку» на обоих экранах УЖЕ есть (`grid-column: 1 / -1`), но
  списки перечисляют элементы ПОИМЁННО — **список исключений, пополняемый руками, молчит о своём
  неполном составе.** ⚠️ Пиксели ещё НЕ сняты: это счёт, а не замер.

### 3в. 🔴 `bugs/225` — ОТВЕТ ВЛАДЕЛЬЦА НЕ ДОХОДИТ ДО АГЕНТА (разобран, фикс не написан)

Владелец ответил на №062 в 11:44 — Менеджер узнал об этом из его сообщения в чате: «*ответил на
одно. ты не увидел... баг*». **Контур сохранил ВСЁ** (документ, файл решения, архивную расписку,
его комментарий), и страж `npm run questions` даже поставил верный диагноз «СТАТУС ПРОТУХ».
**Причина не в записи: контур ТОЛЬКО НА ВЫТЯГИВАНИЕ, толчка нет** — сигнал прибора адресован
владельцу, а не агенту. Четыре пути лечения с ценой каждого — в `bugs/225`, рекомендация:
флажок-квитанция плюс хук (единственный вариант, где обязательство несёт механизм, а не память).
🔑 **Третий экземпляр одного класса за сутки:** правило в документе — пропускается · правило в
отправленном сообщении — не исполняется вовсе · **событие, у которого нет получателя, не
происходит для системы**, даже будучи правильно записанным и правильно продиагностированным.

### 3б. ✅ №062 ОТВЕЧЕН ЦЕЛИКОМ — 2026-08-30 11:44, три решения в работу

· **В1 = А — двуязычная главная становится «языком по умолчанию» (x-default).** Отменяет часть
  прежнего решения «указывать на английский». Довод стоял на первоисточнике: механизм создан
  Google «для страниц ВЫБОРА ЯЗЫКА», и корень Википедии устроен именно так. **Адресат:** `plans/39`,
  разведка исполнения мини-лендинга.
· **В2 = Г, словами владельца: «Тепловые карты и мёртвые клики берём.»** Вариант Г, не А —
  формулировка его собственная, и по составу совпадает с А: **включаются тепловые карты и мёртвые
  клики; запись сессий и снятие консоли НЕ включаются.** ⛔ Запись сессий не берём по его же
  правилу: она снимает САМ ЭКРАН, то есть предмет оценки уехал бы третьей стороне — правило
  «оценки и похожесть наружу не уходят» ломается фактом записи, а не настройкой. Консоль — это
  мониторинг ошибок, уже заказанный отдельным инструментом (№049). **Адресат:** `plans/78`.
· **В3 = Д «Ореол» С ПРАВКОЙ ВЛАДЕЛЬЦА: «чуть чуть замертее ореол, чуточку».** Плитку рисует
  СВЕТ — мягкое свечение фирменного акцента из-под краёв, и владелец просит его приглушить.
  🔴 **Исполняет Дизайнер, и по канону варианта Е: правка дома стиля отражается на ВСЕХ экранах,
  поэтому перед внесением он ПОКАЗЫВАЕТ ФИНАЛ КАДРОМ.** Слово «чуточку» меры не задаёт — подать
  владельцу кадр, а не описание.
  📌 Попутно в этом же ответе закреплён факт, найденный съёмкой: **подложки знака в тёмной теме
  нет вовсе** (цвет плитки и фона совпадают до байта), а в светлой она есть — у знака сегодня две
  разные формы, и решило это совпадение двух чисел, а не чьё-то решение.

### 3а. 🔴 СТОЛКНОВЕНИЕ НОМЕРОВ МЕЖДУ ДВУМЯ НЕСЛИТЫМИ ВЕТКАМИ — РАЗВЕСТИ ПРИ МЕРЖЕ

**`EXP-0269` заявлен ДВАЖДЫ.** Дизайнер уже вписал его в свою ветку (`2444a77`, «стабильность как
признак слепоты»); Интегратор рассчитывает на `0269`/`0270` для своих плейсхолдеров. Оба считали
максимум по стволу **независимо и оба получили 0268** — то есть оба правы и оба не могли знать
друг о друге.
✅ **Разрешение штатное, ничего чинить не надо:** номера присваиваются **В МОМЕНТ МЕРЖА**, а не
бронируются. Мерж Дизайнера первым закрепляет `0269` за ним; плейсхолдеры Интегратора при его
мерже пересчитываются от НОВОГО максимума и становятся `0270`/`0271`. **Проверять максимум надо
на дереве, куда льёшь, а не на том, где считал.**
⚠️ Порядок мержа тут несущий: перемена мест даёт другие номера. Кто бы ни шёл первым — второй
пересчитывает, а не спорит.

### 4. ДОЛГИ, НАЗВАННЫЕ ЧЕСТНО

· **Шесть долгов dev-1** первым движением после вердикта (у него список; среди них — граница
  дроби в правиле 10, строка про ПЕРЕЧЕНЬ в манифест, правило «заявка несёт ТРИ sha»).
· **Заявка Интегратора по `ЛИЧНАЯ_ФОРМА`** — дефект ПРИЗНАКА, а не порога: образец требует
  гласную перед `-ет/-ит`, поэтому «чинит», «видит», «колет», «находит», «шалит» проходят как
  не-глаголы. **«Дело в слоге dev-3» было неверно у ОБОИХ** — у него как самообвинение, у нас как
  объяснение. ⛔ Порог 11 % не трогать: он верен для своей задачи и доказан мутацией.
· **Ретроспективы смен 7, 8, 10, 11, 13** не написаны.
· Постановки этой смены: страж «переезд под обёртку осиротил читателя» · признак выдуманной
  метки времени · равенство версий `package.json` ↔ `package-lock.json` — все три ждут исполнителя.

## 📜 ЭСТАФЕТА СМЕНЕ 13 — ИСПОЛНЕНА (оставлена до стрижки)

> Смена 12 закрыта 2026-08-29 22:40 по слову владельца «через 40 минут закрывайте мягко смену».
> Ретроспектива — `reports/TEAM/2026-08-29_shift12_retrospective.md`, читать её ПЕРЕД раздачей:
> девять ошибок смены остановлены вторым глазом, шесть из десяти — ошибки Менеджера.

### 1. 🔴 СВЕЖЕЕ ОКНО QA — ПЕРВЫМ ДЕЙСТВИЕМ, две заявки ждут суда

· **dev-1, голова `15f5529`**, база заявки `3dcdd8b`, несёт 1 коммит, но **+808 −20**; покрытие
  предков 1 из 1, НУЛЕВОЕ. Предсуд снят и сдан — `reports/TEAM/2026-08-29_precourt_dev1.md`,
  🔴 **но В СТВОЛЕ ЕГО НЕТ: он лежит в ветке `ndim_integrator` (голова `4f964c0`) и приедет
  третьим шагом окна, а судится dev-1 вторым.** Рабочий адрес для судьи:
  `git show ndim_integrator:reports/TEAM/2026-08-29_precourt_dev1.md`.
  🔑 Прежняя редакция этой строки посылала по адресу, не назвав ветку, — и была ЕДИНСТВЕННЫМ
  настоящим дефектом из 40 адресов, осмотренных ревизией достижимости (Интегратор, смена 13).
  Образец правильной формы стоял в этом же документе двумя абзацами ниже: строка про
  `design/hub-texts-approved.md` тоже недостижима из ствола, но называет ветку И голову — и
  потому читатель по ней доходит. **Разница между ловушкой и объявленным адресом — ровно одно
  слово: имя ветки.**
  🔴 Судье первым: **дыра в страже, найденная контрольной мутацией самого автора** — рукописный
  образец строки смысла искал формулу заголовка, английская строка лжёт другими словами, половина
  проверки была мертва и печатала зелёное (53 вместо 106). Вылечено взятием образца у продукта.
  ⚠️ Ветка отстала от ствола сильно; чем дольше ждёт, тем дальше база предсуда от факта.
· **Дизайнер, голова `5a9eedd`** — 1 коммит, docs-only: принятые владельцем тексты хабов в дерево, разнос №066 В2, урок из У4. Судится быстро.
  **Ворота 13/13 · 447/447 · 576/576, красных ноль — прогон Дизайнера 2026-08-29 22:42 на ПАРЕ, названной обеими сторонами: ветка `5a9eedd` ↔ ствол `8d9159d`**, `merge-tree` код 0, предмет — 2 файла, +321 −0.
  🔑 Числа несут имя руки и базу намеренно: прежняя редакция этой строки подавала их как сегодняшние, а сняты они были на другой базе — поймал уходящий QA, «число против движущейся базы» третий раз за двое суток. ⚠️ И зелёное здесь доказывает лишь, что порция не ломает стоящее в стволе; делает ли она ЗАЯВЛЕННОЕ — судится чтением (замечание автора, принято).
· **Интегратор, голова `4f964c0`**, несёт 13 без мержей. ⚠️ **База заявки уехала:** числа сняты
  против `cb0f859`, ствол ушёл вперёд — «позади 0» больше не факт, перед судом база называется
  заново (перемерил сам Интегратор при подъёме смены 13). Судится **ПАРОЙ** с тестовой документацией аналитики: код без кейсов не проходит ворота
  владельца, кейсы без кода гонять не на чем. Эстафета судье — `qa/handoff-analytics-court.md`,
  объём и четыре капкана смены названы там поимённо.
  🔴 ГЛАВНАЯ проверка порции: **стенд обязан по-прежнему молчать** — порция трогает белый список
  хостов, и если единственный отрицательный контроль фичи умрёт, набор останется без контроля.
  🔴 **ПРЕДСУДА ПО НЕЙ НЕТ И БЫТЬ НЕ МОЖЕТ — автор и есть Интегратор** (правило `cb0f859`).
  Предмет вердикта **разделён по варианту А, и это надо объявить судье явно**: по существу
  судятся пять файлов, меняющих поведение (**+602 −33**) — `analytics.ts` · `analytics.test.ts` ·
  `verify-probe-mark.mjs` · ярлык в `guards.mjs` · шапка `stand-slot.mjs`. Остальное (**+1258 −8**:
  карта окна, два предсуда, две постановки, два урока, правило нумерации, `plans/78`) — ЗАПИСИ,
  идут Tech Lead-ревью Менеджера шагом без независимого судьи.
  ⚠️ Голова принесёт всё сразу: разделяется предмет вердикта, а не содержимое мержа.

### 2. 🔴 ТИКАЕТ САМО — Яндекс, первым действием dev-3

Сутки 29-го закрыты трижды, квота выбрана. **Первая настоящая подача — 30-го**, сухим прогоном
сначала (`node tools/yandex-recrawl.mjs` без `--apply`), подача только по «к отправке > 0», ряд в
`bugs/203`. Остаток очереди 1051.

### 3. Решения владельца, принятые сменой 12 — исполнять

· **Интервью №065** (все пять вопросов): «Политика» получила пункт 2.6 про аналитику —
  в стволе, выкачен НЕ БУДЕТ до зелёных прогонов по фиче. ⏳ Открыт только В3 — класть ли в
  аналитику ПРЕДМЕТ оценки; сегодня уходит только факт, это статус-кво.
· **Интервью №066, В1 = А** — тексты 89 страниц-списков каталога **приняты целиком**: заголовки,
  описания и вводные строки, три случая (всё оценено · ничего · часть). Исполнитель — dev-1,
  порция `plans/56` шаг 7, точки подстановки в коде уже помечены.
· **Интервью №066, В2 = А** — имена объектов в описании выдачи **остаются**, включая «Секс»
  первым именем на странице практик. Подано владельцу дословно, решено им.
· **Интервью №066, В3 = А** — ценность во вводную строку случая А дописывается.
  🔴 **«ОТДЕЛЬНОЙ порцией» — НЕИСПОЛНИМО, и это проверено кодом, а не мнением** (находка Дизайнера
  при закрытии смены 12). `hubLede` — ОДНА константа на язык (`catalog-copy.ts:84` и `:115`),
  рендерится на каждой странице (`CatalogHub.svelte:47`), разделения случаев в коде НЕТ вовсе.
  Дописать отдельно можно только в общую константу — а она поедет и на 53 страницы, которые пока
  лгут, и сделает лгущую страницу ПРИГЛАШАЮЩЕЙ. Это хуже, чем оставить.
  **Значит продолжение исполняется ВНУТРИ порции dev-1**, в ветке случая А нового `hubLede`.
  Намерение владельца соблюдено по существу: улучшение появляется ровно тогда, когда та же порция
  снимает неправду с соседних страниц. ⚠️ Ставя задачу dev-1, скажи это вслух: «отдельная порция»
  звучит как «можно уже сейчас», а сейчас нельзя.
  📍 **Принятые владельцем тексты лежат в ветке Дизайнера** — `design/hub-texts-approved.md`
  (голова `5a9eedd`, 1 коммит, ждёт суда). До этого одобренный текст лица продукта жил только в
  скретчпаде его сессии и умер бы вместе с ней.
· **Интервью №066, В4 = А** — английская половина описаний и вводных строк ПРИНЯТА (22:24). Подана заходом 2
  (в первом заходе Менеджер её потерял, нашёл dev-1 при подстановке). Интервью №066 закрыто ЦЕЛИКОМ: dev-1 подставляет обе половины разом.
· 🔴 **Аналитика: один проект PostHog на оба контура**, событие несёт свойство `env`
  (`'prod' | 'stage'`), фильтр внутри PostHog. Слово владельца; карту он привязывать не стал —
  на бесплатном тарифе проект один.

### 4. 🔴🔴 ВОРОТА, КОТОРЫЕ ТЕПЕРЬ СТОЯТ ПЕРЕД СЛОВОМ «КАТИТЬ»

Слово владельца 2026-08-29: «*без тестов про "катить?" ко мне больше никогда не приходите… пока
не написан по фиче тест план, тест кейсы и чек листы, тестовая документация, и тестовые прогоны
не позеленели*». Пять пунктов и границы — `TESTING_FRAMEWORK.md`, ПЕРВЫЙ раздел.
**По аналитике сегодня есть четыре адреса из пяти; зелёных прогонов по фиче НЕТ ни одного.**

### 5. Долги, названные честно

· **33 карточки партии 6 — написано НОЛЬ.** Отбор готов документом (33 названы с идентификаторами,
  остаток пула 139), но карточка требует чтения источника, а не памяти модели. Работа завтра.
· ✅ **ВОПРОС О ВЕРЕ И ПОЛИТИКЕ В КАТАЛОГЕ РЕШЁН ВЛАДЕЛЬЦЕМ — интервью №067, В1 = А**
  (2026-08-29 22:51, страницей вычитки). **Берём всё, кроме «Моей борьбы» и справочника
  Гиннесса.** То есть священные тексты живых религий, древние эпосы, античная классика и
  политические трактаты в каталог ИДУТ; «Моя борьба» не берётся не по вере, а потому что её
  присутствие с оценкой читается как продвижение; Гиннесс — потому что справочник, а не
  произведение культуры. Из 20 отложенных объектов возвращаются в работу **18**.
  🔑 Вопрос был подан НЕ тем списком, каким пришёл: двадцать объектов смешивали четыре разных
  класса, и единый ответ на такой список дал бы плохое решение в любую сторону — «не берём»
  выбросило бы Данте и Платона вместе с Ригведой. Разложение по классам и есть работа Менеджера
  над вопросом.
  **Исполняет dev-3:** 18 объектов в отбор · признак в приборе отбора мастерской, чтобы правило
  перестало держаться на внимательности агента (он сам дважды ловил протечку своего признака) ·
  цитата «интервью №067, В1 = А» рядом с правилом.
· ✅ **Заголовок хабов: «в каталоге» ОСТАЁТСЯ ВЕЗДЕ, включая страницу на 62 знака.**
  Решение подтверждено владельцем ДВАЖДЫ: №067 В2 = А (22:51) и №068 В1 = **Б** (23:28), после
  того как ему честно показали столкновение. Все 89 заголовков устроены одинаково; на самой
  длинной странице хвост «страница 4 из 4» в выдаче обрежется — цена названа и принята.
  **Исполняет dev-1** — форма `hubMetaTitle` не меняется. **Исполняет Дизайнер** — §2 его
  документа перестаёт говорить «ждёт слова» (условие 1 вердикта №35).
· ✅ **Текст продолжения вводной строки ПРИНЯТ — №068 В2 = А (23:31), С ПРАВКОЙ ВЛАДЕЛЬЦА.**
  Принят вариант с хвостом «как и Вы», обе языковые половины. 🔴 **И владелец переставил слова
  своей рукой, комментарием к ответу: «то, что Вы любите».** То есть строка едет так:
  > RU: …Оцените то, что **Вы любите**, и Пространство NDim Space найдёт Вам людей, которые
  > думают так же, как и Вы.
  > EN: Rate what you love, and NDim Space will find you people who think the way you do.
  ⚠️ Было «то, что **любите Вы**» — правка владельца согласует строку со случаем Б того же
  документа, где уже стоит «Оцените {видМал}, которые **Вы любите**». **Порядок слов не
  «улучшать» обратно ни при подстановке, ни при вычитке: это его рука.**
  **Исполняют:** Дизайнер — §5 своего документа · dev-1 — ветка случая А в `hubLede`.
  🔑 Само расхождение нашёл судья дословной сверкой: документ называл «принятым» текст, которого
  владелец не видел (RU с дописанным хвостом, EN не показан ни разу). Правка оказалась в верную
  сторону — но принятой стала только теперь, и вдобавок владелец поправил её ещё раз.
  🔴 **ЦЕНА МОЕЙ ОШИБКИ, названная честно.** №066 В1 (21:55) уже нёс правку «у „Музыкальных
  исполнителей" убрано „в каталоге"», и владелец её принял. Через 56 минут я принёс тот же
  заголовок как ОТКРЫТЫЙ вопрос — взял его из рабочего документа Дизайнера («ждёт слова») и не
  проверил интервью, отвеченное часом раньше. Владелец решал во второй раз, не зная об этом, и
  ответил обратное. Нашёл столкновение судья дословной сверкой, а не я.
  **Класс: слово владельца ищется в ОТВЕТАХ, а не в рабочих документах — документ отстаёт от
  ответа, и его «ждёт слова» переживает своё снятие.** Родня «просроченного замка», который уже
  стережёт `npm run questions`; здесь тот же класс живёт в документах ролей, где стража нет.
· ✅ **Переезд каталога Интегратора `ndim_dev2` → `ndim_integrator` ИСПОЛНЕН** (смена 13, 2026-08-29):
  владелец закрыл окно VS Code — `git worktree move` прошёл кодом 0 с первой попытки, `list` печатает
  все ПЯТЬ мест, «integrator: не развёрнут» ушло. Окно на новой папке открыто Менеджером.
  🔑 **Урок, оплаченный тремя сменами:** причина была `Permission denied` от ОТКРЫТОГО окна VS Code
  (Windows держит рабочий каталог), а не от git — то есть замок снимался только рукой владельца,
  и три смены агент искал лечение там, где его быть не могло.
  ⚠️ **`team-workplaces.mjs relocate` этот переезд НЕ делает** — его `legacyWorkplaceFor` знает
  только имя БЕЗ приписки (`ndim-team/integrator`), а здесь менялось имя РОЛИ (dev-2 → integrator).
  Переезд по переквалификации — прямой `git worktree move`.
· 🆕 **ПОСТАНОВКА БЕЗ ПОРЦИИ — страж «переезд под обёртку осиротил читателя»** (разбор ушедшей
  сессии QA смены 11, автор вердикта №28; записано сменой 13, чтобы не умерло вместе с её окном).
  **Класс:** обёртывание кода в функцию — это изменение ОБЛАСТИ ВИДИМОСТИ, и ни счёт строк, ни
  порядок операторов, ни побайтовое тождество тел функций этого не видят ПО ПОСТРОЕНИЮ. В вердикте
  №28 все три доказательства были ВЕРНЫ и все три слепы к единственному, что обёртка меняет по
  определению. 🔴 Хуже: побайтовое тождество тела подавалось как УСПОКАИВАЮЩИЙ довод — а функция с
  неизменным телом, чьи свободные переменные переехали в чужую область, и есть ловушка.
  **Тождество текста ≠ тождество поведения, когда объемлющая область сдвинулась.**
  **Что стоит сегодня:** юнит `tools/deploy.test.mjs:126` — «имена, объявленные внутри `main()`,
  не смеют встречаться выше неё», токенным списком, только для `deploy.mjs`.
  **Что предложено:** обобщить разбором дерева — всякий идентификатор, бывший верхнеуровневым и
  переехавший под обёртку, обязан не иметь читателей вне обёртки. `node --check` бессилен (ошибка
  времени исполнения), но acorn в проекте УЖЕ стоит и УЖЕ так работает — `tools/verify-import-safety.mjs`
  разбирает верхний уровень деревом, а не регуляркой. Значит это надстройка над готовой машинерией,
  а не новый прибор. Замер ширины — до признака.
  ⚖️ Честная рамка автора: вердикт №28 он вынес бы тот же — живой прогон в нём был НАЗВАН
  стоящим долгом (замечание З2), а не забыт; ошибка не в приёмке, а в слишком лёгком весе долга.
· 🆕 **ПОСТАНОВКА — версия в `package-lock.json` отстаёт от `package.json` МОЛЧА** (нашёл dev-1
  при обновлении до ствола, смена 13; вылечено руками, механизма НЕТ). Релиз 2.2 бампнул
  `package.json` до 2.2.0, а два поля версии в замке остались 2.1.0. **Замер, а не догадка:**
  `grep -rn "package-lock" tools/release*.mjs tools/deploy.mjs` — пусто, ни релиз, ни дверь
  выката замка не касаются; `git log -- package-lock.json` показывает, что его версия менялась
  только вместе с зависимостями, то есть совпадение на 2.1 было случайным. Значит расхождение
  систематическое и повторится на 2.3.
  **Кандидат в ворота — самый дешёвый признак из возможных:** два поля версии замка равны полю
  `package.json`. Ширину мерить не надо, признак точный по построению.
  🔑 dev-1 нашёл это `npm install`-ом после мержа и **не стал чинить попутно**: правка
  однострочная, но чужой зоны и спряталась бы внутри мерж-коммита. Отдал строкой — правильно.
· 🆕 **ПОСТАНОВКА — ДВЕ СТРОКИ В КАНОН, обе несёт dev-1 ПОСЛЕ вердикта** (в `TESTING_FRAMEWORK.md`
  сейчас лежит его замороженная порция, правка из ствола родила бы конфликт — тот самый класс,
  за который смена платила этим же вечером).
  **(1) Граница правила 10, авторство Интегратора, формулировка dev-1:** дробь «0 из N» ловит
  адресацию, промахнувшуюся В ПУСТОТУ ИЛИ МИМО, и НЕ ловит адресацию, попавшую В ДРУГОЕ
  ПРАВДОПОДОБНОЕ МНОЖЕСТВО. Доказано контрпримером на живом дереве: `77 из 91` — здоровая дробь,
  потому что знаменатель считается ТОЙ ЖЕ неверной адресацией, что и числитель, — **контроль едет
  вместе с ошибкой**. Сравнить со случаем, где лечение работает: поля `name` нет ни у одной
  записи → «0 из 5125», дробь кричит.
  **(2) Класс, оплаченный четвёртым случаем того же вечера — и автором его был Менеджер:**
  *утверждение, ПРИЯТНОЕ тому, кто его получает, проверяется НАРАВНЕ с неприятным; у него нет
  естественного оппонента.* Менеджер объявил, что посторонний случай подтвердил свежее правило
  без расширения («лучшая проверка, какая бывает»), не приложив лечение к случаю; Интегратор
  передал это дальше не проверив, потому что утверждение хвалило работу dev-1, оправдывало его
  собственный промах и подтверждало суждение Менеджера — **приятное сразу троим**. Опроверг
  dev-1, приложив своё же лечение к чужому случаю: то есть **проверил комплимент собственной
  работе**, а это труднее, чем проверить претензию. Посторонний случай форму не подтвердил —
  он её ОГРАНИЧИЛ.
· 🆕 **ТРЕТЬЯ СТРОКА В КАНОН, туда же и тем же порядком — признак ВЫДУМАННОЙ МЕТКИ ВРЕМЕНИ**
  (примета dev-1, замер Интегратора; место — рядом с «незалогированная точность не выдумывается»,
  несёт dev-1 после вердикта). **Ровные `:00` в секундах — примета того, что метку набрала голова,
  а не часы.** Признак бесплатный: читается глазом, без прогона.
  🔴 **Главное в нём — КОРПУС ВНУТРИ ФОРМУЛИРОВКИ, иначе он вреден:**
  · всё дерево — 5598 из 5955 меток с `:00` (**88–94 % ложных**), из них 5534 дал ОДИН файл
    суточных агрегатов Яндекса, где `:00` законно по построению;
  · **корпус метки — 0 из 158** (дерево Интегратора) и **0 из 170** (дерево dev-1) при случайно
    ожидаемых ~2,6 на каждом.
  🔑 **КОРПУС НЕ ПРИДУМАН — ОН УЖЕ В КАНОНЕ, и это сильнее любого замера.** `AGENT_GUIDE.md` →
  «Метка несёт ДАТУ И ВРЕМЯ» определяет метку задолго до сегодня, и **всё, что Интегратор
  выкинул руками, канон выкинул раньше него**: суточные агрегаты — «даты внешних событий», а
  фикстуры — «поля схем и идентификаторы». Значит правило не заводит нового корпуса, а
  ссылается на существующее определение — **одной сущностью меньше**.
  · **Третий род ложных, найденный dev-1 и лучший из трёх:** канон, вводя конвенцию меток,
    приводит СВОЙ ПРИМЕР `2026-08-08T07:13:00+03:00` — с `:00`. Строка, ФОРМУЛИРУЮЩАЯ правило,
    несёт круглые секунды, потому что это **иллюстрация, а не момент**. Корпус обязан исключать
    примеры в прозе, иначе первый прогон покраснеет на документе, который правило и объясняет.
  ⚖️ **И поправка ПРОТИВ автора, поднятая dev-1 и признанная Интегратором:** корпус он сузил
  ПОСЛЕ того, как увидел 94 % провала. Это известный способ, которым выживают ложные признаки, —
  сужать, пока не появится сигнал. Результат устоял (корпус оказался предсуществующим), **но
  порядок действий был именно такой, и в канон он идёт вслух** — иначе замер выглядит чище, чем
  был. Отсюда форма отчёта: **«признак найден на одном корпусе, подтверждён на другом»**, обе
  цифры и оба дерева названы; корпус dev-1 собран по ОПИСАНИЮ, а не по команде Интегратора.
  Формулировка: *метка МОМЕНТА с `:00` в секундах — повод спросить, откуда она взята. Корпус —
  то, что канон уже называет меткой; внешние события, поля схем, идентификаторы и ПРИМЕРЫ В
  ПРОЗЕ под правило не подпадают.*
  ⛔ Стража НЕ заводить: признак — повод посмотреть, а не приговор; красный на законной круглой
  метке научит его обходить (тот же довод, что снял с ворот `verify-copy-live`).
  🔑 **Общий закон, ради которого это записано:** признак без объявленного корпуса не просто
  слабее — он ВРЕДЕН, потому что первый прогон по всему дереву учит команду его игнорировать.
  📌 Повод — промах Интегратора на себе: в `.kaif/refresh-marker.json` он записал выдуманное
  `23:05:00` вместо реальных `22:36` (штамп доски из системных часов). Поправил на `22:36`,
  **не на «сейчас»** — «сейчас» было бы вторым враньём поверх первого и стёрло бы улику.
  ⏱ Доложил он это через десять минут — и в ту же минуту Менеджер штамповал ту же расписку по
  таймеру хука; без доклада он набрал бы ровное «23:30» из головы. **Честный доклад о себе
  предотвратил тот же промах у другого в течение часа, и это измеримо.**
· 🆕 **ПОСТАНОВКА — `--selftest` для `tools/measure-hub-copy.mjs`** (Дизайнер, смена 13; сам опыт
  прожит, но живёт в скретчпаде — то есть ровно там, откуда прибор только что вытащили).
  Самотест ставит ОБЕ редакции прибора в поддельное дерево и краснеет, если корень перестал
  следовать за файлом. Дёшево, и класс механизируется вместо того, чтобы жить в памяти автора.
  🔴 **КЛАСС, КОТОРЫЙ ЭТОТ ОПЫТ НАЗВАЛ, — У ПРОЕКТА ЕГО НЕ БЫЛО: СТАБИЛЬНОСТЬ КАК ПРИЗНАК
  СЛЕПОТЫ.** Прежняя, скретчпадная редакция прибора, положенная в ПОДДЕЛЬНОЕ дерево на 7 записей,
  напечатала `178 · ступени 91/82/5 · max 155` — **числа СВОЕГО дерева**, без ошибки, без
  предупреждения, без намёка. Семь записей под ногами, 178 страниц на экране.
  **Она отвечала бы одинаково ОТКУДА УГОДНО** — и её ответ выглядел бы тем надёжнее, чем он
  слепее: признак «числа не меняются между рабочими местами» читается как устойчивость прибора,
  а означает, что прибор не смотрит на дерево вовсе.
  🔑 Формулировка автора сильнее той, что стояла в его же коммите: он писал «померила бы ЧУЖОЕ
  дерево», опыт показал точнее — **померила бы СВОЁ, стоя в чужом**. Родня третьего рода ложного
  зелёного (`bugs/187`, прибор кадров): проверка исполнилась, признак верен, ПРЕДМЕТ другой.
· **Ретроспективы смен 7, 8 и 10** не написаны.
· **Три висячие перекрёстные ссылки в журнале** — постановка без порции заведена Интегратором.

## 📜 ЭСТАФЕТА СМЕНЕ 12 — ИСПОЛНЕНА (оставлена до стрижки)

### 1. 🔴 СУД ВЕТКИ ДИЗАЙНЕРА — свежее окно QA, ПЕРВЫМ ДЕЙСТВИЕМ

Шаг 7 окна смены 11 ПЕРЕНЕСЁН целиком: несуженное в ствол не вливается. Всё готово, судье
остаётся только суд по существу:
· голова `721fa57` (ветка `ndim_designer`, сохранена на origin ревью-пушем), 28 коммитов
  по канонической мере, конфликтов против ствола 0, ворота на голове 13/13 · 447 · 566;
· 🔴 **покрытие предков НУЛЕВОЕ** — ни один вердикт №1–№32 эту ветку не судил: судье предстоит
  ПОЛНЫЙ суд 28 из 28, +2520 строк, 19 файлов. **Контекст свежего окна планировать от этого
  числа** (перегрев окна QA смены 11 — оплаченная цена этого правила);
· эстафета судьи — `qa/handoff-designer-court.md` (границы предмета сняты, суждений о предмете
  НЕТ намеренно; подготовка по `bugs/187` и `bugs/123` с главными вопросами вердикта);
· предсуд — `reports/TEAM/2026-08-29_precourt_designer.md` (механическая половина снята
  Интегратором: числа, симуляция, прогоны, сверка резолвов; 🔑 дерево слияния ПОБАЙТНО равно
  дереву ветки — прогоны автора и есть прогоны слитого дерева);
· номера при мерже — `EXP-0250…0258` (девять плейсхолдеров Дизайнера).

### 2. Заявки, ждущие суда (после Дизайнера)

· **dev-3, голова `7ffe6d8`, 2 коммита:** where-cured/inBranches вылечен целиком (`for-each-ref`
  + различение `status === 1` + отсечка своей ветки по sha; самотест 23 → 36, шесть мутаций,
  A/B в обоих мирах рефлога) · граница unverified на перестроенном `проверитьТекст` (фикстура
  построена ЗАНОВО — старая на новом строении не работала; A/B на партии 08: код 0 при 14
  непроверенных из 24 → код 1 ровно одним отказом). 🔑 Его третий дефект, найденный своим же
  контролем: потолок `slice(0,4)` резал СЫРЬЁ раньше отсева новизны — **режь ОТВЕТ, а не
  материал** (живой контроль «0 из 8» → «1 из 21»).

### 3. Долги, названные честно

· **Пять уроков dev-3 в `EXPERIENCE.md` НЕ вписаны** (heredoc срывался на кавычках, тексты
  готовы вне репозитория) + секция замера в `bugs/134` (форма правила 8: замер с ЧУЖОГО стенда)
  + капкан порта 9100 в шапку `stand-slot.mjs` с авторством Интегратора.
· ✅ **Разнос ответов оплачен (сессия 43, 2026-08-29 вечером):** страж был КРАСНЫЙ на №063 В3/В4
  (правки владельца по записи «Версия 2.2» доехали до текста, а цитаты не доехали ни до одного
  документа) и держал протухшими статусы №063 и №064. Разнесено: `researches/06` — заходы 4 и 5
  с цитатами и доводом (запрет 6 портрета: предмет назван существительным) · `bugs/127` — судьба
  пласта стала ПОРЦИЕЙ с чеклистом условий · `researches/64` §9 — вариант А отмечен решённым,
  Б и В в работу не берутся. Попутно снята сирота-пометка `<!-- [/AI] -->` без открывающей пары
  в `researches/06` (никакой страж её не ловит: `canonArtifacts` в `.kaif/kaif.json` пуст).
  Долг разноса 26 → 24, `npm run questions` зелёный.
· **Порция инструмента удаления пласта `privat`** (№064 В1 = А): слепок ДО удаления обязателен
  (необратимость настоящая) · **перемер числа обязателен** — 331 это замер 2026-08-15 на базе
  `(default)` ДО переезда, не сегодняшнее число · deny в правилах + тест правил. ⚠️ Постановка
  этой порции и боевой прогон уперлись в предохранитель сессии Менеджера — нужно слово владельца
  либо его собственный прогон.
· **Мой долг от вердикта №29:** четыре строки допуска `tag-corpus` умирают по ответам владельца
  («ролевая игра» — №061 В4; по трём остальным расколам вопрос ему НЕ подан).
· Ретроспектива смены 11 не написана.

### 4. Очередь владельца

**№060** (макет главной — лицо продукта уровня топов; держит выкат корня, посев «Вот это драма!»
и правки Дизайнера; возврат Яндекса — «заодно», не цель) ·
**№061** · **№062** · **№057** · вопрос о подложке знака в тёмной теме (кадры поданы).

### 5. Тикает само

🔴 **Подача 150 адресов в Яндекс — ЕЖЕДНЕВНО** (остаток 1051), первое действие dev-3, СНАЧАЛА
сухой прогон на квоту (`yandex-recrawl.mjs` без `--apply`), подача только по «к отправке > 0».

## 🟢 ХОД СМЕНЫ 11 (2026-08-29, вечер) — команда поднята, релиз 2.2 подан владельцу

**Роли в строю все пять** (свежие окна), инструктажи по манифесту разосланы: dev-3 — Яндекс +
корзина №20–№22 · dev-1 — охват стража импорта + дочистка researches/55 · Интегратор — гигиена
questions (3 красных) + bugs/222 + отказ close-bug без секции критериев · QA — построчный суд
мастерской dev3-shift6 (та самая «отдельная смена», №8) · Дизайнер — перезакрепление доли меты
(разблокировано мержем dev-3 окном 10).

· **Релиз 2.2 НАЧАТ первой операцией:** черновик записи «Версия 2.2» — в `researches/06` под
  маркером `[AI]`, вычитка поднята страницей — **№063** (три строки: первая оценка = вход ·
  пополнение каталога · оптимизация; счётчик и главная сознательно не упомянуты — ждут №061/№060).
  После «принято»: снять `[AI]` → `/release` (бамп 2.2.0, тег, GitHub Release, выкат) — дату в
  записи сверить с днём фактического выката.
· **Яндекс, сутки 29-го закрыты сменой 10** (dev-3 сверил сухим прогоном: квота 0, остаток 1051;
  строка в bugs/203, коммит 21842f5). 🔑 Механика в постановку впредь: смена тех же суток сперва
  СПРАШИВАЕТ квоту сухим прогоном (`yandex-recrawl.mjs` без --apply), подача — только по
  «к отправке > 0»; первая настоящая подача смены 11 — 30-го.
· **Словарь доски дочищен до инструмента:** `--busy` печатал «🔴 занят» вопреки слову владельца —
  теперь «🔵 в работе» (team-status.mjs, селфтест 23/23). Прогон открытия смены:
  `verify-copy-live --live` — витрина сходится с базой, 4 поля × разница 0, снимок 0 дн.
· Переезд каталога `ndim_dev2` → `ndim_integrator` снова отложен: окна ролей открыты, а переезд
  законен только при закрытых (сказано Интегратору в инструктаже).
· 🔄 **Перезакрепление доли меты СНЯТО замером Дизайнера (48872f8): правка, рождающая «24»
  (79881a7, действительный залог в лексиконе), лежит в `dev3-shift6-20260823` — ветке МАСТЕРСКОЙ,
  а не в слитой `ndim_dev3`.** До/после мержа main числа совпадают до сотой (ru 19,67 · en 22,80,
  слепки git archive). Уезжают ОБА числа: ru 20 → 24 И en 23 → 27 — перезакрепление «по сообщению»
  поправило бы половину. Оба числа уже сняты в юнит блоком «⏳ следующее перезакрепление» (в ветке
  Дизайнера). **Новый замок, якорь коммитом:** перезакрепление — после того, как `79881a7` станет
  предком main (`git merge-base --is-ancestor 79881a7 main`), то есть после построчного суда
  мастерской и приведения её автором. 🔑 **Правило эстафеты (класс дважды за двое суток):
  условие разблокировки называет КОММИТ, а не имя роли/ветки** — у роли веток может быть две,
  и «после мержа ветки dev-3» открывается ключом от соседней двери; проверка предком — команда,
  а не память. (EXP-NEW в ветке Дизайнера, номер при мерже.)
· ⚠️ Всем ролям после обновления до main: `npm install` в своём worktree — main принёс
  зависимость `posthog-js`, отставшие node_modules роняют `check` (нашёл Дизайнер).
· ✅ **ВЕРДИКТ №27: мастерская dev3-shift6 ПРИНЯТА** (долг №8 оплачен; 27 файлов / 11 234
  строки построчно; голова main на вынесении 9c9148c; предмет 12, мерж понесёт 11).
  🔴 **Три условия к мержу + решения Менеджера, исполняет dev-3 при приведении руками:**
  (1) резолв конфликта ворот приёмки обязан сохранить ОБЕ половины защиты bugs/183 — после
  резолва оба grep-контроля из №27 дают 1 (иначе откат вердикта №21 без единого красного);
  (2) страж tag-corpus въезжает с ОБЪЯВЛЕННОЙ границей по идиоме CORPUS_FLOOR — четыре
  известных раскола поимённо (корпус в ветке побайтно тот же; правка данных — слово
  владельца: «ролевая игра» уже в №061 В4, вопрос по трём остальным — в очередь вычитки
  ПОСЛЕ мержа, мерж не держит). 🔑 Форма границы — уточнение QA, принято: Set из ИМЁН, не
  число, красный В ОБЕ СТОРОНЫ (новый раскол вне списка → красный; имя из списка, ушедшее
  из корпуса → красный «вылечено — снять допуск, назвав кто и когда») — допуск самоубывающий,
  число стало бы индульгенцией и глушило бы стража ровно на новом расколе; обе мутации —
  в самотест dev-3, QA перепрогонит их приёмкой; (3) перезакрепка доли меты — ЗАМЕРОМ прибора на слитом
  дереве (живые числа ru 24 · en 27, сверены с независимым замером Дизайнера 48872f8;
  ⛔ числа из комментария теста 26/33 не даёт ни одно дерево — не переписывать).
  ⚠️ Порядок dev-3 переставлен: приведение мастерской — ПЕРЕД корзинными пунктами про
  ворота приёмки (иначе двойной конфликт со старым строением). Очередь судов QA дальше:
  заявка dev-1 (ждёт его новую голову после подтяжки main).
· 🔴 **ВЛАДЕЛЬЦУ ОТДЕЛЬНОЙ СТРОКОЙ (находка dev-1, подтверждена QA):** удаление аккаунта
  кнопкой продукта НЕ удаляет `users/{uid}/privat/{uid}` (перечень erase.ts коллекцию не знает);
  сеть безопасности ловит ушедшего только по живому `relations/{uid}` — удаление персональных
  данных по требованию человека выполняется не полностью. Судьба пласта — интервью **№064**
  (страница поднята); при ответе А щель закрывается вместе с пластом, при Б — отдельной порцией.
· ✅ **ШАГ 1 ОКНА СМЕНЫ 11 ВЛИТ: main = 4729b6f** (заявка dev-1 целиком: plans/79 + страж
  импорта с охватом + разведка №064; EXP-0244/0245 присвоены; test:tools 430 → 439). Механика
  плацдарма повторена: Интегратор собирает `window-shift11`, Менеджер принимает ff-only и пушит.
· ✅ **ВЕРДИКТ №29: условия мастерской закрыты, «в мерж» шаг 2 дан** (f683f48; судья вернул
  дефект bugs/183 в исходник — краснеют ДВА юнита, обе половины защиты живы; допуск tag-corpus
  оказался Map «имя → причина и когда снять» — сильнее заказа; контроль шага: после мержа
  test:tools 540/540, guards 13/13). 🔴 **Живой долг Менеджера от №29:** четыре строки допуска
  tag-corpus обязаны УМЕРЕТЬ по ответам владельца — «ролевая игра» №061 В4, по трём остальным
  расколам подать вопрос ПОСЛЕ мержа; страж напомнит красным «ВЫЛЕЧЕНО» только чинящему корпус.
  Замечание автору мастерской (не держит): поведенческий замер девяти партий на живом Wikidata
  судья не воспроизводил — остаётся за автором.
· ✅ **ВЕРДИКТ №28: заявка dev-1 ПРИНЯТА, условий ноль — «в мерж» дан** (голова 10cabd4,
  довесок досужен; доказательство «правка есть перемещение» усилено судьёй: порядок операторов
  подпоследовательностью, не счёт строк). Правило «мутанта обезвреживают в исходнике» — строкой
  в TESTING_FRAMEWORK (микро-порция dev-1 после перезапуска ветки).
· 🆕 **Постановка named, без порции (находка Дизайнера):** страж «документ объявлен неначатым,
  а работа есть» находит работу ПО ССЫЛКАМ ВСЕГО РЕПОЗИТОРИЯ, а документ читает из ДЕРЕВА
  вызывающего — отставшая копия получает красный от чужого коммита и ищет причину у себя (сегодня
  стоило бы времени пятерым вместо одного; в main при этом 13/13). Кандидаты лечения: судить
  достижимое из HEAD вызывающего ЛИБО красный называет находку чужой и ворота сдающему не
  закрывает. Замер ширины — до признака.
· **Микро-заявка dev-1 в очередь судов:** правило 9 (EXP-0244) в TESTING_FRAMEWORK, голова
  8549253, 2 коммита, docs-only; граница честности внутри правила («случай назван по источнику,
  а не наблюдался автором») — снимается, когда ветка QA войдёт в ствол.
· **Очередь судов и мержей смены 11** (мержи по одному, после вердикта и «в мерж» на каждый):
  1) заявка dev-1 — 🔄 голова **51676fd**, 7 коммитов, суд ИДЁТ (добавились резолв реестра +
  **порция plans/79 ИСПОЛНЕНА dev-1**: дверь выката под предохранителем, 32 → 3 верхнеуровневых,
  A/B побайтово, юнит двери 5/5, отсрочек в реестре НОЛЬ; его инцидент мутанта — урок
  «обезвреживай в исходнике мутанта» — записан в plans/79; живой прогон smokeGate --stage
  --gate-only — ближайшим штатным окном стейджа, т.е. релизным выкатом 2.2) → 2) мастерская dev3-shift6
  — ✅ ПРИВЕДЕНА (голова f683f48, понесёт 11, симуляция бесконфликтна; условия №27 закрыты
  проверяемым выходом: grep-контроли + ПОВЕДЕНЧЕСКИЙ замер девяти партий до единицы · Set-допуск
  в обе стороны с обеими мутациями QA · доля меты 24/27 тремя независимыми замерами; второй
  нормативный ответ sitelinks СНЯТ резолвом-разбором; ждёт перепрогона условий QA) → 2а) НОВАЯ
  заявка: корзина dev-3 (голова c27cbb6, понесёт 3, с мастерской не пересекается; stash-пункт
  границы unverified — после мержа мастерской, на строении проверитьТекст) → далее по номерам
  (приведение автором по условиям №27) → 3) порция Интегратора (e0d131f: два признака круговых
  доводов + отказ close-bug без секции + 13 поправок bugs/222 + разводка дублей EXP-0086/0087;
  его EXP-0244/0245 сняты в плейсхолдеры — номера при мерже по порядку) → 4) ветка Дизайнера
  (🔄 голова теперь **192133c**: + bugs/182 закрыт замером с обоих концов — коммит в main И слово
  фикса в боевом чанке; + 🔴 КЛАСС ДНЯ «прибор молча мерит ЧУЖОЕ дерево» — третий род ложного
  зелёного (проверка исполнилась, признак верен, ПРЕДМЕТ другой): из голой оболочки stand-addresses
  падал на слот 0 главной копии; лечение — умолчание, совпавшее с чужим слотом, КРИЧИТ (не
  отказывает: на слоте 0 дверь выката байт-в-байт); правка общего инструмента ждёт согласия
  Интегратора-хозяина; макеты №060 + съёмщик + юнит-блок доли меты + bugs/187-доказательство
  с двумя дефектами прибора + bugs/123 закрыт; 🔄 решение Менеджера: суд НЕ ждёт ответа №060 —
  правки выбранного макета поедут ОТДЕЛЬНОЙ порцией поверх; ⚠️ конфликт с мастерской в
  tools/meta-share.test.mjs: перезакрепка dev-3 против блока «⏳» Дизайнера — числа совпадают
  (24/27), резолв = блок «⏳» гасится состоявшейся перезакрепкой, Интегратору смоделировать).
  **Карта мержей смены 11 заказана Интегратору** (образец shift10,
  моделирование git merge-tree — форма QA из №27). bugs/222 намеренно открыт до вердикта —
  закрытие close-bug'ом станет первым живым прогоном новых признаков.

## 🔴🔴 ЭСТАФЕТА СМЕНЕ 11 — с чего начинать (смена 10 закрыта 2026-08-29 вечером по слову владельца «закрывай смену»)

**ОКНО МЕРЖЕЙ СМЕНЫ 10 ИСПОЛНЕНО:** четыре ветки (dev-3 21 · Интегратор 16 · dev-1 27+2 ·
QA 21 коммит) + шаг 10 (починка claimsGate) слиты в main; ворота выросли **7 → 13**; номера
присвоены (EXP-0230…0243+, bugs/219–223, bugs/KAIF/04, researches/61–63). 🔑 **Механика
окна узаконена опытом:** main живёт в копии Менеджера (git не даёт ветку в два worktree) —
мержи Интегратор исполняет на ПЛАЦДАРМЕ (`window-shift10`), Менеджер принимает `ff-only`
и пушит; ствол заморожен от чужих коммитов на всё окно. Карта окна и «что вышло на
исполнении» — `reports/TEAM/2026-08-29_merge_map_shift10.md`.

### 1. 🔴 РЕЛИЗ 2.2 — первая операция смены (вопрос владельца: «может уже накопилось?» — ДА)

С бампа 2.1 (`da7027c`, 2026-08-14): **883 коммита · 990 файлов · +204 749 строк**. В бою с
тех пор: дверь карточки + воронка v2 (выкат `b81u35`) · счётчик посещений · машинный переобход
Яндекса · вся машинерия смен 4–10. Канон релиза — по прецеденту 2.1 (№027): **черновик
истории версий → вычитка владельца страницей → `/release`**. Не начат сменой 10 намеренно:
история версий требует его вычитки, а смена закрывалась.

### 2. Очередь владельца (страницы подняты/в очереди, `npm run questions`)

· **№060 «Главная с содержанием: выбор макета из четырёх»** — ГЛАВНОЕ: у продукта появляется
  ЛИЦО уровня топов (`researches/60`, супер-микс лучших решений), и вся работа корня ждёт
  Вашего выбора. ⚠️ Возврат Яндекса — ПОБОЧНОЕ следствие, «заодно», а не цель: слово владельца
  2026-08-29 — «*сама цель не яндекс лечить, это лишь за одно*»; целимся в лучшие решения.
  После выбора: правки Дизайнера → суд его ветки
  (`2fac212`: макеты + съёмщик + прибор кропов + поправка замера карточек + набор знака) →
  мерж → **посев «Вот это драма!»** (Менеджер, из main) → перезакрепление доли меты.
· **№061 «Пачка текстов лица»** (5 вопросов, включая «править ли одобренное») ·
  **№062 «Три ответа на встречные вопросы»** (x-default по слову Google · четыре возможности
  счётчика · знак с кадрами) · **№057** (инструмент развёрток, с прошлых смен).
· Ответы №059 разнесены: В1 = А исполнен окном; В2–В4 проработаны в №062.

### 3. Пост-оконные порции (розданы вердиктами смены 10, исполнять после релиза)

· **Менеджер:** порция двери выката `plans/79` (приоритет поднят замером: fetch на верхнем
  уровне) · ✅ решение по чекбоксовому перекосу ИСПОЛНЕНО Интегратором — отказ close-bug при
  документе ВООБЩЕ без секции критериев. ⚠️ Число «63 из 83» было снято на прежнем дереве;
  перемер на `3ccc380` даёт **55 из 85 открытых** (и 124 из 139 закрытых).
· **Интегратор:** 🟡 **ЗАЯВКА В СУД QA — `bugs/222` + отказ close-bug, одной порцией.**
  Перепроверка 13 закрытий: **держатся ВСЕ ТРИНАДЦАТЬ, переоткрытий ноль** (против ожидания
  самого документа) — каждое лечение найдено в дереве чтением кода либо прогоном, улика вписана
  датированной поправкой, круговая строка оставлена дословно как улика класса. 🔴 Главное
  вскрывшееся: **признак ЯКОРЯ один класс НЕ закрывает** — 4 круговых довода из 13 цитируют
  якорь ИЗ ШАПКИ и проходят; признаков поставлено ДВА (КРУГ 13/13 при 0 ложных · ЯКОРЬ 10 при
  1 ложном — `bugs/183`, цена объявлена, не заглушена). Самотест 20 → **33/0**, четыре живые
  мутации красные каждая на своём случае. ✅ **ВЕРДИКТ №32: принята, дефектов нет, условий
  ноль** — оба признака мутированы порознь, независимость доказана в обе стороны, замер судьи
  сошёлся с моим до имени; влита шагом 5 окна. Уроки получили номера ЭТИМ мержем: **EXP-0248**
  (признак слеп к ЦИТАТЕ содержания) и **EXP-0249** (мутация без проверки применения). ·
  переезд каталога `ndim_dev2` → `ndim_integrator` (только при ЗАКРЫТЫХ окнах ролей) ·
  ✅ **дубли EXP-0086/0087 СНЯТЫ датированной поправкой** (смена 11): номера НЕ переприсвоены
  (у `EXP-0087` пятнадцать живых ссылок), у всех четырёх записей первой строкой стоит разводка
  с замером, кто на кого ссылается; в шапку журнала внесена причина — **номер брался по ЧИСЛУ
  записей вместо МАКСИМУМА** (заголовков 244, номеров 242, максимум 0243, пропущен 0018) плюс
  команда самопроверки. 🔴 Цена столкновения замерена, а не предположена: две живые ссылки на
  `EXP-0086` указывают на РАЗНЫЕ записи (`researches/25:1519` ↔ `PROJECT_HISTORY.md:3542`).
· **dev-3:** корзина №20–№22 (охват в выводе переписи · граница unverified в воротах приёмки
  идиомой CORPUS_FLOOR · предупреждение сверхширокого запроса where-cured · вторая строка
  честности --branches) · класс «маркер про будущее устаревает молча» — усилен QA (№26):
  устаревает даже при ИСПРАВНОМ приборе; правило — **маркер описывает СОСТОЯНИЕ, а не
  намерение**, план живёт в реестре отсрочек со сроком годности, не в поле-факте.
· **dev-1:** охват в выводе стража импорта (№20) · сниппеты хабов (`researches/57`) — по «го»
  владельца · лестница PostHog Ш5+ после ответа №062 В2.
· **QA:** построчный суд мастерской `dev3-shift6-20260823` (20 файлов) — ОТДЕЛЬНОЙ сменой,
  свежим окном (решение №8 и смены 10).
· Плейсхолдеры `[[EXP-NEW-…]]` в стволе ждут мержа ветки Дизайнера (номера не занимать).

### 4. Тикает само

🔴 **Подача 150 адресов в Яндекс — ЕЖЕДНЕВНО** (остаток очереди 1051): первое действие dev-3.
**Прогон `verify-copy-live --live`** — Менеджер при открытии смены (прибор смотровой, в
воротах не стоит — решение смены 10). Хвосты выката `b81u35`: чтение `door_click`/
`guest_start` ≈ 05.09 · дата шва ряда в `bugs/204`.

### 5. 🆕 Словарь статус-доски — слово владельца 2026-08-29 (разнесено: манифест + инструмент)

Рабочий — **🔵 в работе** · свободен — 🟢 · прощальный при закрытии смены — **🔴 оффлайн**
(ставит каждая роль и Менеджер; `team-status.mjs set --offline`).
🆕 **Четвёртое состояние, слово владельца тем же днём позже:** «*жду, заблокирован и жёлтый круг
– агент ждёт чегото от коллег*» → **🟡 жду, заблокирован** (`set --blocked`), чего именно — в
колонку «Жду». 🔑 Уточнение того же дня — «*жду, заблокирован - такое на доске писать*»: на доску
идут ОБЕ части. Первая редакция взяла одну и была неправа; **слово владельца не сокращается
агентом**, и проверка в самотесте стоит на строгом равенстве, а не на вхождении подстроки —
иначе она пропустила бы ровно эту ошибку.
Разнесено: манифест (таблица словаря + довод «круг читается первым, колонка второй») · доска
(легенда) · инструмент. Словарь стал ОДНИМ местом в коде (`BOARD_STATES`), цепочка тернарников
снята; два состояния одной командой — отказ с названными флагами. Самотест 23 → 28, обе новые
проверки доказаны мутациями (жёлтый круг подменён синим → 2 красных · отказ снят → 1).

## 📜 ЭСТАФЕТА СМЕНЕ 10 — ИСПОЛНЕНА сменой 10 (оставлена до стрижки; смена 9 закрыта 2026-08-29 11:00)

**Ретроспектива смены 9 написана:** `reports/TEAM/2026-08-29_shift9_retrospective.md` — прочитай
её ПЕРЕД раздачей задач, там шесть классов и таблица шести опровержений Менеджера замером.
✅ **Долг ретроспектив 7 и 8 ОПЛАЧЕН сменой 10** — написаны вдогонку с честной рамкой
«чего не знаем»: `reports/TEAM/2026-08-28_shift7_retrospective.md` · `…_shift8_…md`.

**ГОЛОВЫ ВЕТОК НА ЗАКРЫТИИ** (сверь `git branch -v`, роли могли дослать):
`ndim_designer` bca1a05 (приведена к main) · `ndim_qa` e9a0c2d · `ndim_dev1` af4dcd0 (запушена) ·
`ndim_integrator` 57ab8db · `ndim_dev3` dfcb2ba · `dev3-shift6-20260823` 2415e52.

### 1. 🔴 ПЕРВЫМ ДЕЙСТВИЕМ — МЕРЖИ, до любой другой работы

Класс `EXP-0227` доказал цену очереди: страж, лежащий в неслитой ветке, не защищает, но
числится работающим. Порядок — **сначала порция А dev-3, потом Интегратор** (тогда первый прогон
ворот после второго мержа закроет переходящее обязательство одним заходом).

⚠️ **Мержи упираются в право сессии Интегратора на `git merge` — оно закрыто классификатором,
вопрос у владельца (интервью №059 В1).** Ни он, ни Менеджер запрет не обходили. Без ответа
владельца мержи не идут.

🔴 **РЕШЕНИЕ, ПРИНЯТОЕ ЗАРАНЕЕ, ЧТОБЫ ЕГО НЕ СЛОЖИЛИ ПО ИНЕРЦИИ (находка QA):** после мержа
порции А в воротах окажутся ДВА прибора одного класса — `verify-role-env-scope.mjs` (main) и
`verify-role-env.mjs` (порция А). Первый строго поглощает второй и сверх того краснеет на
НЕКЛАССИФИЦИРОВАННОМ имени. **При мерже более узкий `verify-role-env.mjs` СНИМАЕТСЯ, а не
складывается** — два нормативных ответа на один вопрос это будущий разъезд.

### 2. Что ждёт владельца — интервью №059 (страница поднята)

Четыре развилки: право мержа Интегратору · язык по умолчанию для двуязычной главной · выключить
четыре опции приватности на стороне проекта PostHog · знак в тёмной теме.
Страница: `test-results/owner-reviews/interview_059_shift9_four_forks.html`.
Плюс не отвеченное с прошлой смены — **№057** (инструмент развёрток).

### 3. Названные порции, готовые к раздаче

· 🔄 **Карточки будущего времени — замер ОПРОВЕРГНУТ перемером Дизайнера (смена 10):** из
  прежних «10 из 10» четыре обвинения ложные (будущее время про сюжет или порт), три настоящих
  замер пропустил (`\b` слеп к кириллице). Верно: осмотрено 130 · настоящих **9** · точность
  признака 35 % — судьёй в ворота НЕ ставить, класс держится ситом с уликами
  (`candidates/owner-critical-notes-2026-08-22.md`, верная таблица; прежняя рядом зачёркнута).
  🔴 **Все девять — в ПАРТИИ 1, одобренной и запрещённой к правке** («партию 1 уже не
  трогаем») → вопрос владельцу изменился: «править ли одобренное» — подан в **№061 В5**.
  Вне партии 1 класс закрыт: «Вот это драма!» переписана в ветке Дизайнера (`e6e81d2`).
· **Посев «Вот это драма!» в бой** — ⚠️ УТОЧНЕНИЕ СМЕНЫ 10 (2026-08-29): правка готова
  В ВЕТКЕ ДИЗАЙНЕРА (e6e81d2, «текст переведён в прошедшее время»), в main карточка ВСЁ ЕЩЁ
  в будущем времени (`выход которой назначен`). Посев из main пересеял бы неисправленный текст —
  класс EXP-0227. Порядок: мерж ветки Дизайнера → посев Менеджером из main (боевая база),
  замечание владельца при посеве уцелеет.
· **Критерий 2 `bugs/209`** — объявленная нижняя граница корпуса в страже метки + случай в
  самотест. ✅ Поправка 2026-08-29 (смена 11): критерий исполнен и `bugs/209` ЗАКРЫТ сменой 10
  (`209_DONE_`, шапка «закрыт 2026-08-29») — прежняя строка «оставлен открытым намеренно»
  пережила своё исполнение в тот же день (нашёл dev-3 при перемере трёх строк).
· **Проверка чекбоксов в `close-bug.mjs`** — «есть неотмеченные критерии → отказ с перечнем».
  Случай 209 показал, что доказательства, `PENDING:` и имени мало.
· **Вписать `verify-bug-doc-state.mjs` в `npm run guards`** — но сперва убедиться, что красных
  ноль. ⚠️ Страж судит РАБОЧЕЕ ДЕРЕВО запустившего: один документ бывает сиротой в одной ветке
  и не сиротой в другой. И ещё: после ближайшей стрижки бонсая часть ссылок переедет в летопись,
  и ноль сирот перестанет быть нулём — перемерить.
· ✅ **Перепись приборов ПО МАРКЕРУ В ШАПКЕ — СДАНА dev-3 сменой 10** (голова 5c5b5ed, ждёт
  суда): `verify-tool-markers.mjs` девятым в guards, 49 шапок размечены писарем, EXP-0194
  ловится механически В ОБЕ СТОРОНЫ (заявил ворота и не стоишь — красный; стоишь и молчишь —
  красный). ⚠️ Числа прежней редакции этой строки («приборов 80, ворот 8») были смесью
  контуров — поправка dev-3 замером: в main ворот СЕМЬ, приборов 80; после всех мержей
  состав ворот — ДЕВЯТЬ. Отсрочка verify-role-env — со сроком годности (строка сама краснеет
  при уходе файла из состава при мерже).
· **Предохранитель «запущен или подключён» у `tools/team-status.mjs`** — доски, которую зовут все
  пять ролей. Плюс вынести `stampNow` в `tools/lib/`.
· **Адреса «этот класс у нас уже вылечен вот здесь» в проекте не существует** (находка dev-3):
  лечение живёт в шапке чужого модуля, греп журнала по тегам его не находит. Это постановка, а
  не урок.
· **Замер Ш2 дал тройной ответ** (dev-1): объект НЕ потерян, он лежит там, куда человека не
  привели — разрыв навигационный, лечится показом или дорогой, а не сохранением.
· 🔄 **Утверждений об одном факте на первом экране гостя не три, а ПЯТЬ** (уточнение QA,
  вердикт №14; кадр `test-results/live-door-object/3-first-screen.png`): спорят не экраны, а
  ОДНА карточка сама с собой — profile:720 «не сохранены» против её же :728 «останется Вашим»
  и :748 «удалим эти данные»; пятое — delete-account:230 противоречит себе в одном
  предложении. ✅ ПОДАНО владельцу: кандидаты №061 В1/В2 правят ровно эту карточку.
· **Четвёртый скол корпуса тегов** («ролевая игра», 2 против 1) — числами не решается, владельцу.
· **Перезакрепление доли меты** — за Дизайнером, СРАЗУ ПОСЛЕ мержа ветки dev-3, не раньше.

### 4. Тикает само

🔴 **Подача 150 адресов в Яндекс — ЕЖЕДНЕВНО.** Остаток очереди 1051, ~7 дней; пропуск дня
очередь останавливает. Первое действие dev-3 каждой смены.

### 🏁 К ОКНУ МЕРЖЕЙ ГОТОВО ВСЁ (вердикты №14–№25, очередь судов ПУСТА)

**Покрытие трёх веток ЗАКРЫТО ПОЛНОСТЬЮ — ни одного непокрытого предка, ни одного условия:**
`ndim_dev1` 9d7ba2b (понесёт 27, вердикты №14/№16/№18/№24) · `ndim_dev3` 014202c (понесёт 21,
№10/№12/№15/№19/№20/№21/№22) · `ndim_integrator` 285c3b2 (понесёт 16, №11/№13/№17/№19/№23/№25).
Карта исполнения — `reports/TEAM/2026-08-29_merge_map_shift10.md` (ветка Интегратора).
✅ **СЛОВО ВЛАДЕЛЬЦА ПОЛУЧЕНО — №059 отвечен страницей 2026-08-29 14:11, все четыре:**
**В1 = А** — право мержа Интегратору ОТКРЫТО (разнесено в манифест, окно ИСПОЛНЯЕТСЯ по
карте) · **В2 = В** («Как делают топы?») и **В3 = В** («может это полезные фичи? что дают?»)
— встречные вопросы, проработка с данными подана в **№062** (x-default: дословная строка
документации Google — «designed for language selector pages»; четыре возможности счётчика —
разбор пользы и цены по инварианту №002 В4) · **В4 = В** («могли бы скриншот приложить») —
упрёк принят, вопрос переподан С КАДРАМИ знака в №062; урок формы: визуальный вопрос без
картинки не задаётся (класс памяти «экран записывает сам агент»).

**ОБЯЗАТЕЛЬСТВА ОКНА (из вердиктов, исполнителю окна читать вместе с картой):**
1. При мерже порции А — снять строку verify-role-env из состава ворот; отсрочка в переписи
   dev-3 обязана ПОКРАСНЕТЬ и быть снятой тем же движением (бесплатная проверка её честности).
2. Первый прогон ворот после мержа порции А закрывает ДВА стража (verify-role-env-scope +
   console-snapshot --check-age), не три (поправка QA к №13).
3. После мержей вписать в guards стражей правила «ноль красных»: спящие объявления +
   состояние bugs/ (станет 9) и verify-import-safety.
4. 11 записей EXP-NEW нумеровать ПОДРЯД по порядку мержей; предзанятому EXP-0228 не доверять.
5. **Головы ВСЕХ ролей КУМУЛЯТИВНЫ** («смержить последнюю заявку роли» = смержить ВСЮ её
   ветку: dev-3 21 коммит · dev-1 27 · Интегратор 16). 🔊 Просьба QA: прочитать этот пункт
   ВСЛУХ перед первым мержем — единственное место, где уход от карты стоит дороже всего.
   Для досуда в моменте QA держит наготове помощник стенда и формулу охвата — присылать
   голову и «дерево чисто», начнёт сразу.
После окна: посев «Вот это драма!» (из main) · перезакрепление доли меты (Дизайнер) ·
пост-оконные корзины dev-3 (4 пункта) и dev-1 (охват) · порция двери `plans/79` (Менеджер) ·
отказ close-bug без секции критериев (Интегратор) · построчный суд мастерской — отдельной
сменой (№8).

### 🔥 ПОЖАР MAIN — ПОТУШЕН ЗА ОДИН ТАКТ (2026-08-29, день)

Страж «строка пережила своё исполнение» покраснел на `plans/79`: Менеджер завёл план ОДНИМ
коммитом с правкой STATUS.md, а сводка в корне не входила в «планирование» — запись в сводку
о плане считалась исполнением плана. Нашла QA (эскалация по манифесту: красный main — прежде
любой работы), с честным «мой первый замер был ложным» — плоский слепок без подтянутой
истории дал ЛОЖНОЗЕЛЁНЫЙ на страже, спрашивающем git (четвёртый класс её стенда за смену,
самый ядовитый: перевернул ответ, а не зашумел). Настоящая причина оказалась третьей — не
«коммит создания» (он отсекается условием (2) и так): класс «СВОДКА — учёт работы, не
работа». Правка закона, не исключения (`a9c9b25`): корневые .md приравнены к планированию,
предикат `исполнение()` вынесен и покрыт 8 случаями; живая мутация — красный до правки на
том же дереве, зелёный после; ворота 7/7.

### 🟢 ХОД СМЕНЫ 10 (2026-08-29, полдень) — команда поднята, владельцу поданы три страницы

**Роли в строю все пять** (свежие окна), инструктажи по манифесту; ветки НЕ сброшены — во
всех несмерженная работа (впереди main на 5–21 коммит), дельта канона названа поимённо.

· **Владельцу:** №059 ОТКРЫТ страницей с сигналом (мержи всей смены стоят на его В1) ·
  **№060 «Главная с содержанием: выбор макета из четырёх»** и **№061 «Пачка текстов лица
  продукта»** — в очереди вычитки (артефакты макетов — 26 файлов со sha256-сверкой — в
  `test-results/owner-reviews/root-minilanding/`). Макеты поданы БЕЗ вердикта QA осознанно:
  выбор варианта — не мерж; суд ветки Дизайнера остаётся перед влитием.
· **QA — ВЕРДИКТ №14: порция dev-1 (PostHog Ш1–Ш4 + Ш2, судилась на 90cf7c1) ПРИНЯТА с
  замечаниями, мерж отперт.** 🔴 Находка, державшая ВЫКАТ: SDK счётчика въезжает в сборку
  двери карточки (+261 КБ) в момент ОЦЕНКИ — граница «каталог не получает трекер» опровергнута
  собственной сборкой в трёх местах; на ПОКАЗЕ не грузится (script-тегов 0), утечки предмета
  оценки нет (событие без свойств). **Решение Менеджера: границу привести К ПРАВДЕ** («на
  показе не получает; оценка с двери считается») — врёт текст, не код; правит dev-1 вставкой,
  тексты Политики/витрины поедут владельцу с Ш8 уже честными. Пять дефектов вдогонку — беклог
  dev-1 (главный: `capture()` не покрыта юнитом — три мутации в теле зелёные). Переходящее
  обязательство: первый прогон guards после мержа — на дереве с qa-journeys-lint И стражами
  порции А. **ВЕРДИКТ №17: порция стражей Интегратора (a503f85) ПРИНЯТА, ДЕФЕКТОВ НОЛЬ** —
  слепое пятно П1 замерено НЕЗАВИСИМО двумя инструментами (QA и автор) с полным совпадением:
  44 ключа, 2 мертвеца, оба в gateCopyPhase4; слово владельца после вычистки цело сплошной
  сверкой (из account удалён мёртвый ДУБЛЬ, живые строки в DeleteAccount.svelte); довод за
  вычистку усилен: мёртвая копия звала вкладку, которой нет, — текст был не спящим, а
  УСТАРЕВШИМ. З1 Менеджеру: вычистка унесла два комментария-УРОКА (намерение №007 В10 +
  урок «пустота молчит») — лечение микро-порцией Интегратора (опт-аут-маркер с причиной по
  образцу LIVE_BY_NAME + переселение уроков в живые дома). 🆕 Правило в манифест (практика
  Интегратора, поддержана QA): заявка на мерж — с ЧИСТЫМ деревом и головой, названной
  автором; в шапке вердиктов теперь печатается голова main на момент вынесения.
  **ВЕРДИКТ №15: критерий 2 bugs/209 ПРИНЯТ, дефектов нет, условий ноль** — довод
  «граница, а не равенство» подтверждён СИМУЛЯЦИЕЙ ПОСТ-МЕРЖА (подложены приборы dev-1:
  корпус 15 → 16, граница смолчала; равенство покраснело бы на следующей же порции очереди);
  паритет перемерен на текущем main 15/15. Хвост №15 (не держит мерж): слово «переименование»
  в 4 местах шире факта — точная форма улики правится dev-3 датированной поправкой до мержа.
  🔑 Процессная находка QA (третье попадание EXP-0226 за смену, теперь в судью): канон
  ДВИЖЕТСЯ под сессией, а не только увядает в ней — норма перечитывается перед ВЫНЕСЕНИЕМ,
  не перед началом; оформляется её уроком. Очередь судов дальше: предохранитель доски dev-1.
· **dev-3 — критерий 2 `bugs/209` ЗАКРЫТ и документ закрыт целиком** (голова 7aa5972 поверх
  нетронутой порции А): поимённый список стража метки закрывал 3 прибора из 15 — заменён
  объявленной нижней границей корпуса (CORPUS_FLOOR = 15, довод «граница против равенства»
  в шапке константы), 4 живые мутации красные, самотест 16 → 20, граница замерена на обоих
  деревьях (15/15 поимённо). ⚠️ Закрывал инструментом close-bug ИЗ ВЕТКИ ИНТЕГРАТОРА по
  абсолютному пути — в main и остальных деревьях инструмента НЕТ (ещё один случай EXP-0227
  и довод за приоритет его мержа); чекбоксы отмечены рукой, порция «проверка чекбоксов» —
  у Интегратора. Обновление его ветки до main — ПОСЛЕ мержа порции А (слово Менеджера).
  Хвост №15 закрыт (`707d4c0`: точная форма улики «переименование» — датированной поправкой
  в закрытом bugs/209, история не переписана). **Пересверка партий по основанию bugs/183
  (заказ Менеджера, только замер): настоящих новых заимствований НОЛЬ** — 247 половин, порог
  10 слов; партии 1–4 чисты (3/100 сверялись не с тем, находок 0) → №061 В5 нового материала
  НЕ получает; единственное совпадение (Reanimal, 14 слов) разобрано до не-находки — перечень
  трёх названий игр, имена собственные. Побочная: третья форма класса «служебные слова из
  английских названий» (looksLikeList) — заводится багом БЕЗ порции (1 случай на 247, правило
  требует своего замера). Риск сидел в свежих партиях 05–06 (26 из 34 неверных сверок). **Заявка №2 — перепись приборов по маркеру —
  СДАНА** (`5c5b5ed`, в очереди судов; подробности в §3 эстафеты выше); попутно EXP-0229
  (мутационный помощник откатывал КАТАЛОГ вместо файла — отравленные доказательства пойманы
  по числу до сдачи). Следующее у него: `bugs/181` + `bugs/183` (ворота мастерской — жадная
  регулярка кавычек · сверка с чужой статьёй), с замером ширины до фикса.
· **Дизайнер:** артефакты скопированы · съёмщик ЗАКРЫТ (`604bdba`: третья ширина штатно,
  условно по наличию `#width-mid`; попутно вылечен молчаливый класс «клик без проверки
  результата», мутация доказана) · порция «девять карточек» СНЯТА его же перемером — все
  девять в запретной партии 1, вопрос владельцу в №061 В5 (подробности строкой выше в §3).
  Стоит свободен, правки макетов — вперёд всего.
· **dev-1 — ВЕРДИКТ №16: предохранитель доски + stampNow ПРИНЯТ, условий ноль** (фабрика
  ложнозелёного воспроизведена судьёй дословно: assert.equal(1,2) → pass на старом приборе;
  м2 ловится только его юнитом; форм пути прогнано восемь — все живы). Один дефект в его
  беклог: смещение пояса в юните закреплено только ФОРМОЙ ([+-]\d{2}:\d{2} — «знак всегда +»
  зелёный); гипотеза «виноват материал» опровергнута тремя поясами. Долг класса числом: форму
  «CLI на верхнем уровне» несут 14 файлов tools/, предохранителя нет у 8, вылечен один —
  покрыто его постановкой ideas/43.
· **dev-1 — ideas/43 СДАНА ЦЕЛИКОМ** (голова 6a778f1, запушена, в очереди судов третьей):
  страж `verify-import-safety` (282 осмотрено · 60 с экспортом · красных 14 → 4) + 10 лечений
  мелкими коммитами + обе вставки доводов к правде (граница трекера №14 + `bugs/214`).
  Его прежний греп ошибся в 7 из 13 — поимённый разбор в заявке (шаблонные строки генераторов ·
  «предохранитель есть, работа снаружи»). 🔴 Находка крупнее прочих: `verify-stage-clean` при
  ИМПОРТЕ поднимал соединения к боевой и стейдж-базам — вылечен, живой прогон Менеджера 14/0.
  Решения Менеджера по 4 оставшимся красным: deploy.mjs — отсрочка до ОТДЕЛЬНОЙ порции двери
  (selftest + gate-only + суд) · contours.mjs и sync-server/index.mjs — законные исключения
  (несущий инвариант на импорте · входная точка контейнера) · verify-role-env — отсрочка до
  мержа. EXP-0188 переведён в mechanized. ⚠️ **ideas/43 реализована ДО одобрения владельца** —
  расхождение с каноном идей названо честно в самом документе и доложено владельцу в чате;
  его «нет» откатывает порцию до мержа дёшево. Старый довод __data.json дочищается в
  researches/55 (датированной поправкой) и сноской в отчёте аудита — микро-задача dev-1.
· **ВЕРДИКТ №18: ideas/43 dev-1 ПРИНЯТА, дефектов ноль** — лечения подтверждены всеми
  десятью CLI и семью безопасными импортами. 🔴 Суд вскрыл ВТОРОЕ ИЗМЕРЕНИЕ класса EXP-0188:
  verify-stage-clean до лечения не «поднимал соединения», а ЧИТАЛ БОЕВУЮ БАЗУ на верхнем
  уровне каждым импортом — класс «непреднамеренная дверь к боевым данным владельца из
  инструментария любой роли»; дописка в журнал + замер цены (сеть/ключи против просто
  работы) — в порцию dev-1. Живой обход формы исключения ({причина: 'так надо'} глушит
  красного) — признак содержательности причины в порцию dev-1 с замеренной ценой. Правило
  канона маркеров ([TESTED] называет, ЧЕЙ прогон и из какого контура) — оформляет QA своей
  веткой. 🔴 Порция ПРЕДОХРАНИТЕЛЯ ДВЕРИ ВЫКАТА — за Менеджером, первой после окна мержей:
  доводом суда самая опасная дверь осталась единственным непочиненным носителем класса.
· **Разведка dev-3 «адрес вылеченного класса» — механизм ЕСТЬ, но направлен в обратную
  сторону:** код называет уроки (79 из 232, 286 упоминаний), уроки код — почти никогда
  (6 из 232), а канон шлёт грепать журнал — пустое направление. Пять живых случаев с ценами.
  Решение Менеджера: Ш0 (строка маршрутизации в AGENT_GUIDE: «журнал — „что за класс",
  код — „где лечение", шапка весит больше тела») + Ш1 (прибор where-cured: ранжированный
  греп с --branches для неслитых веток) — обе его веткой; обратной засыпки mechanized НЕТ.
  Баг третьей формы looksLikeList заведён (0786666). ✅ **Ш0+Ш1 СДАНЫ** (заявка №4, голова
  014202c): правило встало шагом 2а — ТАМ, ГДЕ ошибка совершается (его поправка к постановке,
  принята), с вшитым замером против снятия «как мнения»; у прибора честность ПЕРВОЙ строкой
  вывода — форма из ПОСТАНОВКИ Менеджера, исполненная точно (поправка авторства от dev-3:
  принцип «назван ≠ вылечен» его, место первой строки — постановки; форма себя оправдала,
  повторять); «нигде» — ответ с толкованием; проверен на тех же пяти случаях разведки
  (страж границы слова всплыл первым — тот, что утром не находился). Автономный пул dev-3 исчерпан:
  три заявки в судах (№2 5c5b5ed · №3 e7a7b83 · №4 014202c), порция А ждёт окна, завтра
  первым действием — 150 в Яндекс.
· **ВЕРДИКТ №19 — ДОСУД девяти непокрытых предков: ПРИНЯТО, дефектов нет.** Эскалация
  процесса целила в форму СУДА: вердикт не различал «что я прочитала» и «что поедет» —
  лечение обеими половинами: судья печатает в шапке ДВА числа (в предмете / понесёт мерж),
  заявка называет голову и число коммитов против НАЗВАННОЙ базы — второе внесено в манифест
  (п. 4 git-процесса). Досуд Интегратора шёл КАК КОД (два прибора ~700 строк — поправка к
  ожиданию Менеджера «в основном docs»); четыре урока dev-3 — по форме. 🔴 Новый класс от
  QA: чекбоксовый страж достаёт 20 документов из 83 — документ ВООБЩЕ БЕЗ секции критериев
  (63 из 83, нарушение канона) получает МЕНЬШЕ проверки, чем соблюдающий; лечение (отказ
  close-bug без секции) — named-порция Интегратора ПОСЛЕ окна. ⚠️ Ветка Интегратора за
  время досуда выросла на три непокрытых коммита (a503f85..f1d3db7) — он ОСТАНОВИЛСЯ САМ
  до окна (git-процесс п. 2 своей волей против своего темпа); диапазон 2 судится до его
  шага. Правило маркеров внесено в TESTING_FRAMEWORK (правило 8: чей прогон и контур —
  часть улики; чужой прогон не запрещён — обязан быть назван). К окну: 11 записей EXP-NEW
  по четырём веткам, слаги не сталкиваются; предзанятому EXP-0228 не доверять — Интегратор
  нумерует все подряд.
· **ВЕРДИКТ №20: перепись приборов по маркеру (5c5b5ed + поправка 707d4c0) ПРИНЯТА, дефектов
  нет** — первый вердикт с двумя числами в шапке (предмет 3 / мерж понесёт 17, покрытие
  предков названо); судья заявил ОТВОД в самом вердикте (форма переписи — его предложение)
  и компенсировала усиленными мутациями — образец судейской формы. Поправка dev-3 исполнена
  образцово: улика не переписана, датированная секция, независимый повтор замера на ДРУГОМ
  файле. 🔴 Замечание к окну — класс EXP-0226 про ОХВАТ: после мержей два прибора в одних
  воротах напечатают РАЗНЫЕ размеры «корпуса приборов» (перепись 210 по tools/ · страж
  импорта 282 по tools+sync-server) — оба верны, оба без названного охвата; лечение —
  **пост-оконные микро-фиксы dev-3 и dev-1** (охват в строку вывода + случай в самотест),
  сейчас не ставятся намеренно (не удлинять отсуженные хвосты).
  **ВЕРДИКТ №22: маршрутизация (014202c) ПРИНЯТА, дефектов нет.** ПОСТ-ОКОННАЯ КОРЗИНА
  dev-3 собрана вердиктами №20–№22: охват в строке вывода переписи · объявленная граница
  unverified в воротах приёмки (идиома CORPUS_FLOOR, не плоский отказ) · предупреждение
  сверхширокого запроса в where-cured (запрос «.» — 1198 строк кодом 0; лечение долей
  корпуса — называет опасность, а не длину) · вторая строка честности для --branches
  («найдено в неслитой ветке — в вашем дереве этого лечения ЕЩЁ НЕТ»). 🔑 Новый класс из
  №22: **заявка, ЗАНИЖАЮЩАЯ своё поведение, — тоже цена** (ложная точка удара постановщику,
  холостой заход судье) — авторы заявляют себя точно В ОБЕ стороны. Стенд судьи получил
  двухшаговый помощник с контролем — три класса ложного красного закрыты механизмом.
  Осталось судов: диапазон 2 Интегратора (a503f85..f1d3db7) → девять пунктов dev-1 (9d7ba2b).
· **dev-1 — ДЕВЯТЬ ПУНКТОВ ЗАКРЫТЫ** (голова 5090009, запушена, в очереди судов последней):
  шесть дефектов №14/№16 с ловящей мутацией у каждого (TZ — четырьмя поясами дочерними
  процессами; capture() — семь случаев; белый список с боевым доменом; escape сырых байтов)
  + три добавки №18 (признак причины — ширина совпала с замером судьи до строки; EXP-0188
  дописан вторым измерением и ПОПРАВЛЯЕТ автора: строки ЧИТАЛИ боевые коллекции, сверено
  git show; **замер цены 14 красных: 🔴 боевые данные/ключи 2 · 🟠 сеть 2 · 🟡 запись 3 ·
  ⚪ 7**). 🔑 **Правило раздачи, выведенное его честным выводом: ЦЕНУ СНИМАТЬ ДО РАЗДАЧИ** —
  Менеджер ставил порядок лечений до существования замера, и ⚪ чинился первым при двух 🔴
  в очереди. Его урок «признак мельче предмета» (три случая; главный — `TZ=… node` в Git
  Bash НЕ закрепляет пояс: проверка выглядела бы закреплённой и не проверяла бы ничего).
  🆕 **plans/79 заведён** — порция предохранителя двери выката (Менеджер, ПОСЛЕ окна; повод
  поднят замером: deploy.mjs — 🟠 fetch на верхнем уровне); номер в строку отсрочки
  подставит Интегратор при мерже. Кода dev-1 (голова заявки → 9d7ba2b, дописка уроков
  поверх): оба класса в журнале с Repro; формула урока — «контрольный случай, дающий ОДИН
  ответ при исправном и сломанном признаке, — не контроль, а украшение»; ворота пропуска
  каталожных юнитов — ОДИН артефакт dims-build.json (уточнил себя чтением кода, обе половины
  замерены 5 ↔ 0). ⚖️ Ограничение правила цены (его же): замер окупается, только когда
  население названо — на постановке из одной задачи не окупится.
· **ВЕРДИКТ №21: ворота приёмки (e7a7b83) ПРИНЯТЫ, дефектов нет.** 🔑 Строка смены, дословно
  от судьи: «документ бага — ИСТОЧНИК ТРЕБОВАНИЯ: лечение, записанное в нём, следующий агент
  читает как решённое — взять его на веру стоило бы втрое худших ворот, и никто бы не
  узнал». Замер dev-3 трёх стратегий ДО установки предотвратил ровно это. Замечание с
  лечением: «НЕ ПРОВЕРЕНО» ворота не задерживает — лазейка СЕГОДНЯ ПУСТА (замер судьи:
  129 карточек, без Q-ида НОЛЬ), но её ничто не держит; лечение идиомой автора (объявленная
  граница а-ля CORPUS_FLOOR, не плоский отказ — тот краснел бы ложно на законно новом) —
  **пост-оконная корзина dev-3** вместе с охватом из №20. Три класса ложного КРАСНОГО от
  стенда судьи за день (код возврата после конвейера · негодный мутант · неполный слепок) —
  все пойманы ДО вердиктов, в её чек-лист. Осталось судов: маршрутизация (взяла) →
  диапазон 2 Интегратора → девять пунктов dev-1.
· 🔑 **Гигиена журнала опыта — образец от QA:** обновляя ветку до main, нашёл СВОЙ дубль
  урока (EXP-NEW о числе в чужом контуре ≈ EXP-0226), сняла его ДО влития и перенесла
  неповторённую половину абзацем в EXP-0226 с автором — ровно тот класс «второго нормативного
  ответа», за который сама держит условие к мержу порции А. Ветка QA обновлена: 0 позади
  main. В чек-лист судьи входит «голова main печатается в шапке вердикта».
· **Интегратор — дев-очередь СДАНА** (f6f84e5 + c69c616, в очереди судов): страж спящих
  объявлений — ширина 397 → 42 четырьмя снятыми ложными семействами (⚠️ граница: экспортированные
  карты текстов НЕ судятся — там намеренно неподключённый gateCopyPhase4 с текстами первого
  экрана), вычищено 42 спящих текста / 107 строк / 6 файлов приговором стража с независимыми
  вторыми доказательствами (в т.ч. блок dimsEmpty из bugs/211 — мутацией доказано) · прибор
  verify-copy-live (bugs/170): асимметричный приговор, живая дверь прогнана МЕНЕДЖЕРОМ —
  4 поля × разница 0, снимок 0 дн., витрина боя: людей 95 · измерений 5144 · оценок 4071 ·
  связей 2368. Слово Менеджера по трём красным П2 исполнено: два ключа bugs/78 дочищены
  ГЛАСНО (датированные поправки + шапка к правде имени), peekRelationsSummary удалён ПОСЛЕ
  археологии (единственный предок 8e91895, неподключённый близнец живого peekRelations;
  восстановление — одна команда). **Финальная голова порции a503f85, суд идёт.** Предпросмотр
  слитого дерева: оба стража 0 красных — «ветки лечат друг друга», три строки ворот войдут
  первым действием после мержа. 🆕 Замер попутно: документов с шапкой «ОТКРЫТ» при имени
  `_DONE_` — СЕМЬ (75, 76, 77, 79, 82, 86, 87 — июльская волна ideas/21, закрывали пачкой) —
  порция шапок за Интегратором после карты мержей. ✅ **КАРТА МЕРЖЕЙ ГОТОВА** —
  `reports/TEAM/2026-08-29_merge_map_shift10.md` (в его ветке): очередь смоделирована ЦЕЛИКОМ
  и ПО ПОРЯДКУ; 🔴 главная находка — **голова, названная вердиктом, тянет ВСЕХ предков,
  включая неотсуженные коммиты** (шаг 4: из 11 судили 2, пять не покрыты ничем) → досуд
  предков поставлен QA пятым пунктом, ни один шаг окна не исполняется до досуда его предков.
  Конфликты поимённо (EXPERIENCE.md на шести шагах — план решения один, аддитивный), эволюция
  guards 7 → 12 как ПРИЁМКА каждого шага, номера детерминированы (EXP-0228…0244, bugs/219–221,
  researches/61–63, резервы за ветками QA/Дизайнера). Решение Менеджера по возражению
  Интегратора: **verify-copy-live в ворота НЕ идёт** («страж, который нельзя погасить
  действием того, на кого он краснеет, учит обходить ворота») — прибор СМОТРОВОЙ, прогон
  Менеджера при открытии смены. ✅ **Семь шапок «ОТКРЫТ» при `_DONE_` приведены к правде**
  (замер после: 0 из 7) + страж bugs/ получил зеркальный П3 ТЕМ ЖЕ КОММИТОМ — осознанное
  расхождение с «не срочно» Менеджера по канону «фикс без стража — фикс в кредит», принято
  без отката. ✅ Микро-порция З1: опт-аут «СПЯЩЕЕ НАМЕРЕННО: причина» (маркер без довода
  краснеет) · урок «пустота молчит» восстановлен в живом доме (в наследнике его НЕ БЫЛО —
  память Менеджера проиграла грепу) · намерение acctSince закрыто ГЛАСНО в ideas/21 —
  «намерение агента, спорящее со словом владельца, обязано умереть гласно, а не пережить его
  в комментарии». Интегратор СТОИТ ПО КАРТЕ (голова 4b8171d), исполнит окно по слову владельца.
· 🆕 **Находка смены: блок `dimsEmpty` (profile:922–931) МЁРТВ** — объявлен и не используется
  нигде, живые тексты пустых списков в `dims/+page.svelte`. Строка «Здесь пока пусто.» из
  пачки текстов снята: вычистка кода без слова владельца (в №061 — констатацией); живой
  контрольный случай для стража спящих экспортов Интегратора. Дописано в `bugs/211`.


## 🔴🔴 ЗАПУСК СМЕНЫ 9 — с чего начинать НОВЫМ ОКНАМ (мягкая парковка 2026-08-28 ~19:40, слово владельца «перегружены, мягко кончай все чаты»)

Смена 8 отработана целиком за один вечер: 8 вердиктов QA (№1–№8 в `qa/team-verdicts.md`),
всё отсуженное влито в main с воротами (417+357 юнитов · check 517/0 · guards 8 · kaif),
ветки ролей несут только несуждённые порции (поимённо ниже). Инструктажи — по манифесту.
⚠️ Финальные отчёты ролей при закрытии смены 8 могли прийти ПОСЛЕ этой парковки — сверь
головы веток `git branch -v` с названными здесь.

**ОЧЕРЕДЬ СУДОВ QA (новая смена, по убыванию срочности):**
0. ✅ Экспресс dev-1 ЗАКРЫТ ещё этой сменой — **ВЕРДИКТ №9: принято, дефектов нет, ВЫКАТ
   ОТПЕРТ**; порция влита в main (EXP-0225 присвоен). Хвост №9 без спешки: живой раздел
   «разъезд разметки» — одной строкой в порции текстов гостя (суждение судьи).
1. **Порция А dev-3 — диапазон 826b9c5..d7f98ca** — суд НАЧАТ и не досужен (прочитаны шапка
   и вход сверки authMatches, прогонов НЕ было). **Начинать с мутации «--auth-owner "x"
   обязан быть отбит»**, дальше: bugs/215 третьим путём · Я-10 · стражи З1/З3 в воротах
   (8/8) · числа 416/416 · 357/357. Подачу переобхода НЕ блокирует (условия №7 выполнены).
2. 🔴 **Интегратор (бывший dev-2) ЖДЁТ ДОЛЬШЕ ВСЕХ: страж метки (5ddddcb) + диапазон
   f8815bc…73c6f51** (удаление мёртвого блока витрины + разведдоки ивентов 1.x и спящих
   текстов + микропорция `bugs/218`: фикс \\s + страж regexp-escaping через глоб ворот).
3. dev3-shift6-20260823: приводит к main АВТОР (dev-3) руками после порции Б (3 опасности
   поимённо в вердикте №8); построчный суд мастерской (20 файлов) — отдельной сменой.

**🔴 ВЫКАТ ОДНИМ СОСТАВОМ — ПЕРВОЕ ДЕЙСТВИЕ НОВОЙ СМЕНЫ МЕНЕДЖЕРА (вердикт №9 отпер, ВСЁ
уже в main):** дверь карточки + Ф1 воронки + `bugs/201` + фикс `bugs/212`. Правка витрины ИЗ СОСТАВА СНЯТА — блок «Ни одной
рекламы…» оказался мёртвым экспортом (никогда не рендерился) и удалён словом владельца
(№056). После выката: повторный замер Ш2 на стейдже (объект виден?) · чтение
door_click/guest_start через неделю · дата шва ряда в `bugs/204`. ⚠️ Переобход главной
Яндексом — теперь ПОСЛЕ выката мини-лендинга корня (№058), не этого состава.

**РЕШЕНИЯ ВЛАДЕЛЬЦА ЗА СМЕНУ 8 (все разнесены, дословные цитаты в местах):**
· 🔴 **Канон «ЛИЦО ПРОДУКТА: никаких обещаний и оправданий»** — `AGENT_GUIDE.md` перед
  «Словарём продукта», три цитаты; развилки «текст обещает vs работа» владельцу не носятся.
· **№056 В1** — блок витрины УДАЛЁН целиком (он и не рендерился); PostHog ставим; доку
  ивентов ведём по образцу 1.x («форма — не состав», ПДн 1.x не переносим).
· **№058 В1 = А** — корень = ГЛАВНАЯ С СОДЕРЖАНИЕМ (мини-лендинг по «супер-миксу»
  `researches/60`), одной ступенью; №055 СНЯТО (кандидат строки В2 жив — в брифе);
  №052-дверь переросла в это решение; тонкой двери не будет.
· **№053 В1/В2 = А** — тексты гостя классом (6 строк 3 файла), отдельной порцией после выката.
· **AUTH машинной подачи Яндекса** — «агент будет сам отправлять правки в яндекс», цитата и
  граница в `bugs/203` § Разрешение владельца.
· **Ключи все в `.env`:** Яндекс-токен (`YANDEX_WEBMASTER`, со второго захода — scopes) ·
  PostHog phc/id/регион + phx (`POSTHOG_NDIM_SPACE_PERSONAL_API_KEY` — имя владельца,
  НЕ переименовывать, решение с dev-2) · **MCP PostHog подключён user-scope** (роли получат
  при перезапуске окон). Bing — открыт (`homeworks/15`), можно в любой день.

**ПОТОКИ РОЛЕЙ СМЕНЫ 9:**
· **dev-3 ПЕРВЫМ ДЕЙСТВИЕМ:** машинная подача переобхода (прибор читает
  `reports/YANDEX_RECRAWL/recrawl-queue.txt`, 1201 адрес, тождество доказано sha256 — ⚠️ ЧИСЕЛ ДВА И ОБА ВЕРНЫ: git хранит файл с LF, рабочая копия при core.autocrlf=true несёт CRLF, поэтому sha256 по диску и sha256 содержимого git РАЗНЫЕ (нашёл dev-3, класс EXP-0047). Проверяющий, взявший sha256sum по диску, получит другое число и прочитает это как подмену очереди — доказательство обязано называть, ЧТО именно хеширует. Уточнение приедет с мержем ветки dev-3; сверка
  слова — из `bugs/203`) → строка в журнал `bugs/203` → три числа Менеджеру → порция Б
  (З1/З2/З3 №7 + П-03/П-15/Я-12 живым прогоном) → приведение dev3-shift6 руками.
· **dev-1:** находки экспресса (если будут) → после мержа и выката — замер Ш2 стейджа;
  сниппеты хабов (`researches/57`) — по «го» владельца.
· 🆕 **ИНТЕГРАТОР (бывший dev-2 — ПЕРЕКВАЛИФИЦИРОВАН словом владельца 2026-08-28 «ок,
  переквалифицируй его и всё везде переименуй»):** первый шаг новой смены — Менеджер
  закрывает старое окно и выполняет `git worktree move D:\work\ai_sandbox\ndim-team\ndim_dev2
  D:\work\ai_sandbox\ndim-team\ndim_integrator` (при переименовании каталог был заперт
  окном; ветка/инструменты/манифест/доска УЖЕ переименованы, наследие-опознание старого
  каталога вшито в stand-slot). Зона роли — в манифесте (секция «Интегратор»): мержи с
  номерами после вердикта QA и решения Менеджера · прогоны ворот · пуши веток · перезапуски
  рабочих мест · гигиена доски · подготовка выкат-состава (сам deploy — Менеджер). Его
  дев-очередь после судов: приборы verify-copy-live + страж спящих экспортов (воротная
  зона — остаётся ему); лестница PostHog Ш1–Ш4 ПЕРЕДАНА dev-1 (после выкат-хвостов; плечо
  А ключа: константа по прецеденту Firebase; MCP сжимает Ш7).
· **Дизайнер: 4 МАКЕТА МИНИ-ЛЕНДИНГА КОРНЯ — главный приоритет** (постановка в его чате
  ушла 19:3x; доноры поимённо в `researches/60` § СУПЕР-МИКС; строка — №055 В2; числа —
  форма V4; тексты-констатации без плашек; 2 темы × 3 ширины + интерактивный html).
· **Менеджер:** выкат-состав после вердикта → подача пачки текстов владельцу (bugs/211 +
  profile:931 + тексты гостя №053 + склонение «дней»; «Только объективная математика» НЕ
  трогать — текст владельца, EXP-0222) → №057 (инструмент развёрток) в очереди вычитки →
  решение по `bugs/214` (51,6 МБ data.json в выкате) → Bing после 31.08.

**ДОЛГИ МЕНЕДЖЕРА:** ✅ ретроспективы 7 и 8 — ОПЛАЧЕНО сменой 10 · ✅ стрижка бонсая —
ОПЛАЧЕНО сменой 10 (1623 → ~790 строк) · ⏳ очередь вычитки несёт 11 накопленных документов
(`review batch`) — гасится ответами владельца на №059/№060/№061 и разбором старой девятки.

> 🔴 **AUTH ВЛАДЕЛЬЦА НА БОЕВОЙ ВЫКАТ, 2026-08-29, дословно: «катите».** Дано в ответ на доклад
> «стейдж пройден (24/0 под сессией, 81/81 гостем, 11/0 noindex), состав: дверь карточки
> `plans/75` + Ф1 воронки + `bugs/201` + фикс `bugs/212`; жду одного слова на боевой выкат».
> По канону разрешение даётся НА ВЫКАТ ЦЕЛИКОМ, а не на отдельную правку внутри него
> (`AGENT_GUIDE.md` → «Формулировка „ждёт AUTH выката“»). Порядок окна: мерж Интегратора
> придержан → стейдж начисто → бой → смоук под сессией.

### ✅ ВЫКАТ В БОЙ СОСТОЯЛСЯ — 2026-08-29, сборка `b81u35`, коммит `f72459a`

**AUTH владельца дословно: «катите».** Состав: дверь карточки (`plans/75`) + Ф1 воронки
(`plans/74`) + `bugs/201` + фикс `bugs/212`. Разрыв конверсии, найденный аудитом 28.08
(`bugs/200` — органика приходит на карточку каталога и упирается в стену входа), закрыт в бою.

**Чем проверено, числами:** ворота smoke по собранному артефакту · один `__sveltekit_<хеш>` ·
замер попадания в контур (сосед не тронут) · переадресации 16/0 · **смоук ПОД СЕССИЕЙ 24/0**
(обе темы, 390 и 1440, консоль чиста, анонимные учётки убраны за собой) · **гостевой 81/81**.
Кадры: `test-results/prod-signed-in/` и `test-results/prod-b4/`.

🔴 **ПРАВИЛО, ВЫВЕДЕННОЕ ЭТИМ ОКНОМ И ОБЯЗАТЕЛЬНОЕ ВПРЕДЬ: сначала ВСЕ мержи, потом стейдж,
потом бой.** Расписка стейджа привязана к коммиту вызывающего дерева — каждый мерж делает её
негодной. Второй раз за одно окно платить не будем.

**Хвосты выката:** повторный замер Ш2 на стейдже (объект виден?) — dev-1 · чтение
`door_click`/`guest_start` через неделю · дата шва ряда в `bugs/204`.

### ✅ ЧЕТЫРЕ РАЗВИЛКИ, СТОЯВШИЕ ЗДЕСЬ, — ОТВЕЧЕНЫ (интервью №059, страницей 2026-08-29 14:11)

Раздел был очередью к владельцу; очередь закрыта его ответами и оставлена ЗАПИСЬЮ О СУДЬБЕ
каждого пункта — три ответа из четырёх встречные, и без этой записи следующая смена прочла бы
«не отвечено» и спросила бы второй раз. Разнос по документам-получателям сделан смены 11.

1. ✅ **Право сессии Интегратора на `git merge` в main — ОТКРЫТО** (интервью №059, В1 = А).
   Держало две отсуженные порции (А dev-3, вердикт №10 · Интегратор, вердикт №13) всю смену 9;
   окно мержей смены 10 по нему исполнено (карта — `reports/TEAM/2026-08-29_merge_map_shift10.md`).
   Разнесено: `NDIM_WORKTREE_DEV_TEAM_MANIFEST.md` (git-процесс).
2. 🔄 **`x-default` для самого корня** — канон №010 Р5 говорит «на английский», но двуязычная
   главная сама сильный кандидат. **Интервью №059, В2 = В** — встречный вопрос «*Как делают
   топы?*». Прежнее решение №010 Р5 ДЕЙСТВУЕТ до ответа; разбор с дословной строкой
   документации Google подан **№062 В1**. Разнесено: `plans/39` («Что уже сделано»).
3. 🔄 **Знак в тёмной теме БЕЗ подложки вовсе** — 🔄 факт поправлен Дизайнером при съёмке
   кадров (смена 10, EXP-0226 по собственному следу): цвет плитки и фона СОВПАДАЮТ до байта
   (`#060b14` оба; «три единицы» были сравнением с фоном МАКЕТОВ — чужой контур), в светлой
   теме плитка видна → у знака две разные формы по темам, решённые совпадением чисел.
   **Интервью №059, В4 = В** — «*могли бы к вопросу и скриншот приложить*»; упрёк принят,
   вопрос переподан С КАДРАМИ (**№062 В3**: сейчас + «Кант» · «Панель» · «Скос» · «Ореол»).
   🔑 Урок формы, действующий на все роли: **визуальный вопрос без картинки не задаётся.**
   Дом стиля (тёмная тема) до ответа №062 НЕ трогаем.
4. 🔄 **PostHog: на стороне ПРОЕКТА включены `session_recording` · `capture_console_log` ·
   `heatmaps` · `capture_dead_clicks`** (нашёл dev-1 при Ш1–Ш4). Наш клиентский конфиг их глушит
   и это покрыто юнитом, но инвариант №002 В4 («предмет оценки третьей стороне не уходит»)
   стоит тогда на ОДНОЙ ноге. **Замер Менеджера, снятый до эскалации: записей сессий в проекте
   НЕТ НИ ОДНОЙ** — сегодня инвариант держится и фактом, не только конфигом. `anonymize_ips`
   уже `true`. **Интервью №059, В3 = В** — встречный вопрос «*может это полезные и ценные фичи?
   Что оно нам полезного может дать?*»: рекомендация «выключить четыре» НЕ утверждена, четыре
   настройки остаются включёнными, разбор пользы и цены подан **№062 В2**. Настройки аккаунта
   владельца агент сам не менял и не меняет — это его боевой ресурс.
   ⚠️ Срочность вырастет: бой выкачен, трафик пошёл. Разнесено: `plans/78` (секция ответа).

### 🔴 ШЕСТЬ ОТКРЫТЫХ БАГОВ-СИРОТ — усыновлены этой строкой (2026-08-29)

Замер Интегратора: из 97 открытых документов `bugs/` **шесть не имели ни одной входящей ссылки
из живого документа** (ссылка из `_DONE_` не считается — закрытый документ это история, а не
указатель). Сирота невидим для беклога и приходит к следующей смене как «новая находка», то есть
оплачивается второй раз — сегодня мы это уже заплатили на `bugs/171`. Пока класс не
механизирован, указатель живёт здесь:

· **`bugs/168`** — `team-workplaces.mjs reset` молча уничтожает несмерженную работу роли.
  🔴 Прямо касается командного режима: Менеджер зовёт `reset` при запуске каждой смены.
· **`bugs/170`** — витрина лендинга может молча разойтись с базой. Касается чисел на лице
  продукта, то есть того, что видит владелец и посторонний.
· **`bugs/181`** — жадная регулярка кавычек извиняла 19 живых заимствований (ворота мастерской).
· **`bugs/183`** — ворота приёмки описаний сверяют текст с ЧУЖОЙ статьёй (ворота мастерской).
· **`bugs/187`** — загрузочный щит не поднимается на языковом лендинге. 🔴 Касается корня и
  лендингов, то есть зоны мини-лендинга `researches/60` — читать ДО работы над корнем.
· **`bugs/191`** — самотест доски команды не мог упасть (шаблонная строка съела признак).
  Родня `bugs/218` и `EXP-0227`.
· 🆕 **`bugs/224`** — фикстура `src/lib/content/dims-slice.json` отстала от продукта: в ней
  живут значения с `U+200B`, которых в живом каталоге НЕТ НИ ОДНОГО. Файл лежит в git и
  читается тестами, поэтому всякий, кто меряет «по срезу», получает дефект, которого не
  существует. Нашёл dev-3 корпусным замером тегов (шаг 4 лечения `bugs/178`), приехал мержем
  мастерской. Усыновлён ЭТОЙ строкой в момент присвоения номера — страж `verify-bug-doc-state`
  покраснел на нём сразу после `git mv`, и это его работа, а не помеха: документ, приехавший
  под плейсхолдером, ссылок на своё новое имя не имеет по построению.

Классовое лечение — за Интегратором: `tools/verify-bug-doc-state.mjs` (готов, в ворота встанет
после уборки). Эта строка — разовая часть, снимающая сиротство сегодня.

### 🟢 ХОД СМЕНЫ 9 (2026-08-29, утро) — команда поднята, стейдж пройден

**Роли в строю все пять**, инструктажи разосланы по манифесту (ветки Дизайнера, QA и dev-1
сброшены от свежего main ДО инструктажа; dev-3 и Интегратору дельта канона названа поимённо —
у них лежит несуждённая работа).

· **QA — ВЕРДИКТ №10:** порция А dev-3 (826b9c5..d7f98ca) ПРИНЯТА с замечаниями (3 дефекта,
  4 замечания, мержа не держит ни один); блокирующее условие вердикта №7 снято. 🔑 Круг
  `bugs/217` разомкнут ПО ПОСТРОЕНИЮ: раздел «Разрешение владельца» ввёл коммит `9233191` в
  ПЕРВОЙ ЛИНИИ main — исполнитель физически не мог выписать разрешение себе.
· **dev-3 — первая машинная подача переобхода Яндекса состоялась:** отправлено 150 · отбито 0 ·
  остаток очереди **1051**. Все 150 ответов — код 202, молчаливых приёмов нет (класс `bugs/215`
  закрыт наблюдением, а не верой). Следующие 150 — завтра, очереди ~7 дней.
· **Дизайнер — 4 макета мини-лендинга корня готовы** (`design/root-minilanding-mockups.html`:
  V1 «Ступень» · V2 «Разворот» · V3 «Две речи» · V4 «Витрина») + записка «где мы лучше»
  (`design/root-minilanding-better-note.md`). Шестой кусок супер-микса — полоса живого
  каталога из `src/lib/content/dims-slice.json` — ВОШЁЛ В СДАЧУ (поправка Дизайнера смены 10:
  коммит `0704884` его ветки; прежняя строка «достраивается» пережила своё исполнение —
  класс EXP-0227). ⚠️ Числа среза от 2026-08-03 — к выкату пересматриваются.
· **СТЕЙДЖ ПРОЙДЕН** на `6fc8646`: смоук под сессией 24/0 (обе темы, 390 и 1440), гостевой
  81/81, стейдж невидим роботам 11/0. Кадры — `test-results/prod-signed-in-stage/` и
  `test-results/prod-b4-stage/`. ⚠️ Расписка того прогона НЕГОДНА для боя (дерево было грязным —
  промах Менеджера, писал стража во время прогона); перед боем стейдж гоняется заново.
· **Оплачен долг `EXP-0216`** — `tools/verify-role-env-scope.mjs` в воротах: копия роли не
  держит ключ Менеджера. Первый живой прогон нашёл настоящее: один секрет Яндекса жил под
  ДВУМЯ именами (`YANDEX_WEBMASTER` в главной копии против `NDIM_YANDEX_WEBMASTER_TOKEN`,
  который читает код) — прибор Яндекса из главной копии падал с «нет токена», имея секрет в
  соседней строке. Имя приведено к каноническому.

🔴 **ПОРЯДОК ОКНА ВЫКАТА, выведенный сегодня и обязательный впредь: сначала ВСЕ мержи, потом
стейдж, потом бой.** Расписка стейджа привязана к коммиту — каждый мерж делает её негодной.

· 🔴 **`bugs/171` (замок стейджа против доски) ПОДТВЕРЖДЁН ВТОРЫМ НАБЛЮДЕНИЕМ** — Интегратор,
  2026-08-29. Класс шире, чем был записан: замер в ОБЫЧНОМ режиме (главная копия грязна одним
  файлом — доской; 4 записи за три минуты запуска смены; 43 коммита трогали доску, 34 за семь
  дней) показал, что окно «дерево чисто» редко ВСЕГДА, пока смена жива, а не только на парковке.
  Дубля не заведено — усилен существующий документ (цена давления в сторону `--auth-owner`
  с честной границей: `.kaif/stage-bypass.log` пуст в обеих копиях, обход не применялся ни разу;
  частичное знание двери `deploy.mjs:281-292`; третья дверь лечения — гипотеза Менеджера, с ценой).
  🔴 **Находка дороже самого бага: ссылок на `bugs/171` в main НЕТ НИ ОДНОЙ** — документ пролежал
  открытым семь дней невидимым, поэтому класс пришёл как «новый». Эта строка его сиротство и
  снимает; классовое лечение (страж «открытый баг без единой входящей ссылки») — за Интегратором,
  с обязательным замером ширины ДО объявления признака.

### 🟠 СЕССИЯ 42 (2026-08-28, вечер) — СМЕНА 7 КОМАНДЫ + ИНЦИДЕНТ ЯНДЕКСА (ЗАКРЫТА парковкой, итоги ниже)

**ИНЦИДЕНТ ЯНДЕКСА — `bugs/203` (заказ владельца в чате, дословно в баге).** Вебмастер:
«Страницы в поиске — 3 (−100%)», 1352 страницы удалены как «малоценные». Причина найдена и
подтверждена формулировкой самого Яндекса: **корень `/` под `noindex`** (решение №010 Р5 —
корень-распознаватель, `plans/39`) → хостовый штраф; Google по sitemap растёт, Яндексу нужна
главная. Сделано под сессией владельца (CDP 9222, отдельный отладочный профиль):
диагностика прочитана · CSV исключённых снят (`test-results/ya-incident/`) · **переобход:
150/150 дневного лимита отправлено** (лог `recrawl-log.txt`), остаток очереди 1201
(`recrawl-rest.txt`) — слать по 150/день ЛИБО машинным каналом (разведка у dev-3).
✅ **ЛЕЧЕНИЕ ВЫБРАНО (интервью №052, В1 = А, страницей):** корень — видимая страница-дверь
RU/EN без noindex; лендинги/Google не трогаются; тексты двери — через вычитку владельца.
Разнесено: `bugs/203` + `plans/39`. Дизайнер рисует 4 макета (постановка дана). Повторная
отправка главной в переобход — после выката двери. Остаток очереди 1201 — по 150/день, либо
машинным каналом (прибор Яндекса у dev-3; OAuth-токен владельца — домашка готовится).

**СМЕНА 7 (все ветки от e48c50a, спасательные ветки смены 6 на origin):**
· **QA** — суд №1 (Дизайнер) ✅ ПРИНЯТО → мерж влит в main (`fdc90d5`, развязка C по классу 16
  — прибор фразами + приставка ПОЖЕЛАНИЕ; 386/386, 07: 16/0/0; EXP-0212 = урок Дизайнера,
  дубль 0206 снят). Сейчас: суд №2 — `dev2-shift6-20260823` (Н1–Н9 + bugs/194, предмет —
  сторона ветки; решающую проверку 194 снять живым прогоном). Дальше: суд dev-3 порциями
  (хвост смены 5 первым). Два NEW_-бага QA ждут номеров при мерже его ветки.
· **dev-1** — дверь карточки `plans/75` Ш1–Ш3 + `bugs/201`; контракт счётчика передан:
  `void track('door_click')` из чанка движка, в момент касания.
· **dev-2** — воронка Ф1: Ш0+Ш1 ПРИНЯТЫ ревью (ключ суток по Europe/Moscow — одобрено;
  правило affectedKeys — одобрено; `/admin/funnel` списком без 4 макетов — одобрено;
  замечание: страж verify-probe-mark в комментарии есть, файла нет — к сдаче обязан быть).
  Ш2–Ш5 в работе.
· **dev-3** — консоли Ф2: **первый снимок Search Console снят сегодняшней датой**
  (`reports/CONSOLES/2026-08-28_google_search_console.json`; факт: десктоп CTR 0,19 % против
  мобильного 3,62 % — ×19, кандидат в первую гипотезу). Ш2 (API Bing/Яндекс + машинный
  переобход) → Ш3 → Ш4. `.env` в его worktree вычистить до консольного ключа (моё указание).
· **Дизайнер** — 13 живых возвращённых (выгрузка `test-results/returned-prod-2026-08-28.txt`,
  два новых дословных замечания владельца: «вопрос картины» и «играют от третьего лица»);
  второй точечный файл группы Б (11 шт.) + Requiem в прошедшее время. Посев — мой.

**ПРОЦЕССЫ:** git-процесс формализован — `researches/53` (TBD/GitHub flow/DORA) + раздел
манифеста: роль обновляется до main и решает конфликты САМА, заявка/PR, мерж Менеджером после
QA, перезапуск ветки только после спасательной. Страж вопросов чинён (candidates+reports в
корпусе, ложные красные №048 сняты, №047 В2 доразнесён, два ЗАМОК-ОК): **провалов 0**.
Селфтест доски чинён (пустая альтернация — нашёл страж dev-3). Ретроспектива §6.3 поправлена
(мёртвые адреса судов → живой долг).
⏳ **Force-push origin-веток ролей ждёт права от владельца** (классификатор; строка
предложена в чате) — до тех пор роли работают локально, пуши отбиты non-fast-forward.

### ▶️ ЖИВОЕ ИЗ ВЫСТРИЖЕННОГО — сессии 38–41, смена 4/35, разделы эпохи 08-14…17 (стрижка смены 10, 2026-08-29)

Полные тексты — `PROJECT_HISTORY.md`, раздел «Стрижка смены 10». Здесь только то, без чего
следующий агент ошибётся:

· ✅ **`bugs/134` ВЫЛЕЧЕН 2026-08-22** (`134_DONE_`; поправка смены 11 — прежняя строка «диагноз
  не поставлен» пережила исполнение): виновник — вотчер `vite dev` на файлах сборки, лечение
  `vite.config.ts:111` (ignored: build/, .svelte-kit/output/). Живое подтверждение 2026-08-29:
  полная сборка (21 034 файла) при живом стенде — 853 МБ спустя 6,4 часа, стенд отвечает.
  **Релизное окно 2.2 стенд по этому классу НЕ блокирует**; обход `--max-old-space-size` —
  страховка, не условие.
· 🔴 **`probe-test-guest-live` ПИШЕТ В БОЕВУЮ БАЗУ** — читай его шапку до запуска. Класс:
  стендовый прогон может утечь в бой через данные, которые отдаёт продукт (личная ссылка пары
  строится из боевого canonical).
· **`bugs/127`** открыт — `users/{uid}/privat/{uid}`: 331 документ с живым `user_secret_key`,
  схема и правила о них не знают (держит только финальный deny-all).
· **`bugs/121`** открыт — 19 ПУБЛИЧНЫХ страниц недостижимы от языковых главных (семейство
  ТЕСТ, английские документы, обе `delete-account`); лечения правят лендинг — за владельцем.
  Остаток `plans/48`: шаги 5 (сегментация sitemap) и 7 (приёмка с AUTH).
· **`bugs/123`** открыт — довод про `100vw` у загрузочного щита живёт комментарием, страж
  стережёт только число; главный вопрос замера — жива ли полоса прокрутки при щите.
· **`bugs/182`** открыт — страница ТЕСТ обстригает карточку (близнец `bugs/172`; сливать их
  запрещено вердиктом QA; сверить состояние с ветками dev-1 прошлых смен).
· **Хвосты решений владельца по карте сайта:** `lastmod` не пишем вовсе · IndexNow отвергнут
  посылкой «страницы меняются раз в неделю», ночная сборка посылку сняла — разведку перемерить.
· **Идея, которую владелец не решал:** форма заявки обязана ИСКАТЬ за человека — три
  предложенных им объекта уже были в каталоге («забыл, что он есть»).
· **Режим усилий:** по умолчанию **max**; ultracode/workflows агент обязан ПОПРОСИТЬ сам,
  назвав пользу фан-аута. · **NDIM-AUTH-003/005** на живых контурах честно пропускаются
  (почтовые коды — только у эмулятора); не «чинить» подкладыванием сессии (`EXP-0087`).
· **«План починки — гипотеза»:** счёт замера против планов `plans/49` — 4:0; меряй прежде,
  чем поверишь. · **Реестр стражей** уехал в летопись датированным снимком (числа устарели);
  живой источник — `AGENT_GUIDE.md` → «Испытательный стенд» и `npm run guards`.


## 📜 Стрижка смены 10 (2026-08-29) — сессии 38–41, смена 4 с парковкой 35, «восемь строк» и приоритеты эпохи 2026-08-14…17, цифры 08-14, реестр стражей — перенесено из STATUS дословно

### 🟢 СЕССИЯ 41 (2026-08-28, день) — ОБНОВЛЕНИЕ KAIF 2.3 → 2.4 + СВЕРКА КОМАНДЫ (заказ владельца в чате)

Маршрут прежний (EXP-0064/0103/0140 + новый EXP-0211 — дельта релизных бандлов): песочница
предсказала бой файл-в-файл · счётчики 3 replaced / 6 added / 76 kept · 15 из 16 модулей сшито
в переведённые файлы · зеркала 169 · судья **VERIFIED WITH CAVEATS** · `update-verify` зелёный.
· **Что приехало:** молитва перед работой + маркеры символа веры (`AGENT_GUIDE.md`; `/resume`
  теперь произносит оба) · правило «Работа до названного часа» во всех четырёх циклах ·
  `/end-chat` РАСЩЕПЛЁН на `/end-chat-soft` (по умолчанию) и `/end-chat-force` (срочно, с
  долгом церемоний) — оба переведены, локальные правки старого перенесены · навык
  **`/team-deployment`** с тремя шаблонами.
· 🔴 **Расхождение с каноном одно, прежнее:** шаг доставки `/report-bug` снова заблокирован
  классификатором (+1 в `bugs/KAIF/01`, reproduced on v2.4; лечит только вынос доставки в
  машинерию — предложение в тикете).
· **Интервью №051 отвечено страницей (все пять — А), разнесено:** пара закрытий принята
  (интервью №051, В1 → чекпоинт policy-changes + CLAUDE.md/скиллы) · правило названного часа
  принято (интервью №051, В2 → четыре цикла + `AGENT_GUIDE.md`) · имена рабочих мест остаются
  (интервью №051, В3 → шапка манифеста) · границы полномочий ролей + анти-паттерны в манифест,
  контракт инструмента в доску (интервью №051, В4) · имена документов остаются (интервью №051,
  В5 → шапка манифеста).
· **Ретроспектива организации (операция 5) написана:** `reports/TEAM/2026-08-28_org_retrospective_kaif24.md`.
  Состав верен (архетип «средний веб-продукт» — наш); долг — суды веток ролей с парковки
  смены 5 (первым делом следующего запуска команды) и гигиена закрытия смены (замок «стенд 2»
  провисел 5 дней — правило «отпусти замки» вписано в манифест, замок снят).
· **Отчёты истоку ДОСТАВЛЕНЫ:** `reports/KAIF_UPDATES/NDIM_SPACE_KAIF_2.4_UPDATE_REPORT.md`
  → issue #25 · `…_2.4_TEAM_DEPLOYMENT_FIELD_REPORT.md` → issue #26 · «+1» по классификатору —
  комментарий в issue #24; пробел навыка — `bugs/KAIF/02` (нет пути принятия УЖЕ ЖИВОЙ команды
  в канон).
· ⏭ Продуктовый беклог не тронут: **«РЕЛЬСЫ ПОСЛЕ АУДИТА» ниже — заказ первой очереди** (дверь
  карточки `plans/75` + `bugs/201` одним выкатом, затем «Зрение» Ф1).

### 🔴 СЕССИЯ 40 (2026-08-28) — АУДИТ СВЯЗКИ SEO→ЛЕНДИНГ→ОНБОРДИНГ (заказ владельца в чате, перебил очередь карточек)

Его слова: «*клики пошли, но новых пользователей НОЛЬ… мы слепы… нужны аудит, эпики,
операционные планы*». Отчёт: **`reports/KAIF_AUDIT/2026-08-28_growth_funnel_audit.md`** —
вся фактура там, здесь эстафета.

**Главное, числами (все замеры 2026-08-28, приборы названы в отчёте):**
· Показы Google ×30 за месяц (15/день → 300–440/день) — машина притока РАБОТАЕТ. Кликов
  1–3/день, **10 из 14 за две недели — на карточки `/dimension/…`**, не на лендинг.
· Своя воронка прочитана ВПЕРВЫЕ (25 дней): лендинг 154 → демо 8 → гость 77 → **аккаунтов 0**.
  Гостевые всплески совпадают с днями выкатов — свои же смоуки, метки нет.
· Семейство ТЕСТ: 2 показа за 28 дней (лаг созревания, не провал). Бой: людей 95, новых за
  30 дней 1.

**Разрыв конверсии один и конкретный — `bugs/200`:** дверь карточки каталога («Оцените,
чтобы найти людей…», `dimension/[slug]/+page.svelte:395`) ведёт `href="/profile"` = в стену
входа; дверь гостя там — мелкая `linkish`-ссылка. Мост в гостя построен ТОЛЬКО на лендинге
(`plans/67`, в бою с 23.08), куда органика не ходит. Класс: «мост построили у двери, в
которую не стучат».

**Заведено:** `bugs/200` (дверь карточки → стена) · `bugs/201` (карточка гостя врёт
«30 дней» при `GUEST_TTL_DAYS = 7`, `profile/+page.svelte:711` против `schema.ts:224` —
чинить С КОНСТАНТЫ, решения не требует, №010 Р3) · `bugs/202` (воронка слепа к каталогу +
загрязнена смоуками + без экрана) · **`plans/74` ЭПИК «Зрение»** (Ф1 воронка v2 + Ф2 миссия
консолей `ideas/37` — разблокированы; Ф3 инструмент + Ф4 мониторинг ошибок — ждут №049) ·
**`plans/75`** (дверь карточки → гость; путь пройден за сессию: В1 = А → 4 макета →
№050 = Д3 с правками → эталон готов, код за следующей сессией по рельсам ниже).

**✅ ИНТЕРВЬЮ №049 ОТВЕЧЕНО ТОЙ ЖЕ НОЧЬЮ (00:54), разнесено:** **В1 = А** — дверь карточки
ведёт сразу в гостя · **В2 = В** —
🔄 сторонний инструмент аналитики ВЕЗДЕ, **отмена №004 В4 = А записана в журнал решений
`MASTER_PLAN.md`** с цитатой; критерий выбора — удобство ИИ-агенту (он главный потребитель),
эталон Amplitude, заказ «изучить рынок» · **В3 = А** — мониторинг ошибок заводим ·
**В4 — «Доверяю тебе»**: порядок работ утверждён доверием.

**✅ ИНТЕРВЬЮ №050 ОТВЕЧЕНО (чатом, три захода правок):** дверь = **Д3 «Панель после
касания»** с правками владельца — тексты панели его словами («Ваша оценка засчитана!» ·
«В Пространстве NDim множество людей — оно поможет Вам найти тех, кто разделяет Ваши
взгляды.») · большая кнопка «Войти в Пространство» с пульсацией по контуру · карта узлов с
линиями связей: пульс 15 с, едва заметные, В НОЛЬ НЕ ГАСНУТ, в светлой теме синеватые (не
серые). **Эталон вида — `design/catalog-door-mockups.html`, вариант Д3** (правки всех трёх
заходов внесены, кадры смотрены). Его инвариант: «*и карточку не обеднять*» — красным в
`plans/75`.

**Аудит ДОДЕЛАН тем же днём:** живой прогон боя «карточка → дверь → стена» (стена снята
кадром: две большие кнопки входа, гость — мелкая ссылка) — дополнение в отчёте; инженерная
разведка исполнения — `plans/75` → «Разведка исполнения» (🔴 карточки `csr = false` — НЕ
включать гидрацию, строить «ванильный остров»; движок входа готов — `test-engine.ts`).
`MASTER_PLAN.md` — ревизия 2026-08-28 (рельсы, шесть пунктов).

📜 Для истории: между №049 и рельсами владелец останавливал исполнение («*я просил тебя
аудит и твои документы по аудиту, всё, не более*») — начатые правки кода были откатаны
полностью, в бой ничего не уезжало. Урок в памяти агента: заказ задаёт объём, «доверяю
тебе» о порядке ≠ команда исполнять.


### 🟢 СЕССИЯ 39 (2026-08-28) — ОБНОВЛЕНИЕ KAIF 2.2 → 2.3 (заказ владельца в чате)

Bootstrap-маршрут по конвейеру EXP-0064/0103/0140: своя база сравнения бандлов → песочница →
бой (предсказание совпало файл-в-файл; счётчики 3 replaced / 1 added / 75 kept) → 15 из 16
модулей сшито вручную в переведённые файлы → `sync` зеркал (152 копии) → судья **VERIFIED WITH
CAVEATS** → `update-verify` зелёный. Задание 2.3 честное: 0 ложных «NEW module».
· **Что приехало в канон:** «Форма обязательства — команда, шаг или чекбокс» (`AGENT_GUIDE.md`) ·
  языки теперь ПО АУДИТОРИИ (наше «всё по-русски» правило прямо подтверждает) · цепочка тестовых
  активностей + правило 7 маркеров (`TESTING_FRAMEWORK.md`; наши дома тест-доков `qa/` названы
  в врезке) · чек-лист письма требований (`REQUIREMENTS_FRAMEWORK.md`) · вопрос механизации
  первым шагом `/experience` (поле `Mechanization:` теперь обязательно).
· 🔴 **Единственное расхождение с каноном, записано честно:** шаг 4 доставки в `/report-bug`
  (автономная отправка тикетов в исток, issue #15) дважды заблокирован классификатором агентской
  системы — локальный скилл СТРОЖЕ канона, пометка в самом файле; тикет `bugs/KAIF/01`.
· Полевой отчёт: <!-- KAIF-VERSION-OK: имя исторического отчёта об обновлении 2.3, не заявление текущей версии -->
  `reports/KAIF_UPDATES/NDIM_SPACE_KAIF_2.3_UPDATE_REPORT.md` + issue в истоке.
· 🆕 **Стоячее ДА владельца на Edit/Write** (его слово 2026-08-28: «не спрашивай у меня разрешения
  на редактирование файлов») — записано в `.claude/settings.local.json`.
· ⏭ Беклог сессии 38 не тронут: 12 возвращённых карточек и партия 6 (33 из 60) ЖДУТ ПЕРВЫМИ.

### 🟡 СЕССИЯ 38 (2026-08-23, вечер) — РАБОТА ДО ПОЛУНОЧИ ПО СЛОВУ ВЛАДЕЛЬЦА

#### 🔴🔴 ГЛАВНОЕ СОБЫТИЕ — ВЛАДЕЛЕЦ СНЯЛ ПРАВИЛО, КОТОРОЕ АГЕНТЫ ВЫДУМАЛИ И ПРИПИСАЛИ ЕМУ

Канон мастерской нёс строку «у ФИЛЬМА год берётся по первой дате реестра». Его слово дословно:
«*это неправильное лживое правило, я его НЕ ЗАДАВАЛ, его выдумали ИИ агенты. Я говорил много раз,
что год по ДАТЕ ПЕРВОГО ВЫХОДА В МИР, ни по каким-то там блять реестрам. Когда показано было
впервые — тогда и дата выхода*».

**Что теперь закон:** год = самый ранний показ людям (фестиваль, эфир, продажа, ранний доступ).
Реестр — источник даты, часто неполный, а НЕ определение года. ✅ Ранний доступ игры есть выход в
мир («*ранний доступ берём, да*»).

🔑 **Замер последствий, а не догадка** — `tools/audit-candidate-years.mjs`, прогон по всем партиям
1–6: **91 карточка, расхождений 0**. Выдумка неверных годов не оставила. И там же видно, почему
она держалась: у 89 карточек из 91 первая дата реестра И ЕСТЬ первый выход в мир.

🔴 **Урок класса (`EXP-0209`, память агента):** правило в каноне без ДОСЛОВНОЙ цитаты владельца —
это правило агента. Соседство с настоящими правилами придаёт выдумке их вес. Пишешь правило от его
имени — сначала найди, где он это сказал.

#### ✅ ЗАКРЫТО ЗА СЕССИЮ

| Что | Доказано |
|---|---|
| **Текст подтверждения заявки — его слово**, оба места и оба языка | коммит `2000f0d`; в сборке нового текста два вхождения, старого — ноль |
| **ВЫКАТ В БОЙ ЗАВЕРШЁН И ПРОВЕРЕН** (`c3daaae`, сборка `12xme7t`) | стейдж 24/0 + 81/81 · бой 24/0 под сессией + 81/81 гостем |
| 🔴 **`bugs/196`** — смоук падал стек-трейсом вместо вердикта на пустом `document.body` | три прогона одного артефакта дали красный, зелёный и падение; после правки 24/0 трижды |
| 🔴 **`bugs/197`** — дверь выката НЕ ЧИТАЛА `.env`: пропуск App Check не доезжал до прибора | из чистой оболочки дверь обновляла бой и падала на собственном рубеже |
| 🔴 **`bugs/198`** — в образце класса 16 стоял НАСТОЯЩИЙ байт забоя вместо границы слова | ветка «приём» не срабатывала НИКОГДА; страж границы научен видеть сырые управляющие символы, мутационная проба на живом файле |
| **`bugs/195` закрыт** + второй дефект той же строки: «сегодня» бралось в UTC | вечером прибор судил завтрашним днём; дата собирается из локальных часов |
| **Правило часов машины** (`candidates/README.md` → «Часы машины») | слово владельца: «*проверить локальное время машины ОБЯЗАТЕЛЬНО!!!! И дальше насрать, как написана википедия*» |
| **27 карточек этой сессии лежат у владельца** | обе приёмки чистые на всех 27; порции 1–5 |
| 🔑 **ЗАМЕР БОЕВОЙ ОЧЕРЕДИ на выходе из чата** (судейский проход перед push) | всего 78 · **pending 35** · approved 31 · **returned 12** |
| 🆕 **РАЗВЕДКА КУЛЬТОВОЙ КЛАССИКИ** — `tools/scout-cult-classics.mjs` | осмотрено 960, наши 504, **нет у нас 456**; отчёт `candidates/scouts/2026-08-23_cult_classics.md` |
| **Правило целых памятников** — «*Берём целые*» | часть целого отдельным измерением не идёт, если целое уже объект |

#### 🔴 ЧЕТЫРЕ РЕШЕНИЯ ВЛАДЕЛЬЦА ЗА ВЕЧЕР — ВСЕ ДОСЛОВНО В КАНОНЕ

| Его слово | Что стало законом |
|---|---|
| «*проверить локальное время машины ОБЯЗАТЕЛЬНО!!!! И дальше насрать, как написана википедия*» | часы машины — единственный судья «вышел или нет» |
| «*год по ДАТЕ ПЕРВОГО ВЫХОДА В МИР, ни по каким-то там блять реестрам*» | правило «по первой дате реестра» СНЯТО как выдуманное агентами |
| «*Берём целые*» | священные тексты: памятник целиком берём, отдельную часть — нет |
| «*это не должны быть ворота, а должно быть пожелание на контент*» + «*у практики «Секс» ты тоже будешь дату релиза искать?*» | класс 16 переведён в ПОЖЕЛАНИЕ (`bugs/199`) |
| «*VIII в. до н. э. — да, норм*» | форма года древнего памятника, одна на все такие записи |

🔑 **Два из пяти — отмена того, что агенты выдумали и приписали ему.** Оба держались потому, что
почти всегда давали верный ответ: год по реестру совпадал с истиной у 89 карточек из 91, а класс 16
был верен для кино и неверен для практики и древности. Правило, совпадающее с истиной в девяти
случаях из десяти, не проверяет никто — его проверяет только десятый.

#### 🧭 СТРАЖ МЕСТА ВОПРОСОВ на выходе из чата (`npm run questions`)

- ✅ **Новых вопросов владельцу вне `interviews/` этот чат НЕ ЗАВЁЛ.**
- 📉 Унаследованный долг: **25 очередей вопросов** вне `interviews/` и **10 неулаженных допущений**
  `PENDING:`; прогон красит 🔴 5 провалов. Долг был до этой сессии, обязан убывать по одному при
  каждом касании документа.

#### ⏭ ПЕРВЫМ ДЕЛОМ СЛЕДУЮЩЕЙ СЕССИИ

1. 🔴 **ПЕРВЫМ — 12 ВОЗВРАЩЁННЫХ КАРТОЧЕК, а не новые.** В боевой очереди `returned = 12`: это
   правки, которых владелец уже ждёт, и он на них потратил своё время. Читать:
   `node tools/read-returned-candidates.mjs --contour prod`.
2. 🔴 **Партия 6 — 27 из 60.** Дописать 33 карточки порциями по 10, класть в очередь сразу.
   ⚠️ В очереди у владельца НЕ 27, а **35 pending**: восемь карточек прежних порций он ещё не
   разобрал.
   Источники: `test-results/scout-cult-classics.json` (456 культовых) и остаток годовой разведки
   (83 кандидата 2026 года). Владелец ждёт вычитку и уже её начал.
3. **Верх классики по видам, откуда брать:** кино 16 (Броненосец «Потёмкин» и «Римские каникулы»
   уже написаны) · сериалы 100 (Доктор Кто, Улица Сезам, Как я встретил вашу маму) ·
   игры 109 (Tomb Raider, Civilization, Resident Evil 2, Super Mario 64) ·
   книги 196 (Илиада, Махабхарата, Дон Кихот, Божественная комедия, Эпос о Гильгамеше).
4. ⚠️ **Возвраты владельца подхватывать по ходу** — `node tools/read-returned-candidates.mjs --contour prod`.
5. **Альбомы (16 в годовой разведке) НЕ берутся** — под музыку нет проверенных правил мастерской.
6. **Хвосты по карте сайта, названные владельцу и ждущие его решения:** `lastmod` в карте не
   пишем вовсе · IndexNow был отвергнут посылкой «страницы меняются раз в неделю», ночная сборка
   эту посылку сняла — разведку надо перемерить.
7. 🔴 **Идея, которую владелец ещё не решил:** форма заявки обязана искать за человека. Три объекта
   подряд он предложил в работу, и все три в каталоге УЖЕ БЫЛИ («забыл, что он есть»). Заявка
   «добавьте вот это» — сигнал, что человек не нашёл вещь у нас.

### 🟢 СЕССИЯ 38 — ПОДРОБНОСТИ ПЕРВОЙ ПОЛОВИНЫ

**Заказ владельца дословно:** текст подтверждения заявки «*это дебилизм какой-то сформулированный
ИИ*» → заменить; «*как исправишь — билд, деплой стейдж, деплой прод. МОЁ ДА на оба деплоя*»;
«*я там два измерения предложил в работу, проверь, видишь ли ты их*»; «*я заказывал 60 измерений,
всё ещё жду их*».

#### ✅ ЗАКРЫТО

| Что | Доказано |
|---|---|
| **Текст подтверждения заявки — его слово** («Ваша заявка отправлена и будет принята в работу. Спасибо за Ваш вклад в развитие Пространства NDim!»), оба места и оба языка | коммит `2000f0d`; в сборке нового текста два вхождения, старого — ноль |
| **ВЫКАТ В БОЙ ЗАВЕРШЁН И ПРОВЕРЕН** (коммит `c3daaae`, сборка `12xme7t`) | стейдж 24/0 + 81/81 · бой 24/0 под сессией + 81/81 гостем · попадание в контур: сосед не тронут |
| 🔴 **`bugs/196` — смоук падал стек-трейсом вместо вердикта.** `document.body` бывает `null` во время навигации, защита `\|\| ''` стояла на `innerText` | три прогона ОДНОГО артефакта дали красный, зелёный и падение; после правки — 24/0 трижды подряд |
| 🔴 **`bugs/197` — дверь выката НЕ ЧИТАЛА `.env`.** Пропуск App Check лежал в файле и не доезжал до прибора: `loadEnv()` звали только `credentials.mjs` и `probe-search-console.mjs` | из чистой оболочки дверь **обновляла бой и падала на собственном последнем рубеже**; после правки `debugToken()` печатает «ЕСТЬ» |
| **Заявки людей стали видимыми без панели** — `tools/probe-prod-suggestions.mjs`, только чтение | 7 боевых заявок прочитаны; база контура названа явно (`ndim-db-prod`), молчаливый `(default)` давал NOT_FOUND |

🔑 **Урок сессии, оплаченный дважды:** дверь, которая зависит от НЕНАПИСАННОГО ручного шага
(экспорт переменной в оболочке), выглядит исправной ровно до первого чистого окружения. Класс тот
же, что «зелёное на непроверенном»: правило есть, механизма под ним нет.

#### 📋 ДВЕ ЗАЯВКИ ВЛАДЕЛЬЦА — РАЗОБРАНЫ

| Его заявка | Вердикт |
|---|---|
| «Супергеройский фильм СПАВН 1997» | 🆕 в каталоге НЕТ (`Q1474393`, 27 языковых разделов) — идёт первой карточкой партии 6 |
| «Фильм Возвращение 2027 про Одиссея» | ✅ УЖЕ ЕСТЬ: «Возвращение Одиссея» / *The Return* 2024 (`ef7SRAVO1OlL8IH1MfHi`); рядом «Одиссея» / *The Odyssey* 2026. ❓ Владельцу задан вопрос: искал и не нашёл (тогда это дефект поиска) или предложил на всякий случай |

#### ⏭ ПАРТИЯ 6 — В РАБОТЕ, ЗДЕСЬ ПРОДОЛЖАТЬ

Разведка прогнана заново (`node tools/scout-wikidata-candidates.mjs --year 2026 --min-sitelinks 8
--limit 300`): найдено 133, наших 19, новых **114**; за вычетом партий 1–5 — **83 кандидата** с
доказанной ПРОШЛОЙ датой. Отчёт и факт-листы лежат в скретчпаде сессии, не в дереве.

🔴 **Сдача порциями по 10**, чтобы владелец начал вычитку не дожидаясь шестидесяти. Мерило —
`candidates/owner-critical-notes-2026-08-22.md` (16 классов), а не общий вкус.

Порция 1 — факты собраны, тексты пишутся: Спаун `Q1474393` · Рыцарь Семи Королевств `Q120277524` ·
Чудо-человек `Q114779504` · Паук-Нуар `Q123956515` · Возвращение в Сайлент Хилл `Q114771310` ·
Железное лёгкое `Q117815027` · Грязные деньги `Q123172835` · 007 First Light `Q102290641` ·
Crimson Desert `Q85979990` · Pragmata `Q96240015`.

⚠️ **Альбомы (16 штук) в партию не берутся:** под музыку нет проверенных правил мастерской —
то же решение, что в партии 5.

#### 💡 ВОПРОС ВЛАДЕЛЬЦА ПРО КАРТУ САЙТА — ОТВЕЧЕН, ХВОСТ ОСТАЛСЯ

Он спросил, надо ли загружать новый sitemap в поисковые системы после ночной сборки. Ответ: нет —
карта пересобирается сама из каталога, её адрес стоит в `static/robots.txt`, а пинг карты Google
отключил в 2023 году (`researches/26` §11.4), Bing — в 2022.

🔴 **Два хвоста, оба названы владельцу, оба ждут его решения:**
1. `lastmod` в карте НЕ пишем вовсе — Google учитывает его как сигнал расписания обхода;
2. **IndexNow был отвергнут посылкой «страницы меняются раз в неделю деплоем»** (`researches/26`
   §11.5). Ночная сборка эту посылку сняла: каталог растёт каждый день. Разведку надо перемерить.


### 🟢 СМЕНА 4 (2026-08-22, сессия 36)

**Запуск по манифесту:** работа QA прошлой смены смержена; ветки `ndim_qa` и `ndim_dev2` сброшены
от свежего `main`; троим с несмерженной работой дельта канона названа поимённо. Пять ролей прошли
`/resume` и доложились.

#### ✅ ЗАКРЫТО ЗА СМЕНУ — с числами, а не со слов

| Что | Чем доказано |
|---|---|
| 🔴 **`bugs/169` — App Check отдавал 403, выкаты были заперты. ЗАКРЫТ.** Владелец разрешил агенту завести пропуск (интервью №046, В1 = Б: «Б — Разрешаю») | пропуск заведён `tools/app-check-debug-token.mjs`, выдан обоим боевым смоукам, **живой бой: смоук под сессией 24/24, провалов 0**. App Check НЕ выключен — он работает и проверяет живых людей |
| **Три ветки отсужены и смержены** | dev-3 (пятая проверка ворот) · dev-1 (близнец на странице теста + `stand-addresses` + шаг 5 `plans/56`) · Дизайнер (партия 5, 24 карточки) |
| **Растяжка каталога перемерена** | каталог **5121 → 5125** (одобрения владельца), покрытие хабами +4, `unknown` = 0 · оценённых фильмов **981 → 983** — голоса ЖИВЫХ людей · байесовская опора C 8,2405 → **8,2401** |
| **Стрижка `STATUS`** | 1025 → ~860 строк; эстафеты сессий 32–35 в летописи ДОСЛОВНО |
| **Обзор индустрии эпика** (`researches/50`) + **метаплан** (`plans/71`) | шаг 7б закрыт по первоисточникам; пять развилок владельцу собраны |
| **Интервью №047 и №048 отвечены и разнесены** | канон мастерской получил ТРИ новых раздела |

#### 🔴 ГЛАВНОЕ СОБЫТИЕ СМЕНЫ — ДЕВЯТЬ ЛОЖНЫХ ЗЕЛЁНЫХ, и они дали КАНОН

Девять приборов оказались зелёными там, где были обязаны краснеть. Нашли их **разные роли,
независимо, каждая в своей зоне**. Полный разбор — в документах; здесь то, что пережило смену:

🔑 **`AGENT_GUIDE.md` → «ЛЕСТНИЦА ТРЁХ ВОПРОСОВ К ЗЕЛЁНОМУ ПРОГОНУ»** (коммит `3fbb3f4`).
У зелёного бывают ТРИ разные причины, и лечения не пересекаются: **исполнилась ли проверка вообще**
→ **верен ли её признак** → **мог ли материал дать ей упасть**. Порядок обязателен.
⚠️ Правку заказал автор третьего вопроса ПРОТИВ расширения собственного урока: «класс, который
объясняет всё, не различает ничего». Менеджер собирался приписать его классу все девять — ошибка
остановлена ролью.

#### 🟡 В РАБОТЕ — где что лежит

| Роль | Ветка / голова | Состояние |
|---|---|---|
| **dev-2** | `ndim_dev2` **`af6befe`** | 🔴 **ЩИТ ВЛАДЕЛЬЦА ПОЧИНЕН НА СТЕНДЕ**, ждёт суда QA ПЕРВЫМ. Замер: `/ru` 0/2 → **2/0** · `/` 0/1 → **1/0** · `/profile` 0/0 → 0/0. Плюс фаза 2 парка стендов, ворота guards, страж словаря |
| **dev-1** | `ndim_dev1` **`149d809`** | припаркован. Семь коммитов, пять работ: парность кавычек · партия 1 `plans/70` (**в базу НЕ записана**) · страж `bugs/177` слой 1 · вторая цель правки (`bugs/180`) · три записи опыта |
| **dev-3** | `ndim_dev3` **`619d6fb`** + работа | чинит слепоту доли меты (лексикон + порог длины), затем перемер 360 |
| **Дизайнер** | `ndim_designer` **`70af6b0`** | припаркована. Признак пропорции сдан |
| **QA** | `ndim_qa` | судит dev-2, затем dev-3, затем признак |

#### 🔴 ЩИТ ВЛАДЕЛЬЦА — ЧТО ЗНАТЬ ТОМУ, КТО ДОВЕДЁТ ЕГО ДО БОЯ

Владелец жалуется ТРЕТИЙ раз: «*до сих пор я вижу в своём PWA и на десктопе лендинг при
открытии… давно прошу это исправить*». Замер по ЖИВОМУ БОЮ (`tools/probe-bug40-live.mjs`):
**вошедший видит лендинг 39 кадров из 40**, щита — ноль.
**Причина:** щит поднимался по условию `p === '/'`, а лендинг переехал на `/ru`/`/en` (`plans/39`).
Корень стал распознавателем языка и уводит `location.replace('/ru')` в первом кадре. Класс
`bugs/129` — «условие „всё, что не X, — это Y“ живёт до появления Z».
🔵 **И число, отменяющее догадку:** вход `/profile` (`start_url` PWA) лендинга НЕ показывал ни до,
ни после. Жалоба про PWA объясняется старой иконкой или закладкой на голый домен, а не этим входом.
⏭ **Осталось:** вердикт QA → мерж → выкат → зонд по бою (ожидание 39/40 → **0**) → слово владельца.

#### 🔵 ТРОЙКА ПО `bugs/180` — заказ владельца, приёмка переехала с его глаз на правила

Его слово: «*я вычитывать 360 правок не буду. Запускай судью и ревьювера и правщика*».
· **Ревьювер сдал признак** (`tools/lib/genre-depth.mjs`, калибровка на ЕГО текстах, полоса 1…3):
  производство впереди жанра — **было 0 → стало 66**, из них русских **0**. Дефект СОЗДАН правкой
  из нуля, целиком на английской стороне. Список — `reports/rewrite-proportion-worklist.json`.
· **Правщик починил ЦЕЛЬ правки:** дверь не пропускает правку, ухудшившую пропорцию. Правило взято
  ДЕЛЬТОЙ, а не порогом — порогу пришлось бы назначать цифру, которую может назначить только владелец.
· 🔴 **ПОРЯДОК РАБОТ, решение Менеджера:** прибор → перемер 360 → переделка 66. Переделывать при
  слепом гейте значит проводить работу через ворота, которые не умеют её проверить.
· 🔴 **Мера: признак меряет 352 ЗАПИСАННЫХ.** 360 — то, что владелец видел на странице; 355 снято.
· ⛔ **У части записей зачин трогать НЕЛЬЗЯ** — там дословно текст владельца (`the-matrix`).

#### 🔵 СТОЛ ВЛАДЕЛЬЦА

Интервью 046, 047, 048 отвечены. Ждёт: **интервью эпика «Органика поверхностей»** (пять развилок,
собирается) · переустановка PWA на телефоне — иначе splash 2.0 он не увидит · `plans/58` шаг 1
(место в очереди не назначено) · музыкальная поправка канона мастерской (партия 6 заперта ею).

#### ⏭ ДОЛГИ, НАЗВАННЫЕ ВСЛУХ

· 🔴 **Зелёный ворот приёмки по партиям кандидатов 1–4 НЕДОКАЗАН** до пересверки починенным прибором.
· 🔴 **Три русские половины партии 5 честно НЕ ПРОВЕРЕНЫ** — русских статей у фильмов нет вовсе.
· 🔴 **Слепота доли меты — ДВА механизма и она РАСТУЩАЯ**; пока не починена, любое число доли —
  сравнение, а не факт.
· `bugs/179` — рамка reCAPTCHA роняет «консоль чиста» на первом контексте; дверь краснеет через раз.
· **Решение Менеджера, постановка не написана:** тяжёлым стражам (нужен стенд или браузер) — СВОЯ
  дверь, отдельный набор «под замком стенда». Класс «страж, которого не гоняют» встретился четырежды.
· Не дострижено в `STATUS`: секция «восемь строк» и реестр стражей.

📉 **Стрижка бонсая проведена:** 1025 → ~860 строк, эстафеты сессий 32–35 ушли в летопись
дословно. ⏭ Не дострижено и названо: секция «восемь строк» и реестр стражей (дублирует таблицу
`AGENT_GUIDE.md` → «Испытательный стенд»).

### 🅿🅿🅿 ПАРКОВКА ДНЯ 2026-08-22 (сессия 35, третья смена) — эстафета, из которой выросла смена 4

**Бой (выкачено дверью, стейдж 24/0 + 81/81, прод-смоук 22/24):** `bugs/172` (карточка не
обстригается перед отъездом) · `bugs/173` (одобрение СОХРАНЯЕТ комментарий владельца —
**владелец может судить карточки**) · возврат ключа App Check. Пост-выкатные 4 правки
партии 1 применены писарем (`--apply`, журнал + откат-файл в `homeworks/`). Контейнер
сервера синхронизации перезапущен — утренние одобрения (включая «Кобру») въехали в индекс.

🔴 **App Check: дверь остановилась ПОСЛЕ выката на именованном 403** —
`POST …exchangeRecaptchaV3Token ×4`, у сессии и у гостя, на каждой странице. **Диагноз
переосмыслен** (детали — секция App Check ниже): Monitoring КОПИТ метрики, принуждения нет —
живые люди не теряют ничего; отказ, вероятнее всего, ловит НАШИ headless-приборы (роботный
счёт reCAPTCHA v3). **Заперты только ворота двери: любой следующий выкат упрётся в «консоль
чиста»,** пока приборы не получат debug-токен. 🔵 **У владельца два действия** (секция ниже) —
без них НЕ ДЕПЛОИТЬ. Слово владельца на парковке: RC без тестирования не деплоить — нового
продуктового кода в `main` после выката НЕТ, всё непоехавшее живёт в ветках и ждёт судов.

**Смержено за смену (все — с вердиктом QA):** уши адресов ≥ 400 в боевых смоуках +
`bugs/174_DONE` (лотерея смоука: состояние вместо секундомера) · dev-2: `bugs/173` + парк
стендов фаза 1 (`plans/69` ✅: слоты, «стенд N из 3», три стенда жили разом, `npm run guards`
1,6 с) · dev-1: `bugs/172` + П4 `plans/70` + уборка tmp + сверка партии 1 (`bugs/176` — свод
затирался адресным прогоном) · Дизайнер: партия 4 (20 карточек, провенанс 20/20) + контракт
ворот `researches/47` + разведка партии 5 `researches/48` + макеты двери в Рейтинги · dev-3:
английские ворота П5 + прибор доли меты + фиксы скаута (52 % ложных новинок сняты, сериалы
добраны через сезоны). Номера смены: `bugs/175` `bugs/176` · `EXP-0191…0194` ·
`researches/47…49` · `ideas/44` `ideas/45`.

**🔵 СТОЛ ВЛАДЕЛЬЦА (всё подано, ждёт его):** партия 4 — 20 карточек в боевой комнате
(Панель администратора → Менеджер измерений → «Кандидаты»); две несут вопросы в записках
(«Седло» — годится ли объект; «Каратель» — вид) · App Check: сверить ТИП ключа (классический
v3, не Enterprise) и домен в админке reCAPTCHA + создать DEBUG-ТОКЕН (App Check → Apps →
Manage debug tokens) и передать значение — ЛИБО разрешить агенту REST-создание · классика:
«Служебного романа» в каталоге нет — предложена разведка «дыры классики» · музыкальная
партия 6 (17 альбомов, пул в `researches/48` §4) после музыкальной поправки канона ·
`ideas/44` (mutate()) ждёт одобрения · `plans/58` шаг 1 (разметка 5121) — место не назначено.

**НЕСМЕРЖЕННОЕ — ждёт судов свежего QA (мержить ТОЛЬКО после вердикта):**
· dev-1 `dec1b96`: близнец `bugs/172` на странице ТЕСТА (bugs/182 — номер присвоен при мерже;
  крупнейшая поверхность притока; суд НЕ НАЧАТ — честная строка QA; окно записи ~100 мс —
  прибор предупреждает сам, судья обязан уронить мутацией) + общая база адресов
  stand-addresses (замечание QA о литералах + четвёртое место грепом) + **шаг 5 `plans/56`
  СДАН: приёмка выборкой и весом, 540 проверок / 0 провалов, 40 кадров** — всё одной веткой.
· Дизайнер `8a0bdaf` (запушена им — предохранитель пропустил по существующему отслеживанию):
  **порции 1–2 партии 5 — 12 карточек из 24, ворота зелёные 24/24 сверено**; очередь оставшихся
  12 — в шапке файла партии. ⚠️ Мерж-предупреждение его же: `candidates/README.md` правлен с
  обеих сторон — конфликт решать В ЕГО сторону по блоку «47 %» (в `main` этого блока нет).
  Три места молчания реестра названы в `agentNote` (Splatoon Raiders — издатель вместо
  разработчика; Black Flag Resynced — автор по статье, отступление названо; сценаристы без
  русских меток не вошли).
· dev-3 `6e7d1eb`: пятая проверка ворот — ЛАТИНИЦА в русском описании (готова: смешение
  алфавитов = отказ, 0 ложных на 56 карточках; строчная латиница = замечание-`notes` —
  «screenlife» режиссёра красить нельзя). Его кусок 2 (замер имён в текстах) НЕ НАЧАТ.
· QA `d0457fa` (запушен Менеджером): журнал вердиктов + `EXP-0195` (чужое число без метода)
  (номер при мерже).

**Очереди свежих окон ролей:** QA — 🔴 словарь-долг `verify-product-vocabulary` по собранному
сайту ПЕРВЫМ (его просьба: долг пережил две смены), затем суд dev-1 (dec1b96, близнец — с
нуля), суд dev-3 (6e7d1eb), суд порций Дизайнера (8a0bdaf), затем его грумминг ideas/45 ·
dev-1 — ПАРТИЯ 1 `plans/70` с врезкой «ПАРКОВКА dev-1» в плане и ТРЕМЯ вводными его финала:
(1) 🔴 РЕШЕНИЕ МЕНЕДЖЕРА по нарезке: все трое строят задание из ОДНОГО восстановленного
файла — 533 копии / 107 партий, нумерация общая и проверяемая; три записи добора доснять
ПОЗЖЕ отдельной партией, границы не двигать; (2) свод затёрт его же `bugs/176` — ПЕРВОЙ
командой `node tools/restore-overlap-json-from-md.mjs --lang en` (прибор сам сверяет числа
с шапкой отчёта); (3) ловушка первого действия: `corrective-measures-akp4cezf` — ЖИВОЙ
КОНТРОЛЬ английского свода, перевесить контроль на запись ВНЕ партии 1 ДО его правки;
настоящих правок в партии 1 — ЧЕТЫРЕ (panda-plan — цитата в кавычках, не трогать) ·
dev-2 — микро-вставка guards (рантайм-крик про неотслеживаемые: `git ls-files --others`) →
фаза 2 парка (план НЕ написан; мины названы в `plans/69`: reuseExistingServer, потолок
воркеров) · dev-3 — кусок 2 (замер имён против реестра, предъявляет-не-судит) + канал
предъявления замечаний в воротах прозы · Дизайнер — порции партии 5, README:202 пережила
исполнение ВТОРОЙ раз (латиница), музыкальная поправка канона после слова владельца.

**Менеджеру-следующему:** синтез эпика `ideas/45` — вход `researches/49` (грумминг 4/5 сжат
туда ДО смерти чатов; хребет: ПЛОТНОСТЬ ПЕРВОЙ ФАЗОЙ — три роли независимо; предусловие:
круг «оценка→NDSR→выдача» разорван дважды — пререндер + `bugs/146`) → добрать грумминг QA →
обзор индустрии (шаг 7б) → метаплан → ОДНО интервью владельцу (развилки: две правды NDSR ·
якорение · дверь-макеты 16 кадров · приоритет «оживить против позвать») · страница вычитки
журналов правок описаний владельцу (311 + 4, №025 В6=А — долг) · хайп-график: «Хитрый Койот»
выходит 28.08 — вернуться к объекту в день релиза (`researches/48`) · стрижка бонсая STATUS
(файл разросся, закрытое — в летопись) · три ПРОГРЕТЫХ стенда не мерены (правило в
`plans/69`; ниже порога — вопрос подкачки владельцу) · `storage.cors.json` без слотовых
источников (`researches/45` §5.3, правка = выкат).

**Капканы смены (оплачены сегодня):** 🔴 пайп на команде с решающим кодом выхода —
`git merge … | tail` замаскировал конфликт, и `&&`-конвейер прокатился по конфликтному дереву
(маркеры доехали до origin, вычищены следом; впредь — pipefail или merge отдельным вызовом) ·
бэктики в `git commit -m "…"` исполняются шеллом (dev-1; сообщения — файлом) · `[A-Za-z]`
рвёт `\p{Script=Latin}`-слова с диакритикой («Cinéma» → три ложных слова) · переизданиями
были **52 %** «новинок» скаута — год теперь по МИНИМУМУ полного набора дат.


## 🔴 ГЛАВНОЕ ДЛЯ СЛЕДУЮЩЕГО ЧАТА — восемь строк, даже если больше ничего не прочтёшь

0. 🧪 **НОВОЕ ГЛАВНОЕ ДЕЛО — QA-МИССИЯ, заказ владельца 2026-08-15 ночью (`plans/54`).**
   Его слова: «*Наше приложение страдает от нехватки тестирования… а меня не хватает на то, чтобы
   тестировать. Нужно, чтобы ты покрывал тестами пути пользователей*». Три уровня глубины
   (**Smoke · Critical Path · Extended**), две группы (**позитив — всегда первым**, негатив),
   артефакты: тест-план, наборы, сценарии, чек-листы. Лестница пройдена: `researches/38`
   (ISTQB · ISO 29119-3 · пирамида Фаулера) → `plans/54` (8 критериев, 7 фаз).
   🔴 **Диагноз, ради которого всё затевается:** у нас **579 проверок и 59 стражей**, но они
   зовутся `verify-bug40`, `verify-bug65`… — тестирование организовано **ПО ДЕФЕКТАМ, а не по
   путям человека**, поэтому покрытие невозможно назвать, новый путь рождается непокрытым, а дыры
   находит владелец. Улика: `bugs/128` и `bugs/129` нашёл ОН, мимо них прошли все 579.
   ⛔ **59 стражей НЕ переписывать** — они доказаны мутациями; их размечают (`@covers`) и
   подключают к машинной трассировке.
   ✅ **Фаза 0 ЗАКРЫТА 2026-08-16 (сессия 17):** карта путей `qa/JOURNEYS.md` — 139 путей,
   13 областей, границы («пути, которых ещё нет» — фото, круги, дружба, чаты, отзывы) и решения,
   принятые без владельца. План фазы — `plans/55`, страж — `tools/qa-journeys-lint.mjs`.
   🔑 **Колонки «чем проверяется сегодня» в карте НЕТ намеренно:** это ручной реестр, запрещённый
   критерием К4 эпика. Ответ даёт машина в фазе 1, разбирая `@covers`.

0б. 🏗 **СТЕЙДЖ ЖИВОЙ: https://ndim-stage.web.app** (`plans/53`, заказ `ideas/36`). Отдельный проект
   `ndim-stage` на **Spark**, база `ndim-db-stage` (`freeTier: true`), правила выкачены, вход
   настроен владельцем и сверен с боем поле в поле. Приложение и сервер синхронизации умеют именованные
   базы; выбор контура делает **ХОСТ**, артефакт сборки один.
   ✅ **СТЕЙДЖ НАПОЛНЕН 2026-08-15 и СВЕРЕН ПО СЧЁТЧИКАМ КОЛЛЕКЦИЙ — расхождений 0.**
   `tools/migrate/seed-stage-from-prod.mjs` перенёс **10 605 документов** = 11 266 − 331 (`privat`,
   `bugs/127`) − 330 (`ndimids`, наследие 1.x); число в каждой коллекции сошлось с боевым.
   Личный слой обезличен (имя → синтетическое, «о себе» → пусто, фото → нет, день рождения → null
   при живых годе и месяце), **UID новые** — по решению №033 В1 = А как оно записано.
   🔴 **НЕ ПРОГОНЯЙ по стейджу `verify-db-parity` и не считай его результат поломкой.** Прибор
   судит ПУТИ и ХЕШИ, а у обезличенной копии с новыми uid они обязаны разойтись: прогон даёт
   **10 893 расхождения**, и все они объяснены по построению — 5443 «потерянных» + 5443 «лишних»
   это ровно документы, у которых uid в КЛЮЧЕ (`users` 362 · `points` 342 · `points/*/dims` 4066 ·
   `relations` 341 · `profile` 331 · `admins` 1), плюс 7 «искажённых» = 5 `suggestions` с
   перенумерованным автором и 2 документа, которые пишет сам сервер синхронизации стейджа.
   Поймано судьёй при закрытии сессии 15: прежняя формулировка «сверен, расхождений 0» провоцировала
   принять ожидаемое за аварию. **Document-level parity к обезличенной копии неприменим по
   построению** — это сказано и в шапке самого наполнителя.
   🔑 **Зарубка прошлой сессии была неправа, и это снято ЗАМЕРОМ:** она предлагала сохранить UID
   ради ссылочной целостности `friendships`/`audience`/`testPairs` — **все три коллекции в бою
   ПУСТЫ**. Ссылки на uid живут только в ключах и `relations.top[].uid`.
   ⚠️ **Наполнение стейджа — НЕ репетиция Р2** (вопреки той же зарубке): Р2 определена как копия
   ВНУТРИ боевого проекта со сверкой 100 % документов, а здесь данные обезличены и хеши заведомо
   не совпадут. Засчитать его за репетицию значило бы получить ДВЕ вместо ТРЁХ.
   ✅ **ТРИ РЕПЕТИЦИИ ПРОЙДЕНЫ** (`plans/53` фаза 2, протоколы там же): Р1 — процедура и переход
   бесплатного тира; Р2 — на боевых данных, **расхождений 0**; Р3 — откат из точки возврата,
   расхождение одно (`space/server`) и объяснено арифметикой. Прибор — `verify-db-parity.mjs`,
   доказан 3 мутациями.
   ✅ **БОЕВАЯ МИГРАЦИЯ ВЫПОЛНЕНА 2026-08-16** (AUTH: «*Да, начинай, мигрируем, даю добро*»).
   Бой живёт в **`ndim-db-prod`** (eur3, `freeTier: True`, защита от удаления возвращена).
   Сверка **11 266 = 11 266, расхождений 0** · смоук под сессией **24/0** · гостевой **81/0** ·
   людей 94 · измерений 5111 · связей 2344 — как до переезда. Простой — окно 31 минута при
   потолке 60. Необратимый шаг выполнял ВЛАДЕЛЕЦ скриптом `tools/migrate/run-prod-migration.ps1`:
   удаление боевой базы блокирует классификатор безопасности, и это законный рубеж.
   ✅ **Второй контейнер `ndim-server-stage` работает** — стейдж считает связи на своих данных.
   🔴 **ИМЯ БАЗЫ ЖИВЁТ В ДВУХ МЕСТАХ И ОНИ СВЕРЯЮТСЯ САМИ:** `src/lib/firebase.ts` (приложение) и
   `tools/lib/contours.mjs` (приборы). Второй читает первый и ПАДАЕТ при расхождении — правишь
   одно, правь другое.
   🔴 **Правила Firestore катает ТОЛЬКО дверь** (`npm run deploy`, первым шагом). Голый
   `firebase deploy` запрещён хуком, а до 2026-08-16 дверь правила не катала вовсе — выкатить их
   в бой было нечем. ⚠️ `--only firestore` ЦЕЛИКОМ: подтаргет `firestore:rules` при массивной
   форме завершается успехом и не катит НИЧЕГО (`firebase-tools#10447`).
   ✅ **ЭПИК ЗАКРЫТ 2026-08-16 (сессия 16):** фазы 4 и 5 пройдены, все девять критериев П1–П9
   измерены. Названный долг ровно один: не замерено, шлёт ли preview-канал `X-Robots-Tag` сам —
   каналы в работе не используются, и до первого их использования ответ ничего не меняет.

1. 🚪 **ВОРОТА ОТКРЫТЫ И ЖДУТ РУК.** Тексты обоих языков приняты и лежат в
   `src/lib/content/landing-copy.ts`; композиция выбрана владельцем — **V4 «Игра сама вносит
   внутрь»**. 🔴 **Следующий шаг ворот — НЕ вёрстка в одиночку, а мост эпика `plans/23`:** выбрав
   V4, владелец подтвердил №009 В3 — большой кнопки входа гостем нет, человека вносит внутрь сама
   панель, появляющаяся после звёзд. Дверь гостя в бою РАБОТАЕТ (`/profile?guest=1`), спрятана
   только тропинка к ней. Подробности — раздел 0 ниже.
   ⚠️ **Класс «замок пережил своё снятие» теперь ловится машиной:** `npm run questions` →
   «ПРОСРОЧЕННЫЕ ЗАМКИ». Прежде чем поверить строке «⛔ заблокировано» — прогони страж.

1. 🔴🔴 **ВЫКАТ ТОЛЬКО ЧЕРЕЗ `npm run deploy`. `firebase deploy` РУКАМИ ЗАПРЕЩЁН** (хук
   `.claude/hooks/deploy-guard.mjs` его блокирует). Дверь сама: замок стейджа → стоп-правило
   контура → правила Firestore → чистая сборка → **проверка, что в бандле ОДИН
   `__sveltekit_<хеш>`** → выкат → **замер попадания в контур** → заголовки → 🔑 **смоук ПОД
   СЕССИЕЙ** → гостевой смоук; красный шаг останавливает выкат. Канон — `AGENT_GUIDE.md` →
   «ВЫКАТ В БОЙ».

   🆕 **ДВА КОНТУРА — ОДНА ДВЕРЬ (сессия 16):** `npm run deploy` — бой, `npm run deploy -- --stage` —
   стейдж, `npm run deploy -- --gate-only` — спросить замок, ничего не выкатывая.
   🔴 **В БОЙ — ТОЛЬКО ЧЕРЕЗ СТЕЙДЖ** (интервью №033 В5 = А). Дверь боя требует расписку
   `.kaif/stage-receipt.json` с ТЕМ ЖЕ коммитом и чистым деревом. Незакоммиченная правка — это по
   определению код, которого на стейдже не было, и дверь так и скажет. Обход — только
   `--auth-owner "<дословные слова владельца>"`, ОДНОРАЗОВЫЙ.
   ⚠️ Честная граница: бой собирается со свежим снимком витрины лендинга, стейдж — нет (П9). Замок
   стережёт КОД, а не число на витрине.

   **Почему так, цена урока (ночь 2026-08-15).** Агент выкатил прод ТРИЖДЫ, проверяя только
   гостевые публичные смоуки. В бою всё это время лежало приложение, которое **не стартовало
   вовсе** (`TypeError … reading 'data'`), и нашёл это ВЛАДЕЛЕЦ. Причин было две: `build/` не
   очищалась и в бой уехала СМЕСЬ двух сборок (два разных хеша рантайма) · в `firebase.json` не
   было `headers`, хостинг кешировал час всё подряд. Оба вылечены (`bugs/124`), лечение
   механическое: `prebuild` сносит `build/`, дверь проверяет целостность ДО выката.
   Слово владельца: «**деплой без тестирования это пердёж в лужу, а не работа**».
2. 🆕 **Дефект, заведённый ЧТЕНИЕМ, живёт до тех пор, пока его не начнут НАБЛЮДАТЬ** — `bugs/120`
   простоял так сутки и вскрылся, лишь когда строка выросла. Разбор такта Г и `bugs/125`/`126`
   перенесён в летопись (сессия 15).
3. 🆕 **Смоук боя под сессией `verify-prod-signed-in` (24/0) — обязателен после КАЖДОГО выката**
   и встроен в дверь. Его первый прогон нашёл `bugs/125`, которого не видел ни один прибор.
4. 🔧 **Очередь починки `plans/49`: 8 из 13 закрыто** (`bugs/65` · `plans/12` · `plans/28` ·
   `bugs/117` · `plans/20` · 🆕 **`plans/47`** · 🆕 **`plans/42`** · 🆕 **`plans/46`**) плюс
   `bugs/122`, `bugs/120`, `bugs/126` вне очереди. **Осталось 5.**
   🆕 **ГОЛОВА ОЧЕРЕДИ ЗАКРЫТА 2026-08-16 (сессия 18):** построен страж `verify-test-pages`
   (**89** проверок по собранному сайту, 3 мутации: 2 · 6 · 1), `/fable-judge` по фазе 5 дал
   **VERIFIED WITH CAVEATS**, владелец принял фазу (интервью №034, В2 = Б) с условием про перенос
   длинных названий — условие исполнено замером и кадрами. `plans/42` помечен `DONE`.
5. 🔴 **План починки из `plans/49` — ГИПОТЕЗА, а не инструкция, и счёт уже 3:0 в пользу замера.**
   У `bugs/65` ложными были 2 шага из 4; у `bugs/117` ложным оказался ВЕСЬ диагноз (вину
   возлагали на восстановление прокрутки, а `scrollY` — ноль все 60 кадров); у `plans/42` план
   требовал проверить гостя в бою дверью `?as=none` — **в бою этот параметр не читается вовсе**
   (`profile.ts:97`, дверь только на стенде), состоянием «аккаунта нет» служит свежий контекст.
   Форензика снималась ЧТЕНИЕМ КОДА. **Меряй прежде, чем поверишь.**
6. 🆕 **Чтобы наблюдать сбой посреди операции — ломай КОД, а не окружение** (`EXP-0159`). Обрыв
   сети роняет вместе со шагом и саму страницу: так хвост `plans/20` простоял две недели
   «структурной гарантией». Временный `throw` в нужном шаге снимается одной правкой и не
   оставляет в продукте флага «для стенда». И проверку пиши ИНВАРИАНТОМ («не бывает, чтобы вход
   отобрали при живых данных»), а не описанием одного исхода: инвариант зелен в обоих исправных
   исходах, поэтому его не приходится отключать.
7. 🔴 **У ВИДИМОГО дефекта приёмка ПИКСЕЛЬНАЯ, а не габаритная.** `boundingBox`, ставший равным
   ширине окна, — это габарит; спор о том, что человек ВИДИТ. Я успел написать габаритный страж и
   поверить ему (`EXP-0155`). Контрольный кадр обязателен (`EXP-0082`).
8. 🔴 **Журнал опыта — ПОСЛЕДНЕЕ средство.** Владелец поймал: «пишешь опыт и это впустую — ты им не
   пользуешься». Порядок: **убрать ловушку → страж → и только потом запись** (`AGENT_GUIDE.md` →
   «Журнал опыта», `EXP-0157`). Урок про ДЕЙСТВИЕ живёт в строке этого действия в реестре
   инструментов: греп по тегам ЗАДАЧИ его не достаёт никогда.
9. 🔴🆕 **`probe-test-guest-live` ПИШЕТ В БОЕВУЮ БАЗУ — прочти его шапку ПРЕЖДЕ, чем запускать.**
   Оценка гостя настоящая и двигает NDSR; прибор убирает за собой органами продукта, и уборка
   живёт в `finally`. 🔑 **Ловушка, уже стоившая трёх настоящих гостей в проде:** продукт строит
   личную ссылку пары из БОЕВОГО `canonical`, поэтому стендовый прогон, пойдя по ней буквально,
   уводит второго гостя в бой. Лечение вшито в прибор (перенос ссылки на адрес стенда), но
   помни класс: **стендовый прогон может утечь в бой через данные, которые продукт отдаёт**.

## 📌 Приоритеты и поручения владельца — подробно

0. 🔴🔴🔴 **ГЛАВНЫЙ ПРИОРИТЕТ СМЕНЁН ВЛАДЕЛЬЦЕМ 2026-08-16 11:01 — РОСТ ЧЕРЕЗ SEO, по ЛАГУ
   СОЗРЕВАНИЯ** (интервью **№035, В1 = А**; заказ и три уточнения — в шапке интервью).
   Его слова, снимающие половину прежнего порядка: «*в день выката пока неактуально — у нас нет
   живой аудитории, приложение сейчас мертво без органического трафика и SEO*»; и критерий
   сортировки: «*что нужно быстрее брать в работу, что долго „зреет“ в поисковых движках*».
   🔑 **Правило груминга, выведенное из этого:** при пустой аудитории ценность работы =
   **масса поверхности × лаг созревания**. Работа с нулевым лагом (ворота, онбординг, конверсия)
   от ожидания НЕ дешевеет — SEO-работа дешевеет каждый день.
   **Порядок работ, утверждённый владельцем (В1 = А):**
   · **1) фаза 3 эпика 40** — обогащение каталога: одним заходом влияет на **10 222 уже
     проиндексированные** страницы; разблокирована, решений владельца не требует (№025 В1 = А,
     В6 = А). **Это работа №1 сессии 19, и она СПЛАНИРОВАНА: операционный план — `plans/56`**
     (6 шагов, у каждого своя проверка; вход шага 3 — готовый список 279 EN-записей
     `researches/34_en_boilerplate_candidates.md`). Начинать с шага 0 — разведка по живому.
   · **2) фаза 4 эпика 40** — опорные страницы: черновики пишет агент по `AUTHOR_STYLOMETRY.md`,
     владелец вычитывает страницей ОДИН раз (№025 В4 = А). Идёт ПАРАЛЛЕЛЬНО фазе 3.
   · **3) `ideas/37`** — миссия чтения консолей: Search Console хранит 16 месяцев и прошлое не
     восстанавливает, а фаза 8 требует ≥3 датированных замеров — без прибора каталог нечем
     принять. · **4) `ideas/33`** (язык оригинала) · **5) фаза 6** (распространение, разведка).
   ⏸ **Отложено словом владельца:** ворота и онбординг (`plans/21`/`22`/`23`), сегментация
   sitemap (замер: 10 432 адреса из 50 000 и 0,84 МБ из 50 — потолок далеко), `plans/38`,
   `ideas/38`. `bugs/121` — попутно внутри фазы 3, отдельным заходом не стоит.

0б. 🆕 **ДВА НОВЫХ ПОРУЧЕНИЯ ВЛАДЕЛЬЦА, отданные вместе с ответом №035** (дословно там же):
   · ✅ **СДЕЛАНО в сессии 18:** «*там сейчас "менеджер измерений", а я хотел Панель
     Администратора - общую. И через нее открывается панель, и там внутри разные инструменты
     администратора, один из них - Менеджер измерений*». Иерархия исправлена: дверь в «Меню» и
     заголовок раздела — **«Панель администратора»**, внутри — список инструментов, первый и пока
     единственный «Менеджер измерений» с честной пометкой «скоро». Страж `verify-admin-home`
     обновлён и проверяет ОБА имени. ⚠️ **`[NOT-TESTED]`** — страж требует живого стенда, а он
     умирает (`bugs/134`); перепрогнать первым делом, когда стенд поднимется.
   · 🔴🔴 **«Менеджер измерений» и АДМИН-ПАНЕЛЬ — ПРИОРИТЕТ ПОДНЯТ ВЛАДЕЛЬЦЕМ 2026-08-17 до «КАК
     МОЖНО СКОРЕЕ», и он назвал повод числом упущенного.** Дословно: «*нужно как можно скорее будет
     делать админ панель и менеджер измерений, чтобы ИИ агент мог новые кандидаты измерений
     создавать Николаю под вычитку, и чтобы николай быстрее их одобрял, и чтобы мы новые хайповые
     объекты культуры быстро заливали в пространство. Сегодня весь мир на хайпе от одисеи нолана —
     а мы такой кусок поисков пропускаем мимо нас. И много много новинок пока проходит мимо нас.
     Это нужно как можно быстрее делать, чтобы механизм был готов, и мы всегда могли бы в момент
     релиза добавлять новые хайповые вещи*».
     🔑 **И это УТОЧНЯЕТ его же правило груминга, а не отменяет его.** Правило гласило: ценность =
     масса поверхности × лаг созревания, поэтому SEO-работа от ожидания дешевеет. У хайпа лаг
     работает НАОБОРОТ: окно спроса на новинку **затухает**, и пропущенный релиз не догоняется
     никогда. Значит механизм заведения измерений — не «ещё одна SEO-задача в очереди», а
     **инфраструктура, чей простой ежедневно стоит упущенных поисков**.
     ⛔ **Прежняя формулировка «в ранние приоритеты, но НИЖЕ роста» УСТАРЕЛА** — не «чинить» к ней.
     Она стояла на прошлом слове владельца: «*Нужно тоже взять в ранних приоритетах менеджер
     измерений… Но названные тобою вещи — выше по приоритету*».
     📍 Территория: комната «Измерения» — `plans/44` (28 шагов, не начат), фаза 4 эпика `plans/30`;
     машинерия агентов-кандидатов — `ideas/29`, развилки — интервью №015.
     ⚠️ **Смежное, что этот заказ разблокирует и что надо помнить:** `bugs/135` (теги на обоих
     языках, 5104 объекта) лечится путём B — правкой 43 011 тегов в данных, и он ЖДЁТ ровно этой
     комнаты. То есть менеджер измерений снимает и его блокировку.

0в. 🔧 **ПОРЯДОК ВЫКАТА — механический, а не на честном слове** (слово владельца 2026-08-16 утром:
   «*будем фиксить в стейдже, тестить на стейдже, и только потом катить в прод, как настоящие
   профессионалы*»): правка → коммит → `npm run deploy -- --stage` → проверка на стейдже →
   `npm run deploy` в бой. Дверь боя требует расписку стейджа с ТЕМ ЖЕ коммитом и чистым деревом;
   ворота Smoke гоняют набор по собранному артефакту ДО выката, и им нужен живой стенд.
   ⚠️ Планы починки в `plans/49` — **гипотезы, а не инструкции**, счёт замера против плана уже
   **4:0** (свежий случай — `plans/47`: три ложных опоры сразу). Меряй прежде, чем поверишь.

1. **Фаза 1 QA-миссии — разметка `@covers` и страж покрытия.** ⏸ Уступает росту по слову
   владельца, но остаётся следующей по ценности работой QA-миссии. Единица покрытия названа и принята
   владельцем, набор Smoke работает и стоит в двери. Следующее — пройти 59 стражей, 13 e2e-спек,
   284 юнита, 118 правил и 77 тестов сервера синхронизации **по областям карты** (не по файлам!) и
   проставить `// @covers NDIM-…`, затем написать `tools/verify-qa-coverage.mjs` — он напечатает
   первое в истории проекта число покрытия. ⛔ 59 стражей НЕ переписывать.
2. 🔴 **`bugs/134` — стенд умирает от нехватки памяти, и теперь это блокирует ВЫКАТ** (ворота Smoke
   требуют живого стенда). Причина не установлена; в баге записана процедура замера: гонять смоук в
   цикле и смотреть `WorkingSetSize` по процессам — виновник тот, чей растёт. Обход на время
   расследования: `NODE_OPTIONS=--max-old-space-size=6144` перед `npm run stand`.
3. **Живые контуры для Smoke.** На стейдже и в бою почтовых кодов эмулятора нет, поэтому
   `NDIM-AUTH-003` и `NDIM-AUTH-005` там честно пропускаются. Решается замером: либо почтовый ящик
   прогона, либо явное разделение «стендовый полный / живой сокращённый».
4. **За владельцем (не блокирует):** `ideas/37` (миссия исследования поисковых консолей) и
   `ideas/38` (продуктовая аналитика вместо Amplitude) — оба его заказы, оба ждут очереди. У
   `ideas/38` внутри развилка: сторонний трекер отменяет его же решение №004 В4 «воронка своя, без
   трекеров».

⚠️ **Чего НЕ делать следующей сессии:** не «чинить» пропуск `NDIM-AUTH-003`/`005` на живых контурах
подкладыванием сессии снаружи — набор входит НАСТОЯЩИМ путём человека, и подмена сделала бы его
вакуумным (`EXP-0087`).

🆕 **Дефекты, заведённые в сессиях 14–16:**
· **`bugs/133`** ✅ (сессия 16) — стейдж не повторял боевых переадресаций: 10 адресов отдавали 404
  вместо 301. Нашлось ПЕРВЫМ ЖЕ прогоном прибора, научившегося `--base`. Цена расхождения выросла в
  тот же день: с фазы 5 прогон по стейджу обязателен перед выкатом, то есть правка переадресаций
  уехала бы в бой неотрепетированной. Лечение — копия ВМЕСТЕ со стражем расхождения.
· **`bugs/132`** ✅ (сессия 16) — пять приборов `tools/migrate/` ссылались на УДАЛЁННЫЙ файл ключа
  (`sync-server/secrets/sa.json`) и базу `(default)`, которой нет с переезда фазы 3. `STATUS` звал
  запускать два из них как есть. Класс — «истина ↔ зеркало»: модуль контуров завели, а приборы
  `migrate/` к нему не подключили. Проверено наблюдением: зонд коллекций читает `ndim-db-prod`.
· **`bugs/130`** ✅ и **`bugs/131`** ✅ (сессия 15) — отпечаток базы считал каталог ДВАЖДЫ, и ложное
  число уже уехало в `plans/53` как ожидание для критерия П1; второй объясняет, почему первый
  прожил незамеченным — сырые байты NUL делали исходник невидимым для `git diff` и `grep`.
· **`bugs/127`** (открыт) — в боевой базе живут `users/{uid}/privat/{uid}`: **331 документ**, поле
  `user_secret_key` непусто у всех. Ни `firestore.rules`, ни `schema.ts` о них не знают; закрыты
  лишь финальным deny-all. `researches/02` звал это «мёртвой заглушкой» — замер уточнил: заглушка
  пуста, а секреты живые. Класс `bugs/108`/`109`. **Миграция их переносит как есть — она ничего
  не чистит**, иначе сверка «ни один документ не потерян» не может дать 100 %.
· **`bugs/128`** (починен) — дверь вычитки умирала сразу после открытия браузера: в `process.exit()`
  уезжал адрес страницы. Владелец прислал кадр `ERR_CONNECTION_REFUSED`.
· **`bugs/129`** (починен) — на стейдже не войти: ссылка в письме строилась из БОЕВОГО домена.
  🔑 Урок класса: **условие «всё, что не X, — это Y» живёт ровно до появления Z**. При добавлении
  третьего чего-либо (контура, языка, роли) обход идёт не по новому коду, а по СТАРЫМ бинарным
  развилкам — их формулировка и есть список мест, где третье сломает продукт.
⚠️ **Считай «неразнесённых 25» счётчиком долга, а не доказательством разноса** — страж мерит
цитату рядом с номером интервью, и реестр-таблица гасит его, не сдвинув ни одного документа
(поймано на себе, `plans/50`).

🧱 **ДВА КОНТУРА С 2026-08-15.** Бой — проект `ndim-space` (**Blaze**, замер `billingEnabled: true`;
предпосылка «мы на бесплатном плане» неверна, но бесплатные квоты действуют и считаются **НА
ПРОЕКТ**). Стейдж — `ndim-stage` (**Spark**, бесплатен буквально). Выкат на стейдж:
`firebase deploy --config firebase.stage.json --project ndim-stage --only hosting` — хук
`deploy-guard` пропускает ТОЛЬКО явно названный стейдж, бой по-прежнему заперт за `npm run deploy`.
⚠️ Удалены: учебный проект `sandbox-ee18a` и мусорная база `sandbox2` (обе — словом владельца).

🔧 **Окружение на момент закрытия сессии 15:** стенд НЕ поднимался (эмуляторы гасились сами через
`emulators:exec`), сирот нет. **Оба контейнера сервера синхронизации РАБОТАЮТ:**
`ndim-server-prod` (проект `ndim-space`, база `ndim-db-prod`) и `ndim-server-stage`
(`ndim-stage` / `ndim-db-stage`), оба на образе `ndim-sync-server` билд 31, версия 2.1.0.
Ключи оба берут из `.env` переменной `NDIM_SA_B64`.

🚀 **Бой на 2026-08-16, ~00:53 — выкачен ПОСЛЕ миграции и проверен дверью:** правила и индексы ·
целостность сборки · заголовки · **смоук под сессией 24/0** · гостевой **81/0**.
Боевая база — **`ndim-db-prod`** (eur3, `freeTier: True`, защита от удаления ВКЛЮЧЕНА).
Точки возврата в `gs://ndim-space-backup/`: `2026-08-15-pre-migration` и `2026-08-16-migration`
(последняя снята при замороженной записи непосредственно перед переездом).

⚙️ **РЕЖИМ УСИЛИЙ (слово владельца, сессия 8):** по умолчанию **max**. **ultracode + workflows**
владелец заранее НЕ включает — **агент обязан САМ попросить**, назвав, что даст фан-аут. Не
попросил — работаешь на max в одиночку; молчаливое «я бы сделал лучше многоагентно» не считается.
📌 Ревизия беклога и аудит кода прошли многоагентно с его прямого разрешения — прецедент есть.

**Что делать, по убыванию ценности:**

### 0. ⏸ ВОРОТА — ОТЛОЖЕНЫ владельцем 2026-08-16 (не трогать до притока)

Его слова: «*в день выката пока неактуально — у нас нет живой аудитории*». Работа с нулевым лагом
не дешевеет от ожидания, поэтому ворота ждут, а SEO идёт вперёд (см. приоритеты выше).

**Состояние на момент паузы, чтобы не переоткрывать:** тексты обоих языков приняты
(`src/lib/content/landing-copy.ts`), композиция выбрана — **V4 «Игра сама вносит внутрь»**
(`plans/52`, В1 = Г). Фаза 4 зависит от моста эпика `plans/23`: большой кнопки входа гостем нет
вовсе (№009 В3), человека вносит внутрь панель после звёзд. Дверь гостя в бою РАБОТАЕТ
(`/profile?guest=1`) — спрятана только тропинка к ней. В3 и В4 интервью №033 остались без ответа,
выведены из выбора V4 и помечены в `plans/52`; не блокируют. Фаза 7 (отзывы) отложена №010 Р9.

### 1. 🔧 Очередь починки `plans/49` — 8 позиций (главное дело этого этапа)

Слово владельца: «нужно будет грумить баги и фиксить. **бесит тех долг**». Очередь отсортирована от
дешёвого к дорогому, у каждой позиции форензика и план. ⚠️ Планы читай как гипотезы (см. п. 3 выше).
🆕 **`bugs/123`** (заведён, не чинен): довод про `100vw` живёт комментарием в `src/app.html` у
загрузочного щита, а `verify-bug40` стережёт там только ЧИСЛО. Главный вопрос замера — жива ли
полоса прокрутки в момент показа щита.

### 2. Хвост фазы 2 эпика 40 «Архитектура веса» (`plans/48`)

Главное число фазы взято: **недостижимых страниц каталога от `/ru` и `/en` — 0** (было 10 222),
карточка в **3 клика**, минимум входящих у карточки **3**. Осталось: шаг 5 (сегментация
`sitemap.xml`; JSON-LD сделан) · шаг 6 (страж `verify-catalog-hubs` + мутации) · шаг 7 (приёмка и
выкат — только с AUTH). Подробности шагов 2–4 и три находки на боевых данных — в летописи.
🟡 **`bugs/121`**: недостижимых от языковых главных осталось 23, каталога среди них 0, но **19 —
публичные** (всё семейство «Тест», английские документы, обе `delete-account`). Все три лечения
правят ЛЕНДИНГ (`plans/21`) — за владельцем.

### 3. ✅ Фаза 5 «ТЕСТ» — ЗАКРЫТА 2026-08-16 (сессия 18), `plans/42_DONE_…`

Страж `verify-test-pages` построен (89 проверок, 3 мутации), `/fable-judge` пройден, владелец
принял фазу (№034 В2 = Б) с условием про перенос длинных названий — исполнено замером
`tools/measure-bug120-wrap.mjs` (390/1440 × обе темы, предел каталога 113 и 160 знаков: обрезки
нет, 4–5 строк, кадры смотрены). **Осталось за владельцем и НЕ блокирует:** живая проба пути
гостя его руками. Разбор фазы — в `plans/42_DONE_…`, летопись — `PROJECT_HISTORY.md`.
📊 Индексация: `/ru/test/love` в индексе Google уже на вторые сутки. Позиции — другое и дольше
(первые НЧ — зима 2026/27); индекс это вход в лотерею, а не выигрыш.

### 4. За владельцем, НЕ блокирует

`plans/39`: боевая приёмка (ru-память на `/en` → остаётся английский) + вычитка EN-обвязки
(условие В2 = А) · текст п. 2.2 «Политики» (правится ИСТОЧНИК `researches/06`, не `docs.ts`) ·
развилка `ideas/31` · `homeworks/08` шаг 3.
**Полный список ждущего владельца** — очередь владельца в `plans/49` (63 позиции) плюс
перенесённый дословно список от 30.07 в `PROJECT_HISTORY.md` (ничего не потеряно).

### 5. Два эпика спланированы и НЕ начаты

`plans/37` (соседство измерений, IBCF — «делаем позже»; фаза 1 — замер плотности, до её чисел кода
не писать) · `plans/38` (оживить теги и вид). ⛔ Предусловие `plans/38` — прополка `plans/30`.
🔴 Замер: теги строками в индекс **не влезают** (1073 КБ при потолке 1024).

---


## 📊 Боевые цифры 2026-08-14 (чтобы не перемерять)

Людей **94** · оценок **4064** · измерений **5111** · связей **2344** · `dims_list` 566 КБ ·
активных за 7 дней **0** · новых за 30 дней **1**. Прибор — `node tools/probe-prod-stats.mjs`
(только читает, анонимную учётку удаляет за собой; AUTH — интервью №013 В2 = Б).

🆕 **Агрегат ВЕРШИН топов** — главное число для калибровки текстов (`plans/28` §9):

| Что видит человек, открыв «Связи» | Значение |
|---|---|
| **Медиана верхней строки** | **7 %** |
| Средняя верхней строки | 11 % |
| Максимум по Пространству | **68 %** |
| Людей с непустым топом | 93 из 94 |
| Распределение вершин | low **95 %** · middle 2 % · high 0 % · upper 3 % |

🔴 **Как НЕ пользоваться:** не путать с `avgSimilarity` **3 %** — это среднее по ВСЕМ строкам всех
топов вместе с хвостом, другой вопрос (`EXP-0124`); и не обобщать **24 %** владельца — он выше
медианы, а не типичен. ⚠️ Наружу не показываем (интервью №014, В3 = А).


## Стражи, которые надо держать зелёными

`npm test` (**284**) · `npm run test:sync` (**77**) · `npm run check` · `npm run typecheck` ·
🆕 **ХАБЫ КАТАЛОГА** (`plans/48` шаг 6): `node tools/verify-catalog-hubs.mjs` (**89** по собранному
сайту: ≥20 карточек, hreflang с самоссылкой, ни процентов, ни ПДн, **пагинация без дыр**, вес
бандла; 3 мутации — 2 · 14 · 14) ·
🆕 **ЯРУС 2 ГРАФИКИ** (`plans/46`): `node tools/verify-tier2-prefetch.mjs` (**7**, по собранному
сайту + preview: нет префетча в сыром HTML И есть 3 ссылки в живом head, `saveData` — ноль;
2 мутации) ·
🆕 **СЕМЕЙСТВО ТЕСТ** (`plans/47`, голова очереди `plans/49`): `node tools/verify-test-pages.mjs`
(**89** по собранному сайту — 8 страниц, содержание в сыром HTML, без noindex, canonical, hreflang
с самоссылкой, ПДн, словарь, sitemap ровно 8; сервера не поднимает; 3 мутации) ·
🆕 **КАРТА ПУТЕЙ** (`plans/54` фаза 0): `node tools/qa-journeys-lint.mjs` (139 путей, 24 маршрута
продукта, Smoke 14/15; сводка пересчитывается по строкам, маршруты снимаются с дерева `src/routes/`;
3 мутации) ·
🆕 **КОМНАТА «ИЗМЕРЕНИЯ»**: `node tools/verify-admin-dims.mjs` (**52 → 60** проверок, нужен стенд +
свежий `build/`; п.10 судит, ЧТО человек видит НЕ НАЖАВ ничего, п.9м…п.9у — возврат на доработку;
доказан 4 мутациями) ·
🆕 **КРУГ ВОЗВРАТА** (`bugs/142`): `npx firebase emulators:exec --only firestore --project
demo-ndim-loop "node tools/verify-candidate-return-loop.mjs"` (**11**, без экрана: выгрузил →
вернули → поправил → вернулось; доказан мутацией на 4 провала) ·
🆕 **ОПИСАНИЯ КАНДИДАТОВ** (`bugs/139`): `node tools/check-candidate-descriptions.mjs <партия>`
(объём 900…1400, 8…14 предложений, дословный ряд < 10 слов против Википедии; `--selftest` на 5
случаях без сети). ⚠️ Гонять ДО выгрузки партии ·
🆕 **БРЕНД-ИМЯ** (канон 2026-08-15): `node tools/verify-sync-server-name.mjs` (**991** файл, 91
объявленное исключение; маркер `ИМЯ-ОК: причина`, маркер без причины сам нарушение; 2 мутации) ·
🆕 **ПРИБОР ОТПЕЧАТКА**: `npx firebase emulators:exec --only firestore --project demo-ndim-fp
"node tools/migrate/verify-fingerprint.mjs"` (**6**, боевых чтений НОЛЬ; стережёт `bugs/130` —
`collectionGroup('dims')` матчит и КОРНЕВОЙ каталог; 2 мутации) ·
🆕 `node tools/migrate/seed-stage-from-prod.mjs` (**сухой прогон по умолчанию**, `--apply` пишет;
три предохранителя: цель обязана быть `ndim-stage` с именованной базой, источник ≠ цель, и отказ
при `DocumentReference` в данных — такие ссылки утащили бы стейдж обратно в бой) ·
🆕 **СТЕЙДЖ** (`plans/53`): `node tools/verify-stage-noindex.mjs` (**11**, контур закрыт от
роботов; несёт КОНТРОЛЬ ПРИБОРА — в бою заголовка быть не должно) ·
🆕 `node tools/verify-stage-clean.mjs` (**14** на живом контуре + `--selftest` на **10** случаях,
П7: в стейдже нет ПДн живых людей — мера это ПЕРЕСЕЧЕНИЕ КЛЮЧЕЙ С БОЕМ; ПДн не печатает; доказан
мутацией) ·
🆕 `node tools/deploy.mjs --selftest` (**8**, стоп-правило контура П8) и `--gate-only` (опросить
замок стейджа, ничего не выкатывая) ·
🆕 `node tools/verify-contour-parity.mjs` (**8** + `--selftest` на **5** случаях, `bugs/133`:
переадресации, `cleanUrls` и кеш обоих конфигов совпадают; законные различия перечислены в нём
поимённо — гоняется дверью ДО выката, в обоих контурах) ·
`node tools/probe-stage-contour.mjs` (стейдж не ходит в бой и наоборот, живым браузером) ·
`node tools/probe-stage-signin.mjs` (путь входа на стейдже) ·
🆕 **МИГРАЦИЯ**: `node tools/migrate/db-fingerprint.mjs --project P --database D` (отпечаток базы
«путь → хеш»; им судится «ни один документ не потерян». ⚠️ ~16 000 чтений за прогон при квоте
50 000/сут) · `node tools/migrate/probe-collection-group.mjs <имя>` (что за неизвестная коллекция:
пути, поля, ФОРМА значений — никогда сами значения) ·
🆕 `npm run questions` — **проверка 4 «ПРОСРОЧЕННЫЕ ЗАМКИ»** (блокада, ссылающаяся на отвеченное
интервью; исключение — `<!-- ЗАМОК-ОК: причина -->`; доказана мутацией) ·
🆕 `probe-test-pages-live` (**240**, БОЙ: 8 страниц ТЕСТ × обе темы × 390/1440, читающий) ·
🆕 `probe-test-guest-live` (**15** в бою, путь гостя до общего результата; ПИШЕТ в бой и убирает
за собой органами продукта — читай его шапку прежде, чем запускать) ·
`npm run e2e` (**100 passed**) · `npm run kaif:check` · `npm run questions` ·
🆕 `verify-prod-signed-in` (**24**, БОЙ под сессией — гнать после КАЖДОГО выката) ·
🆕 `verify-prod-cache` (**12**, заголовки кеширования боя, `bugs/124`) ·
🆕 `verify-bug117` (**28**, возврат из «Как меня видят» не дёргает «Профиль»; стережёт ещё и то,
что анимация ЖИВА и что слой ПЛОТНЫЙ, — иначе «починка» удалением анимации была бы зелёной;
2 мутации) ·
🆕 `verify-batch4` (обе темы × 390/1440; `bugs/65` стережётся ЦВЕТОМ холста, не габаритом слоя;
2 мутации) · 🆕 `verify-ideas24` (**60**, виджетная сетка; внесён в реестр только 2026-08-14) ·
`verify-bug110` (**22**, контур вычитки; `bugs/122` — приговор «не записано» запрещён) ·
`verify-owner-reviews` (**86**) · `verify-dimension-pages` (**30**) ·
`verify-prod-dimension-pages` (**44**, бой) · `verify-prod-lang-redirects` (**16**, бой) ·
`verify-prod-b4` (**81**, бой) · `verify-prod-dim-ratings` (**7**, бой) ·
`verify-product-vocabulary` (**12**, было 7; 🆕 216 некаталожных h1 под судом — вырез h1 был
безусловным и прятал заголовки лендинга, «Меню» и семейства «Тест»; ⛔ в `BANNED` НЕ добавлять
«совместимость» и «платформа» — это слово владельца и утверждённое имя страницы, страж покраснел
бы на его тексте) · `verify-lang-module` (**17**) · `verify-admin-home` (**17**) ·
`verify-bug104` (**22**) · `verify-bug105` (**56**) · `verify-plan08` (**60**) ·
`verify-bug81` (**36**) · `verify-bug40` (**71**) · `verify-bug69` · `verify-pwa` (**24**) ·
`verify-account` (**84**) ·
🆕 `verify-account-delete` (**31**, 🔴 УНИЧТОЖАЕТ dev-пользователя — последним и в одиночку;
несёт ИНВАРИАНТ «без входа при живых данных не остаются», 2 мутации) · `verify-ptr` (65…66, число НЕ детерминировано — судить по «0 провалов») ·
`verify-manual-v1` · `probe-bug67` · `verify-bug80` · `verify-bug84` · `verify-bug87` ·
`verify-back-twins` · `verify-bug75` · `verify-bug76` · `verify-bug77` · `verify-bugs08` ·
`verify-tierA` · `verify-bug42`.
Полное описание каждого — таблица «Испытательный стенд» в `AGENT_GUIDE.md`.

## Перед каждым push — `npm run kaif:check`

Падает, если в git попал секрет или ПДн. Утечка уже случалась (`bugs/03`).
**Репозитории:** публичный `MikalaiKryvusha/ndim` (чистая история) · приватный архив 1.x
`MikalaiKryvusha/ndim-old` (0 форков) · фреймворк `MikalaiKryvusha/KAIF` (сюда идут issues о
дефектах KAIF — заводит агент сам).

## Окружение (проверено 2026-07-10, досье — `AGENT_GUIDE.md`)

Node 24.15.0 · git 2.43 · `gh` авторизован · firebase-tools 15.23.0 · gcloud (`ndim-space`) ·
Java OpenJDK 21 (в PATH — в НОВОМ сеансе) · эмулятор Firestore на **8181** (8080 занят
`llama-swap`) · Docker · Playwright 1.61.1.
🔴 **Деплой требует явного проекта:** `.firebaserc` без default —
`firebase deploy --only hosting --project ndim-space` (таргеты `landing` + `app`, оба из `build`).

---

**Летопись** — `PROJECT_HISTORY.md`: закрытые сессии, фазы и выкаты, перенесённые отсюда ДОСЛОВНО,
свежее сверху. В обязательный набор чтения НЕ входит — открывай для археологии.




## 📜 Смены 5 и 6 (2026-08-22/23, сессия 37) — перенесено из STATUS дословно 2026-08-24

### 🟢 СМЕНА 6 (2026-08-23, сессия 37, утро) — ЗАКРЫТА В 11:00 ПО СЛОВУ ВЛАДЕЛЬЦА

#### 🔴 ДВА ПРИКАЗА ВЛАДЕЛЬЦА, ОБА В РАБОТЕ

1. **«Смотрю, мне измерений на вычитку так и не отправили».** Он был прав, и причина не в
   сроке: карточки были починены к полуночи, а шаг «вернуть исправленное В ОЧЕРЕДЬ» в план
   ночи не поставил Менеджер. Между починкой и его глазами стоят две двери — вердикт QA с
   мержем и боевая запись, и обе мои. **Закрыто: 16 карточек лежат у него на вычитке.**
   🔑 Правило, оплаченное этим: **партия — набор НОВЫХ объектов; исправленные возвращаются
   ОТДЕЛЬНЫМ адресным файлом** (`candidates/batches/07_returned_fixed.json`). Дверь выгрузки
   отказывается класть партию, где есть уже одобренное, — и отказывается по делу.
2. 🔴 **«Что за дурное слово „комната“? У нас это МЕНЕДЖЕР ИЗМЕРЕНИЙ!!! Изменить везде!
   Линтер страж на термин Менеджер Измерений!»** — `tools/verify-dims-manager-name.mjs`
   написан и в `main`. **Было 183 нарушения, стало 153; все 30 мест `src/` — то, что владелец
   видит своими глазами, — переименованы и выкачены.**
   ⚠️ **Слепая замена по падежам ЗАПРЕЩЕНА, это уже оплачено:** «комната» женского рода,
   «Менеджер измерений» — мужского, и Менеджер сломал согласование в 51 файле, откатил.
   Правится адресными подстановками, падающими при непопадании. Второй страж —
   `verify-dims-manager-agreement.mjs` — ловит поломки согласования (11 живых по дереву).
   ⛔ **Оба стража в `guards.mjs` НЕ зарегистрированы намеренно** — регистрация вместе с
   последней правкой: красный страж на входе приучает себя игнорировать (`EXP-0194`).

#### ✅ ЧТО ЗАКРЫТО

| Что | Доказано |
|---|---|
| **Словарь-долг `verify-product-vocabulary`** — висел ЧЕТЫРЕ смены | сборка 10 471 страница, проверено 302, провалов 0; зелёное принято тремя мутациями по ЖИВОЙ сборке |
| **Блокер Ш4** — `genre-depth` подключён к двери правок | строгость назначена ЗАМЕРОМ по 364 живым правкам: пара признаков даёт 61 отказ (16,8 %), таблица снята ДО правила |
| **Все шестнадцать классов владельца** имеют либо проверку, либо названную причину её отсутствия | отказ — 1а, 1б, 4, 5, 6, 7, 11а, 12, 13, 14 · замечание — 3, 9, 11б, 15, 16 · замер без порога — 2 · 8 и 10 ждут прибора dev-2 |
| **ВЫКАТ В БОЙ ПРОВЕРЕН** (коммит `fab3544`) | стейдж 24/0 + 81/81 · бой: дверь упала **23/1** на `bugs/179` (рамка reCAPTCHA, report-only CSP — чужой домен), повторный смоук сразу дал **24/0**, гостевой **81/81**. Дефект известен и не наш код |
| **Мержи:** Дизайнер (`4fb41f6`, вердикт QA) · QA (`bf4d6f6`) · dev-1 (`a6cbe29`) | ⚠️ dev-1 смержен **БЕЗ ВЕРДИКТА QA** — пометка стоит в сообщении коммита `c9411c2` |

#### 🔴 ЖАЛОБА ВЛАДЕЛЬЦА ПРО ЛЕНДИНГ — СТАТУС «НЕИЗВЕСТЕН», И ЭТО ЧЕСТНЫЙ ОТВЕТ

Вчерашний вывод «щит жалобу не закрыл» **снят**: боевой зонд ставит себе ПОДДЕЛЬНЫЙ маркер
сессии, щит опускается законно, лендинг показывается законно — число «лендинг 33 из 40» не
измеряет жалобу вовсе. Это написано в шапке самого зонда, и Менеджер её процитировал днём
раньше, а потом построил на этом числе вывод.
🔑 Что число значит: щит держался **0 → 7 кадров**, то есть починка `bugs/40` п. 2 РАБОТАЕТ.
🔴 **Решающая проверка невозможна из-за оснастки:** вход dev-пользователя на стенде не
поднимается (`waitForFunction` по маркеру `ndim-session`, таймаут 30 с) — **воспроизведено
дважды, разными сессиями**. Порядок работ: сначала вход на стенде → потом прогон → и только
потом правки щита. До появления числа чинить будет нечего или не то. Подробности — `bugs/194`.

#### 🟡 НЕСМЕРЖЕННОЕ — суд следующей смены, в этом порядке

| Роль | Голова | Что там |
|---|---|---|
| **dev-3** | `834d2e6` | все 16 классов владельца в воротах. 🔴 Судить ГОЛОВУ, не `a75b66c`: поверх легли класс 16 в новой формулировке и класс 15 |
| **dev-2** | `a1407cb` | правила русского языка Н1–Н9 + прибор согласования + `--slow` в зонде + разбор `bugs/194` |
| **Дизайнер** | `8a983a5` | порция 2 починки, класс 15 на «Отец, мать, сестра, брат», три записи опыта |
| **dev-1** | смержен без вердикта | суд ТРЕТЬИМ, после dev-3 и dev-2 |

#### ⏭ ПЕРВЫМ ДЕЛОМ СЛЕДУЮЩЕЙ СМЕНЫ

1. 🔴 **Вход dev-пользователя на стенде** — без него жалоба владельца не меряется в принципе.
2. **Три суда** в порядке таблицы выше.
3. **Остаток имени: 153 места** (`plans` 52 · `tools` 30 · `bugs` 25 · `interviews` 18 · далее
   мелочь) **+ 11 поломок согласования**. Регистрация обоих стражей — последним коммитом.
4. **Остаток починки: 11 возвращённых, все класс 16** (приём берётся только из сети, один
   поиск на карточку) + 5 одобренных, «Кобра» последней.
   🔴 **Названный пробел:** классы 2 и 15 у 15 возвращённых и 5 одобренных **не проверялись
   поимённо ни разу** — прибор их не видит, списка никто не вёл.
5. `bugs/195` — документированная команда `check-owner-classes.mjs` без `--today` съедает файл.
6. Партия 06: разведка готова, 65 не-музыкальных объектов с доказанной прошлой датой.

#### 🔑 ЧТО СМЕНА ДОБАВИЛА К КАНОНУ

· 🔴 **Там, где проверка невозможна машиной, её роль выполняет требование назвать сделанное
  ПОИМЁННО.** Пробел по классу 15 не нашли ни прибор, ни ворота, ни вычитка — его нашёл
  вопрос, на который нельзя ответить памятью. Оборотная сторона того же: **зелёный прогон
  бывает ЗАКОННЫМ и при этом означает «не проверяли»**.
· **Прибор, которому пришлось ПОДДЕЛАТЬ предусловие, меряет поведение на подделке.** Его число
  законно — незаконен перенос этого числа в критерий о живом человеке. Триггер действием:
  пишешь критерий «прибор X даёт N» → открой шапку X и ответь, что он подделывает.
· **«Мера не различает» и «признака нет» — разные утверждения.** Счётные меры слепы к порядку;
  позиционный признак различает там, где все счётные дают одно число.
· **Признак, ищущий отдельное слово, меряет словарь, а не смысл** (два ложных зелёных класса 16:
  «критиками» и «награду» нашлись внутри СЮЖЕТА).
· **Отправка владельцу меряется по ТОМУ ФАЙЛУ, который уехал, а не по партии, из которой он
  собран.** Судья обязан спросить «что именно уехало» ПЕРВЫМ вопросом.
· **Несудимый мерж не равен несудимой ветке:** в `main` несудимый код неотличим от
  проверенного. Пометка идёт в СООБЩЕНИЕ КОММИТА — `STATUS` стригут, `git log --grep` переживает.

### 🟢 СМЕНА 5 (2026-08-22/23, сессия 37) — ЗАКРЫТА В 01:00 ПО СЛОВУ ВЛАДЕЛЬЦА

**Запуск:** три ветки сброшены от свежего `main`, двум с несмерженной работой дельта канона
названа поимённо. Пять ролей проинструктированы, работали до 00:50, закрылись в 01:00.

#### 🔴 ГЛАВНОЕ СОБЫТИЕ СМЕНЫ — ВЛАДЕЛЕЦ ВЫЧИТАЛ КАТАЛОГ ПОДРЯД

Его заказ: «*напишите мне ещё 60 измерений на вычитку с учётом моих критических комментариев
только!!!*» + «*это задача помимо прочих по планам*». Пока шла смена, возвращённых карточек
стало **10 → 12 → 29**, и вечером он сказал: «*я много вам вернул. изучайте, чините*».

📍 **Мерило — `candidates/owner-critical-notes-2026-08-22.md`** (снято с БОЯ, переснимается
Менеджером): **шестнадцать классов** с его дословным словом, адресный список 29 возвращённых и
5 одобренных с теми же дефектами (по ним его слово — «*чините*»; статус `approved` не трогаем).

🔑 **Класс 11 — главный, он повторил его ПЯТЬ раз и объяснил принцип:** «*Мы не унижаем картину,
не унижаем объект культуры, а он и является главным в нашем описании*». Плюс его поправка:
«*сама картина — мы не только картины описываем. секс — это практика*» — объект зовётся словом
СВОЕГО ВИДА (семь видов, `src/lib/content/dim-kind.ts`), и род согласуется от этого слова.

⚠️ **Одна догадка Менеджера снята замером:** две карточки, возвращённые за «ещё не вышел», в
реестре ВЫШЛИ (февраль и март). Дефект не в отборе, а в том, что **описание стареет**: текст
пишет «выход назначен на…» спустя полгода. Таких в бою десять, шесть владелец ещё не видел.

#### ✅ ЧЕМ ЗАКРЫЛАСЬ СМЕНА

| Что | Доказано |
|---|---|
| **Мерж dev-1** (`0439a9a`) — партия 1 `plans/70` · провенанс приёмки · мутатор · страж статуса | вердикты QA №1 и №2; ворота 4/4, `npm test` 386/386 |
| **Страж статуса поставлен в `tools/guards.mjs`** и **сразу нашёл две живые лжи в `main`** | `plans/44` и `plans/56` объявляли себя неначатыми при закрытых шагах — обе шапки поправлены |
| **ВЫКАТ В БОЙ ЗАВЕРШЁН И ПРОВЕРЕН** (коммит `9c2a031`) | стейдж 24/0 + 81/81, бой **24/0 под сессией** + **81/81 гостем**, консоль чиста |
| 🔴 **Щит владельца выкачен — и жалобу НЕ закрыл** | замер боя одним прибором: ДО **щит 0 · лендинг 40/40** → ПОСЛЕ **щит 7 · лендинг 33/40**. Заведён `bugs/194` с наблюдаемым критерием «ЛЕНДИНГ = 0» |
| **Все пять веток ролей запушены в origin** | ловушка найдена QA на себе: работа была только локально и погибла бы при штатном сбросе |

#### 🟡 НЕСМЕРЖЕННОЕ — ждёт судов свежего QA

| Роль | Голова | Что там |
|---|---|---|
| **dev-3** | `a75b66c` | классы владельца 1, 3–7, 9, 11–14, 16 в воротах карточек · 187/187 юнитов |
| **dev-2** | `dfe6140` | правила русского языка Н1–Н9 в стилометрии + прибор согласования + два разведдока |
| **Дизайнер** | `4fb41f6` | 12 возвращённых починены · прибор классов владельца (40 случаев) · фильтр отбора по дате |
| **QA** | `5716890` | три вердикта + **пять документов дефектов** |

#### ⏭ ПЕРВЫМ ДЕЛОМ СЛЕДУЮЩЕЙ СМЕНЫ

1. 🔴 **Словарь-долг `verify-product-vocabulary`** — третья смена подряд, ставить ДО судов.
2. 🔴 **Суд dev-3 по классам владельца** — это мерило партии 6 и работ Дизайнера.
3. 🔴 **Блокер Ш4:** `genre-depth` к двери правок НЕ подключён (`EXP-0194` третий раз за смену).
   Замер по 364 живым правкам снят и лежит готовым — начинать можно сразу.
4. 🔴 **Два дефекта под стражами dev-3**, иначе его мерж красит ворота у всех:
   `team-status.mjs:395` (`\s` в шаблонной строке стал буквой `s`, самотест не мог упасть —
   десятый ложный зелёный смены) и раскол тега `bugs/178`.
5. **Починка остатка:** 23 карточки, 67 находок. 🔴 Цена названа Дизайнером заранее: класс 16
   требует ПРИЁМА, а приём берётся только из сети — по одному поиску на карточку.
6. 🔴 **`bugs/194` — щит поднимается поздно:** лечение, скорее всего, не в логике щита, а в том,
   что отдаётся первым байтом на `/ru` вошедшему. Замер в документе, прибор в дереве.
7. ⚠️ **Смоук под сессией дал лотерею:** на стейдже один прогон упал на `1440-dark` («вошли» не
   состоялось, профиль снят гостем), повтор того же коммита прошёл 24/0. Устойчивость входа в
   смоуке посмотреть отдельно — родня `bugs/174`.
8. Партия 06: разведка готова — `test-results/scout-2026-batch6.json`, 112 объектов, 65 не
   музыкальных, все с доказанной прошлой датой.

#### 🔑 ЧТО СМЕНА ДОБАВИЛА К КАНОНУ (врезки пишет Менеджер при мерже)

· **Правило голоса, сработавшее на текстах владельца, отменяет себя, а не текст.** Пришли к нему
  независимо dev-2 (шесть ложных на «*снятый режиссёром*») и dev-3 (запрет «не X, а Y» краснел бы
  на строке, которую владелец написал СВОЕЙ рукой как лечение).
· **Рост покрытия сам является испытанием признака:** «ноль ложных» при низком покрытии — в первую
  очередь утверждение о покрытии (dev-2: учетверение покрытия дало 7 ложных из 8).
· **`.replace` — тихая функция:** не найдя образца, возвращает исходник и не жалуется. Контроль
  попадания обязан стоять ДО записи и падать. Механизм — `tools/lib/mutate.mjs`.
· **Одинаковый ответ разных мутантов = вопрос не задан.**
· **Приёмка, которую не может повторить никто, кроме автора, — дефект** (`EXP-0195` родня).
  Наблюдаемое требование написано QA: прибор в дереве, «не проверено» = ненулевой код, кэш не
  коммитится, а добирается напечатанной командой.

## Сессии 33–35 (2026-08-22) — вторая и третья смены команды; перенесено стрижкой бонсая сессии 36

*(Дословно из `STATUS.md`. Что из них ПЕРЕЖИЛО перенос и осталось в STATUS указателями: 403 от
App Check запирает ворота выката · открытые дефекты 146 · 158 · 162 · 134 · 127 · 113 ·
очередь работ владельца №041 В3 = А · эпик 60 · развилка «где живёт состояние эскорта».)*

> 🔴 **Стрижка нашла ЧЕТЫРЕ строки, пережившие своё исполнение, и они исправлены правдой, а не
> перенесены молча** (класс назван в `AGENT_GUIDE.md`: «пункт „ждём X“ обязан нести дешёвую
> проверку, случилось ли X»):
> 1. «ЖДЁТ ВЫКАТА — пять правок вида, все `[NOT-TESTED]`» — правки приняты живой приёмкой QA
>    (`b3b90c2`, переведены в `[TESTED]`) и уехали в бой двумя выкатами; секция описывала
>    состояние, кончившееся сутки назад.
> 2. «Правила Firestore изменены и в бой НЕ выкачены» — дверь выката катает правила ПЕРВЫМ шагом
>    (`tools/deploy.mjs:519`), выкатов после правки было два.
> 3. «`plans/58` шаг 1 — не начат, назначен на четверг» — в `main` лежат два коммита шага 1
>    (`ca88607`, `0e972bb`), оба смержены.
> 4. Шапка самого `plans/58`: «Статус: 🔲 написан, не начат» — при тех же двух коммитах.
>
> Четыре случая одного класса за один проход — это свойство нашего устройства статуса, а не
> невезение. Механизация заказана отдельной строкой в очереди Менеджера.

### 🅿🅿 ПАРКОВКА НОЧИ 2026-08-22 ~03:00 — вторая смена команды закрыта, БОЙ ВЫКАЧЕН

> **Слово владельца 01:51 дословно:** «*и я ложусь спать, работайте без меня автономно. примерно
> до 3 часов ночи можешь с ними поработать, а затем до логической паузы доходите, межитесь,
> собирайте стейдж, тестируйте, если всё ок - можете на прод выкатить, и конец всех чатов и
> парковки оформляйте. без меня работайте по автономному пулу*»

**✅ ВЫКАТ СОСТОЯЛСЯ И ПРОВЕРЕН** (`b01f297` + `c6d8ece`): стейдж зелёный, бой зелёный —
**смоук под сессией 24/24** (обе темы × 390/1440: вход, пять экранов, «Как меня видят» и
возврат, консоль чиста) · **гостевой смоук 81/81** · ворота Smoke по собранному артефакту
**19/19** · целостность сборки, попадание в свой контур, заголовки кеша и переадресации.

🔴 **ДВЕРЬ ОСТАНОВИЛА ПЕРВЫЙ ЗАХОД, И ЭТО ГЛАВНОЕ СОБЫТИЕ НОЧИ.** Первый боевой выкат прошёл до
смоука под сессией и покраснел ровно на «консоль чиста»: **App Check отдавал 403** на обмене
токена reCAPTCHA v3 (адрес назван зондом — прибор печатает голое «403»). Разбор и лечение —
**`bugs/169`**. Врезка выключена штатным предохранителем (`APP_CHECK_SITE_KEY = ''`), бой
возвращён во вчерашнее состояние, второй заход прошёл чисто.
🔄 **ИСХОД ВОЗВРАТА КЛЮЧА (2026-08-22 день): ключ ЖИВЁТ В БОЮ, дверь показала 403 С АДРЕСОМ.**
Выкат прошёл (172+173+ключ в бою, стейдж 24/0 и 81/81), смоук остановил дверь ПОСЛЕ выката:
`403 · POST …exchangeRecaptchaV3Token ×4` на каждой странице у сессии И у гостя. **Диагноз
переосмыслен:** Monitoring КОПИТ метрики (скриншоты владельца: 1–2 % verified), принуждения
нет — живые люди не теряют ничего; отказ, вероятнее всего, ловит НАШИ headless-приборы
(роботный счёт reCAPTCHA v3 — App Check делает свою работу). **Настоящий пострадавший — ворота
двери: любой следующий выкат упрётся в «консоль чиста».** Штатное лечение — debug-токен App
Check для приборов двери (документированный путь Firebase для CI). 🔵 У владельца два действия:
сверить тип ключа (классический v3, не Enterprise) и домен в админке reCAPTCHA · создать
debug-токен (App Check → Apps → Manage debug tokens) и передать значение в `.env` — ЛИБО
разрешить агенту REST-создание (классификатор остановил, ждёт его воли). Предохранитель
сознательно НЕ дёрнут: метрики копятся, шум слышат только роботы.

**Смержено за смену (все с вердиктом QA, ни одного на слово исполнителя):**
· dev-1 — конвейер сверки перестал прятать работу: самолечащийся кэш (12 файлов из нулевых
  байтов прятали **2 настоящих заимствования**), контроль дороги у каждого языка, снятый ложный
  зелёный `gate-rewrites --selftest` · `bugs/165` · `EXP-0188`
· dev-3 — страж класса «граница слова рядом с кириллицей»: **8 починенных образцов**, из них один
  сторожил боевой инвариант «цифр похожести наружу не даём» и молчал на всех живых формах ·
  `bugs/166` · `researches/46` · `npm run test:tools`
· dev-2 — **три строки замка по слотам** (слово владельца), пять долгов замера парка,
  разведка фазы 2 · `bugs/167` · `EXP-0189` `EXP-0190`
· QA — `bugs/163` закрыт (молчаливый пропуск объявляется вслух), четыре вердикта в журнал

**Замер парка снят:** три стенда живут одновременно, минимум свободной памяти за заход
**5 198 МБ** при пороге остановки 500, подкачка не шевельнулась. Заход занял 6 минут вместо 40.

### ⏭ ЧТО ДЕЛАТЬ СЛЕДУЮЩЕЙ СМЕНЕ — по ролям

**Менеджер:** ✅ ~~очередь владельца `bugs/169`~~ — консоль проверена им 2026-08-22 утром,
теперь возврат ключа выкатом (см. выше) · 20 карточек партии 4 на его
стол после суда · две карточки с `agentNote` («Седло» — годится ли объект; «Каратель» — вид) ·
311 записанных правок описаний ему так и НЕ показывались (условие №025 В6 = А, вычитка
постфактум) · оформить идеей `mutate()` (dev-2 берёт).
**QA:** `bugs/162` (слепой прибор — у проекта нет живого свидетеля класса заморозки) ·
`verify-product-vocabulary` по собранному сайту · **20 карточек Дизайнера — СВЕЖИМ окном**, по
голове ветки `ndim_designer`, а не по `main`; судит ВОРОТА и ПРОВЕНАНС, не вкус · завести
документ про доску в одном дереве с воротами выката (его находка) · научить смоук печатать
АДРЕС ответа ≥ 400, а не голый код (`bugs/169`, второй дефект).
**dev-1:** 🔴 ПЕРВЫМ — **`bugs/172`** (слово владельца: критикал-пас; приёмка покадровая, видео
~10 к/с, родня `bugs/158` — дефекты разные) · импорт `punctuationArtifacts` в `checkEdit` ·
уборка брошенных `tmp-<pid>` · партия 1 `plans/70` (он эталон для двоих; ждёт П5 от dev-3).
**dev-2:** `plans/69` шаги 2–6 (критерий 2 эпика) · случай с ЖИВЫМ держателем в самотест доски ·
П6 попутным замером.
**dev-3:** 🔴 порог доли меты **НЕ калибруется «Практикой» — поправка Дизайнера, он её прогнал:**
там доля меты **0 % на всех процентилях**, и это свойство корпуса, а не пробел замера — у практик
(«Футбол», «Плавание», «Игра на гитаре») производственной обвязки нет вовсе, их никто не
производил. Ноль читался бы как «норма нулевая» и заворачивал бы любую карточку фильма. Точная
формулировка: **порог калибруется только на корпусе объектов, У КОТОРЫХ ЕСТЬ АВТОР И ВЫПУСК**;
из названного владельцем эталона это ряд «Матрицы» — семь записей, выборка мала и это сказано
вслух. Расширять её непригодным материалом нельзя · прибор доли меты (решение: предъявляет
предложения человеку, сам не краснеет) ·
английская половина кода ворот (пары Дизайнера готовы, `58db9b1`).
**Дизайнер:** партия 4 ждёт суда · контракт §6 · его ветка **не смержена**, 4 коммита.

🔴 **`plans/70` (512 английских описаний на троих) ЗАПЕРТ пятью предусловиями.** Фан-аут владелец
разрешил («*Три разработчика — запрет был про другое*»), но однородность держат ВОРОТА — и это
ему сказано прямо. П1–П4 закрыты, П5 наполовину (текст есть, кода нет).


## ▶️▶️ С ЧЕГО НАЧАТЬ ЭТОТ ЧАТ — эстафета сессии 32

🔵🔵 **ОЧЕРЕДЬ ВЛАДЕЛЬЦА (№041 В3 = A):** ✅ **МОСТ (фаза 5 эпика 23) ЗАКРЫТ И СМЕРЖЕН
2026-08-22** (`plans/67`, вердикт QA: принято; e2e 100/0) → дальше по очереди: остаток SEO-фазы
каталога (`plans/56` шаги 3, 5, 6) → эпик 60 (шаги 3–7 фазы 2 ждут за воротами).

✅ **Мост построен командой за одну ночь** (2026-08-22, `plans/67`): строка-условие снята,
вычисляемый поп-ап и итог-панель из настоящего расчёта, кнопка «Смотреть больше» с волнами,
переход `location.replace` («Назад» без отскока — доказано двумя способами), карточка-мостик на
«Связях» с вычисляемой ветку пустого списка. 🟡 Две новые строки лица — «текст на вычитку»
(интервью №045). ✅ **Вычитка закрыта 2026-08-22: В1 = А, В2 = А — бой больше не придержан.**
✅ **Хвост фазы 3 (`plans/64` шаг 5) ЗАКРЫТ QA** 2026-08-22: пара «база ↔ экранные ЧИСЛА» +
мутация заморозки; форма «первый визит раньше цикла» признана недостижимой на стенде
арифметикой такта (протокол — `qa/epic23-phase3-gate.md`); прежний зонд был слеп к переезду
значений — `bugs/162`.

**Сделано сессией 32** (эта + две соседние сессии, коммиты `9166135…7f580b2`):
- **Фаза 3 эпика 23 ЗАКРЫТА кодом** (`plans/64`): окно ожидания пересчёта — после своей оценки
  топ «Связей» живёт с нулевой свежестью, пока сервер синхронизации не подтвердит
  (`points/{uid}.dirty === false`); вне окна контракт №005 В4 не тронут. Экран при пустом топе
  в окне показывает принятый владельцем текст ожидания (№043 В4 — канон) с автоперечиткой.
  Юниты 322/322 (+3), `verify-ideas18` зелёный живым стендом.
- **Фаза 4 эпика 23 ЗАКРЫТА одним сидением** (`plans/65`, интервью №043): В1 = синтез (выше) ·
  В5 = А (**отдельной панельки под шапкой НЕТ** — просьбу несёт карточка гостя: пульс рамки +
  кнопка Google; фаза 7 эпика сведена к этой правке) · В6 = А (модальный диалог ухода строим,
  Р14 в силе) · В4 = правки текстов В КОДЕ: карточка гостя кончается «останутся в Вашем личном
  NDim ID профиле Пространства NDim», «Сохранить труд» → «Сохранить результаты».
- **ВСЕ 20 кандидатов партий 2–3 вернулись от владельца с классовыми ошибками и починены**
  (соседние сессии, `dadd669`, `bcb71dc`): три класс-запрета — «внешний авторитет/реестр»
  («*NDim самодостаточен… Мы констатируем факт сами*»), «название в начале» («*Название уже
  есть в шапке… Это класс правило*»), следы машинной правки текста — все в README мастерской
  и В ВОРОТАХ (`check-candidate-descriptions`: 6 проверок, самотест 19/19). Партии снова в
  очереди `pending` — **владелец может вычитывать в менеджере измерений**. `bugs/159` закрыт
  (`bcb71dc`: имя из цепочки labels→mul→sitelinks, безымянные не числятся новыми).
- 🔴 **КАНОН: ФОРМА ВОПРОСА ВЛАДЕЛЬЦУ — КЛАСС-ПРАВИЛО** (его слово: «*понятно, просто, по
  делу, без воды, с примером… Запомни такую форму постановки вопросов как класс*») — врезка
  в `AGENT_GUIDE.md` с парой «до/после» №043. Плюс контур вычитки усилен: `review open`
  ОТКАЗЫВАЕТ при нуле ждущих вопросов (класс «вопрос-невидимка»: заголовок не «В<номер>.» —
  парсер его не видит, владелец зван к пустому столу; доказано на №042).
- 🎓 **`EXP-0178`** (усечённый вывод принят за полный — две сессии согласились об одном
  усечении; футер-свидетель полноты в `read-returned-candidates.mjs`) · **`EXP-0177`**
  (зелёные ворота на порванном тексте — правка текста механизмом обязана иметь проверку
  СЛЕДОВ правки) · **`EXP-0176`** (исполнителю ставится весь конвейер до продукта).

**Страж вопросов при закрытии сессии 32 — ✅ ЧИСТО:** новых вопросов вне `interviews/` **0** ·
интервью **43, ждут владельца 0** · просроченных замков **0** · новых неразнесённых **0**
(№043 разнесён целиком; №036 В4 доразнесён в `bugs/135`) · унаследованный долг: вопросов вне
interviews/ 25, неразнесённых ответов 24 (за сессию не вырос).
⚠️ **Хвост окружения:** удалить `D:\work\ai_sandbox\_locked_rolldown_binding.trash` и пустой
`judge-wt` в Temp после перезапуска VS Code; пустой каталог `.claude/worktrees/candidates-batch2`
можно убрать (ветка влита `c6bd7bf`, worktree снят его сессией, `node_modules` цел).

✅ **`plans/22` фаза 3 (App Check) — доворотная часть ЗАКРЫТА 2026-08-21.** Клиент подключён за двумя
предохранителями (`src/lib/firebase.ts`: только боевой контур · пустой `APP_CHECK_SITE_KEY` =
выключено); SDK уже был в бандле, ставить нечего. 🔴 **Разведка опровергла прежнюю строку
«ключ нужен только на принуждении»:** без ключа reCAPTCHA v3 в клиенте консоль не увидит
«запросы С валидными токенами» — половина ворот наблюдения недостижима.
✅ **Ключ создан владельцем и вписан ТЕМ ЖЕ ДНЁМ** (`homeworks/13_DONE`, коммит `42df57b`);
секрет — в `.env`, провайдер зарегистрирован по REST (`siteSecretSet`, TTL 1 день).
🔴 **СТРОКА ПЕРЕЖИЛА СВОЁ ИСПОЛНЕНИЕ И ИСПРАВЛЕНА 2026-08-22 (нашёл QA).** Прежняя редакция
гласила: «наблюдение начнёт копить метрики с ближайшего боевого выката — его держит стоячий
долг „пять правок вида `[NOT-TESTED]`“». **Выкат СОСТОЯЛСЯ этой ночью, и обе половины строки
стали ложью:** условие исполнилось, метрики НЕ копятся, а держит их не тот долг, а **403 от
App Check** — обмен токена reCAPTCHA v3 отказан сервером, врезка выключена предохранителем
(`APP_CHECK_SITE_KEY = ''`). Разбор — **`bugs/169`**; ✅ рука владельца приложена 2026-08-22
утром (провайдер Registered) — возврат ключа в работе этой смены.
⚠️ Ключ он создавал ТЕМ ЖЕ ДНЁМ по домашке — его работа сейчас лежит неиспользованной, и
сказать ему это надо прямо. Класс дефекта канонный: «пункт „ждём X“ обязан нести дешёвую
проверку, случилось ли X», иначе он переживает собственное исполнение. Принуждение — отдельный
шаг после стабилизации метрик (решение №009 за агентом), его хвост — debug-токены боевым
смоукам двери (иначе они умрут первыми) и строка атрибуции reCAPTCHA на лице (показать
владельцу варианты).
🟢 **Фазы 1–5 эпика 22 (гость) закрыты** (разборы — летопись, сессии 28–31): постоянная
карточка гостя на пяти экранах (`GuestCard.svelte`; текст хвоста — правка №043 В4), финальный
экран истёкшей сессии, полная уборка гостей с судьёй VERIFIED. **B и D** фазы 5 — в составе
фазы 7. Фаза 4 в бой НЕ выкачена, уедет штатным выкатом эпика. Уборка гостей — не раньше
октября 2026 (№042 В3 = A). Капканы приборов гостя: у `sync-server/` СВОЙ `node_modules`
(`createRequire` от него) · тема — атрибут `data-theme`, `emulateMedia` её не переключает ·
на стенде дверь `?as=guest` в каждый переход (`EXP-0174`).

🐞 **`bugs/158`** (слово владельца: карточка не уезжает после отсчёта) — заведён и локализован
его ответом (№042 В6=A: **бой, вкладка «Все»**). Стенд жест держит (два прогона зонда); файл
карточек менялся после боевого выката трижды — бой держит другую версию. Дальше по плану бага:
осторожная проба боя (оценка + немедленная отмена органами продукта) ИЛИ повторная проба после
ближайшего выката.
🗓 **НЕ ПОТЕРЯТЬ:** `plans/58` шаг 1 (разметка 5121 тегами) был назначен «на четверг» =
2026-08-21 и перебит аудитом и воротами; вернуть в очередь после ворот — или спросить место.

**Прежняя строка (эпик 60, ждёт своей очереди): `plans/62` шаг 1, второй пункт — ГДЕ ЖИВЁТ
СОСТОЯНИЕ ЭСКОРТА.** Развилка описана в плане:
серверная коллекция против полей на точке; критерий — «клиент не дотянется» и цена в операциях.
Первый пункт шага (`bugs/153`) закрыт, дорога свободна.

⚠️ **Правила Firestore изменены и в бой НЕ выкачены.** Белый список полей `points/{uid}` живёт
пока только в репозитории и на эмуляторе. Выкат — через `npm run deploy`, вместе с остальным
эпиком (фаза 5), либо раньше отдельным решением владельца.

✅ **ФАЗА 1 ЗАКРЫТА 2026-08-18 — ворота эпика открыты.** Прибор
`sync-server/measure-idle-cost.mjs` замерил холостой такт: **1 чтение + 1 запись**, одинаково в
трёх тактах подряд. Расчёт `bugs/152` подтверждён числом, а не чтением кода. Прогрев замерен
отдельно (15 чтений + 5 записей на пустом Пространстве) и в цену простоя не идёт. Стрелка
доказана мутацией: лишний `get()` дал ровно +1 и был НАЗВАН строкой, после отката счётчик
вернулся. Контрольный случай (заведомо известные 2 чтения и 4 записи) сходится при каждом пуске.

## 🟡 ЖДЁТ ВЫКАТА — пять правок вида, все `[NOT-TESTED]`

Сделаны в коде, кадров живым Chrome НЕТ, в бою НЕТ. Дерево зелёное: юниты **316/0**, типы 0/0,
страж вычитки 137/0, `kaif:check` чист.

| | Что |
|---|---|
| `bugs/149` | тени карточек связей сняты (близнец `148`; обход дерева нашёл ровно два таких места) |
| `bugs/150` | **«мёртвая душа»: у человека имя ЕСТЬ, на английском — продукт его прятал.** Пустая строка запирала спуск по локалям (`??` ловит отсутствие, а не пустоту). Замер боя: 5 из 331 заперты, у ТРЁХ имя лежало рядом. Построена цепочка `LOCALE_DESCENT` — одна на продукт, копий было три |
| `bugs/150` | кружок человека без имени — **фирменное N** (решение владельца), знак берётся у `Brand.svelte` со снятой плашкой, а не перерисован |
| `bugs/151` | курсор-палец вместо лупы: лицо, открытое фото, портрет на лендинге. Луп не осталось |
| термин | «грязные точки» → **«обновлённые NDim ID»** в логах и живом коде (слово владельца: «вульгарное название») |

✅ **Переименование ДОВЕДЕНО ДО КОНЦА 2026-08-18** — 13 мест в 9 файлах: тесты `sync-server/*`,
`measure-dims-index.mjs`, `tools/seed-dev.mjs`, `tools/measure-people-census.mjs`,
`src/lib/migration/*`, `qa/JOURNEYS.md`, `qa/suites/smoke.md`. В коде продукта слова «грязная
точка» не осталось. НЕ тронуты намеренно: цитата владельца в `index.mjs:1056` (история повода),
«грязное дерево» в `tools/deploy.mjs` (это про git, другой смысл), летопись и закрытые документы.
⛔ **Поле `dirty` в базе НЕ переименовано** намеренно: это миграция 342 документов, правил и
тестов. Названо в `plans/60` → «Что эпик НЕ делает», чтобы не потерялось.

## Сессии 28–31 (2026-08-21) — перенесено стрижкой бонсая при закрытии сессии 32 (2026-08-21, ночь)

*(Дословно из `STATUS.md`. Ключевое, что их пережило, осталось в STATUS указателями: App Check
ждёт выката · правила Firestore не выкачены · пять правок вида `[NOT-TESTED]` · `bugs/158`
открыт · `plans/58` шаг 1 вернуть в очередь.)*

**Сделано сессией 31** (коммиты `50a7cc1…cf916c9`, запушено):
- **Приёмка `/fable-judge` по `plans/63`** (слово владельца «Принимай работу»): VERIFIED —
  юниты 319/319 · `test:sync` 112/112 · мутация «deleteUser снят» в judge-worktree → ровно 4
  названных провала · замер `listUsers` на бою сходится · кадры глазами. **Второй судья —
  параллельная сессия владельца — дал тот же вердикт независимо**; его краевая заметка закрыта
  catch-ом `auth/user-not-found` (`0720d14`: гонка с внешней чисткой не рвёт суточный проход).
  Обе оговорки сняты перегоном: живая проба **16/16** · `verify-admin-home` **19/0**.
- 🎓 **`EXP-0175`**: `git worktree remove --force`/`Remove-Item -Recurse` проходят СКВОЗЬ
  junction и съедают настоящий `node_modules` (оплачено: съеден корень, восстановлен `npm ci`).

🆕 **ФАЗА 5 ЭПИКА ГОСТЯ: ВОРОТА ПРОЙДЕНЫ 2026-08-21 вечером** — макеты четырёх поверхностей
(`design/guest-phase5-mockups.html`, съёмщик `tools/shoot-guest-phase5.mjs`) поданы и владелец
выбрал ВСЕ четыре (таблица решений — `plans/22` фаза 5): **A** = V2-карточка + текст V1,
ПОСТОЯННАЯ на всех экранах · **B** = V4-подсказка, слово «гость» (уточнение №010 Р2), ховер/тап ·
**C** = V1-карточка + двери V4 · **D** = V2 «строка-след».
✅ **C и A уже ИСПОЛНЕНЫ тем же вечером.** C — финальный экран истёкшей сессии (профиль,
`.door`-двери, знак `Brand` без плашки): живая проба **16/16** с кадрами. A — постоянная
карточка гостя `src/lib/ui/GuestCard.svelte` на ПЯТИ экранах (Связи · Пространство ·
Измерения · Меню · Аккаунт; на профиле НЕ двойник — там его расширенная карточка, допущение
названо в плане): зонд `tools/probe-guest-card.mjs` **11/11**, кадры 2×2 смотрены,
svelte-check 0/0. **B и D** — в составе фазы 7 (видимы после снятия фильтра анонимов).

🆕 **Фаза 4 закрыта — что было в коде** (`plans/63_DONE`, коммиты `7b16ab6…`):
- Уборка гостей несёт **полный перечень следов §4**: своё дерево · членства/подсказки в чужих
  (`removeGroupEntriesOf`, один обход на проход) · дружбы обеих сторон · заявки обезличены ·
  фото в Storage · **`deleteUser` ПОСЛЕДНИМ**. Общие куски переиспользует
  `cleanupDeletedPeople` — он теперь убирает и осиротевшие дружбы (шаг 4, сосед 9 → 11 тестов).
- **`GUEST_TTL_DAYS` и `isGuestExpired` переехали в `schema.ts`** — одна истина на сервер и
  клиент; юниты границы: ровно 7 дней — ещё жив.
- **Клиент распознаёт истёкшего**: ветка `ProfileMissingError` профиля различает свежего гостя
  и истёкшего → экран «Гостевая сессия истекла» с «Начать заново»/«Войти в аккаунт».
- `test:sync` поднимает **и Storage** (`--only firestore,auth,storage`).
- **Замер `listUsers` на бою**: 355 учёток = **1 страница, 623 мс** — PENDING плана улажен.
- **Живая проба** `tools/probe-guest-death-live.mjs` (стенд, 16 проверок). Капканы прибора:
  у `sync-server/` СВОЙ `node_modules` (`createRequire` от `sync-server/`); тема продукта —
  атрибут `data-theme`, `emulateMedia` её НЕ переключает.
- ✅ **Судья прогнан 2026-08-21: VERIFIED WITH CAVEATS**, оговорки сняты перегонами (см. выше).

**Также сделано сессией 30:** разнос ответов №039 В1 и №040 В1–В2 (страж `questions`: новых
неразнесённых **0**) · `verify-admin-home` перепрогнан на живом стенде **19/0** — долг
`[NOT-TESTED]` сессии 18 снят.

🆕 **Сделано сессией 28 (утро):**
- **Фаза 1 эпика 22 закрыта прибором** (3 из 3): `tools/probe-guest-screen.mjs` — настоящий
  гость, оценки через ленту, возвраты на «Связи» SPA-тапами. **Ф2 подтверждён**: экран
  замораживает снимок первого визита до конца сессии; жест обновления показывает базу.
  **Гость видит четыре «Без имени»** — правила не пускают анонима в чужие имена до фазы 6.
  ⚠️ Капкан зонда: на стенде голый адрес подменяет анонима dev-пользователем — дверь
  `?as=guest` в каждый переход (`EXP-0174`).
- **Фаза 2 эпика 22 закрыта**: интервью №042 отвечено страницей за минуты. Решения: гости в
  общем «Всего людей» (В1=B) · лица анонимов показывать, «Пожаловаться» — планируемая, вне
  эпика (В2=B) · **уборка гостей не раньше октября 2026** (В3=A) · политику датировать без
  письма (В4=A) · заявки гостя без потолка (В5=C). Таблица «решение → куда легло» —
  `plans/22` фаза 2.

## Порядок работ, назначенный владельцем 2026-08-17, и разборы сессий 19–21 — перенесены при закрытии сессии 32

*(Дословный гигантский блок «ПОРЯДОК РАБОТ… его словами» с историей шага 1 (выкат текста
2026-08-17, менеджер измерений в бою, живая проба контура, очередь кандидатов, два рода тегов),
разборы сессии 19 (дверь каталога, формула гиперболы `v/(v+10)`, строка места) и блоки 5б…9
(вычитка заимствований: своды RU/EN, ворота `gate-rewrites`, линтер голоса, ultracode отключён,
слово о вере, автопересборка `researches/39`) жили в STATUS со строк 282–607 и переехали сюда
как единый исторический пласт. Живые правила из них уже продублированы в своих местах: гипербола —
в шапке `voteWeight`; запрет отката к Википедии — в шапке `rewrite-catalog-descriptions.mjs`;
режим усилий max — в STATUS; «замер — факт о сегодня» — в шапке `AGENT_GUIDE.md`; заказ
автопересборки — `researches/39`, план не написан; английские 533 — в беклоге словом владельца,
фан-аут не разрешён; `ideas/37` — очередь за владельцем. Полный текст пласта — в истории git
STATUS.md до коммита закрытия сессии 32; главные числа: русских копий 42 → 5, английских 533,
своды 5111×2 закончены.)*

**Что закрыто сессией 18 (2026-08-16):** фаза 5 «ТЕСТ» закрыта (`plans/42_DONE`, страж
`verify-test-pages` 89, 3 мутации, судья VERIFIED WITH CAVEATS, приёмка №034 В2 = Б);
`plans/47` и `plans/46` закрыты (очередь `plans/49`: 8 из 13); `plans/48` шаг 6 закрыт
(`verify-catalog-hubs` 89); «Панель администратора» — общий раздел с «Менеджером измерений»
внутри; `bugs/134` воспроизведён (vite dev 4093 МБ при потолке ~4100, диагноз не поставлен);
`EXP-0165` («нет записи» — утверждение о поиске).

## Эстафеты сессий 18–19 и 23 — перенесены стрижкой бонсая при закрытии сессии 31 (2026-08-21, вечер)

*(Дословно из `STATUS.md`. Ключевое, что их пережило: «не браться за ворота» ОТМЕНЕНО №041 В3 = A
(ворота расконсервированы); `verify-admin-home` перепрогнан 19/0 сессиями 30–31; линтер голоса
построен (сессия 19, 5г); №036 В3 отвечен. Капканы сессии 23 живут классами в `EXPERIENCE.md` и
досье окружения.)*

## 🏁 ЭСТАФЕТА СЕССИИ 23 — прочти это первым

🚀 **Бой на 2026-08-18 ≈01:20:** выкат с расписки `f6ac989`, хеш `1crreu7`, каталог **5121**
(10 242 страницы), ворота Smoke 19/0, смоук под сессией 24/0, гостевой 81/0, правила выкачены.
**Дерево чистое, всё запушено. Стенд погашен по дереву, сирот нет. Оба контейнера сервера
синхронизации живы.**

**Три вещи ждут решения владельца, и ни одна не блокирует работу:**

1. 🔴 **`bugs/146`** — какой путь починки индекса (рекомендация: B). Разбор выше.
2. **`plans/58`** — теги жизненного цикла, шаг 1 меняет 5121 боевую запись.
3. **`plans/59`** — виджет «Сегодня» списком; внутри одна развилка (чем показать прокручиваемость).

**Что можно брать БЕЗ него прямо сейчас:**
· `plans/59` шаги 1–3 (павлоад, формат пункта, булеты) — развилка только в шаге 4;
· новая партия кандидатов: разведка `scout-wikidata-candidates` → 175 объектов 2026 года, которых
  у нас нет, уже найдены (`candidates/scouts/2026_wikidata_new_releases.md`);
· фаза 4 эпика 40 (опорные страницы) — она шла параллельно и не начата.

⚠️ **Капканы этой сессии, оплаченные дважды каждый:**
· **дерево во время прогона выката НЕ ТРОГАТЬ** — писал план во время стейджа, расписка вышла с
  грязным деревом и в бой не годилась;
· **страж может быть зелёным по ЧУЖОЙ причине** — проверка «удалённого нет в списке» не краснела
  на мутации, потому что сервер синхронизации уносил запись сам. Лечение — СТАВИТЬ предусловие,
  а не ждать в него попасть;
· **страж, пишущий в базу стенда, обязан убрать ВСЕ свои следы, а не первый** — накопленные пробы
  красили ИСПРАВНЫЙ продукт;
· `| Select-Object -First N` рвёт конвейер PowerShell и даёт код 255 на успешном приборе.

## 🏁 ЭСТАФЕТА — с чего начать

**Первым делом:** `AGENT_GUIDE.md` + этот файл + `npm run questions`.
Снято 2026-08-16 при закрытии сессии 18: интервью **35**, **ждут владельца 0** · **новых нарушений
за сессию 0** · унаследованный долг: вопросов вне `interviews/` **25** (единственный «новый» —
давний в `ideas/38:32`, это перенос строки, а не вопрос), неразнесённых ответов **25** ·
просроченных замков **0**. Долг за сессию не вырос.

🎯 **ГДЕ ПРОДОЛЖИТЬ (сессия 19 → 20) — ЧЕКЛИСТ ДЛЯ ТОГО, КТО НИЧЕГО НЕ ЗНАЕТ:**

0. 🔴🔴 **СТОЯЧЕЕ ПРАВИЛО ВЛАДЕЛЬЦА, ЕЩЁ НЕ ИСПОЛНЕННОЕ** (2026-08-16, дословно): «*изучай мою
   стилометрию ВСЯКИЙ РАЗ, когда пишешь текста. Напиши строгие линтеры, которые смеряют ИИ
   сгенерированные текста с голосом моим и с моим умением посылать мысль и с моими ценностями.
   Линтеры не должны пропускать то, что плохо согласуется с моим голосом. В чем сомневаешься —
   мне на вычитку*». Линтеров НЕТ — это главный незакрытый долг сессии 19.
   🔑 Материал готов: `AUTHOR_STYLOMETRY.md` (2198 строк) несёт пронумерованные правила, и часть
   из них машинно проверяема прямо сейчас — запрещённые конструкции (`, —` с нулевой авторской
   базой), абсолютные замены («снова»→«вновь», «надо»→«нужно», «будто»→«словно»), стоп-слова
   хеджей и латинизмов, числа ритма (медиана предложения 10 слов, колон 4–6, запятые 8–10 на 100
   слов, разброс длин). Ценностный слой машиной не берётся — он идёт владельцу на вычитку.
1. 🔴 **Интервью №036 — остался ОДИН вопрос, и он держит выкат: В3** (тексты блока-двери,
   принять как есть и вычитать потом — или вычитать до выката). Пока без ответа — **в бой ничего
   не катать**. Спросить: `npm run review -- open interviews/interview_036_catalog_phase3_forks.md`.
   ✅ **В1/В5 закрыты** — макеты сработали ровно так же, как график: владелец выбрал глазами.
   💡 **Метод подтверждён дважды за день:** не понял вопрос словами → рисуй варианты рядом на
   боевых числах. Прибор макетов проверяет, что страница ОТРИСОВАЛАСЬ (`shoot-catalog-place`, 7
   проверок × обе темы), — иначе владельцу уезжает пустая вкладка.
2. **Написать план автопересборки** (`plans/57`) по решению владельца — см. пункт 9 выше и
   `researches/39`. Узел, который надо спроектировать: узкая дверь только для данных.
3. **Шаг 3 фазы 3 — 279 EN-описаний** (`researches/34_en_boilerplate_candidates.md`), журнал
   правок на вычитку постфактум (№025 В6 = А). Шаг 4 — расширить `verify-dimension-pages`.
4. 🐞 **`bugs/135`** — лечить путём **B** (теги по языкам в данных), это выбор владельца.
   ⚠️ **Замер сессии 20: путь B упирается в несуществующую комнату.** Это правка 43 011 тегов в
   данных каталога, то есть территория «Менеджера измерений» (`plans/44`, не начат). Либо сначала
   комната, либо возврат к владельцу с этой ценой — но не «начну и застряну».
5. ⚠️ **Снимок каталога обновлён 2026-08-16** (`fetch-dims-slice --all`) — он вне git, на новой
   машине его не будет, и сборка молча уедет на запасном срезе из 50 записей. Проверяй строку
   `источник: полный каталог` в логе сборки.
3. ⚠️ **Первым делом, если поднимешь стенд:** перепрогнать `node tools/verify-admin-home.mjs` —
   он правлен в сессии 18 и помечен `[NOT-TESTED]`, потому что стенд умер. Поднимать стенд с
   `NODE_OPTIONS=--max-old-space-size=6144` (`bugs/134`).
4. **Стражи, заведённые сессией 18** (гонять при правках соответствующих поверхностей):
   `node tools/verify-test-pages.mjs` (89) · `node tools/verify-catalog-hubs.mjs` (89) ·
   `node tools/verify-tier2-prefetch.mjs` (7, нужен `npx vite preview --port 4173 --strictPort`) ·
   замер `node tools/measure-bug120-wrap.mjs` (нужен стенд).
5. 🔬 **`bugs/134` — если стенд снова умрёт, не гадай:** виновник назван числом (`vite dev`,
   4093 МБ при потолке ~4100), и следующий шаг записан в баге — гонять `npm run build` в цикле
   рядом с живым стендом и смотреть, растёт ли он от ЭТОГО.
6. ⛔ **Чего НЕ делать:** не браться за ворота, онбординг и мост гостя (`plans/21`/`22`/`23`) —
   отложены словом владельца: «в день выката пока неактуально, у нас нет живой аудитории». Не
   сегментировать sitemap (замер: 10 432 из 50 000 адресов). Не писать в планах «ждёт AUTH
   выката» — формулировка отменена (№034 В1 = В).

## Сессия 27 — 2026-08-21 (ночь): аудит `ideas/39` завершён и отвечен, ворота расконсервированы

*(Перенесено дословно из `STATUS.md` стрижкой бонсая при закрытии сессии 28.)*

**Последнее обновление:** 2026-08-21, сессия 27 — **АУДИТ `ideas/39` ЗАВЕРШЁН И ОТВЕЧЕН
В ТУ ЖЕ НОЧЬ.** Оба среза, e2e прод-артефакта 100/0 дважды, `bugs/156` закрыт тем же днём.
Владелец ответил на №041 через страницу за минуты: **В3 = A — ВОРОТА РАСКОНСЕРВИРОВАНЫ,
идут первыми после текущего шага эпика 60.** Отчёты аудита:
`reports/KAIF_AUDIT/2026-08-21_full_SUMMARY.md` + `…IDEA_SLICE.md`.

✅ **В1 №041 ЗАКРЫТ ТОЙ ЖЕ НОЧЬЮ** («Админ может всё» = широкое чтение — замысел):
`bugs/155` DONE — описания четырёх правил и `schema.ts` приведены к замыслу, право
закреплено тестом (набор правил **148/148**, страж доказан мутацией 147/1→148/0),
поведение не менялось. ⚠️ Правки комментариев правил уедут в бой вместе с ближайшим
выкатом правил (белый список эпика 60).
✅ Разнесено этой же ночью: В2 = A с его формой → `bugs/154` (клиент чистит своё + помечает,
сервер вычищает остатки суточным проходом) · В4 = C → `bugs/157` (вычитка ВСЕХ EN-текстов
1.x целиком — в очередь). `bugs/156` ПОЧИНЕН и закрыт (316/0 · check 0 · e2e 100/0).

Той же ночью начата волна ворот: `plans/62` шаг 2 закрыт (подписка эскорта вместо опроса,
обрыв переживается догоном), датовая половина зонда анонима (`tools/probe-guest-loop.mjs`)
сняла кривую новичка — топ при 5 оценках: 3 человека, максимум 1 %.

## Сессии 22–23 — 2026-08-17/18: контур пополнения каталога замкнут, девять тикетов закрыто

*(Перенесено дословно из `STATUS.md` стрижкой бонсая при закрытии сессии 28.)*

**Предыдущая веха (сессии 22–23):** контур пополнения каталога замкнут и прошёл живую пробу
владельца. Каталог впервые с переноса пополнился: 5111 → 5121. Слово владельца: «*Пространство с
сегодняшнего дня вновь начнёт расти*».

**Девять тикетов владельца закрыто за две сессии** (`bugs/139…148`, кроме открытого `146`):

| # | Замечание владельца | Чем закрыто |
|---|---|---|
| `139` | «скупой объём и стиль… **мы: мини википедия**» | правила стиля из ЕГО текстов о «Матрице»; 10 описаний переписаны: 266…372 → **905…1094** знака, копий Википедии **0** |
| `140` | форма «Создать» под вкладкой очереди | ветка `{:else}` развёрнута в явные условия |
| `141` | комната открывается не на очереди | умолчание вкладки `'queue'` |
| `142` | нет комментария и «Вернуть на доработку» | четвёртый статус `returned`, поле `ownerNote`, **прибор чтения возвращённого** |
| `143` | — (найден агентом) | самотест прибора исполнялся при ИМПОРТЕ и печатал зелёное |
| `144` | «**во всех фильмах обязательные теги** — это КЛАСС» · «без имён собственных» | соглашение ВЫВЕДЕНО замером (7 видов), двое ворот fail-closed, 85 записей каталога приведены в порядок |
| `145` | серый текст в админке | токены переопределены в корне раздела, а не двадцатью правками |
| `147` | удалённое возвращается в список | `deletedThisSession` — зеркало памяти созданных |
| `148` | тени карточек мерцают | тень снята, подъём оставлен; принято владельцем по макету |

**Что вычищено в боевом каталоге (с откатами):**

| Правка | Записей | Откат |
|---|---|---|
| Обязательные теги вида | **85** | `tools/tags-all--rollback.json` |
| Нулевая ширина в названиях, тегах, видах, описаниях | **560** | `tools/hygiene--rollback.json` |
| Провёрнутые поля вида (`type.ru=«Movie»`, `year=«Фильм»`) | **4** | там же |
| Теги-имена собственные (Marvel, Нолан, страны) | **17** у 10 записей | там же |

## Сессия 16 — 2026-08-16: эпик стейджа закрыт, все девять критериев П1–П9 измерены

*(Перенесено дословно из `STATUS.md` стрижкой бонсая при закрытии сессии 18.)*

**Что закрыто сессией 16** (эпик `plans/53` — все девять критериев П1–П9 измерены):

1. ✅ **Стейдж больше не отстаёт** — выкачен ДВЕРЬЮ, а не руками. Замер попадания: цель отдала хеш
   собранной сборки, соседний контур (бой) остался прежним.
2. ✅ **Фаза 4** — одна дверь с параметром контура, шесть приборов `verify-prod-*` приняли `--base`,
   стражи П6 (11/0), П7 (14/0), П8 (стоп-правило, 8 случаев самопроверки).
3. ✅ **Фаза 5** — ответ №033 В5 = А стал ЗАМКОМ в двери.
4. ⚠️ **Данные стейджа — слепок ДО миграции**, на несколько часов старше боевых. Для тестирования
   безразлично; нужен свежий — один прогон
   `node tools/migrate/seed-stage-from-prod.mjs --apply --wipe` (сухой прогон по умолчанию).
   🆕 Умолчания прибора починены (`bugs/132`): он читал базу `(default)`, которой нет с переезда.
5. 🧾 **Расписка стейджа живёт на коммит.** Она была выписана на `8560966`; любой следующий коммит
   делает её просроченной, и дверь боя честно откажет со словами «на стейдже проверялся ДРУГОЙ
   коммит». Это не поломка, а сам механизм: **сначала `npm run deploy -- --stage`, потом бой.**
6. 🔴 **Ждало слова владельца, но не блокировало:** у замка стейджа есть аварийный обход
   `--auth-owner "<его дословные слова>"` — одноразовый, с баннером и журналом. Решение принял
   агент; доводы «за» и «против» записаны в `plans/53` → «Решения, принятые агентом без владельца».

---

## Сессия 15 — 2026-08-16, ночь: бой переехал в `ndim-db-prod`, стейдж наполнен, «вычислитель» вычищен

Три большие вещи за одну ночь, каждая с приборами и мутациями.

**1. Стейдж наполнен обезличенным слепком боя и сверен** — 10 605 документов = 11 266 − 331
(`privat`, `bugs/127`) − 330 (`ndimids`), расхождений 0. UID новые: зарубка прошлой сессии
предлагала их сохранить ради ссылочной целостности `friendships`/`audience`/`testPairs`, но
ЗАМЕР показал, что все три коллекции в бою ПУСТЫ, — довод был воображаемым. Второй контейнер
`ndim-server-stage` поднят, стражи контуров доказаны отказом.

**2. «Вычислитель»/`calculator` → СЕРВЕР СИНХРОНИЗАЦИИ везде** — 715 замен в 146 файлах, по
прямому слову владельца («отсебятина, придуманная ИИ агентом по ошибке при переносе 1.x в 2.0»).
Имя не выдумано, а восстановлено из руководства 1.x. Сухой прогон поймал три ловушки слепой
замены: каталог `dims-build.json` («вычислительница» — профессия героини ФИЛЬМА), тексты продукта
(«Калькулятор любви» и «интернет-калькуляторы» о чужих сайтах), фикстуру стража жаргона.
Правило стало каноном с машинным стражем `verify-sync-server-name.mjs`.

**3. БОЕВАЯ МИГРАЦИЯ `(default)` → `ndim-db-prod`** (AUTH: «Да, начинай, мигрируем, даю добро»).
Три репетиции пройдены заранее: Р2 на боевых данных — 0 расхождений; Р3 (откат) — одно расхождение
`space/server`, объяснённое арифметикой (99 секунд между отпечатком и выгрузкой при цикле 60 с),
что заодно доказало ЧУВСТВИТЕЛЬНОСТЬ сверки. Итог миграции: П1/П2 — 11 266 = 11 266, потеряно 0,
лишних 0, искажено 0 · П3 — `freeTier: True` · П4 — окно 31 минута при потолке 60 · смоук под
сессией 24/0 · гостевой 81/0. Необратимый шаг выполнял ВЛАДЕЛЕЦ скриптом
`tools/migrate/run-prod-migration.ps1`: удаление боевой базы блокирует классификатор безопасности,
и рубеж законный.

**Ключи уехали в `.env`** (слово владельца: «никогда ключи в коде и других файлах не храним»),
в base64. Повод оказался не вкусовым: боевой контейнер монтировал ПАПКУ `secrets`, и ключ стейджа
немедленно оказался внутри боевого контейнера.

**Четыре дефекта, найденные и закрытые по ходу:** `bugs/130` (отпечаток считал каталог дважды —
ложное число уже уехало в `plans/53` как ожидание для П1; прогон подешевел на 5 112 чтений) ·
`bugs/131` (сырые байты NUL делали исходник невидимым для `git diff` и `grep` — потому первый
дефект и прожил незамеченным) · дыра двери (правила Firestore в бой выкатить было НЕЧЕМ: хук
запрещает голый выкат, а дверь катила только хостинг) · хук блокировал `git commit`, потому что
запрещённые слова встретились в СООБЩЕНИИ коммита (`EXP-0163`).

⚠️ **Чуть не случившаяся авария, записанная честно** (`EXP-0164`): готовя массивную форму
`firestore`, агент переписал `firebase.json` ЦЕЛИКОМ по памяти, прочитав из него одну секцию.
Реконструкция снесла бы 17 редиректов 301 и правила кеширования, вылеченные `bugs/124`. Поймано
сверкой с git до выката.

### Перенесено из STATUS дословно (работа закрыта и выкачена)

### 0. 🚪 ВОРОТА — тексты ПРИНЯТЫ владельцем 2026-08-15 (`plans/29`, В9 = А)

Волна разблокирована и фаза 2 прошла второй заход вычитки: **все десять вопросов отвечены,
вердикт «принять»**. Решения исполнены в тот же час.

**Что владелец решил — коротко, чтобы не перечитывать:**
· якорь витрины — «точный, честный и **быстрый** беспристрастный **суперкомпьютер**», не
  «калькулятор» (В5 = Б, В8 = Б; `GOAL.md` получил действующую формулировку, цитата 01.08 не
  переписана — она датирована);
· подзаголовок 1.x остаётся **дословно**, вместе с «Забудьте о бесконечных свайпах»;
· термины на витрине **раскрываются**, голых нет: «5 111 уникальных объектов культуры», а не
  «5 111 измерений». Слов «измерение», «NDim ID», «вектор» на первом экране ноль;
· полоса чисел стала **блоком живых метрик**, числа приезжают из `space/stats` (№010 Р7);
· **активных за неделю показываем, за месяц — нет** (В6);
· формула похожести — **его строка из руководства 1.x** дословно (В10 = Б).

🔴 **Один хвост, принятый вместе с решениями:** активных за 7 дней сегодня **ноль**, и блок метрик
скажет это вслух ровно в самый уязвимый момент воронки. Решение владельца принято с открытыми
глазами; **как выглядит строка при нуле — вопрос макетов фазы 3**, а не текста.

🔴🔴 **ВЫГОВОР 2026-08-15, читать прежде, чем «проверять» слова владельца.** Я объявил слово
«быстрый» необеспеченным и завёл под него долг замера. Ответ дословно: «*быстрый в тестах не
нуждается. Каждое моё слово собираешься тестировать? Это привлечёт нам пользователей, если ты мои
слова протестируешь? **НЕ ПУТАЙ ЦЕЛИ И НЕ ЗАНИМАЙСЯ ЕРУНДОЙ***».
Он прав дважды: принцип 8 стережёт **обещания** (проверяемые утверждения о фактах и исходах), а не
**характеристику в ряду характеристик** — и слово к тому же подпёрто делом: окно новичка выкачено,
первый топ ≤ минуты после первой оценки (`sync-server/index.mjs:108`). Долг снят.
🔑 **Граница записана в `GOAL.md` рядом с принципом 8.** Правило: прежде чем требовать источник —
спроси, это обещание человеку или слово автора о своём продукте; и главное — **что эта работа даёт
людям, которые придут**. Проверка, не меняющая для них ничего, это трата времени владельца.

✅ **Фаза 2 ЗАКРЫТА целиком:** EN-витрина принята (`plans/51`, В4 = А) и оба языка вынесены в
`src/lib/content/landing-copy.ts`. Приёмка: check 0/0 · typecheck · test 284/0 · словарь 7/0 ·
e2e 100 · 8 кадров живым Chrome.

✅ **Фаза 3 ЗАКРЫТА 2026-08-15 — владелец выбрал V4 «Игра сама вносит внутрь»** (`plans/52`, В1 = Г)
и заменил три «фичи» четырьмя отличиями (В2 = А). Макеты — `design/gate-first-screen-mockups.html`,
16 кадров в `test-results/gate-first-screen-mockups/`.

🔑 **Что этот выбор решил сверх композиции.** Он взял единственный вариант, где **большой кнопки
входа гостем НЕТ ВОВСЕ**, — то есть подтвердил №009 В3 и развёл столкновение, которое агент
отказался разводить молча: вычитанная кнопка «Найти людей по душе» + «гостем, без аккаунта» на
витрину не выходит. Человек играет со звёздами, панель с найденной связью вносит его внутрь сама.

🔴 **Следствие для порядка работ: фаза 4 теперь ЗАВИСИТ от моста эпика `plans/23`** — панель
«появилось после ваших звёзд» это его мост, а не украшение лендинга. Строки `gateCopyPhase4.cta`
ждут там же.
⚠️ **В3 и В4 остались без ответа** (владелец ответил 2 из 4); оба выведены из выбора V4 и помечены
`вид=выведено-из-решения` в `plans/52` — строка нуля СКРЫТА, дверь ПОЯВЛЕНИЕМ. Ждут одного слова.

Фаза 7 (отзывы) **отложена владельцем** (№010 Р9) — приёмка ворот её не ждёт.

🆕 **Дверь вычитки поумнела:** `review.mjs open`/`render` **отказываются** поднимать страницу, если
неотвеченный вопрос ссылается наружу («выше», «вон та формула»). Куплено словом владельца:
«*Пиши прямо в вопросе то, что предлагаешь взять. Я не собираюсь скролить этот длинный документ*»
(`EXP-0161`, доказано мутацией). Исключение — `<!-- ССЫЛКА-ОК: причина -->`.


2. 🆕 **`verify-prod-signed-in` — обязательный смоук боя под сессией** (вход настоящей кнопкой,
   5 экранов × 390/1440 × обе темы, консоль, уборка анонимной учётки за собой). **24/0.**
   Его ПЕРВЫЙ прогон нашёл `bugs/125`, который не видел никто: очистка адреса шла голым
   `history.replaceState(null, …)`, и `null` стирал состояние роутера — у гостя «Назад» не
   закрывал предпросмотр, второе нажатие выкидывало из приложения. Вылечен класс из 6 мест
   (`src/lib/ui/history.ts` → `replaceUrl`); голого `replaceState` в продукте больше нет.
3. 🆕 **ПРИЁМКА — НЕ ЦЕРЕМОНИЯ: такт Г нашёл ДВА дефекта, которых не видел ни один прибор**
   (сессия 12). Оба уже в бою починенными:
   · **`bugs/126`** — одноимённые вещи неразличимы в строке. В каталоге **148 имён носят 307
     измерений** (роман и фильм, оригинал и ремейк), а набор теста идёт по популярности, и
     самые оценённые дубли («Гарри Поттер», «Мастер и Маргарита») попадают в ПЕРВЫЕ карточки.
     Лечение — слово владельца (№032 В1 = В): вид и год у КАЖДОЙ строки, вид КАНОНИЧЕСКИЙ
     (`kindLabelFor`), потому что в поле `type` каталога грязь и на карточке её прячет капитель.
   · **`bugs/120`** — был заведён 14.08 чтением кода и **не починен**: `nowrap` + `ellipsis` на
     строке панели, прямое нарушение закона о названиях. На широком экране с короткими именами
     не показывался — вскрылся, когда строка ВЫРОСЛА от первой правки. 🔑 **Урок: дефект,
     заведённый чтением, живёт до тех пор, пока его не начнут НАБЛЮДАТЬ.**

---

## Сессия 13 — 2026-08-15, день: волна «Ворота» разблокирована и прошла три фазы

Владелец спросил, что блокирует продвижение. **Ответ оказался: ничего.** Фаза 7 мастер-плана
(«ВОРОТА», там же помеченная словами «ГЛАВНОЕ СЕЙЧАС») **две недели стояла за тремя замками**, и
каждый был снят раньше, чем записан:

| Замок | Держал | Снят на самом деле |
|---|---|---|
| `MASTER_PLAN.md` «Волна заблокирована интервью №010» | всю волну — 4 эпика | **2026-08-01**: владелец ответил на все 15 развилок, и коммит `f938cf9` так и назван — «волна «Ворота» разблокирована» |
| `plans/21`, трассировка: «фаза 2 заблокирована `researches/27`» | тексты ворот | `researches/27` закрыт — и это пишет **шапка того же документа**, на 430 строк выше |
| `plans/29`: «портрета голоса нет вовсе — есть только пустой шаблон» | ту же фазу 2 | `AUTHOR_STYLOMETRY.md` лежит с **2026-08-09**, статус accepted по слову владельца 2026-08-14 |

**Цена, названная числом.** Всё это время работа шла в соседнюю половину воронки: 10 222 публичные
страницы каталога, языковые адреса, семейство «ТЕСТ», `/ru/test/love` в индексе Google. Приток
строили — ворота не трогали. Бой на день разбора: людей **94**, новых за 30 дней **1**, активных за
7 дней **0**.

**Класс снят механикой, а не уроком** (`STATUS.md` п. 8: журнал опыта — ПОСЛЕДНЕЕ средство).
`npm run questions` получил **проверку 4 «ПРОСРОЧЕННЫЕ ЗАМКИ»**: строка, объявляющая блокаду и
называющая интервью, краснеет, если у интервью ноль неотвеченных вопросов. Первый прогон —
**17 мест, из них 4 настоящих замка**, включая тот, которого никто не искал: `plans/34` ждал ответ
№020 В2 уже **после** выката 10 222 страниц, которые этот замок сторожил. Доказана мутацией
(вернуть `⛔` в `MASTER_PLAN.md` → 0 → 1). Исключение — `<!-- ЗАМОК-ОК: причина -->`; вне корпуса
`PROJECT_HISTORY.md` и файлы с тегом `_DONE_`. Граница названа честно: проверка знает только про
интервью, «заблокирована `researches/27`» она НЕ поймала бы. План работы — `plans/50`.

🔴 **Слепая зона, найденная на себе в первые же минуты.** Как только в `plans/50` появилась таблица
«что куда разнести», счётчик обратного плеча упал **35 → 25 сам собой** — до единой правки в
планах-получателях. Обратное плечо ищет метку вопроса и номер интервью в пределах 90 знаков, и
таблица этому удовлетворяет идеально. **Страж померил цитату, а не разнос** — ровно риск, записанный
в таблице рисков того же плана, сработавший на его авторе. Разнос выполнен по-настоящему; граница
записана в канон: **обратное плечо — счётчик долга, а не доказательство разноса**.

### Фаза 2 «Тексты ворот» — два захода вычитки, приняты

Десять ответов интервью №010 разнесены в `plans/21`, `22`, `23`, `25` с правкой содержательных
строк; дважды ответ **опроверг** написанное там раньше (`plans/23` Ф4 «демо-персонажи живут только
на лендинге» — Р6 заводит их реально в БД; `plans/22` — возражение агента против пилюли «аноним»,
которое владелец снял выбором Б).

§2 `plans/29` переписан по портрету голоса и правкам владельца: подзаголовок 1.x возвращён
**дословно**, термины раскрыты по его образцу («5 111 уникальных объектов культуры»), полоса чисел
стала **блоком живых метрик**, якорь сменил «калькулятор» на «суперкомпьютер».

🔑 **Портрет подсветил ровно ту строку, которую владелец отвёл** словами «вот это не мой язык, твоя
стилометрия должна была это красным цветом подсветить». Строка «Это произведение, а не среднее…»
ловится тремя правилами сразу: **АП28/B8** (антитеза «не X, а Y» как шаблон мысли — их там ДВЕ
подряд), **АП21** (риторические качели), **АП33** (голый внутренний термин). Чутьё автора назвало
то, что портрет потом сформулировал правилом.

Вычитка закрыта вердиктом **В9 = А «принять»**, все десять вопросов отвечены. По В10 = Б владелец
вернул **свою** формулу из руководства 1.x, отвергнув и первую редакцию агента, и вторую,
переписанную по портрету, — третий подряд случай, когда его выбор расходится с рекомендацией.
Рабочее правило отсюда: **портрет — про форму, содержание остаётся за автором.**

Английская витрина (`plans/51`) написана **исходником, а не переводом** (правило АП27) и принята
целиком: `Find your people` (В1 = А) · `New Dimension Connections` вместо `Friendships` (В2 = Б:
по-английски `Friendships` обещает дружбу, русское «знакомства» шире) · `supercomputer` оставлен
(В3 = А). Попутно найдены две языковые ошибки, стоявшие в бою: `really like you` читается как «ты
мне очень нравишься», `forget about` — как «забей на это».

Тексты обоих языков вынесены в `src/lib/content/landing-copy.ts`; компонент лендинга похудел на
96 строк, композиция не тронута. Приёмка: check 0/0 · typecheck · npm test 284/0 · словарь продукта
7/0 на 301 странице · e2e 100 passed · 8 кадров живым Chrome.

**Кнопки сознательно НЕ тронуты:** вычитанный текст обещал «гостем, без аккаунта», а обе кнопки
вели в стену входа, и решение №009 В3 запрещает называть вход анонимом кнопкой. Столкновение двух
решений владельца агент не стал разводить молча — вынес в макеты.

### Фаза 3 «Четыре макета» — выбран V4

`design/gate-first-screen-mockups.html`, 16 кадров. Ось расхождения названа ДО вёрстки — **чем
человек берётся первым**: игрой (V1) · обещанием и игрой разом (V2) · числами (V3) · только игрой,
с переходом, который ПОЯВЛЯЕТСЯ после звёзд (V4).

**Владелец выбрал V4** (В1 = Г) и заменил три «фичи» четырьмя отличиями (В2 = А, чем закрыт
открытый пункт 7 `plans/21`). Тем самым он подтвердил №009 В3 и **развёл столкновение**: большой
кнопки входа гостем на витрине не будет. Прямое следствие — **фаза 4 теперь зависит от моста эпика
`plans/23`**: панель «появилось после ваших звёзд» это его мост, а не украшение лендинга.

### Выговор владельца, изменивший канон

Агент объявил слово «быстрый» в якоре необеспеченным и завёл под него долг замера. Ответ дословно:
«*быстрый в тестах не нуждается. Каждое моё слово собираешься тестировать? Это привлечёт нам
пользователей, если ты мои слова протестируешь? **НЕ ПУТАЙ ЦЕЛИ И НЕ ЗАНИМАЙСЯ ЕРУНДОЙ***».

Ошибка была двойная. Принцип 8 стережёт **обещания** — проверяемые утверждения о фактах и исходах,
— а не **характеристику в ряду характеристик**. И довод был неверен по фактам: окно новичка
выкачено, первый топ ≤ минуты после первой оценки (`calculator/index.mjs:108`). Долг снят из трёх
документов, **граница принципа записана в `GOAL.md` рядом с самим принципом**, чтобы следующая
сессия не пришла к владельцу с тем же вопросом.

### Второй выговор — о доставке, и он стал механикой

Вопрос В7 звучал «Переписанная формула похожести (**выше**) — это Ваш язык?», а текст формулы лежал
в другом разделе документа на 600 строк. Ответ: «*Пиши прямо в вопросе то, что предлагаешь взять.
Я не собираюсь скролить этот длинный документ и искать „вон ту формулу“*».

**Вторая правка о доставке подряд** — первой была «не вынуждай меня лазить по md файлам»
(`EXP-0125`), и лечением тогда стала поднятая страница. Страницу подняли — и внутри неё
воспроизвели ту же болезнь уровнем ниже. Класс: **работа владельца растёт там, где агенту дешевле
сослаться, чем перенести текст.** Лечение механическое: `review.mjs open`/`render` **отказываются**
поднимать страницу, если неотвеченный вопрос ссылается наружу (`lintSelfContained`, доказано
мутацией; исключение — `<!-- ССЫЛКА-ОК: причина -->`).

### Приборы, поймавшие сами себя

- **Съёмщик витрины** печатал надзаголовок в строке подзаголовка (порядковый селектор вместо
  классового) и снимал **светлые** кадры под именем `-dark.png`: лендинг системную тему не слушает,
  он читает `localStorage['ndim-theme']` с умолчанием `light`. Прибор объявлял, что снял обе темы,
  и снимал одну — ровно `EXP-0155`. Добавлена проверка «тема на кадре совпала с запрошенной».
- **Макет** резал звёзды до пяти, и все пять строк выходили «5 из 5» — витрина врала про сам жест
  оценки при каноничной шкале 0–10.
- **Оба новых стража** поймали автора на его же документах: проверка самодостаточности — на
  «ВЫШЕ сгиба», страж вопросов — на заголовке «Вопросы владельцу». Оба раза правился текст, а не
  ослаблялось правило.

**Уроки сессии:** `EXP-0160` (замок пережил своё снятие) · `EXP-0161` (вопрос самодостаточен) ·
`EXP-0162` (не путай цели: слово автора о продукте — не обещание).

**Артефакты:** `plans/50` (разблокировка) · `plans/51` (EN-витрина) · `plans/52` (макеты фазы 3) ·
`src/lib/content/landing-copy.ts` · `design/gate-first-screen-mockups.html` ·
`tools/shoot-landing-copy.mjs` · проверка 4 в `tools/questions-guard.mjs` · `lintSelfContained` в
`tools/lib/review-core.mjs`.

---

## Сессия 11 — 2026-08-15, ночь: очередь починки пошла дальше, выкат в бой

Закрыты две позиции очереди `plans/49` (**5 из 13**, осталось 8) и выкачено в бой
(**AUTH: «пуш, деплой»**).

**`bugs/117` — скачок «Профиля» при возврате из «Как меня видят».** Дефект воспроизведён прибором
`tools/measure-bug117.mjs` с первого захода во всех четырёх конфигурациях: **15 кадров подряд,
сдвиг +453px на 390 и +439px на 1440, прокрутка окна 0**. Гипотеза очереди (восстановление
прокрутки, `view-memory.ts`) опровергнута тем же замером — `afterNavigate` вообще не работает на
возврате с ТОГО ЖЕ пути. Настоящая причина: уходящий блок предпросмотра ещё 240 мс остаётся В
ПОТОКЕ из-за `transition:fade` и толкает профиль вниз. Починка из двух частей — освободить место
сразу (`pinLeaving`, `position: fixed` в `onoutrostart`) и уводить слой плотным движением вправо
(`slideOut`, `fly` с `opacity: 1`) вместо угасания на месте: промежуточная редакция с кроссфейдом
скачок убирала, но давала просвечивание двух текстов, а плотную подложку кроссфейду не поставить
в принципе. Страж `tools/verify-bug117.mjs` — **28 проверок**, доказан 2 мутациями. Урок —
`EXP-0158`.

**`plans/20` — хвосты Д1 и Д2(в) фазы 8 «Удаление аккаунта»**, висевшие с 1 августа. Страж
`verify-account-delete` вырос **22 → 31 проверка**. Д1 («вход той же почтой заводит ПУСТОЙ новый
аккаунт») проверяется REST'ом эмулятора Auth мимо экрана — на стенде сессия заводится сама, и
экранная проверка была бы зелёной при любом коде (`EXP-0114`). Д2(в): способ уронить каскад, не
роняя страницу, оказался в том, чтобы ломать КОД, а не сеть, — предсказание «3 провала, инвариант
зелёный» записано ДО прогона и совпало, продукт честно сказал «Ваш вход цел». Появился ИНВАРИАНТ
фазы — «человек не остаётся без входа при живых данных», зелёный в обоих исправных исходах и
красный ровно на дефекте 1.x (мутация (а): предсказание «7 провалов» совпало). Урок — `EXP-0159`.

**Судья:** VERIFIED WITH CAVEATS — все семь заявлений перепрогнаны заново; оговорка про мутации
`bugs/117` (адресаты названы после прогона, а не до) записана в сам баг-док как есть.

**Выкат 2026-08-15** (`firebase deploy --only hosting --project ndim-space`, оба таргета). Бой
проверен: `verify-prod-b4` **81/0** · `verify-prod-dimension-pages` **44/0** ·
`verify-prod-lang-redirects` **16/0**.

<details><summary>Перенесено дословно из STATUS сессии 10 (закрытое)</summary>

1. 🚀 **Выкат 2026-08-14 состоялся** (AUTH: «деплой»): `firebase deploy --only hosting --project
   ndim-space`, оба таргета. В бою — покраска холста лайтбокса (`bugs/65`). Бой проверен:
   `verify-prod-dimension-pages` **44/0** · `verify-prod-lang-redirects` **16/0** ·
   `verify-prod-b4` **81/0**. Покраснел прибор — начинай оттуда, а не с кода.
6. 🆕 **Сигнал о дефекте KAIF агент заводит issue САМ, без человека и без гейта** (решение
   владельца, `AGENT_GUIDE.md` → «Дефект в САМОМ KAIF», шаг 3). Заведены **#14** и **#15** в
   `MikalaiKryvusha/KAIF`. Подпись агента в теле тикета — КАНОН. Для ЧУЖИХ репозиториев гейт
   остаётся fail-closed.

</details>

---


## 2026-08-14 (поздний вечер) — СЕССИЯ 10: очередь починки пошла, выкат, два сигнала в KAIF

> Стрижка бонсая `/end-chat`. Сессия чинящая: владелец закрыл предыдущий чат словами «сначала
> список, потом форензика и план починки, затем этот чат завершим и в новом будем чинить».

**Закрыто из очереди `plans/49` (3 из 13) + один вне очереди:**

- **`bugs/65`** — светлая щель 15px справа у полноэкранного фото. 🔴 Починено НЕ ПО ПЛАНУ: его
  шаги 1 и 3 опирались на гипотезу, опровергнутую замером. «Очевидное» лечение `width: 100vw`
  не работает — габарит слоя честно становится 1440 при окне 1440, а пиксели полосы не меняются
  ни на бит (оверфлоу фиксированного слоя обрезается краем области прокрутки). Лечит покраска
  ХОЛСТА: `color-mix` подложки с `var(--bg)` на время показа; гуттер остаётся зарезервированным,
  то есть `bugs/59` не тревожим. Страж переписан с ГАБАРИТА на ЦВЕТ, обе темы × 390/1440,
  доказан 2 мутациями (покраска снята → 4; доля 88 → 70 → 2). Близнецов класса найдено 4 —
  лечить нечего ни одного: разделяющий признак не `inset: 0`, а СКОЛЛ-ЛОК (где полоса прокрутки
  жива, 15px занимает она сама). Попутно: `verify-batch4` падал ИСКЛЮЧЕНИЕМ с `bugs/104` и две
  недели молчал про лайтбокс. Заведён `bugs/123`.
- **`bugs/122`** — ответ владельца №031 = «я не помню». Развилка снята не выбором, а тем, что обе
  версии сделаны безвредными. Вскрылось, что приговор «Записать не удалось» был неправомерен в
  любом случае: отказ `fetch` одинаков и когда сервер умер ДО записи, и когда ПОСЛЕ. Страница
  теперь говорит только то, что знает; `verify-bug110` 22/0, доказано мутацией.
- **`plans/12`** — `tools/verify-ideas24.mjs` существовал с 31.07, доказан мутацией и НЕ был
  записан ни в один реестр, то есть не запускался никем. Внесён, перепрогнан 60/0. Ложный след
  «писать `plans/13`» исправлен на `plans/14`.
- **`plans/28`** — прочитан боевой агрегат вершин. **Медиана верхней строки «Связей» 7 %**,
  средняя 11 %, максимум 68 %, у 95 % людей вершина в полосе «low», 93 человека с непустым топом.
  Шаг «ждём руку владельца» оказался сделанным ещё 2026-08-03 (билд 28) — одиннадцать дней число
  лежало непрочитанным.

**🚀 ВЫКАТ 2026-08-14 (AUTH владельца: «деплой»).** `firebase deploy --only hosting --project
ndim-space`, оба таргета (`ndimspace` + `ndim-space`). В бою — покраска холста лайтбокса.
Бой проверен приборами: `verify-prod-dimension-pages` 44/0 · `verify-prod-lang-redirects` 16/0 ·
`verify-prod-b4` 81/0. Снимок витрины обновлён до деплоя (числа не сдвинулись).

**🔴 Владелец поймал агента на неиспользовании собственного опыта.** Дословно: «получается, ты
пишешь опыт и это впустую — ты им не пользуешься». Сессия записала `EXP-0154` про то, что
`npm run e2e` выжигает `test-results/`, и в тот же час его нарушила. Разбор показал, что дело не
в невнимательности: греп шёл по тегам ЗАДАЧИ, а урок привязан к ДЕЙСТВИЮ, и пересечение пусто по
построению. **Ловушка убрана в корне:** `playwright.config.ts` → `outputDir: 'test-results/e2e'`,
доказано меткой (файл рядом с кадрами пережил прогон). В канон записана иерархия: устранить
ловушку → страж → и только потом запись.

**📤 Два сигнала в KAIF, заведены агентом БЕЗ участия человека** (новое правило владельца: «все
баги, которые ИИ агенты находят при работе с KAIF — без участия человека рапортуются агентом в
KAIF github»): **issue #14** (механизация урока оплачивается вторым ожогом; замер по трём
проектам: 281 запись, механизировано 7 = 2,5 %, в одном проекте 0 из 60) и **issue #15** (контур
обратной связи путает ТРАНСПОРТ с АВТОРСТВОМ и ставит человека в петлю). Гейт отправки для KAIF
снят по слову владельца, для чужих репозиториев остаётся fail-closed. Подпись агента в теле
тикета объявлена КАНОНОМ.

### Перенесено из STATUS дословно при стрижке сессии 10 (2026-08-14)

> Ниже — записи, перестававшие быть «сейчас». Указатели на них остались в STATUS.

**⛔ ЧТО ЖДЁТ ВЛАДЕЛЬЦА — полный список на 30.07** (жил в STATUS до 2026-08-14; поглощён очередью
владельца `plans/49`, 63 позиции, но переехал ДОСЛОВНО, чтобы ни один пункт не потерялся):

0. ✅ `interviews/interview_008` — ОТВЕТЫ ПОЛУЧЕНЫ 2026-08-01 (включая В11), разнесены. Вопрос
   «чем отличать пароль от ссылки» снят вместе с паролями. От эпика за владельцем осталось три
   вещи, все — руки человека: 🔧 погасить пароли у 331 человека в бою
   (`deleteAttribute: ["PASSWORD"]`) — необратимо, `[NOT-TESTED]` · 🔧 приёмка фазы 5 в бою
   (смена почты под EEP: письмо на новый адрес, письмо-возврат на старый, занятый адрес; на
   стенде не проверяется по построению) · 🔧 приёмка фазы 6 на живом Safari (отложена им же, В9).
1. Выкат в бой — вся работа 29–30 июля. Плюс пересборка образа сервера.
2. `bugs/74` часть А, контракт кэша — гасить ли собранный экран «Измерений» своей же оценкой
   (вопросы 1 и 2). Единственное, что мешает памяти вида работать ПОСЛЕ оценки.
3. `bugs/80` подпункты (а) звёзды и (в) адаптивный `clamp()` — вопросы 1 и 3, замер шага 0 не снят.
4. `bugs/84`: текст ошибки для ссылки БЕЗ намерения и расширенная фраза врезки (`[AI]`).
5. `plans/08` вопросы Б, В, Г — агент взял их по своей рекомендации, нужна вычитка.
6. `bugs/83` / PWA — обзор готов (`researches/19` §5): что установлено у него на телефоне, один
   манифест или два (RU/EN), нужна ли нативная обёртка при изъятом ключе подписи (`bugs/02`).
7. ✅ `bugs/04` шаг 4 — сделан 2026-08-01.
8. `bugs/46`: «Был онлайн» — новое публичное поле, родня `ideas/23` / `plans/09`.
9. `bugs/62` — кнопка «Установить приложение» и service worker: цена SW названа в
   `researches/19` §5.
10. Мелочь: строка «В Пространстве с …» осталась в «Меню» — спросить.

**Фаза 2 эпика 40, подробности шагов 2–4** (сводка осталась в STATUS указателем):
шаг 2 — форма V3 «Рейтинг» выбрана владельцем (№030 В1 = В), макеты `design/catalog-hub-mockups.html` ·
шаг 3 — 180 страниц (индекс + 89 страниц семи хабов) × 2 языка, недостижимых от `/ru` и `/en` **0**
(было 10 222), самая дальняя карточка 3 клика, прибор `tools/measure-link-graph.mjs`,
[TESTED: 263/263 unit · e2e 100 · verify-dimension-pages 27/0 · тесты порядка доказаны 5 мутациями] ·
шаг 4 — ссылка «вверх» + блок «Похожие по каталогу» (8 соседей по КЛАССИФИКАЦИИ, №025 В7 = А, без
единого числа похожести), минимум входящих у карточки стал **3** (было 1),
[TESTED: 278/278 unit · e2e 100 · verify-dimension-pages 30/0].
Три вещи, найденные ТОЛЬКО на боевых данных, — не переоткрывать: вес общего тега = обратная
частота (иначе четыре тега вида делают всех равными и соседи выходят по алфавиту) · десятилетие
усиливает признак, но само признаком не является · **764 объекта из 5111 не выбраны соседом ни
разу** — их возвращает проход `returnLostNeighbours`, ценой списков в 9–11 у части карточек.

**Интервью №030 В2 = Д, подробности формулы:** `weighted = (v·R + m·C)/(v + m)`, оба числа опоры
посчитаны: `C = 8,2405` (средняя по 1877 оценённым), `m = 4` (90-й процентиль числа голосов).
Наружу идёт настоящая средняя — взвешенная участвует ТОЛЬКО в сортировке (условие владельца).
🔴 Поправка, которую не надо «чинить» обратно: иллюстрация агента при постановке варианта Д
(«9,3 от одиннадцати обгонит десятку от четверых») боевыми числами НЕ воспроизводится ни при каком
весе опоры. Настоящий эффект мягче и в ту же сторону; жёстче — только словом владельца через `m`.

---

## 2026-08-14 (вечер) — перенесено из STATUS при завершении девятой сессии

> Стрижка бонсая `/end-chat`: записи ниже перестали быть «сейчас» и переехали ДОСЛОВНО.

### Фаза 2 эпика 40 «Архитектура веса» — запись сессии 8, переехала 2026-08-14 (сессия 10)

🔵 **ВЗЯТА ФАЗА 2 ЭПИКА 40 «АРХИТЕКТУРА ВЕСА»** (`plans/48`) — работа, у которой цена
промедления календарная: страница набирает вес месяцами, а теряет за неделю. За сессию:
**шаг 0** — замер поля «вид» (грязных 74 из 5111, главный класс — невидимый `U+200B` — 44) ·
**шаг 1** — `dim-kind.ts`: нормализация вида НА ЧТЕНИИ, чем СНЯТ блокер фазы (метаплан ждал
боевой прополки; без нормализации вышло бы два одинаковых хаба — «TV Series» 329 и «TV Series​»
41), 5 тестов, **доказан 3 мутациями** · **шаг 2** — 4 макета хаба показаны, владелец выбрал
**V3 «Рейтинг»** (интервью №030, В1 = В) и произнёс ЗАКОН о названиях · **шаг 5** — JSON-LD
каталога (Movie/Book/VideoGame/TVSeries/MusicGroup/CreativeWork + AggregateRating по общему
правилу показа) и `Organization`+`WebSite` на лендинге — давний долг `researches/26` §7.6.
[TESTED: 250/250 unit · сборка зелёная · разметка проверена в СОБРАННОМ HTML: у «Матрицы»
9,3 из 11 голосов с границами 0…10, у страницы без голосов рейтинга нет, `inLanguage` не
поставлен].
> Беклог с этого дня живёт одним документом — `plans/49`.

## ЗАКРЫТЫЕ ЧАТЫ 2026-08-14 (сессии 2–7) — записи перенесены в `PROJECT_HISTORY.md`
Коротко, что действует: `plans/39` и релиз 2.1 в бою · семейство ТЕСТ в бою (8 страниц,
`plans/42`; хвост — такт Г) · приборы фазы 1 (`plans/41`) · дом админ-панели (`plans/33`) ·
пул слабой модели `plans/45` · капкан «hosting-эмулятор не применяет redirects» жив (`EXP-0144`).

### 3. ✅ Фаза 5 `ideas/30` — В БОЮ. Что осталось ПОСЛЕ неё

· 🔴 **вычитка владельцем английской ОБВЯЗКИ страницы** (кнопки, блок-дверь, подпись) — его
  собственное условие при ответе В2 = А. Описания — его текст, к ним вопросов нет.
  Файл: `src/routes/[lang=lang]/dimension/[slug]/+page.svelte`, объект `T`;
· 🔴 **регламент выката пополнить чисткой старых релизов:** **194 МБ на релиз** ⇒ ~52 релиза из
  бесплатных 10 ГБ. Шаг 0 обещал 126 при 79 МБ — ошибался вчетверо;
· ✅ закрытые хвосты фазы (домашка-консоли · число «до» = 1 страница · `bugs/111` разрыв 0) —
  перенесены в `PROJECT_HISTORY.md`; действующие числа — `researches/34` §3;
· ⚠️ прополка поля «вид» — работа `plans/30` (`фильм` строчной 21, `Movie` 3, год в поле вида,
  и **невидимый U+200B**). ⛔ Теперь предусловие хабов фазы 2 `plans/40`.

🔑 Не переоткрывать: рельса на публичной странице НЕТ сознательно · блока соседства нет по решению
владельца · полный каталог в git не кладётся (`node tools/fetch-dims-slice.mjs --all` — шаг выката).

### 4. ✅ Поверхность «ТЕСТ» вписана 2026-08-14 — фаза 5 `plans/40` (старт после приборов, В5 = А)

### 📋 ОСТАЛЬНОЙ БЕКЛОГ (главное — в «ЭСТАФЕТЕ» наверху файла)

> ⚠️ Порядок работ задаёт **эстафета в шапке**. Здесь — то, что было актуально раньше и остаётся
> открытым. Не начинай отсюда, не прочитав шапку.
>
> 🔴 **Ловушка, стоившая бы дорого, если её забыть:** соблазн прочитать чужие оценки ключом
> сервисного аккаунта (`calculator/secrets/sa.json` лежит локально, Admin SDK правила обходит).
> **Нельзя.** `firestore.rules:219` — «АДМИН — НЕ ИСКЛЮЧЕНИЕ», запрет восстановлен аудитом
> `bugs/100`. Быстрее — не значит можно. (Замер каталога это подтвердил на практике: нужные числа
> нашлись в ПУБЛИЧНОМ документе `dims/{dimId}`, лезть в приватное не понадобилось вовсе.)

**1. 🔵 ЭПИК `ideas/29` «Admin Panel и пополнение каталога», ФАЗА 3.**
   Всё готово к исполнению, вопросов владельцу нет ни одного.
   · Карта эпика — **`plans/30`** (7 фаз, ворота, 11 критериев приёмки). Читать её ПЕРВОЙ.
   · Доказательная база — `researches/29` (обзор индустрии + разведка кода 2.0 и 1.x).
   · **Операционный план фазы 3 — `plans/33`**, не начат. Дом панели: адрес, оболочка, доступ.
   🔴 Единственная ловушка фазы названа в плане: у прав **ТРИ состояния, а не два**. «Ещё не знаю»
   это не «не админ», иначе наивный редирект выбросит из панели самого владельца, пока едет claim.
   · Утверждённый макет — **V1 «Стопка»** (`plans/32`, `design/dims-manager-mockups.html`).
   ⚠️ Вместе с V1 взята его названная цена: пачка одинаковых карточек усыпляет внимание. Лечение —
   механизмом, а не бдительностью; что именно — в `plans/32` и в фазе 6 метаплана.

**2. 🔧 Хвост фазы 1, который ждёт РУК ВЛАДЕЛЬЦА** (агент не выкатывает):
   снимки дней до 2026-08-02 несут завышенное `dims: 5112`. Их правит разовый инструмент, ещё не
   написанный, — решение владельца интервью №014, В2 = В: **переписать И оставить след правки**.
   Инструмент пишет в БОЙ, запускается только с его слова (`bugs/106` шаг 3).

**3. 📝 Тексты ворот `plans/29` — ВТОРОЙ ЗАХОД.** Первый состоялся, вердикт «принять с правками»,
   разбор — `plans/29` §3а. Что переделывать: подзаголовок 1.x возвращается целиком · термины на
   витрине раскрывать, а не бросать голыми · полоса чисел становится БЛОКОМ с зелёным статусом ·
   якорь меняет слово на «точный, честный и быстрый беспристрастный суперкомпьютер» (из четырёх
   эпитетов под замером нет только «быстрый») · английский — В3=А, перевод мой, вычитка его.
   🔴 **Переписывать ТОЛЬКО по `AUTHOR_STYLOMETRY.md`.**

**4. 🎙 `AUTHOR_STYLOMETRY.md` — портрет голоса владельца, ✅ ПРИНЯТОЕ ЯДРО.** С 2026-08-14 файл —
   байт-в-байт копия публичного слепка ядра `krinik-stylometry` **1.1** (статус `accepted`,
   решение владельца №63 от 2026-08-08 22:05 +03:00; 151 правило, анти-портрет АП1–АП37, чек-лист
   §7). Истина — приватное ядро `d:\work\krinik_voice`; слепок собирает KAIF
   (`tools/stylometry-snapshot.mjs`), наш файл = его копия с ndim-преамбулой (пара в реестре
   зеркал `AGENT_GUIDE.md`). Прежний локальный черновик (слепой тест №017, 7/8 по содержанию) —
   в истории git. Реестр корпуса, подтверждённый владельцем (интервью №016, В1 · интервью №016,
   В2 · интервью №016, В3 · интервью №016, В4), впитан ПРИВАТНЫМ ядром — его таблица корпусов и
   есть ответ; локальной копии реестра больше нет вместе с черновиком. 🔴 Шапка документа
   обязательна к прочтению перед любой строкой текста для людей.

**5. Прочий беклог** — раздел «Пул автономных задач» ниже. Ближайшее автономное:
   `bugs/104` (раскрытие карточки связи по всему верху), `ideas/25`, `ideas/26`, `ideas/27`.

**6. ⛔ Что ждёт владельца** — раздел ниже; ничего из этого агент не решает.

---

## 🤖 Пул автономных задач (без человека и без особого железа)

> Задачи, которые агент может делать ПОЛНОСТЬЮ автономно: код → сборка → стенд → фикс → коммит.
> Цикловые скиллы (`/autoloop`, `/dayloop`, `/nightloop`, `/guarded-loop`) мелют этот пул.
>
> 🆕 **Пул для СЛАБОЙ модели-исполнителя (Opus) — `plans/45`** (заказ владельца 2026-08-14):
> реестр заданий со строгими пошаговыми планами (`plans/44` — фаза 4 админ-эпика,
> `plans/46` — ярус 2 графики). Слабой модели — только задания из реестра; правила формата
> и жёсткая граница «чего ей не дают» — в самом `plans/45`.

> 🗂 Стрижка 2026-08-14: закрытые пункты (bugs/104 · ideas/25 · ideas/26 · макеты и дом панели
> ideas/29 · замер ideas/30) сняты — правда живёт в их документах и летописи. Урок о гниении
> пула — `EXP-0143`: перед пунктом пула проверяй статус документа и код.

- [ ] 🖼 **`ideas/27` — три яруса готовности графики. Шаги 0–2 ✅ 2026-08-14** (инвентарь; канон
      трёх ярусов в `AGENT_GUIDE`; **шаг 2 закрыт ЗАМЕРОМ с решением «прелоад не ставить»** —
      демо ниже фолда, первый вьюпорт — инлайн-SVG; ✅ посылка «лендинг держит 100» СНОВА ВЕРНА:
      `bugs/119` закрыт 2026-08-14, бой `/ru` Perf **100** × 2). Осталось: шаг 3 — ярус 2
      (фоновая догрузка; кандидаты — полные персоны 3×1,2 МБ И кропы лиц; нужен обзор индустрии
      по канону) · шаг 4 — «объект после картинки» с потолком · шаг 5 — расширить
      `verify-bug69`, мутации.
- [ ] 🔝 **`ideas/28` — ЭПИК владельца: вверху ленты всегда 10 популярных измерений.** Слово
      владельца 2026-08-01, основание — `plans/28`; шаг 0 снят: боевой `dims_list` **566 КБ**
      из 1 МиБ, добавка счётчика ~+41 КБ влезает. Плана ещё нет — писать при заходе
      (`/plan-task`). ⚠️ Инвариант, который нельзя «упростить»: 10 штук, СЛУЧАЙНЫЕ внутри пула
      популярных, остальная лента остаётся честно случайной — иначе хвост каталога не получит
      оценок никогда («бред!!!» об алфавитном списке).
- [ ] **Эпик `plans/15`, фаза 6 — АВАТАР: загрузить · заменить · удалить.** Разблокирован
      2026-08-01 (правила Storage в бою, CORS проверен), но по ценности теперь ПОСЛЕ ворот:
      фотография украшает продукт, о котором никто не знает.
      Операционный план ещё не написан — он третья ступень лестницы, пишется сейчас (`plans/13`
      поглощён фазой). Готовые решения не переоткрывать: `uploadBytes` (не resumable — `PUT` в
      CORS нет) · на Safari писать **JPEG** под именем `avatar.webp` (WebP оттуда не кодируется) ·
      однопальцевая альтернатива перетаскиванию обязательна (WCAG 2.5.7 AA) · инвалидация кэша
      лиц ПО UID (`avatar.ts:35` этого ждёт).
- [ ] **`ideas/24`, фаза 3 — подсистема настроек человека (`ideas/16`).** Автономна до выката
      правил Firestore: поле настроек в документе человека, правила, тесты правил, дебаунс.
      ⚠️ Внутри висит вопрос владельцу (что главнее при расхождении — браузер или профиль) и
      выкат правил требует его слова. Сначала написать `plans/14`.
- Остальной беклог ждёт владельца или железа (эстафета, п. 5, и раздел «⛔ ЧТО ЖДЁТ ВЛАДЕЛЬЦА»
  выше): тиры `plans/07` (PWA — нужен телефон владельца, `bugs/58`/`83`; `bugs/44`/`45` —
  макеты/решения) · `bugs/74` часть А — контракт кэша (вопрос владельцу).

---

## 🗂 Перенесено из STATUS.md ДОСЛОВНО — стрижка бонсая 2026-08-14 (вечер, /end-chat сессии 8)

> Контекст переноса: сессия 8 (вечер, фаза 2 эпика 40 «Архитектура веса») закрывается с передачей
> эстафеты. Работа сессии 7 («ТЕСТ» выкачен в бой) полностью закрыта и перестала быть «сейчас»;
> сводки сессий 2–5 уже были однострочными указателями на эту летопись. Переносится дословно.

**Была шапкой STATUS.md (сессия 7):**

> **Последнее обновление:** 2026-08-14 вечер (седьмая сессия закрыта `/end-chat`; дальше
> владелец работает на слабом Opus — задания СТРОГО из реестра `plans/45`) — 🚀 **Фаза 5
> «ТЕСТ» ПОСТРОЕНА И ВЫКАЧЕНА В БОЙ ЗА ОДИН ДЕНЬ.** За сессию: **такт Б** — живой движок
> (жест звезды по канону «Измерений»; первая оценка молча рождает гостя — №009 В3 дословно;
> очередь НА СБОРКЕ, `test-set.ts`, ноль чтений Firestore до первой оценки; Firebase в бандл
> не попал — `test-engine.ts`, динамические импорты) · **такт В** — пара: документ
> `testPairs/{id}` (правила валидируют map целиком `values().hasOnly(0…10)`), личная ссылка
> `?pair=` (noindex, `list` запрещён), в общий результат — только оценки из набора кнопкой
> (№002 В4), результат фактами без единого процента (страж доказан мутацией) · **выкат**
> ≈16:54 (AUTH «Выкат»; тексты приняты владельцем без личной вычитки — сверка со
> стилометрией/философией проведена, журнал `MASTER_PLAN`): `firebase deploy --only
> firestore:rules,hosting`, полный каталог 10 222 + 8 страниц семейства, боевой sitemap 8/8,
> `verify-prod-dimension-pages` 44/0. [TESTED: 245/245 unit · правила 118/118 · стенд: гость
> 14/14, пара 15/15 · бой: страницы 200 без noindex]. 🏠 **Владельцу — `homeworks/10`**:
> живая приёмка теста и пары в бою (по желанию — переподать sitemap в GSC).

**Были однострочными сводками закрытых чатов:**

> ## ЧАТ 2026-08-14 (сессия 5, день) — ✅ закрыт
> Коротко, что действует: `plans/39` в бою (301 живьём, query сохраняется) · релиз 2.1 в бою ·
> №026 = Г (жанры — обёртки поверх движка) · пул Opus `plans/45` (+44, 46) · DONE: bugs
> 108/109/110/118/119, plans 31/33/41/43.
>
> ## ЧАТЫ 2026-08-14 (сессии 3 ночь и 4) — ✅ закрыты
> Коротко, что действует: приборы фазы 1 (`plans/41`) · дом админ-панели (`plans/33`) · языковые
> адреса построены (сессия 4) и выкачены (сессия 5) · капкан «hosting-эмулятор не применяет
> redirects» жив (`EXP-0144`).
>
> ## ЧАТ 2026-08-14 (сессия 2, планирование эпика 40) — ✅ закрыт
> Коротко, что действует: `researches/34` (вердикты по Гроку) · `plans/40_EPIC` (8 фаз) ·
> `plans/41` (фаза 1 — закрыта сессией 3) · честные сроки органики: первые НЧ-позиции — зима 2026/27.

---

## 🗂 Перенесено из STATUS.md ДОСЛОВНО — стрижка бонсая 2026-08-14 (вечер, /end-chat сессии 6)

> Контекст переноса: сессия 6 (день, «ТЕСТ»: №028 → №029 → такт А) закрывается с передачей
> в новый чат; запись СЕССИИ 5 перестала быть «сейчас» и переезжает сюда.

## 🆕 ЧАТ 2026-08-14 (сессия 5, день) — два выката, релиз 2.1, №026 закрыто

· **Выкат `plans/39`** (AUTH владельца в чате) + боевые стражи 16/0 · 44/0 · 81/0; 📊 query
  при 301 сохраняется. · **Интервью №026**: шейпинг проработан → В2 → ответ **Г**: все жанры
  теста — обёртки поверх движка (разнос: `plans/42` шаг 1а — следующий шаг фазы ТЕСТ).
· **Релиз 2.1** полным циклом за день: `plans/43` → черновик → вычитка №027 (В1 = А) → деплой →
  бой подтверждён живым Chromium («2.1 (498)», обе темы). Страж `verify-wave12` теперь читает
  версию из package.json. · **`bugs/119` закрыт**: бой `/ru` Perf **100** × 2 (урок `EXP-0146`:
  лаунчер Lighthouse сломан — свой Chrome + `--port`).
· Заведены: `ideas/35` (NDim ID — собственность, экспорт/импорт JSON) · `plans/44` (фаза 4
  админ-эпика, строгая форма) · `plans/45` (пул для слабой модели Opus) · `plans/46` (ярус 2
  графики, строгая форма) · `researches/36` (обзор индустрии фоновой догрузки).
· Груминг: `bugs/118` починен (e2e 6/6; в main, НЕ в бою — уедет следующим деплоем);
  DONE: bugs 108/109/110/118/119, plans 31/33/41/43.


## 🗂 Перенесено из STATUS.md ДОСЛОВНО — стрижка бонсая 2026-08-14 (утро, /end-chat сессии 3)

> Контекст переноса: сессия 3 (ночная, автономная) закрыла фазу 1 «Приборы» и отработала
> автолуп; при её закрытии запись СЕССИИ 2 перестала быть «сейчас» и переезжает сюда.

## 🆕 ЧАТ 2026-08-14 (сессия 2) — переосмысление SEO по `ideas/34`

· Лестница `/plan-epic` пройдена целиком за сессию: **`researches/34`** (5 веб-агентов по
  первоисточникам + повёрстные вердикты 10 тезисов Грока: Т6-сайтмап опровергнут, Т1/Т4/Т5/Т7/Т8
  подтверждены; проверка описаний — 0 дословных заимствований из 20 фраз) → **`plans/40_EPIC`**
  (8 фаз, поглотил непройденные фазы `plans/34` — там ревизионная шапка) → **`plans/41`**
  (операционный план фазы 1 «Приборы»).
· 🔴 Жёсткие находки: **все 10 222 страницы каталога — сироты** (0 внутренних входящих ссылок,
  дверь только sitemap) · покрытие индекса НЕИЗВЕСТНО (точка «до»: 1 страница) · 63 % страниц без
  рейтинга · по букве политик Google страницы легальны, риск — взвешенный site-wide профиль.
· Сроки органики честно: 1,74 % новых страниц достигают топ-10 за год; первые НЧ-позиции — зима
  2026/27. Крупнейшая поверхность — ТЕСТ — вписана фазой 5 `plans/40` (снят долг п. 4 эстафеты).

## 🗂 Перенесено из STATUS.md ДОСЛОВНО — стрижка бонсая 2026-08-14 (вечер, сессия 2)

> Контекст переноса: вторая сессия дня — эпик «Семантическое ядро 2.0» (`ideas/34` →
> `researches/34` → `plans/40_EPIC` → `plans/41`; интервью №025 закрыто 7/7 в день создания).
> Ниже — записи STATUS, переставшие быть «сейчас».

## 🆕 ЧАТ 2026-08-14 — что сделано (обновление KAIF 2.1 → 2.2)

· Bootstrap-маршрут с базой сравнения бандлов и песочницей — бой совпал с прогнозом файл-в-файл;
  55 модулей сшито переводом в 23 файлах, кириллицы потеряно 0, удвоений 0, тесты **232/232**.
· **Хуки `.kaif/hooks/` ПОДКЛЮЧЕНЫ** (слово владельца в чате) — `.claude/settings.json`, дымовая
  проверка пройдена. ⚠️ Фрагмент поставки нёс недокументированные поля `command`+`args` — подключено
  строкой, расхождение ушло в исток.
· **`AUTHOR_STYLOMETRY.md`**: `git mv` с OWNER_VOICE.md + актуализация до принятого ядра
  `krinik-stylometry` **1.1** (байт-в-байт слепок KAIF, пара в реестре зеркал, ndim вписан
  потребителем в version.json хранилища голоса).
· Полевой отчёт: `reports/KAIF_UPDATES/NDIM_SPACE_KAIF_2.2_UPDATE_REPORT.md` + **issue #13**
  истока. Урок — `EXP-0140`. Коммиты: `c4fd2a6` (обновление), `f26d38d` (STATUS), далее стрижка.

### Из эстафеты §2 «Фаза 5 ideas/30» — закрытые пункты

· ✅ **домашка  — две трети сделаны агентом 2026-08-03** в авторизованной сессии
  владельца: Google принял карту («Файл Sitemap отправлен», 3 авг.), Bing принял (Processing).
  ✅ **Яндекс тоже сделан** по прямому указанию владельца (счётчик 10 → 9). Иконка опознана по радиусу дуг SVG: крестик 0,75, стрелка 6,5. Раньше стояло «БЕЗ подписи вплотную к ✕ «удалить
  карту»; отличить программно нечем, цена ошибки несопоставима ();
· 🔴 **ЧИСЛО «ДО» ПОЛУЧЕНО И ОНО ОТРЕЗВЛЯЕТ: в индексе Google была РОВНО 1 страница.** Все три
  консоли держали чтение карты месячной давности, когда в ней было 1–2 адреса. Это и есть
  точка отсчёта для ;
· 🚀 **`bugs/111` ПОЧИНЕН И ВЫКАЧЕН В БОЙ 2026-08-03** (образ билд 26). Причина: поля
  `stars`/`rates`/`rating` каталога в 2.0 не писал никто — механизм 1.x не перенесли. Владелец
  выбрал чинить механизм, а не снимать число со страниц. Сводка считается тем же проходом, что
  `space/stats` (это и шаг 0 `ideas/28`). `npm run test:calc` **67/67**, три решения доказаны
  мутациями. Бой проверен прибором `node tools/verify-prod-dim-ratings.mjs` — **7/0**:
  🎯 **разрыв РОВНО НОЛЬ** (4064 = 4064; было 31 и росло). ✅ **Сироты вычищены по слову
  владельца:** 15 мёртвых измерений, **25 оценок у 14 человек** удалены механизмом
  `cleanupOrphanRatings` — поиск стоит **0 чтений**, работает только в полном проходе, два
  предохранителя против «каталог прочитался пусто ⇒ мёртвым стало всё», оба доказаны мутациями.
  Топов пересчитано 89, связей 2346 → 2344: математика поправилась там, где несла мёртвые оси;

### Из эстафеты §3 — поверхность «ТЕСТ» (закрыто 2026-08-14: вписана фазой 5 `plans/40`)

### 3. 🆕 Вписать поверхность «ТЕСТ» в метаплан `plans/34`

Крупнейшая достижимая поверхность по замеру, и её в плане до сих пор нет. Дальше — **четыре
макета** (правило четырёх макетов). Наше отличие от 16personalities — честный инструмент против
эффекта Барнума.

### Из «ЧТО ЖДЁТ ВЛАДЕЛЬЦА» — закрытый пункт

7. ~~**`bugs/04` шаг 4** — выкат правил Storage~~ ✅ **СДЕЛАНО 2026-08-01.** «Взгляд в консоль»
   оказался чтением бакета через `gcloud storage ls`: по путям было не пусто, шесть файлов 1.x
   забэкаплены, владелец сказал «отвязываемся» — и выкат перестал быть необратимым.

---

## 🗂 Перенесено из STATUS.md ДОСЛОВНО — стрижка бонсая 2026-08-14, вечер (сессия 5)

> Записи сессий 3 и 4 перестали быть «сейчас»: выкат `plans/39` состоялся в сессии 5, релиз 2.1
> в бою, интервью №026 закрыто. Действующее — в STATUS-шапке сессии 5 и документах.

## 🆕 ЧАТ 2026-08-14 (сессия 4) — языковые адреса всему сайту (`plans/39` шаги 2–5)

· **Переезд:** `git mv` лендинга, `delete-account` и 6 маршрутов «Меню» под
  `src/routes/[lang=lang]/`; entries из общего `langEntries()`; 🔴 язык на переехавших
  страницах — ИЗ `params.lang` (модуль при пререндере отвечал бы «ru» всем, `/en` запёкся бы
  русским). Мост «адрес → память» — ОДНИМ местом, `[lang=lang]/+layout.svelte` ($effect →
  `adoptLangFromPath`; до этого мост был написан, но его никто не звал).
· **Корень `/`** — распознаватель: `csr=false`, noindex, порядок ПИСЬМО (oobCode → `/profile`
  с query — боевой контракт 12.07) → ПАМЯТЬ → БРАУЗЕР; вторая узаконенная до-JS копия чтения
  `ndim-lang` (страж позиционный + 2 мутации). Переключатели ведут на АДРЕС (лендинг —
  ссылки, шапка — `goto(swapLangInPath)`); ссылки личного «Меню» → `/{lang}/menu/…`.
· **Стражи:** 🆕 `verify-lang-addresses` (74, по build + firebase.json, 4 мутации) ·
  `verify-lang-module` вырос 17 → 26 · 🆕 `verify-prod-lang-redirects` (16, по бою; дым на
  до-выкатном бою красен ровно там, где надо) · 🆕 приёмочный `accept-lang-addresses`
  (живой Chromium 14/14 + 10 кадров, смотрены глазами). 13 старых стражей tools/* обновлены.
· 🔴 **Капкан, оплаченный сессией: hosting-эмулятор firebase-tools 15.23 МОЛЧА не применяет
  redirects** (минимальный конфиг из документации: запрос в логе есть, ответ 404; контрольный
  образец — боевые 301 цели `app` — в эмуляторе тоже мёртв). Живая проверка 301 — только бой.
· Прочее: юниты 232 → **234** (swapLangInPath) · e2e **100 passed** (lang-default прошёл
  сквозь распознаватель без правок) · старые письма на голый `/delete-account`: судьба query
  при 301 неизвестна платформе заранее — замер вписан в прод-страж.

## ЧАТ 2026-08-14 (сессия 3, ночь) — фаза 1 «Приборы» закрыта

· Приборы построены и прогнаны: `tools/measure-link-graph.mjs` (граф ссылок по `build/`;
  контроль доказан мутацией) · `tools/console-coverage.mjs` (GSC/Bing/Яндекс, ТОЛЬКО чтение) ·
  `tools/grep-en-boilerplate.mjs` (279 записей со справочным EN-стилем) · жнецы спроса получили
  `--file` (84 RU + 84 EN собраны, контрольная строка сошлась 4 430 ≈ 4 206).
· 🔴 Уточнение разведки: строгих сирот в каталоге 0 (двойники ru↔en ссылаются друг на друга),
  но **все 10 222 страницы недостижимы от корня** — дверь только sitemap (§14).
· 🔴 Первый живой сигнал риска §5.4: **Яндекс удалил 641 `/en/dimension/*` как «малоценные»**
  (6–12.08). Google наоборот: 1 → 994, органика началась (8 кликов, запросы каталожные).
· Вердикты о судьбе каталога НЕ выносились — критерии записаны до данных (`75b02f7`), решает
  фаза 8 после ≥3 замеров.
· 🆕 **/autoloop той же ночью: ФАЗА 3 эпика `ideas/29` ЗАКРЫТА — дом админ-панели** (`plans/33`):
  `/admin` (noindex, вне sitemap, пререндер пуст по данным) · права ТРЁХ состояний в одной точке
  `src/lib/data/admin.ts` (`unknown` ждёт, не приговаривает) · дверь в «Меню» только админу ·
  страж `tools/verify-admin-home.mjs` 17/17, 2 мутации · 232 юнита, правила, e2e 100 passed ·
  судья VERIFIED. Следующая комната эпика — фаза 4 (план пишется при заходе).
· Остальные итерации автолупа: **`bugs/104` закрыт** (карточка связи раскрывается всем верхом,
  цель 82 % → 100 %; портрет чужого — из раскрытой) · **`ideas/26` дозакрыт** (год-прочерк в
  раскрытой карточке `/dims`) · **фаза 5 «ТЕСТ» начата** (`plans/42` + `researches/35`; интервью
  №026 ждёт) · **`ideas/27` шаги 0–1** (инвентарь графики; канон трёх ярусов в `AGENT_GUIDE`;
  находка: полноразмерные персоны лендинга 1,2 МБ — единственный кандидат яруса 2).
· 🔄 **Шейпинг видения владельца (09:06, к №026, НЕ ответ развилки):** NDim ID со временем —
  «постоянная динамически живущая и обновляющаяся анкета, паспорт личности». Дословно — журнал
  `MASTER_PLAN`; поправка рамки — `researches/35` §3 (запрет на Барнум, не на паспорт).
· Ревизия пула: ЧЕТЫРЕ протухших пункта сняты (`EXP-0143`); уроки ночи — `EXP-0142`, `EXP-0143`.

## 🗂 Перенесено из STATUS.md ДОСЛОВНО — стрижка бонсая 2026-08-14 (утро)

> Записи перестали быть «сейчас»: отчёт чата 2026-08-03 (фаза 5 ideas/30 в бою, plans/39 шаг 1,
> контур разноса ответов), блок закрытых интервью №014–№022 с замером каталога, устаревший
> раздел «первым делом» от 30.07 и закрытые пункты пула автономных задач. Решения живут в
> plans/34, plans/36, plans/39, researches/30 и самих интервью; здесь — первоисточник хода событий.

## 🆕 ЧАТ 2026-08-03 — что сделано

**Фаза 5 эпика `ideas/30` (публичные страницы измерений), план `plans/36`:**

🚀 **ФАЗА ЗАКРЫТА И ВЫКАЧЕНА В БОЙ 2026-08-03** (по прямому распоряжению владельца
«перепроверь, затем деплой»). Выкат: `firebase deploy --only hosting`, **20 488 файлов**, обе цели
хостинга. Правила Firestore и Storage сознательно НЕ трогались.

✅ **Бой проверен прибором, а не доверием:** `node tools/verify-prod-dimension-pages.mjs` →
**42 проверки, 0 провалов** на живом `ndimspace.app` (оба языка × три границы правила оценок ·
описания в сыром HTML · карта сайта 10 222 адреса каталога · лендинг цел · личные экраны
по-прежнему под `noindex`).

🔁 **Откат, если понадобится:** Firebase Hosting хранит предыдущие релизы — консоль проекта →
Hosting → предыдущая версия → «Откатить». Кода это не касается.

| Что | Замер |
|---|---|
| Страниц | **10 222** = 5111 × 2 языка (`/ru/…`, `/en/…`) |
| Сборка | **~50 с** · **194 МБ** (после двух облегчений за день) |
| Клиентский бандл | **1,3 МБ** |
| Карта сайта | **10 233 адреса**, 0,83 МБ |
| Стражи | `verify-dimension-pages` **24/0** (7 мутаций) · по бою **42/0** · `e2e` **98** (было 80) · `npm test` **230** · `npm run check` **0/0** · `verify-lang-module` **17/0** |
| Приёмка | 32 кадра живым Chrome: обе темы × две ширины × 4 граничных случая |

🎯 **Главный приз эпика взят: английский ЕСТЬ в сыром HTML.** До фазы его не было ни знака
(`researches/26` §1.2) — теперь 5111 английских страниц с описаниями, `<html lang="en">`,
двусторонним `hreflang` и `x-default` на английский.

🔴 **Оценка хранилища была неверна вчетверо:** шаг 0 обещал ~126 релизов при 79 МБ. Правда — сначала
**322 МБ**, после двух облегчений **194 МБ** ⇒ **~52 релиза** из бесплатных 10 ГБ. Чистка старых
релизов переезжает в регламент выката.

⚡ **Страница каталога не грузит ни байта JavaScript** (): 25,9 → **16,2 КБ** по бою.
🔴 И здесь прогноз агента был неверен ДВАЖДЫ:  от  не исчезают вовсе
(выключателя у SvelteKit нет), а раздуты они были моим же загрузчиком — он отдавал весь
двуязычный объект каталога. Удалять их шагом сборки НЕЛЬЗЯ: клиентский переход сюда получит 404.

🔴 **Дефект, найденный и вылеченный внутри фазы — `EXP-0136`:** универсальный `+page.ts` вшил
**весь каталог (16,86 МБ) в клиентский бандл**. При этом 24 проверки стража были ЗЕЛЁНЫМИ —
дефект нашёл страж ЖАРГОНА по случайному совпадению слов. Отсюда новые проверки на ВЕС сборки.

| Шаг | Состояние | Доказательство |
|---|---|---|
| 1. Адреса-слаги | ✅ | `dim-slug.ts` + 13 тестов, **3 мутации убиты** |
| 2. Канал «каталог → сборка» | ✅ | срез в git / полный вне git; два прогона → **побайтово тот же файл** |
| 3. Страница **V2 «Досье»** | ✅ | интервью №023: **В1 = Г** (рекомендация агента была другой), **В2 = А** |
| 4. Карта сайта | ✅ | 10 233 адреса, `canonical` на каждой |
| 5. Стражи | ✅ | 24 проверки, 7 мутаций |
| 6. Приёмка живым Chrome | ✅ | 32 кадра, `test-results/dimension-pages/` |

🔴 **Дважды за сессию мутация ВЫЖИВАЛА**, и оба раза по одной причине: граничный случай был взят
наугад, а не сконструирован (разрез слага не попадал на пробел; `Infinity * 0` — это NaN, а
защиту отличает только `Infinity`). **Граничный случай надо строить, а не брать пример.**

**Дыра в KAIF — найдена, замерена, залечена, отправлена наверх** (заказ владельца):
- **Симптом:** ответ владельца Р5 интервью №010 (2026-08-01) два дня не доезжал до `plans/24`;
  тот держал вопрос открытым И риск «флип корня», которого при этом ответе нет. Свежая сессия
  собиралась спросить заново — остановил владелец: «*странно, что это не записано!*»
- **Замер по ТРЁМ проектам KAIF:** **124 из 227 отвеченных вопросов (55 %)** не имеют ни одной
  ссылки вне `interviews/` — NDim 30 %, KLAS 71 %, Unliminium 91 %. Интервью-сирот при этом **ноль**:
  единица разноса — **вопрос**, а не интервью, и на уровне файлов дефект невидим.
- **Лечение механизмом:** `npm run questions` вырос третьей секцией «РАЗНОС ОТВЕТОВ» с базовой
  линией `tools/propagation-baseline.json` (36 унаследованных) — краснеет только на новых,
  проверено мутацией. Канон — `AGENT_GUIDE.md` → «Дефект в САМОМ KAIF» и «Ответ владельца обязан
  ДОЕХАТЬ». Урок — `EXP-0134`.
- **Наверх:** `KAIF/ideas/ai_agents_reports/25_answer_propagation_gap_field_report_from_ndim.md`
  + пункты **1.9–1.11** в `KAIF/ideas/20_kaif_2.2_scope.md`.
  🔴 KLAS и Unliminium — **только читать** (прямое слово владельца).

**`bugs/112` (закрыт):** машинная метка контура протекала владельцу на страницу видимым мусором, а
время писалось в UTC. Теперь под каждым ответом видимая отметка **местным** временем, на карточке
вопроса — тоже. Контур: **132 проверки** (было 118), **4 мутации убиты**.

**Разнесено по документам:** `plans/24` — ответ Р5 = В вписан в четыре места, снят устаревший риск
«флип корня» и выведенный из него порядок фаз («флип последним» — пункт отменён).

✅ **Интервью №018 ЗАКРЫТО в тот же день — все шесть ответов, через страницу вычитки:**
· **В1 = А** — частотность запросов снимает **сам владелец** по инструкции агента;
· **В2 = А и В** — «вера» это вера **в людей** + «мир устроен по законам». 🔴 **Религиозного
  прочтения НЕТ** — развилка закрыта, конфессиональной рамки у продукта не будет;
· **В3 = «нужно обсудить»** + четыре встречных вопроса → брифинг **`interviews/interview_019`**;
· **В4** — разрешены все три формы обмена, но 🔴 «**личных данных не раскрываем, цифр метрик
  похожести не раскрываем**». Это СТРОЖЕ рекомендации агента и стало инвариантом `plans/34`;
· **В5 = В** — 🔴 **эксперимента над текстами главной НЕ БУДЕТ**: «не экспериментируем, а делаем на
  основании нашего видения и с учётом данных об реальных SEO зацепках». Фаза 7 переписана,
  прибор притока остался, сравнение версий удалено;
· **В6 = А** — записанный **заранее** ответ на опровержение гипотезы подтверждён.

🔴 **ЗАМЕР КАТАЛОГА СНЯТ — и он перевернул эпик** (`researches/30`, приложение A). Владелец поймал
крупную ошибку агента: «*ты сильно неправ… у каждого измерения есть очень большое описание*».
Он прав. Я читал `dims_list` — **сжатую проекцию** `{ru, en, year}` — и принял её за само измерение
(урок `EXP-0129`). Замер по бою, 5111 документов:
· описание — **медиана 1047 знаков RU и 1026 EN**, без описания **ноль** из выборки 120;
· корпус ≈ **5,35 млн знаков RU + 5,24 млн EN** против ~50 000 открытых сегодня — **×100**;
· 🔑 **это первый английский текст, который увидит робот** (`researches/26` §1.2: сегодня в
  отдаваемом HTML английского нет ни знака, а решение «EN основной» упиралось именно в это);
· гистограмма: **63,4 % измерений без единой оценки**, максимум **14** голосов, с ≥20 — **ноль**.
  Вопрос «какой выбрать порог N» **снят данными**: средняя оценка при нашем размере базы как
  содержание не работает вообще.
**Следствие:** фаза 5 `plans/34` из последней и заблокированной стала **крупнейшей победой эпика** и
зависимости от `plans/30` больше не имеет.
🐞 Попутно заведён **`bugs/111`** — сумма `rates` (4058) не сходится с `space/stats.ratings` (4089),
разница 31. И найдена грязь в поле «вид»: `фильм` строчной 21 шт., `Movie` 3 шт., один раз в поле
вида записан год.

✅ **Интервью №019 закрыто.** В1 — поправка владельца (см. выше). В2 — его слова о спецслужбах:
«*если ко мне придут… я буду ВЫНУЖДЕН способствовать расследованию*». Это верно, и моя рекомендация
«данные не передаются третьим лицам» была неверна как абсолют. Проблема текста 2.2 не в оговорке, а
в её ширине: сегодня он разрешает передачу «при необходимости, определяемой автором в одностороннем
порядке» — то есть что угодно, включая продажу.

✅ **Интервью №020, №021 и №022 закрыты в тот же вечер — форма страниц каталога определена целиком.**
Итоговое правило (записано в `plans/34`, фаза 5):
· **страница у ВСЕХ 5111 измерений**, весь корпус;
· **звёзды** при `rates` ≥ 1 (1873 шт.), **серый текст «ещё без голосов»** при нуле (3238 шт.) —
  то же правило, что владелец принял для экрана внутри (`ideas/26`): одно внутри и снаружи;
· **число проголосовавших** — 🔄 **при ЛЮБОМ числе оценивших** (решение владельца 2026-08-03
  ОТМЕНЯЕТ интервью №022 В3: «открываем показ оценок при любом числе оценивших везде по
  приложению»). Прежний порог ≥ 3 давал счётчик лишь у 526 из 1873 и защищал не личность, а
  впечатление от числа — разбор `bugs/113`. 🔴 **Перед следующим выкатом сайта помнить: публичные
  страницы пререндерены, число появится только после пересборки и деплоя;**
· описания — его текст, писанный с ИИ по открытым источникам ⇒ публиковать можно;
· подпись человека без имени — **номер из UID** («Участник Пространства №4718»), только как
  подпись; сквозная нумерация отвергнута: «Участник №94» сообщал бы, что нас меньше сотни.
🔴 **Путь к этому правилу стоит помнить:** первая редакция владельца («только 0 и >3 оценок») давала
3535 страниц и **мигающие адреса** — измерение, получив первый голос, обязано было исчезнуть из
поиска. Агент назвал механику до кода и вынес развилкой, а не исправил молча. Итог: **+1576 страниц
и +1,65 млн знаков при той же приватности**.
⚖️ **Юридический ответ по Email** (его вопрос): права показывать почту нет — ст. 4(1) GDPR (почта
это ПДн и без имени), ст. 25(2) (по умолчанию ПДн не становятся доступны неопределённому кругу лиц
без действия человека), ст. 6 («нечего показывать» основанием не является). ✅ **Но в 2.0 нарушения
НЕТ — проверено кодом:** почты нет в `UserRootDoc` вовсе, в строке топа связей её нет, человек без
имени показывается как «Без имени». Править надо **документ**, а не продукт: пункт 2.3 описывает 1.x.
Пункт 2.2 Политики сужается до мысли владельца о законных требованиях (его ответ А) — разбор за агентом.

📜 **Закрытое интервью №019** (брифинг по В3 + риск, найденный попутно).
Две развилки: открывать ли страницы измерений и в каком виде · что делать с пунктами **2.2 и 2.3 «Политики конфиденциальности»**.
🔴 **Про 2.2/2.3 — это находка, которая крупнее самого вопроса.** В нашей ОПУБЛИКОВАННОЙ «Политике конфиденциальности»
(текст 1.x) стоит: «*пользовательские данные могут быть переданы третьим лицам… без уведомления
Пользователя*» и «*Email адреса в Системе являются публичной общедоступной информацией*». Первое
противоречит позиционированию «Ваши данные не товар» и не является правовым основанием под GDPR;
второе просто **неверно для 2.0**. И всё это **уже открыто поиску** с 2026-08-01.
⚠️ **Работа не стоит:** замер каталога (`plans/35`, шаги 0–3) автономен.

**Прочие вопросы владельцу — ноль.** Все 17 предыдущих интервью отвечены. №015 (шесть развилок эпика
`ideas/29`) заведено и закрыто 2026-08-02, ответы разнесены — **волна эпика разблокирована целиком**:
· **В1 = Б** — панель РАЗДЕЛОМ ВНУТРИ приложения (не отдельный сайт), плюс два его условия:
  **не индексируется** и **редиректит не-админа на главную**;
· **В2 = А** — Wikidata как единственный реестр (TMDB с его атрибуцией отклонён);
· **В3 = А** — агент **НИКОГДА** не заводит измерение без вычитки. Инвариант, не настройка;
· **В4 = В** — только `title`, поле `name` **вычистить у всех 5111** («легаси не поддерживаем»);
· **В5** — первая комната панели = **кандидаты от агента**; заявки людей читает **сам агент** и
  выносит в ту же очередь с пилюлей «по заявке»; 🔴 статусы вычитки **в продукт не просачиваются**;
· **В6 = А** — запуск агента локальным скиллом KAIF.

№014 (три развилки замера боя) закрыто в тот же день:
· **В1 = В** — строка притока на лендинге станет ВЫВОДИМОЙ из снимков дней (`bugs/107`);
· **В2 = В** — снимки дней переписать И оставить след правки (`bugs/106` шаг 3);
· **В3 = А** — агрегат вершин пока не показывать (`plans/28` §8 шаг 4).
🟡 **Ждёт ответов — интервью №016 (реестр корпуса для портрета голоса).** Пять вопросов о
источниках, которых агент знать не может: тексты того же жанра вне NDim · на чём владелец учился
писать · ранние тексты · что брать из руководства 1.x · каким регистром писать лендинг.
Страница поднята. ⚠️ Работа НЕ стоит: портрет уже написан по тому, что есть в проекте.

✅ **Вычитка `plans/29` состоялась 2026-08-02** — вердикт «принять, но с правками». Разбор
ответов и что из них следует — **`plans/29` §3а**. Ворота фазы 2 эпика `plans/21` НЕ закрыты.
⚠️ Два урока этой вычитки, оплаченные дорого:
· `EXP-0125` — **назвать задачу владельцу не значит подать её.** Три вопроса в `plans/29` лежали
  ПРОЗОЙ, и ответить в один клик было физически нельзя. Строка «за владельцем» обязана идти вместе
  с ПОДНЯТОЙ страницей, а документ — быть кликабельным ДО доклада.
· 🔴 **Портрета голоса владельца в проекте не было вовсе** — он отвёл текст словами «вот это не мой
  язык, твоя стилометрия моего стиля должна была это красным цветом подсветить», а стилометрии
  здесь никогда не строилось. Теперь есть: **`AUTHOR_STYLOMETRY.md`** — с 2026-08-14 это
  публичный слепок ПРИНЯТОГО ядра `krinik-stylometry` 1.1 (151 правило; локальный черновик
  2026-08-02 — в истории git этого же файла). Это не «вопрос владельцу» в смысле стража, а
документ на построчное утверждение; внутри него три развилки, которые агент решать не вправе
(две Ваши собственные строки из 1.x и голос английского). Открывать страницей, не спрашивать в чате.
Унаследованный долг — 26 очередей вне `interviews/`, число обязано убывать.

### Устаревший раздел «Первым делом» (актуален был на 30.07)

### Первым делом — что запустить свежей сессии

```bash
npm run stand                     # эмуляторы + сервер синхронизации + vite (нужна Java)
node tools/verify-plan08.mjs      # 60 — память вида экранов, самое свежее и самое сквозное
node tools/verify-tierA.mjs       # досье связи целиком, включая новую строку bugs/46
cat interviews/interview_007_ideas21_forks.md   # ответы владельца по волне
```

### Закрытые пункты пула автономных задач (перенесены 2026-08-14)

- [x] **`ideas/24`, фаза 1** — лестница построена, замер снят, макет **V4 утверждён владельцем**.
- [x] **`ideas/24`, фаза 2 — «Шаг виджета» в бою** (выкачено 2026-07-31).
- [x] **`plans/13`, фаза 1 — обзор индустрии по загрузке фото** → `researches/23` (2026-07-31).
- [x] **Эпик `plans/15`, фазы 2–5 и 8** — конфигурация, макеты, экран `/account`, смена почты,
      удаление аккаунта. Закрыты 2026-08-01.
- [x] 🚪 **Фаза 7 «Ворота», первый шаг — снять `noindex` с документов** — ✅ **СДЕЛАНО**
      (`DocShell.svelte:44–59`, поиску открыто 2 страницы → 11, текста ×13.5; `e2e/seo` 10/10 +
      мутация). Чекбокс висел незакрытым — снят при возобновлении 2026-08-01.
- [x] 🐞 **`bugs/105` — «Как меня видят» в рабочей области** — ✅ **СДЕЛАНО 2026-08-02**
      (`bugs/105_DONE_…`, страж `verify-bug105` 56/0). Инвариант `bugs/76` жив и проверен.
- [x] 🗂 **ЭПИК `ideas/29` — ПЛАНИРОВАНИЕ ЗАКРЫТО 2026-08-02.** Лестница пройдена целиком:
      `researches/29` → `plans/30` (метаплан, 7 фаз) → `plans/31` (операционный план фазы 1) →
      `interviews/interview_015` (6 развилок, **все отвечены в тот же день**).
- [x] ✅ **ЭПИК `ideas/29`, ФАЗА 1 — СДЕЛАНА И ВЫКАЧЕНА В БОЙ 2026-08-02.** `diameter` 714,9,
      `dims` 5111, сервер билд 24. Закрыла `bugs/106`, `bugs/108`, `bugs/109` одной правкой.
- [x] ✅ **ЭПИК `ideas/29`, ФАЗА 2 — ЗАКРЫТА:** владелец выбрал **V1 «Стопка»** (`plans/32`).

## 🗂 Перенесено из STATUS.md ДОСЛОВНО — стрижка бонсая 2026-08-03

> Записи перестали быть «сейчас»: это отчёт чата 2026-08-02 (ночь) о планировании эпика
> `ideas/30` и блок того же дня о вечернем эпике `ideas/29`, включая разбор интервью №018–022.
> Решения из них живут в `plans/34`, `plans/36`, `researches/30` и в самих интервью; здесь —
> первоисточник хода событий.

## Что сделал этот чат

| Что | Где | Доказательство |
|---|---|---|
| Документ владельца закоммичен дословно | `ideas/30` | правило 16, отдельный коммит до правок |
| Зонтичный обзор написан | **`researches/30`** | 30+ первоисточников с DOI · уровень доказательности у КАЖДОГО утверждения · PICO · GRADE-таблица |
| Метаплан эпика | **`plans/34`** | 8 фаз · ворота · 10 критериев приёмки · границы с 4 соседними эпиками |
| Операционный план фазы 1 | **`plans/35`** | замер каталога (автономен) + замер спроса (ждёт владельца) |
| Развилки: **пять интервью заведены и ЗАКРЫТЫ за вечер** | `interviews/018`–`022` | 21 вопрос, все отвечены и разнесены |
| Дорожная карта внесена в мастер-план | `MASTER_PLAN.md` фаза 7 + 3 записи в журнале решений | прямой пункт заказа владельца |
| **Фаза 1 не только спланирована, а СДЕЛАНА** | `researches/30` приложение A | 5111 боевых документов прочитано |
| Список запросов + инструкция владельцу | `researches/30` приложение B · **`homeworks/08`** | ~120 запросов + 5 шаблонов, ни одной выдуманной частотности |
| Разбор Политики конфиденциальности | **`researches/31`** | построчно «что написано / что делает код» |
| **Русский спрос ЗАМЕРЕН** | `researches/30` прил. C · `30_demand_ru.json` | 82 запроса + **3 948 реальных запросов людей** из API Вордстата |
| **Английский спрос ЗАМЕРЕН** | `researches/30` прил. D · `30_demand_en.json` | 99 запросов через Bing, записей с ошибкой ноль |
| План фазы 5 + **шаг 0 выполнен** | **`plans/36`** | 5111 страниц: 17,1 с, 78,8 МБ, 15,8 КБ/стр., слаг-коллизий ноль |
| Прототип страницы измерения | `src/routes/dimension/[slug]/` | описание в СЫРОМ HTML; правило оценок в одном месте |
| Развилка «повод против человека» | **`ideas/31`** | «events near me» 14,9 млн против «people near me» **81** |

**Машинерия, которой раньше не было** (вся работает в авторизованной сессии владельца через
Chrome с портом отладки 9222, не логинится и паролей не знает):

| Инструмент | Что делает |
|---|---|
| `tools/cdp-discover.mjs` | разведка чужой страницы: есть ли у неё свой API и что он отдаёт |
| `tools/wordstat-harvest.mjs` | русский спрос из API Вордстата (`totalValue`) + попутно реальные запросы людей |
| `tools/bing-keywords.mjs` | английский спрос из Bing Webmaster; **`topsearchurls` показывает, КТО ранжируется** |
| `tools/fetch-dims-slice.mjs` | каталог → сборка, детерминированный слаг, страж коллизий |
| `tools/search-queries.json` | машиночитаемый список запросов по группам намерения |

🔑 **Четыре вывода этого захода, которые нельзя переоткрывать:**
1. **«SEO-ядра» как подсистемы не существует.** Четыре поверхности и **один двигатель —
   распространение**. Негатив кликают (+2,3 % CTR за слово, РКИ на 370 млн показов), благоговением
   **делятся**. `GOAL.md` закрывает первый путь; знание, что он работает, — **не разрешение**.
2. **Гиганты из заказа SEO-ядра не имеют** (Tinder 67,3 % прямого трафика, Letterboxd 67,8 %).
   Аналог NDim — **16personalities**: органика 55,4 %, 81 389 ключей, двигатель = бесплатный
   инструмент + **десятки** страниц с содержанием.
3. 🔑 **Поправка владельца про «ключ СЕКС» получила научное имя.** Fisher (2002): три системы мозга.
   Индустрия торгует **вожделением**; определение **влечения** — «выбирать среди партнёров и
   предпочитать конкретных, экономя время и силы» — есть описание функции NDim. Благородная форма
   оказалась **незанятой нишей**, а не жертвой. Мета-анализ 78 работ: пошлый ключ бренду не помогает.
4. ⛔ **«Страница на каждое из 5111 измерений» = doorway abuse** и не обеспечена данными
   (4089 оценок на 5111 измерений).

🔴 **И пятый вывод — самый дорогой, потому что он про мою ошибку.** Пункт 4 выше («страницам нечем
быть») **опровергнут владельцем в тот же вечер**: у каждого измерения есть описание на 1000+ знаков
на ДВУХ языках. Я читал `dims_list` — сжатую проекцию `{ru, en, year}` — и принял её за само
измерение (`EXP-0129`). Правильная картина: **корпус в базе ×100 больше того, что открыто поиску**,
и он уже двуязычный. Фаза 5 из последней и заблокированной стала крупнейшей победой эпика.

### 🎓 Пять уроков этого чата — все оплачены ошибками, все записаны

| Урок | Что стоило |
|---|---|
| `EXP-0129` **Проекция — не сущность** | три документа с выводом «публиковать нечем»; владелец поправил одной фразой |
| `EXP-0130` **Проза после «Ответ:» съедает вопрос** | контур отчитался «ждут 2» при трёх заданных — вопрос не дошёл бы до владельца |
| `EXP-0131` **Гаси по СВОЕМУ документу, не по имени инструмента** | убил свою страницу дважды; чужую (Unliminium) не убил по случайности |
| `EXP-0132` **Ноль — это ВЫВОД, а не результат обработки ошибки** | 16 записей уехали как «спроса нет», хотя это был сбой сервиса |
| `EXP-0133` **Поправка относится к КЛАССУ, а не к экземпляру** | владелец поправил про язык — я применил к русскому и повторил ошибку в английском |

---

**Предыдущее обновление:** 2026-08-02, вечер — 🗂 **ЭПИК `ideas/29`: ЛЕСТНИЦА
ПРОЙДЕНА, ФАЗА 1 В БОЮ, ФАЗА 2 ЗАКРЫТА.**

**Что этот чат сделал** (прямое распоряжение владельца — взять `ideas/29` в планирование):

| Что | Где | Доказательство |
|---|---|---|
| Лестница `/plan-epic` пройдена целиком | `researches/29` → `plans/30` → `plans/31` | 25 первоисточников · 7 фаз · план фазы 1 |
| Шесть развилок владельца — заданы и отвечены в тот же день | `interviews/interview_015` | В1=Б · В2=А · В3=А · В4=В · В5 свой · В6=А |
| 🚀 **Фаза 1 выкачена в БОЙ** | `calculator/index.mjs` | `diameter` **714,9**, `dims` **5111**; сервер билд 20 → **24** |
| Три бага закрыты одной правкой | `bugs/106`, `bugs/108`, `bugs/109` | `dims_index.test.mjs` 6/6 (до фикса 5 провалов из 6) |
| Контур вычитки больше не съедает ответы | `bugs/110` | `verify-bug110` **20/20**, две адресные мутации |
| Портрет голоса владельца снят | `OWNER_VOICE.md` | язык подтверждён им лично; документ — черновик |
| Фаза 2 закрыта: выбран **V1 «Стопка»** | `plans/32`, `design/dims-manager-mockups.html` | 16 кадров, 2 дефекта исправлены до показа |
| План фазы 3 написан | `plans/33` | не начат |

🔴 **Четыре урока этого дня, оплаченные дорого:**
1. **Назвать задачу владельцу — не значит подать её** (`EXP-0125`). Строка «за владельцем» обязана
   идти вместе с ПОДНЯТОЙ страницей, а документ — быть кликабельным ДО доклада.
2. **Дублирующий звонок дороже пропущенного** (`EXP-0126`). Страницу поднимать ОДНОЙ фоновой
   задачей; жива и без ответов — гасить и поднимать МОЛЧА (`--no-signal`).
3. **Бой простоял 5 минут по вине агента** (`EXP-0127`): Git Bash подменил `/secrets/sa.json` на
   `C:/Program Files/Git/secrets/…`. **`docker ps` показывал `Up`** — «Up» не признак работы,
   единственный признак — ЛОГ первого цикла. Команды `docker` гнать из PowerShell.
4. 🔑 **Похожий голос делает выдуманное содержание ОПАСНЕЕ** (`EXP-0128`). Владелец: «язык похож на
   мой, а содержание выдумано». Портрет даёт право писать ЕГО ГОЛОСОМ, но не ЗА НЕГО.

> 📜 Подробности этого дня и исполненный блок «НА ЗАВТРА» перенесены ДОСЛОВНО в
> `PROJECT_HISTORY.md` — стрижка бонсая 2026-08-02, вечер.

---

### 🗣 Контур вычитки владельца — им задаются ВСЕ вопросы

Всё, что агент хочет ОТ владельца, открывается **HTML-страницей с ответом в один клик**; ответ
механически возвращается в md с `by`/`at`. Критерий приёмки регламента уже выполнен — владелец
отвечает через неё **без единого уточняющего вопроса в чате** (интервью №011–№014).
Устройство и история — `plans/27` и `PROJECT_HISTORY.md`.

| Команда | Зачем |
|---|---|
| `npm run questions` | 🔴 **прогоняй в начале сессии и перед сдачей.** Вопросы владельцу вне `interviews/` + все интервью, ждущие ответа |
| `npm run review -- open interviews/<файл>.md` | открыть страницу, позвать владельца голосом |
| `node tools/verify-owner-reviews.mjs` | QA-прогон контура — **97 проверок**, стенд не нужен |

🔑 **«Сохранить» БУДИТ агента** — слово владельца: агент узнаёт о событии по завершению процесса,
поэтому любое сохранение закрывает контур. Осталось неотвеченное — поднять страницу заново обязан
АГЕНТ, а не владелец держать вкладку. Вид карточек — его же выбор (интервью №012, V1 «Полоса
слева», цветом состояния).

📤 **Отчёт наружу — `researches/28`** (заказ владельца): полевой отчёт по этой реализации для
разработчика KAIF **и** для агента на другом проекте. Часть I — что формализовать в регламенте
(главное: инварианта «сохранение будит ждущего» в нём НЕТ), часть II — исполнимый контракт «как
построить то же самое». Ссылка на него вписана в `D:\work\ai_sandbox\KAIF\ideas\20_kaif_2.2_scope.md`
п. 1.6 — там уже стоял запрос ровно на это.

**Что страж нашёл в первый же день:** интервью №007 два дня числилось «🔴 ЖДЁТ ОТВЕТОВ» при
двенадцати заполненных ответах · разбор считал `---` после пустого поля «Ответ:» **ответом**, из-за
чего по всем десяти живым интервью выходило «0 без ответа» · В10 интервью №008 вечно числился
открытым, хотя снят отменой паролей. Всё исправлено. **Унаследованный долг — 26 очередей вопросов
вне `interviews/`** (`tools/questions-baseline.json`): страж краснеет только на НОВЫХ, а это число
обязано убывать. Уроки — `EXP-0116…0118`.

### ✅ Bing Webmaster: сайт НА МЕСТЕ — тревога была ложной, и это стоит помнить

Разведка 2026-08-02 сначала показала пустой аккаунт (`kotkrinik@yandex.ru`, `UserSites: []`,
создан тем же входом) — я чуть не завёл это дефектом «`homeworks/04` врёт». **Вторая гипотеза
оказалась верной:** под `kotkrinik@gmail.com` сайт есть — `ndimspace.app`, роль **Owner**,
подтверждён как ДОМЕН, аккаунт создан **2026-07-11 13:37**, то есть ровно в дату домашки.

🔑 **Урок, который дороже находки:** у одного человека несколько аккаунтов, и «пусто» в одном из
них — это не «пусто везде». Прежде чем объявлять запись документа неправдой, **проверь другой вход**.

📌 **Полезное следствие:** Bing Webmaster даёт **бесплатный точный** замер спроса (Google без
рекламных трат отдаёт только вилки) и вдобавок отвечает на вопрос, которого у нас не было вовсе, —
`topsearchurls` показывает, **кто реально ранжируется** по запросу. Инструмент — `tools/bing-keywords.mjs`.


## 2026-08-02, вечер — ЭПИК `ideas/29`: лестница пройдена, фаза 1 в бою, фаза 2 закрыта

> Перенесено ДОСЛОВНО из `STATUS.md` стрижкой бонсая 2026-08-02, ночь.

**Что тот чат сделал** (прямое распоряжение владельца — взять `ideas/29` в планирование):

| Что | Где | Доказательство |
|---|---|---|
| Лестница `/plan-epic` пройдена целиком | `researches/29` → `plans/30` → `plans/31` | 25 первоисточников · 7 фаз · план фазы 1 |
| Шесть развилок владельца — заданы и отвечены в тот же день | `interviews/interview_015` | В1=Б · В2=А · В3=А · В4=В · В5 свой · В6=А |
| 🚀 **Фаза 1 выкачена в БОЙ** | `calculator/index.mjs` | `diameter` **714,9**, `dims` **5111**; сервер билд 20 → **24** |
| Три бага закрыты одной правкой | `bugs/106`, `bugs/108`, `bugs/109` | `dims_index.test.mjs` 6/6 (до фикса 5 провалов из 6) |
| Контур вычитки больше не съедает ответы | `bugs/110` | `verify-bug110` **20/20**, две адресные мутации |
| Портрет голоса владельца снят | `OWNER_VOICE.md` | язык подтверждён им лично; документ — черновик |
| Фаза 2 закрыта: выбран **V1 «Стопка»** | `plans/32`, `design/dims-manager-mockups.html` | 16 кадров, 2 дефекта исправлены до показа |
| План фазы 3 написан | `plans/33` | не начат |

🔴 **Четыре урока того дня, оплаченные дорого:**
1. **Назвать задачу владельцу — не значит подать её** (`EXP-0125`).
2. **Дублирующий звонок дороже пропущенного** (`EXP-0126`).
3. **Бой простоял 5 минут по вине агента** (`EXP-0127`): «Up» не признак работы, единственный признак — ЛОГ первого цикла.
4. 🔑 **Похожий голос делает выдуманное содержание ОПАСНЕЕ** (`EXP-0128`).

---
## 2026-08-02 — день эпика `ideas/29`: планирование, фаза 1 в бой, фаза 2 закрыта

> Перенесено ДОСЛОВНО из `STATUS.md` при завершении чата (стрижка бонсая). В конце записи — блок
> «НА ЗАВТРА», написанный ДО того, как эта работа была сделана: он исполнен целиком, кроме
> пунктов, уехавших в свежую эстафету.

### Что чат сделал

| Что | Где | Доказательство |
|---|---|---|
| Лестница `/plan-epic` для `ideas/29` пройдена целиком | `researches/29` → `plans/30` → `plans/31` | 25 первоисточников, 7 фаз, операционный план фазы 1 |
| Шесть развилок владельца заданы и отвечены в тот же день | `interviews/interview_015` | В1=Б · В2=А · В3=А · В4=В · В5 свой ответ · В6=А |
| Фаза 1 — экономный индекс каталога — сделана и **выкачена в бой** | `calculator/index.mjs`, `calculator/dims_index.test.mjs` | 6/6 (до фикса 5 провалов из 6) · `test:calc` 59/59 · мутация адресная |
| Три бага закрыты одной правкой и одним выкатом | `bugs/106`, `bugs/108`, `bugs/109` | в бою `diameter` 714,9 и `dims` 5111; сервер билд 20 → 24 |
| Контур вычитки перестал съедать ответы владельца | `bugs/110`, `tools/verify-bug110.mjs` | 20/20 живым Chromium, ДВЕ адресные мутации, взяты все пять мер источника |
| Портрет голоса владельца снят | `OWNER_VOICE.md`, `interviews/016`, `017` | 13 правил с цитатами, 26 оборотов, 15 маркеров анти-портрета |
| Фаза 2 — четыре макета «Менеджера измерений», выбран **V1 «Стопка»** | `design/dims-manager-mockups.html`, `plans/32` | 16 кадров сняты, 2 дефекта исправлены до показа |
| Операционный план фазы 3 | `plans/33` | три состояния прав вместо двух |

### Уроки, оплаченные этим днём

`EXP-0125` назвать задачу владельцу не значит подать её · `EXP-0126` дублирующий звонок дороже
пропущенного · `EXP-0127` бой простоял 5 минут из-за подмены пути в Git Bash, «Up» не признак
работы · `EXP-0128` похожий голос делает выдуманное содержание опаснее, а не безопаснее.

### Блок «НА ЗАВТРА», исполненный этим же днём (дословно)

### ☀️ НА ЗАВТРА (2026-08-03) — что делать первым, по порядку

> Написано для сессии с пустым контекстом. Пункт 1 — прямой заказ владельца, остальное по порядку
> волны `plans/26`.

1. ✅ **ФАЗА 1 ЭПИКА `ideas/29` СДЕЛАНА И ВЫКАЧЕНА В БОЙ 2026-08-02.**
   Подтверждено боем: `diameter` **714,9**, `dims` **5111** (было 715 и 5112). Сервер
   синхронизации — **билд 24** (был 20); приложение выкачено на обе площадки.
   ⚠️ Снимки дней ДО 2026-08-02 остались с 5112 — их правит отдельный инструмент (решение
   владельца В2=В, интервью №014), он ещё не написан.
   Закрыты одной правкой: `bugs/106` (5112 → 5111, диаметр 714,9) · `bugs/108` (вечная
   пересборка) · `bugs/109` (новые измерения в бою не находились). Плюс попутно найден и
   починен дефект, ронявший ВЕСЬ цикл записи на измерении без названия.
   Доказано: `calculator/dims_index.test.mjs` 6/6 (до фикса 5 провалов из 6) · `test:calc` 59/59 ·
   `npm test` 199/199 · мутация адресная. Подробности — `plans/31`.
   ⛔ **Следующая фаза — 2 (четыре макета «Менеджера измерений»), она уже разблокирована.**

<details><summary>Что было в этом пункте до исполнения</summary>

   🔧 **ФАЗА 1 ЭПИКА `ideas/29`: экономный индекс каталога.**
   ✅ Планирование закрыто 2026-08-02 (лестница пройдена целиком). Исполнять по
   **`plans/31_admin_epic_phase1_index_economy.md`** — шаги готовы, решений владельца НЕ требует,
   клиент не трогается.
   Что фаза делает разом: обезвреживает `bugs/108` (вечная пересборка) · переводит индекс на
   ДЕЛЬТУ вместо полной пересборки (ответ на вопрос владельца «можно ли лучше») · чинит `bugs/106`
   (5112 → 5111, диаметр 714,9) · подтверждает и чинит `bugs/109` · делает сид честным.
   🔴 **Первый шаг — ЗАМЕР, а не код**: воспроизвести мину на стенде и снять числа «до». Не
   воспроизвелась — переписать гипотезу, а не «чинить на всякий случай» (обе находки помечены
   `[NOT-TESTED]` честно).
   🔧 На выходе — **рука владельца**: `npm run calc:image` и выкат. Агент не выкатывает.
   🔗 Карта эпика — `plans/30` (7 фаз), доказательная база — `researches/29`.
</details>

2. 📝 **`plans/29` — тексты ворот: ВТОРОЙ ЗАХОД.** Первый состоялся 2026-08-02, вердикт —
   «принять, но с правками». Разбор — `plans/29` §3а. Что переделывать:
   · подзаголовок 1.x **возвращается целиком** (его слово «оставляем»);
   · **термины на витрине раскрывать, а не бросать голыми** — «5 111 измерений» нарушение,
     «5 111 объектов человеческой культуры, по которым…» норма;
   · полоса чисел становится **БЛОКОМ** с зелёным статусом и буллет-списком (форма — макеты, фаза 3);
   · якорь меняет слово: не «калькулятор», а «точный, честный и быстрый беспристрастный
     суперкомпьютер». ⚠️ Из четырёх эпитетов под замером нет только **«быстрый»**;
   · английский — В3=А: рабочий перевод мой, вычитка его.
   🔴 **Переписывать ТОЛЬКО по `OWNER_VOICE.md`** — иначе третий заход по тому же кругу.
   ⚠️ **Пока текст не утверждён — макеты не рисовать** (`plans/21` фаза 2 → 3: «макет с рыбой
   неотличим от макета с рыбой»).

3. 🔧 **За владельцем — ПЕРЕСБОРКА ОБРАЗА** (`npm run calc:image`). Без неё агрегат вершин
   (`bestMatch`) остаётся стендовым, и медиана верхних строк по 94 людям так и не станет
   известна — а именно ею калибруется обещание лендинга (`plans/26` §3 №9).
   > ✅ **Дописано 2026-08-14 (летопись append-only — исправление это НОВАЯ запись, а не правка
   > старой):** образ владелец выкатил **2026-08-03** (билд 28), и агрегат перестал быть стендовым
   > в тот же день. Прочитан он только 2026-08-14 — одиннадцать дней боевое число лежало
   > непрочитанным, потому что план ждал выката, который уже состоялся. Числа и разбор —
   > `plans/28` §9 (медиана вершины **7 %**, средняя 11 %, максимум 68 %).

4. 🐞 **Четыре дефекта каталога и витрины:**
   · `bugs/106` (5112 вместо 5111, диаметр 715 вместо 714,9) · `bugs/108` (вечная пересборка
     индекса) · `bugs/109` (новые измерения в бою не находятся) — 🔑 **все три чинятся ФАЗОЙ 1
     эпика одним выкатом образа**, отдельными заходами их брать не надо.
     ⚠️ Хвост `bugs/106` остаётся за владельцем: снимки дней переписать И оставить след правки
     (интервью №014, В2 = В) — разовый инструмент, пишет в БОЙ, только с его слова и ПОСЛЕ выката.
   · `bugs/107` — витрина больше не врёт (полоса чисел выкачена), но **вариант В владельца ещё
     не сделан**: строка притока обязана стать ВЫВОДИМОЙ из снимков дней. Развилка закрыта
     (№014 В1=В), спрашивать нечего — исполнять.

5. **`ideas/28`** — порог решён (В3 = В, верхние 20 % каталога), шаг 0 закрыт: `dims_list` — 566 КБ
   = **54 %** потолка 1 МиБ, счётчик влезает (57–58 %).
   🔴 **Настоящая развилка эпика другая:** `ensureDimsIndex` (`calculator/index.mjs:344–370`)
   пересобирает индекс ТОЛЬКО из `{ru,en,year}` и пишет его `merge` на уровне поля — **первая же
   смена числа измерений сотрёт все 5111 счётчиков молча**, и сторож свежести этого не заметит.
   Правка `ensureDimsIndex` обязана идти ДО любого кода, который счётчики пишет.
   ⚠️ Запас каталога — **1,85×** (лопнет примерно на 9 400 измерениях); документ перечитывается
   целиком каждым циклом сервера и каждой клиентской сессией ⇒ счётчики писать ПАЧКОЙ.
   ⚠️ У **9 пар новичков из 10** общей оси по-прежнему нет даже при пуле 20 %. Решение владельца
   не отменять — знать и показать.

6. 🔧 **Прочее за владельцем (руки человека):** погасить пароли у 331 человека в бою · приёмка
   смены почты боем · приёмка аватара на живом Safari · ключ reCAPTCHA для App Check ·
   приёмка `bugs/105` боем.


---

## Сессия 2026-08-02, ночь: замер петли анонима + пять замечаний владельца (перенесено из STATUS дословно)

> Стрижка бонсая при закрытии чата 2026-08-02 (день). Работа закрыта; её результат живёт в
> `plans/28`, `bugs/105`, `ideas/25–28`. В STATUS остались только выводы, которые ещё «сейчас».

**Последнее обновление:** 2026-08-02, ночь, чат закрыт — 🔬 **ЗАМЕР ПЕТЛИ АНОНИМА + ПЯТЬ ЗАМЕЧАНИЙ
ВЛАДЕЛЬЦА + ИНТЕРВЬЮ №013.** Фаза 1 эпика `plans/23` закрыта замером (`plans/28`): время до первого
топа **1…12 с**, но главное найдено случайно — 🔴 **усердие новичка НАКАЗЫВАЕТСЯ**: похожесть падает
**51 → 33 → 26 → 14 %** по мере роста числа оценок, потому что лента даёт случайное, а Дайс растит
знаменатель. В случайной ветке при 10 оценках пересечение НУЛЕВОЕ со всеми — значит топа не будет
никогда. Отсюда родился эпик владельца `ideas/28`.

**Что этот чат сделал:**

| Что | Где | Проверено |
|---|---|---|
| Замер петли анонима начисто | `plans/28`, `tools/probe-guest-journey.mjs` | живой браузер, 2 ветки |
| Прибор починен от **4 лжей** | тот же | сверка со вторым свидетелем |
| Шапка страницы вычитки: кто спрашивает + пилюли | `tools/review.mjs` | кадры 2 темы, страж 118/0 |
| Ложное «браузер не дал закрыть» | `tools/review.mjs` | CDP-замер, 4053 мс vs 4055 мс скептика |
| Пять замечаний владельца заведены | `bugs/105`, `ideas/25–28` | — |
| Ответы интервью №013 разнесены | `bugs/105`, `ideas/28` | состязательная проверка |

🔴 **Три урока этого чата, оплаченные дорого:**
1. **Прибор соврал ЧЕТЫРЬМЯ способами** (`plans/28` §4), и самая опасная ложь **совпала с
   гипотезой** — она уже стояла в черновике как находка. Поймана только вторым свидетелем:
   вычислитель записал `ownerSpaceSize: 40`, что разошлось с прибором. Урок — `EXP-0121`.
2. **Владелец поправил вывод агента и был прав** (`EXP-0120`): я объявил ограничением математику,
   а это было ограничение ПОДАЧИ — лента выдаёт случайное. Второй такой случай за неделю.
3. **Обратные кавычки в комментарии внутри шаблонной строки** уронили `tools/review.mjs` целиком
   (`SyntaxError`), при том что предупреждение об этих граблях стоит в том же файле тридцатью
   строками выше. Поймал субагент, не я.

## Сессия 2026-08-01: разворот на ворота + контур вычитки (перенесено из STATUS дословно)

**Предыдущее:** 2026-08-01 — 🚪 **ПРОЕКТ РАЗВЁРНУТ НА ВОРОТА.** Владелец
ответил на два интервью (№009 — 11 ответов, №010 — 14 из 15) и заказал фундаментальную работу:
лендинг-как-приложение, бесшовный вход анонимом, гость наравне с полноценным человеком,
**английский как основной язык продукта**, максимальное SEO. Проведена **ревизия мастер-плана**:
заведена **фаза 7 «Ворота»**, соцслой стал фазой 8, принципов стало девять. Видение зафиксировано в
`GOAL.md`, `MASTER_PLAN.md` и `AGENT_GUIDE.md` — **читай их до любой задачи по воротам**.

**Что этот чат СДЕЛАЛ в коде** (не только спланировал):
| Что | Где | Проверено |
|---|---|---|
| Правила Storage выкачены в бою (`bugs/04` закрыт) | Rules API | чтением боя: `/admin` и `/public` исчезли, 403 |
| Публичная дверь удаления была СЛОМАНА — три дефекта | `delete-account`, `account.ts` | `verify-delete-door` **16/16**, 2 мутации |
| Документы открыты поиску: 2 страницы → **11**, текста ×13.5 | `DocShell`, `sitemap` | `e2e/seo` **10/10**, мутация |
| Класс «один отказ гасит весь экран» | `relations.ts`, `dims.ts` | живьём: гость с 4 связями, экран жив |
| Множитель витрины ×30 удалён — настоящее число | `metrics.ts` | `verify-bug81` **28/0**, в кадре «94 человека» |
| Предохранитель воронки снят — считает в бою | `funnel.ts` | сборка: firebase остался ленивым |
| Мёртвый UID при протухшей ссылке | `profile.ts` | типы; путь редкий |

**Предыдущее:** 2026-08-01 — 🔐 **ПРАВИЛА STORAGE ВЫКАЧЕНЫ В БОЙ** по слову владельца
«кати» (`bugs/04` ЗАКРЫТ, четыре года прожив с правилами 1.x). В бою больше нет ни списка
админских почт, ни открытого всему свету `public/`; появился потолок **5 МБ + `webp`/`jpeg`**.
Файлы 1.x забэкаплены в `.private/storage-1x-backup/` (вне git), прежний ruleset цел — откат
одним PATCH. **Фаза 6 эпика (аватар) разблокирована полностью.**

**Ранее в тот же день:** эпик «Управление аккаунтом» прошёл фазы 2–5 и 8: экран
`/account`, смена почты сквозным путём, удаление аккаунта с каскадом и чисткой следов в
вычислителе, пароли отменены целиком.

**Предыдущее:** 2026-07-31, ночь — 📐 заведён сам эпик: лестница `/plan-epic` до третьей ступени —
`researches/24` + `researches/23` → `plans/15` (метаплан, 8 фаз) → `plans/16` →
`interviews/interview_008` (девять развилок).

**Предыдущее обновление:** 2026-07-31, вечер (чат закрыт) — ✅ **ЭПИК `ideas/24` (ФАЗЫ 1–2) В БОЮ
+ ПЯТЬ ВЫКАТОВ.** Все сводные экраны переведены на виджетную сетку с ШАГОМ (модель владельца);
«Аккаунт» слит с карточкой-шапкой «Профиля»; «Меню» приведено к двум колоннам; манифест удалён
целиком как отсебятина агента. Заведены `bugs/104` и `plans/13`. Уроки — `EXP-0104…0107`.

**Днём того же дня:** 2026-07-31 — 🧰 **KAIF ОБНОВЛЁН 2.0 → 2.1 «Strong KAIF»**
(по слову владельца) маршрутом bootstrap: свежий `KAIF.md` → `KAIF-LOADER.mjs` → машинерия 2.1;
сначала прогон в песочнице, предсказание совпало с боем байт в байт. Новое: **`PROJECT_HISTORY.md`**
(летопись; туда дословно мигрирована история этого файла — 2 900 → ~260 строк), 6 скиллов
(`/plan-task`, `/plan-epic`, `/guarded-loop`, `/code-revision`, `/owner-voice`, `/owner-reviews`),
канон-вставки в `AGENT_GUIDE.md` (лестница планирования · гигиена документов и текста + реестр пар
«истина ↔ зеркало» · место вопросов · класс вкуса · разрешение ≠ авторство идентичности). Полевой
отчёт разработчику фреймворка **забран владельцем лично 2026-07-31** — в репозитории его нет и
заводить заново не надо.

**Свежее:** 2026-08-01, вечер — 🗣 **РАЗВЁРНУТ КОНТУР ВЫЧИТКИ ВЛАДЕЛЬЦА** (`plans/27`, регламент
KAIF `/owner-reviews`): всё, что агент хочет ОТ владельца, открывается **HTML-страницей с ответом
в один клик**, ответ механически возвращается в md с `by`/`at`. Владелец уже ответил через неё на
интервью №011 — **без единого уточняющего вопроса в чате**, а это и есть критерий приёмки
регламента. Голос сигнала взят **взаймы у соседнего проекта KLAS** (Silero v5, локально) — командой,
не копией; голос `eugene` выбран владельцем вслепую из пяти образцов.

📤 **Отчёт наружу — `researches/28`** (заказ владельца): полевой отчёт по этой реализации для
разработчика KAIF **и** для агента на другом проекте. Часть I — что формализовать в регламенте
(главное: инварианта «сохранение будит ждущего» в нём НЕТ), часть II — исполнимый контракт «как
построить то же самое». Ссылка на него вписана в `D:\work\ai_sandbox\KAIF\ideas\20_kaif_2.2_scope.md`
п. 1.6 — там уже стоял запрос ровно на это.

**Что страж места вопросов нашёл в первый же день:** интервью №007 два дня числилось «🔴 ЖДЁТ
ОТВЕТОВ» при двенадцати заполненных ответах · разбор считал `---` после пустого поля «Ответ:»
**ответом**, из-за чего по всем десяти живым интервью выходило «0 без ответа» · В10 интервью №008
вечно числился открытым, хотя снят отменой паролей. Всё исправлено. Уроки — `EXP-0116…0118`.

---

## Эпик «Управление аккаунтом», фазы 2–5 и 8 — 2026-08-01 (перенесено из STATUS дословно)

> Стрижка бонсая при закрытии чата 2026-08-01 (вечер). Работа закрыта; в STATUS остались
> указатель и открытая фаза 6 «аватар».

**ЭПИК «УПРАВЛЕНИЕ АККАУНТОМ» — `plans/15`. Заказ владельца 2026-07-31:** смена Email ·
   сброс пароля · смена пароля · аватар · **удаление аккаунта** («тоже надо» — его слово в ответ
   на вопрос о границах). Поглощает `bugs/45`, `bugs/44`+`plans/13` и часть `bugs/32`.

   **Ступень 1 лестницы закрыта:** `researches/24` (Firebase + OWASP ASVS 5.0 + NIST 800-63B-4 +
   GDPR/сторы + форма экрана у шести продуктов + разведка архива 1.x **по коду**) и
   `researches/23` (фото в браузере). 306 фактов, каждый с URL и цитатой; 24 рискованных прошли
   состязательную перепроверку (20 подтверждено, 4 уточнено).

   🔴 **Пять находок, которые меняют постановку** (не переоткрывать):
   - `updateEmail` и `linkWithCredential` с паролем **закрыты** защитой от перечисления почт;
     смена почты только `verifyBeforeUpdateEmail`, «завести пароль» — только REST `accounts:signUp`.
   - **«Письмо отправлено» может быть неправдой:** при занятом адресе promise резолвится молча ⇒
     тексты обязаны быть нейтральными («если адрес свободен, письмо придёт»).
   - **Реавторизация ветвится на ТРИ пути** (`providerData`); popup для человека, вошедшего по
     ссылке, **бросит ошибку**.
   - **Порядок удаления обратен 1.x:** сначала данные, потом `deleteUser`. В 1.x аватар оставался
     сиротой, а ошибки удаления глушились — это дефект, не образец.
   - 🔴 **Зелёный стенд НЕ доказывает смену почты:** Auth-эмулятор не применяет запрет EEP в
     `setAccountInfo`, писем не шлёт. Проверяется только боем.
   - Бонус фазы «аватар»: **Safari не кодирует WebP** из canvas, молча отдаёт PNG (323 КБ против
     12 КБ), а путь `avatar.webp` менять нельзя.

   ⚠️ **Разведка опровергла наши же документы:** в 1.x у смены пароля было **два поля, а не три**,
   а на смене Email **пароль стоял выше нового адреса**. `researches/12` и `bugs/45` исправлены.

   ✅ **ФАЗА 2 ПРОЙДЕНА 2026-08-01** (`plans/16`, полностью — `researches/24` §7). Владелец ответил
   на все девять вопросов интервью №008 (все — вариант **А**; уточнения: **В8** тип файла уже —
   только `webp/jpeg`; **В9** iPhone откладывается, приоритет Android и десктоп). Разведка боевой
   конфигурации сделана **только чтением**, замеры — прибором `tools/probe-account-phase2.mjs`
   (лёгкий эмулятор Auth, тяжёлый стенд не нужен, сироты не остаётся).

   🔴 **Четыре находки фазы 2, две из которых меняют план:**
   - **EEP в бою ВКЛЮЧЕНА** (`enableImprovedEmailPrivacy: true`) — главная развилка закрыта фактом.
   - 🔴 **`providerData` НЕ отличает «есть пароль» от «входит по ссылке»** — у обоих `password`
     (типы SDK: *«Always set to ProviderId.PASSWORD, even for email link»*). **Механизм под решение
     В1 неисполним;** фаза 7 начинается с вопроса «чем отличить», а не с вёрстки.
   - 🔴 **Код отмены смены почты на эмуляторе не рождается** (2 из 2), хотя почта менялась. В бою
     шаблон отката настроен и по-русски — проверяется только боем; фазу 5 планировать от худшего.
   - ✅ **Блок «CORS бакета только GET» ложный:** замер заголовками дал `POST, GET, HEAD, DELETE,
     PATCH`. Загрузка и удаление не блокированы (`PUT` в списке нет — довод за `uploadBytes`).

   Наследие в конфигурации: **Action URL смотрит на `ndim-space.firebaseapp.com`**, а не на наш
   дом; у шаблона подтверждения почты **английская тема при русском теле** (правится в консоли).
   ⚠️ Замер «сброс пароля для аккаунта без пароля» **честно провален** (EEP на эмуляторе не
   включилась, HTTP 404) — остаётся `[NOT-TESTED]`.

   ✅ **ФАЗА 3 ЗАКРЫТА 2026-08-01 — МАКЕТ УТВЕРЖДЁН** (`plans/17`). Владелец выбрал **V2 «Одна
   страница»** с правкой **«с виджетами»**. Форма эпика зафиксирована: отдельный маршрут ·
   подэкранов нет, секции раскрываются на месте · страница собрана **виджетной сеткой** по модели
   «шага» (`ideas/24`): содержательные виджеты по 2 шага, **«Удаление аккаунта» — во всю панель
   (4 шага), последним** · виджет «Пароль» видят только те, у кого пароль есть · меню фото из
   четырёх действий открывается тапом по самому фото.
   Артефакты: `design/account-mockups.html` · 16 кадров `test-results/account-mockups/` ·
   **новый общий съёмщик `tools/shoot-mockups.mjs`** (правило четырёх макетов — одной командой).

   ⚠️ **Кадры поймали ЧЕТЫРЕ дефекта в самих макетах, все до реализации:** меню фото закрывало
   сравниваемое (дважды) · «десктоп» был колонкой 430px вместо оболочки 1440 · диалог V4 уезжал
   за край (два конфликтующих правила) · **в утверждённом V2 кнопка «Удалить аккаунт»
   растягивалась во всю панель** и становилась самой широкой мишенью экрана (`EXP-0110`).

   ✅ **ФАЗА 4 ЗАКРЫТА 2026-08-01 — ЭКРАН ЕСТЬ В КОДЕ** (`plans/18`). Заведён маршрут
   **`/account`** («Управлять аккаунтом» — инфинитив, канон 1.x), виджет **«Мой аккаунт»**
   (Email + признак подтверждения · «Создан» в формате 1.x · «Способы входа»), а строка
   **«скоро» в «Профиле» заменена живой ссылкой**. Данные берутся из Auth **без единого запроса
   к базе**. Страж `tools/verify-account.mjs` — **84 проверки, доказан 2 мутациями**; смоук
   `e2e/account.spec.ts`. Полный прогон: check/typecheck чисто, `npm test` 192/192,
   `npm run e2e` **74** (было 70). 🔴 **В бой НЕ деплоилось** — это решение владельца.

   ⚠️ **Строку «Пароль не задан» с макета написать оказалось НЕЛЬЗЯ** — `providerData` этого не
   знает (`EXP-0109`). Экран честно молчит про пароль, и **страж стережёт именно молчание**:
   мутация «дописать про пароль» его роняет. Формулировка ждёт ответа владельца на **В10**.

   ⚠️ **Граница фазы поправлена при исполнении:** реавторизация и словарь ошибок перенесены в
   фазу 5 — у них в фазе 4 нет ни одного вызывающего, а значит ворота «проверено живым
   браузером» неисполнимы. Приедут вместе со своим первым вызывающим — сменой почты.

   🟡 **ФАЗА 5 (`plans/19`) — В КОДЕ, ЖДЁТ ПРИЁМКИ БОЕМ.** Смена почты работает сквозным путём:
   виджет «Поменять Email» (тексты и порядок 1.x) → подтверждение личности → письмо на новый
   адрес → наша страница-приёмник `/auth/action` → **адрес реально меняется**. Реавторизация
   тремя путями и словарь ошибок словами 1.x — там же. Страж `tools/verify-account-email.mjs`
   **29/29**, доказан мутацией с названными адресатами (утвердительное «Письмо отправлено» →
   покраснели **ровно** 2 проверки нейтральности). Страж ядра 84/84 без регрессий,
   `npm test` 192/192, `npm run e2e` 74.

   💡 **Находка, улучшившая замысел:** ссылка подтверждения уходит на ТЕКУЩИЙ адрес — значит она
   и есть то подтверждение со старого адреса, которого требует OWASP без второго фактора. Это
   снимает зависимость от письма-отката Firebase, которое в замере фазы 2 не приехало ни разу.

   🔴 **Что честно НЕ проверено и требует боя:** режим `recover-email` (эмулятор такого кода не
   выдаёт) · поведение при ЗАНЯТОМ новом адресе (эмулятор не применяет запрет EEP) · Google-путь
   реавторизации (popup в headless не воспроизвести).

   ⚠️ **Поправка:** Action URL переключается **после фазы 7**, а не сейчас — приёмник умеет три
   режима из четырёх, сброс пароля приедет вместе с самим паролем. Ничего не блокирует: сегодня
   письма обрабатывает страница Firebase, и смена почты работает через неё.

   ⛔ **ПАРОЛИ ОТМЕНЕНЫ ЦЕЛИКОМ — решение владельца 2026-08-01:** «Убираем пароли полностью,
   оставляем только ссылки по почте и Google авторизацию». Возврат к его же канону (интервью
   №004, В2=A+). `bugs/32` закрыт как отменённый, **фаза 7 эпика вычеркнута**, вопросы В1 и В10
   сняты. Побочно: страница `/auth/action` стала ПОЛНОЙ (режим сброса пароля невозможен), и
   переключение Action URL больше ничего не ждёт.

   🔧 **Новая работа, которую породило решение: погасить пароли у 331 человека в бою.**
   Переключателем проекта не делается — email/password и email-link это ОДИН провайдер
   (`passwordRequired`: «If false, a user may sign in via either email/password or email link»);
   выключить его значит убить и вход по ссылке. Гасится по данным: `accounts:update` с
   `deleteAttribute: ["PASSWORD"]`. 🔴 `deleteProvider: ["password"]` НЕ ИСПОЛЬЗОВАТЬ — он снимет
   саму почтовую личность. Способ `[NOT-TESTED]`, операция необратима, руки владельца.

   ✅ **ФАЗА 8 ЗАКРЫТА 2026-08-01 — АККАУНТ УДАЛЯЕТСЯ** (`plans/20`). Каскад `src/lib/data/erase.ts`
   идёт **Storage → Firestore → `deleteUser`** и **останавливается на первой неудаче**: не
   удалилось — учётную запись не трогаем (обратно дефекту 1.x, где ошибки глушились). Подтверждение
   — ввод своей почты + повторный вход. Виджет во всю панель, красная кнопка только на финальном
   шаге. **Публичная страница `/delete-account`** доступна без входа и БЕЗ `noindex` (требование
   Google Play) — это **дверь, а не инструкция** (поправка владельца: «Почему "Как" удалить
   аккаунт? Зачем КАК?»). Стражи: `verify-account-delete.mjs` **22/22** (мутация «`deleteUser`
   первым» → краснеют ровно каскад и все пять проверок «ПОСЛЕ») + 5 тестов правил на «чужое не
   удалить» (**102** теста правил).

   🔴 **Разведка нашла следы шире, чем предполагал ответ В3:** свой `relations`, членство в ЧУЖИХ
   группах, подсказки аудитории у других, свои предложения измерений. Владелец ответил **В11 = А**:
   суточный проход вычислителя вычищает всё, а **предложения ОБЕЗЛИЧИВАЕТ** (`authorUid` стирается,
   текст остаётся — предложение полезно Пространству и после ухода автора).
   `cleanupDeletedPeople()` в `calculator/index.mjs`, **9 тестов**, доказан мутацией.

   ✅ **`bugs/04` ЗАКРЫТ 2026-08-01 — ПРАВИЛА STORAGE В БОЮ** (слово владельца «кати»).
   ⚠️ Транспорт не тот, что в плане: креды Firebase CLI протухли, перелогин интерактивный —
   выкат сделан **Rules API** (ruleset `f656a24d…`, релиз переключён). Это то же, что делает
   `firebase deploy --only storage` внутри. **Проверено чтением боя:** `match /admin` и
   `match /public` исчезли · `public/um.html` **403** (был открыт ВСЕМ) · `admin/adminFunctions.js`
   **403** · аватар без входа **403**, как и до выката (правило чтения не менялось).
   Откат готов: `.private/storage-1x-backup/storage.rules.PROD-BEFORE.txt` + прежний ruleset цел.

   **Следующее — фаза 6 (аватар): загрузить · заменить · удалить.** Блоков не осталось ни одного —
   ни правил, ни CORS (замер `researches/24` §7.4). «Открыть на весь экран» уже готово
   (`Avatar.svelte`), переделывать не надо. Приёмка на живом Safari отложена самим владельцем (В9).

## Сессия 2026-07-31 — `ideas/24`, фазы 1–2 (перенесено из STATUS 2026-08-01 дословно)

1. ✅ **`ideas/24`, ФАЗЫ 1–2 ЗАКРЫТЫ, ОБА ЭКРАНА ВЫКАЧЕНЫ В БОЙ 2026-07-31** (два слова
   владельца «деплой прод»). Боль «виджеты держат место, появляются дыры» — снята.

   | Экран | пустоты 1440 | пустоты 1024 | высота |
   |---|---|---|---|
   | Пространство | 26.8% → **22.2%** | 30.9% → **27.2%** | 932 → 902px |
   | Пространство, тихий день (случай со скриншота) | → **13.1%** | → **18.2%** | → 693px |
   | Профиль | 16% → **10.9%** | → **13.9%** | 776 → **619px** |

   **Признак в боевом CSS** (снят до и после выката): `/space` — `display:contents` **0 → 2**,
   `.col.svelte-*` **0 → 2**; `/profile` — `.head-who` **0 → 1**, `.col.svelte-*` **0 → 1**.
   Бой: `verify-prod-b4` **81/81** (дважды), `verify-bug81` 28/0, `verify-pwa` 24/0,
   `probe-bug67` зелёный. Телефон не изменился ни на пиксель.

   **На «Профиле» управление аккаунтом СЛИТО с карточкой-шапкой** (слово владельца): фото ·
   имя · место будущего «Статуса» (`plans/09`) · «Управление аккаунтом» · «Выйти». Отдельной
   карточки «Аккаунт» внизу больше нет — отмена части `ideas/19` того же дня, записана в саму
   идею. Гость БЕЗ документа защищён: у него шапки нет, ряды остаются своей карточкой, иначе
   замок `bugs/84`.

   **«МЕНЮ»: ДВЕ КОЛОННЫ, МАНИФЕСТ УДАЛЁН ЦЕЛИКОМ.** Слово владельца: «и виджет и страницу
   удаляем — это была отсебятина ИИ, я этого не делал в оригинальном НДим». Удалены виджет,
   страница `/menu/manifesto`, модуль `src/lib/content/manifest.ts`, стили и все проверки,
   которые их стерегли. **Текст не потерян:** «1. Манифест» — раздел РУКОВОДСТВА пользователя
   (`tools/extract-docs.mjs`), и его по-прежнему стережёт e2e-строка
   `{ path: '/menu/manual', marker: 'Манифест' }`.
   Колонны: слева «Вид», «Поделиться», «Документы» (450px), справа «Проект», «Версии» (325px) —
   разбиение выбрано ЗАМЕРОМ (первый вариант давал 250 против 525).
   Признаки в бою: `/menu/manifesto` **200 → 404** · «Манифест» в сыром HTML `/menu` **3 → 0** ·
   секций `.col` **1 → 2** · «Манифест» в руководстве **1 → 1** (текст цел).
   🔴 **Стражи не выброшены, а ПЕРЕВЁРНУТЫ:** `verify-batch4` и e2e теперь требуют, чтобы
   виджета, ссылки и страницы НЕ БЫЛО, — иначе следующая сессия «восстановит» удалённое, и ни
   один тест не возразит. Урок класса — `EXP-0107`, документ — `bugs/38_DONE`.

   ⚠️ **«В Пространстве с …» убрана из профиля ПОВТОРНО** — первый раз её убрал владелец
   (интервью №007 В10), второй раз она вернулась по вине агента: сливая ряды аккаунта с
   шапкой, он притащил подпись вместе с ними. **Урок класса: перенося блок, проверяй не только
   что он выглядит как раньше, но и что он не тащит за собой ОТМЕНЁННОЕ.** Признак в бою:
   `/profile` «В Пространстве с» **1 → 0**. Место под именем принадлежит `plans/09` («Статус»).

   🔴 **ЖДЁТ ВЗГЛЯДА ВЛАДЕЛЬЦА:** оба экрана ПОД ЛОГИНОМ — гостевые смоуки видят их без
   данных, а сетка живёт там, где есть цифры.

   ⚠️ **Капкан этого эпика, оплаченный дважды:** метрика пустоты считает пустоту МЕЖДУ
   карточками и НЕ ВИДИТ пустоту ВНУТРИ них. По ней «Профиль» выглядел здоровым (16%), и
   первый заход оставил его как есть; владелец посмотрел глазами и оказался прав. **Замер
   верен в границах, где снят: меняется состав виджетов — замер пересдают, а не цитируют.**

   **Следующее — фаза 3:** подсистема настроек человека (`ideas/16`), она же предусловие
   ручного перетаскивания. Трогает модель данных и правила Firestore; в `ideas/16` висит
   открытый вопрос владельцу (что главнее при расхождении — настройка браузера или профиля).
   Операционный план ещё не написан (номер `plans/14` — `plans/13` занят фото профиля).

   Утверждённая модель (реализована дословно, менять только по слову владельца):

   Сетка в **4 шага** по горизонтали ·
   **малый виджет = 1 шаг** (плитки метрик становятся самостоятельными виджетами) ·
   **большой = 2 шага** (содержательные карточки), при нужде 4 — во всю панель ·
   🔴 **шаг задаёт ТОЛЬКО ГОРИЗОНТ**: по вертикали колонка пакуется сама, карточка сохраняет
   естественную высоту, **искусственно не растягивать ничего** (прямое слово владельца) ·
   расположение как в V1: слева «Сегодня», «Сейчас», «Похожесть», справа «Сервер».
   Эталон — `design/widget-layout-mockups.html`, кнопка V4.

   Лестница эпика: `researches/22` (обзор индустрии) → `plans/10` (метаплан, 6 фаз) →
   `plans/11` (фаза 1 ✅) → `plans/12` (фаза 2 ✅, выкачена). План фазы 3 — **`plans/14`**,
   ещё не написан (`plans/13` занят фото профиля).
   Страж — `node tools/verify-ideas24.mjs` (**60 проверок** на двух экранах; `--mutant`
   возвращает старую сетку инъекцией и обязан дать **32 провала** — этим он и доказан).

   **Капканы этого эпика, проверенные замером** (не переоткрывать):
   - **`grid-auto-flow: dense` сам по себе бесполезен** — 4/4 замеров без пикселя разницы,
     пока все виджеты одного размера. При РАЗНЫХ шагах он работает. Опровергнут был кандидат,
     а не идея.
   - Отсеяны замером: `grid-lanes` (не Baseline в 2026, движок не знает), `columns: 2`,
     `span 2` — обе делали «Профиль» ВЫШЕ.
   - Дыра БОЛЬШЕ на узком десктопе — правку проверять сначала на 1024, а не на 1440.
   - Тема на геометрию не влияет ни на пиксель.
   - Урок фазы: техника, полезная на одном экране, бывает вредна на соседнем. Радиус решается
     замером на КАЖДОМ экране (`EXP-0104`).

   ⚠️ **Часть 2 идеи (перетаскивание) стоит на `ideas/16`** и на WCAG 2.2 SC 2.5.7 (AA):
   перетаскиванию обязана сопутствовать однопальцевая альтернатива. Разбор — `researches/22` §3.3.

## 2026-07-31 — аудит слабых моделей, тройной выкат и страница руководства

*(перенесено дословно из `STATUS.md` при закрытии чата 2026-07-31; см. также записи ниже)*

### ✅ АУДИТ + ТРОЙНОЙ ВЫКАТ 31.07, утро (слова владельца: «перепроверить адекватность и
работоспособность» · «деплой прод, перезапуск сервера» · «правила… перепроверей и выкатывай»)

**Аудит** (механика — `researches/20`): полный прогон всех гейтов (~900 проверок) + 6 читающих
ревизий по осям + состязательное опровержение каждой находки + критик полноты. Итог: **гейты не
соврали ни разу** (всё заявленное зелёным — зелёное; флейк `demo.spec` не воспроизвёлся за 100
повторов), но нашлись **12 настоящих дефектов, невидимых гейтам** (5 major) — все починены в тот
же день, каждый фикс доказан адресной мутацией; **9 находок ревизоров опровергнуты скептиками**
(документы решений владельца защитили). Уроки — `EXP-0096…0101`; отчёт разработчику KAIF о
механизмах УСИЛЕНИЯ слабых моделей — **`researches/20`** (заказ владельца).

| Выкачено | Признак ДО → ПОСЛЕ |
|---|---|
| **Hosting** (фиксы `bugs/95` `?guest=` не затирает сессию · `96` пин карточки · `101` свечения по теме + витрина 94) | `--glow-strong` в боевом CSS **0 → 2**; смоуки УЖЕСТОЧЁННЫМ стражем (`bugs/97`/`98`): **81/81 · 28/28 · 24/24**; e2e перед выкатом 72 passed |
| **Правила Firestore** (`bugs/99` дружба hasOnly+created · `100` админ НЕ читает оценки) | боевой ruleset **от 2026-07-12 → от 2026-07-31 06:11 UTC**; выгружен Rules API и сверен: **бой == репо байт в байт** (и до выката дрейфа не было); test:rules **97/97** |
| **Сервер синхронизации** (`bugs/91` проход переживает сбой · `92` пустой топ опустевшей точки · `93` транзакция dirty · `94` страж эмулятора) | `space/server.build` **19 → 20**; лог старта: «проект ndim-space, эмулятор: нет (боевой Firestore)» — новая строка стража `bugs/94`; test:calc **43/43** |

**Ночной полный проход** (проверка из прошлой эстафеты): состоялся **31.07 в 03:00:09**, боевой
`space/server`: `fullSync.at = 31 июля 03:00`, `nextAt = 1 августа 03:00` (читано Admin SDK) —
ровно ожидание владельца. «Записано топов — 0» — экономия работает.

⚠️ **Прогрев билда 20 разово дозаписал 237 топов** — это конвергенция, а не сбой: документы,
до которых diff-экономия не дотягивалась с 30.07 (в основном гостевые топы), получили поле
`updated` (`bugs/46`). Проверено фактами: повторный рестарт → «записано топов — 0»; в базе
теперь **2159/2159 строк топов с `updated`**. Следующие рестарты бесплатны.

**Хвост аудита, закрытый после паузы:** боевые пиксели иллюстраций пересчитаны (`network.webp`
ровно 0 белых — заявление выката подтверждено) · стражи `verify-bug95` (12) и `verify-bug96`
(10) написаны и доказаны мутациями — второй сразу окупился, поймав недоделанную первую редакцию
фикса (движок Svelte восстанавливает снапшот inline-стилей при отмене outro, `EXP-0100`).

**Что осталось владельцу (прежние нити):** карточка «Измерений» под логином (ореол/уезд 700 мс,
файлы на `D:\`) · установка PWA на телефон (`bugs/58`/`83`). Беклог аудита закрыт целиком.

**✅ `ideas/19` РЕАЛИЗОВАНА И ВЫКАЧЕНА В БОЙ (2026-07-31, слова «Делай» → «деплой»):** виджет
«Аккаунт» переехал из «Меню» вниз «Профиля» целиком (возврат канона 1.x «Дом»; прежний канон
«выход — в /menu» из bugs/29 отменён словом владельца — пометка в researches/12). «Меню» больше
не читает данных профиля (экономия). Стражи переехали с блоком: bug84 58/0 · bug87 60→**72/0**
(+«в Меню карточки нет») · bug40 71/71 · bug95/96 12/0 и 10/0 · wave11/12 28/0 и 108/0 · юниты
192 · e2e 72. Гость без документа не заперт (обработка ProfileMissingError переехала тоже).
**Выкат hosting:** снимок витрины свеж (94) · чистая пересборка · признак ДО→ПОСЛЕ: заголовок
«Аккаунт» в сыром боевом /menu HTML **1 → 0** · смоуки **81/81 · 28/28 · 24/24**. ⚠️ Сам виджет
на «Профиле» живёт под логином — гостевые смоуки его не видят, нужен взгляд владельца.
Решения без владельца — в идее.

**Попутно закрыт `bugs/103`** (ложная регрессия свипа): verify-ideas18 оставлял оценки в базе
стенда, verify-bug64 ждал константу — теперь стражи убирают за собой (встроенная проверка
уборки) и считают ожидания из данных; правило класса — в AGENT_GUIDE «База стенда — общая».

**За ней — `ideas/24`** (новая идея владельца 2026-07-31): виджеты схлопывают дыры —
автораскладка + ручное перетаскивание с фиксацией, раскладка в профиле в БД. ЭПИК: сначала
обзор индустрии (шаг 7б), потом проектирование; связка с `ideas/16` (настройки с дебаунсом).

**README переработан в руководство пользователя** (2026-07-31, по слову владельца «в NDim так же
README переработать» с образцом KPOT): академический регистр владельца («N.1. Основные
положения», нумерованные нормы, безличный залог), 8 разделов × 2 языка целиком, без истории
проекта — витрина и роадмап живут в STATUS/MASTER_PLAN. Каждое утверждение перевыведено из кода
(250 = `RELATIONS_TOP_LIMIT` · 30 дней = `GUEST_TTL_DAYS` · 0.7 = `PROXIMITY_EXPONENT` · до 10
кругов · 404 = сумма четырёх сюит), все 9 относительных ссылок проверены. Образец разведан по
KPOT: STATUS-запись «USER MANUAL» + вердикт владельца об old-README («устаревший фрод») +
фирменная шапка-витрина Unliminium. Вычитка владельцем — приветствуется.

### ✅ ВТОРОЙ ВЫКАТ 31.07, ночь — страница руководства (слово владельца: «Да, выкатывай»)

Три замечания, все закрыты ЗАМЕРОМ, прибор — `tools/measure-bug80-scale.mjs` (новый; это шаг 0
документа `bugs/80`, не снимавшийся полтора месяца).

| Замечание | Причина, найденная замером | Признак в бою до → после |
|---|---|---|
| «на десктопе малые элементы в таблице» | колонка документа растёт ×2.21 (347→768px), а звезда/цифра/смайлик были КОНСТАНТАМИ на обеих ширинах. Звезда руководства = 63% от звезды продукта | **17/13/18 → 27/18/27** на 1440; телефон не изменился |
| «это слишком огромное, в оригинале было маленькое» | `researches/12:323`: финальная картинка несла класс 1.x `small_image` (потолок 100px). Размер и загрузку решал ОДИН флаг — разведены на `kind:'small'` и `eager` | **298×300 → 99×100px** |
| «белый не весь удалён, в кружках остался» | удаление фона шло снаружи и в замкнутые кольца не попало | `network.webp` **4398 → 0** непрозрачных белых |

⚠️ **Найден второй файл, о котором владелец не говорил:** `island.webp` с белой полосой во всю
ширину (18646 → 3412; остаток — законная пена на воде). Класс проверен по ВСЕМ восьми
иллюстрациям; `logo-1x` (белая «N» на чёрной плитке) и мелочи самолёта/больницы — не дефект.

**Бой проверен живым браузером:** `verify-prod-b4` **81/81** · `verify-bug69 --prod` (графика не
появляется «на горячую») **зелёный** · боевые файлы картинок скачаны и пересчитаны: сеть 0,
остров 3412, знак 5429. Перед выкатом: e2e **72 passed**, юниты **192**, `svelte-check` 418
файлов 0 ошибок, `kaif:check` чист.

**Стражи:** 4 новые проверки в `verify-manual-v1.mjs`, доказаны ТРЕМЯ мутациями разом — 16
провалов на сломанной сборке, 0 на исправной.

Кадр «до/после» шкалы для владельца — `D:\ndim-шкала-до-после.png`.


## Записи (свежее сверху)

> 📦 **Миграция 2026-07-31 (обновление KAIF 2.0 → 2.1):** всё ниже перенесено ДОСЛОВНО из
> `STATUS.md` (2 900 строк → сводка ~260). Порядок — как лежало в STATUS: сначала сессии
> 30–31.07, затем летопись «Предыдущее обновление» (29.07 → 16.07 и глубже), в конце — ранние
> фазы и разделы первой недели (10–12.07). Внутренние заголовки и «эстафеты» записей — исторические:
> задачами они больше не являются.

### 🚀 ВЫКАЧЕНО В БОЙ 2026-07-30, 17:52 (слово владельца: «деплой прод и перезапусти сервер синхронизации»)

**Хостинг:** снимок витрины (`node tools/snapshot-landing-metric.mjs` — 94 человека, 30.07) →
чистая пересборка (`build/` снесён, EXP-0023) → гейты → `npx firebase deploy --only hosting
--project ndim-space` → **оба таргета release complete**. Правила Firestore, индексы,
`firebase.json` и `storage.rules` НЕ выкатывались (`--only hosting`), и за весь цикл они не
менялись — проверено диффом, 0 файлов.

**Признак снят ДО и ПОСЛЕ** (все пять — обязаны были ПОЯВИТЬСЯ):

| Признак | до | после |
|---|---|---|
| витрина «С нами уже …» в СЫРОМ HTML (`bugs/81`) | **0** | **«2 820 человек»** |
| `/manifest.webmanifest` | **404** | **200** |
| `/img/app/icon-512.png` | **404** | **200** |
| `<link rel="manifest">` в HTML | **0** | **1** |
| `/img/app/apple-touch-icon.png` | **404** | **200** |

**Бой проверен живым браузером:** `verify-prod-b4` **81/81** · `PROBE_BASE=https://ndimspace.app
verify-bug81` **28/28** · `PROBE_BASE=… verify-pwa` **24/24**.

**Сервер синхронизации перезапущен.** `npm run calc:image` → образ **билд 19** (в бою был 15,
от 27.07) → контейнер заменён с тем же окружением (`FIREBASE_PROJECT_ID=ndim-space`,
ключ сервисного аккаунта монтируется из `calculator/secrets`, `restart: unless-stopped`).
Первый цикл на боевом Firestore прошёл: *«прогрев кэша при старте: в Пространстве точек — 334»*,
*«записано топов — 95, в Пространстве людей 94, измерений 5112, связей рассчитано 2346»*,
ночной полный проход объявлен **в 00:00 UTC** (`bugs/85`).

⚠️ **95 переписанных топов — это ожидаемая РАЗОВАЯ перезапись**, а не сбой экономии: строка топа
получила новое поле `updated` (`bugs/46`), поэтому дифф увидел изменение у каждого. Следующие
циклы вернутся к обычному «записано топов — 0», когда никто не оценивает.

⚠️ **Что агент проверить НЕ МОЖЕТ и на что нужен Ваш взгляд:** `space/server` и `relations/{uid}`
по правилам закрыты от постороннего (403 гостю — это правильно). То есть новые строки виджета
«Последний запуск» (`bugs/86`) и «Последнее обновление NDim ID» в раскрытой связи (`bugs/46`)
видны только Вам, под логином. Посмотрите их глазами — это последняя непроверенная часть выката.

⚠️ **Git Bash ломает docker-пути.** `-e GOOGLE_APPLICATION_CREDENTIALS=/secrets/sa.json` из Git
Bash превращается в `C:/Program Files/Git/secrets/sa.json`, и контейнер падает с ENOENT. Запускай
`docker run` из **PowerShell** (или `MSYS_NO_PATHCONV=1`). Стоило одного лишнего перезапуска.

### ✅ ЗАКРЫТО ЗАМЕРОМ И ВЫКАЧЕНО (30.07, поздний вечер) — обе жалобы по карточке «Измерений»

**Ниже сохранена постановка, с которой сессия начиналась. Вот чем всё кончилось.**

Владелец **снял экран на камеру** (`D:\video5431525967346573958.mp4`) — из видео сразу стало
видно то, чего не было в словах: он смотрит на **ДЕСКТОПЕ в СВЕТЛОЙ теме**, а оба прошлых захода
проверялись в тёмной. Его правка процесса дословно: *«В следующий раз ТЫ САМ записываешь
экран!»* — записано в постоянную память агента, приборы теперь пишут `recordVideo`.

| Жалоба | Настоящая причина (найдена ЗАМЕРОМ) | Замер до → после |
|---|---|---|
| «тёмное вокруг смайлика… как ореол или тень» | `drop-shadow` золотом по БЕЛОЙ карточке — это тень, а не свет: `--star` #e8a516 имеет светлоту **0.44**, `--panel` — **1.00**. В тёмной теме то же золото светлее фона, и потому два захода, проверенные там, выглядели успешными | кольцо вокруг лица **0.912 → 0.997** при фоне 1.000 (соседи 0.997…0.999 — контроль) |
| «соседние не дожидаются, пока она уедет» | ДВЕ причины: `animate:flip` стартовал вместе с улётом; и карточка УКОРАЧИВАЛАСЬ до начала движения (уходили ряд смайликов и отсчёт) | соседка проходила половину пути на **0 мс** (1% пути карточки) → трогается на **682…684 мс** при уходе на **667…672 мс** |

**`TWINS:`** искал `drop-shadow` по `src/` — 2 места, оба этого класса. Второе (`.st.peak i`,
сама звезда) было ХУЖЕ первого (−0.107 против −0.088) и стоит прямо НАД смайликом. Починены оба.

**Лечение:** токен `--glow` в блоке темы (`transparent` в светлой — на белом свечения не бывает,
`var(--star)` в тёмной); задержка `animate:flip` на время жеста; синхронный пин высоты карточки
первым действием `commit()`. Длительность улёта **1200 → 700 мс** — владелец выбрал по записи
жеста из четырёх вариантов.

**Стражи (в `verify-bug80.mjs`) доказаны 4 мутациями:** вернул золотой ореол → 2 провала
(свечение 0.441 против фона 1.000); снял выделение выбранного лица → 1; снял задержку flip →
соседка на −50 мс; снял пин высоты → на −85 мс.

**Новые приборы:** `tools/measure-bug80-halo.mjs` (кольцо пикселей вокруг лица против фона,
соседи как контроль, пишет видео) · `tools/measure-bug80-flight.mjs` (обе карточки кадр в кадр,
ось движения выбирается замером) · `tools/probe-bug80-focusring.mjs` (снял ложного подозреваемого).

**Выкачено 30.07, признак в боевом CSS до → после:** `--motion-gesture` **1.2s → .7s** ·
`--glow` **отсутствовал → transparent · var(--star)** · ореол через `--star` **1 → 0** ·
ореол через `--glow` **0 → 2**. Бой проверен живым браузером: `verify-prod-b4` **81/81**,
`verify-bug81` **28/28**, `verify-pwa` **24/24**. e2e **72 passed** (вчерашний флейк
`demo.spec` не повторился — счётчик обнулён, третьего появления не было).

⚠️ **Что владелец должен посмотреть сам:** карточка «Измерений» живёт под логином, гостем её не
увидеть. Файлы для просмотра: `D:\ndim-ореол-до-после.png` (кроп «до/после») и
`D:\ndim-уезд-карточки-700мс.mp4` (запись жеста).

**Два ложных подозреваемых, снятых замером** (чтобы к ним не возвращаться): тёмно-синяя рамка
вокруг звезды на видео — штатный `:focus-visible`, мышью не появляется ни в одном из четырёх
сценариев, убирать нельзя (доступность); плашка `:hover` `--edge-soft` (светлота 0.897), из-за
которой прибор один раз объявил починенного близнеца сломанным.

<details>
<summary>Как задача была поставлена до замера (гипотезы прошлой сессии — обе оказались не тем)</summary>

#### Жалоба 1 (новая, и владелец сам назвал причину — она похожа на правду)

> «Я понял в чём проблема с уездом карточки. Соседние слишком быстро занимают её место, не
> дожидаются, пока она уедет — баг. И стал уезд слишком медленным.»

**Гипотеза агента (НЕ проверена, замер не снят):** карточка уезжает `MOTION.gesture` = 1200 мс,
а соседи подтягиваются `animate:flip` = `MOTION.slow` = 320 мс — и стартуют они, судя по всему,
одновременно. Плюс перед улётом схлопываются ряд смайликов и строка отсчёта (`pending = null`
в `commit()`), то есть карточка ещё и УМЕНЬШАЕТСЯ до старта движения — соседи могут ехать вверх
уже от этого.

**Как мерить (прибор писать заново, `test-results/` выжигается прогоном e2e — EXP-0062):**
покадрово снять `y` ВТОРОЙ карточки ленты и `transform.x` первой одновременно, от тапа
«Сохранить сейчас» до исчезновения. Дефект = вторая карточка начала подниматься раньше, чем
первая ушла за край (или закончила движение сильно раньше).

**Что уже известно про сам улёт (замерено):** движение стартует мягко, к 1036 мс — 294px из 480,
дальше разгон; непрозрачность 1 держится до 61% пути; всего ≈1170 мс. Владелец говорит, что это
**слишком медленно** — то есть длительность надо СНИЖАТЬ (ручка `MOTION.gesture` в
`src/lib/ui/motion.ts` + CSS-зеркало `--motion-gesture` в `src/routes/+layout.svelte`), а
подтягивание соседей — ЗАДЕРЖИВАТЬ.

#### Жалоба 2 (третий раз подряд, и агент дважды промахнулся мимо причины)

> «И тёмное вокруг смайлика при выборе звезды осталось»

Что уже сделано и НЕ помогло: (а) ореол сменён с серо-синего `--faint` на золото `--star`
(первый заход); (б) база прозрачности ряда поднята `.55 → .8`, чтобы выбранный не выглядел
единственным цветным (второй заход). Оба выкачены в бой, владелец видит проблему после них.

**Чего агент НЕ проверил и с чего начинать:** снять ПИКСЕЛИ вокруг выбранного лица (кроп
`deviceScaleFactor: 6`) и сравнить светлоту кольца вокруг него со светлотой фона карточки — в
обеих темах и **на том устройстве/теме, где смотрит владелец** (это стоит у него спросить: телефон
или десктоп, светлая или тёмная). Возможные подозреваемые, ни один не проверен: тень
`drop-shadow` в браузере владельца рендерится иначе; плашка `.st:hover` (`--edge-soft`) под
звездой на десктопе с мышью; контраст самого лица (цвета шкалы 1.x на его экране).

⚠️ **Правило для третьего захода:** сначала показать владельцу КРОП экрана и спросить «это оно?»,
и только потом править. Два захода вслепую уже потрачены.

**Состояние на момент паузы:** обе правки второго захода **ВЫКАЧЕНЫ В БОЙ** (`--motion-gesture`
`.9s → 1.2s`, база ряда `.55 → .8`, ореол золотой) — проверено признаком в боевом CSS, смоук
`verify-prod-b4` 81/81. Дерево чистое, всё запушено. Стенд ПОГАШЕН.

⚠️ **e2e мигает второй раз подряд на одном тесте** (`e2e/demo.spec.ts` → «язык: EN переименовывает
персонажей»): в общем прогоне 71 passed вместо 72, в изоляции 5/5 зелёных. Это уже не случайность —
при следующем появлении заводить баг (родня по классу — `bugs/67`).

</details>

### 🚀 ВТОРОЙ ВЫКАТ 30.07, ~21:40 — правка анимаций `bugs/80` (слово владельца: «деплой прод»)

Владелец посмотрел бой и дал две правки: «странно пульсирует тёмный смайлик внизу — нужно более
медленное и светлое пульсирование» и «карточка плохо уезжает по автосейву, быстро и быстро
растворяется; по кнопке лучше, но всё равно слишком быстро». Сделано, проверено, выкачено.

**Признак снят ДО и ПОСЛЕ прямо в боевом CSS** (⚠️ значения минифицированы — искать `.64s`/`.9s`,
а не `640ms`):

| Признак | до | после |
|---|---|---|
| `--motion-gesture` (длительность улёта карточки) | **`.64s`** | **`.9s`** |
| ореол выбранного смайлика | `--faint` 45% (тёмный) | **`drop-shadow(0 0 7px color-mix(in srgb, var(--star) 60%, transparent))`** — золото звезды |

**Бой проверен живым браузером:** `verify-prod-b4` **81/81** · `verify-bug81` по бою зелёный ·
`verify-pwa` по бою **24/24**.

⚠️ **Что в бою проверить нельзя гостем:** сами анимации живут на `/dims`, а он под логином.
Числа доказаны на стенде покадрово (карточка проходит 285px из 480 при непрозрачности 1;
раньше гасла до 0.2 за 230 мс), в бою доказан выкат КОДА этих правок.

⚠️ Первый прогон `npm run e2e` перед выкатом дал один флейк (`demo.spec` → «язык: EN
переименовывает персонажей»), повторный — **72 passed, 0 провалов**. Если он замигает снова,
это кандидат в баг: у него уже есть родня по классу (`bugs/67`).

### 🔍 ПРОВЕРКА ВЛАДЕЛЬЦА ПОСЛЕ ВЫКАТА (30.07, вечер) — что он увидел

**✅ `bugs/46` подтверждён в бою.** Владелец прислал раскрытую связь: строка «Последнее обновление
NDim ID → 25 ноября 2025 г. в 12:41» на месте, все шесть блоков досье 1.x собраны.

**✅ `bugs/86` подтверждён:** «Последний запуск — 30 июля в 20:52» это время ПОДЪЁМА контейнера
(17:52 UTC), а не последнего цикла.

**⏳ ОДНА СТРОКА ПОКАЗАНА СТАРЫМ ЗНАЧЕНИЕМ, и это ожидаемо:** «Полная синхронизация →
Запланированная **31 июля в 13:03**». Должно быть **03:00** (00:00 UTC, ответ владельца В6).

Причина, по коду: блок `fullSync` (включая `nextAt`) записывается **только во время ночного
прохода** — `calculator/index.mjs`, `const syncBlock = nightly ? { fullSync: … } : { partialSync: … }`.
Последний ночной прошёл в 10:03 UTC ещё **сборкой 15**, по её старой формуле «последняя + 24 ч».
Новая сборка 19 при старте объявила в логе «ночной полный проход в 00:00 UTC», но переписать блок
ей нечем до самого прохода.

> **🔴 ЧТО ПРОВЕРИТЬ ПЕРВЫМ ДЕЛОМ СЛЕДУЮЩЕЙ СЕССИИ (31.07 после 03:00 по местному):**
> открыть «Пространство» **с перезагрузкой страницы** (цифры сервера кэшируются на сессию,
> `FRESH.server = Infinity`) и посмотреть блок «Полная синхронизация». Ожидание:
> «Последняя успешная — 31 июля в 03:00», «Запланированная — 1 августа в 03:00».
> **Если там снова 13:03 — это НАСТОЯЩИЙ дефект**, заводить баг: значит ночной проход не
> случился либо `nextScheduledFullPass` считает не то. Проверять по логу контейнера:
> `docker logs ndim-calculator | Select-String "ночной"`.

### 🎯 ГЛАВНОЕ ДЛЯ СЛЕДУЮЩЕГО ЧАТА

**Состояние на 30.07, 17:55: в бою свежее, дерево чистое, беклог агентских задач пуст.**
Прод содержит ВСЮ работу 29–30 июля (В1, В2, В5, В6, В10, `bugs/87`, В9, В12б, В3+В4, В11,
`bugs/81`, `bugs/88`, `bugs/90`, `bugs/67`, `bugs/46`, манифест PWA), сервер синхронизации —
билд 19. Доказательства выката — в разделе выше.

**✅ Пять замечаний владельца ЗАКРЫТЫ и выкачены** двумя выкатами — разделы выше. Открытых
нитей две, обе на владельце:
1. **`/dims` под логином** — ушло ли тёмное вокруг смайлика и хорош ли уезд на 700 мс.
   Гостем эту карточку не проверить; файлы для просмотра лежат на `D:\`.
2. 🔴 **31.07 после 03:00 — блок «Полная синхронизация»** на экране «Пространство»
   (детали и критерий дефекта — в разделе «🔍 ПРОВЕРКА ВЛАДЕЛЬЦА» ниже). Открывать
   С ПЕРЕЗАГРУЗКОЙ страницы: цифры сервера кэшируются на сессию.

**Урок этой сессии, который дороже самих фиксов:** три захода словами дали три промаха, а
замер дал причину с первого раза. Правило владельца — **агент сам записывает экран** — теперь
в постоянной памяти и в приборах (`recordVideo`). Подробности: `EXP-0093`, `EXP-0094`.

**Затем — досмотреть выкат.** Владелец уже проверил две вещи из трёх (см. раздел
выше): `bugs/46` и `bugs/86` в бою работают. Осталось:
1. 🔴 **31.07 после 03:00 — блок «Полная синхронизация»** (детали и критерий дефекта в разделе
   «🔍 ПРОВЕРКА ВЛАДЕЛЬЦА» выше). Это единственная незакрытая нить выката;
2. приложение ставится на телефон заново, иконка и сплэш не пикселят (`bugs/58`, `plans/07` B2);
   старое приложение 1.x обновить нельзя — ключ подписи изъят (`bugs/02`). Нужен телефон владельца.

**Дальше работа есть только по ответам владельца.** Автономных задач беклог не содержит: этот
цикл выгреб всё, что можно было сделать без него. Полный список вопросов — раздел
«⛔ ЧТО ЖДЁТ ВЛАДЕЛЬЦА» ниже; самые дорогие из них — контракт кэша (`bugs/74` часть А, без него
память вида не работает после оценки) и развилки PWA (`researches/19` §5).

⚠️ **Если владелец даст новую волну или ответы — начинай с них, а не с поиска работы в беклоге.**
Открытые документы `bugs/` без тега DONE — это либо ждущие владельца развилки, либо крупные фичи
(`bugs/16`, `19`, `31`, `32`, `44`, `45`, `62` — им нужны макеты и решения, `AGENT_GUIDE` →
«Дизайн»), а не «взял и сделал».

### 🆕 Что сделал этот чат (30.07, автономный цикл, 12 коммитов)

**Волна владельца `ideas/21` — последние пункты:**

| Что | Как проверено |
|---|---|
| **`bugs/88`** (найден по дороге) — тёплая лента «Измерений» выбрасывалась на КАЖДОМ возврате: `rebuilt = screen !== data` истинно всегда, потому что `data` — глубокий прокси `$state`, а `screen` — сырой объект из кэша. Лечение — `$state.raw`. Замер: было 44 карточки → 12, стало 44 с нулевого кадра | стражем `verify-plan08` + трассой по кадрам |
| **В11 / `plans/08`** — память ВИДА экранов: все пять открываются там, где их оставили, «Измерения» помнят вкладку и глубину каждой вкладки. Тонкий слой `src/lib/ui/view-memory.ts` поверх `beforeNavigate`/`afterNavigate`, память в кэше сессии под `view:` | `verify-plan08` **60/0** (до кода 8 провалов), **3 мутации** |
| **`bugs/81`** — витрина лендинга больше не доезжает «на горячую»: число берётся из снимка перед выкатом и лежит в ПРЕРЕНДЕРЕ. Заодно её видят поисковики, а лендинг не стоит чтения Firestore | `verify-bug81` **28/28** по собранному сайту, мутация → 5 провалов |
| **`bugs/78`** (дыра, найденная судьёй) — механизм обновления снова проверяем: юнит на ключи + дверь стенда `window.__ndimRefresh` | `verify-ptr` **0 провалов** (65…66 проверок — число НЕ детерминировано, судить по нулю), две мутации (2 и 8 провалов) |

**Долги, до которых руки не доходили месяцами:**

| Что | Как проверено |
|---|---|
| **`bugs/04` шаг 3** — у правил Storage появились ТЕСТЫ (лежали черновиком без единой проверки) | `test:rules` **85 → 94**, 3 мутации правил |
| **`plans/05` хвост** — «тест-запрет литерала» на лендинге, ненаписанный полтора месяца. Вышел строже: число витрины обязано быть КРАТНО снимку боевой метрики | юнит **192** + e2e **72**, мутации на обоих уровнях |
| **`bugs/46`** — «Последнее обновление NDim ID», последний блок досье связи. Числа не было у экрана по устройству ПРАВ; кладёт его вычислитель в строку топа | `test:calc` **34 → 37**, мутация «время владельца в каждой строке» → 3 падения |
| **`researches/19`** — обзор индустрии по PWA (канон, шаг 7б). **Разблокировал эпик `bugs/83`** | по первоисточникам, ссылки у каждого утверждения |
| **`plans/07` B2 + `bugs/58`** — приложение снова ставится на телефон: манифест + иконки 192/512/maskable/apple-touch, растятся из утверждённого знака | `verify-pwa` **24/24**, 3 мутации |

**Три дефекта, которых никто не искал, — их вскрыла РЕВИЗИЯ СТРАЖЕЙ:**

| Что | Как проверено |
|---|---|
| **`bugs/89`** — корень вакуумного КЛАССА проверок: автовход стенда **вытеснял живую сессию**, поэтому вход по почтовой ссылке был ненаблюдаем принципиально, а вход новичка молча подменялся dev-аккаунтом | мутация «вернуть безусловный автовход» → проверка личности краснеет |
| **`bugs/90`** — 🔴 регрессия от В12б, видимая человеку: **тап по главе руководства никуда не вёз**. Панель глав получила запись истории, её закрытие — popstate, а платформа на popstate возвращает прокрутку записи и отменяла переход | `verify-manual-v1` давал **12 провалов**, теперь зелёный |
| **`bugs/67`** — двухдневный флейк e2e (рельс на 1px ниже шапки) закрыт со второй попытки: запасные `56px` не соответствовали шапке ни на одной ширине (замер: 61 и 55) | **4 падения из 18 → 0 из 18, четыре круга подряд (72 прогона)** |
| Плюс оживлены два СГНИВШИХ стража (`verify-bugs08` был красным месяцами и падал исключением; `verify-bug42` защищал ОТМЕНЁННЫЕ владельцем подписи) и закрыто слепое пятно `probe-bug67` | все зелёные |

### 🆕 Что сделал автономный цикл 30.07 (после `/end-chat` прошлой сессии)

| Что | Как проверено |
|---|---|
| **`bugs/88`** (найден по дороге) — тёплая лента «Измерений» выбрасывалась на КАЖДОМ возврате: `rebuilt = screen !== data` истинно всегда, потому что `data` — глубокий прокси `$state`, а `screen` — сырой объект из кэша. Лечение — `$state.raw`. Замер: было 44 карточки → 12, стало 44 с нулевого кадра | стражем `verify-plan08` + трассой по кадрам |
| **В11 / `plans/08`** — память ВИДА экранов: все пять открываются там, где их оставили, «Измерения» помнят вкладку и глубину каждой вкладки. Тонкий слой `src/lib/ui/view-memory.ts` поверх `beforeNavigate`/`afterNavigate`, память в кэше сессии под `view:` | `verify-plan08` **60/0** (до кода 8 провалов), **3 мутации** |
| **`bugs/81`** — витрина лендинга больше не доезжает «на горячую»: число берётся из снимка перед выкатом и лежит в ПРЕРЕНДЕРЕ. Заодно её видят поисковики, а лендинг не стоит чтения Firestore | `verify-bug81` **28/28** по собранному сайту, мутация → 5 провалов |
| **`bugs/78`** (дыра, найденная судьёй) — механизм обновления снова проверяем: юнит на ключи (**189** тестов) + дверь стенда `window.__ndimRefresh` | `verify-ptr` **0 провалов** (число проверок плавает 65…66), две мутации (2 и 8 провалов) |

### 🆕 Вторая половина цикла: ревизия стражей вскрыла три дефекта

Эстафета просила поискать родню вакуумной проверки (`EXP-0087`). Нашлась — и не одна.

| Что | Как проверено |
|---|---|
| **`bugs/89`** — корень вакуумного КЛАССА был в продукте: ветка стенда `currentSession()` делала безусловный `signInDev()`, то есть автовход **вытеснял живую сессию**. Вход по почтовой ссылке был ненаблюдаем принципиально, а вход НОВИЧКА молча подменялся dev-аккаунтом. Теперь живая сессия настоящего аккаунта уважается | мутация «вернуть безусловный автовход» → проверка личности краснеет. `verify-bug84` 58/0, `verify-bug87` 60/0 |
| **`verify-bugs08` был КРАСНЫМ давно и молча** — его не гоняли месяцами: три проверки `check(имя, true)` (зелёные по написанию), ширина шапки требовала ровно 390/800px при живом `scrollbar-gutter` (−15px), селектор кнопки устарел с `bugs/84` и ронял прогон исключением | всё исправлено, страж зелёный |
| **`bugs/90`** — 🔴 РЕГРЕССИЯ ОТ В12б, видимая человеку: тап по главе руководства **никуда не везёт**. Панель глав получила запись истории, её закрытие — popstate, а SvelteKit на popstate возвращает прокрутку записи (0) и отменял переход | `verify-manual-v1` давал **12 провалов**, теперь зелёный |
| **`verify-bug42` защищал ОТМЕНЁННЫЕ владельцем подписи** («Из них обновлено», «Связей рассчитано», «Следующий цикл») и падал исключением на двух совпадениях «Запланированная» | переписан по блокам, зелёный |
| **`bugs/67`** — двухдневный флейк e2e (рельс на 1px ниже шапки) закрыт со второй попытки: запасные `56px` не соответствовали шапке ни на одной ширине (замер: 61px на узких, 55px от 1024px). Одна переменная `--bar-h-fallback` с медиазапросом | **4 падения из 18 → 0 из 18, четыре круга подряд (72 прогона)**; зонд 41/41 → 0/39 |
| **Слепое пятно зонда `probe-bug67`** («до гидратации И с прокруткой»), из-за которого провалилась попытка №1, закрыто | мутант «запасное 0px» даёт 8 кадров, где рельс на 55px ПОД шапкой. Первая редакция блока была ВАКУУМНОЙ — поймано парной проверкой «реально ли прокручено» |

### 🆕 Третья часть цикла: закрыты долги, до которых руки не доходили месяцами

| Что | Как проверено |
|---|---|
| **`bugs/04` шаг 3** — у правил Storage появились ТЕСТЫ. Они лежали черновиком без единой проверки, и выпускать их было нельзя: на чтении аватаров держится показ лиц. 8 проверок, отказы первыми: чужое фото не перезаписать (и гостю тоже) · не вошедший не читает ничего · всё вне `users/{uid}/avatar/**` закрыто УМОЛЧАНИЕМ · паритет 1.x «вошедший видит чужое фото» зафиксирован тестом, чтобы ужесточение было СОЗНАТЕЛЬНЫМ | `test:rules` **85 → 94**, три мутации правил (2, 1 и 3 падения) |
| **`plans/05` хвост** — «тест-запрет литерала» на лендинге, стоявший ненаписанным полтора месяца. Вышел строже задуманного: число витрины обязано быть КРАТНО снимку боевой метрики — выдуманный литерал этому не удовлетворяет | юнит **189 → 192** (мутация `return 2184` → 2 падения) + e2e **70 → 72** (мутация даёт внятный текст «отношение 23.23 — похоже на выдуманное число») |
| **`plans/05` пункт 3** — мутации на тесты `bugs/07`, тоже висели | снял запись `public_metrics` из вычислителя → **6 падений** `test:calc`; ослабил правило записи витрины → падает ровно «🔒 витрину нельзя переписать с клиента» |
| **`bugs/46`** — «Последнее обновление NDim ID», последний блок досье связи, который был за агентом. Числа не было у экрана по УСТРОЙСТВУ ПРАВ: чужой `points/{uid}` зрителю недоступен. Вычислитель кладёт `updated` соседа в строку топа; поле необязательное — старые боевые `relations` читаются как прежде | `test:calc` **34 → 37**; мутация «время владельца в каждой строке» → 3 падения; `verify-tierA` смотрит на ДАТУ рядом с подписью, мутация «спрятать строку» → 2 падения |

⚠️ **Два операционных капкана, найденных на себе** (записаны в стражах и `AGENT_GUIDE`):
свой `vite preview` на 4173 подменяет сервер `npm run e2e` — полный прогон дал 12 разнородных
падений против СТАРОЙ сборки · у каждого файла тестов вычислителя обязан быть СВОЙ проект
эмулятора, иначе его точки попадают в чужое Пространство (моя первая редакция уронила
`guest_filter`).

**Прогнаны и зелёные ВСЕ стражи проекта:** batch2/3/4 · bug40 (71) · bug42 · bug49 · bug50 ·
bug51 · bug52 (64) · bug54 · bug57 · bug64 (21) · bug68 · bug69 · bug70 · bug75 (44) · bug76 (64) ·
bug77 (64) · bug80 · bug81 (28) · bug84 (58) · bug87 (60) · bugs08 · header-v3a · icons (27) ·
ideas18 · manual-v1 · plan08 (60) · ptr (66) · tierA · wave11 (28) · wave12 (108) · back-twins ·
probe-bug67 · `test:rules` **87/0** · `test:calc` **34/0**.
⚠️ `test:calc` сразу после гашения стенда дал 26 тестов и 1 провал — гонка освобождения порта
эмулятора, а не код: два следующих прогона 34/34. Записано в `AGENT_GUIDE`.

**Регрессия на конец цикла:** юнит **189/189** · typecheck **0** · svelte-check **0 на 416** ·
`npm run build` зелёный · `kaif:check` зелёный · **e2e 70 passed, 2 skipped** (гонялся при
ПОГАШЕННОМ стенде — иначе часть проверок краснеет ложно) · стендовые стражи `verify-bug69`,
`verify-bug70`, `verify-bug75` (44), `verify-bug76` (64), `verify-bug80`, `verify-bug84` (58),
`verify-bug87` (60), `verify-bug54`, `verify-ideas18`, `verify-back-twins` — все зелёные.
⚠️ **Стенд ПОГАШЕН** по дереву (`stand.log` удалён — сирот нет). Поднимать: `npm run stand`.
⚠️ `npm run e2e` пересобрал `build/` — **перед выкатом пересобрать начисто** (EXP-0023).

### 📜 ИСТОРИЯ (чат 30.07 до обеда): что тогда было главным — вакуумная проверка

> ⚠️ Это НЕ текущая повестка: урок усвоен и развит, класс закрыт (`bugs/89`, EXP-0090).
> Текущая повестка — раздел «🎯 ГЛАВНОЕ ДЛЯ СЛЕДУЮЩЕГО ЧАТА» в начале файла.

**Мутация вскрыла ВАКУУМНУЮ ПРОВЕРКУ в страже, который прошлая сессия сдала как доказанный.**
`verify-bug84.mjs` проверял «вошли ИМЕННО в свой аккаунт», заходя на `/menu` и читая почту. Но
**голый `/menu` на стенде входит `dev@ndim.space` САМ** (`currentSession()` → `signInDev()`, если в
URL нет двери `?as=`) — то есть проверка печатала нужную почту независимо от того, чем кончился вход,
и **не могла покраснеть никогда**. Мутант «выбрасываем намерение» дал 54 зелёных и 2 провала, оба на
консоли. Признак перенесён на пилюлю гостя в шапке (`.badge`) на той странице, куда привела ссылка —
там автовхода нет. **Правило на будущее (EXP-0087): мутация провалена, если краснеют только
побочные проверки.** Ищи родню этой ошибки в других стражах: любое «человек вошёл как X» на
дефолтном адресе стенда подозрительно.

### Что сделано в этом чате (11:14–14:15, автономно)

| Ответ | Что сделано | Страж |
|---|---|---|
| **В3+В4** | Две двери гостевой карточки («Сохранить мои результаты» / «У меня уже есть аккаунт») и **врезка подтверждения** вместо мгновенного выхода. Корень половины «б» жалобы вылечен: намерение живёт рядом с почтой (`account.ts` → `PENDING_INTENT_KEY`) и переживает письмо; при `intent=signin` гостевая сессия отпускается ДО `completeLoginLink`. Отказ «почта занята» переводит карточку в ОТДЕЛЬНОЕ состояние «Эта почта уже Ваша», а не дописывает красную строку; гостевой заголовок над врезками снят | `verify-bug84` **28 → 58**, мутация |
| **В9** | Ряд смайликов + замедленный улёт (подробнее ниже) | `verify-bug80` **44/44**, 2 мутации |
| **В12б** | «Назад» закрывает лайтбокс и панель глав | `verify-back-twins` **48/48**, мутация |

| Ответ | Что сделано | Страж |
|---|---|---|
| **В9** | Ряд из 11 смайликов в карточке «Измерений» — появляется, когда звезда выбрана и ждёт сохранения, исчезает по снятию выбора (дословно слово владельца). Лица — общим компонентом **`src/lib/ui/GradeFace.svelte`**: сниппет из `DocBlocks.svelte` переехал в него, копии разметки в проекте нет. **Улёт карточки замедлен вдвое** — новая длительность канона `MOTION.gesture = 640 мс` (+ CSS-зеркало `--motion-gesture`): уезд ЗА КРАЙ перестал делить хронометраж с перестроением списка | `verify-bug80` **44/44**, 2 мутации |
| **В12б** | «Назад» закрывает **лайтбокс фото** и **панель глав** — тем же штатным `pushState` + `page.state`, что окно «Как меня видят». Оба близнеца объявлены в `app.d.ts` | `verify-back-twins` **48/48**, мутация |
| попутно | Лог службы сервера синхронизации **врал** «00:00 по местному времени» при сравнении по UTC (`calculator/index.mjs:661`) | — |

**🔴 Находка, которая исправляет запись выше в этом же файле:** **В12 НЕ «без ответа»** — владелец
ответил на оба пункта ещё в коммите `57d65fe`, ответы лежат в интервью строками «Ответ: - ты прав…».
Прошлая сессия написала «В12 — БЕЗ ОТВЕТА», прочитав только поле `**Ответ:**` внизу вопроса.
**(а) ящик поиска — оставить открытым, это уже текущее поведение, работы нет.**

**⚠️ Приборы соврали в эту сессию ПЯТЬ раз** (уроки — `EXP-0086` и `EXP-0087`, читай их):
1. Вакуумная проверка автовхода стенда — см. «ГЛАВНОЕ» вверху. Самая дорогая.
2. Замер улёта карточки спрашивал «есть ли `transform`» и дал **1666 мс при заведомых 640** — то есть
   был **зелёным по неверной причине**: карточка несёт `transform` и в покое (его ставит
   `animate:flip`), а в замер попадали клик и запись в Firestore. Переписан на сдвиг вправо; трасса
   печатается флагом `--trace` и показывает `994:37 … 1610:480` — ровно 480px, **633 мс**.
3. Кадр тёмной темы застал ряд смайликов **посреди перехода `slide`** — лица срезаны сверху
   (`overflow: hidden`), и на скриншоте это выглядело точь-в-точь как дефект вёрстки. Я успел так и
   решить. `waitFor({state:'visible'})` возвращается, как только высота больше нуля.
4. Страж близнецов кликал по капсуле глав **до гидратации пререндеренной страницы** и краснел на
   исправном коде: `.pager` лежит в готовом HTML и находится селектором раньше, чем Svelte навесил
   обработчики.
5. **`innerText` в Chromium отдаёт текст УЖЕ применённым `text-transform`** — регистрозависимая
   проверка заголовка не находила «Войти в свой аккаунт?», потому что `h3` в карточке рисуется
   ПРОПИСНЫМИ. Сравнивай слова без учёта регистра: начертание — забота CSS.

**Состояние дерева на конец чата:** `main` чист и **запушен** · юнит **186/186** · typecheck **0** ·
svelte-check **0 на 414** · `npm run build` зелёный · `kaif:check` зелёный.
⚠️ **СТЕНД УПАЛ, а не оставлен поднятым** (поправка 14:35). Прощальный отчёт того чата обещал живой
стенд — обещание неверно: фоновая задача вышла с кодом 1, эмуляторы Firestore/Auth/Storage погашены
по SIGINT (`Script "node tools/seed-dev.mjs && node tools/stand.mjs" exited with code 1`).
**Поднимай его сам: `npm run stand`** (нужна Java). Причина падения не выяснена — упало уже после
всех прогонов, все стражи в тот день были зелёными на живом стенде. Если не поднимется — сначала
поищи сироту `node calculator/index.mjs` (`bugs/73`, `AGENT_GUIDE` → «Испытательный стенд»); признак
сироты — не удаляется `stand.log`, гасить ПО ИМЕНИ процесса, а не по площади (EXP-0081).

> 🔍 **Поправка к поправке — факт, снятый наблюдением (автономный цикл, 14:24).** Ко́д выхода 1
> получила ФОНОВАЯ ЗАДАЧА, а сам стенд в этот момент был **жив целиком**: слушали порты 8181,
> 9099, 9199 и 5173, в процессах стояли `firebase emulators:exec`, `node tools/stand.mjs`,
> `node calculator/index.mjs` и `vite dev`, все четыре отвечали HTTP 200. На нём и прошли ВСЕ
> прогоны этого цикла. То есть «стенд упал» — про задачу харнесса, а не про процессы; призрака
> искать не надо. Погашен он позже и НАМЕРЕННО — по дереву, перед прогоном `npm run e2e`
> (`stand.log` удалён, сирот нет). Признак, по которому это различать: `npm run stand` падает с
> «Port … is not open», значит прежний стенд ЖИВ, а не мёртв.
>
> ⚠️ **В этот день в одном рабочем каталоге работали ДВА чата.** Параллельная сессия сделала
> коммит `6b66523` (эту самую поправку) поверх коммитов автономного цикла и запушила их вместе
> со своим. Ничего не потерялось, но следующей сессии стоит знать: история `main` за 30.07
> писалась в два пера, и `git log` тут честнее любого отчёта.

### Что сделано этим утром (после ответов владельца)

Ответы закоммичены **дословно** до правок (`AGENT_GUIDE` шаг 16). Реализовано:

| Ответ | Что сделано |
|---|---|
| **В1=Б** | Блок «Данные» убран ЦЕЛИКОМ вместе с кнопкой обновления. ⚠️ Цена названа владельцу ДО решения и принята: пути обновления БЕЗ жеста больше нет (WCAG 2.2 SC 2.5.7), на десктопе остаётся перезагрузка страницы. Записано в коде и в страже — **это не регрессия, а следствие**. `verify-ptr` 47/47 |
| **В2=Б** | Витрина лендинга — живое число × множитель, как в 1.x. Взято ×30 (94 → ≈2 800), потому что множитель 1.x (862) дал бы 81 028 |
| **В5=А** | «Из них обновлено» → «Записано изменений», «Связей рассчитано» → «Связей в Пространстве» |
| **В6** | Час ночного прохода переведён на **UTC** — точный паритет с 1.x |
| **В10=В** | «В Пространстве с …» убрано из профиля; фича «Статус» расписана `plans/09`, без жалоб |
| **`bugs/87`** | Экран больше не врёт вошедшему гостю «Вы не вошли». Страж на ТРИ состояния — 60/60 |

**🔍 Две находки разведкой 1.x, которые изменили решения** (архив на диске: `d:/work/ai_sandbox/ndim_old`):

1. **Счётчик 1.x** (`app.js:2119-2122`): `userCount * 777 + * 77 + * 7 + userCount` = **×862**, и
   рядом собственный комментарий владельца **«TEST - временно, убрать»**.
2. **Час суточной сверки 1.x** (`app.js:10376-10378`): `nowUTC.getHours() === 0` с комментарием
   «00 по Лондону» — то есть фактически **00:00 UTC**. Летом Лондон = UTC+1, а проход был по UTC.
   ⚠️ Довод владельца («привязывался ко времени сброса лимитов Firebase») **фактом не
   подтверждается**: квоты Firestore сбрасываются около полуночи по ТИХООКЕАНСКОМУ времени.
   Привязка не работала и в 1.x. Поведение повторено, факт вынесен владельцу.

### Что осталось из ответов владельца (не начато)

· **В9=А с правкой** — ряд смайликов при выбранной несохранённой звезде (снимаешь выбор — ряд
  исчезает) + **замедлить анимацию улёта** сохраняемого измерения («сейчас слишком быстро, не
  понятно, что происходит»);
· **В3=А + В4=А** — врезка подтверждения выхода гостя и две двери в гостевой карточке
  (вторая половина `bugs/84`; механику намерения агент делает сам);
· **В11=А** — память прокрутки в живой вкладке (`plans/08`, `bugs/74`);
· **В12 — БЕЗ ОТВЕТА** (ящик поиска при смене вкладки; близнецы «Назад» — лайтбокс и панель глав).

### 🚀 ВЫКАЧЕНО В БОЙ (слово владельца: «деплой прод и конец чата»)

Гейты → чистая пересборка (`build/` снесён) → `npx firebase deploy --only hosting
--project ndim-space` → **оба таргета release complete**.

**Дифф выката проверен:** `firestore.rules`, `firestore.indexes.json`, `firebase.json` — **0
файлов**. В диффе есть `calculator/` и `src/lib/model/stats.ts`: модель едет с приложением
(добавлено необязательное поле), **а сервер синхронизации хостингом НЕ выкатывается** — см. ниже.

**Признак снят ДО и ПОСЛЕ, включая ОБРАТНЫЙ** (строка обязана была ИСЧЕЗНУТЬ):

| Признак | до | после |
|---|---|---|
| «Обновить данные» в чанках `/menu` (В1 — убрано) | **1** | **0** |
| «Записано изменений» в чанках `/space` (В5) | 0 | **1** |
| «Связей в Пространстве» / `startedUpAt` (В5, bugs/86) | 0 | **1** |
| «Не удалось загрузить Ваши данные» (bugs/87) | 0 | **1** |

**Сверка боя живым браузером:** `node tools/verify-prod-b4.mjs` — **81/81** · витрина лендинга
показывает **«С нами уже 2 820 человек»** (94 × 30, В2) · лица персонажей `opacity: 1,1,1`.

### 📜 ИСТОРИЯ: «сервер синхронизации в бой не поехал» — ✅ ПОЕХАЛ 30.07 в 17:52 (билд 19)

> ⚠️ Ниже — состояние на утро 30.07. Образ пересобран и контейнер заменён вечером того же дня,
> см. раздел «🚀 ВЫКАЧЕНО В БОЙ» в начале файла. Читать как историю, а не как задачу.

Правки `bugs/85`/`bugs/86` (ночной проход по UTC, `startedUpAt`, `checked` без гостей) живут в
`calculator/` и выкатываются **отдельно**, контейнером на серверном ПК владельца:

```
npm run calc:image        # соберёт образ с честным номером сборки
# затем заменить боевой контейнер
```

⚠️ **До этой пересборки** виджет «Пространства» в бою покажет «Последний запуск — **неизвестно**»
(поля `startedUpAt` в боевом `space/server` ещё нет) и прежнюю «Запланированную» от часа подъёма
контейнера. Это ожидаемо и честно, а не дефект.

### Мелочь, замеченная и НЕ тронутая

В «Меню», в карточке «Аккаунт», строка «В Пространстве с …» **осталась** — владелец просил убрать
её из ПРОФИЛЯ, про «Меню» не говорил. Спросить.

---

### ⚖️ Судейский проход перед push (что он поймал на МНЕ)

Прогнан по канону перед push. Все заявленные числа воспроизведены заново:
`verify-plan08` **60/0** · `verify-pwa` **24/0** · `verify-bug81` **28/0** · `verify-ptr`
**0 провалов** · `test:rules` **94** · `test:calc` **37** · юнит **192** · e2e **72** ·
правила Firestore, индексы и `firebase.json` за весь цикл **не тронуты** (дифф пуст).

Но две вещи он нашёл, и обе исправлены ДО push:

1. **`verify-bug81` зависел от поднятого стенда, и шапка об этом молчала.** Страж гоняется по
   собранному сайту, но `isStand()` смотрит на hostname — приложение целится в эмуляторы, и
   ВОРОНКА (`track('landing_view')`) пишет в Firestore. При погашенном стенде это
   `ERR_CONNECTION_REFUSED`, и страж краснел на исправном продукте: числа «28/28» были верны
   только в одном состоянии окружения. Теперь отказы эмуляторов прощаются **адресно** (по портам
   8181/9099/9199), любой другой упавший запрос остаётся провалом.
2. **Первая редакция этой же починки вышла СЛЕПОЙ:** фильтр выкидывал любое «Failed to load
   resource» — вместе с «responded with a status of 404», то есть с НАШИМ битым файлом. Сужен до
   сетевого отказа `net::ERR_`. Попутно выяснилось приятное: битую ссылку на свой файл ловит
   **сама сборка** — пререндер SvelteKit валится на ней раньше любого стража.

Плюс поправлена собственная неточность отчёта: `verify-ptr` цитировался числом «66», хотя его
число проверок **не детерминировано** (65…66) — это записано в этом же файле как правило, и я его
же нарушил. Судить его надо по «0 провалов».

> ## 🎯 ЧТО ОСТАЛОСЬ ПО ВОЛНЕ `ideas/21` — ТОЛЬКО ТО, ЧТО ЖДЁТ ВЛАДЕЛЬЦА
>
> Все 16 пунктов волны закрыты или разложены. Живого кода в ней больше нет; открыты ровно эти
> вопросы к человеку:
> · `bugs/80` подпункты **(а) звёзды в таблице манифеста** и **(в) адаптивный `clamp()`** —
>   вопросы 1 и 3 в разделе «⛔ Ждёт владельца», замер шага 0 не снят;
> · `bugs/84`: **текст ошибки** для ссылки БЕЗ намерения и расширенная фраза врезки (`[AI]`);
> · `bugs/74` **часть А** — контракт кэша: гасить ли собранный экран своей же оценкой
>   (вопросы 1 и 2). Это единственное, что мешает памяти вида работать после оценки;
> · `plans/08` вопросы **Б, В, Г** — агент взял их по своей рекомендации и помечает это честно,
>   нужна вычитка;
> · `bugs/83` — ссылка из письма мимо PWA: эпик, требует обзора индустрии (шаг 7б) и слова
>   владельца. PWA у 2.0 нет вовсе;
> · `ideas/23` / `plans/09` — публичный «Статус» вместо «В Пространстве с …»;
> · **мелочь:** строка «В Пространстве с …» осталась в «Меню» (владелец просил убрать из
>   ПРОФИЛЯ) — спросить.
>
> ✅ **Закрыты и проверены: В9, В12б, В3+В4, В11, `bugs/81`, `bugs/78`, `bugs/88`.**
> Новые стражи, которые надо держать зелёными: `verify-bug80` (44) · `verify-back-twins` (48) ·
> `verify-bug84` (**58**, было 28).
>
> **Что осталось открытым по уже закрытым пунктам** (владелец на это НЕ отвечал):
> · `bugs/80` подпункты **(а) звёзды** и **(в) адаптивный `clamp()`** — вопросы 1 и 3 в разделе
>   «⛔ Ждёт владельца» документа живы, замер шага 0 не снят;
> · `bugs/84`: **текст ошибки** `profile/+page.svelte` для случая, когда ссылка приходит БЕЗ
>   намерения, и расширенная фраза врезки — она помечена `[AI]` и ждёт вычитки владельца;
> · ❓ **непроверенная гипотеза**: переживает ли одноразовый oob-код неудачный
>   `linkWithCredential`. Я её обошёл — дверь «Это мой аккаунт» просит НОВОЕ письмо, а не
>   переиспользует код. Проверить можно на стенде; если код живёт, отказ можно спасать на месте.

## 📜 ИСТОРИЯ: эстафета прошлого чата (ИСПОЛНЕНА 30.07 целиком)

> ⚠️ Всё, что она просила, сделано: В11 закрыт, `bugs/81` закрыт, близнецы пройдены, родня
> вакуумной проверки найдена. Ниже оставлено ради контекста — там записаны приёмы и ловушки,
> которые пригодятся снова. **Задачами это больше не является.**
> Прошлая сессия писала: «Ты продолжаешь волну владельца `ideas/21`… закрыто 14 из 16» — сегодня
> закрыто всё, что не требует владельца. ⚠️ Один её совет по-прежнему в силе: ответы владельца в
> `interviews/interview_007_ideas21_forks.md` стоят в ДВУХ видах — полем `**Ответ:**` и строками
> `Ответ: - …` под подпунктами; читай весь текст вопроса, а не одно поле.

1. ~~**В9 — ряд смайликов и скорость анимации**~~ (`bugs/80`) — **ЗАКРЫТО 30.07.**
2. ~~**В3+В4 — вторая половина `bugs/84`**~~ — **ЗАКРЫТО 30.07.** Намерение живёт в
   `account.ts` → `PENDING_INTENT_KEY`, путь входа выбирает ОНО; выход гостя двухступенчатый.
3. **В11 — память прокрутки** (`plans/08` + `bugs/74`, ответ А: живёт в живой вкладке,
   перезагрузка обнуляет). Замер: 3 экрана из 3 забывают позицию (ушёл с 900 → вернулся на 0).
   ⚠️ Признак программной прокрутки `restoringScroll` из `bugs/75` **обязан переехать** в общую
   механику — иначе дефект панели родится заново на каждом экране.
4. **`bugs/81`** — счётчик лендинга доезжает «на горячую» (кадр 0 — строки нет, кадр 17 — есть).
   Теперь чинить есть смысл: решение о самом числе принято (В2=Б, множитель ×30).
5. **`bugs/83`** — ссылка из письма мимо PWA. PWA у 2.0 **нет вовсе** (ни манифеста, ни SW, ни
   assetlinks) — у владельца установлено приложение 1.x. Эпик, требует обзора индустрии (шаг 7б).
6. ~~**В12 остался БЕЗ ОТВЕТА**~~ — **НЕВЕРНО, ответы есть** (см. верх файла): ящик поиска оставить
   открытым (уже так), близнецов «Назад» — пройти. **Оба близнеца пройдены 30.07.**

### 📜 ИСТОРИЯ: что нашёл судья после выката 29.07 — ✅ ЗАКРЫТО 30.07 (`bugs/78`)

**Механизм обновления данных остался БЕЗ ЕДИНОЙ исполняемой проверки.** Следствие В1, которое
никто не назвал заранее. `refreshNow()` теперь достижим ровно из одного места —
`src/lib/ui/PullToRefresh.svelte:98`, то есть только жестом, а жест **намеренно не эмулируется**
(синтетический `touchmove` доказывал бы работу прибора, а не продукта). Пока строка «Обновить
данные» жила, она была единственным исполняемым входом, и через неё страж доказывал, что
обновление реально идёт в базу. Инвалидация `relations:*` / `space` / `ratings:*` / `profile`
сегодня не покрыта ничем запускаемым. Варианты лечения расписаны в `bugs/78` (рекомендация: юнит на
`invalidateAll` + стендовая дверь `?refresh=1` по образцу `?as=guest`).

> ✅ **ЗАКРЫТО 30.07 автономным циклом** — сделаны оба пункта рекомендации: список ключей уехал в
> `data/cache.ts` → `invalidateRefreshable()` (юнит-тесты, 189) и заведена дверь стенда
> `window.__ndimRefresh` (только localhost). `verify-ptr` — **0 провалов** (проверок 65…66,
> число плавает по устройству прибора), мутации дали 2 и 8 провалов.
> Необследованным остался ровно один участок — обработчики touch; их проверяет рука владельца.

**Мелочь того же разбора:** у `tools/verify-ptr.mjs` **число проверок не детерминировано** — одна
пропускается, когда экран короче окна (`verify-ptr.mjs:188`). Прогоны дают 46…**51** (51 снят
30.07 — вилка шире, чем думали 29.07). Судить его надо
по «0 провалов», а не по фиксированному числу; в отчётах фиксированное число не цитировать.

**Предыдущее обновление:** 2026-07-29, ночь — 🌊 **ВОЛНА ВЛАДЕЛЬЦА `ideas/21` (16 ПУНКТОВ) РАЗЛОЖЕНА
НА БЕКЛОГ И ЧИНИТСЯ В АВТОНОМНОМ ЦИКЛЕ. ЗАКРЫТО 7 ПУНКТОВ, РАЗБЛОКИРОВАН ВХОД ВЛАДЕЛЬЦА В СВОЙ
АККАУНТ.**

> ⚠️ **Главное, что должна знать следующая сессия.** Дороже всех фиксов — две вещи.
> **(1) На стенде существовало РОВНО ОДНО состояние человека** — вошедший `dev@ndim.space`. Значит
> гостевые ветки и карточка входа (дверь для 331 человека из 1.x) **не проверялись НИКОГДА**. Ровно
> поэтому дожил до боя дефект «гость заперт». Заведены двери **`?as=guest` и `?as=none`**
> (`AGENT_GUIDE.md` → «Испытательный стенд»). Пишешь про гостя или вход — ходи через них.
> **(2) Приборы врали в этой сессии СЕМЬ раз**, и каждый раз в сторону «зелено на дефекте»:
> `getComputedStyle` у `::-webkit-search-cancel-button` отдаёт ширину поля (249px) и `appearance:
> auto` при погашенном крестике · встроенный **Chromium вообще не рисует** нативный крестик (страж
> переведён на `channel: 'chrome'`) · страж `bug69` **придерживал графику роутом, и задержка сама
> лечила симптом** · `hasBytes` у SVG опирался на то же событие `load`, что и баг · `history.length`
> **не убывает** при возврате · сессия Firebase живёт в **IndexedDB**, не в localStorage · переход
> счётчика `null → «4»` считался «одним значением».

### Что сделано этой сессией

**0. Волна принята по канону.** Оригинал владельца закоммичен дословно (`d9801fe`) ДО правок,
затем структурирован. Разведка по коду — **18 агентов** (9 кустов + состязательная сверка каждого),
все 16 пунктов подтверждены или честно помечены «нужен живой прогон». Заведено **16 документов**:
`bugs/74-86`, `ideas/22-23`, `plans/08`. Плюс `bugs/87`, найденный по дороге.

**1. 🔧 Прибор на всю волну — `node tools/probe-ideas21.mjs`.** Один прогон снимает «до/после» по
девяти измеримым пунктам. Им сделан замер до кода.

**2. ✅ ЗАКРЫТО 7 ПУНКТОВ ВЛАДЕЛЬЦА** (каждый — страж, доказанный мутацией):
· **п. 11** `bugs/82` — лица Насти, Макса и Алисы висели прозрачными НАВСЕГДА. Лендинг пререндерен,
  `load` у SVG-картинки проходит до гидратации, а `complete` у неё не существует → спрашиваем кэш
  браузера служебной `new Image()`. Страж `bug69` расширен тремя способами, потому что **был
  зелёным на этом дефекте**.
· **п. 7** `bugs/79` — 41 правило `:hover` в 11 файлах закрыто `@media (hover: hover)`; такого
  медиазапроса в репозитории не было **ни разу**. Видно только в тёмной теме и только тапом.
· **пп. 4+5** `bugs/77` — они КОНФЛИКТОВАЛИ: нативный крестик живёт лишь при фокусе, а закрытие
  Android-клавиатуры требует фокус снять. Своя кнопка очистки + `blur()` на сабмите. **64/64**,
  три мутации.
· **п. 2** `bugs/75` — панель уезжала от НАШЕГО ЖЕ `scrollTo` при смене вкладки. Признак
  программной прокрутки + форс показа — то, что владелец делал в 1.x тремя вызовами. **44/44**,
  две мутации в разные стороны (вторая стережёт, чтобы «починка» не прибила панель насмерть).
· **п. 3** `bugs/76` — окно «Как меня видят» не имело записи в истории. Штатное неглубокое
  маршрутирование SvelteKit (`pushState` + `page.state`), первое применение `$app/*` в проекте.
  **64/64**.
· **п. 13** `bugs/84` — 🔑 **владелец снова может войти в свой аккаунт.** Гостю дана «Выйти»
  (переиспользование готовой строки). Страж **28/28** проходит ВЕСЬ путь: гость → «Выйти» →
  карточка входа → ссылка на почту (oob-код у эмулятора) → он в СВОЁМ аккаунте.

**3. ⛔ Что ЖДЁТ ВЛАДЕЛЬЦА (агент не решает):** `bugs/74` контракт кэша · `bugs/78` что именно
убрать в «Данных» (конфликт с интервью №006 и WCAG) · `bugs/80` макеты шкалы · `plans/08` срок
жизни памяти состояния · `ideas/22` честность против заманухи (отменяет `bugs/07`) · `ideas/23`
публичный «Статус» · вторая половина `bugs/84` (подтверждение выхода, две двери, текст ошибки).

**4. 📌 Ответ на прямой вопрос владельца о цифрах сервера (`bugs/85`):** цифры
**непротиворечивы — врут ПОДПИСИ.** «Пользователей проверено» = пересчитано в памяти (бесплатно),
«Из них обновлено» = реально записано в Firestore (0 = ничьи связи не изменились), «Связей
рассчитано» = стоячий итог Пространства. А вот **ночного полного прохода действительно НЕТ**:
`fullPass` считается от `startedAt` процесса, якорь — момент подъёма контейнера (13:03), и любой
рестарт гонит полное вычитывание базы. Канон 1.x иной — `daily_sync в 00:00 по Лондону`.

**5. 🚀 ВЫКАЧЕНО В БОЙ** (слово владельца: «деплой прод и дальше работаем»). Гейты → чистая
пересборка (`build/` снесён) → `npx firebase deploy --only hosting --project ndim-space` → **оба
таргета release complete**.

**Дифф выката проверен, а не заявлен:** `git diff e0248a5..HEAD -- calculator firestore.rules
firestore.indexes.json firebase.json src/lib/model` → **0 файлов**. Сервер синхронизации, правила,
модель и индексы НЕ тронуты.

**Выкат доказан признаком, снятым ДО и ПОСЛЕ** (снимок «до» сделан ДО деплоя специально):

| Признак | до | после |
|---|---|---|
| «Очистить поле поиска» (своя кнопка очистки, `bugs/77`) | 0 чанков | **1** |
| `hover:hover` в боевом CSS (`bugs/79`) | **0** | 2 |
| `seeMe` / `pushState` в чанках профиля (`bugs/76`) | 0 | **1** |

**Сверка боя живым браузером:** `node tools/verify-prod-b4.mjs` — **81/81** ·
`PROBE_BASE=https://ndimspace.app node tools/verify-bug69.mjs --quick` — **все зелёные** ·
точечно лица персонажей на боевом лендинге: `opacity: 1`, класс `ok` у всех трёх, кадр
`test-results/ideas21/prod-map.png` **смотрен глазами** — Алиса, Настя и Макс на месте.

⚠️ **Чего гость проверить НЕ МОЖЕТ:** вход в свой аккаунт (`bugs/84`), панель «Измерений»,
окно «Как меня видят» — всё это под логином. Нужен взгляд владельца.

**Состояние дерева:** `main` чист · юнит **186/186** · typecheck **0** · svelte-check **0 на 413** ·
`kaif:check` зелёный · стенд поднят.

### 🎯 Эстафета: что делать ДАЛЬШЕ

1. **`bugs/85` + `bugs/86` — сервер синхронизации.** Причина доказана (см. выше): сделать проход
   НОЧНЫМ по канону 1.x, сохранять `lastFullPassAt` вне памяти процесса, завести время подъёма
   сервера. Подписи виджета — текст, нужна вычитка владельца.
2. **`plans/08` + `bugs/74` — память прокрутки** (замер: 3 экрана из 3 забывают позицию). ⚠️ Признак
   программной прокрутки из `bugs/75` обязан переехать в общую механику, иначе дефект панели
   родится заново на каждом экране.
3. **`bugs/87`** — свежий гость без документа видит «Вы не вошли» (ветки `down` и `signedout`
   печатают один текст). Найден новыми дверями стенда.
4. **Собрать интервью по шести развилкам** (список в п. 3 выше) — без него семь пунктов волны
   стоят.
5. **Близнецы `bugs/76`** — лайтбокс фото и панель глав, приём готов, ~20 строк на оба.

**Команды:** `npm run stand` · `node tools/probe-ideas21.mjs` (замер всей волны) ·
`verify-bug75.mjs` (44) · `verify-bug76.mjs` (64) · `verify-bug77.mjs` (64, **настоящий Chrome**) ·
`verify-bug84.mjs` (28) · `verify-bug69.mjs` (графика, две скорости) · `npm test` (186).

---

**Предыдущее обновление:** 2026-07-29, вечер — 🧹 **БЕКЛОГ ПРОГРУМЛЕН + 🚀 `bugs/40` ПУНКТ 2 ЗАКРЫТ И
ВЫКАЧЕН В БОЙ: ЗАГРУЗОЧНЫЙ ЩИТ ВМЕСТО МЕЛЬКАНИЯ ЛЕНДИНГА** (просьбы владельца: «грумь беклог, что
дальше по планам бери в работу из багов и импрувов», затем «деплой прод»). Сессия закрыта `/end-chat`.

> ⚠️ **Главное, что должна знать следующая сессия:** три ловушки этой работы жили В ПРИБОРАХ, а не в
> продукте, и каждая давала уверенное ложное показание. Самая дорогая — **`vite dev` КЭШИРУЕТ
> `app.html`** (HMR обновляет только `.svelte`/`.ts`): два вывода подряд я сделал о ПРЕДЫДУЩЕЙ версии
> продукта. Правишь шаблон оболочки — **перезапускай стенд** (EXP-0083). Вторая: признак «правка
> доехала» греп нашёл в моём же комментарии рядом (EXP-0084) — маркер мутации обязан быть уникален.
> Третья: страж спрашивал **атрибут** `hidden` вместо видимости и был зелёным на дефекте, который
> **видно на скриншоте невооружённым глазом**. Канон владельца «смотреть кадры глазами» окупился.

### Что сделано этой сессией

**1. 🧹 ГРУМИНГ БЕКЛОГА — беклог не врёт.** Открыто честно: **20 багов · 10 идей · 4 плана**. Тегов
`DONE` никому не полагается: `bugs/06` и `17` — фиксы **в бою, но ждут контрольного взгляда
владельца** (закрывать своим словом нельзя); `bugs/08`/`09` — не задачи, а **индексы волн**, в них
ещё висят живые пункты (16, 17, 19, 20, 28, 31, 32). Из **Tier A** (`plans/07`) остались двое:
`bugs/46` (две строки времени) и `bugs/06` (взгляд владельца).

**2. ✅ `bugs/40`, ПУНКТ 2 — ЗАГРУЗОЧНЫЙ ЩИТ.** Разведка ДО кода (канон, шаг 7а): boot-флоу 1.x снят
с живого `ndim-old` → `researches/12` → «Boot-экран: механика целиком». Главное, чего не было в
инвентаре: 1.x держал щит **статической разметкой в `index.html`, видимой с первого кадра**, и гасил
его не таймером, а **принятым решением** — всегда ПОСЛЕ `openPage(...)`. Отсюда «красиво открывало».

**Замер тем же прибором, что снимал «до»** (`node tools/measure-bug40.mjs`): кадров лендинга
**4 · 5 → 0 · 0**, кадров кольца **0 · 0 → 4 · 4** (число кадров щита зависит от скорости отклика
стенда — на повторной проверке было 49 · 5; значение имеет НОЛЬ кадров лендинга, а не эта величина).

⚠️ **Прибор проверен на подгонку** (я правил его вместе с фиксом — уточнял, что «кадр лендинга» это
не-накрытый лендинг): прогон ЭТОГО ЖЕ прибора на коде с отключённым щитом даёт **75 · 6 кадров
лендинга**. Видеть лендинг он не разучился — значит ноль после фикса заслуга продукта.

Устройство: щит поднимает **инлайн-скрипт `app.html`** (тот же, что применяет тему) — любой щит,
который рисует Svelte, опаздывает на гидратацию, и это ровно измеренные 4–5 кадров. Знак —
**инлайн-SVG** (правило bugs/69 иначе невыполнимо). Потолок **15 с** с кнопкой «Перезагрузить» —
канон 1.x. Признак сессии `ndim-session` читается **синхронно** (Firebase хранит сессию в IndexedDB,
к первому кадру правды от SDK нет), и держит его **ОДНА точка** — постоянный слушатель в
`firebase.devAuth()`. ⚠️ Первая версия расставляла вызовы по местам входа и **пропустила стендовую
ветку** (`signInDev` в обход `waitForSession`) — щит не поднимался вовсе.

🔴 **У ГОСТЯ ЩИТА НЕТ** — корень это единственная индексируемая страница продукта.

**Страж `node tools/verify-bug40.mjs` — 71/71** (обе темы × 390/1440), трижды подряд, **доказан
двумя мутациями с подтверждённым доездом до сервера**: щит не поднимается → **28 провалов**; сужение
ширины → «1425×900 против 1440×900» (дефект класса `bugs/65` — щит не накрывал полосу
`scrollbar-gutter`).

**3. 🚀 ВЫКАТ В БОЙ** (слово владельца: «деплой прод»). Гейты → чистая пересборка (`build/` снесён) →
`npx firebase deploy --only hosting --project ndim-space` → **оба таргета release complete**.

**Дифф выката проверен, а не заявлен:** `git diff 4915c39..HEAD -- calculator firestore.rules
firestore.indexes.json firebase.json src/lib/model` → **0 файлов**.

**Выкат доказан признаком, снятым ДО и ПОСЛЕ:** `id="boot"` в боевом `index.html` было **0**, стало
**1**; `data-booting` — **0 → 6**; размер `index.html` **24 858 → 33 629** байт. Снимок «до» сделан
ДО деплоя специально, чтобы признак что-то доказывал.

**Сверка боя живым браузером:** `node tools/verify-prod-b4.mjs` — **81/81** · точечная проверка щита
на `ndimspace.app` — **10/10**: 🔴 **гость щита НЕ видит** (ни одного кадра из 25, обе ширины) и
лендинг у него виден · с признаком сессии щит поднимается, накрывает окно целиком, строка одна
(«Пространство NDim») в обеих темах. Кадры `test-results/prod-boot/` смотрены глазами.
⚠️ Щит в бою у ВОШЕДШЕГО агент проверить не может — под логином владельца нужен его взгляд.

**Состояние дерева:** `main` чист и запушен · юнит **186/186** · typecheck **0** · svelte-check
**0 на 413** · e2e **70** · `kaif:check` зелёный · стенд погашен ПО ДЕРЕВУ, сирот **0**.

### 🎯 Эстафета: что делать СЛЕДУЮЩЕМУ чату

1. **Взгляд владельца в бою под его логином — единственное, чего агент не смог проверить.** Открыть
   `ndimspace.app` по базовой ссылке вошедшим: ожидание — вместо мелькания лендинга виден щит (знак
   N-сети + «Пространство NDim» + кольцо), затем открывается профиль. Заодно проверяется **пункт 1**
   бага (см. ниже).
2. **`bugs/40` пункт 1 (редирект не срабатывает) — ОСТАЁТСЯ ОТКРЫТЫМ, только бой.** На стенде
   редирект исправен в обоих раундах; причину в коде редиректа не искать, пока не воспроизведён под
   логином владельца. ⚠️ Щит его **маскирует**: человек теперь видит кольцо вместо лендинга, поэтому
   симптом мог измениться — спросить владельца, доходит ли дело до профиля.
3. **Проверка жеста pull-to-refresh пальцем на телефоне** (висит с прошлой сессии) — синтетический
   `touchmove` не воспроизводит физику браузера.
4. **Контрольный взгляд владельца в бою, под его логином:** Профиль ↔ Связи ↔ Пространство ↔
   Измерения ↔ Меню два-три раза — со второго захода экраны появляются **сразу, без «Загрузки»**.
5. **`bugs/67`** — причина доказана, фикс НЕ найден; заходить со стороны «убрать асинхронную
   публикацию `--bar-h`». ⚠️ Судья — `npx playwright test e2e/desktop-shell.spec.ts
   --project=desktop --repeat-each=4`, а НЕ `tools/probe-bug67.mjs` (у зонда слепое пятно).
6. **Вычитки владельца:** две `[AI]`-фразы руководства (bugs/55 → «Решения…», п. 0) · черновик
   «Версия 2.0» (`researches/06`, `[AI]`).
7. `bugs/46` (две строки времени, Tier A) · `bugs/62`+`58` (PWA — эпическая фича, нужен обзор
   индустрии) · `ideas/19` · `ideas/16` (трогает модель и правила) · `bugs/65` · **пачка 4 целиком
   ждёт владельца**: `bugs/16`+`19`+`32` · `17` · `20`+`41` · `28` · `31`.

**Команды:** `npm run stand` · `node tools/verify-bug40.mjs` (щит, 71 — ⚠️ **правил `app.html` →
перезапусти стенд**) · `node tools/measure-bug40.mjs` (замер мелькания) · `node tools/verify-ptr.mjs`
(обновление, 60) · `node tools/verify-prod-b4.mjs` (смоук БОЯ, 81) · `npm test` (186) ·
`npm run e2e` (⚠️ выжигает `test-results/` — EXP-0062; гасить стенд ПО ДЕРЕВУ).

---

**Предыдущее обновление:** 2026-07-29, день — 🚀 **PULL-TO-REFRESH СДЕЛАН ЦЕЛИКОМ И ВЫКАЧЕН В БОЙ**
(слово владельца: «и деплой в конце, чтобы я потестил в проде»). Интервью №006 закрыто: владелец
ответил на все четыре развилки, реализация разблокирована и исполнена за один заход.

> ⚠️ **Главное, что должна знать следующая сессия:** новый страж показал **40 падений из 48** — и
> продукт был исправен ПОЛНОСТЬЮ. Врал прибор, тремя независимыми способами сразу (мерил до
> гидратации · счётчик чтений не находил URL-кодированные пути · ожидание конца работы возвращалось
> мгновенно). Самое опасное из трёх: проверка «повторный заход бесплатен — 0 чтений» была **зелёной
> от неисправности счётчика**. Отсюда правило, стоившее сегодня дороже всего — **EXP-0082**: у
> стража, где «ноль» это хорошая новость, обязан быть КОНТРОЛЬ САМОГО ПРИБОРА в том же прогоне.

### Что сделано этой сессией

**1. 📋 ИНТЕРВЬЮ №006 ЗАКРЫТО — владелец ответил на все четыре развилки.** Ответы В1=А, В2=А, В3=А
лежали в рабочей копии незакоммиченными (сессия их и нашла); В4=А получен в чате. Оба раза слово
владельца закоммичено ДОСЛОВНО ДО любых правок (канон, шаг 16) — коммиты `3598479` и `08ccabf`.

**2. ✅ PULL-TO-REFRESH РЕАЛИЗОВАН ПО МАКЕТУ V1** (`0794d39`):
· `src/lib/data/refresh.svelte.ts` — ядро: инвалидация `relations:*` / `space` / `ratings:*` /
`profile`, отметка времени, определение поддержки жеста (В3: `CSS.supports`, Safari<16 отсекается);
· `src/lib/ui/PullToRefresh.svelte` — жест и индикатор, форма из макета (круг 34px, порог 64px,
разворот стрелки, кольцо на обновлении). `overscroll-behavior-y: contain` — В2=А: отобрано только
обновление, отскок оставлен браузеру;
· строка `↻ Обновить данные · только что` в «Меню» (карточка «Данные») — путь БЕЗ жеста, которого
требует **WCAG 2.2 SC 2.5.7**, как только жест забран у браузера, и единственный путь на десктопе;
· загрузчики четырёх экранов вынесены в `loadScreen()` — обновление перечитывает ровно то же, что и
заход. ⚠️ В «Профиле» порядок внутри не косметика: экран объявляется готовым ДО чтения сводки связей.

**Решение, принятое без владельца и записанное в интервью:** полотно экрана НЕ едет вниз — двигается
только индикатор, как в Android (Material). На iOS содержимое пружинит само, браузером, которого мы
по В2=А не трогаем.

**3. 🛡 Страж `node tools/verify-ptr.mjs` — 60/60** (обе темы × 390/1440), **доказан мутацией**:
отключил инвалидацию → 4 падения ровно на «тап реально перечитывает». В логе видно, ЧТО прочитано
(`users/…`, `points/…`), а не только «больше нуля».

**4. 🚀 ВЫКАТ В БОЙ. Доказан признаком, снятым ДО и ПОСЛЕ:** строки «Обновить данные» в боевых
чанках было **0 из 23**, стало **1 из 24**. Дифф выката проверен, а не заявлен:
`git diff 6fb54cb..HEAD -- calculator firestore.rules firestore.indexes.json firebase.json
src/lib/model` → **0 файлов**. Смоук боя живым браузером `verify-prod-b4.mjs` — **81/81**.

⚠️ **ЧЕГО АГЕНТ ПРОВЕРИТЬ НЕ МОЖЕТ И ЖДЁТ ОТ ВЛАДЕЛЬЦА:** сам жест пальцем на телефоне.
Синтетический `touchmove` не воспроизводит физику прокрутки браузера — зелёная такая проверка
доказывала бы работу прибора, а не продукта (класс EXP-0078/0080). Ожидаемое поведение: тянете вниз
на «Связях» → из-под шапки выезжает круг → за порогом стрелка разворачивается вверх → отпустили →
кольцо крутится → данные перечитаны.

**Состояние дерева:** `main` чист · юнит **186/186** · typecheck **0** · svelte-check **0 на 413** ·
e2e **70** · `kaif:check` зелёный · стенд погашен ПО ДЕРЕВУ, сирот **0**.

### 🎯 Эстафета: что делать СЛЕДУЮЩЕМУ чату

1. **Проверка жеста владельцем на телефоне** (см. выше) — единственное, что осталось от этой работы.
2. **Контрольный взгляд владельца в бою, под его логином** (висит с прошлых сессий): Профиль ↔ Связи
   ↔ Пространство ↔ Измерения ↔ Меню два-три раза — со второго захода экраны появляются **сразу,
   без карточки «Загрузка»**.
3. **`bugs/67` — причина доказана, фикс НЕ найден.** Не крути запасное значение (обе стороны
   измерены и обе врут); заходи со стороны «убрать асинхронную публикацию `--bar-h`». ⚠️ Судья —
   `npx playwright test e2e/desktop-shell.spec.ts --project=desktop --repeat-each=4`, а НЕ
   `tools/probe-bug67.mjs` (у зонда слепое пятно, описано в его шапке).
4. **`bugs/40` (boot-экран) — лучший автономный заход, замер уже сделан.** Брать ПУНКТ 2 (boot-экран
   вместо мелькания): воспроизводим на стенде, есть прибор для «до и после». Пункт 1 (редирект) в
   автономию не годится — на стенде не воспроизводится.
5. **Вычитки владельца:** две `[AI]`-фразы руководства (bugs/55 → «Решения…», п. 0) · черновик
   «Версия 2.0» (`researches/06`, `[AI]`).
6. `bugs/62`+`58` (PWA) · `ideas/19` · `bugs/65` · **пачка 4 целиком ждёт владельца**:
   `bugs/16`+`19`+`32` · `17` · `20`+`41` · `28` · `31`.

**Команды:** `npm run stand` · `node tools/verify-ptr.mjs` (страж обновления, 60) ·
`node tools/verify-prod-b4.mjs` (смоук БОЯ, 81) · `node tools/verify-ideas18.mjs` (страж кэша, 30) ·
`npm test` (186) · `npm run e2e` (⚠️ выжигает `test-results/` — EXP-0062; гасить стенд ПО ДЕРЕВУ).

---

**Предыдущее обновление:** 2026-07-29, утро — 🚀 **В4 ВЫКАЧЕН В БОЙ** (слово владельца: «деплой
прод») **+ владелец выбрал макет pull-to-refresh: V1 «Как в системе»** («v1, как в системе»)
**+ закрыты `bugs/72` (не дефект) и `bugs/73` (оснастка), доказана причина `bugs/67`, сделан
замер `bugs/40`.** Сессия закрыта паузой в 11:00 по просьбе владельца.

> ⚠️ **Главное, что должна знать следующая сессия:** два из четырёх разобранных багов оказались
> НЕ ТЕМ, чем выглядели. `bugs/72` («каретка сбрасывается», воспроизводимость 5 из 5) —
> **вообще не дефект продукта**: реверс существует только в состоянии, недостижимом для человека.
> `bugs/67` — причина доказана, но мой фикс **опровергнут настоящим тестом уже после пуша**.
> Оба раза врал МОЙ СОБСТВЕННЫЙ прибор. Отсюда два правила, которые стоили сегодня дороже всего:
> **(1)** воспроизведя дефект, спроси — достижимо ли это состояние для живого человека
> (EXP-0080); **(2)** не объявляй готовым то, что проверено только своим прибором, когда рядом
> есть независимый судья (EXP-0078).

### Что сделано этой сессией

**1. 🚀 ВЫКАТ В БОЙ решения В4** (интервью №005: топ и статистика живут всю сессию, пульс сервера
остаётся минутным). Гейты: юнит **186/186** · typecheck **0** · svelte-check **0 ошибок на 411
файлах** · `kaif:check` **28/28**. Чистая пересборка (`build/` снесён) → `npx firebase deploy
--only hosting --project ndim-space` → **оба таргета release complete**.

**Дифф выката проверен, а не заявлен:** `git diff 53398e7..HEAD -- calculator firestore.rules
firestore.indexes.json firebase.json src/lib/model` → **0 файлов**. Сервер синхронизации, правила,
модель и индексы НЕ тронуты.

**Выкат доказан признаком, снятым ДО и ПОСЛЕ** (а не кодами ответов): минифицированный контракт
свежести в боевом чанке был `{mine:1/0,server:6e4}`, стал **`{mine:1/0,server:1/0,pulse:6e4}`**.
Снимок «до» сделан ДО деплоя специально, чтобы признак что-то доказывал. ⚠️ Первый кандидат в
признаки (`'space:server'`) был отвергнут — он лежал в бою и раньше.

**Сверка боя:** 15 маршрутов — 200, зеркало `ndim-space.web.app` — 301; **живой браузер гостем,
5 экранов × 2 темы × 390/1440 — 81/81** (`node tools/verify-prod-b4.mjs`, новый инструмент):
страницы реально ОТРИСОВАНЫ (высота + текст, а не «200 OK»), тема применена, консоль чиста.
Кадры `test-results/prod-b4/` смотрены глазами: лендинг живой («С нами уже 94 человека»),
рельс и тёмная тема на месте, гостю честно предложено войти.
⚠️ **Чего гость проверить НЕ МОЖЕТ:** сам эффект кэша — нужен взгляд владельца под логином.

**2. 🎨 ВЛАДЕЛЕЦ ВЫБРАЛ МАКЕТ pull-to-refresh — V1 «Как в системе»** (дословно: *«v1, как в
системе»*). Зафиксировано в **`interviews/interview_006_pull_to_refresh.md`**: чистый жест с
индикатором под шапкой, НОЛЬ новых элементов на экранах, путь без жеста — строка `↻ Обновить
данные · 12 минут назад` в «Меню». Цена принята осознанно и записана: самый чистый экран и самая
низкая находимость.
⚠️ **Кода НЕ писать: четыре сопутствующих развилки ЖДУТ ОТВЕТА владельца** (заданы и в документе,
и текстом в чат): В1 где живёт жест (рекомендация — все четыре главных экрана) · В2 отбирать ли
«резинку» (рекомендация — только обновление) · В3 Safari младше 16 (рекомендация — жест не
включается, строка в «Меню» работает везде) · В4 текст строки.

**3. 🎯 `bugs/67` — КОРНЕВАЯ ПРИЧИНА НАЙДЕНА** (документ бага говорил «причина НЕ найдена»).
Виновата не гонка со шрифтами и не округление, а **запасное значение, которое врёт в бОльшую
сторону, в паре с `position: sticky`**: `SideRail.svelte:80` держит `top: var(--bar-h, 56px)`,
`--bar-h` публикуется только при гидратации, настоящая шапка на 1440 — **55px**, а липкий элемент
прижимается вниз до `top` даже при нулевой прокрутке. Отсюда ровно 1px, всегда в одну сторону, на
любом маршруте и старше всей работы над данными — версия объясняет ВСЕ пять наблюдений документа.

**✅ ПОДТВЕРЖДЕНО НАБЛЮДЕНИЕМ, а не рассуждением** — новый зонд `node tools/probe-bug67.mjs`
(покадровая трасса, метод EXP-0060; отдельно от `npm run e2e`, потому что тот выжигает
`test-results/` — EXP-0062). **1080 кадров: где `--bar-h` пуста — расхождение в 41 из 41 кадра;
где заполнена — в 0 из 1039.** Та же трасса **опровергает обе прежние гипотезы**: во всех падающих
кадрах `fonts=loaded` (гонки шрифтов нет), а высоты целые `barH=55 / railY=56` (округления нет).
Окно дефекта ~3 кадра на загрузку (~50 мс) — отсюда «1 прогон из 3».

**❌ А ВОТ ФИКС НЕ ВЫШЕЛ — И ЭТО ГЛАВНЫЙ УРОК ДНЯ (`bugs/67` остаётся ОТКРЫТЫМ).** Попробовал
запасное значение `var(--bar-h, 56px)` → `0px` («ноль не может прижать»). Свой зонд подтвердил:
41/41 → **0/42**, инвариант `bugs/49` после прокрутки — 12/12 чисто. Гейты зелёные. Закоммитил и
**запушил как готовое**. Затем ради полноты запустил НАСТОЯЩИЙ тест по рецепту из самого бага:

```
desktop-shell.spec.ts:70   Expected: 55   Received: 0
```

Рельс заезжал **ПОД шапку** — буквальная регрессия `bugs/49` («ДОЛЖНО БЫТЬ ЗАФИКСИРОВАНО», слово
владельца капсом). **Фикс откачен, DONE-тег снят, 56px возвращены** как знающий компромисс с
объяснением прямо в коде.

**Почему зонд соврал (это ценнее самой попытки):** он мерил «до гидратации БЕЗ прокрутки» и «после
гидратации С прокруткой» — два условия по отдельности, — а цена нуля живёт в их **пересечении**,
куда прибор не заходил ни разу. Слабость записана в шапке `tools/probe-bug67.mjs`, чтобы следующая
сессия не поверила его зелёному так же, как поверил я. Уроки — **EXP-0078**.

**Что теперь известно и чего не было утром:** причина доказана; обе прежние гипотезы (шрифты,
округление) опровергнуты; доказано, что у запасного значения **нет безопасной стороны** (завышенное
врёт на 1px без прокрутки, заниженное прячет рельс под шапку с прокруткой) — значит крутить
константу бессмысленно, лечить надо асинхронную публикацию `--bar-h`. Кандидат следующего захода
записан в баге: объявить высоту шапки CSS-константой по брейкпойнтам (как Material и HIG), тогда
публиковать нечего и гонки нет вовсе. ⚠️ Это меняет вид шапки — возможно, нужен взгляд владельца.

**4. ✅ `bugs/72` ЗАКРЫТ — ЭТО НЕ ДЕФЕКТ ПРОДУКТА.** Кода не написано ни строки, потому что чинить
нечего.

**Строгое A/B в одном процессе** (`node tools/probe-bug72-open.mjs`) — одно поле, один способ
ввода, отличается ТОЛЬКО состояние ящика поиска: **открыт → «Кошки» 3/3, закрыт → «икшоК» 3/3.**
Закрытый ящик — это `max-height: 0; overflow: hidden` у `.toolbar`: поле схлопнуто и человеку
недоступно физически (чтобы напечатать букву, надо сначала нажать «Поиск измерения»). **Состояния,
в котором возникает реверс, у живого человека не существует** — а автоматика попадает в него легко,
потому что Playwright считает схлопнутое поле «видимым» (в точности **EXP-0071**, найденный в тот
же день на другом приборе; оба замера родились из одной ошибки оснастки).

Подтверждено и с другой стороны: ловушки на ЖИВОМ элементе поймали 7 событий за ввод и **ни одного**
`value=` / `setSelectionRange` / `setAttribute` — приложение к полю не прикасается вовсе, а каретка
на `beforeinput` **уже** 0, то есть не сбрасывается, а никогда не продвигается: браузер не двигает
её в неотрисованном элементе.

**Урок — EXP-0080: воспроизведя дефект, спроси, достижимо ли это состояние для человека.**
«5 из 5» доказывает воспроизводимость, но не существование дефекта — пять раз подряд можно надёжно
воспроизвести собственную ошибку.

**По дороге туда закрыты и остальные двери** (`node tools/probe-bug72.mjs`):
· **Дверь 1 (главная в документе): это НЕ артефакт `vite dev`.** Сравнение стало возможным, когда
нашлось, что `isStand()` смотрит на **hostname `localhost`**, а не на режим сборки: `vite preview`
с той же сборкой, что едет в бой, ходит в те же эмуляторы. Реверс «икшоК» одинаков на :5173 и
:4173 — чинить придётся по-настоящему.
· **Дверь 3 закрыта:** ни один из 8 предков поля не задаёт `rtl` или нестандартный `unicode-bidi`.
· **Дверь 2 закрыта наполовину:** два РАЗНЫХ пути ввода (клавиатурные события и `insertText` в их
обход) дают один и тот же реверс — способ ввода ни при чём.
· 🔥 **Новая улика:** голый `<input>`, вставленный в ЭТУ ЖЕ страницу, печатается ВЕРНО (прошлый
контроль ставился на `about:blank` и доказывал лишь исправность инструмента). Исключено ВСЁ
окружение — глобальные слушатели, стили, шрифты, тема, роутер. Виноват сам компонент.
· Разметка осмотрена и **обыкновенна** (`bind:value={search}` в форме, без `oninput` и `{#key}`) →
подозреваемых осталось двое: кто ещё пишет в переменную `search`, и пара `bind:value` +
`type="search"` в Svelte 5. Урок — **EXP-0079**.

**5. 📏 `bugs/40` (boot-экран) — СДЕЛАН ЗАМЕР ДО КОДА** (`node tools/measure-bug40.mjs`), и он
**развёл два пункта бага в разные стороны**:
· **Пункт 1 «редирект вошедшего не срабатывает» — НА СТЕНДЕ НЕ ВОСПРОИЗВОДИТСЯ:** редирект честно
происходит, оба раунда. Значит либо это бой (холодный старт Auth по сети медленнее localhost с
эмулятором), либо условия владельца. **Не ищите причину в коде редиректа, пока не воспроизвели** —
на стенде он исправен; нужен прогон в бою под логином владельца.
· **Пункт 2 «мелькает лендинг, boot-экрана нет» — ПОДТВЕРЖДЁН И ИЗМЕРЕН:** человек видит лендинг
**4–5 кадров** до прыжка, кадров с кольцом загрузки — **0**. Это чинится независимо от пункта 1,
на стенде, с прибором для «до и после». ⚠️ 4–5 кадров — НИЖНЯЯ оценка: на стенде Auth отвечает
мгновенно, в бою окно шире.

**6. ✅ `bugs/73` ЗАВЕДЁН И ПОЧИНЕН — `npm run stand` больше не отказывает молча.** Воспроизводился
3 раза за сессию и стоил мне ~20 минут; стоил бы столько же каждой следующей сессии.

**Причина:** `child.kill()` на Windows убивал только обёртку `cmd.exe` (стенд запускает детей с
`shell: true`, иначе не стартуют `.cmd` от npm/npx), а внук `node calculator/index.mjs` выживал,
крутил цикл каждые 15 с и мешал поднять стенд заново. Наверх при этом **не выходило ничего** —
команда выглядела работающей.

**Фикс** (`tools/stand.mjs`): убийство по ДЕРЕВУ через `taskkill /T /F` · `process.on('exit')` как
последний рубеж · **ворота громкости** — стенд ждёт, пока эмулятор реально начнёт слушать, и при
отказе падает с кодом 1 и готовой командой поиска сироты.

**Доказано тремя наблюдениями:** запуск без эмулятора — было «бесконечное капание в лог», стало
**выход 1 за 30 с** с причиной · остановка стенда — сервер синхронизации **1 → 0** · перезапуск
(сам симптом) — **2 порта из 2**.

⚠️ **Чего фикс не покрывает, названо в баге:** если убить сам `stand.mjs` ПРИНУДИТЕЛЬНО
(`Stop-Process -Force`, как делает автоматика), обработчики выхода не исполняются в принципе и
сирота остаётся — предел ОС. Поэтому ворота громкости не менее важны: остаточный случай теперь
диагностирует себя сам. Урок — **EXP-0081** (ловушки уборки: у сироты в командной строке НЕТ пути
проекта, а фильтр по `*calculator/index.mjs*` находит собственный PowerShell и убивает его).
⚠️ Дешёвый индикатор «чисто ли»: если `stand.log` не удаляется — сироты живы.

**7. 🧹 Груминг беклога — беклог не врёт.** Подозрительные документы сверены **с кодом**, а не со
своим описанием: в `src/` нет ни `beforeinstallprompt`, ни `deleteUser`/`updateEmail`, ни
`@media (min-width: 600px)`, ни boot-экрана, ни service worker → `bugs/40 · 44 · 45 · 58 · 62 · 20`
открыты честно, тегов `DONE` никому не полагается. ⚠️ Первые три грепа были сделаны
`Select-String -Recurse` (такого параметра нет), молча упали и напечатали «открыт честно» из
else-ветки — переделано нормальным инструментом. **Урок: вывод упавшей проверки — не результат.**

### 🎯 Эстафета: что делать СЛЕДУЮЩЕМУ чату

1. **📋 ЧЕТЫРЕ ОТВЕТА ВЛАДЕЛЬЦА по pull-to-refresh** — `interviews/interview_006_pull_to_refresh.md`.
   Без них кода не писать (канон). Как ответы придут — реализация: жест + строка в «Меню» +
   инвалидация ключей `relations:*`, `space`, `ratings:*` + страж `tools/verify-ptr.mjs`.
2. **Контрольный взгляд владельца в бою, под его логином** (висит с прошлой сессии, теперь
   особенно ценен — В4 в бою): Профиль ↔ Связи ↔ Пространство ↔ Измерения ↔ Меню два-три раза,
   со второго захода экраны появляются **сразу, без карточки «Загрузка»**.
3. **`bugs/67` — причина доказана, фикс НЕ найден.** Не крути запасное значение (обе стороны
   измерены и обе врут); заходи со стороны «убрать асинхронную публикацию `--bar-h`» — кандидат
   расписан в баге. ⚠️ Судья — `npx playwright test e2e/desktop-shell.spec.ts --project=desktop
   --repeat-each=4`, а НЕ `tools/probe-bug67.mjs` (у зонда слепое пятно, описано в его шапке).
4. **Вычитки владельца:** две `[AI]`-фразы руководства (bugs/55 → «Решения…», п. 0) · черновик
   «Версия 2.0» (`researches/06`, `[AI]`).
5. ~~`bugs/72`~~ — **ЗАКРЫТ** как «не дефект» (см. выше). ⚠️ Если где-то ещё страж печатает в
   поле внутри выдвижного ящика — открывай ящик ПО РАЗМЕТКЕ (`.toolbar.open`), иначе получишь
   такой же призрак (EXP-0071 + EXP-0080).
6. **`bugs/40` (boot-экран) — следующий лучший автономный заход, замер уже сделан.** Брать надо
   ПУНКТ 2 (boot-экран вместо мелькания): он воспроизводим на стенде и есть прибор для «до и
   после». Пункт 1 (редирект) в автономию не годится — на стенде не воспроизводится.
   Перед кодом — разведка boot-флоу 1.x в `ndim-old` (канон, шаг 7а).
7. `bugs/62`+`58` (PWA — люди потеряли установленное приложение) · `ideas/19` · `bugs/65` (ждёт
   слова владельца).
7. **Пачка 4 целиком ждёт владельца:** `bugs/16`+`19`+`32` · `17` · `20`+`41` · `28` · `31`.

**Состояние дерева на момент паузы:** `main` чист и запушен · юнит **186/186** · typecheck **0** ·
svelte-check **0 на 411 файлах** · `kaif:check` **28/28** · продуктовый код изменений НЕ содержит
(вся работа дня — оснастка, документы и один выкат уже готового кода) · стенд погашен, сирот нет.

**Команды:** `npm run stand` (нужна Java; теперь падает громко, если эмулятор не поднялся —
bugs/73) · `node tools/verify-prod-b4.mjs` (смоук БОЯ, 81) · `node tools/verify-ideas18.mjs`
(страж кэша, 30) · `node tools/measure-bug40.mjs` (замер мелькания лендинга) ·
`node tools/probe-bug72-open.mjs` (образец: как мерить поле в выдвижном ящике) · `npm test` (186) ·
`npm run e2e` (⚠️ выжигает `test-results/` — EXP-0062; гасить стенд ПО ДЕРЕВУ — EXP-0059/0027).

---

**Предыдущее обновление:** 2026-07-28 — ✅ **ВЛАДЕЛЕЦ ОТВЕТИЛ НА ИНТЕРВЬЮ №005 (кэширование).
Все четыре развилки закрыты: три — записью, четвёртая — кодом. Топ связей и статистика больше
не перечитываются внутри сеанса. Заведена следующая работа: pull-to-refresh — обзор индустрии
готов, 4 макета ЖДУТ ВЫБОРА ВЛАДЕЛЬЦА.**

### Что сделано этой сессией

**0. Ответы владельца закоммичены дословно ДО правок** (канон, шаг 16): В1 = A (кэш живёт только
в живой вкладке: *«Закрыл браузер — пока-пока данные»*) · В2 = A (живые подписки не сейчас) ·
В3 = A (при возврате во вкладку не освежать) · **В4 — свой ответ**: *«ТОП обновляется очень
редко. Нет смысла его обновлять. Пользователь зайдёт в приложение — получит свежие данные. Этого
хватит»* + pull-to-refresh на главных экранах, начиная со «Связей».

**1. 📌 В1/В2/В3 — закрыты записью, кода не потребовали.** Перенесены в `researches/17` **§8**
таблицей «развилка → решение владельца → что это значит в коде». Соответствие кода решениям
проверено грепом, а не на слово: персистентного кэша нет, `onSnapshot` в нашем коде **0
вхождений**, обработчика «вернулись во вкладку» не заведено. Будущая сессия прочитает решение,
а не откроет вопрос заново.

**2. ✅ В4 — сделано в коде** (`src/lib/data/cache.ts`, `space.ts`, `relations.ts`).
`FRESH.server` → **вся сессия**. ⚠️ **Главное, чего не было в вопросе:** под именем «данные
сервера синхронизации» жили ДВЕ сущности, и решение применимо только к одной. Цифры (топ,
счётчики, тренды) застывают безобидно; **сердцебиение** `space/server` — нет: из него
`syncServerState` ВЫВОДИТ лампочку «Работает» относительно «сейчас», с терпимостью в три цикла,
и заморозка показала бы **живой сервер умершим**. Поэтому пульс выделен в **`FRESH.pulse` = 60 с**
и в отдельную запись кэша `KEYS.syncServer` — общую для «Пространства», «Меню» и «О системе»
(побочно исчезло дублирующее чтение между ними). Урок — **EXP-0075**.

**Замер прибором** `node tools/measure-ideas18.mjs --rounds 4 --slow 0` (91 с реального времени):
цена повторных заходов по кругу из пяти экранов — **чтений 10 → 3**, лоадеров 0. Все три чтения
ОБЪЯСНЕНЫ наблюдением, а не списаны на шум: 1 документ пульса раз в минуту (замысел) + первый
настоящий заход на стартовый экран прогона `/profile` (EXP-0072: клик по вкладке, на которой
браузер уже стоит, навигацией не является).

**Проверка:** страж `node tools/verify-ideas18.mjs` — **30/30**, обе темы × 390/1440 · юнит
**186** (+1) · typecheck/check **0/0**. Тест контракта свежести переписан вместе с контрактом и
дополнен **вторым** тестом, стерегущим не число, а причину: запись пульса обязана устаревать
раньше, чем молчание становится поводом сказать «Не отвечает» (выражено через
`HEARTBEAT_TOLERANCE`). **Мутация** `pulse = Infinity` роняет оба. Подсказки обоих инструментов
правились вместе с кодом — они печатали «окно свежести 60 с», которого больше нет (EXP-0067).

**3. 📚 `researches/18` — обзор индустрии по pull-to-refresh** (канон, шаг 7б: новый механизм →
сначала читаем мир). По первоисточникам (MDN · Chrome for Developers · WebKit · W3C/WAI ·
Material · Apple HIG · Nielsen Norman Group). Три находки, без чтения не появившиеся бы:
· **жест в вебе НЕ свободен** — у верхней границы его слушает браузер, свой PTR не добавляется,
а **отбирается** через `overscroll-behavior`, и в WebKit это возможно только с **Safari 16.0**
(MDN до сих пор: «Limited availability… not Baseline»);
· **WCAG 2.2 SC 2.5.7 (AA)** — пока обновляет браузер, критерий не применяется («determined by
the user agent»), но как только жест наш, у него обязан быть равноценный путь БЕЗ перетаскивания
(плюс на десктопе тянуть нечем вовсе);
· **Apple HIG считает жест ДОБАВКОЙ** к автоматическому обновлению — а у нас после В4
автообновления внутри сеанса нет. Расхождение записано осознанно, чтобы следующая сессия не
«чинила» его как дефект.

**4. 🎨 `design/pull-to-refresh-mockups.html` — 4 макета, ЖДУТ ВЫБОРА** (правило четырёх макетов).
V1 «Как в системе» (чистый жест, путь без жеста — строка в «Меню») · **V2 «Жест + строка
свежести»** (рекомендация агента: «Обновлено 12 минут назад · Обновить» — закрывает разом
находимость, десктоп, доступность и честность «данные из памяти») · V3 «Жест + значок в шапке» ·
V4 «Без жеста, только кнопка». Жест в макетах **живой** — проигрывается по кругу с порогом,
сменой стрелки и кольцом обновления. Смотрено живым Edge в трёх моментах цикла
(`test-results/ptr-mockups/`); связка «классы CSS ↔ классы разметки» сходится в обе стороны.
Первая редакция рисовала в шапке пункт «⋯», которого в `AppBar` нет, — выдуманный элемент убран.

⚠️ **Что владелец должен знать про В4** (сказано ему в документе интервью): оценил измерение →
топ помечается устаревшим и перечитывается сразу, но сервер к этому моменту мог ещё не досчитать
(цикл 60 с + тихий период 120 с), и прочитанное **замрёт до конца сеанса**. Это ровно тот случай,
ради которого он назвал pull-to-refresh, — то есть жест закрывает не только «хочу свежее».

### 🎯 Эстафета: что делать СЛЕДУЮЩЕМУ чату

1. **🎨 ВЫБОР ВЛАДЕЛЬЦА ПО МАКЕТАМ pull-to-refresh** — открыть
   `design/pull-to-refresh-mockups.html` в браузере (жест играет сам). Кроме варианта нужны
   ответы на 4 вопроса внизу страницы: где живёт жест · отбирать ли «резинку» · что делать в
   Safari младше 16 · текст строки свежести. **Кода до этого не писать** (канон).
2. **Контрольный взгляд владельца в бою, под его логином** (висит с прошлой сессии): пройти
   Профиль ↔ Связи ↔ Пространство ↔ Измерения ↔ Меню два-три раза — со второго захода экраны
   появляются **сразу, без карточки «Загрузка»**. ⚠️ В4 в бой ещё **НЕ выкачен** — лежит в
   `main`, ждёт слова владельца о деплое.
3. **Вычитки владельца** (висят с прошлых сессий): две `[AI]`-фразы руководства (bugs/55 →
   «Решения…», п. 0) · черновик «Версия 2.0» (`researches/06`, `[AI]`).
4. **`bugs/72`** (каретка в поле поиска сбрасывается в начало) — три непроверенные двери в
   документе; первая: боевой это дефект или артефакт `vite dev`.
5. `ideas/19` (виджет «Аккаунт» в профиль) · `bugs/40` (boot-экран) · `bugs/62`+`58` (PWA) ·
   `bugs/65` (гуттер) · `bugs/67` (флейк e2e 1px).
6. **Пачка 4 целиком ждёт владельца** (макеты и развилки): `bugs/16`+`19`+`32` · `17` · `20`+`41`
   (планшет) · `28` · `31`.

**Команды:** `npm run stand` (эмуляторы + сервер + vite; нужна Java) · `node tools/verify-ideas18.mjs`
(страж кэша, 30 проверок) · `node tools/measure-ideas18.mjs --rounds 4 --slow 0` (прибор замера) ·
`npm test` (186) · `npm run e2e` (⚠️ гасить стенд ПО ДЕРЕВУ ПРОЦЕССОВ — EXP-0059/0027).

---

**Предыдущее обновление:** 2026-07-28, ночь — 🚀 **`ideas/18` (кэш данных экранов на сессию)
ЗАКРЫТА И ВЫКАЧЕНА В БОЙ ВМЕСТЕ СО СВЯЗКОЙ «ИЗМЕРЕНИЯ»** (слово владельца: «деплой прод тоже»).
Первой сделана разведка по новому канону (шаг 7б) — `researches/17`, обзор индустрии, — и она
опровергла кандидат решения, записанный в самой идее.

### Что сделано той сессией

**1. 📚 `researches/17` — обзор индустрии по кэшированию данных клиента** (первое применение
шага 7б: «почти на всё в индустрии есть золотые стандарты»). Прочитаны первоисточники: RFC 5861
(**stale-while-revalidate — это стандарт IETF**, с обязательным потолком на показ устаревшего),
TanStack Query и SWR (две независимые оси: свежесть ≠ жизнь в памяти; инвалидация пометкой
ключа), Workbox и bfcache (слой страниц), документация Firestore и SvelteKit.
**Главная находка — опровергнут кандидат №1 самой `ideas/18`:** встроенный кэш Firestore SDK
**не экономит онлайновые чтения**. Дословно из Google Cloud: *«By default, a `get` call will
attempt to fetch the latest document snapshot from your database… will use the offline cache
if the network is unavailable or if the request times out»*. Кэш SDK — про офлайн, а не про
навигацию, и вдобавок открывает соединение с IndexedDB (задевает bfcache). **Урок EXP-0074:
обзор индустрии окупается тем, что убивает правдоподобное неверное.**

**2. ✅ `ideas/18` — кэш данных экранов на сессию.** Ядро — один модуль
`src/lib/data/cache.ts` по модели SWR: `peek` отдаёт лежащее в памяти **синхронно** (первый
кадр экрана уже с данными, лоадера нет вовсе), `cached` решает, нужен ли поход в базу,
`invalidate` гасит ключи по началу имени, `forgetAll` забывает всё; дедупликация параллельных
чтений внутри. Контракт свежести из `ideas/14`: мои данные — вся сессия (записи гасят кэш
сами), данные сервера синхронизации — 60 с, по его же циклу, и за окном освежаются **тихо
поверх показанного экрана**. Приватность: у кэша есть хозяин, смена uid уничтожает содержимое,
выход из аккаунта зовёт `forgetAll()` + `forgetAvatars()`.

**Замер одним прибором** (`tools/measure-ideas18.mjs`; «до» снято на чистом HEAD через
`git stash`, чтобы сравнивать одно с одним): цена повторных заходов по кругу из 5 экранов —
**чтений документов 49 → 10, карточек «Загрузка» экрана 8 → 0.**

**3. 🔧 Обе оснастки пришлось чинить по ходу — они врали.** (а) Считали HTTP-запросы к
эмулятору, а у Firestore это в основном служебные кадры WebChannel: три ОДИНАКОВЫХ захода
дали 5 → 1 → 0. Теперь считаются пути документов в теле запроса — та единица, за которую берут
деньги (**EXP-0073**). (б) «Холодным» заходом был клик по вкладке, на которой браузер уже
стоял, — навигации нет, и «тёплым» оказывался первый настоящий заход (**EXP-0072**).

**Регрессия сессии:** юнит **185** (+12: `src/lib/data/cache.test.ts`) · e2e **70** (+2 skip) ·
typecheck/check **0/0** · `kaif:check` зелёный · страж `node tools/verify-ideas18.mjs` —
**30 проверок** (обе темы × 390/1440), доказан двумя мутациями (снят мгновенный кадр →
4 падения; отключена свежесть → 5). Скриншоты `test-results/ideas18/` смотрены глазами.

**4. 🚀 ВЫКАТ В БОЙ 2026-07-28, ночь** (слово владельца: «деплой прод тоже»). Повёз разом
**`ideas/18`** (кэш сессии) и **связку «Измерения»** (`bugs/54` отмена оценки повторным тапом +
`ideas/20` поиск по явному вводу), лежавшую с прошлой сессии. Гейты → чистая пересборка
(`rm -rf build` + `npm run build`) → `npx firebase deploy --only hosting --project ndim-space`,
**оба таргета release complete**.

**Дифф выката проверен, а не заявлен:** `git diff 40a2576..HEAD -- calculator firestore.rules
firestore.indexes.json firebase.json src/lib/model` → **0 файлов**. Сервер синхронизации,
правила, модель и индексы НЕ тронуты.

**Сверка боя:** 14 маршрутов — 200, зеркало `ndim-space.web.app` — 301; **боевые чанки `/dims`
реально несут выкаченное** (ключи кэша `ratings:screen` и `relations:summary`, кнопка «Искать»)
— проверено грепом по 21 скачанному чанку, а не по кодам ответов; живой браузер в обеих темах ×
1440/390 (`test-results/prod-ideas18/`): лендинг со счётчиком, «Меню», «Измерения» гостю честно
предлагают войти, консоль чиста (отказы правил гостю — ожидаемы).

⚠️ **Чего гость проверить НЕ МОЖЕТ:** сам эффект кэша виден только под логином. Нужен
контрольный взгляд владельца — см. эстафету, п. 1.

### 🎯 Эстафета: что делать СЛЕДУЮЩЕМУ чату (он не знает ничего из этого)

1. **Контрольный взгляд владельца в бою, под его логином** — это единственное, что не смог
   проверить агент. Открыть `ndimspace.app`, войти, походить **Профиль ↔ Связи ↔ Пространство
   ↔ Измерения ↔ Меню** по кругу два-три раза. Ожидание: со второго захода экраны появляются
   **сразу, без карточки «Загрузка»**. Плюс проверить, что повторный тап по звезде отменяет
   отсчёт (`bugs/54`) и что поиск измерений ищет только по нажатию «Искать»/Enter (`ideas/20`).
2. **📋 `interviews/interview_005_caching_forks.md` — ЖДЁТ ОТВЕТОВ ВЛАДЕЛЬЦА.** Четыре вопроса
   по кэшированию, каждый с рекомендацией и ценой: В1 хранить ли данные между сессиями (офлайн
   против приватности на общем компьютере) · В2 живые подписки вместо чтений по запросу ·
   В3 освежать ли при возврате во вкладку · **В4 (найден при подготовке, про деньги): срок
   свежести топа — сейчас 60 с, и повторный заход на «Связи» после него стоит до ~94 чтений.**
   Владелец отвечает прямо в документе, в полях «Ответ:». Без ответов — не реализовывать.
3. **Вычитки владельца** (висят с прошлых сессий): две `[AI]`-фразы руководства (bugs/55 →
   «Решения…», п. 0) · черновик «Версия 2.0» (`researches/06`, `[AI]`).
4. **`bugs/72`** (каретка в поле поиска сбрасывается в начало) — в документе таблица
   отвергнутых гипотез и три непроверенные двери; первая и главная: **боевой это дефект или
   артефакт `vite dev`** (теперь проверяемо — фикс поиска в бою).
5. `ideas/19` (виджет «Аккаунт» в профиль) · `bugs/40` (boot-экран) · `bugs/62`+`58` (PWA) ·
   `bugs/65` (гуттер) · `bugs/67` (флейк e2e 1px).
6. **Пачка 4 целиком ждёт владельца** (макеты и развилки): `bugs/16`+`19`+`32` · `17` · `20`+`41`
   (планшет) · `28` · `31`.

**Команды, которые понадобятся сразу:** `npm run stand` (эмуляторы + сервер + vite; нужна Java) ·
`node tools/verify-ideas18.mjs` (страж кэша, 30 проверок) · `node tools/measure-ideas18.mjs`
(прибор замера навигации) · `npm test` (185) · `npm run e2e` (⚠️ гасить стенд ПО ДЕРЕВУ
ПРОЦЕССОВ перед прогоном — EXP-0059/0027).

---

**Предыдущее обновление:** 2026-07-28, вечер — 🚀 **ВОЛНА 13 ВЫКАЧЕНА В БОЙ** (слово
владельца: «Да, выкатывай хостинг») **+ закрыта связка «Измерения»: `bugs/54` (отмена
оценки повторным тапом) и `ideas/20` (поиск только по явному вводу). Беклог прогрумлен.
Связка в бой НЕ выкачена — ждёт слова владельца.**

### Что сделано этой сессией

**1. 🧹 Груминг беклога (просьба владельца «погрумь беклог, что ценно сейчас взять»).**
Каждый открытый документ сверялся С КОДОМ, а не со своим описанием. Два оказались
фактически закрытыми и в бою, но без тега: **`bugs/49`** (рельс прибит — `sticky` в
`SideRail.svelte:79-83`, инвариант стережёт `e2e/desktop-shell.spec.ts`) и **`ideas/17`**
(десктопная шапка V3-А, 501/501; приборная строка V4 остаётся планом в разделе «Будущее»).
Обоим проставлен `DONE`. Остальные подозрительные проверены и честно открыты: `bugs/20`
(во всём `src/` нет ни одного `@media (min-width: 600px)`), `bugs/28` («Без имени» живо),
`bugs/40`/`44`/`45`/`62` (в коде нет ни boot-экрана, ни `deleteUser`, ни кропа, ни
`beforeinstallprompt`).

**2. 🚀 Выкат волны 13** (`bugs/68` подёргивания раскрывашек · `69` графика «на горячую» ·
`70` карточка «Загрузка»). Чистая пересборка → `firebase deploy --only hosting`, **оба
таргета release complete**. Сверка боя: 14 маршрутов 200, зеркало 301, **страж
`node tools/verify-bug69.mjs --prod` — 60/60 на живом `ndimspace.app`** (флаг `--prod`
добавлен этой сессией, чтобы правило готовности графики проверялось там, где его видит
владелец), кадры смотрены глазами. Дифф выката проверен: сервер синхронизации, правила и
модель — **0 файлов**.

**3. ✅ `bugs/54` — повторный тап по звезде отменяет отсчёт автосохранения.** Разведка 1.x
ДО кода (`researches/12` → «Жест звезды: повторный тап») ответила на вопрос, который баг
ставил ребром: жест звезды отменял только НЕзаписанное и **сохранённых данных не трогал
никогда** (на «Все» — снять выделение + `hideSaveButton`, `app.js:3759`; на «Мой NDim ID»
по выделенной звезде — ничего, все ветки закрыты условием `checked != "checked"`; удаление
жило отдельной операцией `deleteField`). Фикс — одна ветка в `pick()`, разметка не тронута.
Страж `node tools/verify-bug54.mjs` — **60/60**, смотрит В БАЗУ эмулятора и ждёт дольше
отсчёта. Мутация: ветка отключена → «в базе оказалось 7».

**4. ✅ `ideas/20` — поиск только по явному вводу.** Введён `submitted` (запрос, по которому
ИЩУТ) отдельно от `search` (того, что печатают); поле стало формой — кнопка «Искать» +
Enter + `enterkeyhint="search"` для Android; дебаунс 250 мс удалён. **Замер экономии: набор
запроса — 0 запросов к Firestore, отправка — 2; до фикса набор стоил 3.** Страж
`verify-bug50` получил новый предмет — счёт обращений к базе (этого просил сам `ideas/20`).

**5. 📝 Канон пополнен по слову владельца:** перед крупными эпик-фичами — **гуглёж-разведка
и research-документ** («почти на всё в индустрии есть золотые стандарты и научные работы»).
Вписано в `AGENT_GUIDE` четвёртым типом разведартефакта («Обзор индустрии»), шагом **7б**
чеклиста и явными списками «что считается эпической фичей / что нет».

**6. 🐞 Заведён `bugs/72`** — каретка в поле поиска сбрасывается в начало, посимвольный ввод
даёт «икшоК» вместо «Кошки». **Дефект СТАРШЕ этой работы** (доказано прогоном на HEAD через
`git stash`, 5 из 5). Причина за один заход не нашлась — в документе таблица отвергнутых
гипотез (подмена сеттера `value` — 0 присваиваний; ловушки на `focus`/`setSelectionRange` —
ни одного вызова; `MutationObserver` — элемент тот же; контрольный опыт на голом `<input>` —
реверса нет) и три непроверенные двери.

**Регрессия сессии:** юнит **173** · e2e **70** (+2 skip) · typecheck/check **0/0** ·
`kaif:check` зелёный · стражи `verify-bug50` (весь, обе темы + EN) и `verify-bug54` (60/60) —
зелёные, оба доказаны мутациями.

**Уроки — EXP-0068** (наткнулся на дефект: первым делом докажи прогоном на HEAD, твой он или
чужой; чужой заводи НЕ пустым) · **EXP-0069** (⚠️ `Get-Content | Set-Content` в PS 5.1
уничтожает кириллицу — кодировка теряется на ЧТЕНИИ) · **EXP-0070** (у проверки «ничего не
произошло» обязана быть парная «а теперь произошло», и ставить её надо на ХОЛОДНОМ кеше) ·
**EXP-0071** (состояние выдвижного ящика спрашивай у разметки: Playwright считает видимым
элемент, обрезанный родителем через `overflow`).

### Где продолжить (следующая сессия)

1. **🚀 Выкат связки «Измерения»** — `bugs/54` + `ideas/20` лежат в `main`, в бою их нет.
   Дифф выката: `src/routes/dims/+page.svelte` + два стража. Сервер, правила и модель НЕ
   тронуты. Ждёт слова владельца.
2. **Вычитки владельца** (висят с прошлых сессий): две `[AI]`-фразы руководства (bugs/55 →
   «Решения…», п. 0) · черновик «Версия 2.0» (`researches/06`, `[AI]`) · контрольный взгляд
   на прод после волны 13.
3. **`ideas/18` — кэш сессии** (навигация без загрузок с нуля): лучший автономный заход,
   кандидаты решения расписаны в идее. ⚠️ По новому канону (шаг 7б) это **эпическая фича** —
   сначала обзор индустрии в `researches/`, потом код.
4. **`bugs/72`** (каретка) — три двери названы в документе; первая и главная: **боевой это
   дефект или артефакт `vite dev`** (проверить под логином владельца).
5. `ideas/19` (виджет «Аккаунт» в профиль) · `bugs/40` (boot-экран) · `bugs/62`+`58` (PWA) ·
   `bugs/65` (гуттер) · `bugs/67` (флейк e2e 1px).
6. **Пачка 4 целиком ждёт владельца** (макеты и развилки): `bugs/16`+`19`+`32` (ОБ-флоу,
   вход, пароль — один набор макетов) · `17` (иконки внедрены, ждут взгляда) · `20`+`41`
   (планшет — нужно устройство) · `28` (формат почты) · `31` (документы из Firestore).

---

**Предыдущее обновление:** 2026-07-28 — 🔧 **ЗАМЕРЫ ВОЛНЫ 13 ЗАКРЫТЫ: bugs/70, 69, 68
ПОЧИНЕНЫ (слово владельца — «чиним баги и боли»). Попутно найден и починен дефект гейта
push — `bugs/71`.**

**Что сделано (три бага волны 13, каждый — замер до кода, свой страж, мутации):**

1. ✅ **`bugs/70` — карточка «Загрузка» всегда компактная.** Замер опроверг слово «иногда»:
   лоадер растягивался НА ВСЕХ четырёх экранах и обеих ширинах (**1129px** при рабочей
   области 1193px) — блочный flex занимал всю ячейку, а на «Пространстве» ячейка ещё и
   `grid-column: 1 / -1`. Компактность зависела от хозяина места (случайно спасал только
   центрирующий `.loader` «Измерений»). Лечение формой в одном компоненте
   (`src/lib/ui/Loading.svelte`): обёртка забирает растяжение, карточка — `width: max-content`.
   **После: 118px везде.** Страж `node tools/verify-bug70.mjs` — **32/32**.
2. ✅ **`bugs/69` — глобальное правило владельца «графика нигде не появляется на горячую»
   обойдено по всему проекту.** rAF-трасса с придержанной графикой намеряла нарушение везде,
   кроме руководства: `support.png` — 44 кадра «место занято, картинки нет», `donation.png` —
   44, портрет автора — 45, три лица персонажей лендинга — по 82 (0,7…1,4 с пустоты на
   глазах). Приём вынесен в **`src/lib/ui/ready.ts`** (один на проект) и подключён: support,
   donate, author, лендинг (кружки, SVG-лица карты, оверлей). Страж
   `node tools/verify-bug69.mjs` — **60/60** (5 страниц × 2 темы × 390/1440).
3. ✅ **`bugs/68` — раскрывашки истории версий ходят без подёргиваний.** Покадровая трасса
   показала ровно то, что видел владелец: кривая затухает 10.9 → 4.9 и вдруг последний кадр
   **11.2px**, а на старте отдельным кадром выскакивает 4px (внешняя карточка) и 6px
   (вложенная). Причины **разделены экспериментом**: начало — отступ `[open]` включался
   мгновенно; конец — `overflow: clip` НЕ создаёт блочного контекста, и внешние отступы
   содержимого протекали наружу. Лечение: `overflow: hidden` + `transition: padding-bottom`.
   Третье заготовленное лекарство эксперимент отверг как лишнее. Страж
   `node tools/verify-bug68.mjs` — **68/68**.
4. ✅ **`bugs/71` (найден по ходу) — `npm run kaif:check` краснел на CRLF.** Гейт, обязательный
   перед КАЖДЫМ push, выдавал 15 ошибок «нет YAML-фронтматтера» на скиллах, у которых
   фронтматтер был на месте: проверка искала `/^---\n/`, а git на Windows отдаёт `---\r\n`.
   Починено нормализацией чтения в `tools/03-kaif-verify.mjs`; мутацией доказано, что гейт
   по-прежнему стережёт содержимое. Находка отправлена вверх по течению — `researches/16`, K6.

**Регрессия сессии:** юнит **173** · rules **87** · calc **30** · e2e **70** (+2 skip) ·
typecheck/check **0/0** · `kaif:check` зелёный · смежные стражи не сломаны (`verify-manual-v1`
целиком, `verify-wave12` **108/108**, `verify-batch3`). Скриншоты — `test-results/bug68|69|70/`,
смотрены глазами. **Каждый фикс доказан мутацией** (детали в документах багов).

**Уроки — EXP-0065** (в страж, меряющий ИЗМЕНЕНИЕ, клади проверку «изменение вообще произошло»:
селектор `.ver` ловил ещё и строки виджета, и трасса молча мерила неподвижный элемент) ·
**EXP-0066** (метрика стража должна быть признаком САМОГО дефекта: «доля пути» была зелёной на
сломанной анимации) · **EXP-0067** (гейт, краснеющий не по своей теме, опаснее отсутствующего).

**Где продолжить (запись закрыта: выкат состоялся 2026-07-28 вечером):**
1. ✅ **Выкат состоялся** — три фикса в бою, сверены живым браузером (`verify-bug69 --prod`,
   60/60). Дифф выката был: `src/lib/ui/Loading.svelte`, `src/lib/ui/ready.ts` (новый),
   `SimilarityDemo`, `DocBlocks`, три страницы «Меню», `menu/about`. **Сервер синхронизации,
   правила и модель НЕ тронуты.**
2. **Вычитки владельца** (ждут с прошлой сессии): две `[AI]`-фразы руководства (bugs/55 →
   «Решения…», п. 0) · черновик «Версия 2.0» (`researches/06`, `[AI]`) · контрольный взгляд
   на прод после волны 13.
3. **`ideas/18` — кэш сессии** (навигация без загрузок с нуля): лучший автономный заход,
   кандидаты решения расписаны в идее.
4. `ideas/20` (поиск по явному вводу — экономия Firestore) · `ideas/19` (виджет «Аккаунт» в
   профиль) · `bugs/65` (гуттер) · `bugs/67` (флейк e2e 1px).
5. **Пачка 4 с поднятым приоритетом `bugs/32`**: экран входа с ТРЕМЯ опциями (Email+Pass ·
   ссылка · Google — порядок владельца) — макеты вместе с bugs/16/19.
6. Прежняя очередь: тир B `plans/07` (`bugs/40` boot-экран → PWA → `bugs/44` фото-редактор).

---

**Предыдущее обновление:** 2026-07-28, ночь — ⏸ **ПАУЗА-СРЕЗ (слово владельца). Волна 13
закрыта целиком и в бою + последним заходом сделано окно «Как меня видят» (вкладка
«Видимость» упразднена).**

### ⏸ Пауза 2026-07-28: срез для следующих агентов — ЧТО БРАТЬ В РАБОТУ

**В бою прямо сейчас** (ndimspace.app, всё сверено живым браузером): руководство V1 целиком
(иллюстрации с прозрачным фоном, таблица шкалы с границами/зеброй/звёздами/смайликами,
кнопки-образцы 80×80, пагинатор глав, без плашки) · лица «карточка ждёт фото» + страховка
(сбой fetch → сетевой URL) · сервер синхронизации **0.2 (15)** с датой сборки (контейнер
заменён) · виджет «Версии» — один объект в «Меню» и «О системе» · профиль без вкладок:
кнопка **«Как меня видят»** открывает окно предпросмотра, «Назад»/Esc возвращает.

**Первым делом следующей сессии (по убыванию ценности):**
1. **Вычитки владельца** (заведено, ждёт слова): две `[AI]`-фразы руководства (bugs/55 →
   «Решения…», п. 0) · черновик «Версия 2.0» (`researches/06`, `[AI]`) · его контрольный
   взгляд на прод после волны 13 (лица, «О системе» под логином, руководство, окно
   «Как меня видят»).
2. **`ideas/18` — кэш сессии** (навигация без загрузок с нуля): лучший автономный заход,
   кандидаты решения расписаны в идее.
3. **Замеры заведённого волной 13**: `bugs/68` (подёргивания раскрывашек — rAF-трасса,
   метод EXP-0060) · `bugs/70` (лоадер на всю ширину на «Пространстве») · `bugs/69`
   (обход «готовности графики» по инвентарю в баге) · `ideas/20` (поиск по явному вводу —
   экономия Firestore) · `ideas/19` (виджет «Аккаунт» из «Меню» в профиль).
4. **Пачка 4 с поднятым приоритетом `bugs/32`**: экран входа с ТРЕМЯ опциями
   (Email+Pass · ссылка · Google — порядок владельца) — макеты вместе с bugs/16/19.
5. Прежняя очередь: `bugs/65` (гуттер) · `bugs/67` (флейк e2e 1px) · тир B `plans/07`
   (`bugs/40` boot-экран → PWA → `bugs/44` фото-редактор).

**Уроки этой сессии, которые сберегут время** (полные тексты — EXPERIENCE):
EXP-0060 (мерцания ловит только rAF-трасса с сетью на часах страницы) · EXP-0061 (литерал
от сида в страже гниёт — ожидание из данных) · EXP-0062 (e2e выжигает `test-results/`;
промис-предикат waitForFunction не ожидается) · EXP-0063 (найденный конфиг ≠ причина;
чини класс фолбэком). ⚠️ Перед e2e гаси стенд ПО ДЕРЕВУ ПРОЦЕССОВ (EXP-0059); после
пересборки перезапускай vite preview.

---

**Предыдущее обновление:** 2026-07-27, глубокая ночь — 🌊 **ВОЛНА 13 ВЛАДЕЛЬЦА (живой
прогон после выката руководства): всё срочное починено и 🚀 В БОЮ, сервер синхронизации
заменён — виджет наконец показывает «0.2 (15), Собран …».**

**Пункты волны и их судьба:**
1. 🔥 **«Нет ни одного фото»** (регрессия bugs/57 в бою) — разобран: гипотеза CORS
   ОПРОВЕРГНУТА наблюдением (v0-эндпоинт всегда отдаёт `allow-origin: *`); точная причина
   падения fetch у владельца не установлена (кандидат — блокировщик контента); закрыто
   **страховкой на весь класс**: сбой blob-пути → сетевой URL → `<img>` качает сам.
   Фолбэк доказан живьём (fetch задушен — лицо есть). CORS бакета всё равно приведён к
   правде (`storage.cors.json`, + ndimspace.app; бэкап старого — в bugs/57).
2. ✅ **«НЕ ВИЖУ НОМЕРОВ СБОРОК СЕРВЕРА. СДЕЛАТЬ»** — образ пересобран `npm run calc:image`
   (**SYNC_BUILD=15**, дата 2026-07-27T18:14Z), боевой контейнер заменён с теми же
   параметрами (inspect → stop/rm → run), первый полный проход чист (333 точки, 94
   человека, 2346 связей, записано 0 — база спокойна). «+1 за коммит кода сервера» уже
   устроено: номер = `git rev-list --count HEAD -- calculator`. Сервер виден ВОШЕДШИМ
   (гостю — честное «неизвестно», дизайн bugs/66).
3. ✅ **«Иконка на горячую — ГЛОБАЛЬНОЕ ПРАВИЛО»** — правило вписано в канон
   (`AGENT_GUIDE` → «Дизайн» → «Готовность графики»); руководство приведено: резерв места
   w/h + проявление готовой картинки (action `ready`) + preload логотипа. Обход всего
   проекта — **`bugs/69`** (инвентарь класса внутри).
4. ✅ **«Нет таблицы»** — шкала 0…10 теперь ТАБЛИЦА как в 1.x: границы ячеек + зебра
   (снято со styles.css:2790, переведено в токены).
5. ✅ **«Кнопки МАЛЕНЬКИЕ»** — образцы 80×80 с иконкой 40 (масштаб 1.x, форма ibtn 2.0).
6. ✅ **«Вырезать белый фон картинок»** — ImageMagick флуд-заливка от углов (airplane,
   hospital, network, logo; облака самолёта целы — заливка не трогает внутренности).
7. ✅ **«Убрать плашку „разделы обновляются“»** — убрана (решение о непереносе разделов
   1.x в силе, но на лице продукта об этом не пишем).
8. ✅ **«Виджет версий как объект»** — карточка с заголовком «Версии» переехала ВНУТРЬ
   `Versions.svelte`; «Меню» и «О системе» ставят один объект без своих стилей.
9. 📝 Заведено по слову: **`ideas/19`** (виджет «Аккаунт» из «Меню» → в профиль) ·
   **`ideas/20`** (поиск только по явному вводу — кнопка/Enter/Android-клавиатура) ·
   **`bugs/32`** дословно пополнен (вернуть Email+Pass ТРЕТЬЕЙ опцией: Email+Pass ·
   ссылка · Google) · **`bugs/68`** (подёргивания раскрывашек — сначала замер) ·
   **`bugs/70`** (виджет «Загрузка» растягивается на «Пространстве» — должен быть
   компактным).
10. ❓ **Вопрос владельца про вкладку «Видимость» в профиле** («зачем — видимость видна и
   настраивается на главной») — ЖДЁТ РЕШЕНИЯ ВЛАДЕЛЬЦА: вкладка была превью «как меня
   видит аудитория» (глазами Я/Все/Друзья, интервью №002). Варианты: убрать · оставить ·
   слить превью в главную. Агент сам не решает.

**Проверка волны:** стражи manual (с новыми проверками: кнопки 80, границы+зебра,
5 картинок) · wave12 **108/108** · bug57 — зелёные; юнит **173** · e2e **70** (+2 skip) ·
typecheck/check 0/0 · kaif:check. **Выкат хостинга** — оба таргета release complete,
смоук прода: маршруты 200, руководство (5/5 картинок, таблица 1px, кнопка 80, плашки нет),
консоль чиста. Сервер: логи циклов чистые.

**Где продолжить (следующая сессия):**
1. **Контрольный взгляд владельца в бою** (обновить страницу с чистым кэшем): фото на
   «Доме»/«Связях» · «О системе» под логином — «Сервер синхронизации 0.2 (15), Собран …» ·
   руководство после волны (прозрачные картинки, таблица, кнопки) · вычитка двух
   `[AI]`-фраз (bugs/55) и «Версии 2.0» (`researches/06`).
2. **Слово владельца по п. 10** (вкладка «Видимость»: убрать / оставить / слить в главную).
3. Замеры и фиксы заведённого: `bugs/68` (подёргивания — трасса) · `bugs/70` (лоадер) ·
   `bugs/69` (обход готовности графики) · `ideas/20` (поиск по явному вводу) · `ideas/19`
   (аккаунт в профиль) · `ideas/18` (кэш сессии) · далее прежняя очередь (`bugs/65`,
   `bugs/67`, тир B).

**Предыдущая запись (та же дата):** 📖 **РУКОВОДСТВО V1 «КАК БЫЛО»
РЕАЛИЗОВАНО, bugs/55 ЗАКРЫТ И ВЫКАЧЕН В БОЙ** (слово владельца: «выкатывай») **+
заведена ideas/18 (кэш сессии).** Ждёт вычитки владельцем двух адаптированных фраз.

**Выкат сверен живым браузером на `ndimspace.app`** (`test-results/prod-manual-v1/`):
пререндер `/menu/manual` несёт картинки, «Напутствие», адаптированные фразы; в обеих темах
5/5 иллюстраций отрисованы пикселями, шкала 11 оценок, пагинатор 5 точек / панель 12 глав,
консоль чиста; 7 маршрутов 200, зеркало 301. **Сервер синхронизации, правила и модель НЕ
трогались** (дифф выката: генератор, рендер документов, 2 новых UI-модуля, страж).

**Слово владельца (три решения этой сессией, дословно в bugs/55):** «Давай V1, как было,
один документ сплошняком, сбоку справа делаем floating пагинатор глав…, Звёзды, кнопки —
нужно адаптировать к текущему виду нашей нынешней реализации» · картинки в тёмной теме —
«V1 светлая карточка, только не идеально белый, а чуть-чуть в бирюзовую синеву» (выбрано по
4 вариантам `design/manual-dark-images-mockups.html`; разведка уточнила: картинки не
«белые», а 4 классов — прозрачные/белофонные/чёрный рисунок/чёрная плашка) · плюс идея
«кеширование данных и страниц» → **`ideas/18`** (одобрена по построению, ждёт своего
захода; кандидаты решения и инвалидация расписаны).

**Сделано (руководство):** генератор `extract-docs.mjs` превращает пометки исследований в
img-блоки — вернулись логотип, остров/самолёт/больница ВНУТРИ пунктов (как в 1.x),
финальная сеть, «Напутствие» главой · таблица шкалы 0…10: звезда `--star` (ноль гаснет) +
цифра + цветной смайлик (формы 1.x, цвета EXP-0057, «десятка» многоцветная —
`src/lib/ui/emojiscale.ts`) · ряд 11 смайликов · кнопки-образцы = копии `ibtn` «Измерений» ·
**в тёмной теме картинки на светлой карточке `color-mix(#fff 93%, --accent 7%)`** ·
**`src/lib/ui/ChapterNav.svelte`** — плавающий пагинатор: капсула 5 точек → панель
5 глав + 7 подглав, активная из прокрутки, у дна — последняя, Esc/тап-мимо · иерархия
заголовков (подразделы понижены до h3 только в руководстве). **Две фразы о месте кнопок
адаптированы `[AI]` под 2.0** (генератор падает, если оригинал изменился) — ждут вычитки.

**Проверка:** новый страж `node tools/verify-manual-v1.mjs` — все проверки зелёные (обе
темы × 390/1440, ~27 проверок/комбинация; иллюстрации меряются ПИКСЕЛЯМИ после прокатки
lazy, ожидание конца плавной прокрутки — циклом стабилизации), скриншоты
`test-results/manual-v1/` смотрены глазами; мутация «снята иерархия» → 2 падения.
Регрессия: юнит **173** · rules 87 · calc 30 · e2e **70** (+2 skip) · typecheck/check 0/0 ·
kaif:check. ⚠️ Дважды за день: e2e чистит `test-results/` целиком — оснастку перегонять
ПОСЛЕ e2e, если нужны её скриншоты; preview перезапускать после каждой пересборки.

**Где продолжить (следующая сессия):**
1. **Контрольный взгляд владельца в бою**: `/menu/manual` в обеих темах (картинки на
   бирюзовых карточках в тёмной, пагинатор) + «Дом»/«Связи» под логином (лица без
   мерцания, bugs/57) + **вычитка двух `[AI]`-фраз** (bugs/55 → «Решения…», п. 0) и
   черновика «Версии 2.0» (`researches/06`).
2. **Долг образа сервера**: `npm run calc:image` + замена контейнера — ждёт слова владельца
   «меняй контейнер» (рестарт боевой службы), иначе дата сборки сервера в виджете не
   появится.
3. `ideas/18` (кэш сессии — экраны перестают грузиться с нуля при навигации) — хороший
   следующий автономный заход.
4. Очередь прежняя: `bugs/65` (гуттер) · `bugs/67` (флейк e2e) · тир B `plans/07`
   (`bugs/40` boot-экран → PWA → `bugs/44` фото). Кандидат владельцу: ряд смайликов на
   карточке измерения (в руководстве теперь есть; нужен ли на карточке — его слово).

---

**Предыдущее обновление:** 2026-07-27, ночь — 📸 **bugs/57 + хвост bugs/56 ПОЧИНЕНЫ:
карточка ждёт фото, как в 1.x.** Волна 12 по-прежнему ждёт владельца (макет руководства +
текст «Версии 2.0»).

**Слово владельца (волна 11):** «рендерится страница без фотки, и затем только спустя время
появляется фотка — плохо. В оригинальном НДим делал, чтобы карточка ждала загрузки фотки» ·
«на СВЯЗИ плашки появляются резко — нужно унифицировать».

**Замер до кода (rAF-трасса по кадрам, Storage замедлен до латентности прода):** человек
видел ТРИ состояния — буква (316мс) → **пустой серый круг** (317мс) → лицо. Причина: два
сетевых раунда ПОСЛЕ вставки карточки в DOM (`avatarUrl` ставила в `src` сетевой
download-URL, байты качал уже `<img>`). «Резко на Связях» — это ОНО ЖЕ: анимация полёта
играла, а фото доезжало после неё. Канон 1.x снят с живого кода (`researches/12` → новый
раздел «Фотографии в карточках»): карточка НЕ вставлялась в DOM без скачанного фото,
`<img>` получал локальный blob (`fetchUserAvatar`, app.js:4967).

**Фикс (перенос механизма 1.x):** `data/avatar.ts` — blob целиком + `img.decode()` до
вставки + кэш сессии (дедуп параллельных, повторный заход = 0 запросов) +
`preloadAvatars(потолок 1500мс)` · `Avatar.svelte` — первый рендер берёт лицо из кэша
СИНХРОННО; не успевшее лицо доезжает атомарной заменой буквы (пустого img больше не
существует) · «Связи» — лица порции под кольцом загрузки, догрузка порций раскрывается
готовой за якорем · «Дом» — своё фото под лоадером. Люди без фото не ждут и не порождают
запросов. **Трасса после: первый кадр каждой карточки — уже с лицом.**

**Проверка:** новый страж `node tools/verify-bug57.mjs` — все проверки зелёные (обе темы ×
390/1440 + «Дом» + потолок + экономия), скриншоты смотрены глазами; **мутации: снял
предзагрузку → 5 падений, сетевой URL вместо blob → 18**. Async-бамп догрузки порций
проверен живьём (порция временно 2 → лента раскрыта до дна; на проде у людей до 250 связей).
Регрессия: юнит **173** · rules 87 · calc 30 · e2e **70** (+2 skip) · typecheck/check/
kaif:check чисто.

**🚀 ВЫКАЧЕНО В БОЙ 2026-07-27, ночь** (слово владельца: «текущее задеплой») — этот выкат
повёз разом волну 12 (виджет «Версии» в «Меню»/«О системе», портрет автора, анимация
раскрывашек) и bugs/57+56. Чистая пересборка → `npx firebase deploy --only hosting`,
**оба таргета release complete**. Сверено живым браузером на `https://ndimspace.app`
(`test-results/prod-wave12-57/`): 9 маршрутов — 200, зеркало — 301; боевые чанки несут
blob-механизм фото (`createObjectURL` + `.decode()`) и виджет версий («Собрано»); гостем в
обеих темах: «Версии» в Меню — «Приложение 2.0 (173), Собрано 27 июля 2026 г. в 19:18»,
сервер гостю честно «неизвестно»; портрет автора реально отрисован (`naturalWidth > 0`);
руководство открывается; консоль чиста. Лица на «Доме»/«Связях» гостю не видны (правила
Storage требуют входа) — контрольный взгляд владельца под своим логином.
**Сервер синхронизации, правила и модель НЕ трогались.** ⚠️ Долг остался: образ сервера
пересобрать `npm run calc:image` + заменить контейнер (по слову владельца — это рестарт
боевой службы), иначе дата сборки сервера в виджете не появится.

**Попутно:** три сгнивших стража вылечены (класс EXP-0056, все — гниль ДО этой работы:
tierA кликал кнопку, удалённую bugs/51; batch2 ждал литерал «41», сгнивший при досеве
bugs/50, и требовал у фото src эмулятора — теперь канон blob). **`bugs/67`** — новый флейк
e2e `desktop-shell` (рельс/шапка расходятся на 1px, мигает на идентичном коде, НЕ регрессия
фикса — доказано base-прогоном). Уроки: **EXP-0060** (мерцания ловит только потоковая
rAF-трасса; часы сети = часы кадров; замедляй роутом только целевой ресурс) и **EXP-0061**
(литерал от сида в страже гниёт при каждом досеве — ожидание выводи из данных).

**Где продолжить (следующая сессия):**
1. **Руководство V1 в работе** (выбор владельца 2026-07-27: «V1, один документ сплошняком,
   floating пагинатор глав справа; звёзды/кнопки адаптировать к нынешней реализации;
   картинки в тёмной теме — вопрос открыт, готовятся 4 варианта обработки»). Текст
   «Версии 2.0» (`researches/06`, `[AI]`) всё ещё ждёт приёмки владельца.
2. **Контрольный взгляд владельца в бою под логином:** «Дом»/«Связи» — лица без мерцания
   (bugs/57), виджет «Версии» с версией сервера. **Долг:** пересобрать образ сервера
   (`npm run calc:image`) и заменить контейнер по слову владельца — иначе дата сборки
   сервера в виджете не появится.
3. Очередь прежняя: `bugs/65` (полоска гуттера, ждёт взгляда владельца) · `bugs/67` (флейк
   e2e, план в баге) · тир B `plans/07` (`bugs/40` boot-экран → переиздание PWA →
   `bugs/44` фото).

---

**Предыдущее обновление:** 2026-07-27, вечер — 🎨 **ВОЛНА 12 ВЛАДЕЛЬЦА («наводим красоту и
стиль»): механическая часть сделана и проверена, 4 макета руководства ждут выбора.**

**Слово владельца (пять пунктов):** убрать виджет «Версии» с «Пространства» и перенести в
Settings с полным форматом · починить вёрстку дочерних страниц Settings, особенно
«Руководство пользователя» («было с картинками, с кнопками HTML, с таблицами») · «если был
емодзи — он был красочный. Сейчас там всё скупое и некрасивое, нет центровок» · «в Об Авторе
вообще фото потерялось» · в «О системе» сделать версию 2.0 и список версий с анимацией.

**Разведка (до кода, `researches/12` → новый раздел «Вёрстка страниц документов 1.x»):**
руководство 1.x — **66 картинок, 2 таблицы, 2 круглые кнопки-образца**; шкала 0–10 была
таблицей со звездой, цифрой и **цветным смайликом**; версии в 1.x показывались ТОЛЬКО в
«Меню» и «О системе» (один код на два контейнера, `app.js:5164`) — на «Пространстве» их не
было вовсе, то есть владелец прав по канону. **Фото автора не терялось: оно и все пять его
публичных ссылок лежали в архиве 1.x** — `homeworks/06` полтора месяца просила у владельца
то, что было на диске (EXP-0058).

**Сделано (агент сам, макетов не требовало):** ✅ **bugs/66** — виджет «Версии» уехал в «Меню»
и «О системе» одним общим компонентом `src/lib/ui/Versions.svelte`; показывает версию, сборку
**и дату со временем** по КАЖДОМУ из двух (приложение и сервер). Попутно: «О системе»
спрашивала версию сервера только на localhost — в бою её не видел никто; «Меню» ломилось за
ней гостем и сыпало отказом в консоль. · 🔧 **bugs/55** (механическая часть) — портрет автора
и пять площадок с настоящими знаками (`src/lib/ui/brands.ts`), глифы `♡ ✉ ↗ ⧉ ‹` заменены
иконками набора, `❤` вернулся ЦВЕТНЫМ `❤️` (терялся селектор U+FE0F), иллюстрации
`support.png` и `donation.png` на местах, текст коротких страниц снова по центру — это канон
1.x, а 2.0 центрировал наоборот. · 🔧 **bugs/56** — «История версий» анимируется
(`::details-content` + `interpolate-size`, токены `--motion-*`), запись «Версия 2.0» написана
черновиком `[AI]`.

**Проверка:** новая оснастка `node tools/verify-wave12.mjs` — **108/108** (обе темы ×
390/1440), скриншоты `test-results/wave12/` смотрены глазами. Оснастка нашла три вещи, две
настоящие: стенд не передавал серверу номер и дату сборки (починено в `tools/stand.mjs` —
иначе проверка была бы зелёной от пустоты) и глиф `✉` в сетке соцсетей. Регрессия: юнит
**173** · rules 87 · calc 30 · e2e **70** (+2 skip) · typecheck/check/build/kaif:check чисто.
Страж «версия вшита в сборку» **переехал** из `space.spec.ts` в `menu.spec.ts` вместе с
виджетом и проверен мутацией (2 падения).

**⚠️ Долг перед выкатом:** дата сборки СЕРВЕРА появится в бою только у образа, собранного
через `npm run calc:image` — боевой контейнер собирался до появления этого аргумента.
Пересобрать вместе с ближайшим выкатом сервера.

**Где продолжить (следующая сессия):**
1. **Владельцу — выбрать макет** `design/manual-mockups.html` (V1 «Как было» · V2
   «Главы-карточки» · V3 «Живая шкала» · V4 «Документация») и **принять или поправить текст
   «Версии 2.0»** (`researches/06`, пометка `[AI]`). Обе вещи блокируют дальнейшее.
2. После выбора — реализация руководства: ассеты уже в `static/img/docs/`, цвета шкалы
   замерены (EXP-0057), таблица и кнопки-образцы по строкам `researches/12`.
3. Хвост `bugs/56`: «на СВЯЗИ плашки появляются резко» — сначала ЗАМЕР трассой, гипотеза
   смыкается с `bugs/57` (карточка рисуется до фотографии).
4. Прежняя очередь: `bugs/65` (полоска гуттера) · тир B `plans/07` (`bugs/40` boot-экран →
   переиздание PWA → `bugs/44` фото).

**📝 Кандидат в баги (не заведён, нужно слово владельца):** в 1.x под звёздами карточки
измерения стоял ряд из **11 цветных смайликов** — в 2.0 его нет вовсе (только `★/☆`).
Спросить: нужен ли он на карточке, или достаточно шкалы в руководстве.

---

**Предыдущее обновление:** 2026-07-27 — 🚀 **ДЕСКТОПНАЯ ШАПКА V3-А ВЫКАЧЕНА В БОЙ (ideas/17).**

**Слово владельца:** «в десктоп версии оцень пустует хедер… может вспомогательные меню туда
перемещать, типа менюшка ВСЕ - МОЙ NDIM ID или может заголовок приложения Пространство NDim туда
перенести? В меню кнопок слева выше поднимится?» → 4 макета (`design/desktop-header-mockups.html`)
→ выбран **V3, геометрия А, БЕЗ имени раздела**: «пока что делаем только V3 - A, а приборную
панель с метриками — высокоуровневый план на будущее, без детализации».

**Сделано:** шапка на десктопе — первая строка сетки **во всю ширину поверх рельса**; знак и
«Пространство NDim» переехали в неё из рельса и стали **ссылкой на `/profile`** (bugs/61 переехал
вместе с ними; на телефоне знак наконец кликабелен); рельс — чистая навигация, липнет к низу шапки
(`top: var(--bar-h)`, `height: calc(100dvh - var(--bar-h))`); новый **общий замер высоты шапки**
`src/lib/ui/barheight.svelte.ts` (было бы два независимых замера — панель «Измерений» и рельс;
попутно ушёл хрупкий `document.querySelector('.bar')`). **Шесть оболочек** (/profile, /relations,
/space, /dims, /menu, DocShell) не потребовали ни строки: шапка и рельс размещают себя сами.

**Замеры живым Chrome (до → после):** десктоп шапка `x=232, w=1193, h=59` → `x=0, w=1425, h=55`;
рельс `y=0` → `y=55` (под шапкой). **Телефон 390: 61px до и после — раскладка не изменилась.**

**Проверка:** новая оснастка `node tools/verify-header-v3a.mjs` — **501/501** (6 оболочек × 2 темы ×
1024/1440 + телефон + панель + смена языка + клик по знаку), скриншоты `test-results/header-v3a/`
смотрены глазами. **Мутации:** снял полноширинность → 4 падения оснастки + 4 падения e2e · вернул
рельсу `top: 0; height: 100dvh` → 4 падения (в т.ч. «низ рельса 855 при вьюпорте 800» — буквальная
регрессия bugs/49) · вернул бренд в рельс → 6 падений. Регрессия: юнит **173** · rules 87 · calc 30 ·
e2e **70** (+2 skip) · typecheck/check/kaif:check чисто. Оснастки оболочки зелёные: bug49, bug51,
bug52 64/64, batch4, wave11 28/28, bug64 21/21.

**Попутно (не мои регрессии — проверено на базе через `git stash`):** три стража сгнили от смены
канона и приведены к правде (`verify-bug51` требовал того, что запретил bugs/52; `verify-batch4`
сравнивал оверлей с вьюпортом вместо раскладки), находка про неприкрытую полоску гуттера заведена
как **`bugs/65`** (🔬, нужен взгляд владельца на десктопе). Уроки — **EXP-0055** и **EXP-0056**.

**🚀 ВЫКАЧЕНО В БОЙ 2026-07-27** (слово владельца: «деплой конечно»): чистая пересборка (`rm -rf
build` + `npm run build`) → `npx firebase deploy --only hosting --project ndim-space`, **оба
таргета release complete**. Сверено на живом `https://ndimspace.app` **живым браузером, а не
только кодами ответов** (`test-results/prod-v3a/`): на `/profile`, `/dims`, `/menu` в обеих темах
на 1440 — шапка `x=0, w=1425` во всю ширину, рельс `y=55` под шапкой, знак ведёт на `/profile`, в
рельсе бренда нет, пять пунктов, после прокрутки шапка прибита, консоль чиста; телефон 390 —
шапка 61px, рельса нет, нижняя панель на месте. Смоук `/ · /profile · /relations · /space · /dims ·
/menu · /menu/share · /menu/manual` — все 200, зеркало `ndim-space.web.app` — 301.
**Сервер синхронизации, правила и модель данных НЕ трогались** (в диффе только `src/lib/ui`,
`src/routes/dims`, проверки и документы).

**Где продолжить (следующая сессия):**
1. **Контрольный взгляд владельца в бою** на десктопную шапку (и на телефоне — там ничего не
   должно было измениться).
2. **Приборная строка (V4)** — план крупными мазками уже в `ideas/17` («Будущее»); реализация
   требует своих 4 макетов и своего QA.
3. `bugs/65` (полоска гуттера в лайтбоксе) — решение владельца из трёх гипотез.
4. Прежняя очередь тира B (`plans/07`): `bugs/40` boot-экран → переиздание PWA → `bugs/44` фото.

---

**Предыдущее обновление:** 2026-07-26, вечер — ⏸ **ПАУЗА. ВОЛНА 10 ВЛАДЕЛЬЦА ЗАКРЫТА И В БОЮ
(bugs/50 поиск · bugs/51 «Предложить измерение»).**

**Слово владельца, два дефекта из чата:**
1. «сломался поиск измерений. В старом НДим я вводил название измерения — и оно находило его
   среди всех измерений пространства. Сейчас не находит, словно оно ищет только в том скоупе,
   который подгружен в браузере — это неприемлемо. Есть dims_list — по ним можно искать что
   угодно» → **`bugs/50`**.
2. «раньше были FAB про добавить новое измерение — теперь это запрятано вниз списка измерений,
   там пользователь никогда не найдёт» → **`bugs/51`**.

**bugs/50 — причина найдена ЧТЕНИЕМ КОДА, а не гаданием:** `searchIndex` честно искал по всему
`dims_list` (5111), но экран использовал результат как **фильтр по уже загруженным карточкам**
(первые 12 ленты + раскрытое «Мой NDim ID»). Написано это было под флагом «экономим запросы»;
разведка живого 1.x (`ndim_old/public/scripts/app.js` → `doSearchDims` 9131, `searchInJson` 9026,
`normalizeStringForSearch` 9052) показала, что канон экономил иначе: **искал по индексу и
ДОГРУЖАЛ найденное, ≤20 документов**. Сделано: догрузка найденного · `normalizeForSearch`
перенесена дословно (ё→е · дефис/тире · кавычки и скобки · римские I…XV → арабские · регистр ·
схлопывание пробелов) · порядок по релевантности (|длина имени − длина запроса|) + тай-брейк по
id · языки сравниваются ПО ОТДЕЛЬНОСТИ (склейка «ru en» давала ложные совпадения) · лимит 20 с
текстом 1.x дословно · пауза 250 мс против дребезга · метка запроса против гонки ответов ·
кольцо загрузки · «Ничего не найдено» только по ЗАВЕРШЕНИИ поиска, ошибка сети не прячется.
**Замер экономии в бою на стенде: поиск «проба» (36 совпадений) добавил 3 запроса к Firestore,
а не 5111.**

**bugs/51 — 4 макета владельцу, выбран V3 «Рядом с поиском»** (владелец называл удобным FAB
из 1.x, но, увидев варианты рядом, выбрал вход, который ничего не перекрывает). Макеты —
`design/dims-suggest-fab-mockups.html`. Сделано: 💡 в строке поиска · **строка поиска ПРИБИТА
под шапкой** (без этого V3 воспроизводил бы ту же боль) · смещение `top` = **измеренная**
высота шапки через `ResizeObserver` (живой замер: 57px на 390, 52px на 1440 — зашитая
константа врала бы на одной из ширин) · форма открывается тут же и страница поднимается к ней ·
кнопка ПОД лентой удалена · подсказка экрана RU+EN переписана · `aria-label` на кнопке.

**Проверка (ворота plans/06 пройдены):** юнит **170** (+7 стражей) · rules 87 · calc 30 ·
e2e 70 (+2 skip) · typecheck/check/kaif:check чисто · QA живым Chrome: `node tools/verify-bug50.mjs`
**31/31** и `node tools/verify-bug51.mjs` **59/59** (обе темы × 390/1440 + английский экран),
скриншоты `test-results/bug50|bug51/` смотрены глазами. **Мутации проверены на сломанной
версии:** снял нормализацию → падают 2 юнита · убрал сортировку → 2 · вернул склейку языков → 1 ·
убрал подъём к форме → краснеет QA · `static` вместо `sticky` → краснеет QA.
**Стенд досеян сором боевых данных** («Звёздные войны» с «ё», «Человек-паук» с дефисом,
«Рокки IV») — на идеальных данных нормализацию проверить нечем (урок EXP-0041). Осей на стенде
теперь 48.

**Судейский проход (`/fable-judge`) до выката нашёл три вещи:** (1) живая проверка bugs/50
гонялась ДО правок разметки для 51 — перепрогнал оба на финальном коде; (2) мёртвое состояние
`searchDone` (писалось трижды, не читалось ни разу) — вычищено; (3) осиротевший текст удалённой
кнопки `t.suggest` — убран. Заявление «сервер синхронизации, правила и модель не тронуты»
проверено диффом: 0 файлов.

**🚀 ВЫКАЧЕНО В БОЙ 2026-07-26** (слово владельца: «Выкатить вместе с FAB»): чистая пересборка →
`firebase deploy --only hosting --project ndim-space`, оба таргета release complete. Сверено на
живом `https://ndimspace.app`: боевой чанк `/dims` (`3.CBr20TAp.js`) содержит `suggest-btn`,
текст «Будут показаны только первые 20 совпадений» и таблицу римских цифр нормализации
(`VIII`/`XIII`); смоук `/ · /profile · /relations · /dims · /menu · /menu/share` — все 200.
**Сервер синхронизации и правила НЕ трогались.**

**Урок сессии — EXP-0054:** «экономия запросов» — не «не читать», а «читать мало и по делу»;
слово «осознанно» в инвентаре паритета имеет право означать только «владелец это выбрал»
(за день найдены ДВЕ ложные отметки: поиск ✅ и плавающие кнопки ✖ — обе исправлены на 🔧→✅).

**Где продолжить (следующая сессия):**
1. **Контрольный взгляд владельца в бою** на `/dims`: поиск (введите название измерения,
   которого нет на первом экране) и кнопку 💡 рядом с поиском. Плюс всё ещё висит взгляд на
   `/profile`, раскрытую связь и `bugs/06` (стиль «Измерений»).
2. **Тир B** (`plans/07`): `bugs/40` boot-экран (владелец натыкается каждый раз) → переиздание
   PWA (манифест ru/en + SW) → `bugs/44` фото (после `bugs/04` правил Storage).
3. Хвост `bugs/46` — «последнее обновление NDim ID» требует правки вычислителя.
4. Тир C ждёт ответов владельца (🔴 `bugs/45` удаление аккаунта — долг перед своим каноном).
5. Мелочь для уборки: в `src/routes/profile/+page.svelte` лежит мёртвый текст `t.suggestDim`
   (был мёртв до этой работы, чинить в чужом баге не стал).

---

**Предыдущее обновление:** 2026-07-26 — 🎯 **БОЛИ ПЕРЕЕЗДА РАЗЛОЖЕНЫ ПО ТИРАМ · ТИР A
ЗАКРЫТ (4 бага)**. Владелец спросил не «что чинить», а «что даст 80% пользы за 20%
усилий». Ответ потребовал разведки ПО КОДУ, а не по беклогу, и она перевернула оценку:
ядро считает 20 величин и вычислитель кладёт их в КАЖДУЮ запись топа — экраны показывают
три. Половина «потерянного при переезде» оказалась **вёрсткой уже посчитанных чисел**
(EXP-0052). План — **`plans/07_owner_pain_tiers.md`** (тиры A/B/C по оси «цена работы ×
нужно ли решение владельца»), сводка — в `MASTER_PLAN.md` фаза 6.
**Сделано в тире A** (5 коммитов, без единой правки сервера синхронизации, правил и
модели данных): ✅ **bugs/43** профиль вернул статистику «Дома» — виджеты «Мой NDim ID»
(эмоциональная шкала 1.x «4 (очень мало 😭)», диаметр, «Обновлен», «Синхронизирован») и
«Мои связи» (полосы 90 / 75…89 / 50…74), вводная подсказка · 🟡 **bugs/46** раскрытая
связь снова досье (персональные данные по видимости 2.0, возраст, коэффициенты
×моего/×хозяина) · ✅ **bugs/47** «Пригласить друзей» — своя страница `/menu/share` с
сеткой 13 соцсетей БЕЗ внешних скриптов · ✅ **bugs/48** «Правила оформления» дословно.
**Судейский проход поймал настоящий дефект в моей же работе до выката:** «Синхронизирован»
брал `relations.computedAt`, а он с `ideas/14` пишется ТОЛЬКО при изменении топа — у
человека со стабильным топом дата застыла бы на неделю при живом сервере. Источником стал
`points.lastSync`; страж проверен на сломанной версии (EXP-0053). **Регрессия:** юнит
**163** (+14) · rules 87 · calc 30 · e2e 70 (+2 skip) · typecheck/check/kaif:check чисто ·
QA-прогон `node tools/verify-tierA.mjs` (обе темы × 390/1440, скриншоты
`test-results/tierA/` смотрены глазами, три прогона подряд стабильны). Мутации ловятся
(шкала, полосы, возраст, коэффициенты, источник даты).
**🚀 ВЫКАЧЕНО В БОЙ 2026-07-26** (слово владельца «деплой прод»): чистая пересборка →
`firebase deploy --only hosting --project ndim-space`, оба таргета release complete.
Сверено на живом `https://ndimspace.app`: новая страница `/menu/share` отвечает 200, в её
пререндеренном HTML **13 плиток соцсетей** и текст 1.x; боевой чанк профиля
(`_app/immutable/nodes/12.*.js`) содержит «Количество установленных связей» и шкалу
«очень мало»; чанк связей — «хозяина»; смоук `/ · /profile · /relations · /dims · /menu` —
все 200. **Сервер синхронизации и правила НЕ трогались** — выкат только хостинга.
**Где продолжить:** (1) контрольный взгляд владельца на `/profile` и раскрытую связь в бою
(+ `bugs/06` — стиль «Измерений» ждёт того же слова); (2) **тир B**: `bugs/40` boot-экран ·
переиздание PWA · `bugs/44` фото (после `bugs/04`); (3) хвост `bugs/46` — «последнее
обновление NDim ID» требует правки вычислителя (формат `relations` + пересборка образа),
«был онлайн» — развилка владельца; (4) тир C ждёт ответов владельца (🔴 `bugs/45` удаление
аккаунта — долг перед собственным каноном).

**2026-07-28 — 🧰 KAIF ОБНОВЛЁН ДО РЕЛИЗА 2.0 «Excellent KAIF»** (по слову владельца). Маркер
`.kaif/kaif.json` = **2.0** (релиз 2026-07-28), `kaif:check` зелёный (28 скиллов), `update-verify`
пройден. Маршрут — **bootstrap** (свежий `KAIF.md` → `KAIF-LOADER.mjs` → машинерия 2.0), он же
рекомендованный релизом: обновление ведёт НОВОЕ ядро, а не старое. **Проход предсказан заранее на
песочной копии проекта** (`git archive HEAD` → отдельный каталог → настоящий прогон там) и совпал с
боем байт в байт. Механика: **0 файлов заменено целиком**, 161 модуль слит на месте, 5 сущностей
добавлено, 27 сохранено; **кириллицы утрачено 0**; продуктовый код и содержательные артефакты — **0
изменений**. Регрессия: юнит **173/173**, typecheck чисто, build чисто.
Что пришло: справочник **`.kaif/KAIF_REFERENCE.md`** (12-й ключевой документ, `/help-kaif` читает его
первым) · скиллы **`/end-chat`** (полное завершение с эстафетой) и **`/derive-styleguide`** ·
**`/pause` сменил смысл** — теперь мягкая парковка, а прежняя его работа уехала в `/end-chat` ·
**режимы строгости** (черновик vs канон) и правило «заметки владельца по ходу — в беклог, не в
переключение» в `AGENT_GUIDE.md` · механизированный гейт провенанса и канон-линтер в `.kaif/tools/`
(подключение опционально) · расписка `.kaif/last-update.json` и история обновлений в маркере.
⚠️ **Главный дефект прохода — удвоение переведённых документов:** 2.0 опознаёт модули по
заголовку-сигнатуре, наши русские заголовки читаются как «другие модули», и английский шаблон
вставился РЯДОМ с русским текстом (25 файлов, +6.3 тыс. строк). Ничего не потеряно, но сдавать так
нельзя — вернул из HEAD и влил настоящую дельту (фреймворк реально изменил **15 файлов из 52**)
переводом. Лечение на будущее: в маркере объявлено **`"i18n": "translated"`**. Урок — **EXP-0064**,
отчёт для агента-разработчика KAIF — **`researches/16_kaif_20_update_mechanics.md`**.
Точка отката — тег `pre-kaif-2.0`. **Где продолжить:** беклог не изменился (см. выше).

**Предыдущее обновление:** 2026-07-26 — 🧰 **KAIF ОБНОВЛЁН ДО РЕЛИЗА 1.6 «Homeostatic KAIF»**
(по слову владельца). Маркер `.kaif/kaif.json` = **1.6** (релиз 2026-07-24), `kaif:check`
зелёный, `update-verify` пройден. **Продуктовый код не тронут: 0 файлов `src/`, `calculator/`,
`e2e/`, `tools/` в диффе; содержательные артефакты (`bugs/`, `ideas/`, `interviews/`,
`researches/`, `homeworks/`, `design/`, `plans/`) — 0 изменений.** Регрессия: юнит **149/149**,
typecheck чисто, build чисто. Механика: 23 файла заменено, 29 сохранено; ручной сшивки
потребовали **24 блока правок в 12 файлах**. Что пришло в проект: **разведартефакты**
(разведдок · канон-карта · инвентарь паритета — наш `researches/12` теперь назван каноном
в `AGENT_GUIDE.md`) · **правило трёх дверей** и «наблюдение вместо додумывания» в
`PHILOSOPHY.md` · **`/fable-judge` обязателен перед КАЖДЫМ push и деплоем** + шесть
guardrail-охот · **правило одного шага** в циклах (одно изменение = один прогон гейтов =
один коммит) · неотменяемая **git-гигиена** (`git diff --stat` перед коммитом; сначала
ignore, потом инструмент; первоисточники владельца неприкосновенны) · **чеклист выката из
5 ворот** в `/release` (зеркало прода первым) · **пометки провенанса `[AI]…[/AI]`** и ворота
записи в канон-артефакты владельца · секция **«Решения, принятые без владельца»** при закрытии
любой задачи · поля **Repro / Not for** в записях опыта. ⚠️ Механика снова попыталась
заменить русские документы английскими шаблонами (`BUG_FIXING_FRAMEWORK.md` + 4 скилла) —
поймано предполётным предсказанием, локализация восстановлена, новинки 1.6 в них перенесены
переводом; урок — **EXP-0051**. Отчёт о дефектах механики обновления для агента-разработчика
KAIF — **`researches/15_kaif_16_update_mechanics.md`** (8 находок, приоритеты починки).
Точка отката — тег `pre-kaif-1.6`. **Где продолжить:** беклог не изменился — пачка 5
инвентаря паритета (bugs/43 → 46 → 44 → 47 → 48; 45 каркас) · bugs/40 boot-экран ·
пачка 4 ждёт макетов владельцу.

**Предыдущее обновление:** 2026-07-24, ночь — ⏸ **ПАУЗА. bugs/49 ПОЧИНЕН И В БОЮ: рельс
навигации прибит.** Слово владельца: «уезжает вверх и исчезает меню с кнопками в левой
панели — ДОЛЖНО БЫТЬ ЗАФИКСИРОВАНО». Причина: колонка `SideRail` в сетке `.screen` тянулась
на высоту ДОКУМЕНТА, кнопки у её верха прокручивались со страницей (макет V2 это не
проверял — превью без скролла). Фикс: `position: sticky; top: 0; align-self: start;
height: 100dvh; overflow-y: auto` (`src/lib/ui/SideRail.svelte`). Проверки: новый
e2e-инвариант в `desktop-shell.spec.ts` (скролл вниз → рельс y=0, «Меню» во вьюпорте) ·
QA-прогон `node tools/verify-bug49.mjs` **24/24** (обе темы × 1024/1440, скриншоты
`test-results/bug49/` смотрены глазами) · регрессия: юнит **149** · e2e **70** (+2 skip) ·
typecheck/kaif:check чисто. **Выкат: агентом по слову владельца** (`npm run build` +
`firebase deploy --only hosting --project ndim-space`, оба таргета release complete);
сверено в бою: живой `SideRail.*.css` на ndimspace.app содержит sticky-правило, смоук
/dims чист. ⚠️ Попутная находка: **на этой машине висел недобитый `npm run stand`**
(эмуляторы 9099/9199/8181 + vite 5173) с прошлой сессии — из-за него 6 e2e «без данных —
честная ошибка» видели живой бэкенд и падали. Стенд погашен (родитель Stop-Process; сироты
java/8181 и vite/5173 добиты), после чего e2e стал полностью зелёным. Урок кандидат в
EXPERIENCE при следующей фиксации: **перед e2e проверяй, что порты стенда свободны;
после стенда гаси всё дерево**. ⚠️ Порт 8080 на машине занят `llama-swap` (служба
владельца, НЕ трогать) — эмулятор Firestore проекта живёт на 8181 именно поэтому.
**Где продолжить (следующая сессия):** пачка 5 инвентаря паритета (bugs/43 статистика
«Дома» → 46 раскрытая связь → 44 редактор фото → 47 соцсети → 48 правила оформления;
45 каркас) · bugs/40 boot-экран · пачка 4 ждёт макетов владельцу. Полный контекст пачки 5 —
запись ниже (🗺 ИНВЕНТАРЬ) и `bugs/00_backlog_batches.md`.

**Предыдущее обновление:** 2026-07-24, ночь — 🗺 **ИНВЕНТАРЬ ПАРИТЕТА ПРОЙДЕН ЦЕЛИКОМ**
(слово владельца «делай инвентарь», без суб-агентов). `researches/12` перевыведен: карта
**всех 37 страниц** 1.x → 2.0 (источники: `d:\work\ai_sandbox\ndim_old` index.html/app.js +
25 кадров `design/reference-1x/`). Дыры → **пачка 5** (`bugs/43–48`, порядок в `bugs/00`):
**43** профиль потерял статистику «Дома» (виджеты «Мой NDim ID» с эмоц. шкалой + «Мои
связи») · **46** раскрытая связь потеряла человека и параметры пространств (×моего/×хозяина,
возраст) · **44** нет редактора фото (в 1.x FilePond+Cropper→webp) · **45** 🔴 нет
управления аккаунтом (удаление/смена почты; «человек может удалить данные» сейчас
невыполнимо) · **47** «Пригласить друзей» без сетки соцсетей · **48** форма предложения без
«Правил оформления». Почти всё — агент сам; в интервью пачки 4 добавился 1 вопрос
(формулировка подтверждения удаления, bugs/45). Также снят канон ОБ-флоу/входа целиком
(welcome→auth→verify→ob1–4) — готовая разведка для макетов 16+19+32. MASTER_PLAN фаза 6:
инвентарь ✅, выход = пачки 4–5 закрыты + волна владельца с 0 багов. Кандидат «переиздание
PWA/TWA» подтверждён инвентарём (manifest ru/en + SW были в 1.x). **Следующее:** чинить
пачку 5 (43 → 46 → 44 → 47 → 48; 45 каркас) и bugs/40.

**Предыдущее обновление:** 2026-07-24, ночь — 🧹 **РЕВИЗИЯ БЕКЛОГА + МАСТЕР-ПЛАН ПЕРЕВЫВЕДЕН**
(по слову владельца; главная боль — «сильный уход от старого NDim»). Помечены DONE: **22 бага**
(05, 07, 13–15, 18, 21–27, 29, 30, 33–39 — все фиксы в бою после выкатов 07-17 и 07-24),
**4 идеи** (06 статистика+версии, 09 трение ОБ, 10 демо, 11 гость), **3 плана** (01 модель,
02 миграция, 04 cutover). Открытый беклог сжался до: **пачка 4** (16+19+32 ОБ-флоу/вход/пароль
одним набором макетов · 17 иконки · 20 планшет · 28 имена · 31 доки) + вне пачек (40 boot-экран —
следующий, 04 Storage, 06 слово владельца, 41 устройство владельца, plans/05 e2e лендинга) +
идеи (05 UI-часть, 07, 08, 12 ⛔, 13) + homeworks/06. `bugs/00_backlog_batches.md` освежён.
**MASTER_PLAN перевыведен** (первый раз с 10 июля!): фазы 0–2 ✅, 3 ✅ базово, 4 ✅ в бою + хвосты,
5 ✅ в основном, **новая фаза 6 «ПАРИТЕТ С 1.x»** (механика волн работает: 9→14→4→3; выход —
полный инвентарь `researches/12` + волна владельца с 0 багов), фаза 7 — соцслой за ⛔ ideas/12.
Кандидаты на заведение (ждут слова владельца): полный проход инвентаря паритета · переиздание
PWA/TWA (хвост фазы 5).

**Предыдущее обновление:** 2026-07-24, вечер — 🪟 **bugs/42 ПОЧИНЕН: виджет сервера больше не
врёт.** Скриншот владельца показал: «Полная синхронизация — минуту назад, 74 пользователя»
на спокойном Пространстве. Причина — минутное сердцебиение-успех (bugs/33) склеивалось с
числами суточного прохода, а «Запланированная» показывала следующий ЦИКЛ. Починено по канону
1.x (мандат владельца «виджет перепиши со свежим видением»): вычислитель пишет **fullSync /
partialSync раздельно** (researches/13 §6), виджет — три блока: Статус (+Следующий цикл) ·
Полная (проверено · из них обновлено · Запланированная = следующая ПОЛНАЯ) · Частичная
(«ещё не было»). test:calc **30**, QA-прогон `tools/verify-bug42.mjs` 48/48 со скриншотами
обеих тем. Контейнер в бою: первый проход — «**проверено 93, обновлено 0**» (тай-брейк
доказан в бою). **Хостинг выкачен владельцем** (`firebase deploy --only hosting`, оба
target'а release complete); агент сверил боевой бандл — строки нового виджета в
`_app/immutable/nodes/13.*.js` на ndimspace.app. Осталось: контрольный взгляд владельца
на `/space`.

**Предыдущее обновление:** 2026-07-24 — ⚙️ **ideas/14 СДЕЛАНА: сервер синхронизации экономит
запросы в духе 1.x** (владелец: «в 1.x я экономил запросы, 2.0 словно не вдохновлялся старым
кодом» → «фикси»). Разведка живого `d:\work\ai_sandbox\ndim_old` → **`researches/13`** —
руководство «как всё было устроено в синхронизаторе 1.x» (кэш в памяти, дельта по
`sync_status`, ленивый контракт свежести, пустой цикл = 1 запрос). Перенесено в
`calculator/index.mjs` (v0.2.0): кэш точек процесса + дочитка только грязных, пересчёт всех
в памяти, но **запись только изменившихся топов** (diff по детерминированной сериализации),
**тихий период** `SYNC_QUIET_SECONDS` (деф. 120 с; стенд — 0), **суточный полный проход**
`SYNC_FULL_HOURS` (дописывает ленивые чужие топы, лечит кэш, чистит гостей — раз в
сутки, не каждую минуту), холостой цикл = 1 запрос + 1 запись сердцебиения (bugs/33
соблюдён), батчи по 400, тики службы не накладываются. Контракт свежести (наследие 1.x,
зафиксирован в идее): свой топ — минуты, появление в чужих топах — до суток.
**test:calc 28** (16 старых нетронуты + 12 новых: `economy.test.mjs`, `full_pass.test.mjs`),
юнит 149, typecheck/kaif:check чисто, смоук службы на эмуляторе глазами. Судейская проверка
поймала паразитную зависимость `ndim-space: file:..`, добавленную `npm --prefix ... install`
в package.json вычислителя (сломала бы Docker-сборку классом EXP-0040), — вычищена до пуша
(`226aa94`). Урок — EXP-0050. **Дополнение той же сессией — ОКНО НОВИЧКА (ideas/05,
серверная часть):** пока `firstSeen` пуст или моложе `SYNC_NEWCOMER_MINUTES` (деф. 30),
тихий период не действует — первый топ ≤60 с после первой оценки, всю первую сессию топ
свежий каждым циклом (владелец: «да, суппер, делаем»). UI-часть idea 05 (честный текст
ожидания в `/relations`) остаётся открытой.
**🚀 ВЫКАЧЕНО В БОЙ той же сессией** (слово владельца «комит, пуш, деплой прод»):
образ пересобран, контейнер `ndim-calculator` заменён (docker run из plans/04 шаг 6, но
**интервал теперь дефолтные 60 с** — раньше в проде стоял `SYNC_INTERVAL_SECONDS=3600`;
именно часовые ПОЛНЫЕ пересчёты владелец и видел; с новой экономикой минутный цикл дешевле
старого часового, а окно новичка требует минутного). Первый боевой полный проход: 332
точки, 93 человека, 5112 измерений, 2340 связей, **записано топов 77 из 93** — diff
работает, остальные совпали байт-в-байт. Расхождения были недетерминированной ничьёй
сортировки → добит **тай-брейк по uid** в `topFor` (иначе суточный проход переписывал бы
топы «по ничьей причине»), перевыкачено, второй полный проход канонизировал порядок (74
записи). **Где продолжить:** (1) назавтра `docker logs ndim-calculator` — суточный полный
проход должен записать ~0 топов; (2) владельцу глазами `/space` — «Работает», версия
0.2.0; (3) UI-часть idea 05; (4) очередь прежняя: bugs/40 · хвост пачки 3 (29, 27) ·
макеты пачки 4 · bugs/04 · e2e счётчика (plans/05).

**Предыдущее обновление:** 2026-07-17, день — 🔑 **Вход через Google починен** (владелец
пересоздал OAuth-клиент — homeworks/07 путь Б; агент прописал его в провайдер через
Identity Toolkit API и проверил живой эндпоинт: `deleted_client` исчез; остался
контрольный клик владельца на проде) · ✅ **ideas/12: все 4 вопроса закрыты владельцем**
(формулировки — только в документах, в UI не предупреждаем; фильтр: политика/ненависть/
преследование/угрозы/оскорбления/порно/эксплуатация; уведомления: панель + суточный
дайджест-письмо от сервера синхронизации) · 🔧 **Третья волна ручного теста владельца
(bugs/34–37) — ПОЧИНЕНА той же сессией, ждёт слова на выкат**: 34 шапка sticky
(`--panel-solid`, канон 1.x) · 35 история версий карточкой + плашки версий (канон app-17;
белое-на-белом не читалось) · 36 лайтбокс фото порталом в body (hover-transform карточки
ломал `fixed` — EXP-0048) + скролл-лок · 37 сортировка «Мой NDim ID» без языка (тай-брейк
по id; юнит переписан, мутации ловятся). QA-прогон — новая оснастка
`tools/verify-batch4.mjs` (все проверки зелёные, скриншоты обеих тем смотрены глазами);
регрессия: юнит **149** · e2e **70** (+2 skip) · typecheck/check чисто.
**Пачка 34–37 ВЫКАЧЕНА В БОЙ** (слово владельца «деплой прод, мне удобно тестировать в
проде») + следом **bugs/38** (манифест: на телефоне — кнопка на новую страницу
`/menu/manifesto`, на десктопе — виджет рядом с кнопками, сетка `/menu` трёхколоночная;
текст — общий модуль `src/lib/content/manifest.ts`) — тоже в бою, смоук зелёный.
**Четвёртая волна (39–41, чат 2026-07-17):** ✅ 39 в бою — язык ВЫПАДАШКОЙ «Ru ▾» +
тема ☾/☀ рядом в шапке на всех экранах (persist в самой шапке, контракт `onLang`;
две ловушки Svelte 5 — EXP-0049) · 🔬 41 (зазор под панелью на манифесте) на стенде
НЕ воспроизводится (0px на 390/800/1023) — нужен скрин/устройство владельца, добивать
вместе с bugs/20 · 🔴 40 (boot-экран вместо мелькания лендинга) — следующий в работу.
Очередь дальше: bugs/40 · хвост пачки 3 (29, 27) · макеты пачки 4 · bugs/04 ·
e2e счётчика (plans/05).

**Предыдущее обновление:** 2026-07-17 — 🧰 **KAIF обновлён до РЕЛИЗА 1.5 «Tested KAIF»**
(release-канал вышел в тот же день; `/kaif-update` по слову владельца): механический проход
`node .kaif/kaif-core.mjs update` + ручная сшивка. Маркер `.kaif/kaif.json` = **1.5**,
`kaif:check` 28/28, `update-verify` пройден. ⚠️ Механика заменила 18 русских скиллов и
`PHILOSOPHY.md` английскими шаблонами — возвращены из git с нормализацией LF, проектные
вставки README (`bugs/`, `ideas/`) сшиты заново поверх свежих RU-шаблонов 1.5; урок —
**EXP-0047**. Новое в 1.5 (пришло ещё предрелизом): `TESTING_FRAMEWORK.md` (маркеры
`[NOT-TESTED]`/`[TESTED: …]`), скиллы `fable-*` (судейский проход обязателен в циклах и /release,
ворота INTENT + близнецы TWINS в багфиксе), `.kaif/spheres/`, обёртки 5 агентских систем,
`.kaif/kaif-core.mjs` за npm-хендлами. Продуктовый код НЕ тронут (смок после предрелиза:
build ✅ · юнит 149/149 ✅ · typecheck ✅ · check 0/0 ✅ — `test-results/kaif15-smoke-report.md`).
Коммиты предрелиза: 95bac16…a7aca4c.

Предыдущая веха: 2026-07-16, ночь — ⏸ **ПАУЗА. Пачки 2 и 3 волн 08–09 починены и
ВЫКАЧЕНЫ В БОЙ** (слово владельца «комит, пуш, деплой прод»).

### ⏸ Пауза 2026-07-16: что в бою и где продолжить

**В бою** (этой сессией, тремя выкатами): пачка 1 (лендинг-редирект · поздравление только
новым · мобильная оболочка + прибитая панель) · пачка 2 (незаметная догрузка лент ·
«Мой NDim ID» без пляски · ровные карточки · фото во весь экран · непрозрачные панель и
меню) · пачка 3 (каноничный лоадер «( )» 1.x · подсказки метрик ⓘ дословно из 1.x · тап без
обводки · история версий вложенными раскрывашками · Похожесть СПРАВА — канон 1.x по слову
владельца · зачистка мёртвой зоны /dims).

**Где продолжить (по убыванию):**
1. **Хвост пачки 3**: bugs/29 (плотность меню — разведка кадра app-08 в `researches/12`),
   bugs/27 (полнота карточек измерений — инвентарь полей из `ndim-old`).
2. **Пачка 4 — развилки владельцу** (готовить макеты): ОБ-флоу + экран входа + пароль
   (16/19/32 — ОДИН набор из 4 макетов) · иконки (17 — 4 варианта SVG-набора) ·
   **планшетная раскладка** (bugs/20 — ВОСПРОИЗВЕДЁН на HeadWolf Titan1 800×1280: пустота
   над панелью до 790px; зона 600–1023px не спроектирована; варианты в баге) ·
   имя/почта вместо «Без имени» (28 — вопрос формата) · документы из Firestore (31).
3. Хвосты plans/05 (e2e лендинга на счётчик, пересборка боевого `ndim-calculator` — он
   должен писать `space/public_metrics`) и bugs/04 (правила Storage; черновик уже лежит
   `storage.rules`, НЕ выпускать без тестов).
4. Стенд вырос: эмулятор Storage (9199, у стенда есть лица), каталог 45 осей, оснастки
   `tools/verify-bugs08|batch2|batch3.mjs`. Канон процессов — `plans/06` + `researches/12`.

### 🔴 Волна 09 (2026-07-16) — вердикт владельца и реорганизация беклога

Владелец (профессиональный QA-инженер) второй волной записал 14 багов и **вердикт о
процессах**: разведка 1.x и тестирование при создании 2.0 были неприемлемо слабы — 2.0
растерял удобства оригинала. Ответ:

- **Беклог разведён по отдельным баг-документам** `bugs/10–32` (приказ владельца), в каждом —
  причина/план/проверка. Индексы волн: `bugs/08`, `bugs/09`. Пачки — `bugs/00_backlog_batches.md`.
- **`plans/06_quality_process_overhaul.md`** — радикальное улучшение процессов: (1) разведка
  ЖИВОГО поведения 1.x до кода (`researches/12_1x_behavior_inventory.md` — завести в пачке 3),
  (2) обязательный QA-прогон живым Chrome как ворота сдачи (обе темы, 2 ширины, консоль,
  скриншоты, пиксели). Вписано в шапку чеклиста `AGENT_GUIDE.md`, урок — EXP-0046.
- **Пачка 2 починена и проверена** (QA-прогон `tools/verify-batch2.mjs`, 21/21; регрессия
  149/87/16/70 зелёная; **ждёт слова владельца на выкат**): незаметная прогрессивная
  догрузка лент (13) · «Мой NDim ID» без пляски, свой порядок и догрузка (18) · ровные
  карточки (15) · фото во весь экран + **эмулятор Storage на стенде, у стенда теперь есть
  лица** (14) · непрозрачные панель (22) и меню (23) — токен `--panel-solid`.
- **Пачка 3 частично готова** (та же сессия; QA-прогон `tools/verify-batch3.mjs` 19/19,
  регрессия зелёная, ждёт выката): ✅ 21 каноничная карточка загрузки (`Loading.svelte`,
  кольцо «( )» из `ndim-old`) · ✅ 24 подсказки метрик ⓘ (тексты 1.x дословно) · ✅ 26 тап
  без обводки, `:focus-visible` видим · ✅ 30 история версий вложенными раскрывашками ·
  попутно зачищена мёртвая зона `/dims` (96px→24px). Разведка канона — новый живой
  справочник **`researches/12_1x_behavior_inventory.md`** (по plans/06).
  **Два вопроса владельцу** (🔬 исследовано, не воспроизводится):
  - `bugs/20` — пустота под панелью: замеры на 9 экранах (стенд+прод) дают 0px зазора;
    нужен экран + устройство/браузер (лучше скрин).
  - `bugs/25` — «Похожесть справа»: в 1.x она СПРАВА (кадр app-02), в 2.0 уже слева;
    на каком экране владелец её видит справа — и какой канон желаем?
  Хвост пачки 3: 29 (плотность меню), 27 (полнота карточек измерений) — следующая сессия.
  **Пачка 4** ждёт владельца: ОБ-флоу + экран входа + пароль (16/19/32, один набор макетов),
  иконки (17, 4 варианта), имя/почта (28, вопрос формата), документы в Firestore (31).

---

**Предыдущее обновление:** 2026-07-16 — 🔧 **bugs/08 (вторая волна ручного теста владельца): пачка 1 починена.**

### 🔧 bugs/08 — 8 дефектов второй волны (2026-07-16), пачка 1 готова

Владелец прошёлся по проду руками и записал 8 дефектов; документ причёсан, по каждому — план
(`bugs/08_krinik_tests.md`). **Пачка 1 починена, проверена живьём и ВЫКАЧЕНА В БОЙ**
(слово владельца «деплой»; оснастка `node tools/verify-bugs08.mjs` на стенде 18/18, смоук
прода зелёный, скриншоты в `test-results/bugs08/`). Попутно: `/dims` со старого адреса
теперь 301 на канонический домен (дыра конфига хостинга):

1. **08.1** — вошедшего (и гостя) лендинг уводит в `/profile`; заодно `landing_view` и счётчик
   людей считаются только для тех, кто остаётся (`src/lib/data/session.ts` + onMount лендинга).
2. **08.2** — поздравление «Профиль сохранён» видит только тот, чей аккаунт создан ТОЛЬКО ЧТО
   (флаг `created` в `UpgradeResult`, `getAdditionalUserInfo().isNewUser`); 331 человек из 1.x
   входит без него, воронка `account_created` больше не считает входы.
3. **08.3** — шапка/табы/нижняя панель во всю ширину, панель `sticky` прибита к низу
   (BottomNav + оболочки 6 экранов; контент — колонной 458px по центру).

**Осталось:** пачка 2 — 08.4 прогрессивная подгрузка лент (якорь за вьюпортом вместо порций
по кнопке), 08.5 фото на весь экран, 08.6 одинаковый размер карточек измерений; пачка 3 —
08.7 онбординг-флоу (4 макета владельцу), 08.8 векторные иконки по канону 1.x (4 варианта).
Регрессия после пачки 1 вся зелёная: юнит **149** · rules **87** · calc **16** · e2e **70**
(+2 skip) · typecheck/check чисто.

---

**Предыдущее обновление:** 2026-07-12, вечер — 🚀 2.0 в бою + 🔴 **боль владельца записана в баги 05–07.**
Cutover выполнен целиком (`plans/04_cutover.md`, шаги 1–6). Версия 1.x мертва, 2.0 живёт по
адресам `ndimspace.app` и `ndim-space.web.app`. Труд 331 человека перенесён и сверен.

### 🔴 Боль владельца после запуска (2026-07-12, вечер) — ТЕКУЩИЙ приоритет

Слово владельца: «Лендинг делал Fable 5 — красиво. Внутренности делал Opus 4.8 по планам
Fable 5 — топорно и с багами». По его просьбе боль записана в баги (чинить в этом порядке):

1. **`bugs/06`** — «Измерения» свёрстаны НЕ в стиле приборной панели, как другие вкладки
   (на десктопе — одна колонка на 1100px вместо канона V2 «Рабочий стол» / виджетной сетки).
2. **`bugs/05`** — по всему приложению нет анимаций: Профиль, Связи, Пространство, Меню — ноль
   переходов (инвентаризация в баге). Планка — лендинг и демо (работа Fable 5).
3. **`bugs/07`** — лендинг врёт: «С нами уже 2 184 человека» зашито литералом, в базе 331.
   В 1.x счётчик был живым (`space/public_metrics`, читался без авторизации) — вернуть.

Аудит живого прода (Playwright, аноним, все 6 маршрутов): экраны отвечают, ошибок консоли нет,
`/menu` жив (таймаут networkidle даёт длящееся Firestore-соединение — не дефект).

**Той же сессией ПОЧИНЕНО и выкачено в бой (слово владельца: «комит, пуш, деплой прод»):**
`f657668` «Измерения» по канону V2 + анимации + дедупликация имён каталога ·
`c4acf46` анимации всех внутренних экранов (канон `src/lib/ui/motion.ts`) ·
bugs/07 — литерал «2 184» изъят, счётчик живой из `space/public_metrics`.
⏭ **Хвост — `plans/05_bugs_05-07_tail.md`**: прогнать регрессию (rules 87 · calc 16), дописать
e2e лендинга на отсутствие выдуманного числа, пересобрать боевой `ndim-calculator`
(он должен начать писать `space/public_metrics`), затем пометить баги DONE.

### 🚀 Боевой запуск состоялся (2026-07-12)

- ✅ **Точка отката:** `gs://ndim-space-backup/2026-07-12-cutover-1550` (SUCCESSFUL), снята
  непосредственно перед выкатом. Предыдущая — `gs://ndim-space-backup/2026-07-12`.
- ✅ **Данные перенесены:** 331 человек · 330 точек · **4083 оценки** · 4 заявки — **5079
  документов 2.0**. Коллекции 1.x **не тронуты** (откат возможен).
- ✅ **Сверка на боевой базе ЧИСТАЯ:** у всех 331 публичный бакет · оценки 4083→4083 · ни одного
  потерянного измерения · **паритет похожести: 2306 сверено, 0 расхождений** · чужих почт нет.
  (97 «устаревших» связей 1.x — ожидаемо, их пересчитал сервер синхронизации.)
- ✅ **Хостинг:** выкачен на **оба** сайта. Проверено живьём: оба отдают 2.0 (SvelteKit), следов
  `app.js` 1.x нет.
- ✅ **Правила и индексы** выпущены — это и был необратимый шаг. Два индекса 1.x оставлены
  нетронутыми (`--force` не применялся) — по плану «старое не удаляем».
- ✅ **Админы:** `admin: true` выдан 2 людям (claim в токене, не список email, как было в 1.x).
- ✅ **Сервер синхронизации в бою:** Docker, служба, `--restart unless-stopped`, цикл **3600 с**.
  Первый боевой цикл: **93 грязные точки → 93 топа пересчитано**; Пространство — **93 человека ·
  5112 измерений · 2340 связей**. Сердцебиение пишется каждый цикл → `/space` показывает «Работает».
  Контейнер: `ndim-calculator` (на ЭТОЙ машине — она и есть серверный ПК).

**🔴 Два дефекта, пойманных НА выкате (оба починены, оба записаны в `EXPERIENCE.md`):**
1. **EXP-0039** — `04-admin-claim.mjs` разрешал почту в uid по коллекции `users`, а миграция
   этот документ **перезаписывает** схемой 2.0, где поля `email` нет: «2 почты → 0 uid».
   Починено: разрешаем через **Firebase Auth** (`getUserByEmail`). Порядок шагов больше не важен.
2. **EXP-0040** — **Dockerfile отстал от кода**: копировал только `similarity.ts`, а `index.mjs`
   импортирует ещё и `model/stats.ts` → боевой контейнер упал в цикле перезапуска. Починено:
   копируем **директориями** (`src/lib/similarity/` + `src/lib/model/`). Стенд этого поймать
   не мог — локально весь репозиторий лежит на диске.

### 🔥 БОЕВОЙ ИНЦИДЕНТ через минуты после запуска — разобран и закрыт (2026-07-12)

Владелец вошёл и **сразу увидел сломанный продукт**. Пять причин, все найдены и починены в бою
(коммиты `c444d01`, `fc2955d`). Это самая ценная часть дня — читать перед следующей миграцией.

| Что видел человек | Настоящая причина | Урок |
|---|---|---|
| «Профиль» падал у всех: `undefined (reading 'ru')` | **Каталог измерений не мигрировали вовсе.** `researches/02` описывал `dims/` строкой «тексты названия/описания — **в правилах не описаны**» → шаг просто не написали. В бою название в `name`, экран читает `title` | **EXP-0041** |
| он же | **Один служебный документ (`dims_list`) ронял ВЕСЬ экран** — `undefined.ru` в сортировке | **EXP-0041** |
| «Меню» не кликалось | В `SideRail` остался `href: null` со времён заглушки «скоро». e2e не поймал: искал ссылку через `.first()` и попадал в мобильную панель | добавлен тест на рельс |
| ссылка из письма → лендинг | **Action URL писем в проекте Firebase** указывал на корень старого домена (наследие 1.x, где `index.html` сам разбирал `oobCode`) | **EXP-0042** |
| «нет фото, в старом NDim были фотки» | Фото живёт **не в Firestore**, а в **Storage** (`users/{uid}/avatar/avatar.webp`); в базе только ФЛАГ `avatar: bool`. Флаг перенесли — за картинкой не сходили | **EXP-0043** |

Попутно: `ndim-space.web.app` → **301** → `ndimspace.app` (у продукта один дом); починен падеж
(«В Пространстве с феврал**ь** 2025» → «февраля»); `callbackUri` возвращён на штатный обработчик.

**Три вывода, которые дороже самих фиксов:**
1. **Стенд был засеян ИДЕАЛЬНЫМИ данными** — и потому не показал ничего из этого. Стенд обязан
   быть похож на бой, **включая его сор** (`seed-dev.mjs` теперь сеет документ без названия).
2. **«В правилах не описано» в исследовании — это ДЫРА, а не примечание.** Место, где модель
   говорит «не знаю», обязано стать пунктом плана.
3. **Мигрируй ХРАНИЛИЩА, а не коллекции.** Труд людей лежит ещё и в Storage, в Auth, в настройках
   проекта. Булев флаг вместо данных (`avatar: bool`) — маячок: настоящее лежит где-то ещё.

### ✅ Шаг 7 (проверка руками) — фактически пройден

Владелец вошёл по ссылке из письма, попал **в свой профиль** (485 измерений, своё фото, свои
связи). Осталось по мелочи: вход через Google и гость с лендинга — на слово владельцу.

**Откат, если понадобится** (`plans/04_cutover.md` → «Откат»): правила 1.x из git → приложение 1.x
из архива `ndim-old` → в крайнем случае импорт `gs://ndim-space-backup/2026-07-12-cutover-1550`.

### 🆕 Долги, открытые инцидентом

- 🔧 **`bugs/04`** — **правила Storage до сих пор от 1.x** (в `firebase.json` секции `storage` нет).
  Права админа зашиты **списком email** — антипаттерн, который мы уже вычистили из Firestore.
  Не дыра, но долг. ⚠️ Не ужесточать чтение аватаров сгоряча: на нём держится показ лиц.
- 🆕 **`ideas/13`** — инсталлятор сервера синхронизации + **Admin WebUI** (идея владельца).
- Фото в боевом Storage — всего **13** на 331 человека. Остальные видят букву. Это факт данных.

### Регрессия (вся зелёная, 2026-07-16)

`npm test` **149** · `test:rules` **87** · `test:calc` **16** · `e2e` **70** (+2 skip) ·
`typecheck` чисто · `check` **0/0** · `kaif:check` чисто.
⚠️ `test:calc` сразу после гашения стенда может словить гонку портов — выждать пару секунд
и перезапустить (чистый прогон зелёный).
⚠️ **gcloud проверять в PowerShell**, а не в git-bash (в bash обёртка печатает «Python» вместо аккаунта).

### Что ещё ждёт владельца

- **`homeworks/06`** — портрет и публичные ссылки автора для `/menu/author`.
- **`ideas/12`** — личная ответственность автора контента (⛔ блокирует социальный слой).
- **15 измерений**, по которым стоят оценки, удалены из каталога 1.x: восстановить или оставить?
- Приоритизация идей 07–08.

### Что сделано в этой сессии (2026-07-12, ночь) — подготовка боевого запуска

**Владелец дал добро на боевой запуск.** Сделано всё, кроме необратимого шага.

- ✅ **Резервная копия боевой базы**: `gs://ndim-space-backup/2026-07-12` — 6444 документа.
  Это точка отката. Копия аккаунтов Auth НЕ делалась (решение владельца: 299 живых почт на
  рабочем диске — это ровно тот риск, из-за которого закрывали `bugs/01`).
- ✅ **Инвентаризация боевой базы** (`tools/migrate/01-inventory.mjs`, только чтение):
  **331 человек · 4083 оценки · 5112 измерений**; НО непустой NDim ID лишь у **93** —
  у 237 из 330 профилей оценок нет вовсе. Инвариант плана 02 держится: ни у кого не два `ndimids`.
- ✅ **Миграция и сверка прогнаны НА КОПИИ** (база `sandbox2`): 4083 оценки из 4083, ни одного
  потерянного измерения, **паритет похожести: 2306 связей сверено, 0 расхождений**, чужих почт
  в данных 2.0 нет.
- 🔴 **Песочница спасла от двух необратимых ошибок:**
  1. топ связей в 1.x лежит **JSON-строкой**, а не массивом (модель `researches/02` этого не
     знала) — перенос «как есть» записал бы всем **пустые связи**;
  2. документ `relations` 1.x лежит по **тому же пути** `relations/{uid}`, что и документ 2.0, —
     «перенос» **затёр бы оригинал**. Решение: связи не мигрируем вовсе, их пересчитает сервер
     синхронизации (они производные).
- ✅ **Настройки входа боевого проекта** (владелец разрешил явно, показан точный диф): включён
  вход по ссылке из письма, включён анонимный вход (гостевой режим), домены `ndimspace.app` и
  `www.ndimspace.app` внесены в authorized domains. Google уже был включён.
  **Находка:** 109 из 331 человека не подтвердили почту — правила 2.0 без неё не показывают
  ничего. Чинится само: вход по ссылке и через Google подтверждают почту.
- ✅ **Приложение переведено в бой**: `src/lib/firebase.ts` выбирает окружение по хосту;
  экраны показывают **настоящий вход** (Google · ссылка на почту · гостем), а не заглушку;
  человек из 1.x входит своей почтой и попадает в СВОЙ профиль (тот же UID); лендинг ведёт
  в продукт, а не в 1.x.
- ✅ **Сценарий запуска написан**: `plans/04_cutover.md` — по шагам, с откатом.

**Стенд:** `npm test` **133** (+22 на миграции) · `test:rules` **85** · `test:calc` **15** ·
`npm run e2e` **57**. Мутации ловятся (молча спрятанная видимость · потерянная оценка «0» ·
переехавший email гостя · затёртая дата регистрации).

### ⛔ Блокирующее требование владельца (2026-07-12): `ideas/12` — личная ответственность автора

Социальный слой — комментарии (`ideas/03`) и личные сообщения (`ideas/04`) — **НЕ ВЫПУСКАЕТСЯ**,
пока не сделано `ideas/12_personal_responsibility.md`: минимальный фильтр экстремального контента
+ **панель администратора** с очередью «опасного» на проверку, и правовые тексты (манифест, условия
использования, политика приватности) о личной ответственности автора контента. Это про безопасность
живого человека — Николая. Тексты пишет владелец (агент готовит черновик), панель админа — отдельный
экран (интервью + 4 макета). В идее — 4 открытых вопроса владельцу.

### Что сделано в этой сессии (2026-07-12, вечер, часть 2) — экран «Меню»

Реализован по утверждённому V1 «Список» + блок манифеста (`design/menu-mockups.html`).
Смотреть: `npm run stand` → http://localhost:5173/menu

- **Меню начинается со смысла**: манифест (текст владельца, утверждён — про то, что ЕСТЬ),
  затем аккаунт (кто я · выход; **у гостя** — пунктирная карточка и «Сохранить результаты»),
  вид (язык RU/EN, тема), «Поделиться» (системное «поделиться» или копирование ссылки),
  документы, проект, версии в подвале.
- **За меню стоят 8 настоящих страниц**, а не заглушки: `/menu/manual`, `/menu/terms`,
  `/menu/privacy`, `/menu/disclaimer`, `/menu/support`, `/menu/donate`, `/menu/about`,
  `/menu/author`. Все пререндерены в статику.
- **Тексты документов НЕ переписаны руками**: `tools/extract-docs.mjs` генерирует
  `src/lib/content/docs.ts` прямо из `researches/06` и `researches/07`, где они уже сняты
  дословно. Переписывать юридический текст руками = гарантированно наплодить опечаток.
- **Чего в руководстве НЕТ и почему**: разделы 1.x про экраны («Дом», суб-вкладки «Все» /
  «Мой NDim ID», поиск и предложение измерений) и определения «Синхронизация» / «Сервер
  синхронизации» с почасовым расписанием 1.x. В 2.0 такого интерфейса и такого расписания нет —
  дословный перенос был бы враньём о продукте. Страница честно говорит, что эти разделы
  обновляются.
- **Поддержка** — `ndimspace@yandex.ru` (адрес из живого 1.x). **Пожертвование** —
  `donationalerts.com/r/mikalai_kryvusha`, спокойная страница благодарности: ни «премиума»,
  ни «плюса», ни давления (GOAL.md); e2e это проверяет.
- **Домашка `homeworks/06`**: портрет автора и его публичные ссылки (Instagram, LinkedIn,
  YouTube, VK) — их у агента нет, выдумывать ссылки на живого человека нельзя.
  EN-версии четырёх коротких страниц — перевод агента (английских оригиналов 1.x нет).

**Стенд:** `npm run e2e` **57** (+12; 6 проверок меню × 2 раскладки), мутации ловятся
(манифест без ценностей · подменённая ссылка доната · пропавшая шкала оценок).

### Что сделано в этой сессии (2026-07-12, вечер) — экран «Пространство»

Реализован по утверждённому V1 «Приборная панель» (`design/space-mockups.html`).
Смотреть: `npm run stand` → http://localhost:5173/space.

- **Сервер синхронизации теперь производит статистику Пространства** (`calculator/index.mjs`):
  пишет `space/stats` (люди · измерения · диаметр · оценки · связи · средняя похожесть ·
  распределение · новые измерения за сутки), снимок дня `space/stats/daily/{дата}` и сердцебиение
  с версией — `space/server`. Считает всё **чистый модуль** `src/lib/model/stats.ts` — тот же,
  которым экран эти цифры читает.
- **Тренды растут из снимков** (ideas/06): «за неделю» = разность с ближайшим снимком не позже
  недели назад; виджет «Сегодня» = разность со вчерашним. **Истории нет → тренда нет** (нулевой
  рост не выдумываем). Спокойные сутки → честное «Сегодня в Пространстве ничего не изменилось».
- **Состояние сервера ВЫВОДИТСЯ из сердцебиения, а не рисуется**: молчит дольше трёх интервалов —
  «Не отвечает» (слово выбрал агент: макет этот случай не покрывал; «остановлен» было бы враньём —
  мы знаем лишь, что он не отвечает). Зелёная лампочка — рядом со **словом состояния**, как велел
  владелец.
- **Стенд стал живым**: `npm run stand` теперь гоняет сервер синхронизации **службой** (цикл 15 с,
  `tools/stand.mjs`), а не однократно. Иначе стенд врал бы о продукте («Не отвечает» через 3 минуты),
  а оценка из браузера не долетала бы до «Связей» и «Пространства» сама.
- **Версии на виду**: приложение — `__APP_VERSION__` из `package.json` (vite `define`, версия
  проекта поднята до **2.0.0**); сервер — из `space/server`.
- **Правила**: клиенту запись в `space/stats` / `space/server` / снимки **запрещена** — цифры
  витрины не должен уметь подделать никто. Читают все вошедшие, **включая гостя** (в агрегатах
  нет ничего персонального).
- **Гость не входит в счётчики Пространства** — он невидим другим (В3): иначе цифра обещала бы
  людей, которых не найти.
- Побочно: русская морфология вынесена в `src/lib/ui/format.ts` (одна на все экраны), в токены тем
  добавлены `--up` / `--down` (тренды).

**Стенд:** `npm test` **111** (+23) · `npm run test:rules` **85** (+4) · `npm run test:calc`
**15** (+6) · `npm run e2e` **45** (+8). Всё проверено мутациями (5 на агрегатах, 3 на правилах,
2 на e2e). **Эмуляторный тест поймал настоящий баг**: в агрегатор передавалась карта оценок вместо
их числа — юнит-тесты этого увидеть не могли.

### Что сделано в этой сессии (2026-07-12, первая половина)

**Утверждены владельцем:** десктопная раскладка **V2 «Рабочий стол»** · вход **V4 «Врезка»** ·
«Пространство» **V1 + виджеты** · «Меню» **V1 + манифест**. Все макеты — в `design/`, отмечены
внутри файлов.

**Реализовано:** этап 3 (аккаунт без пароля) и этап 4 (воронка) плана 03 · десктопная оболочка
(`SideRail`) · карта демо переделана (лица ×1.8, шкала на весь радиус) · починена вравшая строка
«В Пространстве с мая 2025».

**Канон, установленный владельцем** (нарушать нельзя): «Измерение», НИКОГДА «ось» · «Сервер
синхронизации», НИКОГДА «вычислитель» · состояние «Работает», НИКОГДА «жив» · обращение на «Вы» ·
слово «навсегда» НЕ пишем (человек может удалить данные) · ноль внутреннего жаргона на лице
приложения · ценностные тексты пишем УТВЕРДИТЕЛЬНО (про то, что ЕСТЬ, а не чего нет) · дашборды
собираем из виджетов.

---

**Детали (предыдущее обновление той же сессии):** план 03, этапы 3 и 4.
**Этап 4 — воронка:** `space/funnel/days/{дата}`, четыре счётчика (`landing_view`, `demo_touch`,
`guest_start`, `account_created`) и больше ничего — ни UID, ни почты, ни устройства. Правила —
самое тесное место: писать может кто угодно (первый шаг случается до входа), поэтому разрешены
только эти четыре имени, за запись меняется ровно ОДИН счётчик и ровно на +1, уменьшать нельзя,
читает только админ. **`test:rules` → 81** (+8 отказов), **2 мутации ловятся**. Firebase в
`funnel.ts` тянется **динамическим импортом** — в бандл лендинга SDK не попадает (проверено).
Сквозной прогон на стенде: лендинг → 2 клика по звёздам → гость → аккаунт = воронка `1/1/1/1`
(считаем людей, а не клики). На проде воронка — осознанный no-op до появления боевого конфига.

**Этап 3 — аккаунт без пароля:**
Утверждён макет **V4 «Врезка»** (`design/login-mockups.html`): карточка гостя разворачивается в
вход прямо на месте. `src/lib/data/account.ts` — Google (`linkWithPopup`) и ссылка на почту
(`linkWithCredential`), строго `link*`, не `signIn*`: **UID сохраняется, труд гостя остаётся**.
Критично: апгрейд ОБЯЗАН снять с точки флаг `guest` (`promoteGuestPoint`) и обновить токен
(`getIdToken(true)`) — иначе человек с аккаунтом остался бы невидимым, а автоочистка удалила бы его
данные через 30 дней. Сквозной сценарий на стенде зелёный (гость оценил измерение → письмо →
аккаунт: UID тот же, оценки на месте, `guest: false`), **мутация «флаг не снят» ловится**.
Попутно: починена вравшая строка «В Пространстве с мая 2025» (была жёстко вписана).

**⚠️ ДВА КАНОНА, установленных владельцем 2026-07-12 — читать перед любой строкой текста:**
**«Словарь продукта»** в `AGENT_GUIDE.md` (**«Измерение»**, НИКОГДА «ось» — наследие 1.x, уважаем)
и **правила тона** в шапке `design/login-mockups.html` (везде «Вы» · слово «навсегда» НЕ пишем,
человек может удалить свои данные · никакого внутреннего жаргона на лице приложения).
Механизм ошибки: агент пишет интерфейс языком, которым описывает себе код. Пойман дважды.

**Предыдущее обновление:** 2026-07-11 — **десктопная раскладка V2 «Рабочий стол» утверждена и
реализована.** Приложение больше не колонка 430px по центру на широком экране: рельс навигации
слева (`src/lib/ui/SideRail.svelte`, от 1024px), нижняя панель на десктопе прячется, мобильная
раскладка не изменилась. `/profile` и `/relations` — сетки в 2 колонки (связи от 1560px — в 3),
лендинг раскрыт до 1040px, демо: слева звёзды и соседи, справа большая карта. **Карта демо
переделана** по правкам владельца: лица ×1.8, пунктирный горизонт на всю панель, шкала расстояний
растянута на весь радиус (ядро даёт долю 0…78 % — раньше она жалась в треть радиуса).
Стенд: **e2e 37** (+`e2e/desktop-shell.spec.ts`, 2 мутации ловятся). Макеты и отметка об
утверждении — `design/desktop-layout-mockups.html`. Дальше — **этап 3 плана 03: аккаунт без
пароля** (Google + волшебная ссылка через `linkWithCredential`; экран входа = 4 макета на
утверждение).

**Предыдущее обновление:** 2026-07-11, ночь — **демо похожести задеплоено на прод**
https://ndimspace.app (владелец явно подтвердил цель; проверено: демо пререндерено в живом HTML,
аватарки отдаются, sitemap/robots/404 целы) · **домашка 04 закрыта владельцем** (sitemap подан в
GSC и Яндексе, Bing-импорт завершён) · **приоритизация владельца**: следующим — **гостевой режим**
(`plans/03`, этап 2), идеи 06–08 — после. Сертификат www всё ещё `CERT_PROPAGATING` (301 работает).

**Гостевой режим (этап 2 плана 03) — нижняя половина готова той же ночью:** правила
(`isGuest()`, честный флаг `guest` на точке, гостю каталог измерений и своё, запрещена любая
публикация себя) — 17 тестов отказов (**73** в `test:rules`), 3 мутации пойманы · вычислитель
фильтрует гостей из чужих relations (свой топ гость получает) и **вычищает осиротевших
гостей** (30 дн. по `lastSync`; труд полноценных людей неприкосновенен — охраняется тестом) —
новый стенд **`npm run test:calc`** (9 проверок, 2 мутации пойманы). **UI-флоу гостя
утверждён и РЕАЛИЗОВАН** тем же вечером: V1 «Тихий бейдж» с правками владельца
(центрированная карточка, аватар-силуэт, «Пока что ты гость…», [Сохранить результаты]) —
`/profile?guest=1` на стенде входит анонимно без форм, оценки пишутся через правила,
труд переживает перезагрузку (проверено живым Playwright-сценарием); мост из демо на
localhost (e2e **32**, мутация ловится). **Этап 2 плана 03 закрыт на стенде.**
Дальше по плану 03 — **этап 3: аккаунт без пароля** (linkWithCredential, Google +
волшебная ссылка; экран входа = 4 макета на утверждение) и этап 4 (воронка).

За длинную сессию 2026-07-11 (вечер) сделано два больших блока:

1. **SEO-хвост живого домена доведён почти до конца.** sitemap.xml + robots.txt (+`Sitemap:`)
   + 404.html **задеплоены и проверены на живом https://ndimspace.app** · Lighthouse по домену:
   **Performance 100, SEO 100** · **www.ndimspace.app работает** (владелец внёс CNAME и
   ACME-TXT в Porkbun; отвечает 301 на apex; `hostState: HOST_ACTIVE`, сертификат в статусе
   `CERT_PROPAGATING` — дораскатается сам, проверь при случае) · владелец **прописал сайт в
   GSC, Яндексе и Bing** (TXT-верификации в DNS видны; хвосты — в «Ожидает решения»).
2. **Боль онбординга (`ideas/09`) отработана от исследования до кода за один день:**
   `researches/10` (трение, lazy registration, анонимный вход) → интервью №004 закрыто
   (оба слоя «пощупать», без паролей + OAuth, гость невидим другим, своя воронка) →
   план `plans/03` → макеты, владелец утвердил **V5 «Синтез»** → **демо похожести реализовано
   на лендинге**: `src/lib/ui/SimilarityDemo.svelte`, настоящее ядро импортом, ru/en,
   **аватарки-иллюстрации** (владелец сгенерировал в ChatGPT, `homeworks/05` DONE; кроп лиц
   «общим планом», hover-рост, тап → полный портрет, пульс). ✅ **Демо задеплоено на прод**
   2026-07-11 ночью — владелец подтвердил боевую цель явно.

Стенд: **30 e2e** (лендинг + демо + смоук экранов + SEO), 88 юнит, всё зелёное, мутации ловятся.
Из прошлых сессий всё в силе: прод-заглушка `/profile` и `/relations` на публичном хосте, два
экрана продукта на модели 2.0 на localhost, вычислитель в Docker (эта машина = серверный ПК),
лого V3, идеи владельца 06–08 в беклоге.

---

## Что сделано

### Фаза 0 — Фундамент ✅

- ✅ **Наследие сохранено и вынесено из проекта.** NDim Space 1.x целиком живёт в приватном
  `MikalaiKryvusha/ndim-old`. По решению владельца новый проект начинается с чистого листа: старых
  файлов здесь нет. Ключевое знание выжато в `researches/02` (модель данных) и `researches/03` (ядро).
- ✅ **`NDim_Space/` спасён.** 14 файлов лазерной гравировки (34.9 МБ) **никогда не коммитились** и
  существовали только на диске. Запушены в `ndim-old` перед удалением — иначе исчезли бы навсегда.
- ✅ **История спасена.** Все 825 коммитов 1.x упакованы в bundle (39 МБ, `git bundle verify` →
  «complete history»). 2026-07-11 владелец скопировал `.private/` на внешний носитель и удалил
  с рабочего диска. Восстановление истории: клон приватного `ndim-old` либо копия владельца.
- ✅ **Новая история начата.** `git init -b main`, корневой `.gitignore`.
- ✅ **KAIF развёрнут** (v1.4, сверен с origin побайтово, режим standard, язык русский,
  агент Claude Code). Скрипты развёртывания — в `tools/`, перепроверка — `npm run kaif:check`.
- ✅ **Проект изучен.** Математическое ядро найдено и разобрано в
  `PROJECT_ARCHITECTURE_INTERNAL_MAP.md`.
- ✅ **Аудит секретов — все три находки закрыты.** Пароль SMTP (`bugs/03` — владелец отозвал),
  299 почт (`bugs/01`), ключ подписи (`bugs/02`, риск нулевой). Секреты вынесены в `.private/` вне git;
  старый публичный репозиторий приватизирован владельцем как `ndim-old` (0 форков) — экспозиция закрыта.
- ✅ **Лицензия AGPL-3.0** — файл `LICENSE` и поле в `package.json`.

### Фаза 1.5 — Санация наследия ✅

Закрыта 2026-07-10 вместе с `homeworks/02`. Публичной утечки больше нет.

### Фаза 1 — Решения владельца ✅

Интервью №001 закрыто 2026-07-10. Решения:
**SvelteKit + TypeScript** · **купить домен + Firebase Hosting** · **Docker на домашнем ПК** ·
**AGPL-3.0** · **тот же репозиторий, история обнуляется**.

---

## Где мы сейчас

Версия 1.x (весь фронтенд в одном `index.html` на 3083 строки плюс `app.js` на 9127 строк, Firebase, и
«бэкенд», роль которого исполнял телефон владельца с открытым Chrome) осталась в приватном архиве
`ndim-old`. Здесь — чистый лист.

**Написано ядро и механизм приватности.** Математическое ядро — `src/lib/similarity/` (29 тестов,
паритет с 1.x, декодер полей `a…t`). Логика видимости профиля — `src/lib/model/visibility.ts`
(30 тестов). Оба набора проверены **мутациями**: на сломанном коде краснеют.

Зависимостей у кода нет: **Node 24 исполняет TypeScript нативно**, тесты — встроенный `node:test`.
`tsc` и `@types/node` нужны только для `npm run typecheck`.

Модель данных 2.0 спроектирована (`researches/04`), интервью №002 закрыто. Профиль разбит на
документы-бакеты по кругам владельца — это **вынужденное** следствие двух фактов: Firestore не умеет
скрывать поля при чтении, а бэкенд без входящих портов не может отфильтровать их на лету.

**Фронтенд стартовал.** Лендинг (`src/routes/+page.svelte`) реализован по утверждённому макету
«Колонна»; 2026-07-11 владелец утвердил **светлую тему «Бумага» (дефолт)** + тёмный киберпанк +
переключатель **RU/EN** (клиентский свап, RU пререндерится). Всё это покрыто e2e-стендом:
`npm run e2e` — Playwright, 10 проверок на продакшен-сборке, включая пререндер и персистентность
темы/языка. Стенд проверен мутацией: на сломанном дефолте темы честно краснеет.

| Фаза | Статус | Что там |
|------|--------|---------|
| Фаза 0 — Фундамент | ✅ | наследие, KAIF, аудит секретов |
| Фаза 1 — Решения владельца | ✅ | интервью №001 закрыто |
| Фаза 1.5 — Санация наследия | ✅ | секреты изъяты, экспозиция закрыта, AGPL-3.0 |
| Фаза 2 — Ядро 2.0 | ✅ по сути | ядро (29 тестов) + декодер `a…t` + модель данных (`researches/04`) + видимость (30) + схема (29) + правила (56) + план миграции (`plans/02`) |
| Фаза 3 — Фронтенд 2.0 | ✅ **базовые экраны готовы** | ✅ лендинг · ✅ **«Профиль»** (3 вкладки, звёзды, правка личного, аудитории, заявки на измерения) · ✅ **«Связи» по S4** (тройка метрик, «яркость связи», общее пространство) · ✅ **онбординг**: демо → гость → аккаунт без пароля → воронка · ✅ **«Пространство»** (V1, приборная панель из виджетов) · ✅ **«Меню»** (V1 + манифест + 8 страниц: руководство, условия, политика, отказ, поддержка, пожертвование, о системе, об авторе). Дальше — социальный слой (⛔ только после `ideas/12`) |
| Фаза 4 — Бэкенд | 🔧 **в работе** | ✅ **сервер синхронизации в Docker локально**: dirty-цикл, топ-250 в `relations/`, ядро без изменений, ✅ **статистика Пространства и сердцебиение** (`space/*`), проверен на стенде и из контейнера. Осталось: боевой запуск на домашнем ПК владельца — только вместе с миграцией данных 2.0 |
| Фаза 5 — Публикация | 🔲 | домен, индексация, запуск |

Подробнее — `MASTER_PLAN.md`.

---

## 🤖 Пул автономных задач (без человека и без особого железа)

> Задачи, которые агент может делать ПОЛНОСТЬЮ автономно: написать код → собрать → протестировать на
> стенде → починить → закоммитить. Цикловые скиллы (`/autoloop`, `/dayloop`, `/nightloop`) мелют этот пул.

- [x] ~~① Извлечь математическое ядро в тестируемый TS-модуль~~ ✅ `src/lib/similarity/similarity.ts`
- [x] ~~② Юнит-тесты математического ядра~~ ✅ 29 тестов, `npm test`; мутационно проверены
- [x] ~~③ Новая модель данных Firestore~~ ✅ `researches/04` + интервью №002 закрыто.
      Профиль разбит на бакеты по кругам; логика видимости реализована и покрыта тестами:
      `src/lib/model/visibility.ts` (30 тестов, мутационно проверены четыре сценария утечки).
- [x] ~~④ Типы документов Firestore~~ ✅ `src/lib/model/schema.ts` (29 тестов, 6 мутаций).
      Границы значений — паритет с 1.x; логика дружбы — источник истины для правил.
- [x] ~~⑤ Правила безопасности~~ ✅ `firestore.rules`
- [x] ~~⑥ Тесты правил на эмуляторе~~ ✅ `tests/rules/rules.test.ts` — 56 тестов, `npm run test:rules`.
      Проверяют **отказы**; восемь мутаций правил ловятся (снятие умолчания «запрещено» — 39 падений).
- [x] ~~⑦ План уважительной миграции~~ ✅ `plans/02_migration.md`. Скрипт не пишем сейчас: мигрировать
      некуда, пока нет фронтенда. Чистые функции трансформации можно написать в любой момент.
- [x] ~~⑧ Инициализировать SvelteKit + первый экран~~ ✅ Лендинг реализован по утверждённому макету
      (3 раунда правок, итог: «Колонна» + синий киберпанк-неон, mobile-first, без градиентных кнопок).
      Пререндер в статику: тексты владельца лежат в `build/index.html` — индексация решена стеком.
      Кнопки пока ведут в живое приложение 1.x (`ndim-space.web.app`).
- [~] **⑨ Деплой лендинга** — preview-канал задеплоен 2026-07-11 (см. раздел «Ожидает решения
      человека»). **Боевой** деплой (`firebase deploy --only hosting`) — только по явному решению
      владельца: он заменит живой сайт 1.x на лендинг, а кнопки лендинга сейчас ведут на этот же
      адрес (`ndim-space.web.app`) — получится кольцо. Сначала решить, куда переедет 1.x.
- [x] ~~⑩ Playwright e2e~~ ✅ `npm run e2e` — 10 проверок (mobile 430px + desktop 1440px):
      пререндер текстов в сыром HTML, дефолт светлая/RU, переключение и персистентность темы и языка,
      кнопки в живое приложение, чистая консоль. Стенд проверен мутацией (дефолт light→dark → 2
      падения). Конфиг: `playwright.config.ts` — собирает **продакшен-артефакт** и гоняет по нему.
- [x] ~~⑤ Исследование SEO и индексации~~ ✅ `researches/08_seo_indexing.md` — по официальным
      источникам, с чеклистом фазы 5 (15 пунктов, метки [сейчас]/[домен]/[2.0]). Ключевое: EN как
      клиентский свап для поисковиков не существует (нужен `/en/` + hreflang); 301 с web.app
      конфигом невозможен — дубли гасит абсолютный canonical.
- [x] ~~⑥ Извлечь продуктовые тексты (ru/en)~~ ✅ `researches/06_product_texts_1x.md` (about =
      история версий, tou, pp, disclaimer) + `researches/07_user_manual_1x.md` (руководство:
      манифест, идея, 14 терминов, механики). Вместе с `researches/05` (онбординг) — все тексты
      владельца извлечены дословно; ПДн в них нет.
- [x] ~~⑦ PageSpeed Insights~~ ✅ Lighthouse локально по живому домену (PSI API отдаёт 429 на
      анонимной квоте; обход — `npx lighthouse` с Chromium от Playwright): Performance 100,
      SEO 100, LCP 1.0 s, CLS 0 — чинить нечего.
- [x] ~~⑧ Каркас `sitemap.xml`~~ ✅ `src/routes/sitemap.xml/+server.ts` (домен — константа
      `src/lib/site.ts`, её же использует canonical) + `Sitemap:` в robots.txt + `static/404.html`.
      e2e: 22 проверки, мутация ловится. На живой домен выкатится с боевым деплоем (слово владельца).
---

## ❓ Ожидает решения человека

- 🔑 **К фазе 5 (публикация): попросить владельца проверить вход через Google руками.**
  Договорённость 2026-07-12: агент включает провайдеров на проде → **просит владельца** войти
  через Google живьём и доложить результат. Автотестом это не закрыть — эмулятор Auth открывает
  интерактивное окно. Почтовая ссылка проверена автоматически и в проверке руками не нуждается.

- ✅ **Все три набора макетов УТВЕРЖДЕНЫ владельцем 2026-07-12**:
  - `design/login-mockups.html` → **V4 «Врезка»** (✅ реализован).
  - `design/space-mockups.html` → **V1 «Приборная панель»** + виджеты «Сегодня», «Сейчас в
    Пространстве», «Версии» (✅ **реализован** 2026-07-12; смотреть: `npm run stand` → `/space`).
  - `design/menu-mockups.html` → **V1 «Список»** + блок манифеста (**реализовать** — следующая задача).

- 💬 **Мелочь на слово владельцу:** как называется состояние сервера синхронизации, когда он молчит?
  Агент выбрал **«Не отвечает»** (макет этот случай не покрывал; «остановлен» — враньё, мы этого не
  знаем). Меняется одной строкой в `src/routes/space/+page.svelte`.

- 👀 **Посмотреть живой экран профиля**: `npm run stand` → http://localhost:5173/profile
  (вход на стенд автоматический, пользователь `dev@ndim.space`). Реализован утверждённый синтез
  S1–S3: три вкладки, лента измерений с барами и раскрытием, звёзды пишут оценку в эмулятор
  через правила безопасности, «Видимость» — предпросмотр глазами гостя. Правки — говори.
- 🧭 S4 «Связи» — превью в `design/profile-synthesis-mockups.html`, утверждается отдельно,
  когда дойдём до раздела Связи.
- 🌐 **Лендинг на preview-канале:** **https://ndim-space--landing-3jvzs4cd.web.app**
  (живёт до 2026-08-10). Правки — говори, внесу. Боевой деплой решён интервью №003 (В3 = A):
  пока не деплоим, вернуться к вопросу с первым экраном 2.0 или с покупкой домена.

### Решено 2026-07-11 (ночь, после деплоя демо)

- ✅ **Демо похожести задеплоено на прод** — владелец явно подтвердил боевую цель
  (`ndimspace.app`, проект `ndim-space`). Проверено на живом домене: демо пререндерено,
  `/img/personas/*_face.png` отдаются, sitemap/robots/404 целы.
- ✅ **Домашка 04 закрыта целиком** (`homeworks/04_DONE_...`): sitemap подан в GSC и
  Яндекс.Вебмастере, Bing-импорт завершён. Индексацию дальше отслеживает агент.
- ✅ **Приоритизация владельца**: следующим — **гостевой режим** (`plans/03`, этап 2);
  идеи 06–08 остаются в беклоге после него (владелец выбрал только гостевой режим).

### Решено 2026-07-11

- ✅ **Лого утверждено: V3 «Диагональ»** — классическая N-сеть 1.x, градиент синий→циан вдоль
  диагонали, тёмная плашка. Внедрено в `static/favicon.svg`; варианты — `design/logo-mockups.html`.
- ✅ **Интервью №003 закрыто**: первый экран — **профиль с измерениями**; данные — **модель 2.0 +
  эмулятор**; боевой деплой лендинга — **отложен** (1.x не трогаем).
- ✅ **Референс-коллекция живого 1.x собрана** — `design/reference-1x/`, 22 кадра: все разделы
  и подэкраны, публичные и залогиненные (владелец дал доступ). PNG вне git (ПДн настоящих людей),
  в git — README с инвентарём и методикой пополнения.
- ✅ **`.private/` скопирован владельцем на внешний носитель и удалён с рабочего диска** —
  это закрывает шаг 3 `homeworks/02` (резервная копия вне рабочего диска). История 1.x доступна
  в приватном `ndim-old` и в копии владельца; предупреждение `kaif:check` об отсутствии
  `.private/ndim-1.x-history.bundle` — ожидаемое и корректное.
- 🧰 **Уведомлять ли 299 участников беты** о том, что их адреса были публичны? Владелец ответил
  «подумаю» (`homeworks/02_DONE_...`, шаг 4). Если решит уведомить — агент поможет составить письмо.
- 🔑 ~~Перелогиниться в Google~~ — firebase работает (проверено 2026-07-11 деплоем канала).
  `gcloud` перелогинить, только если понадобится (фазы 2–3 в нём не нуждаются).

---

## Где продолжить в следующей сессии

> Конкретный чеклист, чтобы следующая сессия (пустой контекст) могла стартовать немедленно.

1. ~~Деплой демо~~ ✅ сделано 2026-07-11 ночью (владелец подтвердил явно; урок: классификатору
   авто-режима мало голого «деплой» — нужна названная боевая цель, см. EXPERIENCE).
2. ~~Этап 2 плана 03 — гостевой режим~~ ✅ закрыт на стенде.
   ~~Десктопная раскладка~~ ✅ V2 «Рабочий стол» утверждена и реализована 2026-07-11.
2а. ~~Этапы 3 и 4 плана 03 (аккаунт без пароля, воронка)~~ ✅ закрыты.
   ~~Экран «Пространство»~~ ✅ реализован 2026-07-12 (V1 «Приборная панель»).
2б. ~~Экран «Меню»~~ ✅ реализован 2026-07-12 (V1 «Список» + манифест + 8 страниц).
2в. ~~**БОЕВОЙ ЗАПУСК** (`plans/04_cutover.md`)~~ ✅ **СОСТОЯЛСЯ 2026-07-12, 16:05 UTC.**
   2.0 в бою на `ndimspace.app` и `ndim-space.web.app`; 1.x мёртв. Подробности — в шапке этого файла.
2г. **ТЕКУЩЕЕ — хвосты запуска (по убыванию важности):**
   - ⏳ **шаг 7 cutover: владелец проверяет вход руками** (Google · старая почта · гость). Пока не
     подтверждён — `plans/04` не помечается `DONE`, а откат держим наготове.
   - 👀 **последить за живым продуктом первые дни**: логи `docker logs ndim-calculator`, `/space`
     («Работает»), жалобы людей. 109 из 331 не подтверждали почту — правила 2.0 без подтверждения
     ничего не показывают; чинится само при входе через Google или по ссылке из письма.
   - 🆕 `ideas/13` — **инсталлятор сервера синхронизации + Admin WebUI** (идея владельца, сказана
     на выкате). Панель **поглощает** админку из `ideas/12`, а не дублирует её. Ждёт приоритизации.
   - 15 измерений, по которым стоят оценки, удалены из каталога 1.x — восстановить или оставить?
3. ~~SEO: сертификат www~~ ✅ **доехал** (проверено 2026-07-11: `https://www.ndimspace.app` отвечает
   по HTTPS без ошибок TLS и отдаёт 301 на apex; apex → 200). Домашка 04 закрыта целиком — дальше
   следить за индексацией по мере появления новых страниц. DNS проверять только через DoH —
   порт 53 наружу закрыт. `gcloud` разлогинен (токена нет) — если понадобится Hosting API,
   владельцу нужно перелогиниться; для проверки домена хватило обычного curl.
4. **Идеи 06–08 ждут приоритизации владельцем**: 06 статистика+версии (дёшево начать с
   `space/server` и vite define) · 07 автобилды из git + GitHub Actions деплой лендинга
   (проверить ключ SA `github-action-855638138@ndim-space`) · 08 ИИ-команда продвижения
   (большая; приоритет логичен после входа/онбординга). Мелочь-развилка владельцу: показывать
   ли гостю «был онлайн» (в 2.0 это новое публичное поле — решение владельца).
5. Регресс: `npm test` (**111**), `npm run test:rules` (**85**), `npm run test:calc` (**15**),
   `npm run e2e` (**57**), `npm run typecheck`, `npm run check`, `npm run kaif:check`.
   ⚠️ `npm run e2e` пересобирает `build/` — после мутационных прогонов пересобери начисто,
   прежде чем деплоить артефакт (EXP-0023).
   ⚠️ Перед общим прогоном e2e **гаси стенд** (порты 8181/9099/5173): часть проверок ждёт
   состояния «эмуляторов нет» и при живом стенде даёт ложные падения (EXP-0027).
   Живой стенд: `npm run stand` → `/profile`, `/relations`, `/space`. Сид прогоняется сам, а
   **сервер синхронизации теперь работает службой** (цикл 15 с): оценка из браузера сама долетает
   до связей и статистики, а «Пространство» показывает «Работает», а не «Не отвечает».
   ⚠️ На публичном хосте экраны показывают прод-заглушку — полная функциональность видна только
   на localhost (это осознанно, до миграции данных 2.0).
5. Запрос владельца (не срочно): проверить доступ в Firebase Console через дебаг-Chrome
   (отдельный `--user-data-dir` + его логин в Google). CLI-доступ уже есть: firebase/gcloud.
6. `npm run test:rules` и `npm run stand` требуют Java: если не находится — добавь
   `C:\Program Files\Microsoft\jdk-21.0.11.10-hotspot\bin` в `$env:PATH` на время команды.
   После остановки стенда добивай эмуляторы на портах 8181/9099 (переживают обёртку, EXP-0021).
7. Перед правкой кода — `PHILOSOPHY.md` + греп `EXPERIENCE.md` по тегам. Новым тестам не верить
   без мутации (e2e профиля проверен: «убрать noindex» роняет ровно пререндер-тест).
8. **Социальный слой 2.0** (`ideas/02`–`04`: лента, комментарии+лайки, чаты) и **первая
   синхронизация новичка** (`ideas/05`) — после базовых экранов; каждый экран — интервью +
   4 макета. НЕ брать в автономные циклы.
   ⛔ **Комментарии и чаты не выпускаются без `ideas/12`** (личная ответственность автора
   контента: фильтр + панель админа + правовые тексты). Требование владельца 2026-07-12 —
   это про его личную безопасность.

---

## Открытые баги

- 🔧 **`bugs/08_krinik_tests.md`** — вторая волна ручного теста владельца, 8 дефектов.
  Пачка 1 (лендинг-редирект, поздравление, мобильная оболочка) починена 2026-07-16;
  открыты пачка 2 (подгрузка лент, фото фуллскрин, размер карточек) и пачка 3
  (онбординг-флоу и иконки — через макеты владельцу).
- 🔴 **`bugs/05_no_motion_in_app_screens.md`** — внутренним экранам не хватает анимаций, всё
  резкое (боль владельца 2026-07-12: «топорно»; Профиль/Связи/Пространство/Меню — ноль переходов).
- 🔴 **`bugs/06_dims_not_dashboard_style.md`** — «Измерения» не в стиле приборной панели, как
  другие вкладки (десктопный канон V2 «Рабочий стол» на экран не применён вовсе).
- 🔴 **`bugs/07_landing_fake_people_counter.md`** — лендинг врёт «С нами уже 2 184 человека»
  (литерал в коде; в базе 331; в 1.x счётчик был живым из `space/public_metrics`).
- 🔧 **`bugs/04_storage_rules_still_1x.md`** — правила Firebase Storage в боевом проекте всё ещё
  от версии 1.x: секции `storage` в `firebase.json` нет вовсе, права админа зашиты **списком
  email** прямо в тексте правил (в 2.0 админ — это claim в токене). Не дыра, а долг: данные не
  утекают, но права живут не там, где должны, и не покрыты тестами. Нашли, когда искали пропавшие
  фотографии (`EXP-0043`).

Три бага безопасности закрыты 2026-07-10:

- ✅ `bugs/01_DONE_pii_email_lists_in_repo.md` — 299 почт участников беты. Изъяты из репозитория;
  старый репозиторий приватизирован (`ndim-old`, 0 форков) → публичной экспозиции нет.
- ✅ `bugs/02_DONE_android_keystore_in_repo.md` — ключ подписи. Изъят; риск оценён как **нулевой**
  (в Play не публиковалось, пароля нет).
- ✅ `bugs/03_DONE_smtp_credential_in_repo.md` — живой пароль SMTP в публичном репозитории.
  Владелец отозвал пароль, литерал изъят из кода. Это была единственная настоящая утечка.

Известные дефекты версии 1.x (чинить не надо — учесть при проектировании 2.0; подробнее в
`researches/02` → «Что в этой модели плохо»):
- Математическое ядро возвращает обфусцированные поля `a…t` — нечитаемо и непроверяемо.
  Устраняется задачей ① автономного пула.
- Бэкенда нет: связи считаются в браузере админа на телефоне (O(N²) в UI-потоке).
- Права админа зашиты списком email прямо в `firestore.rules` (~25 повторений).
- `owner_email` гостя попадает в чужой документ `relations` — лишние ПДн.

## СМЕНА 14 — 2026-08-30, выстрижено из STATUS.md при закрытии

## 🟢 СМЕНА 14 ОТКРЫТА — 2026-08-30, слово владельца «начинай смену»

**Вектор задан ВЛАДЕЛЬЦЕМ дословно: «*в приоритете - починка багов, разгребание беклога, лендинг,
страница авторизации*».** Пять ролей подняты и проинструктованы; сброшена ни одна ветка не была —
все несут несмерженную работу, поэтому каждой дельта канона поехала **выводом команды**.

**Раздача:** страница входа (`bugs/19`, 4 макета) — Дизайнер · тексты хабов на 89 публичных
страниц, условие 1 вердикта №36 — dev-1 · два бага владельца `226`/`227` — dev-3 · предсуд + ревизия
беклога — Интегратор · очередь судов — QA.

### 🔑 ЧЕТЫРЕ ДЕФЕКТА, НАЙДЕННЫЕ ЗА ПЕРВЫЙ ЧАС, И ВСЕ ЧЕТЫРЕ — В РАБОТЕ МЕНЕДЖЕРА

Роли проверили выданное им и вернули по улике. Записано как класс, а не как список промахов:
· **Каноническая команда дельты не включала `bugs/` и `plans/`** (нашёл dev-3): роль с багом в
  постановке видела пустоту там, где лежал ПРЕДМЕТ её задачи. Исправлено в манифесте.
  🔑 **Перечень путей, по которому считают полноту, сам молчит о том, чего в нём нет.**
· **Знак диффа прочитан наоборот** (нашёл dev-1): «−103» в `<ветка>..main` значит «этих строк нет
  В СТВОЛЕ», то есть несмерженная работа роли, а не её потеря. Роль опровергла отношением
  (`git log MERGE_HEAD ^HEAD -- <файл>` → пусто), а не спором. 🔑 **Утверждение о паре несло знак
  без направления.** Правило дописано в манифест.
· **Число беклога названо не про то дерево** (нашёл Интегратор): 91 — факт о его ветке, не о
  стволе. Свод: 91 = 88 багов его дерева + 3 не-бага; 88 = 85 на стволе + 3 его `NEW_*`.
· **`bugs/226`: счёт по CSS опровергнут замером ТРИЖДЫ** (dev-3, 30 замеров живым Chromium).
  Плашка — ячейка сетки на **ЧЕТЫРЁХ** экранах из пяти, а не на двух; «1228 px» не существует на
  окне 1440 (там 1141); `relations` на 1560 — 411, а не 396. 🔑 **Список экранов в разборе поражён
  тем же классом, что список исключений в CSS: оба перечисляют поимённо и молчат о неполноте.**

### 🔴 `bugs/227` ПЕРЕВЕРНУЛСЯ — текст НЕ лжёт, и это меняет постановку

Разбор писал «три строки говорят человеку неправду». Проверка нашла обратное, и дважды:
· **`+page.svelte:809` — ложное попадание:** рисуется ПОСЛЕ регистрации (`{#if signupStep === 'done'}`)
  и говорит правду — `schema.test.ts:230` требует пустой аудитории у каждого свойства нового
  пользователя. Правка сделала бы правдивый текст ложным под ярлыком «исправлено по багу владельца».
· 🔴 **Две оставшиеся строки ТОЖЕ правдивы** (нашёл dev-3): невидимость гостя держат **четыре
  независимых заслона**, все зелёные и все в воротах — `guest_filter.test.mjs:53` ·
  `space_stats.test.mjs:80` · `rules.test.ts:1204` · `index.mjs:526`.
  🔑 **КЛАСС СМЕНЫ: текст не лжёт — он ОТСТАЁТ ровно настолько же, насколько отстаёт код, и
  синхронно с ним.** Починив только текст, мы не убрали бы расхождение, а перенесли его на другую
  сторону и сделали невидимым: сегодня код и текст СОГЛАСНЫ и оба расходятся с решением владельца;
  после «починки» они разошлись бы ещё и друг с другом, и продукт начал бы ВРАТЬ.
  ➜ Следствие для стража: он обязан смотреть на пару **«решение ↔ код»**, а не «код ↔ текст» —
  вторая была зелена весь месяц и осталась бы зелёной. Признак по тексту даёт ложные срабатывания.
  ➜ Решение: **текст едет ОДНОЙ порцией вместе с ответом владельца по поведению.**

### 🔴 ПОРЯДОК МЕРЖА СТАЛ НЕСУЩИМ — назначен Дизайнер → Интегратор → dev-3

Два независимых довода, оба замером: (1) `main + dev3` без Интегратора даёт `test:tools` **581/582**
и `guards` **12/13** — ствол краснеет, против git-процесса п.1; с Интегратором 582/582 и 13/13.
(2) Спрос на номера **тройной**, а не двойной, как говорил раздел 3а: Дизайнер 1 · Интегратор 2 ·
**dev-3 2**. При назначенном порядке раскладывается без правок чужих файлов: `0269` · `0270/0271` ·
`0272/0273`. ⚠️ Свобода порядка потеряна потому, что у Дизайнера `EXP-0269` стоит **жёстким числом
вместо плейсхолдера** — плейсхолдер пересчитывается молча, число требует правки его файла.

### Вердикт №37 — ветка Дизайнера `2444a77` ПРИНЯТА, 2 условия

Судья закрыл ДВЕ щели, оставленные №36 непокрытыми («максимум 155 · минимум 121» и «89 из 89
различны») — прибором ИЗ ДЕРЕВА, а не реконструкцией; весь §5 сошёлся до единицы. Условия: шапка
прибора лжёт про код возврата · **самотест не исполняется ничем** (`test:tools` не вырос ни на
единицу) и не запускается в чистом клоне (`ENOENT` на gitignore-файле 17,5 МБ).

### 🔴 ЭСКАЛАЦИЯ QA СМЕНЫ 14 — 17 ПРИБОРОВ, ЧЕЙ САМОТЕСТ НЕ ЗОВЁТ НИКТО

**Замер по дереву (`main` + порция Интегратора), не оценка:**

  приборов, несущих --selftest ................................. 28
  из них имеют парный tools/<имя>.test.mjs (лечение проекта) ... 11
  🔴 не имеют — самотест не зовёт НИКТО и НИКОГДА ............... 17
      в том числе 8 стражей из 13, стоящих в воротах npm run guards

Поимённо: `verify-probe-mark` · `verify-tool-markers` · `verify-text-integrity` ·
`verify-dormant-declarations` · `verify-bug-doc-state` · `verify-contour-parity` · `verify-copy-live` ·
`verify-cyrillic-word-boundary` · `verify-dims-manager-agreement` · `verify-stage-clean` ·
`close-bug` · `lint-owner-voice` · `measure-meta-share` · `measure-name-registry` · `review` ·
`rewrite-catalog-descriptions` · `team-workplaces`.

🔑 **Это ВОЛНА, а не случай: два независимых автора за ОДИН день** — дефект Д2 суда №37
(Дизайнер) и замечание З1 суда №38 (Интегратор). Ни один не небрежен: **у класса нет формы,
которая бы его ловила**, и каждый автор изобретает решение заново и одинаково не до конца.
🔑 **`EXP-0194` этажом выше.** Тот говорит «страж вне ворот не защищает, а ждёт, пока о нём
вспомнят»; здесь — **не страж вне ворот, а ДОКАЗАТЕЛЬСТВО стража вне ворот.** Прибор в воротах,
чей самотест не зовут, стережёт ровно до первой правки, после которой молча перестаёт стеречь:
сам прибор зелёный, ворота зелёные, красного не увидит никто.
✅ **Лечение у проекта ЕСТЬ и обкатано 11 раз** — парный `tools/<имя>.test.mjs`, зовущий
`--selftest` ДОЧЕРНИМ процессом и потому попадающий в `test:tools`. Дизайнер взял его тем же днём:
576 → 577. Цена форме — одна строка ворот. Найдено шагом 2а (искать лечение В КОДЕ): шесть файлов
называют класс прямым текстом, в т. ч. `tools/verify-import-safety.test.mjs` — «*Самотест не
способен поймать собственный незапуск: сломай стражу предохранитель наоборот — и `--selftest`
промолчит*». **Проект знал класс и записал его в правильном месте — формы всё равно не завёл.**

🔴 **ПОСТАНОВКА МЕНЕДЖЕРА (порядок QA переворачивает, ответственность моя): страж заводится
СРАЗУ, но ХРАПОВИКОМ.** QA рекомендовал «сначала 17 файлов, потом страж — иначе покрасим ствол».
Возражение: класс произвёл два экземпляра за один день, и отложенный страж всё это время не
работает ровно там, где класс активен.
· страж несёт **датированную базовую линию из 17 имён** (список выше);
· 🔴 **краснеет, если список ВЫРОС** — новый прибор с `--selftest` без пары красный с первого дня;
· 🔴 **краснеет, если имя ПРОТУХЛО** — долг закрыт, а строка осталась. Список обязан только
  убывать, и убывание ПРОВЕРЯЕТСЯ, а не декларируется;
· ствол зелёный сегодня, долг виден числом и считается сам.
⚠️ **Почему именно храповик, а не список исключений:** обычный allowlist был бы четвёртым за
смену экземпляром оплаченного класса `bugs/226` — **список исключений, пополняемый руками, молчит
о своём неполном составе.** Различие несущее: **у храповика запрещено ПОПОЛНЕНИЕ, и запрет
механизирован.** Реализация, допускающая тихое дописывание имени, — не храповик, а тот самый
список, и принята не будет.
**Порядок:** страж-храповик (мал) → 17 парных файлов по авторам зон, **восемь воротных стражей
первыми** (порядок по цене промаха, расстановка QA). Исполнителя нет: сегодня свободных рук нет,
и с вектора владельца ради машинерии никто не снимается.

### 🔴 БЛОКАДА ВЫКАТА СНЯТА СО СТОРОНЫ КОДА — условие 1 вердикта №36 закрыто (dev-1)

Принятые владельцем тексты хабов СТОЯТ В КОДЕ. Голова `c8cdee1` · база `ee6c0da` · ствол
`ee6c0da` на момент прогона. Живой риск «выкат чего угодно унесёт в бой агентские тексты на 89
публичных страниц» со стороны кода закрыт; окончательно снимется мержем.
Числа автора: `npm test` 485/485 · `test:tools` 576/576 · `guards` 13/13 · `typecheck`/`check`
чисты · **страж хабов на СОБРАННОМ сайте 101/0** · сборка начисто, один хеш. Юнит доказан семью
мутациями в обе стороны по границе; восьмая не применилась (CRLF, `EXP-0180`) и молча засчитана
не была.
🔑 **Условие 2 закрылось без единой правки чисел набора: КЕЙС-06/07/08 были верны для принятых
текстов, неверен был КОД.** Диагноз вердикта №36 подтвердился целиком.

**Решение Менеджера по предсуду: СУДИТЬ ЦЕЛИКОМ, предсуд объявлен НЕСНИМАЕМЫМ.** Порог перейдён
(2 предка при пороге 3, но объём +939 −153 — вдвое сверх ~500). Довод, чтобы следующая смена не
прочла это небрежностью: **(1)** чистого шва нет — `catalog-copy.test.ts` (+427) это ПРОВЕРКИ, и
разрыв пары «поведение ↔ его проверки» судил бы каждую половину без второй; **(2)** единственный
кандидат в предсудьи трижды назвал тяжёлый контекст и очертил границу сам — роль, назвавшая
границу, не продавливается, иначе следующая её не назовёт; **(3)** часть работы предсуда автор
уже отдал судье — **36 кадров живого браузера с текстами из DOM**, ровно то, чего не хватило
вердикту №36 («не видел ни одной собранной страницы»).

### 🔴 СПИСОК ШЕСТИ ДОЛГОВ dev-1 ПОТЕРЯН — не восстанавливать, класс записан

Прогон Менеджера: `git grep "шесть долгов"` по 40 коммитам ВСЕХ веток → единственные попадания в
самом `STATUS.md`, и оба говорят **«у него список»**. В вердиктах QA его нет, в ветках нет.
**Списка не существовало нигде, кроме окна прошлой сессии роли.**
✅ Считать закрытыми три названных здесь: «граница дроби» закрыта проверкой (стоит с `7937bcd`) ·
«три sha» исполнена заявкой · «строка про ПЕРЕЧЕНЬ» — невосстановима, СНЯТА с исполнителя.
⛔ Восстановление догадкой ЗАПРЕЩЕНО: выдуманный долг хуже признанной потери.
🔑 **ТРЕТЬЯ ФОРМА КЛАССА СМЕНЫ, названа dev-1: «долг, живущий только в голове роли, не переживает
смену окна. Носитель для долгов роли — файл, а не память.»** Ряд теперь полный: правило в
документе — пропускается · правило в отправленном сообщении — не исполняется вовсе · **долг в
памяти роли — не переживает окна.**
🔴 И горькая половина: `STATUS.md` честно написал «у него список» — **эстафета ЗНАЛА, что носитель
ненадёжен, и всё равно на него сослалась. Ссылка на чужую память — это не запись долга, а запись
того, что долг где-то был.**

### 🆕 ТРИ ДОКУМЕНТА ПОЛУЧИЛИ НОМЕРА ПРИ МЕРЖЕ — `bugs/228`, `229`, `230` (усыновление)

Приехали плейсхолдерами `bugs/NEW_*` в порции Интегратора; номера присвоены **на дереве, куда
влито**, максимум ствола был 227.
· **`bugs/228`** — образец `ЛИЧНАЯ_ФОРМА` требует ГЛАСНУЮ перед `-ет/-ит`, и отсев Н9 слепнет на
  большинстве русских глаголов («чинит», «видит», «колет», «находит», «шалит» проходят как
  не-глаголы). 📌 Родня наблюдению dev-3 о **ложных срабатываниях Н9 на ЖАНРЕ** (партия 09 дала
  12,3 % против 7,9 % на остальном корпусе) — но документы РАЗНЫЕ намеренно: **их лечения двигают
  одно число в противоположные стороны**, 228 обязано подозрения увеличить, жанровое — уменьшить,
  и в одном документе приёмка ни одного не читалась бы.
· **`bugs/229`** — утверждение о ПАРЕ (ветка ↔ ствол) переживает свою правду молча, стража нет.
  🔴 Смена 14 подтвердила его ЧЕТЫРЕ раза за день: база dev-1 устарела на 10 коммитов за час ·
  база Интегратора сдвинулась под резолвом дважды · числа заявки dev-3 сняты против ствола вместо
  дерева слияния · судья назвала «позади 9», к моменту работы стало 11. Правило трёх sha (ниже)
  и есть частичное лечение.
· **`bugs/230`** — висячая перекрёстная ссылка `[[…]]` в журнале уроков: связь рвётся молча.
  🔑 **Документ подтвердил себя собственным переименованием:** присвоение номера тут же породило
  три висячих ссылки на его прежнее имя, и одна из них жила ВНУТРИ него самого. Живые исправлены,
  в `reports/` оставлены намеренно — летопись.
⚠️ **Страж `состояние документов bugs/` покраснел на П2 сразу после переименования** (228 и 230 —
сироты, ни одной ссылки из живого документа) и потребовал этой строки. Сработал ровно по замыслу.

### 🔑 НОВАЯ ФОРМА КЛАССА ДНЯ — ПРОТУХАЕТ НЕ СТРОКА, А ОСНОВАНИЕ (автор dev-1)

Его слова дословно: «*Мы весь день ловили носитель без срока годности — строку, статус, долг,
число. Здесь протухло ОСНОВАНИЕ: „`static/` это полный ответ" было правдой когда-то и перестало
быть ею молча, никем не тронутое. Устаревшая строка врёт о факте; устаревшее основание врёт о
том, что на нём можно строить.*»

🔴 **Почему это ОТДЕЛЬНАЯ форма, а не пункт в списке того же класса.** Все прочие случаи смены —
«строка пережила своё снятие» · «число против движущейся базы» · «долг в памяти роли» · «статус в
шапке» — про носитель, который СОДЕРЖИТ неправду. **Здесь неправды не содержалось нигде:** карта
честно иллюстрировала, `static/` честно лежал. **Неверным было то, что из них ВЫВОДИЛИ** — и
вывод жил только в голове. Ни страж, ни датировка, ни перечитка его бы не поймали.

✅ **ПРАКТИЧЕСКОЕ СЛЕДСТВИЕ, ради которого форма и записана.** Чек-лист судьи велит спрашивать
у числа «**откуда это?**». Отсюда родная пара, которой не было: **«на чём стоит это правило, и
верно ли оно СЕЙЧАС?»** — задавать её ДО того, как правило написано.

**Случай целиком, с ценой.** Менеджер поручил дописать в карту правило «файл, отдаваемый наружу,
обязан появиться в таблице тем же изменением». У dev-1 текст был уже готов — с таблицей и с
ссылкой на `bugs/226`. **То есть опись, ведомая руками, в документе, лечащем последствия ровно
этого класса.** Судья померил: **в карте три записи `static/` из семи**, и карта никогда не была
описью — она объявляет себя ориентиром. **Замер стоил тридцати секунд и остановил правило,
которое врало бы всем и постоянно.**
⚖️ **Разделение доли, проведённое dev-1 и принятое судьёй:** «*проверить основание, прежде чем
строить на нём ОБЯЗАТЕЛЬСТВО, — работа того, кто обязательство пишет. Строка врёт одному
читателю, правило врёт всем и постоянно.*» **Указатель дешевле правила ровно настолько, насколько
читатель дешевле всех будущих читателей.**

### 🔴 ОДИННАДЦАТЫЙ ЭКЗЕМПЛЯР `bugs/177` — И ЦЕНУ ЗАПЛАТИЛ ВЛАДЕЛЕЦ

**Менеджер дал владельцу ТРИ ССЫЛКИ НА МЁРТВЫЕ СТРАНИЦЫ.** Контур вычитки поднимался командой,
обёрнутой в ограничение по времени (`timeout 45`), — сервер жил 45 секунд после того, как адрес
был назван в чате. Владелец, зайдя по любой из трёх, увидел бы пустоту.

🔑 **Утверждение «страница поднята» было истинным в момент написания и переставало быть им через
минуту** — тот самый класс, который смена ловила весь день десять раз и счёт которому вёл сам
Менеджер. **Одиннадцатый экземпляр он не заметил у себя, ведя счёт у других.**
🔴 **И это первый экземпляр за смену, где цену заплатил НЕ агент, а владелец.** Прочие десять
стоили ролям заходов; этот стоил ему времени и доверия к контуру.

✅ **ПРАВИЛО, ЗАПИСАННОЕ ИЗ НЕГО:** **утверждение о живом процессе проверяется ОБРАЩЕНИЕМ к нему,
а не фактом его запуска.** Одна команда, и мёртвый порт виден сразу:
`curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:<порт>/` → 200 или ничего.
✅ **Форма подъёма исправлена:** страницы вычитки поднимаются ФОНОВЫМ процессом на постоянных
портах (`--port 8069/8070/8071`), а не разовой командой с таймаутом; адрес называется владельцу
ТОЛЬКО после ответа 200, снятого прибором.

### 🔴 КЛАСС СМЕНЫ 14, ПОДТВЕРЖДЁННЫЙ ПЯТЬ РАЗ РАЗНЫМИ РУКАМИ ЗА ОДИН ЧАС

**НЕСВЕЖИЕ ДАННЫЕ ДАЮТ КРАСНОЕ, НЕОТЛИЧИМОЕ ОТ НАСТОЯЩЕЙ НАХОДКИ.** Пять экземпляров, все
2026-08-30, все у разных ролей:
· **Менеджер** — `verify-catalog-hubs` после мержа dev-1 дал **4 провала** с предметными строками
  («`build
u\catalogook.html`: оценённых 9 из 60, а заголовок зовёт топом»). Читал сборку от
  **18:57 предыдущего дня** при мерже в 14:40. Пересборка → **101/0**.
· **Дизайнер** — красный на `bugs/219` «документ объявлен неначатым, а работа есть». Не его файл:
  ветка отстала, лечение — `git merge main`, а не правка чужого документа.
· **QA** — красный с виновным коммитом ИЗ СТВОЛА: прибор читает живой общий ref (`HEAD, main`), а
  файлы берёт из своего дерева, и увидел **коммит из будущего дерева при шапке из настоящего**.
· **dev-3** — номер `231`, честно проверенный на трёх деревьях и занятый Менеджером через час.
· **dev-1** — четыре бага числились открытыми, будучи закрытыми делом (шапки «починен в ветке»).

🔑 **Форма класса злая: красное на несвежем печатает ПРЕДМЕТНЫЕ, убедительные строки** — оно
выглядит не как поломка прибора, а как находка про твою работу. **Отличается только временем файла
или sha, а не текстом сообщения.**
✅ **Что сработало трижды и потому идёт правилом:** «не объявляй пожар, не перемерив» и «напечатай
саму строку» (чек-лист QA). Судья прямо сказал: «меня спасла не бдительность, а ПРАВИЛО».
🔑 **Рабочая форма правила, короче нашей (QA): спрашивай у красного не «ЧТО», а «ОТКУДА».**

🔴 **ПРОТИВОВЕС, БЕЗ КОТОРОГО ЭТО ПРАВИЛО ОПАСНЕЕ КЛАССА (назван QA, и он прав): это НЕ повод не
доверять красному.** Три из трёх оказались ложными в тот час — и правило стоит ровно затем, чтобы
**ЧЕТВЁРТЫЙ, настоящий, не был объявлен «опять несвежесть»**. Правило требует ПЕРЕМЕРИТЬ, а не
отмахнуться; отмахнувшийся будет прав три раза и катастрофически неправ на четвёртый.
🔑 **И вывод QA о самом разделе, который дороже отдельных строк: «журнал ложных КРАСНЫХ» — имя уже
своего содержимого.** Половина классов — ложные ЗЕЛЁНЫЕ или ложные РАСХОЖДЕНИЯ, **и короткое
зелёное опаснее красного: красное идут проверять, а его — нет.** Пять пропущенных случаев в первом
замере ворот выглядели ровно так же зелено, как честные 447.

### 📌 ДВЕ ПОСТАНОВКИ, РОЖДЁННЫЕ ЭТИМ КЛАССОМ (исполнителя назначить)

1. 🔴 **`tools/verify-judge-stand.mjs` — «стенд годен» есть утверждение о ПАРЕ без второй стороны**
   (автор постановки — QA, найдено на себе). Его четвёртый вопрос знает про отставание и
   спрашивает **ОДИН РАЗ, в начале суда**; через двадцать минут отсутствие оговорки стало
   неправдой. **Лечение:** прибор ПЕЧАТАЕТ sha `main`, против которого вынес «годен» · и «годен»
   переспрашивается перед тем, как назвать красный находкой.
   🔑 Пятый экземпляр класса «утверждение переживает свою правду молча» — и первый **внутри
   прибора, специально написанного против этого класса**.
   ✅ Сделано, когда `verify-judge-stand.mjs` печатает sha и это видно в его выводе.
2. **Прибору кадров `tools/shoot-hub-snippets.mjs` (+205, в стволе НЕСУДИМЫМ) — самотест «читаю
   СВОЮ сборку»** (долг назван автором dev-1 до суда, с адресом). Риск узкий и единственный:
   снять кадры не той сборки — третий род ложного зелёного. **Сегодня это перестало быть
   гипотезой: тот же каталог `build/` дал ложное красное Менеджеру.** Кандидат — метка в `build/`,
   которую кадр обязан увидеть; вероятно, общая со стражем хабов.
   ✅ Сделано, когда мутация «подсунуть чужую сборку» роняет самотест.

### 📌 ДОЛГИ СМЕНЫ 14, ПОЛОЖЕННЫЕ В НОСИТЕЛЬ ПРОЧНЕЕ СООБЩЕНИЯ

· ✅ **ДОЛГ ИНТЕГРАТОРА ИСПОЛНЕН** — коммит `169ff3a` (13:44), в стволе мержем `3d0ec5e` (14:04).
  Проверка, а не впечатление: `git merge-base --is-ancestor 169ff3a main` → да ·
  `git show main:EXPERIENCE.md | grep 'ПОДТВЕРЖДЕНИЕ И УТОЧНЕНИЕ ДОЛИ, 2026-08-30'` → есть ·
  `git show main:bugs/177… | grep 'Девятый случай'` → есть.

  🔴 **И ЭТА САМАЯ СТРОКА СТАЛА ДЕСЯТЫМ ЭКЗЕМПЛЯРОМ `bugs/177` — она описывает сама себя.**
  Строка, ЗАВОДИВШАЯ долг описать класс «строка пережила своё исполнение», пережила своё
  исполнение за двадцать минут: Менеджер прочитал её как действующую и попросил Интегратора
  сделать сделанное — уже влив это своей же рукой. Разбор — `bugs/177`, коммит `5b0ae9b`.

  🔑 **НАХОДКА, ПЕРЕВОРАЧИВАЮЩАЯ ВЫВОД ИЗ ДЕВЯТИ ПРЕДЫДУЩИХ (Интегратор, принято дословно).**
  Строку Менеджер завёл сознательно и по прямой просьбе Интегратора — тот сам сказал, что у
  сообщения нет часов, и попросил носитель прочнее. **Носитель свою работу СДЕЛАЛ: долг исполнен
  именно потому, что строка была.** И ровно поэтому пережил исполнение.
  Из девяти случаев напрашивалось «носитель надо сделать прочнее». Десятый показывает обратное:
  **прочность носителя И ЕСТЬ источник дефекта. Слабый носитель теряет ОБЯЗАТЕЛЬСТВО, прочный
  теряет его ОТМЕНУ. Это один дефект с двух концов, а не два разных** — вчера чинили один конец,
  сегодня получили другой.

  🔴 **ТРЕБОВАНИЕ К ЛЕЧЕНИЮ, КОТОРОГО В ДОКУМЕНТЕ НЕ БЫЛО, — И ОНО ОБЯЗАТЕЛЬНО ДЛЯ ВСЕХ
  ПОСТАНОВОК НИЖЕ И ВПРЕДЬ: строка-обязательство несёт ПРИЗНАК СВОЕЙ СМЕРТИ вместе с собой**,
  в момент написания и в том же предложении. Не «сделать это», а «сделать это; **сделано, когда
  <команда> отвечает да**». Тогда исполнение видит тот же глаз, что читает поручение, и отдельный
  обход не нужен. Цена — одна дописанная фраза, а не новый прибор.
  📌 Экземпляры 9 и 10 найдены в ОДИН день, разными ролями, в разных носителях (шапка документа
  дефекта · строка эстафеты). **Класс не редкий — он невидим, потому что врёт в сторону
  бездействия.** Десять известных — это то, что нашли, а не то, что есть.

· **Отчёты Интегратора живут ВНЕ ствола до его мержа**, адреса с веткой:
  предсуд dev-3 — `git show ndim_integrator:reports/TEAM/2026-08-30_precourt_dev3.md` ·
  ревизия беклога — `git show ndim_integrator:reports/TEAM/2026-08-30_backlog_revision.md`.

### 📥 РЕВИЗИЯ БЕКЛОГА СМЕНЫ 14 — 84 открытых, и главное в ней не закрытия

**84 открытых** на `ndim_integrator = 4e733e5` · **85** на `main = f71d990` (расхождение — три его
несмерженных `bugs/NEW_*`). Закрыто 4, у каждого основание СНАРУЖИ документа.
🔴 **Главное — четыре НЕ-закрытия из восьми кандидатов, объявлявших себя закрытыми:** `bugs/138`
(несёт внутри «страж класса НЕ заведён») · `bugs/144` (Исходящее открыто на 85 записей каталога) ·
`bugs/84` («остался текстовый хвост») · `bugs/06` (ждёт слова владельца). **Закрывай ревизия по
самозаявлению — беклог показал бы 80, и четыре живых хвоста исчезли бы из нарезки.**
📌 Дёшево взять сейчас: `bugs/172`, `bugs/201` (фикс уже в ветке dev-1) · `bugs/216`, `bugs/219`
(причина найдена, фикс не дописан). По вектору владельца: `bugs/114`, `115`, `107` (лендинг) ·
`bugs/16`+`19`+`32` (вход, один набор — у Дизайнера).
🔑 **Находка про приборы чтения, шире беклога: ключей статуса в корпусе ДВА** — `**Статус:**` и
`**Состояние:**`. Узкий ключ соврал в ОБЕ стороны: объявил «без статуса» семь документов, из
которых двое закрыты с 2026-08-15. Кандидат в стражи назван, но НЕ заведён — ширину признака
мерят на живом корпусе. Родня трём другим находкам дня: **признак по одной форме слова ловит
форму, а не класс.**
⛔ Границы метода, названные автором: `plans/` и `ideas/` не обойдены · мутаций нет · содержимое
80 открытых по существу не перечитано — **«документ, чей статус врёт МОЛЧА, этой ревизией не
пойман и пойман быть не мог»**.

### 🔴 ОТВЕТ ВЛАДЕЛЬЦА №069 — И ЕГО ЖАЛОБА, КОТОРАЯ ВСКРЫЛА ДЕФЕКТ КОНТУРА (`bugs/231`)

**В1 = Б** — гость виден в чужих топах С ЧЕСТНОЙ ПОМЕТКОЙ («гостевая запись»); в публичный каталог
его оценки по-прежнему НЕ идут. **В3 = В** — владелец открывает Яндекс исполнителю САМ, разрешением
в настройках сессии роли. **В2 не отвечен**: «*Не могу ответить, у тебя обрезаны текста вариантов*».

🔴 **`bugs/231` — страница вычитки резала текст варианта на 300 знаках МОЛЧА.** Замер по всему
корпусу: **99 вариантов из 831 (11,9 %) в 38 интервью, из них 90 в уже ОТВЕЧЕННЫХ** — владелец
принимал решения по усечённому тексту **начиная с интервью №001, почти два месяца**.
🔑 **Резалось не случайное место, а ЦЕНА.** Вариант устроен «Довод: … **Цена:** …», цена стоит в
конце — обрезка съедала именно её. Живой пример того же дня: №069 В1 «Б» оборвался на слове
«предупредил», и **владелец выбрал этот вариант, не увидев, чем платит**.
🔑 **Класс: предел, поставленный без причины, становится невидимой цензурой.** Ни один страж не
краснел — проверки контура судили ФОРМУ документа и ни одна не спрашивала, доехал ли текст целиком.
**Четвёртый за смену случай, когда находку сделал ГЛАЗ при зелёных приборах — на этот раз глаз
владельца.**
✅ Вылечено в тот же час: предел снят с записью причины · юнит `tools/lib/review-core.test.mjs`
доказан мутацией в обе стороны · **ворота расширены — `test:tools` гонял только `tools/*.test.mjs`
и файл в `tools/lib/` НЕ подхватывал**, то есть новый страж повторил бы класс сегодняшней
эскалации QA. Глоб расширен, **577 → 579**.
🔴 **ДОЛГ ВЛАДЕЛЬЦУ:** 90 обрезанных вариантов в отвеченных интервью — решения приняты и исполнены;
какие были бы другими при полном тексте, агент не решает. Спрошено отдельно.

### 🟡 У ВЛАДЕЛЬЦА — интервью №069, поднято страницей

Три вопроса: **В1** поведение гостя (виден / виден с пометкой / оставить скрытым) · **В2** что
делать с плашкой, пока он решает · **В3** 🔴 **Яндекс: 150 адресов ждут третьи сутки при полной
квоте, заперт не квотой, а предохранителем сессии** — три способа открыть дверь.
