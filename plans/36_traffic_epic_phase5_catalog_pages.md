# План 36 — Эпик `ideas/30`, ФАЗА 5: каталог как страницы-сущности

**Выведен:** 2026-08-02 · **Ступень 3 лестницы** `/plan-epic` · **Метаплан:** `plans/34`, фаза 5 ·
**Доказательная база:** `researches/30` приложения A и B · **Решения владельца:** интервью №020–022 ·
**Соседи:** `plans/24` (языковые адреса, карта сайта) · `plans/30` (наполнение и прополка каталога)

**Якорь метаплана:**

> **Фаза 5 — Каталог как страницы-сущности.** «*в базе лежит корпус примерно в сто раз больше всего,
> что мы показываем миру*» (`researches/30`, приложение A)

---

## Что делает эта фаза

Даёт каждому измерению собственный адрес и HTML-страницу, которую видит поисковик и человек без
аккаунта. Сегодня измерения видны только вошедшим, а экран `/dims` закрыт `noindex`.

**Масштаб, замеренный а не предположенный:** 5111 измерений, описания медианой 1047 знаков RU и
1026 EN, без описания — **ноль** из выборки 120. Корпус ≈ **5,35 + 5,24 млн знаков**. Сегодня
поиску открыто 11 страниц и ~50 000 знаков.

## Правило показа — решено владельцем, пересмотру не подлежит

| Что | Когда | Сколько |
|---|---|---|
| Страница | **всегда** | 5111 |
| Звёзды (средняя оценка) | при `rates` ≥ 1 | 1873 |
| Серый текст «ещё без голосов» | при `rates` = 0 | 3238 |
| Число проголосовавших | при `rates` ≥ 3 | **526** (стояло 892 — ошибка счёта, исправлена 2026-08-03) |

Источники: интервью №021 В1 (все страницы, счётчик при ≥3) и №022 В1 = А (нет голосов — нет звёзд,
как в `ideas/26`). **Одно правило внутри приложения и снаружи.**

---

## 🔴 Шаг 0 — ЗАМЕР ДО КОДА: сколько это весит на самом деле

Без этого числа фаза может упереться в хостинг на выкате, а не на планировании.

**Что уже известно [НАШ ЗАМЕР 2026-08-02]:** сегодня в `build/` **19 HTML-страниц**, средний вес
**41,3 КБ**, вся сборка — **6,2 МБ**, 120 файлов.

**Наивная экстраполяция:** 5111 × 41,3 КБ ≈ **210 МБ на релиз**. Для сравнения: бесплатное
хранилище Firebase Hosting — **10 ГБ**, и каждый релиз хранится отдельно
([квоты Firebase](https://firebase.google.com/docs/hosting/usage-quotas-pricing)). То есть ~50
релизов до того, как придётся чистить старые. Не блокер, но и не мелочь.

⚠️ **41,3 КБ — это вес страницы С ОБОЛОЧКОЙ ПРИЛОЖЕНИЯ.** Публичной странице каталога приложение
не нужно: человеку из поиска нужен текст, а не гидратация. Поэтому первый вопрос шага 0 — **сколько
весит страница измерения БЕЗ лишнего**.

**Что снять:**
1. Собрать **срез из 50 измерений**, а не все 5111. Замерить: вес одной страницы, время сборки,
   вес среза целиком.
2. Экстраполировать на 5111 **по замеру, а не по 41,3 КБ** — и записать оба числа.
3. Замерить **время `npm run build`** до и после. Если сборка вырастет с секунд до десятков минут,
   это меняет ритм работы всего проекта, и знать об этом надо сейчас.
4. Проверить, что срез действительно отдаёт описание **в сыром HTML** (`grep` по `build/`), а не
   подгружает его скриптом.

**Ворота шага 0:** три числа (вес страницы, вес всех 5111, время сборки) записаны в этот план.
🔴 **Кода дальше не пишется, пока их нет.**

---

## ✅ ШАГ 0 ВЫПОЛНЕН 2026-08-02 — три числа сняты, и не экстраполяцией

Вместо среза я собрал **весь каталог целиком**: экстраполяция стоила бы столько же спора, сколько
настоящая сборка — времени.

| Что | Замер | Против моей оценки |
|---|---|---|
| **Средний вес страницы** | **15,8 КБ** (мин 14,9 · макс 17,8) | я предполагал 41,3 КБ — **завысил в 2,6 раза** |
| **Вес всех 5111 страниц** | **78,8 МБ** | предполагал ~210 МБ |
| **Вес `build/` целиком** | **101,7 МБ**, 5 235 файлов | — |
| **Время сборки** | **17,1 с** (было ~5 с) | опасался «десятков минут» |
| Слаг-коллизий на 5111 | **ноль** | страж в инструменте не сработал ни разу |

🔑 **Вывод: препятствия нет.** 17 секунд — это не изменение ритма проекта, а 79 МБ против
бесплатных 10 ГБ Firebase Hosting дают ~**126 релизов** до чистки, а не 50.

**Почему я ошибся в 2,6 раза:** 41,3 КБ — средний вес по СЕГОДНЯШНИМ 19 страницам, среди которых
лендинг и документы, несущие много собственной разметки. Страница измерения легче. **Урок общий:
средний вес по разнородной выборке не переносится на однородную.**

### Что ещё нашлось при замере

1. ✅ **Описание лежит в СЫРОМ HTML** — проверено `grep` по `build/`: и `<title>`, и
   `<meta name="description">`, и текст описания отдаются готовыми, без скрипта.
2. 🟡 **Поправка к моему же первому замеру.** Я насчитал «6 553 знака видимого текста, из них
   собственных 26 %» и чуть не записал это риском тонких страниц. **Это артефакт грубой регулярки:**
   в «видимый текст» попали CSS и HTML-комментарии. После их вычитания оболочка — 1 426 знаков, а
   собственное содержание страницы (1 700–2 700 знаков) её превосходит. **Риска тонких страниц нет.**
3. 🐞 **Комментарии разработчика уезжают в боевой HTML.** В `app.html` живут пояснения на русском
   (про загрузочный щит, `bugs/40`), и они попадают в каждую отданную страницу — ~1,4 КБ × 5111 ≈
   **7 МБ мёртвого веса** плюс внутренний жаргон в исходниках публичных страниц. Не утечка, но
   мусор. Заводится отдельной строкой, чинится в фазе гигиены.

### Что сделано кодом (прототип, не сдача)

| Файл | Что это |
|---|---|
| `tools/fetch-dims-slice.mjs` | выгрузка каталога в сборку + **детерминированный слаг** + страж коллизий |
| `src/lib/content/dims-slice.json` | срез 50 измерений (полный каталог — 16,7 МБ, в git не кладётся) |
| `src/routes/dimension/[slug]/+page.ts` | адреса для пререндера + **правило показа оценок в ОДНОМ месте** |
| `src/routes/dimension/[slug]/+page.svelte` | замерочный прототип страницы |

⚠️ **Это не сдача фазы.** Оформление публичной страницы каталога — новый тип экрана, а значит
подлежит **правилу четырёх макетов**. Прототип сознательно прост и служил замеру.

🔴 **Открытый вопрос реализации, названный до кода:** полный каталог — 16,7 МБ JSON, и в git его
класть нельзя. Значит выгрузка становится **шагом выката** (сначала `fetch-dims-slice --all`, потом
сборка), а в репозитории живёт срез, чтобы сборка работала из чистого клона. Это решение фазы, а не
шага 0.

---

## Шаг 1 — Адреса: слаг вместо случайного идентификатора

Идентификаторы измерений — случайные строки (`00S8ckKACl6WhEdc9n3V`, замер приложения A).
Адрес `/dimension/00S8ckKACl6WhEdc9n3V` бесполезен и человеку, и поиску.

- Слаг выводится из `title` («Идеальный незнакомец» → `idealnyy-neznakomets`, «Battlefield 3» →
  `battlefield-3`).
- 🔴 **Коллизии неизбежны** — в каталоге есть одноимённые произведения разных лет. Разрешение:
  добавлять год, при повторе — короткий хвост идентификатора. Правило обязано быть
  **детерминированным**: один и тот же каталог даёт один и тот же адрес всегда, иначе адреса
  «поедут» при следующей сборке и мы своими руками устроим то, чего избежали в интервью №021.
- Слаги фиксируются в сборке; изменение `title` в админ-панели меняет адрес — **это надо назвать
  `plans/30`**, чтобы правка названия не ломала индексацию молча.

**Ворота:** генератор слагов покрыт тестом на детерминизм и на коллизии (две записи с одним
названием дают два разных стабильных адреса).

### ✅ ШАГ 1 ЗАКРЫТ 2026-08-03

Слаг переехал из инструмента в тестируемое место — **`src/lib/content/dim-slug.ts`**; копии функции
в проекте больше нет (разъехавшиеся копии дали бы разные адреса у инструмента и у сборки, и никто
бы не заметил). Покрыт **`dim-slug.test.ts`, 13 тестов**: детерминизм при 100 вызовах ·
независимость от порядка обхода каталога · две записи с одним названием → два разных стабильных
адреса · безопасность для URL на всём боевом срезе · транслитерация · название из одного мягкого
знака или эмодзи всё равно даёт адрес.

**Три мутации, доказывающие, что тест не слепой** (все убиты): хвост идентификатора убран → 7
падений · обрезка хвостового дефиса снята → 1 · кириллица не транслитерируется → 5.

🔴 **Урок, оплаченный прямо здесь.** Мутация «двойной разделитель» сначала **выжила**: тест брал
произвольную длинную фразу, и разрез по 60 знакам просто не попадал на пробел. Пришлось строить
случай, у которого 60-й знак — ровно разделитель. **Произвольный пример не проверяет граничное
условие; граничный случай надо конструировать.**

## Шаг 2 — Канал «каталог → сборка»

Сайт статический (`+layout.ts: prerender = true`), значит каталог должен попасть в сборку.

🟢 **Прецедент есть, изобретать нечего:** `tools/extract-docs.mjs` уже генерирует
`src/lib/content/docs.ts` из источника. Здесь — то же самое, источник другой.

- Инструмент читает боевой каталог (публичные документы `dims/{dimId}`, права те же, что у
  `probe-prod-stats.mjs`) и кладёт рядом со сборкой.
- ⚠️ **Каталог в сборке = снимок.** Новое измерение появляется в поиске после ближайшего деплоя.
  Это цена статики, она принята и названа владельцу в интервью №019.

**Ворота:** повторный прогон на неизменившемся каталоге даёт **побайтово тот же** результат
(детерминизм — иначе каждая сборка будет выглядеть как изменение всего сайта).

### ✅ ШАГ 2 ЗАКРЫТ 2026-08-03

Открытый вопрос шага 0 («полный каталог 16,7 МБ, в git его класть нельзя») решён **двумя файлами**:

| Файл | Что это | В git? |
|---|---|---|
| `src/lib/content/dims-slice.json` | 50 записей, **запасной** каталог | ✅ да — иначе `npm run build` из чистого клона падает |
| `src/lib/content/dims-build.json` | весь каталог, 16,7 МБ | ⛔ нет (`.gitignore`) — пишется шагом выката |

Выбор делает **`src/lib/content/dims-source.ts`**: полный есть — берёт его, нет — работает на
срезе. Механизм — `import.meta.glob` с `eager`, штатный приём Vite для необязательного файла:
статический импорт несуществующего файла уронил бы сборку, а динамический дал бы промис, которого
`entries()` ждать не умеет.

🔴 **Источник называется ВСЛУХ** в лог сборки (`[dimension] 50 страниц · источник: запасной срез`).
Молчаливая подмена здесь стоила бы дорого: выкат, случайно собранный на срезе, дал бы 50 страниц
вместо 5111 и выглядел бы совершенно исправным.

**Ворота закрыты замером:** два прогона инструмента подряд дали **побайтово тот же файл**
(`md5` совпал). Детерминизм обеспечен не удачей, а **сортировкой по `id`** перед записью: порядок
страниц Firestore менять не обязан, но гарантии на это нам никто не давал.

⚠️ **Находка, которую надо знать до выката, и она НЕ про каталог.** Две сборки подряд дают разные
байты у каждой страницы, и причина общая для всего сайта: SvelteKit подставляет в каждую страницу
имя версии (по умолчанию — метка времени), из-за чего меняются хеши `app.*.js` и `start.*.js`.
То есть «каждая сборка выглядит как изменение всего сайта» — **уже правда сегодня**, независимо от
нас; с 5111 страницами это означает полную перезаливку на каждом выкате. Не блокер (79 МБ), но
считать это следствием фазы 5 будет неверно.

⚠️ Ещё одна честная граница, вскрытая замером: срез в режиме по умолчанию равномерен по **первым
300** документам, а не по всем 5111 (чтение останавливается на первой странице). Для запасного
каталога этого достаточно; замер фазы делался на `--all`, и путать их нельзя.

## Шаг 3 — Страница · ✅ МАКЕТ ВЫБРАН: **V2 «Досье»** (интервью №023, В1 = Г)

**Решение владельца 2026-08-03, интервью №023, В1 = Г:** берём **V2 «Досье»** — сверху карточка
объекта (вид, год, автор, оригинальное название, теги, оценка одним блоком), описание под ней.

⚠️ **Рекомендация агента была другой** (V4 «Соседство»), и это ровно тот случай, ради которого
правило четырёх макетов существует: назвав фаворита, агент всё равно рисует все четыре.

🔴 **И к выбору приложено ЗАДАНИЕ, а не только буква.** Дословно:

> «*дополнительно нужно проработать математику, можем ли мы показывать измерения, находящиеся
> рядом, как в V4 — позволяет ли нам математико-статистический движок такое определять. Что вот эта
> игра сильно нравится вот этим 20 людям, и они так же высоко оценили ещё вот другую игру, значит
> эта другая игра в этом многомерном векторном пространстве рядом с первой, и вот такое между ними
> расстояние.*»

Разбор написан — **`researches/32`** — и владелец по нему решил (интервью №023, уточнение в чате
2026-08-03):

> «*ibcf планируем, делаем позже. слово мое распространяется и на соседство измерений. по и по
> тегам пока не делаем*»

### ⛔ Блока «Рядом в Пространстве» в этой фазе НЕТ ВООБЩЕ

| Путь | Решение владельца |
|---|---|
| Соседство **по людям** (item-based CF) | планируем, делаем **позже** → метаплан **`plans/37`** |
| Показ **числа** близости между измерениями | 🔴 **запрещён**: инвариант «цифр метрик похожести не раскрываем» (интервью №018 В4) **расширен на соседство измерений** |
| Соседство **по тегам и виду** (дешёвый путь, оценок не требует) | **пока не делаем** |

🔑 **Что это значит для будущей сессии, и это не мелочь.** Соседство по тегам я предлагал как
доступное немедленно и ничем не заблокированное. Владелец отклонил именно его. Значит появление
такого блока потом — **отмена решения, а не реализация задела**; в «⛔ Чего не делать» это внесено.

**Страница V2 «Досье» строится сейчас и от этого решения не зависит:** блока соседства в выбранном
макете нет по построению.

### 🌍 Английский — сразу (интервью №023, В2 = А)

**Решение владельца:** выпускаем оба языка одновременно. Английские описания (~5,24 млн знаков)
уходят в мир немедленно; обвязку страницы агент переводит, и она ждёт вычитки владельца **уже в
бою**, а не до него. Страниц становится **10 222**, сборка ~34 с, вес ~158 МБ (из 10 ГБ).
Схему адресов это не переоткрывает — она решена (интервью №010, Р5 = В).

---

## Шаг 3 (материал макетов) — четыре варианта, из которых выбран второй

**`design/dimension-page-mockups.html`** — четыре цельных ответа, 16 кадров сняты и просмотрены
рядом до показа (`test-results/dimension-page-mockups/`). Развилка вынесена
**`interviews/interview_023`**, страница вычитки поднята 2026-08-03.

| Вариант | Суть | Названная цена |
|---|---|---|
| **V4 «Соседство»** (рекомендация агента) | статья + блок «Рядом в Пространстве» | соседей считать по виду и тегам — оценок мало (4089 на 5111), иногда будет выглядеть случайным |
| V1 «Статья» | только текст, модель энциклопедии | выше сгиба непонятно, куда человек попал |
| V3 «Вопрос Вам» | звёзды прямо на публичной странице, без аккаунта | тянет за собой гостевой режим `plans/22` — фаза перестаёт быть автономной |
| V2 «Досье» | карточка объекта сверху | отодвигает описание, а описание и есть смысл этих страниц |

🔑 **Все четыре объекта в макетах — настоящие, из боевого каталога**, и подобраны так, чтобы разом
показать все границы правила оценок: 4 голоса (звёзды и счётчик) · 0 (только серый текст) · 1
(звёзды без счётчика) · 3 голоса при отсутствующих годе и авторе.

⚠️ Второй вопрос интервью №023 — **порядок выпуска английского**: сразу оба языка (5,24 млн знаков
в мир немедленно, обвязка вычитывается уже в бою) или после вычитки. Схему адресов это НЕ
переоткрывает — она решена (интервью №010, Р5 = В: у каждого языка свой префикс).

## Шаг 3 (исходная постановка) — Страница

Содержание: название · вид · автор · год · теги · **описание** · блок оценок по правилу выше ·
приглашение оценить (дверь в продукт).

- Токены продукта и сетка `ideas/24` V4; графика по правилу готовности (`use:ready`, `bugs/69`).
- 🔴 **Ни одного изображения произведения** — обложки и кадры под авторским правом
  (`researches/30` §; интервью №020).
- `title`/`description` страницы собираются из данных, а не сочиняются. Термины на витрине не
  бросаются голыми (правка владельца, `AGENT_GUIDE` → словарь).
- 🔴 **Языковую схему эта фаза НЕ изобретает.** Адреса языков — `plans/24` фазы 6–7. Пока их нет,
  страница живёт по той же схеме, что и остальной сайт, и переезжает вместе со всеми.

---

## ✅ ШАГИ 3–6 ВЫПОЛНЕНЫ 2026-08-03 — фаза собрана и принята живым браузером

### Что стоит в бою-кандидате

| Что | Замер |
|---|---|
| Страниц | **10 222** = 5111 × 2 языка |
| Время сборки | **~20 с** (было ~5 с до фазы) |
| Средний вес страницы | **16,7 КБ** (мин 15,5 · макс 18,5) |
| Страницы каталога в `build/` | **203 МБ** (ru 102 + en 101) |
| Вся сборка | **226 МБ** |
| Клиентский бандл `_app` | **1,3 МБ** (крупнейший чанк 47,7 КБ) |
| Карта сайта | **10 233 адреса**, 0,83 МБ |

🔴 **Поправка к прежней оценке хранилища, и она в худшую сторону ДВАЖДЫ.** Шаг 0 обещал ~126
релизов, исходя из 79 МБ. Замер чистой сборки перед выкатом дал **322 МБ и 20 567 файлов**, то есть
**~31 релиз** из бесплатных 10 ГБ Firebase Hosting.

**Откуда лишнее, и это не ошибка сборки:** SvelteKit кладёт рядом с каждой пререндеренной страницей
`__data.json` — полезную нагрузку для клиентской навигации. Их **10 222 штуки на 41 МБ**, плюс
столько же файлов в счёте.

> 🔄 **Пересъёмка 2026-08-29 (`bugs/214`, dev-1, своя сборка — SvelteKit 2.69.2 · Vite 8.1.4).**
> Числа выше — исторические, замера 2026-08-03. Сегодня под `dimension/` лежит **10 250 файлов
> на 51,6 МБ** (во всей сборке 10 438 на 53,8 МБ — `catalog/` эмитит их тоже). Урезание полей
> ответа помогло весу одной страницы, но корпус вырос быстрее. Довод в `plans/75` и во врезке
> `+page.server.ts` приведён к правде тем же коммитом: **`csr = false` эти файлы НЕ убирает**.

### ✅ `csr = false` СДЕЛАНО 2026-08-03 — и замер опроверг мой же прогноз

У публичной страницы каталога нет ни одного интерактивного элемента, поэтому ей объявлено
`export const csr = false`. Замер до и после, чистая сборка обоих раз:

| Что | До | После | |
|---|---|---|---|
| Вес сборки | **278 МБ** | **210 МБ** | −68 МБ (−24 %) |
| Вес одной страницы | **25,9 КБ** | **17,6 КБ** | −32 % |
| Файлов `__data.json` | 10 222 (41 МБ) | **10 222 (41 МБ)** | ⛔ **не изменилось** |
| JS на странице | бандл + гидратация | **ноль байт** | `modulepreload` 0 · `<script type="module">` 0 · `__sveltekit_` 0 |

🔴 **Я ошибся в механизме, и это стоит записать.** Я обещал, что уйдут `__data.json` — они
остались все до единого. Экономия пришла совсем с другой стороны: из HTML каждой страницы ушли
ссылки `modulepreload`, модульный скрипт и сериализованная нагрузка гидратации. Итог по весу вышел
даже лучше обещанного в абсолюте (−68 МБ против ожидаемых −41), но **правильным прогноз от этого
не становится** — я назвал не ту причину.

### ✅ Хвост из 41 МБ подрезан — и вторая половина вины тоже моя

**Почему `__data.json` вообще существуют:** SvelteKit пишет их всегда, когда у маршрута есть
серверный `load`, — встроенного выключателя нет
([Page options](https://svelte.dev/docs/kit/page-options), обсуждение в
[sveltejs/kit#8331](https://github.com/sveltejs/kit/issues/8331)).

⛔ **И удалять их своим шагом сборки НЕЛЬЗЯ**, хотя соблазн прямой. Клиентский маршрутизатор
приложения о `csr = false` целевой страницы не знает: первый же внутренний переход СЮДА с любого
экрана (например, из будущего каталога `/dims`) пойдёт за данными и получит 404. Значит лечим
**вес**, а не факт существования.

🔴 **А вес был раздут мной.** Загрузчик возвращал ВЕСЬ объект каталога `dim` — оба языка разом —
рядом с уже разобранными из него полями. Описание уезжало в ответ **трижды**: русское, английское
и выбранное.

| Что | Было | Стало |
|---|---|---|
| `__data.json` на страницу | **4 871 байт** | **2 502 байта** |
| `__data.json` всего | **41 МБ** | **24,4 МБ** |
| Вес сборки | 210 МБ | **194 МБ** |

**Итог пути:** 278 → 210 → **194 МБ** (−30 % от исходного). Оставшиеся 24,4 МБ — это само
описание и теги: то, что клиентский переход действительно затребует. Дальше сжимать нечего без
выбрасывания содержания.

📌 Попутно: `npm run check` (svelte-check) нашёл **8 ошибок** — я писал расширение `.ts` в алиасных
импортах `$lib/…`, чего TypeScript не принимает. Сборка это прощала, проверка типов — нет.
Исправлено, **0 ошибок и 0 предупреждений на 436 файлах**.

**Ворота пройдены:** `verify-dimension-pages` 24/0 · `npm test` 220 · `npm run e2e` 80 ·
32 кадра живым Chrome. 🔑 Главное, что проверялось глазами: **тема работает без единого байта JS** —
`data-theme` ставит инлайн-скрипт `app.html` до первой отрисовки, а корневой макет инертен.

⚠️ **Чистка старых релизов переезжает из «когда упрёмся» в регламент выката.**

### Устройство

| Файл | Что |
|---|---|
| `src/routes/[lang=lang]/dimension/[slug]/+page.server.ts` | адреса, данные, `hreflang`, `canonical` |
| `src/routes/[lang=lang]/dimension/[slug]/+page.svelte` | макет **V2 «Досье»** |
| `src/lib/content/langs.ts` | языки публичных адресов, `x-default` = английский |
| `src/params/lang.ts` | ограничитель: `/ru/`, `/en/` — и ничего больше |
| `src/lib/content/dim-view.ts` | контракт «что показывает страница» |
| `src/hooks.server.ts` | `<html lang>` из адреса — семь строк из `researches/26` §5.2 |
| `tools/verify-dimension-pages.mjs` | страж, **24 проверки** |
| `tools/shoot-dimension-pages.mjs` | приёмка живым браузером, 32 кадра |

### 🔴 Дефект, найденный и вылеченный внутри фазы — `EXP-0136`

Первая редакция загрузчика была **универсальной** (`+page.ts`). Универсальный `load` исполняется и
в браузере, поэтому Vite вшил весь каталог в клиентский бандл: **чанк узла 16,86 МБ** при всём
`_app` в 18,1 МБ. Каждый посетитель скачивал бы весь каталог. Лечение — `+page.server.ts`;
после правки `_app` = **1,3 МБ**.

⚠️ **И главное про этот дефект: 24 проверки стража были ЗЕЛЁНЫМИ.** Они смотрели на `build/*.html`.
Нашёл его страж ЖАРГОНА, наткнувшись на слово «сервер синхронизации» в клиентском чанке — оно попало туда
с описанием фильма «Скрытые фигуры». То есть о 17 МБ в бандле я узнал по случайному совпадению
слов. **Отсюда две новые проверки в страже — на ВЕС сборки, а не на содержание.**

### Что поймала приёмка живым браузером

1. **Страницы отдавались без стилей** — и виноват оказался не код, а осиротевший `vite preview`,
   переживший `pkill` и отдававший 404 на таблицу стилей. Тот же класс уже стоил проекту 21
   ложного падения e2e. **Урок стал механизмом:** прибор съёмки теперь делает предполётную
   проверку (сервер обязан отдать таблицу стилей ИМЕННО ЭТОЙ сборки) и отказывается снимать,
   называя команду гашения. Проверено гашением сервера — прибор краснеет.
2. **На странице не было оболочки продукта вовсе** — ни шапки, ни двери назад. Добавлена шапка из
   утверждённого макета (знак · «NDim Space» · «Войти»).
   🔵 **Рельса навигации сознательно НЕТ** — решение агента, дёшево к откату: публичные документы
   «Меню» носят полную оболочку, но их открывают ИЗНУТРИ, уже войдя; сюда человек приходит из
   поиска, и пять пунктов рельса ведут его за стену входа — пять тупиков вместо одной двери.

### Правки соседних стражей, вынужденные ростом каталога

- `e2e/seo.spec.ts`: проверка «личных экранов нет в карте сайта» сверяла **подстроку** `/profile`.
  В каталоге есть измерение **«Profile»**, его публичный адрес `/ru/dimension/profile-qgg1lfbk`
  содержит те же знаки — проверка краснела бы на исправном продукте. Сверяем полный `<loc>`.
- `e2e/no-jargon.spec.ts`: страж словаря продукта покраснел на фильмах **«Скрытые фигуры»** и
  **«Игра в имитацию»** — слово «сервер синхронизации» стоит в их описаниях по делу. Вырезаем блок описания
  (чужой текст), обвязку страницы страж судит по-прежнему.

### Ворота закрыты

`node tools/verify-dimension-pages.mjs` → **24 прошло, 0 провалов**; **7 мутаций убиты**
(звёзды при нуле · счётчик при одном голосе · сломанный `<html lang>` · односторонний `hreflang` ·
каталог выпал из карты · блок соседства вернулся · почта на публичной странице).
`npm run e2e` → **80 passed** · `npm test` → **220** · 32 кадра приёмки, обе темы, две ширины,
четыре граничных случая (0 · 1 · ≥3 голосов · без года и автора).

---

## Шаг 4 (исходная постановка) — Карта сайта и служебное

- 5111 адресов добавляются в существующий генератор (`src/routes/sitemap.xml/+server.ts`).
  Предел одного файла — 50 000 адресов / 50 МБ, влезаем с запасом (`interviews/019`).
- `canonical` на каждой странице.
- ⚠️ **Проверить время ответа карты**: сегодня она собирается из `DOCS`, теперь будет из каталога.

## Шаг 5 — Стражи, доказанные мутациями

| Страж | Что ловит | Мутация, которая обязана его уронить |
|---|---|---|
| ✅ Правило оценок | звёзды при `rates` = 0 | подставить измерение с нулём и ждать звёзд |
| ✅ Счётчик | число проголосовавших при `rates` < 3 | подставить `rates` = 2 |
| Не-дверная страница | страница без описания | вычистить описание у одной записи |
| Приватность | любые данные человека на публичной странице | подставить `uid` в шаблон |
| Сырой HTML | описание подгружается скриптом, а не лежит в HTML | убрать пререндер у маршрута |
| ✅ Детерминизм адресов | слаг «поехал» между сборками | пересобрать дважды и сравнить список адресов |

### Что из этой таблицы уже закрыто (2026-08-03)

**Правило оценок вынесено в ОДНО место — `src/lib/content/dims-rating.ts`** (одно правило внутри
приложения и снаружи, прямые слова владельца, интервью №022) и накрыто **`dims-rating.test.ts`**:
все четыре границы (0 · 1 · 2 · 3), инвариант «счётчик никогда не показывается без звёзд», а также
проход по всему боевому срезу — ни одной неправды.

**Пять мутаций, все убиты:** звёзды при нуле → 3 падения · счётчик при любом числе → 4 · порог
звёзд сдвинут на ноль → 4 · порог счётчика сдвинут на два → 4 · защита от битого значения снята → 1.

🔴 **И здесь мутация тоже сначала выжила.** Тест «битое значение гасится» проверял `NaN`, `-1` и
`Infinity * 0` — а это NaN, и все три ложны БЕЗ защиты тоже. Единственный случай, который
отличает защиту от её отсутствия, — **`Infinity`**: он `>= 1` истинен, и битое поле показало бы
звёзды. Урок тот же, что на шаге 1: **граничный случай надо конструировать, а не брать наугад.**

**Детерминизм адресов проверен по сборке:** два прогона `npm run build` подряд дают одинаковый
список из 50 адресов (на срезе; на полном каталоге проверяется шагом выката).

## Шаг 6 — Приёмка

Живой Chrome: обе темы, две ширины, десяток случайных страниц + три граничных (ноль голосов, один
голос, максимум 14). Скриншоты. `npm run e2e` зелёный. `/fable-judge` перед объявлением готовности.

---

## ⛔ Что эту фазу БЛОКИРУЕТ (и это не отговорки, а названные условия)

1. 🐞 **`bugs/111`** — сумма `rates` (4058) не сходится с `space/stats.ratings` (4089). **Число
   проголосовавших на публичную страницу не выносится, пока счётчики расходятся.** Блокирует только
   счётчик; остальная страница — нет.
2. 🧹 **Грязь в поле «вид»** (`researches/30` A.5): `фильм` строчной 21 шт., `Movie` 3 шт., один раз
   в поле вида записан **год**. Сегодня почти не видно; на странице вид попадёт в заголовок.
   Прополка — работа `plans/30` (там уже есть шаг прополки поля `name`).
3. ⚠️ **Замер спроса** (`homeworks/08`) не блокирует фазу, но решает, **что писать в заголовках** и
   стоит ли группа D усилий. Разумно дождаться, если он придёт в ближайшие дни.

## Риски

| Риск | Как гасим |
|---|---|
| **Сборка вырастает до десятков минут и ломает ритм проекта** | Шаг 0 замеряет ДО кода. Если время неприемлемо — пересматриваем форму (частичная сборка, отдельный маршрут), а не терпим |
| **210 МБ на релиз съедают бесплатное хранилище** | 10 ГБ ≈ 50 релизов. Чистка старых релизов — в регламент выката, а не «когда упрёмся» |
| **Адреса «поедут» между сборками** | Тест детерминизма в воротах шага 1 — тот же класс, что мигающие адреса, которых мы избежали в интервью №021 |
| **Страницы окажутся дверными** | Порог содержания взят с запасом (≥6 данных против требуемых трёх), плюс страж и выборочная проверка на непохожесть |
| **Индексируется 5111 страниц, а трафика ноль** | Это возможный исход, и он назван честно: даже тогда фаза даёт **первый английский текст в отдаваемом HTML** — то, во что упиралось решение «EN основной» |
| **Правка названия в админ-панели молча меняет адрес** | Назвать в `plans/30` как связь между эпиками, а не обнаружить потом |

## Критерии «готово»

1. Замер по `build/`: открытого текста **на два порядка больше** прежнего; английский присутствует
   в **сыром HTML**.
2. Страниц ровно 5111 + прежние 11; карта сайта содержит все.
3. Правило оценок соблюдается на всех трёх границах (0 · 1 · ≥3) — стражи с мутациями зелёные.
4. Ни одной страницы с изображением произведения; ни одной с данными человека.
5. Два прогона сборки подряд дают одинаковый список адресов.
6. Живой Chrome, обе темы, две ширины, скриншоты приложены.
7. `/fable-judge` пройден.

## Трассировка

| Шаг | Якорь `plans/34` |
|---|---|
| 0 | «Фаза 5… крупнейшая победа эпика» + критерий приёмки 6 (проверка на doorway) |
| 1–3 | «Что фаза строит: описание, вид, автор, год, теги, плюс блок оценок по правилу выше» |
| 4 | Критерий приёмки 2 («индексируемых страниц больше 11») |
| 5–6 | Ворота закрытия фазы 5 |
