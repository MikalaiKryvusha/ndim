# План 53 — ЭПИК: STAGE-окружение и переезд баз на именованные

> **Создан:** 2026-08-15 · **Родитель:** `ideas/36_DONE_stage_environment.md` (заказ владельца) ·
> **Статус:** ✅ **ЗАКРЫТ 2026-08-16.** Фаза 0 (точка возврата) 15.08 · фаза 1 (именованные базы,
> контур `ndim-stage`) 15.08 · фаза 2 (три репетиции) 15/16.08 · фаза 3 (боевая миграция, AUTH
> владельца) 16.08 · **фазы 4 и 5 (дверь, приборы, замок) 16.08**. Все девять критериев П1–П9
> измерены. Названный долг: не замерено, шлёт ли preview-канал `X-Robots-Tag` сам ·
> **Исходящее:** развилки → `interviews/interview_033_stage_environment.md` (все закрыты);
> владельцу — подтвердить или запретить обход замка `--auth-owner` (не блокирует)

**Входы:** `researches/37_stage_environment.md` (исследование, ступень 1 лестницы) ·
`plans/04_DONE_cutover.md` (единственная в проекте боевая миграция, прошедшая успешно) ·
`EXP-0038` (репетиция на копии спасла cutover от двух необратимых ошибок).

---

## Вектор цели

**Боль:** между `localhost` и живыми людьми нет ни одной ступени. Сломанная сборка обнаруживается
в бою, и трижды за август её находил владелец, а не приборы (`bugs/124`, `bugs/125`).

**Куда идём:** у проекта появляется второй контур — `ndim-stage`, — где новая версия живёт и ломается
до того, как её увидит человек; а боевые данные переезжают в базу с говорящим именем, чтобы «какая
это база» больше никогда не было вопросом.

**Чего эпик НЕ делает:** не отменяет эмуляторы (`npm run stand` остаётся первым и самым дешёвым
рубежом) и не ослабляет смоук после выката в бой — он остаётся обязательным (слово владельца
2026-08-15).

### Решения владельца, принятые до начала работ (не переспрашиваются)

| Решение | Слова владельца |
|---|---|
| Отдельный проект, а не вторая база в бою | «нужно будет сделать в моем фаербез **ndim-stage** проект» |
| Базы переименовываются переездом | «**мигрируем базы данных**» |
| Имена | `ndim-db-prod` (бой) · `ndim-db-stage` (стейдж) |
| Порог качества миграции | «Перед миграцией **ТРИ теста**… **Права на ошибку у нас нет, это данные наших пользователей**» |
| Денежное ограничение | «Нам нужно оставаться в бесплатном плане» → не выходить за бесплатные квоты |
| Цена меряется | временем работ и сложностью, не деньгами |

---

## Критерии приёмки эпика

Каждый — со шкалой, прибором и целью (`REQUIREMENTS_FRAMEWORK.md` → fit criterion).

| # | Критерий | Шкала | Прибор | Цель |
|---|---|---|---|---|
| П1 | Ни один документ людей не потерян при переезде | доля документов, совпавших с исходными по значению | `tools/migrate/verify-db-parity.mjs` (пишется в фазе 2) | **100 %**, расхождений **0** |
| П2 | Похожесть после переезда не изменилась | число связей с расхождением > 0 | сверка топов до/после, метод `plans/04` | **0** |
| П3 | Бой остаётся в бесплатном тире | поле `freeTier` боевой базы | `gcloud firestore databases describe` | **`true`** |
| П4 | Простой боя при миграции | минуты, когда приложение не может писать | журнал миграции | **≤ 60 мин** |
| П5 | Откат существует и проверен ДО миграции | восстановление из выгрузки во временную базу и её сверка с боем | репетиция 3 (фаза 2) | расхождений **0** |
| П6 | Стейдж невидим поисковикам | доля страниц стейджа с `X-Robots-Tag: noindex` | `tools/verify-stage-noindex.mjs` (фаза 4) | **100 %** |
| П7 | В стейдже нет персональных данных живых людей | число документов людей, пришедших из боя | `tools/verify-stage-clean.mjs` (фаза 4) | **0** |
| П8 | Дверь в бой не может выстрелить в стейдж и наоборот | число прогонов, где контур цели не совпал с заявленным | стоп-правило в `tools/deploy.mjs` + мутация | **0**, мутация обязана краснеть |
| П9 | Сборка остаётся ОДНА на оба контура | число различающихся артефактов сборки между боем и стейджем | сверка хеша `__sveltekit_<хеш>` | **1 артефакт** |

---

## Фазы, их порядок и почему он такой

Порядок выведен из одного соображения: **самый опасный шаг эпика — миграция боевой базы, и его
полигоном служит первый же продукт этого эпика.** Поэтому стейдж строится ДО переезда боя, а не
после: иначе репетировать боевую миграцию будет негде, и «три теста» окажутся тремя обещаниями.

```
Фаза 0 — точка отката и гигиена
   ↓ (без свежей выгрузки боя дальше нельзя)
Фаза 1 — код учится жить с ИМЕНОВАННЫМИ базами  +  контур ndim-stage
   ↓ (полигон построен)
Фаза 2 — ТРИ РЕПЕТИЦИИ миграции          ← сюда упирается «права на ошибку нет»
   ↓ (все три зелёные)
Фаза 3 — БОЕВАЯ МИГРАЦИЯ (default) → ndim-db-prod     ⛔ только с AUTH владельца
   ↓
Фаза 4 — дверь и приборы: выкат на стейдж, смоуки с --base, стражи контура
   ↓
Фаза 5 — процесс: когда стейдж обязателен; документы и карты
```

### Фаза 0 — точка отката и гигиена ⛔ ворота всего эпика · 🔧 В РАБОТЕ с 2026-08-15

**Сделано 2026-08-15:**
- ✅ Написан прибор `tools/migrate/db-fingerprint.mjs` — отпечаток базы «путь → хеш данных»
  (значения не печатаются: в базе ПДн, репозиторий публичный). Им судится критерий П1.
- ✅ **Отпечаток боя снят: 11 266 документов**, полнота подтверждена.
  Файл — `test-results/db-fingerprint/ndim-space--default.json` (вне git).
- 🔴 **`bugs/130` — прибор считал каталог ДВАЖДЫ, и ложное число уехало в этот план.** Сводка
  давала `dims/* 10 224` вместо 5 112 (`collectionGroup('dims')` матчит и КОРНЕВУЮ коллекцию того
  же имени — у нас `dims` носят и каталог, и оценки `points/{uid}/dims`). Заголовочные 11 266 были
  верны — дедуплицировала карта путей; врала только разбивка, а сумма её строк давала 16 378 при
  подписанном итоге 11 266. Обе таблицы этого документа исправлены; прогон подешевел с 16 378
  чтений до 11 266 (**−5 112, это 10 % суточной бесплатной квоты**). Починено и доказано двумя
  мутациями — страж `tools/migrate/verify-fingerprint.mjs` (6 проверок, эмулятор, боевых чтений 0).
- 🔴 **Первый же прогон нашёл дефект — `bugs/127`:** подколлекция `users/{uid}/privat/{uid}`,
  331 документ, поле `user_secret_key` непусто у всех. Ни `firestore.rules`, ни `schema.ts` о ней
  не знают. Без прибора миграция потеряла бы 331 документ молча.
- ✅ Инвентарь боя (он же ожидание для П1):

  | Коллекция | Документов |
  |---|---|
  | `dims/*` | 5 112 |
  | `points/*/dims/*` | 4 066 |
  | `users/*` | 362 · `users/*/profile/*` 331 · **`users/*/privat/*` 331** |
  | `ndimids/*` (наследие 1.x) | 330 |
  | `relations/*` | 341 · `points/*` 342 |
  | `space/*` 4 · `daily` 27 · `days` 14 · `suggestions` 5 · `admins` 1 | 51 |
  | **ВСЕГО** | **11 266** |

🔑 **Принцип фазы 3, выведенный этой находкой: МИГРАЦИЯ НИЧЕГО НЕ ЧИСТИТ.** Переносится всё,
включая наследие 1.x, — иначе сверка П1 не может дать 100 %, а она и есть слово владельца «права
на ошибку нет» в виде числа. Уборка наследия — отдельная работа с отдельным решением.

**Осталось в фазе:**

- Полная выгрузка боевой базы в Cloud Storage (`gcloud firestore export`) — **точка возврата**;
  ровно так делался cutover `plans/04`.
- Инвентаризация боя: сколько документов в каждой коллекции. Это число становится ожиданием для
  П1, и берётся ДО любых действий.
- Судьба `sandbox2` (июльская копия ПДн): развилка В2 интервью №033. По умолчанию — удаляется
  после того, как выгрузка боя проверена.
- **Ворота выхода:** выгрузка существует, её размер и состав сверены с инвентаризацией.

### Фаза 1 — именованные базы в коде + контур `ndim-stage` · 🔧 БОЛЬШЕЙ ЧАСТЬЮ СДЕЛАНА 2026-08-15

**Готово и проверено наблюдением:**

| Что | Замер |
|---|---|
| Проект `ndim-stage` | создан, план **Spark** (`billingEnabled: false`) |
| База `ndim-db-stage`, eur3 | `freeTier: **true**` |
| Правила и индексы | выкачены; проверка поведением: `space/public_metrics` → **404** (наши правила), `points/anyuser` → **403** |
| Вход | настроен владельцем (`homeworks/11`), **сверен с боем поле в поле**: email ✅ · passwordless ✅ · anonymous ✅ · google.com ✅; сверх боя — автоочистка анонимов |
| Приложение | третье обличье по ХОСТУ (`src/lib/firebase.ts`), артефакт сборки один (П9: хеш рантайма в `build/` — **1**) |
| Сервер синхронизации | `FIRESTORE_DATABASE_ID` + третий страж «перекрёстная пара проект × база» |
| Сайт | **https://ndim-stage.web.app**, 20 983 файла |
| **П6** — невидимость поиску | `verify-stage-noindex` — **11/0**, с контролем прибора (в бою заголовка нет) |
| **П8** — разделение контуров | `probe-stage-contour` — стейдж шлёт 3 запроса в `ndim-stage` и **0** в бой; бой — зеркально. Консоли чисты |
| Дерево | typecheck ✅ · check 490/0 ✅ · `npm test` **284/0** ✅ |

**Осталось в фазе 1:** обезличиватель слепка боя и наполнение `ndim-db-stage` данными (В1 = А) ·
второй контейнер `ndim-server-stage` в Docker · Storage стейджа (требует Blaze — названный долг).

---

### 🎯 С ЧЕГО НАЧИНАЕТ СЛЕДУЮЩАЯ СЕССИЯ — зарубка 2026-08-15, ночь

Слово владельца, дословно: «*дальше три репетиции и базу. Нужно сейчас доделать разделение
адекватное стейдж и прод, чтобы мы могли спокойно тестировать и фиксить на стейдже, который
повторяет прод*».

**Шаг 1 — наполнить стейдж (он же репетиция Р2, та же машинерия, что у миграции).**
Написать `tools/migrate/seed-stage-from-prod.mjs`: читает бой `(default)` → пишет
`ndim-stage / ndim-db-stage`.

Правила переноса, выведенные из решений владельца и замеров этой сессии:

| Что | Как переносится | Почему |
|---|---|---|
| `dims/*` (5 112) | как есть | каталог — не ПДн, названия книг и фильмов |
| `points/*` и `points/*/dims/*` (4 066 оценок) | как есть | без них математика связей мертва |
| `relations/*`, `space/*`, `daily`, `days` | как есть | наполняют экраны настоящими числами |
| `users/*` — **личный слой** | почта → `userNN@stage.local` · имя → синтетическое · фото → заглушка · соцсети → пусто | интервью №033 В1 = А |
| **`users/*/privat/*`** (331) | 🔴 **НЕ переносится вовсе** | 331 живой `user_secret_key`, `bugs/127` |
| `suggestions/*` (5) | почта автора обезличивается | там есть адреса |
| `ndimids/*` (330, наследие 1.x) | решить в фазе: 2.0 их не читает | кандидат на «не переносить» |

🔑 **Развилка идентификаторов, которую надо решить ОСОЗНАННО и записать:** сохранять ли UID
как есть. **Предложение агента — сохранять.** UID непрозрачен и сам по себе ничего о человеке не
говорит, зато его сохранение держит ссылочную целостность даром: `relations`, `friendships`
(ключ — пара `uidA_uidB`), `audience`, `testPairs` ссылаются на UID из разных мест, и любая
перенумерация — это отдельный источник ошибок ровно в той работе, где «права на ошибку нет».
Цена решения названа честно: тот, у кого есть доступ И к стейджу, И к бою, сможет сопоставить
оценки с человеком. Доступ есть только у владельца и агента.

**Шаг 2** — сверка: отпечатки боя и стейджа обязаны совпасть по счётчикам всех коллекций, кроме
намеренно опущенных (`privat`, и `ndimids`, если решим не переносить). Это и есть протокол Р2.

**Шаг 3** — второй контейнер `ndim-server-stage` (`FIRESTORE_DATABASE_ID=ndim-db-stage`, свой ключ).

**Шаг 4** — Р3 (репетиция отката) и только затем фаза 3, с AUTH владельца в момент запуска.

⚠️ **Расход квоты, чтобы не удивляться:** один проход чтения боя ≈ 11 000 чтений; за 2026-08-15
уже израсходовано ≈ 44 000 из 50 000 бесплатных. Владелец разрешил превышение: «*для миграции
позволяем себе заплатить немножко*» — цена порядка центов.



Две работы, которые нельзя разделить: стейдж-база **сразу именованная** (`ndim-db-stage`), значит
код обязан уметь именованные базы прежде, чем стейдж вообще заработает.

- **Приложение:** `databaseId()` получает имя базы контура по хосту, а не только из `?db=`
  (`src/lib/firebase.ts:54`). Свойство «окружение выбирает ХОСТ, артефакт один» сохраняется — это
  критерий П9.
- **Сервер синхронизации:** сегодня имени базы не знает вовсе и всегда идёт в `(default)`
  (`sync-server/index.mjs:203`). Учится читать `FIRESTORE_DATABASE_ID`; стоп-правило «боевой ключ +
  чужая база = отказ» по образцу существующего стоп-правила про эмулятор.
  🔑 **Решение владельца 2026-08-15:** «*сервер синхронизации stage должен работать в моем докере,
  но в отдельном от prod контейнере. два независимых контейнера: **ndim-server-prod**,
  **ndim-server-stage***». То есть один образ, два контейнера, у каждого свой ключ сервисного
  аккаунта и своя база — контуры не делят ни конфиг, ни секрет, и «перепутать» нечем.
- **Правила:** `firebase.json` переходит на массивную форму `"firestore": [...]`.
  🔴 **Ловушка, известная заранее:** при массивной форме `firebase deploy --only firestore:rules`
  завершается успехом и **не катит ничего** ([firebase-tools#10447](https://github.com/firebase/firebase-tools/issues/10447)).
  Дверь обязана звать `--only firestore` без под-таргета, и это стережётся проверкой, а не памятью.
- **Проект `ndim-stage`:** создание, база `ndim-db-stage` в `eur3`, правила, индексы, вход, сайт
  хостинга.
- **Наполнение стейджа — ОБЕЗЛИЧЕННЫЙ слепок боя** (интервью №033, В1 = А, ответ владельца
  «ок, как считаешь нужным» 2026-08-15): каталог (5111), оценки (4064), связи (2344) и агрегаты
  переносятся как есть; слой личности заменяется — почта → `userNN@stage.local`, имя →
  синтетическое, фото → заглушка, UID → новый. Обезличиватель пишется поверх
  `src/lib/migration/transform.ts` и **служит одновременно репетицией Р2** фазы 2.
- **Ворота выхода:** на стейдже открывается приложение, входит человек, ставит оценку; правила
  стейджа отвечают так же, как боевые (сверка ответов на одинаковые запросы).

### Фаза 2 — ТРИ РЕПЕТИЦИИ ⛔ ворота перед боем

Заказ владельца дословно: «Перед миграцией ТРИ теста, что все в боевой миграции отработает
корректно». Репетиции разные по природе — иначе это один тест, прогнанный трижды.

✅ **Часть Р1 уже исполнена 2026-08-15 — вопрос бесплатного тира закрыт ЗАМЕРОМ** (опыт на пустых
базах проекта `ndim-stage`, стоимость ноль):

| Шаг опыта | Замер |
|---|---|
| Создана **именованная** `ndim-db-stage` первой в проекте | `freeTier: **true**` → **имя тир не отнимает**, его определяет очерёдность |
| Держатель тира удалён, следом создана `ndim-db-probe` | `freeTier: **true**` → **тир ПЕРЕХОДИТ к созданной после удаления** |
| Наблюдение по бою | `sandbox2` (вторая база) несла `freeTier: false` — то есть вторая база тира не получает |

🔑 **Отсюда порядок фазы 3, теперь доказанный:** выгрузка → **удалить `(default)`** → создать
`ndim-db-prod` → загрузить → сверить. Он сохраняет бесплатный план, а безопасность даёт не живая
старая база, а **доказанный откат** (Р3).

⚠️ **Операционная деталь, которой нет в документации:** имя удалённой базы держится занятым
**около пяти минут** (`Database ID … is not available. Please retry in 243 seconds`). Для боя
безвредно — имена разные, — но если понадобится переиспользовать имя, это пауза в середине
миграции.

✅ Проект `ndim-stage` создан и живёт на **Spark** (`billingEnabled: false`); именованная база на
нём создаётся без платёжного аккаунта — стейдж бесплатен буквально.
✅ `sandbox2` снесена (слово владельца: «старый артефакт, мусор, который забыли удалить»), в бою
осталась одна база `(default)` с `freeTier: true`. Интервью №033 В2 закрыт.

| # | Репетиция | Где | Что доказывает |
|---|---|---|---|
| **Р1** | **Процедура целиком на синтетике** | `ndim-stage` | Что последовательность шагов вообще рабочая: выгрузка → удаление старой базы → создание именованной → загрузка → переключение приложения, правил, сервера синхронизации. 🔑 Здесь же **замеряется `freeTier` новой базы** — то есть проверяется, сохраняет ли выбранный порядок бесплатный план (П3), а не предполагается |
| **Р2** | **На КОПИИ боевых данных** | временная база **внутри боевого проекта** (метод `sandbox2`, `EXP-0038`) | Что процедура выдерживает НАСТОЯЩИЕ данные: 94 человека, 4064 оценки, 5111 измерений, 2344 связи. Сверка 100 % документов (П1) и сверка топов похожести (П2). ⚠️ ПДн не покидают боевой проект — копия живёт внутри того же периметра и уничтожается сразу после сверки |
| **Р3** | **Репетиция ОТКАТА** | временная база в боевом проекте | Что точка возврата настоящая: выгрузка фазы 0 разворачивается в пустую базу и сверяется с боем (П5). Откат, впервые опробованный в момент аварии, — это не откат |

**Ворота выхода:** все три зелёные, сверки без расхождений, письменный протокол каждой.

---

## ✅ ПРОТОКОЛЫ РЕПЕТИЦИЙ — исполнены 2026-08-15/16, ночь

Прибор сверки написан в ту же ночь: **`tools/migrate/verify-db-parity.mjs`** (критерий П1). Он
сравнивает не счётчики, а **множество путей И хеш данных каждого документа** — сравнение по одним
счётчикам зелено при полностью подменённом содержимом. Доказан **тремя мутациями** отпечатка, и
каждая попала в свою графу: искажённый хеш → «искажено 1», удалённый документ → «потеряно 1»,
подброшенный документ → «лишних 1». Внутри есть контроль прибора: сверка пустых отпечатков не
выдаёт «0 расхождений» за успех, а краснеет («сравнивать было нечего»).

### Р1 — процедура целиком · ✅ ЗАКРЫТА

Закрыта не одним прогоном, а двумя фактами:
- **бесплатный тир переходит к базе, созданной после удаления держателя** — замер на `ndim-stage`
  (создать именованную первой → `freeTier: true`; удалить держателя и создать следующую →
  `freeTier: true`). Подтверждено ещё раз этой ночью: обе временные базы репетиций в боевом
  проекте родились с `freeTier: false`, потому что держатель `(default)` был жив;
- **«переключение приложения, правил и сервера синхронизации на ИМЕНОВАННУЮ базу» больше не
  гипотеза, а работающий контур:** `ndim-server-stage` прямо сейчас считает связи в
  `ndim-db-stage`, приложение стейджа читает её же, правила выкачены. То есть шаг, который в
  боевой миграции самый непроверяемый, уже отработан в живых условиях.

### Р2 — на КОПИИ боевых данных · ✅ ЗЕЛЁНАЯ, расхождений 0

| Шаг | Замер |
|---|---|
| 🔴 Запись боя ЗАМОРОЖЕНА (`docker stop ndim-server-prod`) | выгрузка Firestore **не даёт консистентного снимка**, если в базу пишут во время неё |
| Эталонный отпечаток `(default)` | **11 266** документов, **11 266** чтений |
| Выгрузка → `gs://ndim-space-backup/2026-08-15-r2` | экспортировано **11 266** |
| Загрузка → временная `ndim-db-r2` (в боевом проекте) | импортировано **11 266** |
| Отпечаток копии | **11 266** |
| **Сверка П1** | потеряно **0** · лишних **0** · искажено **0** · **РАСХОЖДЕНИЙ 0** |
| Уборка | база `ndim-db-r2` уничтожена сразу после сверки (в ней ПДн) |

🔑 **Побочно доказан и критерий П2** («похожесть после переезда не изменилась»): документы
`relations/*` вошли в сверку по хешу, и все 341 совпали — то есть топы идентичны, а не «похожи».

### Р3 — репетиция ОТКАТА · ✅ ЗЕЛЁНАЯ, расхождение ОДНО и объяснённое

Разворачивалась **точка возврата фазы 0** — `gs://ndim-space-backup/2026-08-15-pre-migration` —
во временную `ndim-db-r3`, затем сверялась с отпечатком фазы 0.

| Что | Замер |
|---|---|
| Восстановлено | **11 266** из 11 266 · потеряно **0** · лишних **0** |
| Искажено | **1** — `space/server` |

🔑 **Почему это ЗЕЛЁНЫЙ, а не красный, и почему объяснение проверяемо, а не удобно:** отпечаток
фазы 0 снят в **16:21:18Z**, выгрузка записана в **16:22:57Z** — между ними **99 секунд**, а цикл
сервера синхронизации **60 секунд**. То есть за этот промежуток он ОБЯЗАН был записать
сердцебиение хотя бы раз, и разойтись мог ровно один документ — тот, который он и пишет каждый
цикл. Все **11 265** документов данных людей (каталог, оценки, точки, профили, связи, `privat`,
`ndimids`, предложения, админы, статистика) восстановились **побитово**.

⚠️ И это же — независимое доказательство ЧУВСТВИТЕЛЬНОСТИ сверки: она нашла один изменившийся
документ среди 11 266. Прогон, давший бы ровные «0 расхождений», был бы менее убедительным.

🔑 **Урок протокола для фазы 3:** запись боя обязана быть заморожена ДО выгрузки, иначе снимок не
консистентен, и «расхождение» в бою придётся объяснять уже задним числом. В Р2 заморозка была,
в фазе 0 её не было — отсюда и единственное расхождение Р3.

### Что репетиции НЕ доказали (граница, названная честно)

- **Удаление боевой `(default)`** ни разу не выполнялось на настоящих данных — только на пустых
  базах стейджа. Это единственный необратимый шаг фазы 3, и его страховка не репетиция, а
  ДОКАЗАННЫЙ откат (Р3) плюс свежая выгрузка непосредственно перед удалением.
- **Простой боя (П4)** не измерен: репетиции шли без удаления держателя тира. Оценка по замерам
  этой ночи — выгрузка ≈ 2 мин, загрузка ≈ 2 мин, создание базы ≈ 1 мин, плюс ожидание
  освобождения имени (~5 мин, если имя переиспользуется; у нас имена разные).

**⛔ ФАЗА 3 ОТКРЫТА, НО НЕ НАЧАТА — она требует AUTH владельца В МОМЕНТ ЗАПУСКА.**
Красная репетиция останавливает эпик — фаза 3 не начинается.

### Фаза 3 — боевая миграция `(default)` → `ndim-db-prod` · ✅ ВЫПОЛНЕНА 2026-08-16

**AUTH владельца в момент запуска:** «*Да, начинай, мигрируем, даю добро*».

| Шаг | Время | Замер |
|---|---|---|
| Запись боя заморожена (`docker stop ndim-server-prod`) | 00:24:46 | — |
| Эталонный отпечаток `(default)` | 00:25 | **11 266** документов |
| Свежая точка возврата `gs://ndim-space-backup/2026-08-16-migration` | 00:27 | выгружено **11 266** |
| 🔴 Удаление `(default)` · создание `ndim-db-prod` · загрузка | — | **выполнил ВЛАДЕЛЕЦ** скриптом |
| **П3** — бесплатный тир | — | `freeTier: **True**` ✅ |
| Отпечаток `ndim-db-prod` | 00:44 | **11 266** |
| **П1/П2** — сверка | 00:45 | потеряно **0** · лишних **0** · искажено **0** ✅ |
| Правила и индексы | 00:50 | выкачены дверью, `--only firestore` целиком |
| Приложение | 00:53 | `npm run deploy`: заголовки ✅ · **смоук под сессией 24/0** ✅ · гостевой **81/0** ✅ |
| Сервер синхронизации на новой базе | 00:55:42 | людей **94** · измерений **5111** · связей **2344** — как до переезда |
| Защита от удаления возвращена | 00:57 | `DELETE_PROTECTION_ENABLED` ✅ |

**П4 — простой.** Полное окно операции **31 минута** (00:24:46 → 00:55:42) при потолке 60.
⚠️ Честная граница: видимый людям простой — ПОДМНОЖЕСТВО этого окна (от удаления базы до выката
приложения), но точную минуту удаления агент не наблюдал — необратимый шаг выполнял владелец.
Публичные страницы каталога не страдали вовсе: они пререндеренный статический HTML и базу не
читают.

🔴 **ДЫРА, ВСКРЫТАЯ ПЕРЕЕЗДОМ И ЗАКРЫТАЯ В ТОТ ЖЕ ЧАС.** Хук `deploy-guard` запрещает ЛЮБОЙ голый
`firebase deploy`, а дверь катила **только хостинг** — то есть выкатить правила в бой было
**нечем**. Пока правила совпадали с лежащими в бою, дыра ничего не стоила; в день, когда база
сменила имя, она стала блокирующей: у новой базы правила по умолчанию запрещают всё. Лечение —
правила стали ПЕРВЫМ шагом двери (`tools/deploy.mjs`), а не исключением из стража. Первым, потому
что следующий шаг — снимок витрины лендинга — сам читает боевую базу.

🔑 **Второй урок, тоже оплаченный этой ночью:** имя базы было зашито ЛИТЕРАЛОМ в пяти местах —
приложение и четыре прибора. Забытое пятое означало бы поход в несуществующую базу с невнятной
«ошибкой сети». Сведено в `tools/lib/contours.mjs`, который ЧИТАЕТ `src/lib/firebase.ts` и падает
при расхождении: пара «истина ↔ зеркало», проверяющая себя при каждом импорте.

⚠️ **И признание о чуть не случившейся аварии.** Готовя массивную форму `firestore`, агент
переписал `firebase.json` ЦЕЛИКОМ по памяти, прочитав из него только одну секцию. Реконструкция
снесла бы **17 редиректов 301** (проиндексированные адреса), `cleanUrls` и правила кеширования,
вылеченные `bugs/124`. Поймано немедленной сверкой с git, оригинал восстановлен, правка сделана
точечной. Класс — «перезаписал то, чего не читал»; лечение то же, что у гигиены коммитов:
**смотреть на цель ДО перезаписи**.

### Фаза 4 — дверь и приборы · ✅ ЗАКРЫТА 2026-08-16

| Что | Замер |
|---|---|
| `npm run deploy -- --stage` — одна дверь, параметр контура | стейдж выкачен ею дважды; порядок шагов общий, различий ровно три и каждое названо в коде |
| **П8** — стоп-правило контура в двери | `node tools/deploy.mjs --selftest` — **8 случаев**, провалов 0 |
| **П8** — замер попадания ПОСЛЕ выката | цель отдала хеш собранной сборки (`1epzgt8`), сосед-бой остался `3ak94l` |
| **П6** — стейдж невидим поиску | `verify-stage-noindex` **11/0** (гоняется дверью на каждом выкате стейджа) |
| **П7** — в стейдже нет ПДн живых людей | `verify-stage-clean` **14/0**: боевых ключей 0 из 364 · 342 · 341; `privat` 0; `ndimids` 0; несинтетических имён 0 из 331; «о себе» 0; фото 0; точных дат 0 |
| Шесть приборов `verify-prod-*` приняли `--base` | `cache` · `signed-in` · `b4` · `dimension-pages` · `lang-redirects` · `dim-ratings` |
| Реестр контуров | `tools/lib/contours.mjs` вырос с имён баз до описания контура целиком и сверяет с приложением ещё и веб-ключи с хостами |

🔑 **Находка фазы, оплаченная мутацией:** снятие проверки «команда не упоминает чужой проект»
прошло по самопроверке НЕЗАМЕЧЕННЫМ — то есть правило ничем не охранялось. Промах, которого не
было в наборе: `--only hosting:ndim-stage` при **боевом** проекте. Случай добавлен, мутация на нём
краснеет адресно.

🔑 **Второе, найденное при переводе приборов на `--base`:** смоук под сессией брал адрес из флага, а
ключ уборки — боевой константой. На стейдже он заводил анонимную учётку и молча не удалял её.
Класс — «адрес оттуда, ключ отсюда»; лечение — контур берётся ОДНИМ объектом.

⚠️ **Не сделано и вынесено как названный долг:** замер, шлёт ли preview-канал `X-Robots-Tag` сам.
Preview-каналы в работе не используются ни разу; замер стоит полного выката во временный канал.
Пока каналы не понадобились, ответ на вопрос ничего не меняет.

### Фаза 5 — процесс и документы · ✅ ЗАКРЫТА 2026-08-16

- ✅ **Ответ В5 = А встроен в дверь замком.** Удостоверяет «та же сборка» **коммит**, а не хеш
  рантайма (тот рождается заново каждой сборкой и запрещал бы выкат всегда): расписка
  `.kaif/stage-receipt.json` несёт sha HEAD и признак грязного дерева, дверь боя требует совпадения
  и чистоты. Расписка выписывается ТОЛЬКО после всех зелёных проверок — дверь до этой строки не
  доживает, если что-то упало.
- ✅ **Обход — только словом владельца и ОДНОРАЗОВЫЙ.** Первая редакция писала обычную расписку,
  то есть одно слово открывало дверь и всем последующим выкатам того же коммита; поймано на себе
  и вылечено — обход уходит в журнал, который дверь никогда не читает.
- ⚠️ **Честная граница замка, названная в коде:** бой собирается со свежим снимком витрины
  лендинга, стейдж — нет (П9). Значит на стейдже проверен тот же КОД с другим числом на витрине.
  Замок стережёт код, а не байты.
- ✅ `AGENT_GUIDE.md` → «ВЫКАТ В БОЙ» получил второй контур и замок; реестр стенда — оба новых
  доказательства. Осталось владельцу: подтвердить или запретить обход `--auth-owner`.

---

## Риски

| Риск | Чем закрыт |
|---|---|
| 🔴 Потеря данных людей при переезде | Фаза 0 (выгрузка) + Р2 (репетиция на копии) + Р3 (репетиция отката) + П1/П2 |
| Выход из бесплатного тира из-за порядка операций | Р1 замеряет `freeTier` до боя (П3) |
| Молчаливый невыкат правил при мульти-БД конфиге | Дверь зовёт `--only firestore` без под-таргета; проверяется мутацией |
| Стейдж попал в поисковую выдачу и конкурирует с боем | П6, страж |
| Прогон на стейдже утёк в бой через данные | Класс известен: продукт строит личные ссылки из боевого `canonical` (`STATUS.md` п. 9). Приборы стейджа обязаны переносить ссылку на адрес контура |
| Два ключа сервисного аккаунта на диске | Оба в `.private/`-режиме, `npm run kaif:check` перед каждым push |
| Провайдеры входа на новом проекте не включаются из CLI | Проверяется замером в фазе 1; при упоре — домашка владельцу на один клик |

## Решения, принятые агентом без владельца

*(заполняется по мере закрытия фаз)*

- Стейдж закрывается от поисковиков **заголовком хостинга**, а не правкой сборки — чтобы
  продакшен-артефакт остался один (П9).
- Дверь в бой **не раздваивается**: один код, параметр контура.
- 🆕 **У замка стейджа есть аварийный обход — `--auth-owner "<дословные слова владельца>"`.**
  Решение агента, и владелец вправе его отменить одним словом. Довод «за»: правило без выхода
  запирает и аварию — если бой лежит, а стейдж почему-либо не выкатывается, чинить его будет
  нечем, и тогда сессия начнёт обходить дверь целиком (то есть вернётся к `firebase deploy`
  руками, ради запрета которого всё и строилось). Довод «против» назван честно: любой выход
  со временем становится дорогой. Поэтому обход стоит дорого — требует ДОСЛОВНЫХ слов владельца,
  печатается баннером, действует ОДИН раз и записывается в журнал.
- 🆕 **«Та же сборка» удостоверяется коммитом, а не байтами артефакта.** Хеш рантайма
  (`__sveltekit_<хеш>`) рождается заново на каждой сборке, поэтому сравнение по нему запрещало бы
  выкат всегда. Цена решения названа: замок стережёт КОД, а не число на витрине лендинга.
