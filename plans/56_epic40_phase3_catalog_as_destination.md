# План 56 — ЭПИК 40, ФАЗА 3: «Каталог: от двери к назначению»

> **Третья ступень лестницы `/plan-epic`** — операционный план ТОЛЬКО этой фазы.
> План фазы 4 пишется отдельно и идёт ПАРАЛЛЕЛЬНО (см. «Границы» ниже).
>
> **Метаплан:** `plans/40` → «Фаза 3 — Каталог: от двери к назначению».
> **Создан:** 2026-08-16 (сессия 18) · **Статус:** 🔲 не начат — готов к исполнению ·
> **Родитель:** `plans/40`; приоритет №1 проекта по решению владельца **интервью №035, В1 = А**
> **Исходящее:** журнал EN-правок «что было → что стало» — на вычитку владельцу ПОСТФАКТУМ
> (интервью №025, В6 = А) · вторая точка тренда покрытия — в фазу 8
>
> **Решения владельца, действующие в этой фазе (не переоткрывать):**
> · №025 **В1 = А** — все 10 222 остаются в индексе, мы их **обогащаем**; вердикт по каталогу —
>   через ~3 месяца по критериям, записанным в `plans/41` ДО данных;
> · №025 **В6 = А** — EN-фикс делает агент, с журналом правок на вычитку постфактум;
> · №025 **В7 = А** — блок соседства только ПО КЛАССИФИКАЦИИ; чисел похожести и порядка близости
>   наружу нет; формулировка «Рядом в Пространстве» запрещена;
> · №022, слово владельца 2026-08-03 — **порог показа оценок отменён**: показываем при ЛЮБОМ
>   числе оценивших. ⚠️ Не «чинить» это обратно;
> · **NDSR** — фирменное имя величины, не переводится ни на один язык.

---

## Зачем эта фаза именно сейчас

Владелец 2026-08-16: «*приложение сейчас мертво без органического трафика и SEO*», и критерий
сортировки работ — **лаг созревания**. Эта фаза выбрана первой не потому, что дешевле, а потому
что её масса — **10 222 уже проиндексированные страницы**: переработанная сегодня страница
начинает зреть сегодня, отложенная на месяц теряет месяц сразу на всём каталоге.

**Что сегодня не так** (это и есть предмет фазы): карточка каталога полезна человеку ТОЛЬКО как
дверь внутрь продукта. Она отвечает на «что это такое», но не отвечает на вопрос, с которым
человек пришёл из поиска, — «стоит ли мне это смотреть/читать». Пока страница не самодостаточна,
она конкурирует с Википедией её же оружием и проигрывает; самодостаточной её делает **NDSR —
то, чего нет больше нигде**.

## Вектор цели

- **Achieve:** страница каталога полезна БЕЗ клика внутрь продукта: блок-дверь переработан в
  смысл NDSR, у видов появились страницы данных («самое любимое»), английские описания очищены от
  справочного стиля. Всё это — на уже проиндексированной массе, без единого нового адреса-пустышки.
- **Maintain:** ни одно число похожести и ни один порядок близости наружу; ПДн на публичных
  страницах нет; вес клиентского бандла не растёт (`EXP-0136`); `verify-dimension-pages` и
  `verify-catalog-hubs` остаются зелёными.
- **Avoid:** массовый `noindex`/удаление карточек (запрещено метапланом до данных) · canonical для
  «тонких» карточек (они не дубли) · выдуманные факты о произведениях · правка ДАННЫХ каталога в
  бою (это `plans/30`, слово владельца).

## Критерии приёмки фазы

1. **Самодостаточность доказана выборкой:** 10 переработанных страниц проверены по чеклисту
   «полезна без клика» (шаг 5), результат вписан в этот план числом, а не впечатлением.
2. **Страж инвариантов расширен и доказан мутацией** — с адресатами, названными ДО прогона.
3. **EN-описания:** 279 записей с маркерами справочного стиля разобраны; журнал «что было → что
   стало» лежит страницей и подан владельцу на вычитку постфактум.
4. **Вес сборки в норме:** клиентский бандл < 3 МБ, крупнейший чанк узла < 500 КБ.
5. `npm test` · `npm run check` · `verify-dimension-pages` · `verify-catalog-hubs` — зелёные.
6. `/fable-judge` по утверждениям фазы — прогнан, вердикт вписан сюда.

## Границы — чего эта фаза НЕ делает

| Что | Куда это относится |
|---|---|
| Опорные страницы под интенты | Фаза 4, отдельный план, идёт ПАРАЛЛЕЛЬНО |
| Правка ДАННЫХ каталога (прополка `вида`, `name`) | `plans/30` / `plans/44` — слово владельца |
| Сегментация `sitemap.xml` | Отложена замером: 10 432 адреса из 50 000, 0,84 МБ из 50 |
| Чтение поисковых консолей | `ideas/37`, следующий по приоритету |
| Блок соседства по похожести (IBCF) | `plans/37`, «до лучших времён» (№025 В7) |

---

## Шаги

### Шаг 0 — Разведка по живому, а не по памяти (автономно) ✅ ЗАКРЫТ 2026-08-16

- [x] Снято с ЖИВЫХ боевых страниц: **четыре** карточки (взят и четвёртый срез — законное
      состояние «голоса есть, средняя ноль»), **оба языка**, итого 8 страниц.
- [x] Перечитан `src/routes/[lang=lang]/dimension/[slug]/+page.svelte` — объект `T`, ветки `ru`/`en`.
- [x] Таблица «что есть → чего не хватает» — ниже, по четырём срезам.

⚠️ Капкан: не читать боевые данные людей. Страница каталога публична и ПДн не содержит — это
проверяет `verify-dimension-pages`; ничего сверх отданного HTML для разведки не нужно. Соблюдено:
снят только отдаваемый HTML.

🔧 **Как снималось** (пригодится следующей сессии): `node fetch` в этой среде до боя не доходит
(`UND_ERR_CONNECT_TIMEOUT`), а `Invoke-WebRequest` доходит. Качает PowerShell в файлы, разбирает
node. И **Svelte дописывает к каждому классу свой хеш** (`class="dim svelte-d710dx"`) — точный
селектор `class="dim"` не совпадает ни с чем и даёт пустоту, похожую на «блоков нет».
Класс искать СЛОВОМ среди классов, маркеры гидратации `<!--[0-->` снимать вместе с тегами.

#### Порядок блоков сегодня (одинаков на всех страницах и в обоих языках)

`← хаб вида` → `H1` + второе имя → **досье** (строка оценки · Вид · Год · Автор · Теги) →
**описание** (433…1658 знаков) → **блок-дверь** → «Похожие по каталогу» (8 ссылок) → подвал.

#### Что человек видит сегодня, по срезам (дословно, бой 2026-08-16)

| Срез | Карточка | Строка оценки | Блок-дверь |
|---|---|---|---|
| **Много голосов** | Гарри Поттер и философский камень (Фильм, 2001) | 8/10 звёзд · **8,4** · NDim Space Rating · «оценено 14 людьми» | одинаков |
| **Один голос** | Дом Дракона (Телесериал, 2022) | **10/10 звёзд** · **10,0** · «оценено 1 человеком» | одинаков |
| **Ноль голосов** | Идеальный незнакомец (Фильм, 2007) | курсивом «Ещё без голосов» | **тот же самый** |
| **Голоса есть, средняя ноль** | Квадробика (Практика, 2014) | 0/10 звёзд · **0,0** · «оценено 1 человеком» | одинаков |

Текст двери сегодня, дословно и ОДИН на все четыре состояния:
заголовок «NDim Space ведёт свою систему рейтингов» · абзац «NDim Space Rating показывает,
насколько объект нравится сообществу, — ищете, что посмотреть или почитать сегодня, загляните к
нам. А если оцените «‹название›» и ещё несколько вещей, которые Вы любите, Пространство покажет
Вам людей, которые думают так же.» · кнопка «Оцените, чтобы найти людей, кто думает, как Вы» →
`/profile`.

#### Числа каталога, пересчитанные под эту фазу (`dims-build.json`, 2026-08-16)

| Что | Сколько | Доля |
|---|---:|---:|
| Объектов всего | **5111** | 100 % |
| Без единого голоса | **3234** | **63 %** |
| С голосами | 1877 | 37 % |
| Из них держатся на **ОДНОМ** голосе | **920** | **49 % оценённых** |
| «Голоса есть, средняя ноль» | 2 | — |
| **Максимум голосов по всему каталогу** | **14** | — |

#### Таблица «что есть → чего не хватает для самодостаточности»

| Срез | Что страница даёт сегодня | Чего не хватает, чтобы она была полезна БЕЗ клика |
|---|---|---|
| **Ноль голосов** (3234, 63 %) | Название, вид, год, автор, теги, описание, 8 соседей. Оценки нет, сказано честно. | Дверь **говорит про рейтинг, которого нет**: «NDim Space Rating показывает, насколько объект нравится сообществу» — при пустой строке оценки. Человеку не предложено ничего, кроме регистрации. |
| **Один голос** (920, 49 % оценённых) | Крупная цифра **10,0** и десять закрашенных звёзд. | Число подано **как вердикт сообщества**, хотя это оценка одного человека. Веса выборки страница не называет — а именно честность и есть то, чем NDSR отличается от чужих рейтингов. Дверь про это молчит. |
| **Много голосов** (единицы; потолок 14) | 8,4 · 14 оценивших — самый доверительный случай каталога. | Не сказано, **много это или мало** и что число значит. Нет места объекта внутри своего вида — а оно у нас посчитано (взвешенный порядок хаба) и есть факт, которого нет больше нигде. |
| **Голоса есть, средняя ноль** (2) | 0,0 при «оценено 1 человеком» — законное состояние шкалы, показано верно. | Читается как поломка. Дверь не помогает отличить «оценили на ноль» от «не оценивали». |

**Вывод шага 0 одной строкой:** блок-дверь **не различает состояний вовсе** — один и тот же текст
стоит и там, где рейтинга нет, и там, где он держится на одном голосе. Это и есть предмет шага 1.

🐞 **Попутно вскрыт дефект, шагом 1 он не лечится** — `bugs/135`: строка «Теги» показывает
**оба языка разом** («кино фильм movie film фэнтези волшебство fantasy magic») и делает это на
ОБЕИХ языковых версиях. Замер: смешанный список у **5104 объектов из 5111 (99,9 %)**, то есть
на 10 208 страницах из 10 222. `+page.server.ts:152` отдаёт `tags: dim.tags` без выбора по языку —
единственное поле карточки, которое через `pick()` не проходит.

⚠️ **Второе подозрение снято замером и оказалось ложным.** Отсутствие строки «Автор» у
«Квадробики» — не дефект: в данных там `"-"`, а `known()` (`+page.server.ts:86`) честно трактует
прочерк как «неизвестно» и прячет строку. У практики автора нет по существу, и её же описание это
говорит. Объектов вовсе без поля `author` в каталоге — **ноль**.

### Шаг 1 — Блок-дверь → смысл NDSR (ядро фазы)

- [x] Переписать блок-дверь так, чтобы он отвечал на вопрос человека из поиска: что за объект,
      **как его оценило Пространство** (NDSR полным именем, счётчик «оценено N»), и лишь затем —
      приглашение внутрь. Тексты RU и EN — по `AUTHOR_STYLOMETRY.md`, с пометками провенанса.
- [x] ✅ **ЗАКРЫТО 2026-08-21** (`dev-2`, ветка `ndim_dev2`). Три состояния, и ни одно не притворяется другим (канон шкалы 0…10):
      **нет голосов** — честная строка «ещё без голосов», звёзд нет · **есть голоса** — звёзды и
      NDSR · **все поставили ноль** — `rates` > 0 при `stars` = 0 это ЗАКОННОЕ состояние, страница
      обязана его показать (в бою таких измерений два).
      > 🔄 **РЕШЕНИЕ ВЛАДЕЛЬЦА ПО НУЛЕВОМУ СОСТОЯНИЮ — интервью №044, В3 (2026-08-22), дословно:**
      > «*должна рисоваться одна серая звезда, как в шкале оценок. Если оценка выше нуля,
      > единица - то одна золотая звезда, и дальше по мере роста оценки растёт количество
      > отображаемых золотых звезд. Ничего никому объяснять не нужно про ноль звёзд - это просто
      > частный случай пространства*». То есть: ОТДЕЛЬНЫЙ объяснительный текст двери для нулевого
      > состояния ОТМЕНЁН, отображение звёзд перестраивается (ноль при живых голосах — одна серая
      > звезда). Правка — постановка dev-2 от 2026-08-22; приёмка кадрами «до/после» через вопрос
      > владельцу «это оно?».
- [ ] ⛔ STOP-ASK, если захочется написать про объект что-то, чего нет в данных каталога:
      **выдуманный факт хуже отсутствующего** (`PHILOSOPHY.md` → три двери).
- **Проверка:** `npm run build`; три карточки трёх срезов открыты глазами в обеих темах.

> 🔄🔄🔄 **ВТОРОЕ СЛОВО ВЛАДЕЛЬЦА, ПО КАДРАМ (2026-08-22) — ДЕЙСТВУЮЩАЯ РЕДАКЦИЯ ПРАВИЛА.**
>
> Он посмотрел пару «до/после» и поправил дословно: «*да, но первую звезду можете тёмно серой
> нарисовать. То есть, оценка между нулём и единицей - это рисуем первую звезду серым тяжёлым
> цветом, только от 1 и выше - первая звезда заливается золотом*».
>
> **Действующее правило:** оценка НИЖЕ ЕДИНИЦЫ (включая ровно ноль) — одна ТЯЖЁЛАЯ СЕРАЯ звезда
> (токен `--dim`); от единицы и выше — только золотые, числом округлённой оценки, пустых позиций
> нет. Правило накрывает ТРИ поверхности одной функцией `starRow`: публичную карточку, хабы
> каталога (`CatalogHub.svelte`) и экран «Измерения» (`dims/+page.svelte`, строка ПОКАЗА;
> шкала ВВОДА не тронута).
>
> ⛔ **ОТМЕНЕНА МОЯ «ЗАЩИТА НА ВЫРОСТ».** В первой редакции положительная оценка ниже 0,5 давала
> одну ЗОЛОТУЮ звезду — я вывел это из его первой фразы «оценка выше нуля → золотая». Уточнение
> читает ту же границу прямо: золото начинается С ЕДИНИЦЫ. Возврат `Math.max(1, …)` — откат
> решения владельца, а не улучшение.
>
> 🔑 **Почему ровно ноль попал в «ниже единицы», хотя сказано «между нулём и единицей»:** его «но»
> произнесено, ГЛЯДЯ НА КАДР НУЛЯ, и в его фразе ровно две ветки. Вынеси ноль из первой — у него
> не останется ветки вовсе, чего он не говорил. Прочтение подтверждено Менеджером до правки.
>
> 🎨 **Цвет не выдуман:** `--dim` — старший из двух серых палитры (светлая тема #6a819a против
> #90a4bb у `--faint`), то есть буквально «тяжёлый серый». В тёмной теме он тоже старший, и
> тяжесть там даёт контраст к фону, а не темнота — это названо вслух, потому что «тёмно-серый»
> на тёмном фоне означал бы невидимый.
>
> 📐 **Случай 0 < r < 1 в каталоге не существует** (замер: 1877 оценённых, ни одного в интервале),
> поэтому кадром он не снят — и это не пробел: `starRow(0)` и `starRow(0,33)` возвращают ОДНО И ТО
> ЖЕ, что закреплено юнитом «кадр нуля ЕСТЬ кадр всего поддиапазона». Утверждение накрывает весь
> интервал, а съёмка показала бы одну точку.

> 🔄🔄 **ПЕРВОЕ СЛОВО ВЛАДЕЛЬЦА 2026-08-22 — интервью №044, В3 (уточнено выше).**
>
> Третий ТЕКСТ двери, описанный ниже, ОТМЕНЁН. Его слово дословно: «*должна рисоваться одна серая
> звезда, как в шкале оценок. Если оценка выше нуля, единица - то одна золотая звезда, и дальше по
> мере роста оценки растёт количество отображаемых золотых звезд. Ничего никому объяснять не нужно
> про ноль звёзд - это просто частный случай пространства*».
>
> **Что стало вместо него:** ряд звёзд рисуется по его правилу — только золотые числом округлённой
> оценки, без пустых позиций; ровно ноль при живых голосах даёт ОДНУ СЕРУЮ звезду цветом `--faint`,
> тем же, каким шкала ВВОДА рисует поставленный ноль. Текст двери снова знает два состояния,
> `ratingState` удалён целиком (у него не осталось потребителя), правило живёт функцией `starRow`
> в `dims-rating.ts`.
>
> 🎓 **УРОК, КОТОРЫЙ ДОРОЖЕ САМОЙ ПРАВКИ.** Я нашёл настоящую беду — состояние, которое молчит о
> себе и выглядит как поломка, — и это подтвердилось. Но лечить её я взялся ОБЪЯСНЕНИЕМ, а
> владелец вылечил ОТРИСОВКОЙ. **Состояние продукта показывают, а не разъясняют:** если человеку
> приходится читать абзац, чтобы понять картинку, — чинить надо картинку. Абзац при этом ещё и
> дороже: он живёт на 10 222 страницах и требует вычитки на двух языках.
> 🔑 И вторая половина: развилку я передал Менеджеру ВСЛУХ («подпись у самого числа — витрина,
> решает владелец»), и она дошла до владельца за час. Не передал бы — правка уехала бы в бой
> моим решением (`EXP-0170`: замер называет цену, но не принимает решения о витрине).
>
> **Кадры до/после** — `test-results/rating-stars/{before,after}/`, по 12 (три случая × 390/1440 ×
> обе темы). Цвета сняты `getComputedStyle`, а не глазами: ноль был десятью звёздами
> `rgb(230,238,247)`, стал одной `rgb(144,164,187)`; оценка 7,5 была десятью позициями, стала
> восемью золотыми `rgb(232,165,22)`.

> ✅ **КАК ЗАКРЫТО.** Третье состояние выведено ОДНОЙ функцией на проект — `ratingState`
> (`src/lib/content/dims-rating.ts`), рядом с правилом показа звёзд и на его опоре: порог голосов
> здесь не сравнивается заново, поэтому копии правила в разметке не появилось (её грепает
> `dims-rating.test.ts`). Разметка ветвится по готовому полю `DimView.ratingState`.
>
> ⚠️ Ниже — как шаг был закрыт ДО правки владельца. Оставлено как история развилки, а не как
> действующее описание: третьего текста в коде больше нет.
>
> 🔑 **Текст владельца НЕ ТРОНУТ НИ НА ЗНАК, и третье состояние встроено в его же форму.** Он сам
> устроил дверь так, что три первых предложения общие, а последнее — своё у каждого состояния
> («Ваша оценка будет первой!» · «Ваш голос отображается в рейтинге…»). Агентом написано ровно
> одно предложение, третье закрывающее: «*Все, кто оценил этот объект, поставили ноль баллов, и
> NDim Space Rating показывает ноль*» (EN: «*Everyone who rated this object gave it a zero, and
> NDim Space Rating shows zero*»). Оно несёт пометку провенанса `[AI]…[/AI]` в коде и ЖДЁТ
> ВЫЧИТКИ ВЛАДЕЛЬЦА — снимает пометку только его слово.
>
> ⚠️ **Утверждение проверяемо, а не придумано:** шкала 0…10 неотрицательна, поэтому средняя РОВНО
> ноль возможна только тогда, когда ноль поставили ВСЕ оценившие. STOP-ASK шага не сработал —
> ни одного факта об объекте, которого нет в данных каталога, на страницу не добавлено.
> Голос проверен прибором: `node tools/lint-owner-voice.mjs` — чисто, медиана колона 4
> (инвариант руки автора), противопоставлений нет.
>
> 🔴 **ЧЕГО Я НЕ СДЕЛАЛ И ПОЧЕМУ — развилка владельцу, а не решение агента.** Строку оценки
> (звёзды + «0,0») я НЕ трогал: подпись у самой величины — это витрина, а витрину решает владелец
> (`EXP-0170`). Сегодня состояние различимо текстом двери; если владелец захочет пояснение прямо
> у числа — это работа Дизайнера по правилу четырёх макетов.
>
> **Смотрено глазами:** 40 кадров, обе темы × 390/1440 × оба языка ×
> **пять** случаев (`tools/shoot-dimension-pages.mjs`, случай «все поставили ноль» добавлен в
> прибор условием, а не слагом). Кадры — `test-results/dimension-pages/`.

### Шаг 2 — 🔄 ПЕРЕПИСАН ЗАМЕРОМ 2026-08-16: место объекта в своём виде — НА КАРТОЧКУ

> 🔴 **Исходная формулировка шага была ДУБЛЕМ, и это доказано чтением кода, а не мнением.**
> Она гласила: «на хабе каждого вида — блок „самое любимое по этому виду": топ по NDSR».
>
> **Хаб УЖЕ является этим блоком целиком.** Форма V3 «Рейтинг» (интервью №030, В1 = В) —
> «топ по версии NDim Space»: `CatalogHub.svelte:68` рисует `<ol class="rank" start={firstRank}>`
> с номером места у каждой строки, а `catalog-hub.ts:165` (`makeComparator`) сортирует по
> ВЗВЕШЕННОЙ оценке (№030 В2 = Д). То есть первая страница каждого хаба и есть «самое любимое
> по этому виду», топ-60, с номерами. Добавить туда блок «самое любимое» значило бы поставить
> список поверх того же списка.
>
> Счёт «замер против плана» в проекте стал **5:0**. План — гипотеза, и эта гипотеза не прошла.

**Чем шаг заменён — тем, чего в замере действительно не хватает.** Таблица шага 0 назвала это
прямо: **карточка не знает своего места**. Место посчитано, публично и бесплатно:

- порядок внутри вида уже вычислен ОДИН РАЗ на сборку (`catalog-source.ts` → `groupByKind`),
  значит место карточки — это индекс в готовом списке, то есть поиск по `Map`, а не вычисление;
- номер места **уже публичен**: его показывает каждая страница хаба. Показать его на карточке —
  не новое раскрытие, а согласование двух поверхностей, которые сегодня знают разное;
- ⛔ это НЕ метрика похожести и не порядок близости: инвариант владельца не задет.

> ✅ **СТРОКА ВЫПУЩЕНА — ВСЕМ, У КОГО ЕСТЬ ОЦЕНКА** (интервью **№036, В5**, решение владельца
> 2026-08-16 страницей вычитки). Его слова: «*Никакой проблемы нет… Всем измерениям, у которых
> есть оценка, ставим место. Когда пространство наполнится людьми и оценками, все проблемы будут
> решены обилием голосов. Ты паникуешь из-за того, что сейчас голосов мало*».
>
> 🔴 **ИСТОРИЯ РАЗВИЛКИ — ЧТОБЫ ЕЁ НЕ ПЕРЕОТКРЫЛИ.** Я снял отрисовку своим замером и предложил
> порог «только топ-50». Владелец порог ОТКЛОНИЛ. Возврат условия `rank <= 50` — это откат его
> решения, а не улучшение.
>
> **Замер, на котором стоял мой довод, верен и остаётся фактом о СЕГОДНЯ** (прибор
> `tools/measure-catalog-place.mjs`, картинки — `design/catalog-place-mockups.html`):
> 96 % оценённых фильмов (937 из 981) стоят в группе объектов с ОДИНАКОВЫМИ числами (191 фильм
> «10,0 × 1» занимает места 242…432, порядок внутри группы разводит слаг); один голос «5» уносит
> фильм со 100-го места на 508-е. **Ошибкой было не измерение, а вывод из него:** числа описывают
> разрежённость данных на сегодня, а механизм строится под рост.
>
> 🔑 **И половина довода умерла ещё до ответа — от правки самого владельца** (№036 В2 = B,
> гипербола): прежняя редакция этого блока говорила «Поттер 534-й, выше него 245 фильмов на одном
> голосе». Пересчёт по действующему коду: **Поттер 151-й из 981, и из 150 фильмов впереди него
> НИ ОДИН не держится на единственном голосе.**

- [x] Считать место объекта среди **ОЦЕНЁННЫХ** объектов своего вида и отдавать его в `DimView`.
      🔴 **Только для оценённых.** Неоценённые (63 % каталога) сортируются по слагу — «3106-е
      место среди фильмов» было бы числом из шума, то есть выдуманным фактом в чистом виде.
      Знаменатель — `rated` своего хаба, а не `count`: «12-е место среди 981 оценённого фильма»
      правда, «12-е из 2774» — нет.
- [x] Поставить строку в досье рядом с оценкой; текст — по `AUTHOR_STYLOMETRY.md`, оба языка.
      ✅ **Проверено живьём 2026-08-21** (`dev-2`): строка стоит в досье под «Автором», оба языка
      («422-е среди 422 оценённых» · «#422 of 422 rated»), знаменатель — `rated` своего хаба.
      Порога по месту нет, неоценённые строки не имеют — стережёт `verify-dimension-pages`.
- [x] ⛔ Никаких чисел похожести, порядка близости и слова «Рядом в Пространстве». ✅ проверено
      стражем по собранному сайту: запретной формулировки 0, процентов в блоке соседей 0.
- **Проверка:** `node tools/verify-catalog-hubs.mjs` зелёный · вес бандла не вырос (считается на
      сервере, `csr = false`) · выборочно: место карточки совпадает с её номером на странице хаба
      — это пара «истина ↔ зеркало», и она обязана сойтись.

### Шаг 3 — EN-описания: снять ЗАИМСТВОВАНИЕ, регистр СОХРАНИТЬ (журнал обязателен)

> 🔴 **ЗАГОЛОВОК ПЕРЕПИСАН 2026-08-22 (Менеджер, по разведке dev-1) — и это не косметика.**
> Прежний заголовок гласил «убрать справочный стиль» и **противоречил прямому слову владельца
> того же 2026-08-17**, записанному в `candidates/README.md`: «*Я писал более вики подобно,
> академически. Ты пишешь как какой-то рассказ… **Мы: мини википедия**.*»
> Свежая сессия, прочитавшая только заголовок, увела бы **510 английских описаний ПРОЧЬ** от
> регистра, который владелец потребовал, — и была бы уверена, что исполняет план.
> **Убираем ЗАИМСТВОВАНИЕ (дословный ряд ≥10 слов против статьи Википедии). Энциклопедический
> регистр — сохраняем.** Тело шага это уже знало (врезка «замер переставил цель шага»),
> заголовок отстал; отставший заголовок и есть та строка, которую читают вместо тела.

> 🔴 **ЗАМЕР 2026-08-16 ПЕРЕСТАВИЛ ЦЕЛЬ ШАГА, и это главное, что о нём нужно знать.**
>
> План гнался за ПЯТЬЮ МАРКЕРАМИ, но §10 `researches/34` искал не их: он нашёл **прилипание к
> Википедии**, а маркеры были всего лишь дешёвым СИТОМ для поиска кандидатов. Разница вскрылась
> сверкой с английской Википедией (метод §10, точные цитаты):
>
> · **Маркер — хорошее сито.** Из шести проверенных текстов первой партии (маркер дат эфира) у
>   **трёх** нашёлся почти дословный ряд: «Гадкие американцы» (20 слов), «Tales from the Crypt»
>   (10 слов), «The Andy Griffith Show» (ряд с числами серий). У трёх других (`rawhide`,
>   `dark-shadows`, `entourage`) заимствования нет.
> · 🔴 **Но моя правка лечила только ВЕРШИНУ.** Заимствование выходит ЗА ПРЕДЕЛЫ предложения с
>   маркером: в двух случаях из трёх я снял маркер, а соседний дословный ряд остался. Правка
>   «по маркеру» создаёт ложное «сделано».
> · **Оставшиеся два маркера сита — НЕ улика.** `voiced by` (196) стоит в скобках кредитной
>   записью — ровно как «(актёр)» у игрового кино в этом же каталоге; `premiered on` — обычный
>   английский глагол премьеры. Четыре проверки (`rick-and-morty`, `the-orville`, `pinocchio`,
>   `vikings`) заимствований не дали. Переписывать их массово значило бы **наказывать
>   совпадение** — ровно то, от чего предостерегает сам список кандидатов.
>
> **Отсюда честная форма остатка** (проверка шага это прямо разрешает): маркеры справочного
> РЕГИСТРА обнулены (`aired on … from … to`, `totaling N episodes`, `consists of N seasons` — 0),
> остаток **245 = 196 `voiced by` + 113 `premiered on`** объяснён как естественная английская
> речь, а не заимствование.
>
> ⏭ **Что осталось настоящей работой:** сверить с Википедией остальные **~19** текстов первой
> партии — там сито уже сработало, и цена пропуска доказана шестью случаями из четырнадцати.
>
> 🆕 **РУССКАЯ ПОЛОВИНА ОТКРЫТА ЗАКАЗОМ ВЛАДЕЛЬЦА** (2026-08-16, дословно: «*тогда и русские
> текста нужно будет править, они тоже из википедии могли писаться*»). Он прав, и это доказано
> в тот же час: прибор `tools/grep-ru-boilerplate.mjs` дал **355 кандидатов из 5111**, а третья
> же сверка нашла настоящий случай — «Деньги на двоих» (`two-for-the-money-1myjyafy`), ряд около
> 20 слов почти дословно из ru.wikipedia. Он стал контрольным случаем прибора.
>
> 🔴 **И это ОПРОВЕРГАЕТ прежний вывод §10** `researches/34` («RU-тексты чисты во всех 10
> случаях»): выборка из десяти дала НОЛЬ там, где заимствование есть. **«Проверено выборкой»
> никогда не значит «в каталоге этого нет»** — вывод по выборке описывает выборку.
>
> 🎓 **Побочный урок, оплаченный здесь же:** контроль прибора `grep-en-boilerplate` был привязан к
> «Гадким американцам» — и упал, когда фаза их ВЫЛЕЧИЛА. **Контроль, привязанный к дефекту,
> умирает вместе с дефектом.** Это не повод снять проверку — повод перевесить её на живой
> положительный случай (сделано: `rick-and-morty-qithkcea`) и записать, почему прежний ушёл.

- [ ] Вход — `researches/34_en_boilerplate_candidates.md`: **279 записей из 5111**, маркеры
      `voiced by` 199 · `premiered on` 114 · `aired on … from … to` 33 · `totaling N episodes` 2.
- [ ] Переформулировать, СОХРАНЯЯ факты; ничего не выдумывать, ничего не удалять из фактов.
- [x] 🌙 **МАШИННЫЙ КОНВЕЙЕР ПОСТРОЕН (ночь 2026-08-17)** — четыре прибора, каждый со своим делом:
      `measure-wikipedia-overlap` (находит) → `build-rewrite-worklist` (готовит задание) →
      фан-аут агентов (пишет) → `gate-rewrites` (принимает МАШИНОЙ) → `rewrite-catalog-descriptions`
      (записывает и ведёт журнал).
      🔑 **Приёмку делает измерение, а не мнение:** остаточный дословный ряд считается тем же
      прибором и по тому же порогу, статья берётся из кэша свода — сеть не нужна.
      📐 Числа ночи: русских копий **2,4 %**, английских **12 %** (ряды до **125 слов**).
      Готовы к записи: `rewrites-06-ru-gated.json` (57) · `rewrites-08-en-span.json` (31).
      🔴 **Три правила, купленные этой ночью и обязательные для следующего круга:**
      · **правь ВЕСЬ накрытый кусок**, а не предложение — ряд пересекает границы предложений;
      · **цитату в кавычках не переписывай** — либо оставь дословно, либо сними целиком вместе с
        утверждением; иначе человеку приписываются слова, которых он не говорил;
      · **совпадение внутри кавычек — не заимствование**, и ворота это знают (`isInsideQuotes`).
- [x] ✅ **ОСТАТОК ПЕРВОЙ ПАРТИИ СВЕРЕН — 2026-08-22, dev-1. Числом: 34 из 34, не проверено 0.**

      Сверены ВСЕ 34 записи партии, а не только ~19 неохваченных, — и это решение окупилось
      сразу же (см. следующий пункт). Прибор: `measure-wikipedia-overlap --lang en --slug <список>`,
      живой контроль дороги (`corrective-measures-akp4cezf`) пройден — ряд 50 слов.
      Отчёт прогона: `test-results/wiki-overlap-addressed-en.md`.

      **Найдено копий: 4.** И вот что делает эту находку важной:

      | Запись | Ряд был | Правка партии 1 в каталоге? |
      |---|---|---|
      | `foyle-s-war-ox93icyl` | 14 слов | ✅ да |
      | `tales-from-the-crypt-7qghn3ft` | 12 слов | да (форма изменена) |
      | `the-love-boat-bvkc6kcp` | 12 слов | ✅ да |
      | `ugly-americans-zgji6eoe` | 12 слов | ✅ да |

      🔴 **ТРИ ИЗ ЧЕТЫРЁХ УЖЕ БЫЛИ «ПОЧИНЕНЫ» ПАРТИЕЙ 1 И ВСЁ ЕЩЁ ЗАИМСТВУЮТ** (замер: 31 правка
      из 34 лежит в каталоге). Это ровно то, о чём предупреждала врезка выше — «моя правка лечила
      только ВЕРШИНУ, заимствование выходит за пределы предложения с маркером», — но теперь это
      не предупреждение, а измеренный факт. У «Фойла» прежняя правка добавила зачин
      «Two people work at his side:» и оставила ЗА НИМ дословную формулу статьи целиком.
      🔑 **Отсюда правило для остальных партий: «партия переписана» НЕ ЗНАЧИТ «партия чиста».
      Приёмка партии — повторный замер по всем её записям, а не по тем, что не трогали.**

- [x] ✅ **Четыре ряда сняты — измерением, а не мнением.** Правки:
      `tools/en-rewrites-NEW-batch1-residual.json` (по трём правилам ночи: правится весь накрытый
      кусок · кавычки не переписываются · совпадение в кавычках не заимствование).
      Сухой прогон писаря: форма 4/4, «до» совпало со снимком 4/4.
      Ворота `gate-rewrites`: **годных 4 · брака 0 · не проверено 0 · «остаётся копией» 0.**
      Остаточный ряд по каждой, тем же прибором и тем же порогом (10 слов):

      | Запись | ряд был → стал |
      |---|---|
      | `foyle-s-war-ox93icyl` | 14 → **9** |
      | `tales-from-the-crypt-7qghn3ft` | 12 → **7** |
      | `the-love-boat-bvkc6kcp` | 12 → **4** |
      | `ugly-americans-zgji6eoe` | 12 → **6** |

      ⚠️ Честно: у «Фойла» 9 при пороге 10 — запас в одно слово. Ряд там состоит из имён актёров
      с ролями, которые переставить можно, а переименовать нельзя; дальнейшее снижение потребовало
      бы выкинуть факт. Названо, чтобы следующий прогон не принял 9 за случайность.

      🔴 **ЗАПИСЬ В БОЕВОЙ КАТАЛОГ НЕ ВЫПОЛНЕНА — она за Менеджером.** `--apply` пишет в боевую
      базу (`tools/rewrite-catalog-descriptions.mjs:361`, контур `prod`), а боевая база по
      манифесту команды — территория Менеджера. Всё готово к одной команде.

- [x] 🐞 **Попутно найден и починен дефект прибора** — `bugs/NEW_addressnyy_progon_zatiraet_svod`:
      адресная сверка молча затирала `researches/34_en_wikipedia_overlap.md` (680 строк и 533
      найденные копии → 34 записи и 4 копии), потому что путь отчёта не зависел от охвата прогона.
      Свод восстановлен из git, прибор разведён по охватам, починка доказана сверкой хеша.
      Там же закрыты две дыры: живой контроль дороги не гонялся в режиме `--slug` вовсе, а
      названный-но-не-найденный адрес молча выпадал из вердикта партии.

- [ ] 🔴 Журнал правок «что было → что стало» — строкой на каждую правку, страницей вычитки
      владельцу ПОСТФАКТУМ (условие №025 В6 = А). Без журнала шаг не считается сделанным.
      🟡 Журнал этой партии написан сухим прогоном:
      `homeworks/catalog_descriptions_journal_en-rewrites-NEW-batch1-residual.md`. Страницу
      вычитки владельцу поднимает Менеджер.
- **Проверка:** повторный прогон `tools/grep-en-boilerplate.mjs` — число записей с маркерами
      УБЫВАЕТ до нуля или до объяснённого остатка; остаток объяснён поимённо.
- ⚠️ Капкан: маркер ≠ заимствование (так сказано в самом списке). Правим стиль, а не наказываем
      совпадение.

### Шаг 4 — Страж инвариантов расширить + мутация

- [x] ✅ **ЗАКРЫТО 2026-08-21** (`dev-2`). Расширить `tools/verify-dimension-pages.mjs`: блок-дверь
      несёт NDSR полным именем там, где есть оценки, и честную строку там, где их нет; три
      состояния шкалы различимы.
- [x] **Мутация с адресатами, названными ДО прогона** (канон: страж без мутации не доказан).
- **Проверка:** числа падений вписаны сюда; после отката — ноль.

> **ЧТО ДОБАВЛЕНО В СТРАЖА** (6 проверок, итог вырос 35 → **40**; блок ПЕРЕНАЦЕЛЕН 2026-08-22
> вслед за правкой владельца — стерёг три текста двери, стережёт РЯД ЗВЁЗД): число золотых равно
> округлённой оценке · пустых позиций нет вовсе · серой звезды нет там, где оценка выше нуля ·
> золотой звезды нет там, где оценка ровно ноль · ряды с золотыми реально наблюдались ·
> 🔴 ОДНА СЕРАЯ звезда реально наблюдалась.
>
> 🔑 **Механика поимённого добора выборки ПЕРЕЖИЛА смену предмета — по прямому вердикту QA.**
> Текст отменили, а защита от вакуумной проверки осталась и просто пошла за новым предметом:
> объектов с нулевой средней при живых голосах по-прежнему два из 5121, и в обычную выборку они
> по-прежнему не попадают. Это и есть проверка, живущая дольше своего повода.
>
> 🔑 **ГЛАВНОЕ В ЭТОМ БЛОКЕ — НЕ ПРОВЕРКА, А ВЫБОРКА.** Объектов состояния «все поставили ноль»
> в каталоге ДВА из 5121, и в обычную выборку «каждая N-я страница» они не попадают никогда:
> проверка была бы вакуумной, а «0 нарушений» и «0 наблюдений» печатаются одинаково
> (`EXP-0092`). Поэтому их слаги ВЫЧИСЛЯЮТСЯ ИЗ ДАННЫХ и добираются в выборку принудительно,
> а число наблюдений печатается по каждому состоянию. Слаги в страж НЕ вшиты: контроль,
> привязанный к записи, умирает вместе с ней (`EXP-0163`) — поставят «Квадробике» пятёрку,
> и страж пойдёт за состоянием сам.
>
> **ТРИ МУТАЦИИ, АДРЕСАТЫ НАЗВАНЫ ДО ПРОГОНА, ПОПАЛИ ВСЕ ТРИ:**
>
> | Мутация | Куда | Ждал | Получил |
> |---|---|---|---|
> | **M1** «состояний два»: `ratingState` всегда возвращает `rated` | правило → ловят юниты | падение 2 тестов из 15 | **2 из 15** («средняя РОВНО ноль», «битая средняя»), 13 зелёных |
> | **M2** «дверь врёт состоянием»: ветка `all-zero` рисует текст `doorVoted` | разметка → ловит страж | 4 + 4 нарушения (2 объекта × 2 языка) | **4 и 4**, итог 38/2 |
> | **M3** «прибор не добирает редкое»: снят поимённый добор выборки | сам страж | «наблюдений 0» | **наблюдений 0**, итог 39/1 |
>
> **Мутации ПОСЛЕ правки владельца (2026-08-22), адресаты названы до прогона:**
>
> | Мутация | Куда | Ждал | Получил |
> |---|---|---|---|
> | **M5** «ноль рисуется как ничто»: `starRow(0)` → `{gold: 0, grey: 0}` | правило → юниты | падение 2 тестов | **4 из 17** — предсказание было УЖЕ факта: инвариант «ряд никогда не пуст» стерегут ещё три соседних теста, включая добавленный по вердикту QA |
> | **M6** «вернуть десять позиций с пустыми хвостами» | разметка → страж | «пустых позиций нет» краснеет, счёт золотых врёт на нулевых | **146 пустых и 4 нарушения счёта**, итог 38/2 |
> | **M7** «прибор не добирает редкое» (повтор на новом предмете) | сам страж | «наблюдений 0» | **наблюдений 0**, итог 39/1 |
>
> ⚠️ Промах предсказания M5 назван вслух намеренно: мутация попала ШИРЕ, чем я обещал, и это не
> победа стража, а неточность моей модели собственных тестов. Записано, чтобы следующая сессия
> читала числа, а не мою уверенность.
>
> 🔴 **Почему M1 бьёт по юнитам, а не по стражу, и это НЕ слабость стража.** Страж берёт
> состояние у ПРОДУКТА (`import { ratingState }`), чтобы не держать вторую копию правила, —
> значит мутацию самого правила он по построению не увидит: он ошибётся вместе с продуктом.
> Ровно поэтому правило стережётся юнитами, а страж стережёт РАЗМЕТКУ. Мутация, поставленная не
> на тот слой, доказала бы не то.
>
> **После отката всех трёх — 40 прошло, 0 провалов.**

### Шаг 5 — Приёмка выборкой и весом

- [ ] 10 страниц (разные виды × три среза оценённости) по чеклисту «полезна без клика»: отвечает
      ли страница на вопрос человека, не открывая продукт. Результат — числом.
- [ ] Замер веса: клиентский бандл < 3 МБ, крупнейший чанк узла < 500 КБ (`EXP-0136`).
- [ ] Живой Chrome: обе темы × 390/1440, кадры смотреть ГЛАЗАМИ.
- [ ] Попутно закрыть `bugs/121` в части каталога, если правки затронут достижимость.

### Шаг 6 — Сдача

- [ ] `/fable-judge` по утверждениям фазы, вердикт — сюда.
- [ ] Строка в `STATUS.md` · веха в `plans/40` · тег `DONE` в имени этого файла.
- [ ] ⚠️ **Выкат — обычным порядком через стейдж** (`npm run deploy -- --stage` → проверка →
      `npm run deploy`). Формулировку «ждёт AUTH выката» НЕ писать (интервью №034, В1 = В).
      🔴 Предусловие: ворота Smoke требуют живого стенда, а он умирает (`bugs/134`) — поднимать с
      `NODE_OPTIONS=--max-old-space-size=6144`.

## Риски

| Риск | Уровень | План |
|---|---|---|
| Переработка текста ухудшит уже накопленный вес страниц | средний | Правим смысл и полноту, НЕ трогаем заголовки и адреса; вердикт фазы 8 сравнит покрытие по датам |
| Агент выдумает факт об объекте ради «полезности» | высокий | Шаг 1 несёт STOP-ASK; выдуманное число/факт — дефект по определению |
| EN-правка уедет в бой без вычитки владельца | средний | Условие №025 В6 = А исполняется журналом; журнал — обязательный признак «готово» шага 3 |
| Работа растянется и потеряет нить | средний | Шаги мелкие, у каждого своя проверка; STATUS обновляется по закрытии каждого |
