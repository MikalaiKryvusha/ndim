# План NEW — операционный: внедрение PostHog Cloud EU («Зрение», фаза 3)

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16.
<!-- ВЕРА-КОНЕЦ -->

> **Создан:** 2026-08-28 (смена 7, роль `dev-2`) · **Родитель:** `plans/74` фаза 3; решение
> владельца — интервью **№054, В1 = А** (PostHog Cloud EU) по разведке `researches/55` ·
> **Статус:** 🟢 РАЗВИЛКА ВИТРИНЫ РЕШЕНА владельцем (интервью №056, 2026-08-28 18:08) —
> «го» Менеджера дано, ждёт домашки владельца (ключи PostHog) ·
> **Исходящее:** домашка `homeworks/16` (аккаунт PostHog EU — теперь нужна точно) · правка
> «Политики» (`researches/06`) через вычитку; выкат — дверью Менеджера
>
> ## 🔴 РЕШЕНИЕ ВЛАДЕЛЬЦА — интервью №056, В1 (страницей, 2026-08-28 18:08), дословно:
> «*Удалить нахуй весь этот текст. Он не моим голосом написан и со мной не согласован. Он
> не соответствует моей стилометрии. Аналитику PostHog ставим, смотрим, как я документировал
> аналитические ивенты в старом NDim Space 1.x, так же локально ведём документацию, чтобы её
> можно было отслеживать*»
>
> Разнос решения по трём частям:
> 1. **Блок витрины «Ни одной рекламы и ни одного чужого счётчика» УДАЛЯЕТСЯ ЦЕЛИКОМ**
>    (заголовок + абзац, оба языка, `landing-copy.ts:220–226`) — не переписывается. Никакой
>    новой строки на его месте: новый текст мог бы быть только его голосом, а его слова нет.
>    Отличий на витрине становится три. Развилка «плечо А/Б» этим снята: обещание, спорившее
>    с трекером, перестаёт существовать. Правка витрины едет ВЫКАТОМ-СОСТАВОМ смены 8.
>    📜 Провенанс снятого блока — `plans/29` §2.4 (прошёл вычитку страницей 2026-08-15 в
>    составе плана; слово владельца 2026-08-28 — «не согласован»). Урок процесса: вычитка
>    страницы ПЛАНА не делает согласованной каждую строку ЛИЦА продукта.
> 2. **PostHog ставим** — лестница Ш1+ открыта; живая установка ждёт ключей (домашка 16).
> 3. 🆕 **Документация аналитических ивентов — по образцу владельца из 1.x, локально и
>    отслеживаемо (в git).** Первый шаг — разведка `ndim-old`: КАК он документировал ивенты
>    (формат, место, состав записи) → `researches/NEW_` → наша документация ивентов ведётся
>    тем же образом. Белый список событий из этого плана становится частью той документации.

Низкоуровневый план исполнителя. Разведка не дублируется: числа, лимиты и доводы выбора — в
`researches/55`, здесь только исполнение.

## Цель (вектор)

ИИ-агент получает читающий доступ к поведению людей и собирает по нему продуктовые гипотезы;
владелец изредка заглядывает глазами. Ни одна оценка по измерению наружу не уходит; лендинг
остаётся лёгким; каталог не получает трекер НА ПОКАЗЕ, а оценка с двери считается; лицо
продукта не врёт ни одной строкой.

## ⛔ Блокеры — без них не начинать

1. **Домашка `homeworks/16_posthog_account_eu`** — аккаунт заводит владелец, регион ЕС
   необратим. Без трёх ключей шаги Ш2+ неисполнимы.
2. **«Го» Менеджера по этому плану.**

## 🔴 ГЛАВНАЯ НАХОДКА ПЛАНИРОВАНИЯ: витрина ломается ТРИЖДЫ, и третий раз — не про счёт

Строка витрины (`landing-copy.ts:223`), дословно:

> «*На этой странице и внутри Пространства нет ни одной рекламы и **ни одного чужого счётчика**.
> Мы считаем **ровно четыре числа** в сутки — сколько человек зашло, сколько подвигало звёзды
> здесь, сколько вошло гостем, сколько создало аккаунт, — и в этих числах **нет ни имени, ни
> почты, ни адреса, ни устройства**.*»

| # | Что ломается | Почему |
|---|---|---|
| 1 | «ни одного чужого счётчика» | PostHog — ровно чужой счётчик |
| 2 | «ровно четыре числа» | **уже неправда с Ф1**: их шесть, и перечисление в строке не сходится |
| 3 | 🔴 «нет ни имени, ни почты, ни адреса, ни устройства» | **это обещание ПРИВАТНОСТИ, а не счёт** |

**Третий пункт — самый дорогой, и его надо назвать владельцу прямо.** Наша воронка правда не
знает о человеке ничего: там шесть чисел в документе на сутки. Любой продуктовый инструмент устроен
иначе — он по определению различает посетителей (иначе не построит воронку и удержание), то есть
заводит идентификатор устройства, а по умолчанию пишет ещё и IP.

Что мы можем сделать настройкой: **не собирать IP** (`disable_ip_capture`) и **выключить
автозахват** — это Ш3. Чего сделать нельзя: убрать идентификатор посетителя, не убив сам смысл
инструмента.

**Отсюда честная развилка для владельца, и решает её он, а не агент:** либо строка переписывается
(перестаёт обещать «ни устройства»), либо инструмент не ставится. Промежуточного пути здесь нет —
любая формулировка, оставляющая обещание в силе при живом PostHog, будет враньём на лице продукта,
а `GOAL.md` против этого и написан.

📌 Строку правит владелец вычиткой; агент готовит вариант и цену, а не свершившийся факт.

## Границы — что этот план НЕ делает

- **Каталог не получает трекер НА ПОКАЗЕ; оценка с двери считается.** 10 222 страницы стоят
  на `csr = false` и при простом просмотре карточки трекера не грузят (разбор —
  `researches/55` §2.2). Но дверь оценки — отдельная сборка, и SDK въезжает в неё: человек,
  ОЦЕНИВШИЙ объект с карточки, в аналитику попадает.
  🔴 Формула исправлена 2026-08-29 по вердикту QA №14: прежняя редакция читалась как «каталог
  не получает трекер» без оговорки, и её опровергла собственная сборка (+261 КБ в `build/door/`;
  на показе — ноль script-тегов, замер QA). Границу привели К ПРАВДЕ, загрузку не откладывали —
  решение Менеджера. Довод: Ш8 повезёт владельцу тексты Политики и витрины, а выкатывать их
  поверх границы, которую опровергает своя же сборка, нельзя.
  Сколько людей открыло карточку — по-прежнему отвечает Search Console.
- **Свою воронку не трогаем и не заменяем.** Она независима от чужого сервиса и служит эталоном
  сверки на накрутку (`researches/55` §8).
- **Session replay не включаем** — на наших экранах живут оценки.
- **Мониторинг ошибок — фаза 4**, отдельная работа, хотя у того же вендора.
- **Баннер согласия** — правовой вопрос, планом не решается; вынесен владельцу отдельной строкой.

## Шаги

- [x] **Ш1. Ключи в `.env` по канону.** `PH_PROJECT_KEY` (публичный, уезжает в код),
      `PH_HOST` (`https://eu.i.posthog.com`), `PH_PERSONAL_KEY` (ЧИТАЮЩИЙ, в код не попадает
      НИКОГДА). `.env.example` пополняется именами БЕЗ значений.
      **Проверка:** `npm run kaif:check` зелёный; греп по сборке не находит `phx_`.
- [x] **Ш2. SDK только там, где живёт наш JS.** Лендинг, приложение, страницы «Тест».
      Загрузка — динамическим импортом, как у своей воронки (`EXP-0028`): SDK уезжает отдельным
      чанком и в главный бандл не попадает.
      **Проверка:** вес `build/` до и после; греп чанков лендинга на `posthog`; на ПОКАЗЕ
      карточки каталога трекер не грузится (счёт script-тегов = 0).
      ⚠️ Критерий «чанки карточки каталога обязаны остаться прежними ПОБАЙТНО» снят
      2026-08-29 как НЕВЕРНЫЙ, а не как невыполненный: сборка двери оценки — часть пути с
      карточки, и SDK въезжает в неё по построению (+261 КБ в `build/door/`, замер QA,
      вердикт №14). Критерий требовал того, чего решение не обещало.
- [x] **Ш3. 🔴 Настройки приватности — и они ДОКАЗЫВАЮТСЯ, а не объявляются.**
      `autocapture: false` (иначе полетят тексты кнопок и подписи — а на экранах живут названия
      измерений) · `disable_session_recording: true` · `disable_ip_capture` · `persistence`
      без cookies (`localStorage` или `memory` — выбрать по цене для воронки).
      **Проверка — юнит на КОНФИГ:** тест читает объект настроек и падает, если `autocapture`
      включён или запись сессий не выключена. Инвариант №002 В4 держится машиной, а не памятью.
- [x] **Ш4. Список событий — белый, а не «что придёт».** Явные события повторяют шаги своей
      воронки плюс то, чего у неё нет (путь внутри приложения). ⛔ Имя события говорит о МЕСТЕ
      пути и НИКОГДА о предмете оценки: «оценил» допустимо, «оценил измерение X» — запрет.
      **Проверка:** юнит на список имён — новое имя, содержащее идентификатор измерения, роняет
      прогон. Тот же приём, что пара «шаги ↔ правила» у своей воронки.
      ✅ **Доведён до конца двумя движениями:** места вызова — `10faf21` (2026-08-29 10:12);
      пара «список ↔ места вызова» в обе стороны — четвёртой парой в `verify-probe-mark.mjs`
      (2026-08-29 вечером, Интегратор). Разбор обоих — в разделе «Что осталось» ниже.
      🔑 Приём тот же, что обещала строка выше, и теперь он исполнен буквально: список перестал
      быть списком и стал ПАРОЙ, обязанной совпадать.
- [ ] **Ш5. Правка «Политики» — кандидат владельцу.** Источник — `researches/06`, не `docs.ts`.
      Замер объёма сделан при планировании: действующий текст (п. 2.2) уже разрешает передачу
      данных третьим лицам «по усмотрению автора», но **третью сторону НЕ НАЗЫВАЕТ** — значит
      правка не переписывает Политику, а ДОБАВЛЯЕТ named-абзац: кто, где хранит (ЕС, Франкфурт),
      что собирается и чего не собирается.
      **Проверка:** страница вычитки поднята, ответ владельца получен и разнесён.
- [ ] **Ш6. Три строки витрины — вычитка владельца.** Готовится вариант текста с ценой каждого
      решения (см. развилку выше). ⛔ Агент текст лица не меняет сам.
- [x] **Ш7. MCP-доступ агенту.** `mcp-eu.posthog.com`, личный ключ только на чтение.
      ✅ **ИСПОЛНЕН, и вот ЧЕМ подтверждён** (отмечено Интегратором 2026-08-29): доступ прожит
      делом, а не проверен намерением — через MCP сняты живые настройки проекта **260193**, и
      снятые числа стоят в этом же документе двумя местами: таблица эскалации
      (`session_recording_opt_in`, `capture_console_log_opt_in`, `heatmaps_opt_in`,
      `capture_dead_clicks`, `anonymize_ips`, `cookieless_server_hash_mode: 0`,
      `ingested_event: false`) и поправка исполнения 2 (`anonymize_ips: true` как настоящий
      механизм вместо несуществующей опции). Прочитать чужой проект и вернуть его состояние
      поимённо нельзя, не имея читающего доступа.
      ⚠️ **Чего этим НЕ подтверждено, названо честно:** проверка наблюдением, как она записана
      ниже, — «спросить о ВЧЕРАШНЕМ ДНЕ и сверить с рядом своей воронки» — **невыполнима до
      выката**: `ingested_event: false`, ряда нет. Отметка закрывает ДОСТУП, а не сверку рядов;
      сверка остаётся первым делом после первого боевого дня.
      Прежняя формулировка проверки: агент задаёт инструменту вопрос о вчерашнем дне и получает
      число, сходящееся со своей воронкой по порядку величины. Расхождение — не поломка, а
      материал: это и есть эталонная сверка, ради которой своя воронка остаётся.
- [ ] **Ш8. Сдача.** Оба набора юнитов, типы, `check`, `guards`, стенд. Выкат — дверью Менеджера,
      ВМЕСТЕ с правками витрины и Политики: строка, ставшая неправдой, не имеет права пережить
      свой выкат даже на день.

## 🔴 ПОПРАВКИ ИСПОЛНЕНИЯ — что план назвал неверно (dev-1, 2026-08-29)

Записано здесь, а не молча исправлено в шагах: план — документ, и следующий читатель обязан
увидеть, ЧТО было не так, а не только исправленный текст.

1. **Имена ключей.** В Ш1 стоят `PH_PROJECT_KEY` / `PH_HOST` / `PH_PERSONAL_KEY`. Действующие
   имена дал ВЛАДЕЛЕЦ: `POSTHOG_PROJECT_TOKEN` · `POSTHOG_PROJECT_ID` · `POSTHOG_REGION` ·
   `POSTHOG_NDIM_SPACE_PERSONAL_API_KEY` (последнее — 🔴 не переименовывать). Исполнено по ним.
   Публичный ключ уехал в код константой (плечо А, прецедент веб-конфига Firebase).
2. 🔴 **Опции `disable_ip_capture` НЕ СУЩЕСТВУЕТ** в `posthog-js` (проверено по типам пакета
   1.422.5, `@posthog/types` → `PostHogConfig`). Похожая по имени `ip` существует и помечена
   дословно «*@deprecated — THIS OPTION HAS NO EFFECT*». Опция, которой нет, молча не делает
   ничего — конфиг выглядел бы защитой приватности и ею не был; прогон бы этого не поймал.
   **IP отбрасывается НАСТРОЙКОЙ ПРОЕКТА, и она уже включена:** `anonymize_ips: true` (снято
   через MCP, проект 260193, 2026-08-29). Урок класса — в `EXPERIENCE.md`
   (`EXP-NEW-имя-опции-вендора-из-плана`).
3. **`persistence` без cookies выбрана как `sessionStorage`.** Соблазнительный
   `cookieless_mode: 'always'` НЕ взят: в типах пакета сказано, что без включения режима в
   настройках проекта **все события молча игнорируются**, а на проекте он выключен
   (`cookieless_server_hash_mode: 0`). Это кандидат в улучшение, но парой «настройка + код».
4. **Замер веса Ш2 сделан, число названо:** главный бандл (entry, 10 723 Б) SDK не содержит;
   PostHog лежит отдельным чанком **259 340 Б сырых / 84 987 gzip / 72 278 brotli**; карточка
   каталога НА ПОКАЗЕ клиентского JS не грузит (0 script-тегов). Во всей сборке один
   `__sveltekit_rfjf7h`.
   🔴 **Поправка 2026-08-29 (вердикт QA №14): моё «0 чанков» было замером НЕ ТОГО.** Я считал
   чанки страницы каталога и не посмотрел в `build/door/` — сборка двери оценки, в которую SDK
   въезжает (+261 КБ). На показе он по-прежнему не грузится (QA пересчитала script-теги: ноль),
   так что вывод «показ лёгкий» устоял, а вот граница «каталог не получает трекер» — нет.
   Класс тот же, что `EXP-0226`: число снято в одном контуре, а предъявлено как про весь путь.
   ⚠️ **Развилка, вытекающая из числа:** SDK в 8 раз тяжелее главного бандла и грузится сразу
   после монтирования лендинга (там срабатывает `landing_view`). Путь отхода, названный в
   «Рисках», — грузить после первого касания — имеет свою цену: тогда `landing_view` перестанет
   считать ровно тех, кто ушёл не коснувшись, то есть самых нужных для воронки. Решать по
   замеру Lighthouse после выката, а не сейчас.

## 🔴 ЭСКАЛАЦИЯ МЕНЕДЖЕРУ — инвариант №002 В4 стоит на ОДНОЙ ноге

Снято через MCP 2026-08-29 с проекта 260193. Наш конфиг глушит это на клиенте; настройки
проекта — не зона роли, поэтому строки идут Менеджеру, а не правятся молча:

| Настройка проекта | Сейчас | Почему это важно |
|---|---|---|
| `session_recording_opt_in` | 🔴 `true` | Запись сессии разрешена НА ПРОЕКТЕ. Кадр нашего экрана — предмет оценки целиком. Любой клиент, поднявшийся без нашего конфига, начнёт писать |
| `capture_console_log_opt_in` | 🔴 `true` | Консоль уезжает вместе с записью |
| `heatmaps_opt_in` | 🔴 `true` | Координаты касания на ленте суть указание на карточку измерения |
| `capture_dead_clicks` | 🔴 `true` | Тот же довод |
| `anonymize_ips` | ✅ `true` | IP не собирается — настоящий механизм вместо несуществующей опции |
| `autocapture_exceptions_opt_in` | ✅ `true` | Мониторинг ошибок — решение №049 В3 = А, оставить |
| `cookieless_server_hash_mode` | `0` | Выключен; см. поправку 3 |
| `ingested_event` | `false` | Ни одного события ещё не приходило — ряд начнётся с выката |

**Рекомендация:** выключить первые четыре и на стороне проекта. Тогда инвариант держат ОБЕ
ноги, а не одна наша.

### 🔵 ОТВЕТ ВЛАДЕЛЬЦА НА ЭТУ ЭСКАЛАЦИЮ — встречный вопрос, четыре опции ОСТАЮТСЯ ВКЛЮЧЁННЫМИ

Эскалация дошла до владельца страницей. **Интервью №059, В3 — ответ = В**, дословно:
«*может это полезные и ценные фичи? Почему мы ими не пользуемся? Что оно нам полезного может
дать?*»

**Что это меняет в плане, чтобы следующий исполнитель не принял рекомендацию за решение:**

1. **Рекомендация выше НЕ утверждена.** Четыре настройки проекта (`session_recording_opt_in` ·
   `capture_console_log_opt_in` · `heatmaps_opt_in` · `capture_dead_clicks`) сегодня
   **включены на стороне проекта и остаются включёнными**. Таблица выше описывает текущее
   состояние верно; её последняя строка — предложение агента, а не исполненное решение.
2. **Инвариант №002 В4 продолжает стоять на одной ноге — на нашем коде.** Клиентский конфиг
   глушит все четыре, это покрыто юнитом; живой замер Менеджера (2026-08-29) — записей сессий
   в проекте НЕТ НИ ОДНОЙ. То есть сегодня инвариант держится и фактом, но одной невнимательной
   правкой конфига его можно снять — это и есть невыплаченная цена ожидания.
3. **Разбор пользы и цены каждой из четырёх возможностей подан отдельно — интервью №062, В2**
   (что каждая даёт, чего стоит и почему мы их не звали, по инварианту №002 В4). Ответа на
   него ещё нет.
4. 🔴 **Настройки аккаунта владельца агент не меняет сам** — это его боевой ресурс. Ни до, ни
   после ответа: исполняет он либо агент по его прямому слову.

## Что осталось после Ш1–Ш4 — поимённо, чтобы не искать

> 🔴 **ПОПРАВКА 2026-08-29 20:5x +03:00 (Интегратор): ПЕРВАЯ СТРОКА ЭТОГО РАЗДЕЛА ПЕРЕЖИЛА СВОЁ
> ИСПОЛНЕНИЕ НА ДЕСЯТЬ ЧАСОВ, и оставлена дословно ниже, потому что она — улика класса.**
>
> Она гласила: «*Четыре новых события ждут своих мест вызова*». На момент чтения (20:30) все
> четыре стояли на своих местах уже с **10:12** того же дня — коммит `10faf21`
> «feat(plans/78): четыре новых события встали на свои места пути», предок `main`:
>
> | событие | место вызова | довод места |
> |---|---|---|
> | `rating_saved` | `src/lib/data/profile.ts:299` | СЛОЙ ДАННЫХ: в `saveRating` приходят ОБЕ дороги — лента «Измерения» и дверь карточки каталога через `saveTestRating`. Инструментовка на экране покрыла бы одну и молча потеряла вторую |
> | `profile_filled` | `src/lib/data/profile.ts:396` | тот же довод |
> | `person_opened` | `src/routes/relations/+page.svelte:76` | раскрытие карточки похожего: показ топа и внимание к конкретному человеку — два разных факта |
> | `relations_view` + `has_matches` | `src/routes/relations/+page.svelte:228` | `onMount` экрана «Связи», ЧИСЛО ЦЕННОСТИ |
>
> **Почему строка выжила и чем это лечится.** Страж `verify-status-outlived` судит ПЛАНЫ по их
> статусу и коммитам — раздел «что осталось» ВНУТРИ плана он не видит. Дыра названа поимённо и
> имеет адрес; стража под неё сегодня не заводим (замер ширины впереди — тот же довод, что в
> `bugs/NEW_utverzhdenie_o_pare_perezhivaet_svoyu_pravdu_bez_strazha.md`).
> 🔑 Класс общий и уже стоил времени четырежды за день: **цитируется СВОДКА вместо предмета.**
> Сводка внутри плана стареет ровно так же, как строка рельса в `STATUS.md`, и права быть
> постановкой не имеет — постановка проверяется командой по предмету.

~~**Четыре новых события ждут своих мест вызова:** `rating_saved` (сохранение оценки),
`relations_view` + свойство `has_matches` (экран «Связи» — это ЧИСЛО ЦЕННОСТИ),
`person_opened` (раскрытие карточки похожего), `profile_filled`.~~ ✅ **ИСПОЛНЕНО `10faf21`**
(разбор — во врезке выше). Белый список стоял под них с Ш4; шесть шагов воронки уезжают в
PostHog сами через `track()`.

- ✅ **ПАРА «БЕЛЫЙ СПИСОК ↔ МЕСТА ВЫЗОВА» ЗАВЕДЕНА 2026-08-29 (Интегратор), и до неё её не было
  ни в одну сторону.** Замер, из которого это следует: `analytics.test.ts` читал `analytics.ts`,
  `site.ts`, `.firebaserc` и `firebase.ts` — и ни разу файлы мест вызова. Значит молча проходили
  ОБА разъезда, и оба дают один и тот же вечный ноль: имя, выброшенное из экрана правкой
  вёрстки (список продолжает обещать событие), и имя, дописанное в список и никем не позванное.
  `capture()` глотает по контракту — ни один не кричит сам.
  **Где живёт:** четвёртой парой ВНУТРИ `tools/verify-probe-mark.mjs`, а не отдельным прибором —
  там уже стоят три пары тихого нуля, и разложить один класс по двум файлам значило бы завести
  пару, обязанную совпадать.
  🔑 **Пара сверяет ИМЯ события со списком и НИКОГДА место вызова с экраном** — иначе первая же
  правка вёрстки дала бы ложный красный, а места выбраны в слое данных намеренно (см. таблицу).
  **Третье условие пары:** пересылка `capture(step)` внутри `track()` — её исчезновение не
  убирает ни одного литерала и потому невидимо для обеих сторон сверки.
  **Чем доказана — четыре живые мутации на дереве, не синтетика:** снят настоящий вызов
  `capture('person_opened')` → 1 адресный красный · имя переименовано в незнакомое → **2**
  красных с обеих сторон и с названным файлом · снята пересылка `capture(step)` → 1 красный при
  нетронутых литералах · 🔑 снят настоящий `track('door_click')` при ОСТАВЛЕННОМ упоминании в
  комментарии (в файле их осталось четыре) → красный, потому что комментарии снимаются до
  поиска; без этого некролог прикрывал бы удалённый вызов, и страж был бы зелёным.
  ⚠️ **Собственная мутация нашла дефект признака:** имя с цифрой (`profile_filled_v2`) он не
  видел вовсе — половина пары была слепа на целом классе имён. Признак расширен, случай стоит в
  самотесте. Самотест **21 → 34**.
- ✅ **Юниты на события — добавлены два, и оба закрывают то, чего не закрывал ни один прежний.**
  Главный: `has_matches: false` ДОЕЗЖАЕТ. Событие заведено ради вопроса «сработал ли продукт», и
  интереснее в нём тот, кому НЕ показали никого; прежний набор проверял только `true`. Правка
  отсева свойств с `value === undefined` на соблазнительное `if (!value)` прошла бы ВСЕ 30
  прежних случаев, а `false` молча исчезал бы — ряд показывал бы одни успехи. Доказано мутацией:
  она роняет **ровно эти два случая и ни одного больше**. `npm test` 447 → **449**.
- **Побайтная сверка чанков каталога с прежней сборкой** не делалась (нужны две сборки, до и
  после). Замер «карточка не несёт JS вовсе» её по смыслу перекрывает, но формально это
  проверка Ш2, и она честно помечена невыполненной.
- **Живой приём событий не наблюдался** — `ingested_event: false`, бой ещё не выкачен.

## Риски, названные заранее

- **Блокировщики режут трекер.** Часть людей его не увидит — числа PostHog будут НИЖЕ своих.
  Это не дефект, это свойство; и это ещё один довод оставить свою воронку эталоном.
- **Googlebot исполняет JS** и попадёт в события лендинга ровно так же, как попадает в свою
  воронку (`bugs/202`). Дешёвой чистой защиты нет; лечится чтением рядов, а не фильтром по
  User-Agent на клиенте.
- **Вес SDK.** У PostHog есть slim-сборка, но она экспериментальная (`researches/55`). Если
  замер Ш2 покажет, что лендинг просел, — путь отхода: грузить SDK ПОСЛЕ первого взаимодействия,
  а не при загрузке. Решать по замеру, а не заранее.
- **Идентификатор посетителя** — см. развилку витрины. Это не риск исполнения, а решение
  владельца, и без него Ш6 не закрывается.

## Решения, принятые без владельца

Ведётся по ходу. На сегодня одно: **список событий белый, а не автозахват** — иначе инвариант
№002 В4 держался бы на дисциплине, а не на построении.
