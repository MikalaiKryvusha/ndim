# План 67 — Фаза 5 эпика 23: МОСТ (интерактив, который сам вносит человека внутрь)

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16.
<!-- ВЕРА-КОНЕЦ -->

> **Создан:** 2026-08-21 · **Родитель:** `plans/23` фаза 5 · **Композиция:** утверждена владельцем
> (синтез №043 В1, `plans/65`) — НЕ переоткрывается · **Исполнитель:** dev-1 (команда
> NDIM_WORKTREE_DEV_TEAM, ветка `ndim_dev1`) · **Постановщик:** Менеджер ·
> **Статус:** 🟡 разведка кода закрыта, код НЕ начат (парковка на перезагрузку машины 2026-08-21).
> **Следующий шаг — Ш1** (снятие строки 138); постановка Менеджера в силе, новой не требуется.
>
> ✅ **Решения Н1, Н3, Н4 ОДОБРЕНЫ Менеджером 2026-08-21** (сообщением, после парковки):
> правку устаревших ссылок «строка 115» в `plans/23`/`STATUS.md` делаем при закрытии фазы ·
> вычисляемая вторая половина фразы мостика принята КАК РАБОЧАЯ, персонажей не упоминаем,
> строка живёт с плашкой «текст на вычитку» и **до ответа владельца НЕ канон** — в очередь
> вычитки её ставит Менеджер страницей, когда фаза дойдёт до сдачи · дверь (а)
> `location.replace` принята вместе с обоснованием Н4.
>
> Это СОБСТВЕННЫЙ низкоуровневый опер-план разработчика (манифест команды: «очень низкоуровневые
> операционные планы разработчики пишут себе сами»). Уровень фичи — в `plans/23`.

## Что строим (композиция владельца, дословно)

Основа **А4 «Мостик демонстрации»** с пояснением-карточкой на «Связях» + кнопка
[Смотреть больше] с **пульсацией-волнами** (А2) + **итог-панель с барами похожести НАД блоком
с кнопкой на лендинге** (половина А3 — на «Связях» панель НЕ повторяется).
Макеты: `design/handhold-mockups.html`, варианты `a2`, `a3`, `a4`.

## 🔴 Разведка кода — находки, меняющие постановку

### Н1. Строка `+page.svelte:115` ПЕРЕЕХАЛА

Лендинг больше не живёт в корне: `plans/39` шаг 2 развёл его на `/ru` и `/en`. Корневой
`src/routes/+page.svelte` (84 строки) — теперь только распознаватель языка.

**Настоящий адрес долга:** `src/routes/[lang=lang]/+page.svelte:138`

```
if (['localhost', '127.0.0.1'].includes(location.hostname)) demoUrl = '/profile?guest=1';
```

Объявление `let demoUrl = $state(APP_URL)` — строка 62, комментарий-долг — строки 47–61,
передача в демо — строка 264 (`<SimilarityDemo {lang} appUrl={demoUrl} />`).
Ссылки «строка 115» в `plans/23` и `STATUS.md` устарели — при закрытии фазы поправить.

### Н2. Куда ведёт мост — «первый экран продукта» это `/profile`, а не «Связи»

`src/routes/profile/+page.svelte:2` — «Экран «Профиль» (NDim ID) — **первый экран продукта 2.0**».
Ворота фазы говорят про «Назад» **с первого экрана продукта** — значит про `/profile`.

Дверь `/profile?guest=1` — единственное место, где рождается гостевая сессия
(`profile/+page.svelte:342–367`): уважает живую сессию (`bugs/95`), переиспользует анонима,
заводит документ (`ensureSpaceExists`), бьёт воронку `guest_start` только для НОВОГО гостя,
затем вычищает параметр через `replaceUrl` (`bugs/125`). Второй такой двери не строим — KISS.

**Решение:** кнопка ведёт в `/profile?guest=1`. Панель `AFTER` в макетах (шапка «Связи») —
условность макета, показывающая «человек внутри»; обязывающая часть синтеза — пояснение-
карточка НА «СВЯЗЯХ», а не мгновенная посадка на «Связи».

### Н3. 🔴 Синтетиков нет — и текст мостика из макета частично ЛОЖЕН

Макет `a4` несёт: «**Это была демонстрация** — вот Ваши настоящие связи. Алиса, Макс и Настя
останутся рядом, пока не появится первая настоящая; убрать их можно крестиком.»

Второе предложение описывает Р6/Р10 (синтетики Макс/Алиса/Настя в БД) — **их в базе НЕТ**
(подтверждено постановкой Менеджера). Более того: демо-измерения («Люблю тишину»,
«Путешествия», «Спорт», «Книги», «Чувство юмора» — `SimilarityDemo.svelte:26–32`) вымышлены
и в настоящем каталоге отсутствуют (греп по `src/` даёт только тест видимости), поэтому
перенести оценки из демо в гостевой профиль нельзя — сопоставлять не с чем. Свежий гость
приходит **с нулём измерений**.

Следствие: у свежего гостя «Связи» пусты, и «вот Ваши настоящие связи» показывало бы на пустоту.

**Решение (вычисляемое, а не сценарное):** карточка-мостик достраивает вторую половину фразы
по факту наличия карточек:
· список не пуст → «Это была демонстрация — вот Ваши настоящие связи.» (слова владельца);
· список пуст → «Это была демонстрация — здесь будут Ваши настоящие связи.»
Вторая формулировка — **НОВАЯ строка, идёт на вычитку владельцу** (через Менеджера), до ответа
живёт с пометкой «текст на вычитку». Предложение про Алису/Макса/Настю НЕ пишем вовсе: оно
описывает несуществующее поведение.

### Н4. Разбор ненамеренного отскока «Назад» — ВЫБОР И ОБОСНОВАНИЕ

Как сегодня: лендинг в `onMount` зовёт `hasSession()`; есть сессия (гость — тоже «внутри») →
`location.replace('/profile')` (`[lang=lang]/+page.svelte:118–130`). Гость, нажавший «Назад»
с `/profile`, попадает на лендинг и молча выталкивается обратно — отскок.

`plans/23` даёт две двери:
- **(а) мост не оставляет записи истории** — переход через `location.replace()`;
- **(б) лендинг перестаёт выталкивать гостя.**

**Выбрана (а).** Обоснование: ворота этой же фазы требуют дословно «вошедший человек
по-прежнему не разглядывает лендинг (`bugs/08.1` не регрессировал)» — а (б) и есть регрессия
`bugs/08.1` (`bugs/10_DONE_landing_ignores_session`). Дверь (б) принадлежит эпику 21 (по В7
лендинг становится мини-приложением с FAQ и отзывами) — там она будет осознанным решением со
своими воротами, здесь — нарушением своих. Плюс (а) дешевле и честнее: запись лендинга в
истории гостю не нужна, «Назад» уносит его туда, откуда он на лендинг пришёл.

Цена (а), названная честно: человек, пришедший на лендинг прямой ссылкой в новой вкладке,
после моста «Назад» не имеет куда вернуться — история пуста. Это не отскок и не петля, а
обычный конец истории вкладки.

Реализация: клик по кнопке моста — `preventDefault()` + `location.replace(demoUrl)`. Разметка
остаётся ссылкой `<a href>` (средний клик, «открыть в новой вкладке», доступность, робот) —
перехватывается только обычный левый клик без модификаторов.

### Н5. Поп-ап обязан быть ВЫЧИСЛЯЕМЫМ

`plans/26` §1: хозяин поп-апа — `plans/23`, а `plans/21` даёт правило «поп-ап выводится из
расчёта, а не из сценария». В демо уже есть всё нужное: `sorted` (`SimilarityDemo.svelte:145`)
— связи, отсортированные по похожести настоящим ядром (`computeRelation`). Поп-ап берёт
`sorted[0]`: имя и процент из расчёта, а не из литерала «Алиса — 61 %».

## Состав работ (порядок исполнения)

- [ ] **Ш1.** Снять `[lang=lang]/+page.svelte:138` — `demoUrl` перестаёт зависеть от
      `location.hostname` и всегда равен `/profile?guest=1`. Комментарий-долг 47–61 переписать:
      долг закрыт, назвать чем и когда. `APP_URL` как дверь почтовых ссылок (строки 103–107)
      не трогаем.
- [ ] **Ш2.** Поп-ап над самым похожим персонажем — вычисляемый из `sorted[0]`, появляется
      после действия человека (первое `rate()`), а не по таймеру. Не перекрывает лицо
      (урок Ш3 `plans/65`).
- [ ] **Ш3.** Итог-панель с барами похожести — НАД блоком с кнопкой, из тех же `sorted`.
- [ ] **Ш4.** Кнопка [Смотреть больше] с волнами: появляется после действия человека;
      пульсация гаснет при `prefers-reduced-motion` (см. `$lib/ui/motion`).
- [ ] **Ш5.** Переход через `location.replace` (Н4).
- [ ] **Ш6.** Карточка-мостик на «Связях» (`src/routes/relations/+page.svelte`, рядом с
      `<GuestCard />`, строка 351) — вычисляемая по Н3, показывается пришедшему мостом.
- [ ] **Ш7.** Юниты на вычисляемость поп-апа/панели/карточки + `npm run check`, типы, линтер.
- [ ] **Ш8.** Живой Chrome: «Назад» с `/profile` — наблюдение против описания Н4; обе темы,
      390/1024/1440. 🔴 Стенд — только под замком доски.

## Ворота закрытия (из постановки Менеджера и `plans/23` дословно)

С лендинга человек попадает в анонимную сессию **одним нажатием и без слова «гость»** ·
вошедший не разглядывает лендинг (`bugs/08.1` не регрессировал) · поведение «Назад» описано
здесь и совпадает с наблюдением в живом Chrome · воронка `guest_start` не сломана · обе темы,
390/1024/1440 · юниты + линтер + типы зелёные · `/fable-judge`.

## Что НЕ трогаем

Тексты владельца и словарь продукта («Вы», никогда «навсегда», «Измерение» не «ось») ·
чужие ветки и файлы ролей · `deploy`, Docker, боевая база, push в `main`.

## Открытый вопрос к владельцу (через Менеджера)

Новая строка «Это была демонстрация — здесь будут Ваши настоящие связи.» (Н3, пустой список) —
на вычитку. До ответа живёт с пометкой «текст на вычитку».
