# План 65 — Фаза 4 эпика 23: два набора макетов, одно сидение владельца

> **Создан:** 2026-08-21 · **Родитель:** `plans/23` фаза 4 · **Статус:** 🔵 макеты построены,
> кадры сняты и смотрены, интервью №043 поднято владельцу · **Исходящее:** выбор владельца по
> наборам А и Б + вычитка двух рабочих текстов (ожидание «Связей» из `plans/64`, короткая
> просьба Б2/Б3).

**Якорь метаплана** (`plans/23` фаза 4): *«кнопка [Смотреть больше] появляется и мягко
пульсирует, светится и испускает волны»* (В3) и *«панелька, которая мягко пульсирует»* (В4).
Правило четырёх макетов — `AGENT_GUIDE` → «Дизайн». Оба набора показываются **вместе, одним
документом**, чтобы владелец потратил одно сидение.

## Что изменилось со времён метаплана — и вошло в макеты

1. **V4 «Игра сама вносит внутрь» уже выбран** (`plans/52`, В1 = Г): панель перехода после
   звёзд — утверждённая композиция. Набор А макетирует ДЕТАЛИ: форму зова и **первую секунду
   после нажатия** (то, чего V4 не показывал).
2. 🔴 **Постоянная карточка гостя уже стоит на всех экранах** (фаза 5 эпика 22, выбор владельца,
   в коде — `GuestCard.svelte`) и несёт тот же смысл, что панель В4. Сшивка `plans/26` §4
   требует считать ДОЗУ напоминаний суммой — поэтому набор Б несёт вариант Б1 «карточка гостя
   и есть просьба» (новой панели нет; рекомендация агента) и честного двойника Б4, чтобы выбор
   не был предрешён.
3. Р13 («оба») — во всех вариантах Б две кнопки: Почта и Google.

## Ось расхождения, названная ДО вёрстки

**Набор А — чем зовёт переход и что в первой секунде:** А1 тихий свет · А2 волны (дословно В3)
+ шторка · А3 итог-панель с барами, повторяющаяся на «Связях» · А4 мостик «Это была
демонстрация» (Р6/Р10, рекомендация метаплана).
**Набор Б — где живёт просьба:** Б1 пульс существующей карточки гостя · Б2 тонкая строка под
шапкой · Б3 маячок-пилюля (доступна, не показана) · Б4 панель-карточка (двойник карточки —
показан для честности выбора).

## Шаги

- [x] **Ш1.** `design/handhold-mockups.html` — 8 вариантов по дому стиля; тексты канона
  (поп-ап и «Смотреть больше» из V4/В3, карточка гостя дословно из продукта, 7 дней из схемы);
  единственные новые строки помечены плашкой «текст на вычитку».
- [x] **Ш2.** Кадры: `node tools/shoot-mockups.mjs design/handhold-mockups.html` — 32 кадра
  (8 × 2 темы × 390/1440), в панели набора отдельная кнопка 1024 для живого взгляда.
- [x] **Ш3.** Кадры смотрены РЯДОМ (урок `plans/11`), три дефекта пойманы ДО показа и починены:
  зов А1/А2 был неразличим статикой (волнам дорисованы статичные кольца, свету — цветная тень) ·
  поп-ап перекрывал кружок Алисы · в Б3 раскрытая панель наезжала на карточку гостя (состояния
  разнесены «до/после тапа»). Различия пар назвны: А1↔А2 — форма зова и шторка; А1↔А3 —
  итог-панель с барами и её повтор на «Связях»; А1↔А4 — карточка-мостик; Б попарно — носитель
  просьбы (карточка · строка · пилюля · панель).
- [x] **Ш4.** Интервью №043 — вопросы несут варианты внутри (`EXP-0161`); страница вычитки
  поднята, макеты открыты владельцу отдельной вкладкой.
- [ ] **Ш5.** Внести правки владельца, зафиксировать выбор, открыть фазу 5 (мост).
  ✅ **В1 РЕШЁН 2026-08-21 20:42 (интервью №043) — СИНТЕЗ, а не один вариант** (класс
  `MEMORY`: владелец выбирает не то, что называет): основа **А4 «Мостик демонстрации»
  с пояснением на странице связей** + **кнопка с пульсацией** (волны А2) + **итог-панель
  с прогресс-барами связей НАД блоком с кнопкой на лендинге — но НЕ на странице связей**
  (половина А3 без повтора). Это композиция моста для фазы 5.
  ✅ **В4 РЕШЁН с двумя правками владельца, разнесены в код:** хвост фразы карточки гостя —
  «останутся в Вашем личном NDim ID профиле Пространства NDim» (`GuestCard.svelte`);
  «Сохранить труд» → **«Сохранить результаты»**; текст ожидания «Связей» принят как есть —
  пометка ВРЕМЕННЫЙ снята (`relations/+page.svelte`).
  🔄 **В2 и В3 — «не понимаю проблему и вопрос»:** вопросы были написаны жаргоном
  («доза напоминаний») — нарушение правила 3 голоса владельца (понятность мерится по
  читающему строку впервые). Переспрошены простыми словами в том же интервью,
  страница поднята повторно.

## Ворота закрытия (из метаплана дословно)

Владелец выбрал по одному варианту в каждом наборе и сказал, что поправить.
