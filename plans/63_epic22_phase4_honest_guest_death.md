# План 63 — Эпик 22, фаза 4: честная смерть гостя

> **Создан:** 2026-08-21 · **Родитель:** `plans/22_guest_equals_person_epic.md` → фаза 4
> («Честная смерть гостя: срок, все следы, учётная запись, возвращение») ·
> **Статус:** 🔧 написан, не начат ·
> **Исходящее:** —

**Решения владельца, на которых план стоит (все получены, ничего не ждём):**
срок — **7 дней от рождения аккаунта** (№010 Р3: «*От даты создания анонимного аккаунта.
Создал — отсчёт пошёл*») · включение уборки — **не раньше 2026-10-01** (№042 В3: «*Начнём
удалять с октября*») · фото гостя показывается, значит уборка Storage обязательна (№042 В2) ·
заявки гостя без потолка, значит их осиротение — штатный случай уборки (№042 В5).

## Цель

**Достичь:** гость, не ставший человеком, умирает в названный владельцем срок ЦЕЛИКОМ — данные,
следы у других людей, фотография, учётная запись Auth — и вернувшийся в мёртвую сессию получает
объяснение, а не пустой продукт.
**Сохранить:** труд полноценных людей неприкосновенен (уважительная асимметрия, шапка
`cleanupStaleGuests`); 9 тестов `sync-server/deleted_people.test.mjs` зелёные.
**Избежать:** удаления хоть одного неанонимного аккаунта уборщиком; включения уборки раньше
октября.

## Критерии приёмки (сделано, когда)

1. Тест «гость истёк → ни одного следа из таблицы §4 метаплана» зелёный и **доказан мутацией
   с названными адресатами** (якорь ворот фазы 4).
2. Тест «за живым гостем и за полноценным человеком не убирают» зелёный (якорь ворот).
3. После уборки истёкшего: `admin.auth().getUser(uid)` бросает `user-not-found` — **учётной
   записи в Auth нет** (якорь: «Учётная запись Auth — удалять вместе с данными»).
4. Предохранитель календаря: прогон с датой «до 2026-10-01» не удаляет НИКОГО и пишет в лог
   причину; с датой «после» — удаляет; оба случая — тестом с мутацией.
5. Вернувшийся в пустоту гость видит состояние «сессия истекла» с предложением начать заново
   (якорь: «Клиент честно отвечает тому, кто вернулся в пустоту»); механика распознавания
   покрыта тестом, финальные текст и форма приедут из макетов фазы 5.
6. Две дыры §3 закрыты по построению: бессмертие вечно-`dirty` гостя и невидимость гостя без
   `lastSync` невозможны в новой схеме отбора (якорь: «Починить две прочитанные дыры»).

## Ход решения — отбор кандидатов через Auth, а не через `lastSync`

Сегодняшний отбор (`points.where('guest'==true)` + `lastSync`) меряет БЕЗДЕЙСТВИЕ и не видит
гостя без точки. Слово владельца — «от даты СОЗДАНИЯ». Дата создания живёт в Auth
(`metadata.creationTime`), и только там она правдива. Поэтому кандидатов даёт
**`admin.auth().listUsers`**: анонимный провайдер И `creationTime < now − 7 дней`. Это закрывает
обе дыры §3 (ни `dirty`, ни `lastSync` в отборе не участвуют), ловит гостя, не поставившего ни
одной оценки (данных нет, а учётная запись есть), и делает асимметрию проверяемой: апгрейд
меняет провайдер — бывший гость перестаёт быть кандидатом по построению. `listUsers` Firestore-
чтений не тратит (канон «экономить запросы»). PENDING: цена `listUsers` на бою — страниц мало
(сотни учёток), подтвердить числом в шаге 2.

## Шаги

- [x] **1. Календарный предохранитель.** ✅ 2026-08-21. Константа `GUEST_CLEANUP_START_MS =
  2026-10-01T00:00+03:00` (зона владельца, ручки окружения нет намеренно — как у
  `ESCORT_MAX_RUNS`); до неё `cleanupStaleGuests` возвращает 0 и пишет в лог «уборка гостей
  спит до 2026-10-01» (якорь: «уборка по сроку жизни не включается раньше 2026-10-01»). Спит
  ВСЯ уборка гостей по сроку, включая прежнюю 30-дневную, — по букве «начнём удалять с октября».
  Тест: `sync-server/guest_death.test.mjs` (3 проверки: обе стороны даты + точная граница
  `START − 1 мс` + строка лога) — **доказан 2 мутациями с названными до прогона адресатами**
  («предохранитель снят» → 3 провала · «дата уехала на год» → ровно 1, «после включения»).
  Постановка сроков в `guest_filter.test.mjs` переведена на подставное «сейчас» после включения
  (`AWAKE = 2026-10-02`) — иначе до октября тесты уборки красили бы исправный продукт.
  Полный набор: **96/96**.
- [x] **2. Новый отбор кандидатов** — ✅ код и тесты 2026-08-21, ⏳ хвост: замер цены на бою
  (якорь: «`GUEST_TTL_DAYS` → 7 по семантике из ответа В-Б»):
  `listUsers` (страницы 1000) → анонимный провайдер (`providerData` пуст) + возраст по
  `metadata.creationTime` > 7 дней → кандидат; `dirty`/`lastSync` в отборе не участвуют.
  `GUEST_TTL_DAYS = 7` с провенансом №010 Р3; комментарий о совпадении с 30-дневной чисткой
  Identity Platform переписан честно (совпадение разорвано сознательно).
  🆕 **Решение, принятое без владельца — СТРАХОВКА переходной дыры:** пока уборка спит до
  октября, внешняя 30-дневная авточистка убивает учётки — их guest-точки не видны `listUsers`
  никогда. Вторым проходом вычищаются guest-точки без живой учётки (список живых уже в руках,
  ноль лишних обращений; при ПУСТОМ списке учёток страховка молчит — пустой Auth это нездоровый
  источник, а не «все умерли»). Общее стирание следов вынесено в `eraseGuestTraces`.
  Тесты: describe 2 `guest_death.test.mjs` (7 проверок: истёкший вычищен целиком · молодой жив ·
  полноценный не кандидат ни при каком возрасте · вечно-`dirty` смертен · гость без единой
  оценки в кандидатах · страховка · честный счёт; граница шага зафиксирована тестом «учётка
  истёкшего пока ЖИВА — `deleteUser` принадлежит шагу 3») — **доказан 2 мутациями с названными
  до прогона адресатами** («отбор ослеп к провайдеру» → ровно 2 · «страховка снята» → ровно 2).
  `test:sync` теперь поднимает и Auth-эмулятор (`--only firestore,auth`); describe уборки
  «по 30 дням бездействия» из `guest_filter.test.mjs` уехал сюда — той семантики больше нет.
  ⚠️ Капкан эмулятора, снятый зондом: `importUsers` игнорирует `providerData` молча — неанониму
  провайдер рождает только `passwordHash`; `creationTime` импорт принимает честно.
  Полный набор: **99/99**.
  ⏳ **PENDING (не улажено, доделать):** цена `listUsers` на боевом числе учёток не замерена —
  ожидание «сотни учёток = 1 страница», подтвердить числом и вписать сюда.
- [ ] **3. Полный перечень следов** (якорь: «догоняет `cleanupDeletedPeople()` по перечню следов
  таблицы §4»): к сегодняшним точке/`relations`/`users`-дереву добрать — членства в чужих
  группах (`collectionGroup('members')`), подсказки аудитории у других
  (`collectionGroup('audience')`), обезличивание заявок, **дружбы `friendships/{pair}` обеих
  сторон**, **фотографию в Storage** (`users/{uid}/avatar/**`, Admin SDK бакета), и последним —
  **`deleteUser` в Auth**. Общие куски вынести так, чтобы `cleanupDeletedPeople` их
  переиспользовал, не меняя своего поведения.
- [ ] **4. Дружбы — и для ушедшего человека тоже** (якорь таблицы §4: «новая дыра ДЛЯ ОБОИХ
  уборщиков»): `cleanupDeletedPeople` учится удалять осиротевшие `friendships/{pair}`. Блок
  обоснования в коммите — почему меняются тесты соседа, если меняются.
- [ ] **5. Клиент: «гостевая сессия истекла»** (якорь: «Клиент честно отвечает…»): распознавание —
  сессия анонимна, `auth.currentUser.metadata.creationTime` старше TTL, документов нет →
  состояние «истёк» с предложением начать заново; НЕ путать со свежим гостем (создан минуты
  назад, документов ещё нет). Текст и форма — из макетов фазы 5; здесь — механика и временная
  прямая формулировка по словарю продукта.
- [ ] **6. Набор тестов фазы** — `sync-server/guest_death.test.mjs`: критерии 1–4 + мутации
  (снят предохранитель · потерян один след из перечня · отбор зацепил неанонима). Прогон
  `npm run test:sync` целиком: 77 старых + новые, 0 провалов.
- [ ] **7. Живая проба на стенде**: родить гостя зондом, подделать возраст (подставной `now`),
  прогнать полный проход, убедиться прибором — следов нет, Auth пуст, экран истёкшего объясняет.
  Кадры глазами.

## Проверка — чем наблюдается каждое утверждение

`sync-server/guest_death.test.mjs` (новый, шаг 6) — критерии 1–4, мутации там же ·
`sync-server/deleted_people.test.mjs` (существующий, 9 тестов + дружбы из шага 4) ·
живая проба шага 7 — журналом в этом плане · клиентское распознавание — юнит-тест рядом с
модулем сессии (шаг 5). Все артефакты рождаются ШАГАМИ этого плана, не «потом».

## Риски (по Мёрфи)

1. **Уборщик задел неанонима** — самый дорогой исход (труд живого человека). Реакция: фильтр по
   провайдеру — единственный источник правды; тест-инвариант «полноценный не кандидат ни при
   каком возрасте» + мутация; на живой пробе — контрольный полноценный рядом.
2. **Правка `cleanupDeletedPeople` (дружбы) ломает соседа.** Реакция: его 9 тестов гоняются на
   каждом шаге; поведение расширяется, не меняется; красный тест соседа = стоп и разбор.
3. **`listUsers` на бою дороже ожидания** (страницы, лимиты Admin SDK). Реакция: замер в шаге 2
   ДО опоры на механизм; дорого → фолбэк: возраст из Auth только для кандидатов, отобранных
   дешёвым запросом по `points` + отдельный проход по `users` без точек.

## Чего фаза НЕ делает (границы, чтобы не уползти)

Не снимает фильтр анонимов в чужих топах (фаза 7) · не трогает правила Firestore (фаза 6, ждёт
App Check) · не строит напоминания о сроке (эпик 23) и кнопку «Пожаловаться» (планируемая, вне
эпика) · не выкатывается в бой отдельно — уедет штатным выкатом эпика (фаза 8).
