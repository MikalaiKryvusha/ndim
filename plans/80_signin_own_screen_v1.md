# План 80 — Экран входа становится СВОИМ экраном: V1 «Колонна» в бой через стейдж

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16.
<!-- ВЕРА-КОНЕЦ -->

> **Создан:** 2026-09-04 · **Статус:** 🟢 МАКЕТ ВЫБРАН ВЛАДЕЛЬЦЕМ, работа разблокирована ·
> **Родитель:** `bugs/19` (открыт с волны 09) · **Цепочка решений:** №070 → №072 → №073 → №074 ·
> **Эталон вида:** `design/signin-screen-mockups.html`, вариант **V1 «Колонна»**

## Слово владельца — дословно, чат 2026-09-04

> «*берём в1. кнопку гостя делаем яркой синей, как ещё одна основная, слово "или" заметнее.
> делаем план и имплементацию, выкат в стейдж, тесты агентом*»

и раньше в тот же вечер:

> «*в кнопку входа анонимом добавь "гостем без регистрации"*»

Плюс его же решения цепочки, уже записанные: **№073 В1 = А** (тексты приняты как написаны) ·
**№073 В3 = Б** (боевые строки правятся ВМЕСТЕ с выбранным экраном, одной порцией — то есть
этим планом) · **№009 В3** (запрет слов «Попробовать» и «Осмотреться гостем» — сказан о ЛЕНДИНГЕ;
этот экран не лендинг, а слово «гостем» на кнопку вернул он сам сегодня).

## Цель (вектор)

**Боль:** «Войдите в Пространство» сегодня — карточка ВНУТРИ оболочки `/profile`: с табами,
рельсом и нижней панелью приложения, в которое человек ещё не вошёл. Переключателя темы там нет
вовсе. Это `bugs/19`, открытый с волны 09.

**Куда идём:** вход — собственный экран в стиле лендинга, без оболочки приложения, с глобальными
переключателями языка и темы. Три двери на нём равновесны по доступности и разновесны по смыслу:
Google и почта — регистрация, гость — «посмотреть без регистрации», и гостевая дверь по слову
владельца такая же яркая, как основная.

## Критерии приёмки — сценарной формой (`REQUIREMENTS_FRAMEWORK.md`)

### К1. Вход перестал быть карточкой внутри приложения

- **Ситуация.** Человек не вошёл, приложение открыто на любом экране, требующем аккаунта.
- **Действие.** Человек попадает на экран входа.
- **Результат.** На экране НЕТ нижней панели приложения, НЕТ бокового рельса и НЕТ табов
  профиля; в верхнем правом углу есть переключатель языка и переключатель темы.
- **Проверка.** `node tools/verify-signin-screen.mjs` печатает `нижняя панель: нет · рельс: нет ·
  табы: нет · переключатели: 2` и выходит с кодом 0.

### К2. Гостевая дверь — вторая основная, а не тихая рамка

- **Ситуация.** Экран входа открыт в светлой теме на ширине 390.
- **Действие.** Прибор снимает вычисленный стиль кнопки гостя и кнопки Google.
- **Результат.** У обеих кнопок одинаковый цвет фона, и он не прозрачный.
- **Проверка.** тот же прибор печатает `гость и Google: фон совпадает` и выходит с кодом 0.

### К3. Слово «или» читается как развилка, а не как шов

- **Ситуация.** Экран входа открыт.
- **Действие.** Прибор снимает вычисленный стиль разделителя.
- **Результат.** Кегль не меньше 14 пикселей, начертание жирное, цвет — основной текстовый,
  а не приглушённый.
- **Проверка.** тот же прибор печатает `или: 14px+ / 700 / не приглушён`.

### К4. Текст кнопки гостя — ровно тот, что продиктовал владелец

- **Ситуация.** Экран входа открыт на русском.
- **Действие.** Прибор читает надпись на гостевой кнопке.
- **Результат.** Надпись равна строке «Смотреть Пространство NDim Space гостем без регистрации».
- **Проверка.** прибор сравнивает побайтово и краснеет на любом расхождении.

### К5. Экран живёт на стейдже, а не только на машине

- **Ситуация.** Работа собрана и выкачена дверью `npm run deploy -- --stage`.
- **Действие.** Агент открывает стейдж-адрес и проходит экран входа.
- **Результат.** Экран отдаётся, три двери на месте, консоль без ошибок.
- **Проверка.** смоук двери выката зелёный + кадры экрана в обеих темах на двух ширинах.

## Шаги

- [ ] **Ш1. Новый маршрут экрана входа.** Отдельная страница вне оболочки приложения, в стиле
      лендинга. Решить и записать адрес (кандидат: `/[lang]/signin`), не ломая существующие ссылки.
- [ ] **Ш2. Перенос текстов.** Заголовок, лид и три двери — из V1 макета, с текстами, которые
      владелец уже утвердил (№073 В1 = А) и кнопкой гостя по его сегодняшнему слову.
- [ ] **Ш3. Вид: гостевая дверь второй основной + «ИЛИ» заметнее.** Ровно как в макете V1 после
      правки 2026-09-04.
- [ ] **Ш4. Глобальные переключатели языка и темы** на экране — то, чего у карточки не было вовсе
      (это половина `bugs/19`).
- [ ] **Ш5. Переезд входов.** Все места, что вели в карточку внутри `/profile`, ведут на новый
      экран; возврат после входа — туда, откуда пришли.
- [ ] **Ш6. Страж `tools/verify-signin-screen.mjs`** — исполняет К1–К4, доказывается КРАСНЫМ на
      сломанной версии до того, как ему поверят (`TESTING_FRAMEWORK.md`, ворота 5), и получает
      блок `@guard` с угрозой и зазором.
- [ ] **Ш7. Ворота проекта зелёные:** типы, юниты, `test:tools`, `npm run guards` не хуже, чем до
      работы.
- [ ] **Ш8. Выкат в стейдж** дверью `npm run deploy -- --stage` и смоук под сессией.
- [ ] **Ш9. Приёмка агентом на стейдже:** обе темы, две ширины, кадры, консоль.

## Риски, названные заранее

1. **`bugs/19` шире этого плана.** В нём же сказано про ОБ-флоу целиком. Этот план закрывает
   экран и его вид; онбординговый поток за ним — отдельная работа. Граница названа, чтобы
   `bugs/19` не был закрыт наполовину молча.
2. **Возврат после входа.** Самое хрупкое место переезда: человек, пришедший с карточки каталога
   или из глубины приложения, обязан вернуться туда же. Это отдельная проверка, а не «должно
   работать».
3. **Смоук стейджа уже падал** (`bugs/196`, `bugs/197`). Если дверь выката упадёт не на нашей
   работе, а на этих известных дефектах — это НЕ повод объявить выкат зелёным; повод сказать
   владельцу, на чём именно.

## Решения, принятые без владельца

Ведётся по ходу.
