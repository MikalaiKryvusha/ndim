# План 88 — ЭПИК: аналитика сервера синхронизации, которая помогает расти

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16.
<!-- ВЕРА-КОНЕЦ -->

> **Создан:** 2026-09-12 · **Родитель:** заказ владельца 2026-09-12 · `MASTER_PLAN.md` фаза 4
> (риск роста) и фаза 7 («Зрение», `plans/74`) · **Разведка:** `researches/68` ·
> **Статус:** 🔵 МЕТАПЛАН НАПИСАН, фаза 0 (разведка) закрыта; фаза 1 ждёт решений владельца ·
> **Вовне:** 🔴 ДА — события уходят на серверы PostHog; правка «Политики» вероятна.

> 🔴 **ЗАКАЗ ВЛАДЕЛЬЦА, ДОСЛОВНО:** «*было бы хорошо, чтобы сервер синхронизации имел бы свой
> профиль пользователя в ПостХог и тоже аналитику присылал бы в аналитику с припиской у каждого
> ивента `[server]`*» · «*аналитику сервера — оформляй эпиком, с разведкой, подумай, что важно в
> аналитику добавить, что поможет долгосрочным целям NDim Space — превращение в гигансткую
> систему с огромным трафиком*».

## Зачем этот эпик существует

Сегодня сервер синхронизации **невидим для аналитики**. Клиентская воронка обрывается на
`rating_saved` и возобновляется на `relations_view` — а между ними лежит ВСЯ работа сервера, то
есть **то единственное, ради чего продукт существует: человеку нашли человека**. Мы не знаем ни
сколько это заняло, ни получилось ли вообще.

Разведка (`researches/68`) нашла три вещи, которые превращают заказ из «отправлять события» в
работу уровня эпика:

1. **Главное число сети — не число людей, а ЛИКВИДНОСТЬ** (доля нашедших и скорость находки), и
   она физически измерима ТОЛЬКО на сервере: связи считает он один.
2. **Заказ владельца и документация PostHog расходятся** по профилю сервера — расхождение
   разрешимо, но требует решения, а не умолчания (фаза 1, развилка Р1).
3. **Риск роста из `MASTER_PLAN.md` фазы 4 становится наблюдаемой кривой.** Пересчёт
   квадратичен, живёт на домашнем ПК; кривую «время прохода ↔ число людей» надо иметь ДО того
   дня, когда придёт трафик, а не в этот день.

## Вектор цели эпика

- **Achieve:** у проекта появляется ответ на три вопроса, которых сегодня нет ни у кого:
  **скольким людям мы помогли**, **скольким НЕ помогли**, и **когда мы упрёмся в потолок**.
- **Maintain:** предмет оценки наружу не уходит (№002 В4) · отправка не задерживает цикл и не
  роняет сервер при обрыве связи · имена серверных событий отличаются от клиентских (приписка
  `[server]`, требование владельца и одновременно требование документации против двойного счёта).
- **Avoid:** превращение аналитики в дорогой и плохой лог · метрики тщеславия (DAU/MAU, время в
  приложении) — они противоречат `GOAL.md` · новый канал деанонимизации (`bugs/113`).

## Фазы

### Фаза 0 — Разведка ✅ ЗАКРЫТА 2026-09-12
`researches/68`: метрики ликвидности отрасли · документация PostHog о профилях и цене ·
локальная опись того, что сервер уже знает и что уже меряет клиент · раздел «худшие решения,
не берём, поимённо».

### Фаза 1 — Решения владельца и ОДНО событие на живом контуре 🔵 СЛЕДУЮЩАЯ
Три развилки уровня владельца (ниже) + **самое дешёвое и самое ценное событие, доведённое до
конца**: `[server] started` — какой код в бою. Сегодня это знает только git замороженной копии.
Выбрано первым не по важности для роста, а потому что оно **доказывает весь тракт** (ключ,
отправка, приписка, профиль, устойчивость к обрыву) на событии, которое не несёт ни одного
человеческого данного. Ошибиться им безопасно.

**Ворота фазы:** событие видно в PostHog · тракт не задерживает цикл (замер) · обрыв связи не
роняет сервер (мутация) · предмет оценки не уходит (проверено перехватом тела).

### Фаза 2 — ЛИКВИДНОСТЬ 🔲 главная ценность эпика
`relations_computed` · `first_relations` · **`relations_empty`** · `escort_finished`.

🔑 `relations_empty` — самое ценное событие эпика: **скольким людям мы не смогли помочь.** Это
наш «supply gap», и он прямо говорит, каких измерений не хватает каталогу. Сегодня этот вопрос
не задаётся вовсе.
⛔ Предусловие: проверка на канал `bugs/113` — отдельным шагом, не оговоркой.

### Фаза 3 — ПОТОЛОК 🔲
`cycle_finished` · `full_pass_finished` · `quota_state` с длительностями и числом операций.
**Результат фазы — кривая «время прохода ↔ число людей»**, а не набор событий. Закрывает долг
`MASTER_PLAN.md` фазы 4: «нужен замер, при каком числе людей полный проход перестаёт
укладываться» (стоит там незакрытым с 2026-08-01).

### Фаза 4 — ЗДОРОВЬЕ 🔲
`cycle_failed` · `cleanup_erased` (🔴 удаления аккаунтов видны в аналитике, а не только в логе).
⚠️ Граница: НЕ замена мониторингу ошибок (`plans/74` Ф4) и НЕ замена суточной сводке
`tools/sync-server-health.mjs`.

### Фаза 5 — ЭКРАН И ПРИЁМКА 🔲
Ряды сведены в экран Панели администратора рядом с воронкой (`ideas/06`); суточное наблюдение;
сверка «что показывает аналитика» против «что показывает лог».

## 🔴 Три развилки — их решает ВЛАДЕЛЕЦ, не агент

Выносятся страницей интервью. Каждая с предложением агента внутри.

**Р1. Профиль сервера: как именно.** Владелец просит серверу свой профиль; документация PostHog
предостерегает против общего «system»-профиля — события с профилем личности до 4 раз дороже, и
на «огромном трафике» это счёт.
*Предложение агента:* событие **про человека** идёт под UID человека (склеится с его путём на
клиенте), событие **про систему** — под профилем сервера, но с `$process_person_profile: false`.
Профиль сервера существует и виден, дорогой обработки личности не плодит.

**Р2. Один проект PostHog или отдельный для сервера.**
*За общий:* события сервера и человека сходятся в одной воронке — а без этого не измерить
«оценил → нашли» сквозняком. *За отдельный:* шум инфраструктуры не мешает продуктовым рядам.
*Предложение агента:* общий, а разделение — свойствами события.

**Р3. Тексты продукта.** Витрина и «Политика» говорят, что уходит наружу. Появление серверных
событий — повод их перечитать.
*Предложение агента:* перечитать перед фазой 2 (там появляются события про людей), а не перед
фазой 1 (там их нет).

## Риски

1. 🔐 **Новый канал деанонимизации** — родня `bugs/113`. Событие «связи пересчитаны» не должно
   давать привязать оценку к личности. Проверка — шаг фазы 2, не оговорка.
2. **Отправка задерживает цикл или роняет сервер при обрыве.** Сервер живёт на домашнем ПК.
   Отправка асинхронная, потеря события допустима, падение отправки не роняет цикл — и это
   доказывается мутацией, а не обещается.
3. **Цена на росте.** Правило «системные события без профиля личности» закладывается в фазе 1
   именно ради этого, пока переделывать дёшево.
4. ⚠️ **Соблазн заменить аналитикой лог.** Аналитика отвечает на продуктовые вопросы, лог и
   сводка — на инженерные. Смешение даёт дорогой и плохой лог.
