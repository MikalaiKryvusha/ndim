# План 57 — Автопересборка для краулеров: узкая дверь только для данных

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16. **Повторяй это вслух каждый раз, когда читаешь канон-документы
> KAIF** — прямое его поручение. Разбор повода — в шапке `AGENT_GUIDE.md`.
<!-- ВЕРА-КОНЕЦ -->

> **Что это:** операционный план одной работы. · **Родитель:** `researches/39` (разведка, решение
> владельца закрыто там же). · **Заказ владельца дословно:** «*если хоть одно изменение было —
> пересобираем, заливаем во время дневной синхронизации сервером синхронизации, но после того, как
> он выполнил расчёты по синхронизации. Сервер синхронизации должен уметь собирать и публиковать
> образ нашего приложения*». · **Статус:** написан 2026-08-17, НЕ начат.

## Зачем это нужно — одной строкой

Сайт статический: оценки и рейтинги вмораживаются в HTML **на сборке**. Пока никто не пересобирает,
роботы месяцами видят числа дня последнего выката — а `npm run deploy` **вообще не обновляет снимок
каталога** (`fetch-dims-slice` в двери нет, `dims-build.json` вне git). То есть сегодня свежие данные
не доезжают до поиска ВООБЩЕ, даже при ручном выкате.

## 🔴🔴 РАСШИРЕНИЕ ЗАКАЗА 2026-08-17 — ЦЕПОЧКА ОДОБРЕНИЯ, И ОНА ЗАДАЁТ ФОРМУ ПЛАНА

Слова владельца, дословно, оба сообщения:

> «*Я в продакшене буду одобрять кандидатов. Нужно, чтобы сервер синхронизации умел увидеть diff по
> dims_list и засинкать dims_list до нового состояния, и автоматически при билде по этим новым
> измерениям собрать страницы в сборку, чтобы с новыми страницами билд сервер синхронизации
> публиковал*».
>
> «*Контур должен автоматически работать, где на ключевых точках я только вычитываю измерения*».

**Это не новая работа поверх плана, а его ГЛАВНЫЙ смысл, названный целиком.** Отсюда принцип
проектирования, которым план обязан мериться на каждом шаге: **автоматично всё, кроме вычитки.**
Любой шаг, который в норме требует руки владельца помимо суждения об измерении, — дефект плана.

### Цепочка целиком: что где происходит и что из этого УЖЕ работает

| # | Звено | Состояние на 2026-08-17 |
|---|---|---|
| 1 | Агент кладёт кандидата в `dim_candidates` | ✅ **работает** — прибор `tools/seed-dim-candidates.mjs`, очередь в комнате |
| 2 | 🔑 **Владелец вычитывает и одобряет** — ЕДИНСТВЕННАЯ его точка | ✅ **работает** — вкладка «Очередь», кнопки одобрить/правка/отклонить |
| 3 | Одобрение рождает `dims/{id}` тем же путём, что ручная форма | ✅ **работает**, страж `verify-admin-dims` п.9д–п.9з |
| 4 | Сервер синхронизации видит расхождение и **доливает `dims_list` ДЕЛЬТОЙ** | ✅ **уже устроено именно так** — `ensureDimsIndex`: сверка `built.docs` с числом документов, дочитываются ТОЛЬКО новые. Закреплено тестом `sync-server/dims_index.test.mjs` (набор 78) |
| 5 | Снимок каталога для сборки пересняли | 🔴 **ЕДИНСТВЕННОЕ НЕЗАКРЫТОЕ ЗВЕНО.** `npm run deploy` снимок НЕ обновляет; `dims-build.json` вне git. Без этого шага измерение в бою есть, индекс о нём знает, а СТРАНИЦЫ у него нет |
| 6 | Сборка делает страницы по новым измерениям | ✅ работает по построению: страницы каталога строятся из снимка, значит свежий снимок = свежие страницы. Проверено сегодня: лог сборки «10222 страниц · источник: полный каталог» |
| 7 | Сервер синхронизации публикует сборку | 🔴 не начато — шаги плана ниже |

🔑 **Вывод, который экономит работу:** звено 4 переизобретать НЕ НАДО, оно есть и доказано. План
закрывает звенья **5 и 7**, и звено 5 — одна строка `node tools/fetch-dims-slice.mjs --all` перед
сборкой внутри узкой двери. Именно поэтому узкая дверь и называется «только для данных».

⚠️ **И одна честная цена, которую владелец обязан знать заранее:** пересъём снимка стоит **5111
чтений** Firestore при бесплатной квоте 50 000 в сутки, то есть **10 % суточной квоты на одну
пересборку**. При суточной частоте это в пределах; при попытке пересобирать чаще (например, после
каждого одобрения) квота станет узким местом раньше, чем диск или трафик. Отсюда правило: пересборка
собирает НАКОПЛЕННОЕ за сутки, а не гонится за каждым одобрением.

### 🔴 ЖИЗНЕННЫЙ ЦИКЛ ПРАВКИ — третье требование владельца, 2026-08-17

Дословно: «*и если вдруг я редактировал измерение в продакшене, оно должно получить статус
отредактированного, и такой статус висит до тех пор, пока сервер синхронизации не соберёт очередную
сборку, это отредактированное измерение возьмёт в работу, и после публикации очередного ночного
билда — обновит статус, что оно в бою*».

**Требование по существу правильное и закрывает настоящую дыру:** сайт статический, поэтому между
«я поправил описание» и «люди и роботы это видят» лежит целая пересборка. Сегодня эта разница
человеку НЕ видна ничем, и он не может отличить сделанное от доехавшего.

#### 📐 Поправка к ФОРМЕ, и вот её основание

Хранить состояние словом («отредактировано» / «в бою») — значит завести флаг, который обязан
совпадать с реальностью и потому однажды с ней разойдётся: упавшая публикация, ручной выкат,
пересборка без снимка — и флаг врёт, причём молча. Это ровно тот класс, за который проект уже
платил (реестр пар «истина ↔ зеркало» в `AGENT_GUIDE`).

**Предлагаю две МЕТКИ ВРЕМЕНИ вместо флага:**

| Метка | Кто пишет | Когда |
|---|---|---|
| `time.updated` | панель (комната «Измерения») | при каждой правке и при одобрении кандидата |
| `time.published` | **сервер синхронизации** | после УСПЕШНОЙ публикации сборки |

Статус тогда не хранится, а **выводится**: `updated > published` → «отредактировано, ждёт сборки»;
иначе → «в бою». Разойтись с правдой такое сравнение не может по построению, а на экране владелец
видит ровно то, что он и просил, — пилюлю «ждёт сборки» до ночного билда и её исчезновение после.

#### 💰 Цена, посчитанная заранее, потому что она решает устройство

Помечать после каждой сборки ВЕСЬ каталог — это **5111 записей в сутки** при бесплатной квоте
**20 000 записей**, то есть четверть суточной квоты на одну пересборку. Поэтому помечается **только
то, что было отредактировано** (обычно единицы записей), а «в бою» — это состояние по умолчанию для
всего, чего никто не трогал: нет метки `updated` — нечего и ждать.

#### 🔴 ЛОВУШКА, которую обязан учесть исполнитель

Пересборка длится минуты, и владелец может править ВО ВРЕМЯ неё. Значит `time.published`
выставляется по значению `updated`, **снятому вместе со снимком каталога**, а не по «сейчас»:
иначе правка, сделанная после снимка и до публикации, будет помечена опубликованной, не попав в
сборку, — и человек увидит «в бою» то, чего в бою нет. Правило: **помечаем ровно те записи, чьё
`updated` не новее снимка, по которому собрали.**

⏭ Работа НЕ начата: это шаг плана, а не сделанное. Требует правки комнаты (писать `time.updated`),
сервера синхронизации (писать `time.published` после публикации) и правил Firestore (границы полей
`time`, сейчас белый список полей документа их не знает — граница фазы, см. `plans/44` шаг 1).

## Вектор цели

Раз в сутки, внутри дневного цикла полной синхронизации и **после** её расчётов, сайт
пересобирается со свежим снимком каталога и публикуется — **если изменилось хоть что-то**. Хранится
10 версий. Настоящая дверь выката (`npm run deploy`) при этом не ослабляется ни на шаг.

## 🔴 Узел, ради которого этот план вообще нужен

Автомат **не может** пройти три шага боевой двери: ворота Smoke требуют живого стенда, смоук под
сессией требует человека с логином, замок стейджа требует нового коммита. Коммит же при
автопересборке НЕ меняется — меняются только ДАННЫЕ.

🔑 **Различение, на котором стоит весь план:** обычный выкат везёт **новый КОД**, автопересборка
везёт **те же самые исходники с новым снимком данных**. Это разные риски. Мерить их одной дверью —
значит либо остановить автоматизацию совсем, либо ослабить дверь для настоящего кода.

**Отсюда форма ответа: ВТОРАЯ, УЗКАЯ дверь, которая физически не умеет везти код.**

## Критерии приёмки

- [ ] **К1. Дверь данных отказывает при любом изменении кода.** Если хоть один файл `src/`,
      `static/`, конфигов сборки или `package-lock.json` отличается от коммита, на который выписана
      расписка боевой двери, — дверь данных останавливается ненулевым кодом. Доказано мутацией:
      правка одного байта в `src/` роняет дверь.
- [ ] **К2. Публикуется только `hosting`.** Правила Firestore, индексы и функции дверь данных не
      катает НИКОГДА — у неё нет для этого ни повода, ни проверок.
- [ ] **К3. Пустых выкатов нет.** Дрейф 0 → выката нет (`tools/measure-catalog-drift.mjs`,
      `REBUILD_THRESHOLD = 0`). Доказано: прогон на двух одинаковых снимках не публикует ничего.
- [ ] **К4. Не чаще раза в сутки.** Второй запуск в те же сутки отказывает, даже если дрейф есть.
- [ ] **К5. Целостность сборки проверена** — во всей сборке один `__sveltekit_<хеш>` (`bugs/124`).
- [ ] **К6. Читающие стражи прогнаны по СОБРАННОМУ сайту** до публикации: `verify-dimension-pages`,
      `verify-catalog-hubs`, `verify-test-pages`. Им не нужен ни стенд, ни сессия.
- [ ] **К7. После публикации прогнаны читающие боевые пробы:** `verify-prod-dimension-pages`,
      `verify-prod-lang-redirects`, `verify-prod-cache`. Красная проба — громкий отчёт, а не тишина.
- [ ] **К8. Версий в хостинге не больше 10** — уборка выполняется той же дверью.
- [ ] **К9. Расчёт связей не страдает.** Сборка идёт ПОСЛЕ расчётов; её падение не роняет
      синхронизацию и не откладывает следующий цикл.
- [ ] **К10. Документ боевого запуска обновлён** (`plans/04_cutover.md`): как именно публикуется
      сайт теперь, каким ключом и с какой машины.

## Границы — чего этот план НЕ делает

- ⛔ **Не ослабляет `npm run deploy`.** Ни один его шаг не снимается и не смягчается.
- ⛔ **Не катает код.** Никогда, ни при каких флагах. Изменился код — работает человек и обычная дверь.
- ⛔ **Не трогает правила Firestore и индексы.**
- ⛔ **Не заменяет смоук под сессией.** Он остаётся обязательным для выката кода.

## Шаги

### Шаг 1 — Прибор различения: «изменился ли КОД»

- [ ] `tools/verify-code-unchanged.mjs`: сравнивает рабочее дерево с коммитом расписки
      (`.kaif/stage-receipt.json`) по путям, влияющим на сборку. Список путей — ЯВНЫЙ и записан
      в самом приборе с объяснением, почему каждый попал.
- [ ] 🔴 Снимок каталога (`src/lib/content/dims-build.json`) из сравнения ИСКЛЮЧАЕТСЯ поимённо и с
      причиной: он и есть то, ради чего дверь существует. Исключение объявляется одной строкой,
      как `ИМЯ-ОК` у стража бренд-имени.
- [ ] **Проверка:** мутация — правка одного байта в `src/app.html` роняет прибор; правка снимка
      каталога его НЕ роняет.

### Шаг 2 — Дверь данных `tools/deploy-data.mjs`

- [ ] Порядок шагов, и каждый останавливает дверь ненулевым кодом:
      1. код не менялся (шаг 1) · 2. свежий снимок каталога (`fetch-dims-slice --all`, проверить
      строку «источник: полный каталог») · 3. дрейф > 0 · 4. не чаще раза в сутки (отметка на диске)
      · 5. ЧИСТАЯ сборка · 6. целостность `__sveltekit` · 7. читающие стражи по сборке · 8.
      публикация `--only hosting` · 9. читающие боевые пробы · 10. уборка версий до 10.
- [ ] `--dry-run` по умолчанию — как у писаря описаний: публикует только явный `--apply`.
- [ ] `--selftest` без сети: проверяет порядок шагов и то, что каждый умеет остановить дверь.
- [ ] ⚠️ Хук `deploy-guard` обязан пропускать эту дверь ЯВНО по имени — и продолжать блокировать
      голый `firebase deploy`. Правка хука входит в шаг.

### Шаг 3 — Расписание на серверном ПК

> ✅ **РАЗВИЛКА ЗАКРЫТА — интервью №037, В1 = А**, с уточнением владельца дословно:
> «*Полагаю, можно сделать один контейнер NDim, внутри него разные образы, один из образов —
> сборщик сайта*».
>
> 🔑 **Как это читать инженерно.** Контейнер не содержит образов — образ порождает контейнер;
> владелец описывает ОДНУ СБОРКУ NDim из нескольких образов, где сборщик сайта — отдельный образ.
> Это и есть вариант А в исполнении: **отдельный сервис в общей связке `docker compose`**, на той
> же машине, в том же суточном цикле, со своим деревом зависимостей. Падение сборщика не роняет
> расчёт связей, потому что это разные контейнеры одной связки.

- [ ] Отдельный образ-сборщик в общей связке NDim (`docker compose`), рядом с сервером
      синхронизации и на той же машине.
- [ ] Запуск — по сигналу окончания расчётов, а не по часам: сервер синхронизации пишет отметку
      «полный проход завершён», задача её ждёт.
- [ ] **Проверка:** сутки живого наблюдения — в логе видно «расчёт закончен → дверь данных → выкат
      либо отказ по дрейфу».

### Шаг 4 — Наблюдаемость

> ✅ **РАЗВИЛКА ЗАКРЫТА — интервью №037, В2**, ответ владельца дословно: «*Писать в лог + писать
> мне письмо на сервисный email*». Это не вариант А (автоматика себя НЕ выключает) и не В
> (молчать нельзя): работа продолжается, но отказ доезжает до человека письмом.

- [ ] Отчёт одной строкой в лог на каждый прогон: дрейф в процентах и штуках, решение (выкат/отказ),
      номер версии, время.
- [ ] **Письмо владельцу на сервисный адрес при отказе.** Автоматика при этом продолжает работать:
      выключать её владелец не просил.
      🔴 **Канала для такого письма у проекта СЕГОДНЯ НЕТ, и это надо построить.**
      `tools/send-outbound.mjs` существует, но он про другое — публикация тикетов в чужих
      репозиториях от имени владельца, и стоит за гейтом ручного одобрения. Служебное письмо от
      автоматики — отдельный, НЕ интерактивный канал.
      ⏭ Не хватает двух вещей, и обе за владельцем: **адрес сервисной почты** и **чем слать**
      (SMTP с паролем приложения · почтовый сервис с ключом · расширение Firebase). Ключ, каким бы
      он ни был, живёт только в `.env` — канон 2026-08-15.
- [ ] Красный прогон обязан быть ЗАМЕТЕН: молчащая автоматика хуже отсутствующей.
- [ ] **Проверка:** прогон с намеренно красным стражем оставляет в логе явный отказ, шлёт письмо и
      не публикует. Письмо проверяется наблюдением — получено ли оно, а не «отправлено без ошибки».

### Шаг 5 — Сдача

- [ ] `/fable-judge` по утверждениям плана.
- [ ] Строки в `STATUS.md` и `AGENT_GUIDE.md` → «ВЫКАТ В БОЙ» (две двери, и чем они различаются).
- [ ] `plans/04_cutover.md` — как публикуется сайт теперь.
- [ ] Тег `DONE` в имени этого файла.

## Риски

1. **Ключ выката на серверном ПК.** Дверь данных публикует, значит на машине лежит доступ. Ключ —
   только в `.env` (канон 2026-08-15), и это должен быть отдельный узкий доступ, а не боевой ключ
   на все руки.
2. **Тихая деградация.** Автомат, который публикует каждый день, может месяцами возить сломанное,
   если читающие пробы недостаточны. Отсюда К7 и шаг 4.
3. **Диск.** 334 МБ на артефакт плюс 10 версий в хостинге. Уборка обязательна (К8), иначе 10 ГБ
   кончатся примерно за три месяца (`researches/39`).
4. **Ложное чувство свежести.** Дверь возит ДАННЫЕ; она не заменяет выкат кода и не чинит того,
   что сломано в коде. Это записано в границах.

<!-- ВОПРОС-ОК: раздел не задаёт вопросов, а указывает на интервью №037, где они и живут -->
## Что ждёт владельца

Две развилки этого плана заданы в **`interviews/interview_037_auto_rebuild_forks.md`** — исполнитель
(контейнер или соседняя задача на той же машине) и поведение при трёх красных прогонах подряд.
Место вопросов — только `interviews/`, и здесь стоит указатель, а не очередь.
