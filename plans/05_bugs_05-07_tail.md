# План 05 — Хвост багов 05–07: тесты и проверка (передача следующей сессии)

> Сессия 2026-07-12 (вечер) починила bugs/05 (анимации), bugs/06 (стиль «Измерений»)
> и bugs/07 (живой счётчик лендинга), но упёрлась в лимит токенов. Владелец велел:
> тесты — в план, следующей сессии. Код закоммичен и выкачен в бой (см. STATUS.md).

## Что уже сделано (не переделывать)

- `f657668` — «Измерения»: каноническая оболочка V2 + анимации + `dimCardTitle` (5 юнитов).
- `c4acf46` — анимации Профиль/Связи/Пространство/Меню/навигация; канон `src/lib/ui/motion.ts`.
- bugs/07: калькулятор пишет `space/public_metrics` (`{people, computedAt}`, тот же счёт,
  что `space/stats.people`), лендинг читает его динамическим импортом (`src/lib/data/metrics.ts`)
  и без числа строку НЕ показывает. Правило чтения без авторизации уже было в `firestore.rules`.
- Тесты УЖЕ НАПИСАНЫ, но НЕ ПРОГНАНЫ (нужен эмулятор/Java):
  - `sync-server/space_stats.test.mjs` — «публичная витрина лендинга …» (people = 2);
  - `tests/rules/rules.test.ts` — «витрина лендинга читается БЕЗ авторизации…» и
    «🔒 витрину лендинга нельзя переписать с клиента…».

> ## ✅ Хвост закрыт 2026-07-30 автономным циклом (пункты 1–3), 4–6 — состояние ниже
>
> **Пункт 2 — «тест-запрет литерала» — написан наконец.** Он полтора месяца стоял здесь
> невыполненным, и это ровно тот случай, о котором `EXP-0058`: задача в документе не равна
> задаче сделанной. Запрет вышел строже, чем задумывалось («нет такой строки в HTML»):
>
> · **юнит** `src/lib/data/metrics.test.ts` (3 проверки, юнит **189 → 192**): число витрины
>   обязано быть КРАТНО снимку боевой метрики; снимок — целое больше нуля с датой-днём ISO;
>   исторический литерал 2 184 к витрине отношения не имеет. Мутация «`return 2184`» → 2 падения;
> · **e2e** `e2e/landing.spec.ts` → «пререндер: витрина людей стоит в HTML и НЕ выдумана»
>   (e2e **70 → 72**): в СЫРОМ HTML строка есть (это приёмка `bugs/81`) И число кратно снимку,
>   импортированному из того же сгенерированного файла, что использует продукт. Мутация даёт
>   внятный текст: «витрина (2184) обязана быть кратна снимку (94); отношение 23.23 — похоже на
>   выдуманное число (bugs/07)».
>
> ⚠️ **Условие «`.joined` не виден без данных» из пункта 2 УСТАРЕЛО и заменено на обратное.**
> После `bugs/81` число живёт в пререндере (снимок берётся перед выкатом), поэтому строка видна
> ВСЕГДА, в том числе без эмуляторов. Прежняя формулировка охраняла отменённое поведение.
>
> **Пункт 3 — мутации проверены** (обе, в этом же цикле): убрал запись `space/public_metrics` из
> `sync-server/index.mjs` → **6 падений** в `test:sync`; ослабил правило записи витрины
> (`isAdmin()` → `verified()`) → падает ровно «🔒 витрину лендинга нельзя переписать с клиента».
>
> **Пункт 1 — регрессия прогнана целиком** (числа выросли с тех пор): юнит **192** ·
> `test:rules` **94** (появились правила Storage, `bugs/04`) · `test:sync` **34** · e2e **72** ·
> typecheck 0 · svelte-check 0 на 417 · `kaif:check` зелёный.
>
> **Пункты 4–6 — что осталось и почему не агентом:**
> · п. 4 (проверка боя) осмысленна ТОЛЬКО после следующего выката: в бою сейчас старое поведение
>   витрины, `bugs/81` туда не уехал. Инструмент готов — `PROBE_BASE=https://ndimspace.app node
>   tools/verify-bug81.mjs`;
> · п. 5 (пересборка боевого контейнера) — машина и ключи владельца, `npm run sync:image`;
> · п. 6: `bugs/05` и `bugs/07` уже помечены DONE; **`bugs/06` открыт** — стиль «Измерений» это
>   UI-решение, ему нужны макеты и слово владельца, агент его не закрывает.

## Что сделать следующей сессии

1. **Прогнать всю регрессию** (перед этим погасить стенд — порты 8181/9099/5173, EXP-0027):
   `npm test` (149) · `npm run test:rules` (было 85, станет 87) · `npm run test:sync`
   (было 15, станет 16) · `npm run e2e` (66) · `npm run typecheck` · `npm run check` ·
   `npm run kaif:check`. Юниты и svelte-check уже зелёные (149 · 0/0).
2. **Дописать e2e лендинга** (`e2e/landing.spec.ts`): в пререндеренном HTML лендинга НЕТ
   выдуманного счётчика — ни «2 184», ни «2,184» (помни про NBSP: нормализуй ` `,
   в старом литерале пробел был неразрывным); `.joined` не виден без данных (эмуляторов
   на e2e нет → строка честно скрыта). **Мутация**: вернуть литерал в `t` — тест обязан упасть.
3. **Мутационно проверить новые тесты** (канон стенда): убрать запись `public_metrics` из
   `sync-server/index.mjs` → падает тест калькулятора; ослабить правило записи → падает rules-тест.
4. **Проверить бой после деплоя** (деплой сделан этой сессией, слово владельца):
   - лендинг `https://ndimspace.app` больше не содержит «2 184» (сырой HTML);
   - `/dims` на десктопе: рельс слева, карточки-панели в 2 колонки;
   - счётчик «С нами уже N человек» появится ТОЛЬКО после того, как боевой
     `ndim-sync-server` пересчитает цикл с изменениями (см. п. 5) — до этого строки нет, это честно.
5. **Пересобрать боевой контейнер сервера синхронизации** (EXP-0040: копирует директории,
   но новый `index.mjs` попадает только при rebuild):
   `docker build -f sync-server/Dockerfile -t ndim-sync-server .` из корня, затем перезапуск
   по шапке `sync-server/docker-compose.yml` (FIREBASE_PROJECT_ID=ndim-space + ключ SA).
   ⚠️ Если эта сессия уже сделала rebuild — см. STATUS.md, не делать дважды.
   Проверка: `docker logs ndim-sync-server` без ошибок; после цикла с грязными точками
   в боевой базе появляется `space/public_metrics`.
6. Если всё зелёно — пометить `bugs/05`, `06`, `07` статусом ✅ и переименовать в `DONE`
   (git mv), обновить STATUS.md, этот план — тоже в DONE.

## Открытые мелочи, замеченные попутно (НЕ блокируют)

- `src/lib/data/funnel.ts` → `standOnly()`: воронка на проде — no-op со времён, когда не было
  боевого конфига. Конфиг теперь есть (`firebase.ts` выбирает окружение по хосту) — гейт
  устарел, воронка на бою не считает НИЧЕГО. Снять гейт + убедиться, что e2e не зашумит.
  Возможно, заслуживает отдельного бага.
- В `tools/seed-dev.mjs` намеренно грязные названия («Алхимик» (1988)) — это правильно
  (EXP-0041), не «чинить».
