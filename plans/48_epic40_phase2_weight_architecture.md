# План 48 — эпик 40, фаза 2: «Архитектура веса — хабы, перелинковка, разметка»

> **Создан:** 2026-08-14 · **Родитель:** `plans/40_EPIC_semantic_core_v2.md` → Фаза 2 ·
> **Статус:** 🔵 план написан 2026-08-14, шаг 0 закрыт замером · **Исходящее:** решение по
> боевой прополке поля «вид» → владельцу страницей вычитки; 4 макета хаба → владельцу ·
> **Ступень 3 лестницы** `/plan-epic` · **Доказательная база:** `researches/34` §3.4, §6.3, §14, §15

---

## Зачем эта фаза идёт ПЕРВОЙ среди незакрытых

Вопрос владельца 2026-08-14: «чему нужно много времени, чтобы созреть, и это нужно сделать <!-- ВОПРОС-ОК: ЦИТАТА уже отвеченного вопроса владельца — обоснование выбора фазы, а не вопрос к нему -->
пораньше». Ответ дают наши собственные замеры вчерашней фазы 1, а не рассуждение:

| Число (`researches/34` §14–§15, 2026-08-14) | Что оно значит |
|---|---|
| Google: **994** из 10 222 в индексе | за 11 дней с единицы — дверь открыта |
| **9 237** = «Обнаружена, не проиндексирована» | робот знает адрес из sitemap и **не приходит** |
| **10 222 из 10 222** недостижимы от корня | ни одного пути от `/`; пары `ru↔en` замкнуты сами на себя |
| Яндекс: **−641** страница за 6 дней, «малоценная» | это уже не медленный рост, а активная потеря |
| Органика: 8 кликов · 149 показов · позиция 46,7 | и она **каталожная** — поверхность живая даже без веса |

Страница набирает вес месяцами (§8: 1,74 % новых страниц входят в топ-10 за год), а теряет его
за неделю. Значит цена промедления здесь измеряется календарём, а не объёмом работы, — и это
единственная работа в беклоге с таким свойством.

## Вектор цели (по `REQUIREMENTS_FRAMEWORK.md`)

- **Achieve:** ни одной страницы каталога, недостижимой от лендинга; любая карточка — **≤3 кликов**
  от `/{lang}`.
- **Achieve:** у каждой страницы каталога **≥3 внутренних входящих** ссылки (критерий 2 метаплана;
  сегодня — ровно 1, языковой двойник).
- **Achieve:** структурированная разметка каталога проходит валидатор Google; `Organization`+
  `WebSite` на лендинге (долг `researches/26` §7.6).
- **Maintain:** инварианты владельца — ни одного числа похожести, ни порядка близости, ни данных
  другого человека на публичных поверхностях; блок соседства — **только по классификации**
  (интервью №025, В7 = А), в формулировках никогда «Рядом в Пространстве».
- **Avoid:** хаб-пустышка. Хаб существует только при **≥20 карточках** — иначе он сам становится
  тонкой страницей и работает против site-wide профиля (§4.3, §5.4).

## Критерии приёмки фазы (Scale · Meter · Target)

1. **Достижимость.** Scale: страниц каталога, недостижимых от `/{lang}` · Meter:
   `node tools/measure-link-graph.mjs` по свежему `build/` · Target: **0** (сегодня 10 222).
2. **Входящие ссылки.** Scale: минимум входящих на страницу каталога · Meter: тот же прибор ·
   Target: **≥3** у 100 % карточек.
3. **Глубина.** Scale: клики от `/{lang}` до самой дальней карточки · Meter: BFS того же прибора ·
   Target: **≤3**.
4. **Инварианты — стражем с доказанной мутацией.** Meter: `tools/verify-catalog-hubs.mjs` ·
   Target: ≥3 мутации роняют стража с ожидаемым числом падений.
5. **Разметка.** Meter: валидатор Google Rich Results по 3 образцам (Movie · Video game · Book) ·
   Target: 0 ошибок; `AggregateRating` — только там, где правило показа его разрешает.
6. **Вес не вырос.** Meter: замер бандла (`EXP-0136`: универсальный `load` уже уносил каталог в
   браузер) · Target: клиентский чанк не прибавил ни килобайта каталога.
7. Регрессия зелёная: `npm test` · `npm run test:sync` · `npm run e2e` · `verify-dimension-pages`.

## Якоря метаплана (трассировка)

> Фаза 2: «хабы каталога (вид/год/теги…) · prev/next-цепочки как краулируемый каркас · ссылки
> лендинг → каталог/опоры · JSON-LD: Movie/Book/Game/CreativeWork + AggregateRating…,
> Organization+WebSite на главную · сегментация sitemap по типам страниц».
> Ворота: «прибор карты ссылок показывает 0 сирот в затронутых сегментах · разметка проходит
> валидатор Google · страж на „хаб не показывает ни одного числа похожести" доказан мутацией ·
> `npm run e2e` зелёный».

---

## 🔑 Решение, снявшее блокер фазы (принято 2026-08-14, инженерное — не уровня владельца)

Метаплан ставил условием прополку поля «вид» в бою (`plans/30`): грязное поле дало бы
**два визуально одинаковых хаба** — «TV Series» (329) и «TV Series​» с невидимым `U+200B` (41).

Замер шага 0 показал, что блокер снимается дешевле: **нормализация на ЧТЕНИИ**, одно место
(`src/lib/content/dim-kind.ts`). Хабы группируются по каноническому виду, а боевая прополка
перестаёт быть предусловием и остаётся **гигиеной данных** — идёт своим чередом и своим темпом,
со словом владельца, никого не задерживая.

Это применение `PHILOSOPHY.md`: застряли не потому, что задача трудная, а потому что понимали её
как «сначала почини данные в бою». Данные чинить всё равно нужно — но не для того, чтобы построить
хаб.

⚠️ Цена, которую платим сознательно: пока бой не прополот, **сырое** значение вида по-прежнему
приезжает в Firestore-клиент приложения (экран `/dims`). Нормализация покрывает публичные
поверхности; внутренний экран увидит `U+200B` до прополки. Названо здесь, чтобы следующая сессия
не сочла это регрессией.

---

## Шаги

### Шаг 0 — Замер поля «вид»: что именно грязное ✅ 2026-08-14

> Сделано сухим прогоном по `dims-build.json` (5111 записей, только чтение).
> **Грязных записей — 74 (1,4 %)**, по классам:
>
> | Класс | Записей | Примеры |
> |---|---|---|
> | невидимый `U+200B` | **44** | `"TV Series​"` 41 · `"Телесериал​"` 1 · `"TV Show​"` 3 |
> | регистр RU | 23 | `"фильм"` 21 · `"роман"` 1 · `"видеоигра"` 1 |
> | регистр EN | 23 | `"movie"` 21 · `"novel"` 1 · `"video game"` 1 |
> | год вместо вида | 4 | `"1998"` · `"1993"` ×2 · `"1994"` |
> | язык не в своём поле | 5 | `"Movie"` в RU ×3 · `"Фильм"`/`"Роман"` в EN |
> | пустая строка | 1 | `type.en === ""` |
>
> **Хабы после нормализации (порог ≥20):** Movie **2774** · Video game **1221** ·
> TV Series **370** · Novel **231** · Practice **228** · Music Artist **198** · Book **64** —
> семь хабов покрывают **5086 из 5111 (99,5 %)**.
> **Хвост — 25 записей в 6 мелких видах** (TV Show 11 · Novella 5 · Animated Movie 3 ·
> Internet Show 3 · Lecture 2 · Graphic Novel 1): хаба не получают по порогу.
> **Значений вида, неизвестных словарю, — 0.** Пять записей с годом или пустотой в одном поле
> спасены вторым полем (у «Six Days Seven Nights» русский вид «1998», английский — «Movie»).
> `[TESTED: 2026-08-14 · прогон на полном каталоге; суммы сходятся с 5111]`

### Шаг 1 — Модуль канонического вида + тест ✅ 2026-08-14 (автономно)

> Сделано: `src/lib/content/dim-kind.ts` (`normalizeRaw` · `kindKeyOf` · `isKnownWithoutHub` ·
> `kindLabel`/`kindTitle` · `isKindKey`) + `dim-kind.test.ts` (**5 тестов**, таблица случаев из
> замера + идемпотентность + проверка по боевому каталогу).
> **Доказан тремя мутациями** с ожидаемым числом падений: не снимать `U+200B` → **2** ·
> не приводить регистр → **3** · влить «Повесть» в «Роман» → **2**; контроль после отката 5/0.
>
> 🔑 **Мутация нашла дыру в самом тесте, и это её работа.** Оба табличных случая с `U+200B`
> имели чистое второе поле и потому спасались опросом второго поля — мутацию ловил только
> боевой тест. Но `dims-build.json` лежит вне git: на чистом клоне тот уходит в ветку среза, и
> мутация не поймалась бы вовсе. Добавлен случай с грязью в ОБОИХ полях — единственная форма,
> которую нечем спасти, кроме самой нормализации.
> `[TESTED: 2026-08-14 · npm test 250/250 · три мутации доказаны]`

- [x] `src/lib/content/dim-kind.ts` — **одно** место на весь проект:
      `normalizeKind(raw: string): string` (снять `U+200B`/`U+200C`/`U+200D`/`U+FEFF`, `trim`,
      привести регистр к канону, отбросить значения-годы и пустые) → `kindKey` (латинский слаг для
      адреса: `movie`, `video-game`, `tv-series`, `novel`, `practice`, `music-artist`, `book`) →
      `kindLabel(key, lang)` (подпись из словаря продукта, **не** из данных).
      🔴 Хвост из 31 записи в хабы НЕ вливается и синонимами НЕ склеивается: слияние «Novella →
      Novel» меняет смысл поля и является правкой данных, а не чтением. Такие записи получают
      `kindKey = null` и живут только в индексной странице каталога.
- [x] `dim-kind.test.ts` — таблица случаев ИЗ ЗАМЕРА шага 0 (все шесть классов, включая обе строки
      с `U+200B`), плюс инвариант «канонический вид не меняется при повторной нормализации».
- **Проверка:** `npm test` зелёный; число различных `kindKey` = 7 + `null`. ✅ 250/250.

### Шаг 2 — 4 макета хаба → владельцу ✅ ЗАКРЫТ 2026-08-14: форма — **V3 «Рейтинг»**

> Показаны 4 макета (`design/catalog-hub-mockups.html`, 16 кадров — 4 варианта × 2 темы ×
> 390/1440, данные боевые). Владелец выбрал **V3 «Рейтинг»** (интервью №030, В1 = В): хаб —
> ранжированный список, «топ по версии NDim Space», номер места · название · год и автор ·
> звёзды · оценка · «оценено N людьми».
>
> 🔴 **Вместе с выбором произнесён ЗАКОН, который шире этой фазы:** «названия не должны обрезаться
> троеточием, всегда полностью влазят — это закон. если длинное — карточка растет по высоте».
> Записан в канон (`AGENT_GUIDE.md` → «Названия не обрезаются НИКОГДА»), исполнен во всех четырёх
> макетах. Там же он вскрыл нарушение **в бою** — `bugs/120` (результат теста, выкат вчера).
>
> ⚠️ Хвост шага: **В2 (порядок карточек) ещё не отвечен.** V3 по построению предполагает порядок
> по оценке — с ним же приходит названная цена: оценён 981 фильм из 2774, значит примерно с 17-й
> страницы пойдёт сорок страниц «ещё без голосов», и карточка переезжает между страницами при
> каждом новом голосе. До ответа шаг 3 строит первые страницы, где оба порядка совпадают.
>
> 📌 Кадры «до/после» закона: `test-results/catalog-hub-mockups/V3-*.png` (пересняты после правки).

**Что делалось (история шага):**

- [ ] Хаб — **новый тип публичной страницы**, значит правило четырёх макетов действует
      (`AGENT_GUIDE` → «Дизайн»). Оболочка — как у публичных страниц каталога (рельса нет
      сознательно), образец — `design/dimension-page-mockups.html`.
- [ ] Что варьируем: плотность сетки карточек · присутствие оценки на карточке (правило показа —
      `dims-rating.ts`, одно на проект) · форма пагинации · место блока «Похожие по каталогу».
- [ ] Кадры — `tools/shoot-mockups.mjs`, обе темы × 390/1440, **смотреть глазами ДО показа**.
      Подать страницей вычитки (`npm run review -- open …`), а не списком в чат.
- **STOP-ASK:** без утверждённого макета шаг 3 не начинается.

### Шаг 3 — Хабы, пагинация, prev/next ✅ ЗАКРЫТ 2026-08-14

> **Сделано.** 180 страниц в сборке (индекс каталога + 89 страниц семи хабов) × 2 языка, форма —
> утверждённая V3 «Рейтинг». Ссылки на семь хабов и на индекс каталога встали на лендинг.
>
> **Главное число фазы взято:** прибор карты ссылок показывает **0 недостижимых страниц каталога**
> от `/ru` и `/en` — было 10 222. Самая дальняя карточка — **3 клика** от языковой главной
> (лендинг → хаб → страница N → карточка), что и обещали критерии 1 и 3.
>
> `[TESTED: 2026-08-14 · npm test 263/263 (13 новых) · npm run e2e 100 passed · check 0 ошибок ·
> typecheck чист · verify-dimension-pages 27/0 · verify-product-vocabulary 7/0 ·
> measure-link-graph: недостижимо 23, из них каталога 0 · живой Chromium: **40 кадров** —
> 5 адресов × 2 языка × 2 темы × 390/1440, все 200, консоль чиста]`
>
> ⚠️ **Кадры приёмки живут в `test-results/`, а `npm run e2e` выжигает эту папку ЦЕЛИКОМ**
> (`EXP-0062`). Прогон e2e при завершении сессии стёр их, и ссылка на кадры стала висеть в
> пустоту — поймано судейским проходом. Порядок: сначала e2e, потом съёмка; ссылаешься на кадры —
> проверь, что они на диске.
>
> **Тесты доказаны ПЯТЬЮ мутациями** с ожидаемым числом падений (контроль после отката — 17/0):
> равенства разводит НАЗВАНИЕ вместо слага → **2** (в том числе тест детерминизма) ·
> неоценённые вперёд оценённых → **2** (юнит и боевой каталог) · первая страница получает адрес
> `…/1` → **1** · сортировка по ЧИСТОЙ средней, без учёта голосов → **1** · опора берёт медиану
> вместо 90-го процентиля → **2**.
>
> ✅ **В2 ОТВЕЧЕН И РАЗНЕСЁН тем же вечером — интервью №030, В2 = Д** («по оценке, но с учётом
> числа голосов»). Порядок считает `makeComparator` по взвешенной величине
> `weighted = (v·R + m·C)/(v + m)`; **оба числа опоры посчитаны, ни одно не придумано** —
> `C = 8,2405` (средняя по 1877 оценённым) и `m = 4` (90-й процентиль числа голосов среди
> оценённых). Опора считается на сборке, печатается в лог и закреплена тестом.
> 🔴 **Показывается по-прежнему настоящая средняя** — взвешенная наружу не выходит: это условие,
> на котором владелец выбрал Д.
> 🔴 **Поправка, записанная и в интервью:** моя иллюстрация варианта Д («9,3 от одиннадцати
> обгонит десятку от четверых») боевыми числами НЕ воспроизводится ни при каком весе опоры.
> Настоящий эффект мягче и в ту же сторону: на первую страницу «Фильмов» поднялись 9,9 × 8,
> 9,7 × 7 и 9,6 × 8, потеснив единичные десятки от четверых.

- [x] `/{lang}/catalog` — индексная страница каталога: 7 хабов + счётчики + **хвост из 25 объектов
      ссылками**. Хвост оказался несущим: без него 25 × 2 языка остались бы недостижимыми, а
      обещание фазы — «ни одной недостижимой».
- [x] `/{lang}/catalog/{kindKey}` и `/{lang}/catalog/{kindKey}/{page}` — пагинация по **60**
      карточек: Movie 47 стр. · Video game 21 · TV Series 7 · Novel 4 · Practice 4 ·
      Music Artist 4 · Book 2 = **89 страниц × 2 языка** (числа сошлись с замером шага 0 в точности).
      🔑 Первая страница живёт БЕЗ номера (`…/catalog/movie`), `…/1` не отвечает вовсе —
      два адреса на одно содержание были бы дублем, который поисковик обязан склеивать сам.
- [x] 🔴 **Глубина решается формой пагинации, а не надеждой:** хаб перечисляет ВСЕ номера своих
      страниц (47 ссылок — норма), иначе карточка на последней странице окажется в 49 кликах по
      цепочке prev/next. Цепочка prev/next остаётся — она краулируемый каркас, но не единственный
      путь. Кадр пагинации на телефоне — `test-results/catalog-hubs/crop/pager-390-*.png`.
- [x] `csr = false` и `prerender` — как у карточек; страницы хабов НЕ тянут каталог в браузер.
      Замер после сборки: крупнейший чанк узла **47,6 КБ**, весь `_app` — **1,3 МБ**; в клиентском
      бандле нет ни одной записи каталога (грепом по названиям).
- [x] `hreflang` двусторонний с самоссылкой на каждой странице хаба.
      🔴 Отсюда же вытекло правило порядка, которое иначе было бы незаметным: **порядок карточек
      обязан не зависеть от языка**, иначе `/ru/catalog/movie/5` и `/en/catalog/movie/5` держали бы
      РАЗНЫЕ объекты, продолжая объявлять себя переводами друг друга. Равенства разводит слаг, а не
      название; накрыто тестом с контролем самой фикстуры.
- **Проверка:** ✅ `measure-link-graph` — недостижимых страниц каталога **0**, глубина карточки **3**.

**Что сделано сверх буквы шага и почему:**

- **Шапка публичной страницы стала общим компонентом** (`src/lib/ui/PublicBar.svelte`). Она жила
  двумя дословными копиями (страница объекта и хаб «Тесты»), хабы каталога стали бы третьей и
  четвёртой: правку владельца «кнопки в хедере квадратные» пришлось бы вносить в четыре места.
  Обе прежние страницы мигрированы, `verify-dimension-pages` подтверждает 27/0.
- **Хабы вошли в `sitemap.xml`** (10 432 адреса). Сегментация карты по типам — по-прежнему шаг 5.
- **Прибор карты ссылок научился мерить глубину от ЯЗЫКОВЫХ ГЛАВНЫХ**, а не только от `/`.
  Корень — распознаватель под `noindex`, он в поиске не участвует; считать «клики от корня»
  значило приписывать каждой странице лишний шаг через страницу, которой в выдаче нет. Прежняя
  метрика сохранена рядом — базовая линия фазы 1 осталась сравнимой.

**🔴 Два красных стража, найденных этим шагом, — оба краснели ДО него, на исправном продукте:**

1. **`npm run e2e` был красным сутки.** Шаг 5 (разметка каталога, выкат накануне) положил описание
   фильма ещё и в JSON-LD, а страж жаргона вырезал только блок описания. Тест краснел на «Скрытых
   фигурах» и «Игре в имитацию» — тех же двух фильмах, что и в прошлый раз: там «вычислительница»
   стоит по делу. Прогон `npm run e2e` в той сессии не делался. Вырез расширен ровно на значение
   поля `description` внутри разметки (сама разметка судится по-прежнему) и накрыт контролем
   прибора: вырез обязан НЕ съедать настоящее нарушение рядом.
2. **`verify-product-vocabulary` краснел на четырёх страницах семейства «Тест».** Не на жаргоне:
   слово «калькулятор» стоит в словаре как чужое имя СЕРВЕРА СИНХРОНИЗАЦИИ, а на этих страницах
   так называется сама страница — «Калькулятор любви», утверждённая владельцем (интервью №028).
   Заведено узкое исключение по ТЕРМИНУ (не по странице), записано в канон словаря и в самого
   стража, доказано тремя контрольными проверками.

**🟡 Найден дефект, который агент не чинит один — `bugs/121`:** после этого шага недостижимых от
языковых главных осталось 23, каталога среди них ноль, но **19 — публичные страницы**: всё
семейство «Тест» (8, выкачено накануне ради самого крупного спроса проекта), английские документы
(9) и обе `delete-account`. Это тот же дефект, что лечила фаза, только на другой поверхности.
Все три лечения правят ЛЕНДИНГ — территорию `plans/21`, — поэтому идут владельцу.

### Шаг 4 — Ссылки с лендинга и блок «Похожие по каталогу» ✅ ЗАКРЫТ 2026-08-14

> **Критерий 2 фазы взят числом:** минимум входящих ссылок у страницы каталога — **3** (было 1,
> языковой двойник). Мерит тот же прибор: `минимум_входящих_по_сегментам` в
> `researches/34_linkgraph_<дата>.json`. Среднее на этот вопрос не отвечает — обещание,
> выполненное «в среднем», не выполнено ни для кого конкретно, поэтому прибор научен мерить
> МИНИМУМ, а не распределение.
>
> `[TESTED: 2026-08-14 · npm test 278/278 (11 новых) · npm run e2e 100 passed · check 0 ошибок ·
> verify-dimension-pages 30/0 · verify-product-vocabulary 7/0 · живой Chromium: **8 кадров
> карточки** — блок соседей и ссылка вверх × обе темы × 390/1440
> (`test-results/catalog-hubs/card/`)]`
>
> ⚠️ Первая редакция этой строки заявляла матрицу «обе темы × 390/1440», а снято было ДВА
> сочетания из четырёх — поймано судейским проходом и доснято.

- [x] **С лендинга — прямо на 7 хабов** — сделано шагом 3 (блок в подвале, минимальное вторжение
      в макет «Колонна»).
- [x] Карточка измерения получает ссылку «вверх» (в свой хаб; у хвостовых видов — в индекс
      каталога) и блок **«Похожие по каталогу»**: восемь соседей по КЛАССИФИКАЦИИ, отбор
      детерминированный, без единого числа похожести. Подпись — «Похожие по каталогу».
      Формулировка «Рядом в Пространстве» запрещена и стережётся.
- **Проверка:** ✅ входящих ≥3 у 100 % карточек каталога.

**Как устроен отбор и три вещи, которые он вскрыл только на боевых данных:**

1. **Вес общего тега — обратная частота (IDF), а не штука.** Замер: теги есть у 5108 объектов из
   5111, но четыре самых частых (`movie` 3155 · `кино` 3136 · `фильм` 3129 · `film` 3091) — это
   САМ ВИД, общий у всех внутри хаба. С ненулевым весом они складывались в одинаковую прибавку
   каждому, равенства разводил слаг, и «Матрица» получала в соседи «10 причин моей ненависти» —
   первое по алфавиту. Теперь тег, встречающийся чаще чем у четверти каталога, весит РОВНО НОЛЬ.
2. **Десятилетие усиливает признак, но само признаком не является.** Пока совпадение десятилетия
   давало право на соседство, вернулся тот же алфавитный мусор — только через другое поле: у
   «Матрицы» жанровых тегов нет вовсе, и в один вес 0,3 попадали ВСЕ фильмы девяностых. Право
   быть соседом даёт только содержательный признак: общий значащий тег либо тот же автор.
3. **Запасной путь по году обязателен:** жанровых тегов нет у 184 фильмов из 2774 (7 %) и у 12
   сериалов — у «Матрицы» и «Побега из Шоушенка» стоят только четыре тега вида. Без запаса их
   карточки остались бы вовсе без соседей, то есть с двумя входящими при обещанных трёх.

**🔴 И главное, что нашлось замером, а не рассуждением, — «возврат потерянных».** Отбор
несимметричен: A считает B соседом, а B выбирает восьмерых других. Замер по боевому каталогу:
**764 объекта из 5111 не выбраны соседом НИ РАЗУ**. Проход `returnLostNeighbours` дописывает
потерянного в список ЕГО ЖЕ соседа — того, у кого список короче. После прохода объектов без
единой входящей — **0**. Названная цена: у части карточек соседей 9–11 вместо 8 (замер: 8 у 4461 ·
9 у 560 · 10 у 66 · 11 у 24). План писал «6–8» — это было указание размера, а «≥3 входящих у
каждой» — ворота фазы; жёсткий потолок вернул бы часть объектов в потерянные.

**Стражи обновлены осознанно, оба падали правильно:**
· `verify-dimension-pages` — проверка «блока соседства НЕТ (отклонён владельцем)» противоречила
  бы слову владельца (№025, В7 = А) и заменена на четыре: блок ЕСТЬ на каждой странице · в нём
  нет ни одного процента · запретной формулировки нет · ссылка вверх есть. Стало 30 проверок;
· `measure-link-graph` — контроль прибора держал поимённый набор якорей контрольной страницы и
  упал, как только карточка получила хаб и соседей. Это его работа. Контроль переписан на ФОРМУ
  набора (доли дверей и диапазон числа соседей), чтобы он краснел от изменения ПРОДУКТА, а не от
  смены слага соседа.

### Шаг 5 — Разметка и сегментация карты сайта

- [ ] JSON-LD на карточке по виду: `Movie` · `VideoGame` · `TVSeries` · `Book` · `MusicGroup` ·
      иначе `CreativeWork`. `aggregateRating` — **только** когда правило показа разрешает
      (`ratingView`), чужие рейтинги не тянуть (§5.6).
- [ ] `Organization` + `WebSite` на лендинге — долг `researches/26` §7.6.
- [ ] `sitemap.xml` → сегменты (лендинг+документы · каталог · хабы · тесты) с индексным файлом:
      покрытие станет диагностируемым ПО СЕГМЕНТАМ, а не одной цифрой.
- **Проверка:** валидатор Rich Results на 3 образцах — 0 ошибок.

### Шаг 6 — Страж и мутации ✅ ЗАКРЫТ 2026-08-16 (сессия 18)

**Сделано.** `tools/verify-catalog-hubs.mjs` — **89 проверок, 0 провалов** по собранному сайту,
сервера не поднимает. Проверяет: индекс ведёт ≥5 хабов · у хаба ≥20 карточек (считаются ССЫЛКИ В
КАРТОЧКИ, а не класс вёрстки — класс сменится при правке макета, ссылка есть смысл хаба) ·
`hreflang` двусторонний с самоссылкой · ни числа процента, ни «похожесть», ни «Рядом в
Пространстве» · нет ПДн · **пагинация без дыр** · вес клиентского бандла < 3 МБ (`EXP-0136`).

🔑 **Про пагинацию — почему проверок ДВЕ.** Первая требует, чтобы страница, НА КОТОРУЮ ВЕДЁТ
ссылка, существовала файлом (иначе человек и робот упираются в 404 посреди обхода, а карточки за
дырой становятся недостижимыми — прямо против главного числа фазы). Вторая ловит дыру в
НУМЕРАЦИИ (1, 2, 4): она означает потерянную третью страницу, а не «просто меньше ссылок».

**Мутации — адресаты названы ДО прогонов, все три совпали:**

| Мутация | Ожидание | Факт |
|---|---|---|
| вид ниже порога возвращён в хабы (`KIND_KEYS` + `LABELS`) | 2 (оба языка) | **ровно 2**, адресно на этом хабе |
| процент похожести вписан в строку хаба (`CatalogHub.svelte`) | 14 (7 хабов × 2 языка) | **ровно 14** |
| снята самоссылка `hreflang` (`catalog-source.ts`) | 14 | **ровно 14**, с указанием языка |

⏭ **Не сделано и названо:** расширение `verify-dimension-pages` проверкой «у карточки есть ссылка
вверх на хаб» — она там УЖЕ ЕСТЬ (`noUp`, проверка «ссылка вверх — в хаб своего вида»), то есть
третий подпункт шага закрыт ранее и переоткрывать его нечем.

<details><summary>Исходные требования шага (для истории)</summary>

- [x] `tools/verify-catalog-hubs.mjs` по СОБРАННОМУ `build/`: хаб ≥20 карточек · ни одного числа
      похожести и слова «Рядом в Пространстве» · пагинация полная и без дыр · `hreflang` с
      самоссылкой · вес клиентского бандла не вырос · блок соседства не раскрывает чужих данных.
- [ ] **Доказать мутациями** (≥3, с ожидаемым числом падений): (1) вернуть в хаб вид с 3
      карточками · (2) вписать процент похожести в блок соседей · (3) убрать самоссылку `hreflang`.
- [x] Расширить `verify-dimension-pages` проверкой «у карточки есть ссылка вверх на хаб».

</details>

### Шаг 7 — Приёмка и выкат

- [ ] Живой Chrome: обе темы × 390/1440, путь «лендинг → хаб → страница 2 → карточка → сосед».
- [ ] `/fable-judge` на утверждения фазы.
- [ ] Полный каталог перед сборкой: `node tools/fetch-dims-slice.mjs --all` (шаг выката).
- [ ] **Выкат — только с AUTH владельца.** После выката: переподать сегментированный sitemap в три
      консоли и снять **второй замер покрытия** — он же вторая точка тренда для вердикта фазы 8.

---

## Что эта фаза НЕ делает

| Что | Почему |
|---|---|
| Хабы по году и тегам | Интервью №025, В2 = А: «год и теги позже по данным» |
| Боевую прополку поля «вид» | Правка данных в бою — слово владельца; идёт отдельно (`plans/30`), фазу больше не блокирует |
| Слияние мелких видов («Novella → Novel») | Меняет смысл поля = правка данных, а не чтение. Владельцу — вместе с прополкой |
| Блок соседства ПО ПОХОЖЕСТИ | «До лучших времён» (№025 В7); IBCF — `plans/37` |
| Переработку блока-двери карточки | Это фаза 3 (каталог как назначение) |

## Риски

| Риск | Гашение |
|---|---|
| +180 страниц хабов сами станут «тонкими» | Порог ≥20 карточек в воротах; хаб несёт счётчики и подписи, а не пустую сетку |
| Пагинация раздувает глубину до 49 кликов | Все номера страниц перечислены на хабе — шаг 3, названо явно |
| Каталог снова уедет в клиентский бандл | `csr = false` + замер веса в критерии 6 (`EXP-0136` — оплачено однажды) |
| Яндекс продолжит вычищать EN-сегмент | Вес и перелинковка — прямой ответ на «малоценная»; проверяется вторым замером, а не мнением |
| Лендинг — территория `plans/21` | Блок ссылок согласован как минимальное вторжение; строка в `plans/26` |
