# План NEW — вход по ссылке из письма: экраны Б3 «Спросить до профиля» и Г2 «Там или здесь без оценок»

> **Создан:** 2026-09-25 (dev-1, постановка №2 Менеджера `ndim-61`) · **Родитель:** `bugs/233` → «РЕШЕНИЕ ВЛАДЕЛЬЦА —
> интервью №096» · метаэпик `plans/104` (ставка 1) · **Статус:** 🔧 план одобрен Менеджером 2026-09-25 ≈17:02 (сообщение получено между коммитом плана 17:01:47 и загрузкой
> портрета голоса 17:03:42; точнее окно не записало) · порция 1
> (Б3 + вкладка) ✅ 2026-09-25 17:32 — ручной прогон 9/9, отчёт `qa/reports/2026-09-25_signin-link-forks.md` · порция 2 (Г2) —
> ✅ 2026-09-25 19:28 — полный прогон ВС-01…ВС-16 без провалов с головы `8390510`, мутанты P1 (13) и G2 (10) 19:29–19:38
> (поправки суда порции 1: п. 5 — граница ВС-16, п. 6 — мутанты файлом, п. 7 — голова в выводе драйвера); парковка
> 19:42–20:41 по слову Менеджера; отчёт `qa/reports/2026-09-25_signin-link-forks-g2.md` дописан и внесён в реестр · *поправка 2026-09-25 19:29: прежде здесь стояло «порция 2 (Г2) — 🔲 не
> начата», хотя код, набор и прогон ВС-10…ВС-15 (17:54–17:56) уже были в коммите парковки `57e0b18`* · **Исходящее:** строки
> EN экранов Б3 и Г2 и формулировка строки «письмо отправлено» для другого браузера — Менеджеру на вычитку владельцу.

## Вектор цели (Achieve)

Гость, сохраняющий результаты, и человек, открывший чужое письмо, попадают туда, куда хотят, и понимают, что произошло:
ни тупика, ни молча чужого профиля, ни вкладки, которая обещает и не делает. Путь ставки 1 «оценил → остался →
вернулся» не рвётся на письме.

## Решения владельца, которые исполняются (дословные адреса)
<!-- attribution-ok: адреса №096/№084 ниже -->

- **№096 В11 = В — Б3.** Ссылка открывает аккаунт maria@example.com, в браузере вошёл nikolai@example.com: прежде профиля
  экран называет оба аккаунта и две дороги. Кадр `D:/work/ai_sandbox/ndim/test-results/owner-reviews/interview-085/Vb3.png`.
- **№096 В12 = Б — Г2.** Гость нажал «Сохранить мои результаты», письмо открыл в другом браузере: экран объясняет, что
  оценки остались там, где их ставили; кнопка «Войти здесь без этих оценок»; под ней — оценки не перейдут. Ссылка несёт
  пометку «письмо гостя» без почты и личности. Перенос оценок сервером — НЕ строится (вариант В отклонён). Кадр `Vg2.png`.
- **№084 В1 = А** — адрес в ссылке, строка аккаунта на шаге «идёт вход»; **№084 В2 = А** — шаг «идёт вход» V1.

## Критерии приёмки (из постановки, сценарной формой — проверка в наборе)

1. **Б3.** Ситуация: в браузере вошёл аккаунт Б (`dev@ndim.space` стенда), ссылка выписана на адрес А. Действие:
   открыть ссылку. Результат: экран «Ссылка из письма для другого адреса» с обоими адресами; «Остаться в аккаунте Б» →
   профиль Б, «Войти в аккаунт А» → шаг «идёт вход» с адресом А → профиль А. Проверка: драйвер набора, кейсы ВС-Б3-*.
2. **Г2.** Ситуация: гость в контексте 1 нажал «Сохранить мои результаты»; контекст 2 чистый. Действие: открыть ссылку в
   контексте 2. Результат: экран «Оценки остались в другом браузере»; «Войти здесь без этих оценок» → вход в аккаунт
   адреса без оценок гостя; в контексте 1 гость с оценками цел. Проверка: кейсы ВС-Г2-*.
3. **Та же вкладка.** Вкладка с шагом «Мы отправили Вам письмо» уходит в профиль после привязки в соседней вкладке того
   же браузера. Проверка: кейс ВС-ТАБ (драйвер `qa/reports/2026-09-25_signin-sent-note.two-tabs.driver.mjs` — основа).
4. **Контроли.** Обычная ссылка входа в чистом браузере — без экранов выбора; ссылка без пометки гостя — без Г2; тот же
   аккаунт в браузере — тихий вход собой, без Б3 (регресс `bugs/233` К2).
5. Набор `qa/suites/signin-link-forks.md` → ручной прогон живым браузером, обе темы × 390/1440, кадры глазами → отчёт
   `qa/reports/2026-09-2x_signin-link-forks.md` + строка реестра.
6. Гигиена: `npm test` · `test:tools` · `guards` · `check` · `typecheck` зелёные; `npm run e2e` под замком стенда.

## Развилки

- `FORK: как опознать «другой аккаунт» (Б3)` · options <A: сравнить адрес вошедшего с адресом из ссылки ДО обращения к
  Firebase | Б: сначала signInWithEmailLink адресом вошедшего и ловить auth/invalid-email> · price of error <Б тратит
  запрос и прячет развилку за отказом; ложный Б3 у самого себя, если сравнение чувствительно к регистру> · consulted
  <`researches/69` §2.3 (FirebaseUI сверяет адреса и спрашивает при расхождении, `emailMismatch`); проба
  `tools/probe-link-email-binding.mjs` — неудачная сверка код не сжигает> → **A**, адреса сравниваются без учёта
  регистра; ссылка без адреса (старое письмо) — прежнее поведение.
- `FORK: как опознать «письмо гостя в другом браузере» (Г2)` · options <A: параметр `from=guest` в адресе возврата +
  «в этом браузере нет памяти письма» (`ndim-pending-email`) | Б: параметр с uid гостя | В: только параметр> · price of
  error <Б несёт личность — запрещено №096 В12; В показал бы Г2 и в том браузере, где гость ставил оценки> · consulted
  <Firebase «Passing State in Email Actions» (состояние едет в `continueUrl`); №096 В12 «без почты и личности»> → **A**.
  Пометка ставится только двери «Сохранить мои результаты» (сессия гостя + намерение `upgrade`); у двери входа без сессии
  — нет. Имя параметра не `guest`: `?guest=` уже дверь гостя на `/profile`.
- `FORK: Г2 при ГОСТЕВОЙ сессии во втором браузере` · options <A: «Войти здесь» привязывает адрес к этому гостю (его
  оценки сохраняются) | Б: гость этого браузера отпускается, вход в аккаунт адреса | В: Г2 не показывать> · price of
  error <Б молча теряет оценки второго гостя — необратимо; A при пересланной чужой ссылке отдаёт оценки второго гостя в
  чужой аккаунт (та же цена, что №084 приняла для входа без вопроса); В оставляет сегодняшнее «ничего не происходит»> ·
  consulted <Firebase «Convert an anonymous account to a permanent account» (linkWithCredential сохраняет uid и данные);
  `AGENT_GUIDE.md` → «Заметки от человека»: «люди уже заполнили свои измерения, этот труд нельзя потерять»> → **A**, шаг
  «идёт вход» называет адрес. ⚠️ Край, а не главный путь: у кого во втором браузере свой гость — редкость. Менеджер
  принял A 2026-09-25 ≈17:02 (опоры — строка «Статус» выше) с условиями: (а) `[AI]`-решение строкой с ценой в «Решениях без владельца» плана и отчёта —
  он выносит его на вычитку; (б) текст под кнопкой Г2 на этом краю обязан остаться правдой (оценки ЭТОГО гостя
  сохраняются) — неправдивую строку кадра не правлю сам, несу Менеджеру пару «кадр / предложение».
- `FORK: чем вкладка «письмо отправлено» узнаёт о сохранении` · options <A: `onIdTokenChanged` | Б: `onAuthStateChanged`
  | В: перечитывание при возврате фокуса> · price of error <Б молчит при привязке (uid тот же) — сегодняшний дефект; В
  не срабатывает, пока вкладка не в фокусе> · consulted <исходник SDK `@firebase/auth` 1.13.3: `_onStorageEvent` при
  том же uid делает `_assign` (isAnonymous, email) и `getIdToken()` → `notifyAuthListeners` шлёт `idTokenSubscription`;
  `authStateSubscription` — только при смене uid> → **A**: вкладка в шаге «отправлено» слушает `onIdTokenChanged`, и как
  только в сессии вошедший человек (не гость) — полная перезагрузка на `/profile` (память вкладки принадлежала гостю).

## Шаги

**Порция 1 — Б3 + вкладка «письмо отправлено»** (заявка на мерж отдельно)
- [x] `account.ts`: чистая функция `linkFork(user, linkEmail)` → `other-account | null` + 4 юнита (параметры Г2 добавит
      порция 2 — план назвал сигнатуру заранее, порция 1 взяла только нужное Б3).
- [x] `SigninScreen.svelte`: шаг `fork-account` — строки RU дословно с кадра Vb3; EN — черновик по портрету, поправлен §7Б.
- [x] `profile/+page.svelte` `finishEmailLink`: развилка до входа; «Остаться» — адрес чистится, профиль Б; «Войти» —
      `signOutUser()` и тот же путь, что у отпущенной сессии (шаг «идёт вход» с адресом А).
- [x] Вкладка «письмо отправлено»: подписка `onIdTokenChanged` пока шаг `sent`, уход на `/profile` у вошедшего.
- [x] Набор `qa/suites/signin-link-forks.md` (Б3 + ТАБ + контроли) → ручной прогон на стенде 9/9 → отчёт → заявка.

**Порция 2 — Г2**
- [x] `sendLoginLink`: пометка `from=guest` у двери «Сохранить мои результаты»; юниты `linkFork` (6) и `guestLetter`.
- [x] `SigninScreen.svelte`: шаг `fork-guest` — строки RU дословно с кадра Vg2; EN — черновик, поправлен §7Б.
- [x] `finishEmailLink`: развилка Г2 (сессии нет → вход адресом ссылки; свой гость → привязка к нему, FORK выше).
- [x] Набор дополнен ВС-10…ВС-15 → ручной прогон → отчёт → заявка.
- [x] Строки кадра Г2 на краю «свой гость» — три пары «кадр / предложение» Менеджеру (не в код); строка гостя «письмо
      отправлено» для другого браузера — пара там же.

## Тексты

Строки RU — дословно с утверждённых кадров. Новые строки (EN обоих экранов): `node .kaif/tools/kaif-voice-lint.mjs load`
до первого слова → `check` (у нас SKIPPED — называется словом) → проход §7Б чистым субагентом → в код с пометкой
черновика → список «строки на вычитку владельцу» в отчёте.

## Риски

- (а) Ложный Б3 у самого себя (регистр адреса, пробелы) — сравнение нормализованное, контроль «тот же аккаунт» в наборе.
- (а) Г2 в браузере, где гость ставил оценки (память стёрта) — показ честный: оценок там и правда нет в сессии.
- (б) Перезагрузка вкладки «отправлено» до записи точки гостя в соседней вкладке — перезагрузка читает базу заново,
  профиль придёт с данными; проверяется кейсом ВС-ТАБ.
- (в) Старые письма без адреса — прежнее поведение (Б1), срок жизни ссылки час.

## Решения, принятые без владельца

Порция 1:
- `[AI]` Б3 опознаётся сравнением адресов ДО Firebase, без учёта регистра и пробелов (FORK выше; одобрено Менеджером).
- `[AI]` Вкладка «отправлено» слушает `onIdTokenChanged` и уходит полной перезагрузкой (FORK выше; одобрено Менеджером).
- `[AI]` EN-строки Б3 — черновик агента, поправленный независимым проходом §7Б; RU — дословно с кадра. На вычитку владельцу.
- `[AI]` Адреса на экране Б3 переносятся в любом месте (`overflow-wrap: anywhere`) — длинный адрес на 390 иначе вылезал бы
  за край; тот же приём, что у строки адреса шага «идёт вход».

Порция 2 (заранее, по условию Менеджера): `[AI]` Г2 при СВОЁМ госте во втором браузере — «Войти здесь» привязывает адрес к
этому гостю; цена — пересланная чужая ссылка отдаёт оценки этого гостя в чужой аккаунт (та же, что №084 приняла для входа
без вопроса). Опора — «люди уже заполнили свои измерения, этот труд нельзя потерять».
