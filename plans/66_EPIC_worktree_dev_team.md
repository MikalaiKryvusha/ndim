# План 66 — ЭПИК: команда разработки NDim Space из ИИ-агентов на git worktree

> **Создан:** 2026-08-21 · **Родитель:** `ideas/40` (заказ владельца) + разведка
> `researches/44` · **Статус:** 🔧 в работе — фаза 0 ✅ 2026-08-21 · фазы 1–4 впереди ·
> **Исходящее:** локальные канон-документы `NDIM_WORKTREE_DEV_TEAM_MANIFEST.md` и
> `NDIM_WORKTREE_DEV_TEAM_STATUS.md` · врезка в `AGENT_GUIDE.md` (командный git-режим) ·
> домашка владельцу на первый запуск

**Вектор цели** (из `ideas/40`, словами владельца): организовать работу над NDim Space в
параллель — командой ИИ-агентов по ролям (Менеджер · Дизайнер · QA · Разработчики 1–3), каждая
роль в своей ветке worktree и своём окне VS Code, связь через сообщения, синхронизация через два
новых локальных канон-документа. Подготовить команду к первому запуску: Николай открывает окна и
пишет Менеджеру — Менеджер запускает команду рассылкой задач.

**Критерии приёмки эпика** (все проверяются наблюдением на первом запуске):

1. Владелец открывает окна ролей ПО ДОМАШКЕ (готовые команды копипастой), ни одного шага «разберись сам».
2. Каждая роль стартует со своим именем-адресом, читает манифест и объявляет себя строкой в
   статус-таблице.
3. Сообщение Менеджера доходит каждой роли; ответ роли доходит Менеджеру (замер живьём).
4. Тестовая задача проходит ПОЛНЫЙ конвейер: постановка → работа в ветке роли → вердикт QA →
   мерж Менеджером в `main` (EXP-0176: конвейер до продукта, не «сдал — покатим потом»).
5. Ни один синглтон машины (стенд/порты/докер/бой) не тронут двумя ролями одновременно — замок
   тяжёлых прогонов работает.
6. Канон цел: `npm run kaif:check` зелёный, вера-строка стоит в обоих новых документах,
   `verify-sync-server-name` зелёный.

---

## Решения, принятые агентом (дёшево откатить — по правилу большого пальца)

| Решение | Основание |
|---|---|
| Рабочие места — сиблинг-каталог **`D:\work\ai_sandbox\ndim-team\<роль>`** | вне дерева главной копии (порядок верхнего уровня — заметка владельца в `AGENT_GUIDE`), VS Code открывает обычную папку; `researches/44` §6 |
| Ветки ролей — **`ndim_designer` · `ndim_qa` · `ndim_dev1` · `ndim_dev2` · `ndim_dev3`**, долгоживущие; после вливания в `main` Менеджер перезапускает ветку от свежего `main`. 🔄 Приписка `ndim_` — правка владельца 2026-08-21 вечером (увидел в Worktree Manager старые `team/*` без приписки); переименовано `git branch -m`, инструменты и манифест обновлены | слово владельца «каждую ветку назвать понятным именем согласно её роли» + «поназывать с припиской ndim_» + защита от дрейфа (`researches/44` §1) |
| Имена сессий = адреса: **`manager` · `designer` · `qa` · `dev-1` · `dev-2` · `dev-3`** (латиница — имя набирает владелец в командной строке `claude --name`; кириллические роли остаются в документах) | `researches/44` §3 — имя сессии и есть адрес доставки |
| Менеджер живёт в ГЛАВНОЙ копии (`D:\work\ai_sandbox\ndim`, ветка `main`), без worktree | идея владельца («главная ветка main»); дверь выката и статус-доска у него |
| `npm ci` в каждом рабочем месте; junction НЕ используем для постоянных мест | EXP-0175; изоляция дороже диска |
| Статус-таблица правится ВСЕМИ ролями по абсолютному пути главной копии, через инструмент `node tools/team-status.mjs` (атомарная правка своей строки) | `researches/44` §4 «общая доска»; правка чужих строк запрещена манифестом |
| Замок тяжёлых прогонов — колонка статус-таблицы «Замок стенда», берётся/отдаётся тем же инструментом | синглтоны портов (`researches/44` §4); отдельного файла-замка не заводим — Оккам |

## Фазы

### Фаза 0 — разведка ✅ закрыта 2026-08-21

`researches/44`: индустрия (worktree-стандарт, оркестратор-исполнители), официальная механика
cross-session messaging (Windows ≥2.1.234; у нас 2.1.238; `ListAgents` видит 4 сессии — транспорт
жив), синглтоны машины, оплаченные уроки EXP-0175/0176/0131, столкновения с каноном названы.

### Фаза 1 — канон-документы команды ✅ ЗАКРЫТА 2026-08-21

Манифест + статус-таблица + инструмент строки статуса + шов с действующим каноном.
**Ворота фазы пройдены 2026-08-21:** оба документа существуют с верой-строкой
(`stamp-creed --check` зелёный, 19 документов); `node tools/team-status.mjs` доказан —
самопроверка **10/10** (отказ на чужую строку · замок с именем держателя · перезапись ровно
своей строки) + живой прогон из главной копии (set/lock/unlock) + конкурентный тест (3
параллельные записи, ни одна строка не потеряна); врезка «КОМАНДНЫЙ РЕЖИМ» в `AGENT_GUIDE.md`
→ Git-процесс; строки в `PROJECT_STRUCTURE_EXTERNAL_MAP.md`; `.gitignore` пополнен замком
доски; `kaif:check` ✅ · `verify-sync-server-name` 0 нарушений · `npm run questions` ЧИСТО.

### Фаза 2 — рабочие места ✅ ЗАКРЫТА 2026-08-21

Инструмент `node tools/team-workplaces.mjs` (create/list/reset/remove, уборка по EXP-0175).
**Ворота фазы пройдены 2026-08-21:** самопроверка **8/8** · все 5 мест развёрнуты
(`ndim-team/{designer,qa,dev-1,dev-2,dev-3}`, ветки `team/*` от `main`, отставание 0) ·
`npm test` в КАЖДОМ worktree **318/318, fail 0** (замер) · повторный `create` идемпотентен
(«уже зарегистрирован», 0 изменений) · хуки `.claude/settings.json` в каждом месте (сверка
встроена в create). **Замер фактов:** `npm ci` из кэша — корень 9 с + sync-server 3 с;
вес node_modules ~376 МБ на место (313 + 63), пять мест ≈ 1,9 ГБ.

### Фаза 3 — репетиция связи и конвейера ✅ ЗАКРЫТА 2026-08-21 (протокол — `qa/team-rehearsal.md`)

Живая репетиция headless-сессией роли QA (`claude -p --name qa` в её worktree, модель sonnet).
**Доказано живьём:** имя `--name qa` стало адресом (`ListAgents` показал `qa`) · доставка
постановки мгновенна · строка доски И замок стенда работают ИЗ worktree (роль выведена из
каталога через git — то, чего selftest покрыть не мог) · полный конвейер: постановка по форме
манифеста → коммит `c9a10d4` в `team/qa` → мерж Менеджером `4817c6f` → ветка перезапущена от
свежего `main`.
**Найден настоящий дефект** (ради этого фаза и была): на перезапуске процесса Менеджера его
адрес умер, и исполнитель переслал отчёт сессии ЧУЖОГО проекта. Лечение — правила 9 и 2
манифеста (адрес отчёта в постановке; недоставленное не пересылается посторонним; отчёт
восстанавливается из артефактов).
**Находка для домашки:** `claude` в PATH — 2.1.232 (messaging на Windows нужен ≥2.1.234);
расширение VS Code — 2.1.238 ✅. Окна открывать через VS Code; терминальный CLI до обновления
для команды не годится.

### Фаза 4 — первый запуск с владельцем 🔧 ПОДГОТОВЛЕНО 2026-08-21, ждёт рук владельца

Домашка **`homeworks/14`** написана: все шаги копипастой — `/rename ndim_<роль>` в окнах VS Code
(расширение 2.1.238; терминальный CLI 2.1.232 для команды не годится до обновления), стартовые
промпты ролей, сигнал Менеджеру. Ветки всех пяти ролей перезапущены от `main` с полным
манифестом. Осталось живое: владелец открывает окна по домашке → роли докладываются → владелец
пишет Менеджеру → Менеджер рассылает задачи.
**Ворота фазы = критерии приёмки эпика** (шапка). `/fable-judge` по эпику — до доклада «готово»
(после живого запуска).

## Чего эпик НЕ делает (названо, чтобы не расползтись)

- Не трогает продукт NDim Space — ни одной строки кода приложения; команда строится, чтобы
  дальше СТРОИТЬ продукт.
- Не выбирает модели/тарифы окон — это рука владельца при открытии окон.
- Не автоматизирует найм/увольнение ролей (расширение парка разработчиков — следующая идея).
- Не заменяет STATUS.md проекта: эстафета сессий во времени остаётся как была; статус-таблица —
  синхронизация ролей в моменте.
- Не строит демон-оркестратор поверх сессий: оркестратор — Менеджер-сессия, живой агент по
  манифесту (Оккам; `researches/44` §2).

---

## Операционная детализация фазы 1 (по канону лестницы: детализируется только следующая фаза)

**Шаг 1.1 — `NDIM_WORKTREE_DEV_TEAM_MANIFEST.md`** (корень главной копии).
Содержание — по `ideas/40` дословно + правила из разведки: роли и зоны ответственности (включая
уточнение «разработчики могут всё, фокус — код; низкоуровневые опер-планы пишут себе сами») ·
карта адресов (роль → имя сессии → каталог → ветка) · регламент общения (когда писать, когда
молчать, `notify_when_idle` вместо опроса, эскалация к владельцу ТОЛЬКО через Менеджера,
форма вопроса №043) · git-дисциплина (ветка роли от свежего `main`, частые коммиты, мержит
только Менеджер после вердикта QA) · ресурсы-синглтоны и замок · граница с каноном (каждая
роль работает по KAIF в своей зоне; что из канона принадлежит какой роли).
✅ Проверка: вера-строка стоит (прибор), `kaif:check` зелёный, документ читается за один присест.

**Шаг 1.2 — `NDIM_WORKTREE_DEV_TEAM_STATUS.md`**: таблица «роль · сессия · занят/свободен ·
что делаю · жду от кого · замок стенда · метка времени». Шесть строк-слотов, шапка-инструкция
из трёх строк. Документ обязан НЕ РАСТИ (слово владельца) — только перезапись строк.
✅ Проверка: строки перезаписываются инструментом, файл не растёт после 10 обновлений подряд.

**Шаг 1.3 — `node tools/team-status.mjs`**: `set <роль> --busy|--free --doing "…" --waiting
"…"` · `lock-stand <роль>` / `unlock-stand <роль>` (отказ, если замок у другого) · `show`.
Правит ТОЛЬКО строку названной роли; пишет по абсолютному пути главной копии (работает из
любого worktree); метка времени сама.
✅ Проверка доказательством: мутация «правка чужой строки» отказана · два конкурентных `set` не
теряют ни одной строки · `lock-stand` при занятом замке отказывает с именем держателя.

**Шаг 1.4 — шов с каноном:** врезка в `AGENT_GUIDE.md` → «Git-процесс» (командный режим: ветки
ролей, мержит Менеджер; соло-сессии — по-старому в `main`); оба новых документа — в `DOCS`
прибора `tools/stamp-creed.mjs` и прогон прибора; строка о команде в `PROJECT_STRUCTURE_EXTERNAL_MAP.md`.
✅ Проверка: `node tools/stamp-creed.mjs --check` зелёный; `npm run questions` без новых нарушений.

**Шаг 1.5 — ворота фазы:** прогнать все проверки шагов + `verify-sync-server-name` (новые
документы не нарушают бренд-имя) + отметить фазу здесь и в `ideas/40`.

---

## Операционная детализация фазы 2 (дописана после закрытия фазы 1 — по канону лестницы)

**Шаг 2.1 — `node tools/team-workplaces.mjs`** с командами:
- `create <роль>|--all` — идемпотентно: каталог `D:\work\ai_sandbox\ndim-team\<роль>` как
  worktree на ветке `team/<роль>` от текущего `main` (существующая ветка переиспользуется) +
  `npm ci` в корне worktree и в `sync-server/` (свой node_modules — капкан «createRequire от
  него», STATUS) + сверка, что хуки (`.claude/settings.json`) приехали.
- `list` — worktree, ветки, отставание от `main`, наличие node_modules.
- `reset <роль>` — перезапуск ветки роли от свежего `main` (шаг Менеджера после мержа);
  ОТКАЗ при грязном дереве роли.
- `remove <роль>` — уборка ПО `EXP-0175`: скан reparse-точек (junction) в известных местах
  (корень worktree и `sync-server/`) → снятие `cmd /c rmdir` → `git worktree remove`; ветка
  НЕ удаляется (коммиты — ценность).
✅ Проверки: `--selftest` на чистых функциях (имена/пути/решение об отказе) + живой цикл
`create → list → reset → remove` на пробной роли.

**Шаг 2.2 — развернуть все 5 рабочих мест** (`--all`), замерить и записать факт: время
`npm ci` и вес node_modules на worktree (разведка обещала замер фактом).

**Шаг 2.3 — решение по `.env`:** в worktree НЕ копируется по умолчанию (наименьшие права:
исполнители работают на эмуляторах, ключи боя — зона Менеджера). Маршрут выдачи явный: задача,
чей конвейер требует ключей, получает их ОТ Менеджера при постановке (урок `EXP-0176` —
конвейер не режется по выдуманной посылке о доступе; посылка здесь ПРОВЕРЕНА: `.env` в свежем
worktree отсутствует по построению, это замер, а не догадка).

**Шаг 2.4 — ворота фазы:** 5 каталогов живут · `git worktree list` совпадает с картой манифеста ·
в каждом worktree `npm test` зелёный (замер) · повторный `create --all` идемпотентен (0 изменений).
