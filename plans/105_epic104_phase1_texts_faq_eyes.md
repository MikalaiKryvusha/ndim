# План 105 — эпик 104, фаза 1: тексты новой V1, FAQ и зрение пути

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16.
<!-- ВЕРА-КОНЕЦ -->

> **Создан:** 2026-09-25 ≈01:4x (агент Opus 5.5) · **Родитель:** `plans/104_EPIC_people_path_from_video_to_relations.md`,
> фаза 1 · **Статус:** 🔵 в работе с 2026-09-25 · дорожка А ответами владельца закрыта до А6 (2026-09-25 08:42) ·
> **Исходящее:** — (заголовок, FAQ и демо отвечены: интервью №096 В14–В16).

**Якорь в метаэпике** (цитата строки фазы 1): «*заголовок главной (четыре варианта на словах поиска, выбор владельца) ·
FAQ по разведке · прибор числа недели · `entry` у `guest_start` · браузер у `landing_view` · журнал выкатов*».

## Вектор цели фазы

- **Achieve:** тексты новой V1 (заголовок, FAQ, новые строки) написаны словами самых сильных запросов, голосом
  владельца и приняты им; путь человека виден числом недели и признаками прихода.
- **Maintain:** структура V1 не меняется (утверждена владельцем); код продукта в этой фазе меняется только в
  аналитике и двери выката; каждый новый прибор несёт тестовую документацию и отчёт о ручном прогоне.
- **Avoid:** текст агента без портрета и без §7Б; число прибора без контроля; утверждение о функции, которого продукт
  не делает.

## Критерии приёмки фазы

1. Владелец выбрал заголовок главной и принял FAQ построчно — ответы интервью лежат в `interviews/`.
2. `node tools/week-number.mjs --week 2026-09-14` печатает пять чисел недели ролика, контроль не ноль; отчёт прогона в
   `qa/reports/`.
3. Событие `guest_start` в стейдже несёт `entry` из закрытого союза `ANALYTICS_ENTRIES` (`src/lib/data/analytics.ts:249`;
   сегодня `landing · catalog_card · direct`, для тестов союз дополняется); прибор `verify-probe-mark` зелёный.
   *(поправка 2026-09-25 02:06: прежняя строка называла «landing · card · root · test» — пересказ, а не код)*
4. `landing_view` корня несёт браузер и признак встроенного браузера; снято на стейдже.
5. `npm run deploy -- --stage` дописывает строку в журнал выкатов; строка прочитана глазами.

## Шаги

### Дорожка А — тексты

- [x] **А1.** Четыре заголовка главной на словах поиска — в макете `design/new-landing-v1.html`, кнопки А–Г; частоты
  Вордстата в пояснении (2026-09-25).
- [x] **А2.** Независимая проверка заголовков по портрету (§7Б, субагент): А, Б, В — годны с правками; Г переписан
  (прежний говорил о функции неправду). Правки внесены; описания выдачи ≤ 160 знаков (2026-09-25).
- [x] **А3.** *(2026-09-25: 12 вопросов в трёх группах в макете; разведка — Google без особых результатов FAQ с 05.2026, у Яндекса разметки нет, небольшие продукты — 4–8 на главной; три ответа — принятые владельцем, девять новых прошли §7Б ≈01:5x, коммит `e80b0a8`: вопрос «как познакомиться» говорил о контактах и «Написать», которых в продукте нет, — заменён на «Что видно в карточке человека в Связях?»; «тем же способом» (З2) и ложная тайна оценок исправлены; отрисованный текст 4 вариантов — 0 запретных слов, консоль 0)* FAQ: разведка (Google, Яндекс, индустрия — число вопросов, форма ответов) → список вопросов языком
  запросов людей (`researches/30_demand_ru.json`, `researches/74` §4) → ответы по портрету (прямой ответ первой фразой)
  → §7Б чистым экземпляром → в макет. *Проверка:* число вопросов и группы соответствуют разведке; в тексте ноль
  «вымышлен»; страж `guard-product-copy` зелёный; текст, отрисованный скриптом, проверен по отрисованной странице.
- [x] **А4.** Новые строки V1 («Ваши Связи» и пояснение, заголовок демо, строки характера персонажей) — §7Б.
  *(2026-09-25 02:04: 12 агентских строк прошли проверяющего чистым экземпляром со сверкой фактов по `src/`; канон
  владельца (подзаголовок 1.x про свайпы, «Знакомства нового измерения», плашка «Ваша самая сильная связь», кнопка
  двери, подвал) не судился. Находка — ошибка факта: «Ваши Связи» над карточками персонажей называл их Связями
  человека, а в Связях их нет (`src/lib/ui/handhold.ts:112` — «Это была демонстрация») → «Ваша Похожесть с Максом,
  Алисой и Настей» + пояснение из подсказки 1.x (`src/routes/relations/+page.svelte:281`). Ещё: имя в строках
  персонажей (З4), «три из пяти фильмов и сериалов», «по Вашим любимым фильмам, сериалам, книгам и играм» вместо «по
  тому, что» (З6), «Пространство NDim Space в числах» вместо «сейчас» (числа снимаются перед выкатом), «Оцените
  звёздами…» в демо. Отрисовка 4 вариантов — 0 запретных слов, консоль 0, кадр 390 просмотрен; пояснение макета
  называет владельцу правки после его «утверждаю».)*
- [x] **А5.** Интервью владельцу: выбор заголовка (А–Г), FAQ построчно, №096 В14 (фильмы или качества в демо). Одной
  страницей. *Проверка:* `npm run questions` — ответы разнесены с цитатой «интервью №NNN, ВN».
  *(2026-09-25 02:1x: вопросы стоят в №096 — В14 (голоса всех пяти фильмов демо), В15 (четыре заголовка + строки выдачи),
  В16 (12 вопросов FAQ после §7Б); `npm run review -- render` чист.)*
  *(✅ 2026-09-25 08:42, страницей: **№096 В15 = А** — заголовок «Знакомства по интересам в Пространстве NDim Space», его
  подзаголовок и строка выдачи варианта А · **№096 В16 = А** — все 12 вопросов и ответов FAQ как в макете · **№096 В14** —
  свой ответ: «*смесь ТОП фильмов, сериалов, качеств - то, что знают или слышали 99% всех людей, очень популярных
  расхожие вещи. Можно до 10 штук, сделать компактно по вертикали список*». Демо меняется в фазе 2 `plans/104`; список
  объектов идёт владельцу на утверждение. Кнопки А–Г в `design/new-landing-v1.html` оставлены — макет хранит выбор, код
  фазы 2 берёт вариант А.)*
- [ ] **А6.** Английская версия текстов — после принятия русских.

### Дорожка Б — зрение пути

- [x] **Б1.** *(2026-09-25: `tools/week-number.mjs` + `tools/lib/posthog.mjs`, набор `qa/suites/week-number.md`, отчёт `qa/reports/2026-09-25_week-number.md` — pass, НВ-06 ждёт первого возврата; неделя ролика: 160 · 9 · 3 · 6 · 0)* Прибор числа недели `tools/week-number.mjs`: HogQL через читающий ключ `.env`
  (`POSTHOG_NDIM_SPACE_PERSONAL_API_KEY`), `env=prod`, по `person_id`; пять чисел + окно; ноль печатается дробью;
  контроль — неделя 14–20.09. Тест-кейсы и чек-лист — `qa/suites/week-number.md`; отчёт прогона — `qa/reports/`.
  Основание: слово владельца №096 В3 = А («раз в неделю Вы получаете одно число»).
- [x] **Б2.** `entry` у `guest_start` (`src/lib/data/analytics.ts`, объявлено и не заполняется).
  *(✅ 2026-09-25 09:08 (коммит `839255a`): место входа едет у `guest_start` от каждой двери — `root` · `landing` · `catalog_card` · `signin` ·
  `restart` · `test` · `direct`; союз и `entryOf()` переехали в `funnel.ts` (профилю не нужен чанк аналитики), `track(step,
  props)`, `saveTestRating(…, entry)` — гостя карточки рождает звезда, и без этого он записывался бы как `test`. Ручной
  прогон сборки под именем стейджа ЕВ-01…ЕВ-07 — 0 провалов, дверь `restart` живьём не пройдена, отчёт `qa/reports/2026-09-25_guest-entry.md`, кейсы —
  `qa/suites/analytics.md` §12. «Политика» п. 2.6 сверена — правка не нужна.)*
  `FORK: options <А — слово двери значением самого параметра ?guest=<слово> | Б — след в sessionStorage по образцу
  rated-trace.ts | В — второй параметр &from=> · price of error <малая: неверное место портит разрез, продукт не ломает; Б
  оставляет протухший след и приписывает вход чужой двери, а у острова карточки нет модулей — след стоил бы кода острова
  на 10 222 страницах; В — вторая сущность в адресе> · consulted <отраслевая практика меток кампании в самой ссылке
  (UTM); образец в коде — одноразовый параметр guest, срезаемый replaceUrl (profile/+page.svelte:463–468); урок веса
  острова rated-trace.ts:106–108>` — выбрано А `[AI]`; прежняя рекомендация Б (ниже, 02:06) пересмотрена этим разбором.
  *Разведка кода 2026-09-25 02:06 (код не менялся):* союз мест входа уже есть и закрыт — `ANALYTICS_ENTRIES =
  ['landing', 'catalog_card', 'direct']` (`analytics.ts:249`, «⛔ Новое место входа добавляется СЮДА»); прежняя строка
  плана «landing · card · root · test» была пересказом и поправлена. `track(step)` (`src/lib/data/funnel.ts:216`)
  свойств не принимает — правка: `track(step, props?)` → `capture(step, props)`. Точки `guest_start`:
  `src/routes/profile/+page.svelte:461` (дверь `?guest=1`) и `src/lib/data/test-engine.ts:63` (место входа известно само —
  тест; в союз добавить `test`). На `/profile?guest=1` ведут ПЯТЬ дверей *(поправка 02:2x по суду: прежде «три», и
  адреса двух стояли на комментариях)*: кнопка корня `src/routes/+page.svelte:263` · демо лендинга — константа `DEMO_URL`
  `src/routes/[lang=lang]/+page.svelte:67` (в `SimilarityDemo` уходит как `appUrl`) · дверь карточки каталога — `DOOR_HREF`
  `src/routes/[lang=lang]/dimension/[slug]/+page.svelte:67` (ссылки :547, :559) · «гостем» на экране входа —
  `continueAsGuest` `src/routes/profile/+page.svelte:559` (туда же ведёт тихая ссылка корня `/profile`, `+page.svelte:267`) ·
  «начать заново» после истёкшей гостевой сессии — `restartAsGuest` `:568`. Все пять шлют `guest_start` одной строкой
  `:461`, и профиль не знает, какая дверь привела человека.
  `FORK: options <А — дверь кладёт место входа в адрес ?guest=1&from=<место>, профиль читает и срезает вместе с guest (:467) |
  Б — дверь оставляет след в sessionStorage по образцу rated-trace.ts / markBridgeCrossed, профиль читает след> · price of
  error <малая: неверное место входа портит один разрез аналитики, продукт не ломает; А виден в адресе и в закладке, Б
  умирает с вкладкой> · consulted <готовый образец в коде — src/lib/door/rated-trace.ts:16–17, 85>`. Рекомендация `[AI]`: Б —
  образец уже в коде, адрес остаётся чистым. Значение вне союза в обоих случаях отбивает `propertyValueIsSafe`. «Политику» п. 2.6 перечитать при правке (правило №082 В3). Проверка — юнит `track` со
  свойством + проба стейджа с перехватом отправки по образцу `tools/probe-signin-failed-live.mjs`, отчёт `qa/reports/`.
  Место входа корня с новой V1 (`landing` или новое слово) — решить вместе с фазой 2.
  Правка «Политики» не нужна (новое свойство — вид двери, предмет оценки не уходит) — сверить по правилу «трогаешь
  аналитику — перечитай Политику».
- [ ] **Б3.** Почему у `landing_view` корня нет браузера (138 из 147 событий за 18–24.09; за неделю ролика — 146 из 152 людей): разведка кода отправки → браузер и признак
  встроенного браузера (Instagram / Facebook) в событии.
  *Разведка 2026-09-25 01:48:* корень шлёт событие ОДНОЙ ИНЛАЙН-СТРОКОЙ без SDK (`src/lib/data/root-landing-view.ts`,
  решение владельца №078 В1 = Г: главная без кода) — поэтому `$browser`, `$os`, `$raw_user_agent` в событие не попадают;
  `distinct_id` случайный на визит и `$process_person_profile: false` — человек только с главной для прибора числа
  недели каждый визит новый, и «вернулся» для него НЕ измерим. Лечение [AI]: строка корня кладёт признак встроенного
  браузера (`Instagram|FBAN|FBAV|FB_IAB` в user agent) и семейство браузера; с новой V1 на главной (фаза 2) главная
  получает код — и тогда вопрос «строка или SDK на главной» решается заново (развилка с ценой веса страницы).
- [ ] **Б4.** Журнал выкатов: `tools/deploy.mjs` дописывает строку в `qa/deploys.md` — дата и время с часов, sha,
  контур, людей всего и живых за 7 дней до выката. Юнит двери выката остаётся зелёным.
  *Разведка кода 2026-09-25 02:08 (код не менялся) — 🔴 постановка шага ломает дверь:* замок боя требует, чтобы коммит
  расписки стейджа равнялся текущему и дерево было чистым (`requireStagePass`, `tools/deploy.mjs:432–445`; «грязное» —
  любой вывод `git status --porcelain`, `:264–267`). Строка в ОТСЛЕЖИВАЕМЫЙ `qa/deploys.md` после выката на стейдж
  пачкает дерево, а её коммит меняет коммит — в обоих случаях следующий выкат в бой откажет. Свои журналы дверь уже
  держит вне git (`.kaif/stage-bypass.log`, `.kaif/stage-receipt.json` — `.gitignore:73–75`).
  `FORK: options <А — выводить журнал из истории выпусков Firebase Hosting (REST sites.releases.list), а дверь кладёт в
  выпуск sha и контур флагом firebase deploy --message (флаг есть — firebase deploy --help) | Б — строка в журнал вне git,
  .kaif/deploy-journal.log, по образцу stage-bypass.log | В — отслеживаемый qa/deploys.md с исключением в проверке
  чистоты дерева> · price of error <В пробивает замок стейджа: исключение в проверке чистоты — дыра, через которую
  однажды уедет непроверенная правка; Б живёт на одной машине и теряется с ней> · consulted <устройство замка двери
  (код выше); справка firebase deploy; REST sites.releases.list — прочитан 2026-09-25 02:09 через
  `gcloud auth print-access-token`: бой `ndim-space` — страница из 100 выпусков и есть следующая (самый ранний на
  странице 2026-07-01), стейдж `ndim-stage` — 63 выпуска с 2026-08-15; у каждого `releaseTime`, `message` пуст (дверь
  флаг не передаёт); последний выкат в бой — 2026-09-19T21:02Z = 2026-09-20 00:02 +03:00>`. То есть журнал выкатов
  УЖЕ существует у хостинга (прочитана первая страница, самый ранний на ней — 2026-07-01); не хватает только sha в выпуске.
  Рекомендация `[AI]`: А (журнал не пишется, а выводится — как «ждёт сборки» в `plans/58`), запасной — Б. Критерий 5
  фазы читать по выбранной форме.
- [ ] **Б5.** Метка ссылки ролика: `tools/video/caption.mjs` ведёт на `ndimspace.app` (главная — №096 В6 = Б) с меткой
  номера ролика.
  *Сверка 2026-09-25 01:51:* сегодня ссылка ведёт на `/ru` с `utm_*` — главная метки отрезает (`caption.mjs:10`). Переключать
  на главную — ТЕМ ЖЕ выкатом, что новая V1 на главной (фаза 2 эпика 104), и только если новая главная метки сохраняет
  (кейс в наборе фазы 2).

## Проверка наблюдением

Каждый шаг Б — ручной функциональный прогон на стейдже (прибор на живых данных, вывод прочитан глазами) и отчёт
`qa/reports/<дата>_<работа>.md` с двумя строками «Гигиена» и «Функциональный прогон». Тексты — кадры макета обеих
тем и двух ширин, просмотренные агентом до показа владельцу.

## Риски

| Уровень | Риск | Что делаем |
|---|---|---|
| а | Заголовок на ключевых словах звучит не голосом владельца | портрет до письма, §7Б после, вычитка владельцем |
| а | PostHog-ключ читающий отсутствует в `.env` на машине | проверить до Б1; нет — одна строка владельцу в чат |
| б | Правка аналитики меняет обещанное «Политикой» | перечитать «Политику» до правки (стоячее правило владельца) |
| б | Журнал выкатов в двери ломает дверь | юнит `tools/deploy.test.mjs` и `--gate-only` до и после; ⚠️ 2026-09-25 разведка Б4: запись в отслеживаемый файл ломает замок боя ПО ПОСТРОЕНИЮ — форма журнала выбирается развилкой Б4 |

## Решения, принятые без владельца

Заполняется при закрытии фазы.
