# План 75 — Дверь карточки каталога ведёт в гостя (лечение `bugs/200`)

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16.
<!-- ВЕРА-КОНЕЦ -->

> **Создан:** 2026-08-28 (сессия 40) · **Статус:** 🟢 КОМПОЗИЦИЯ ВЫБРАНА — №049 В1 = А +
> №050 = **Д3 «Панель после касания» С ПРАВКАМИ владельца** (чат 2026-08-28, дословно в
> №050): центр «Ваша оценка засчитана!» · строка «В Пространстве NDim множество людей — оно
> поможет Вам найти тех, кто разделяет Ваши взгляды.» · большая синяя кнопка «Войти в
> Пространство» с пульсацией по контуру · карта узлов с ТОНКИМИ ЛИНИЯМИ СВЯЗЕЙ, пульсирующими
> светом («сеть живёт, как нейронная»). Эталон вида — `design/catalog-door-mockups.html`,
> вариант Д3. Ш1 разблокирован · **Родитель:** `bugs/200` + аудит
> `2026-08-28_growth_funnel_audit.md` · **Родня:** `plans/67` (мост лендинга — образец
> исполнения и приёмки) · `plans/23` (эпик мягкого ведения — та же семья) ·
> **Опора решений:** №009 В3 («вход анонимом замаскировать в интерактив») · №041 В3 = А
> («ворота первыми»)

## Цель (вектор)

Человек из поиска, нажавший на карточке «Оцените, чтобы найти людей, кто думает, как Вы»,
оказывается в продукте гостем — без единой формы по дороге. Та же механика, что у моста
лендинга (`/profile?guest=1`), на настоящей поверхности притока — 10 222 карточках.

## ⛔ Инвариант владельца — КАРТОЧКУ НЕ ОБЕДНЯТЬ

Его слово (№050, чат): «*чтобы не получилось, что этим макетом обедним наши уже сделанные
двери*». Макет — эскиз ОДНОГО блока, а не всей страницы: «〈сокращено〉» в нём — приём рисунка, не
замысел продукта. В бою карточка остаётся ПОЛНОЙ, меняется только низ блока двери
(кнопка → звёзды + панель). Что обязано остаться нетронутым, по живой структуре страницы:
шапка с языками и «Войти» · ссылка в хаб вида · заголовок + оригинальное название · досье
целиком (оценка/«Ещё без голосов», Вид, Год, Автор, Место в своём виде, Теги) · **полное
описание объекта** (канон мастерской 900–1400 знаков) · тексты двери (doorTitle +
doorVoted/doorNoVotes — слова владельца) · «Похожие по каталогу» · подвал. Приёмка Ш4
включает diff секций «до/после»: исчезнувшая секция = красный.

## Критерии приёмки

1. Клик по двери карточки в БОЮ → человек внутри гостем; форм — ноль (живой прогон + кадры).
2. Живая сессия уважается: вошедший человек по той же двери попадает в свой профиль, гостя
   ему не заводят (канон `bugs/95` — дверь `?guest=1` это уже умеет).
3. Счётчик `door_click` (фаза 1 `plans/74`) в бою ненулевой в первую же неделю.
4. Вес карточки не вырос сверх допуска стража `verify-catalog-hubs`; пререндер целостен.
5. `bugs/201` уезжает тем же выкатом (правка «30 дней» → срок из `GUEST_TTL_DAYS`).

## 🔬 Разведка исполнения (сессия 40, 2026-08-28) — читать ДО первой строки кода

1. 🔴🔴 **Карточки собраны с `csr = false`** (`dimension/[slug]/+page.server.ts:71`) —
   клиентского JS у них НЕТ ВООБЩЕ, и это решение куплено замером (там же, строки 55–70:
   минус 10 222 файла `__data.json` ≈ 41 МБ на релиз; страница без бандла фреймворка).
   ⛔ **НЕ включать `csr = true` «чтобы заработали звёзды»** — это откат оплаченной
   оптимизации на всей SEO-поверхности (замер по build/: страница Теста с гидрацией несёт
   25 модульных ссылок, карточка — 4).
2. ✅ **Выбранная архитектура — «ванильный остров»:** звёзды и панель уже в пререндеренном
   HTML (панель скрыта классом); маленький инлайн-скрипт (ванильный, ~1–2 КБ) вешает
   обработчики; при ПЕРВОМ касании динамически грузится «движок двери» — отдельный чанк,
   собираемый вторым входом Vite из тех же исходников (`test-engine.ts` →
   `saveTestRating`: слой данных чист от `$app/*` и Svelte — проверено грепом импортов).
   Так карточка остаётся без фреймворка, а Firebase грузится только тому, кто тронул звёзды.
3. **Движок уже существует и доказан боем:** `src/lib/data/test-engine.ts` →
   `saveTestRating(lang, dimId, value)` — «первая оценка = вход»: рождает гостя, заводит
   пространство, пишет НАСТОЯЩУЮ оценку тем же `saveRating`, шаг воронки. В бою на страницах
   Теста с 2026-08-14. Не переизобретать.
4. **Звёздный ряд — канон продукта:** 11 позиций 0…10 С ЦИФРАМИ (память владельца: звёзды
   1.x; образец — `test/[slug]/+page.svelte:477–494`). Макет рисовал упрощённо (10 звёзд без
   цифр) — исполнение следует канону жеста, композиция Д3 не меняется.
5. **Гонка «коснулся → сразу нажал кнопку»:** сохранение асинхронно (рождение гостя +
   Firebase-чанк — секунды). Кнопка «Войти в Пространство» обязана ДОЖДАТЬСЯ обещания записи
   (preventDefault → await → переход), иначе оценка потеряется на пороге.
6. **Кэш движка двери:** чанк с фиксированным именем НЕ класть под immutable-кеш (класс
   `bugs/124`); либо хеш в имени + инлайн-скрипт узнаёт его из данных пререндера, либо
   короткий кеш заголовком в `firebase.json`.
7. **Счётчик «открыл карточку» НЕ делаем клиентом** — он тащил бы Firebase на каждый просмотр
   SEO-поверхности ради числа, которое отдаёт Search Console. Воронка каталога: `door_click`
   (касание — Firebase в этот момент и так грузится) + `signin_wall_view` (профиль). Разбор —
   `bugs/202`, поправка к его пункту 1.
8. **Витрина лендинга заденется дважды** (`landing-copy.ts:224`): «считаем четыре числа в
   день» устареет с новыми счётчиками, «ни одного стороннего трекера» — с исполнением В2 = В.
   Обе правки текста — ТОЛЬКО через вычитку владельца, тем же выкатом, что их причина.

## Шаги

- [x] **Ш0. 4 макета двери** — ✅ нарисованы 2026-08-28 (`design/catalog-door-mockups.html`:
      Д1 кнопка-мост · Д2 звёзды на карточке · Д3 панель после касания · Д4 дверь-витрина;
      обе темы, 390/1024/1440, кадры сняты headless Edge и смотрены глазами). Выбор владельца —
      интервью №050 (страница поднята). Прежний состав кандидатов ниже — история постановки.
      (канон «Дизайн»; ПОСЛЕ «А» в №049 В1, ДО кода). Кандидаты
      композиций: A — текущая кнопка, но href в гостя · B — звёзды прямо на карточке (оценка
      первым касанием, как в демо) · C — панель-мостик после касания (как на лендинге, Ш4
      `plans/67`) · D — синтез по мотивам выбора владельца в `plans/65` (мостик + пульсация).
      ⚠️ Уроки: владелец выбирает не то, что называет, — рисовать все четыре
      (`owner-picks-differently-than-he-names`); тексты двери — его слово, без вычитки не
      менять.
- [x] **Ш1. Дверь ведёт в гостя** по выбранному макету — ✅ сделано 2026-08-28 (dev-1, ветка
      `ndim_dev1`). «Ванильный остров»: звёзды 0…10 с цифрами и скрытая панель приезжают
      ПРЕРЕНДЕРОМ, обработчики вешает инлайн-скрипт (2 498 байт в странице), движок грузится
      отдельным чанком по первому касанию (`vite.door.config.ts` → `src/lib/door/engine.ts` →
      `saveTestRating`). `csr` карточек остался `false`. Адрес двери — `/profile?guest=1` в
      пререндере, `APP_URL`/`DEMO_URL` не тронуты. Кнопка ждёт обещание записи (потолок 8 с).
      🔑 **Запасная дверь:** обычная ссылка стоит видимой и прячется классом `door-js` из
      головы документа — JS выключен или остров упал, и человек видит сегодняшнюю дверь.
- [x] **Ш2. Первый экран гостя из карточки** — ✅ замер снят 2026-08-28 (кадр
      `test-results/probe-catalog-door/after-door-390.png`, прогон на стенде под замком).
      **Что человек встречает:** экран «Профиль», карточка гостя, две двери («Сохранить мои
      результаты» · «У меня уже есть аккаунт») и «позже».
      🔴 **Объекта, с которого он пришёл, на первом экране НЕТ**, и рядом стоят две строки,
      которые спорят с дверью: заголовок «Сейчас Вы гость — **Ваши результаты не сохранены**»
      и блок NDim ID «не заполнено» — при том что дверь только что сказала его словами «Ваша
      оценка **засчитана**!». Оценка при этом ЖИВА: прибор читает её из базы у новорождённого
      гостя (значение 8).
      ⚠️ **Честная граница замера:** виден ли объект на экране «Измерения», на стенде выяснить
      НЕЛЬЗЯ — в засеянном каталоге стенда 49 измерений, и того, что оценила дверь, среди них
      нет (проверено REST-ом эмулятора: 404). Это артефакт стенда, а не факт продукта; замер
      повторить на стейдже.
      ⏭ **Кода не написано ни строки** — по постановке. Вопрос владельцу собран в отчёте
      Менеджеру: показывать ли вошедшему из карточки объект, с которого он пришёл, и что
      делать с расхождением «засчитана» ↔ «не сохранены».
      ✅ **ОТВЕТЫ ВЛАДЕЛЬЦА ПОЛУЧЕНЫ (страница №053, 16:47) — вопрос из отчёта закрыт:**
      ✅ **В1 = А (интервью №053) — показать оценённый объект + согласовать тексты, его слова дословно:**
      «*не нужно пугать - что результаты не сохранены. Они сохранены, но в анонимный аккаунт,
      который нужно доделать до полноценного аккаунта, чтобы навсегда сохранить результаты.
      Вот это нужно мягко показать, объяснить, но не пугать. Мы рады приходу пользователя,
      ведём его мягко и понятно в функционал, но чтобы он помнил, что нужно бы аккаунт
      полноценный завести*». ⚠️ Слово «навсегда» — из его чата, НЕ на лицо продукта
      (правило текста «НИКОГДА „навсегда"» действует). Тексты состояния гостя — кандидаты
      на вычитку. Исполнение — ОТДЕЛЬНЫМ шагом после выката двери (чтение одного документа
      каталога на первом экране гостя + правка строк).
      ✅ **В2 = А (интервью №053) — честная строка об отказе записи, его слово: «*можно без моей вычитки, это
      редкое событие - ошибка*»** — формулировку пишет агент по портрету голоса, вычитки не
      ждёт; едет тем же исполнением Ш2-текстов.
- [x] **Ш3. Стража** — ✅ сделано 2026-08-28. Прибор `tools/probe-catalog-door.mjs` по образцу
      `probe-bridge.mjs`: **29 проверок, 0 провалов** на стенде под замком (незнакомец из
      поиска · клик двери · живая сессия · «карточку не обеднять» · кадры обеих тем × 390/1440
      · контроль без JS). Вердикт записи снимается С БАЗЫ И С ЭКРАНА.
      **Доказан двумя мутациями:** остров не сохраняет оценку → 1 провал («гость не рождён»);
      адрес двери возвращён на `/profile` → 3 провала, и мутант ВОСПРОИЗВЁЛ сам `bugs/200`
      («меток гость: 0»). Возврат — 29/0.
      Юниты: `src/lib/door/island-core.test.ts` (8 проверок — исполнимость вырезанного кода,
      охрана допущения разборщика, ПОТОЛОК ВЕСА острова, ожидание записи) и
      `src/routes/profile/guest-facts.test.ts` (`bugs/201`, 3 мутации).
- [ ] **Ш4-подготовка (хвосты, назначенные Менеджером 2026-08-28):**
      · [x] **счётчик `door_click` включён 2026-08-28** по сигналу Менеджера (Ф1 влита в `main`).
        🔴 При включении найден и вылечен ДЕФЕКТ РАЗМЕЩЕНИЯ ВЫЗОВА, а не опечатка: счётчик,
        вызванный ДО записи оценки, терялся у человека БЕЗ сессии — вход, который это же
        касание и запускает, меняет состояние авторизации, и Firestore рвёт незавершённую
        запись (`ERR_ABORTED`). Управляемый опыт: касание без сессии `door_click` 2 → 2,
        касание при живой сессии 2 → 3. Цена, если бы не поймали: воронка не считала бы ровно
        НЕЗНАКОМЦА ИЗ ПОИСКА — того, ради кого дверь построена. Лечение: вызов в `finally`
        после записи оценки; контракт dev-2 соблюдён (счётчик не в цепочке, уходит `void`-ом).
        ⏭ Остаточное ограничение, названное честно: человек, нажавший «Войти в Пространство»
        в ту же секунду, что и звезду, свой `door_click` теряет — переход убивает запись.
        Лечится только сцеплением счётчика с переходом, что контракт запрещает; принято.
      · **повторить замер Ш2 на СТЕЙДЖЕ** — виден ли вошедшему из карточки объект на экране
        «Измерения». На стенде это выяснить нельзя: в засеянном каталоге 49 измерений, и того,
        что оценила дверь, среди них нет (REST эмулятора: 404);
      · выкат ОДНИМ СОСТАВОМ: дверь + фаза 1 «Зрения» + `bugs/201` + правка витрины лендинга
        через вычитку владельца.
- [ ] **Ш4. Выкат:** стейдж → кадры обеих тем × 390/1440 → бой дверью → смоук под сессией →
      чтение `door_click`/`guest_start` через неделю (ворота приёмки — числа, не декларация).

## Замеры исполнения Ш1 (числа, а не впечатление)

| Что | Значение |
|---|---|
| Вес карточки ДО / ПОСЛЕ (`/ru/dimension/1-kla-yfoyymct`) | **30 230 → 37 503 байта** (+7 273, +24 %) — обе цифры сняты сборкой того же дерева |
| Инлайн-остров в странице | **2 498 байт** (было 5 607 с комментариями; режутся на пути в страницу, потолок стережёт юнит) |
| Чанк движка двери | 717 КБ на диске, **~212 КБ gzip** — грузится ТОЛЬКО по касанию звезды |
| Клиентский бандл сайта | **1,4 МБ** при потолке стража 3 МБ — не изменился |
| `verify-catalog-hubs` | **93 / 0** |
| `verify-dimension-pages` | **40 / 0** |
| `verify-product-vocabulary` | **12 / 0**, страниц 302 |
| `verify-sync-server-name` | 0 нарушений |
| Юниты проекта | **397 / 0** |

⚠️ **Названо честно:** +7,3 КБ на страницу — цена самого макета Д3 (ряд из 11 кнопок с
подписями, карта узлов, тексты панели), а не оплошность. До gzip; хостинг сжимает. Страж хабов
мерит КЛИЕНТСКИЙ БАНДЛ, а не вес HTML, — то есть этот рост он поймать не может по построению.
Если владелец или QA сочтут рост дорогим, дешёвые резервы названы: короче `aria-label`
звёзд и меньше линий в карте узлов.

## Риски

- **Наплыв гостей = наплыв точек:** каждый гость — полноправная точка на 7 дней; успех двери
  приближает потолок сервера синхронизации (`MASTER_PLAN` фаза 4, риск роста). Лечение по
  канону: замер до оптимизации — счётчики фазы 1 `plans/74` и есть замер.
- **Информационный интент:** часть пришедших хочет «plot summary», а не знакомств; дверь
  обязана звать, но не обманывать — тексты не трогаем, обещание («найти людей») уже честное.
- **Кэш карточек:** страницы пререндерены и кешируются; после выката проверить свежесть
  отдаваемого HTML (`verify-prod-cache`, `bugs/124`).
