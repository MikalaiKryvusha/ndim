# План 41 — эпик 40, фаза 1: «Приборы — панель притока, карта покрытия, добор спроса»

> **Создан:** 2026-08-14 · **Родитель:** `plans/40_EPIC_semantic_core_v2.md` → Фаза 1 ·
> **Статус:** ✅ ФАЗА ЗАКРЫТА 2026-08-14 — все шесть шагов за одну ночную сессию (сессия
> владельца в chrome-debug оказалась живой, «сессионные» шаги 2–3 прошли автономно по его слову
> «Браузер твой… работай»). Ворота метаплана пройдены: покрытие снято датированным замером ·
> карта ссылок числом · три группы спроса записаны · разделы «чего числа НЕ доказывают» есть
> в §14–§16 и в обоих JSON ·
> **Исходящее:** отчёты замеров → приложения `researches/34`; критерии вердикта фазы 8 — раздел
> этого плана · **Ступень 3 лестницы** `/plan-epic`

---

## Вектор цели (по `REQUIREMENTS_FRAMEWORK.md`)

- **Achieve:** у проекта появляются ЧИСЛА, которых нет ни у кого: сколько из 10 222 страниц в
  индексе и почему остальные нет · граф внутренних ссылок числом · спрос трёх недомеренных
  территорий (Т3 «объект+рейтинг», Т0 виды тестов, Т10 боль).
- **Maintain:** все замеры ТОЛЬКО читают — ни одной записи в бой, ни одного действия в консолях
  кроме чтения отчётов.
- **Avoid:** вердикты о судьбе каталога до данных — эта фаза добывает данные, решает фаза 8 по
  критериям, записанным здесь ЗАРАНЕЕ (§Критерии вердикта ниже).

## Критерии приёмки фазы (Scale · Meter · Target)

1. **Покрытие.** Scale: страниц каталога в индексе Google, из 10 222, + разбивка причин
   не-индексации · Meter: `node tools/console-coverage.mjs` → `researches/34_coverage_<дата>.json`
   + раздел-приложение в `researches/34` · Target: ≥1 датированный замер по всем трём консолям.
2. **Граф ссылок.** Scale: входящие внутренние ссылки per страница; число сирот; глубина от `/` ·
   Meter: `node tools/measure-link-graph.mjs` по свежему `build/` · Target: базовая линия
   зафиксирована в `researches/34` (ожидание по разведке: ~10 222 сироты — прибор обязан его
   подтвердить или опровергнуть числом).
3. **Спрос.** Scale: показы/мес per запрос · Meter: `tools/wordstat-harvest.mjs` (RU) +
   `tools/bing-keywords.mjs` (EN) по новым спискам · Target: 3 группы × ≥10 запросов с числами,
   записаны приложением с датой.
4. **Справочный стиль EN.** Scale: число записей каталога с маркерами
   («aired on … from … to», «totaling N episodes», «voiced by», «premiered on») · Meter: греп-скрипт
   по `src/lib/content/dims-build.json` · Target: список кандидатов в файле; **правок в данных 0**
   (правка — фаза 3, по ответу №025 В6).
5. В каждом отчёте есть раздел **«чего эти числа НЕ доказывают»** (ворота метаплана).
6. Всё новое помечено `[NOT-TESTED]` до первого прогона; после — `[TESTED: дата · как]`.

## Якоря метаплана (трассировка)

> Фаза 1: «прибор чтения консолей (покрытие… запросы…) с записью отчётов в `researches/`» ·
> «прибор карты внутренних ссылок по `build/` (базовая линия сирот)» · «добор замеров спроса
> тремя группами» · «греп EN-каталога по справочным маркерам» · Ворота: «первый отчёт покрытия с
> датой… карта ссылок снята числом… три замера спроса записаны… раздел „чего числа НЕ доказывают"».

---

## Предусловия

- Сборка сайта работает: `npm run build` (для шага 1).
- Для шагов 2–3 — **сессия владельца**: Chrome с CDP-портом 9222
  (`& "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222 --user-data-dir="C:\Users\krinik\chrome-debug"`),
  владелец залогинен в Search Console / Bing Webmaster / Яндекс (та же механика, что `homeworks/09`
  и жатва Вордстата). Шаги 0, 1, 4, 5 сессии НЕ ждут.
- Стенд погашен процессной проверкой, не по `stand.log` (капкан №1 STATUS).

## Шаги

### Шаг 0 — Списки запросов для добора спроса (автономно) ✅ 2026-08-14

> Сделано: `tools/demand-t3-object-rating.txt` (50 RU + 50 EN: 5 шаблонов × 10 живых названий,
> 5 видов × популярное/нишевое по `rates` каталога; «Конджиам» из выборки §10 вошёл естественно) ·
> `tools/demand-t0-tests.txt` (20+20) · `tools/demand-t10-pain.txt` (14+14, контрольная строка
> внутри). Формат: секции `[ru]`/`[en]`, `#` — комментарий; читатель — `tools/demand-lists.mjs`.
> `[TESTED: 2026-08-14 · чтение обратно UTF-8 без кракозябр, счёт по секциям ≥10 везде]`

Подготовить три файла списков (ASCII-имена, содержимое UTF-8; текст едет через файлы, не argv):

- `tools/demand-t3-object-rating.txt` — группа D `researches/30` прил. B: 5 шаблонов
  («{название} рейтинг», «{название} отзывы», «похожие на {название}», «{название} стоит ли
  смотреть/играть», «что посмотреть если понравился {название}») × 10 живых названий из
  `dims-build.json` (5 видов × популярное/нишевое — брать выборку агента §10 `researches/34`) —
  RU и EN варианты. Это закрывает и `homeworks/08` шаг 3.
- `tools/demand-t0-tests.txt` — виды тестов: «тест на совместимость», «тест кто ты из…», «тест по
  фильмам», «love test», «compatibility calculator», «friendship test», «what to watch quiz» и
  ~15 родственных коротких (урок C.1: язык человека, не продукта).
- `tools/demand-t10-pain.txt` — боль: «почему я одинок», «как найти своих людей» (контроль — уже
  замерен: 4 206), «устал от приложений знакомств», «why am I lonely», «dating app fatigue»,
  «alternatives to dating apps» и ~10 родственных.

**Проверка:** списки существуют, в каждом ≥10 строк, кодировка UTF-8 (чтение обратно без кракозябр).

### Шаг 1 — Прибор карты внутренних ссылок (автономно) ✅ 2026-08-14

> Сделано: `tools/measure-link-graph.mjs` → `researches/34_linkgraph_2026-08-14.json` +
> приложение §14 `researches/34`. Ожидание разведки УТОЧНЕНО: строгих сирот в каталоге 0 (у каждой
> страницы один вход — её языковой двойник), но **все 10 222 недостижимы от корня** (пары ru↔en
> замкнуты; дверь — только sitemap). Оба контроля прибора зелёные; битых внутренних ссылок 0.
> `[TESTED: 2026-08-14 · прогон на свежем build/ 10 241 HTML; детерминизм — два прогона байт-в-байт]`

Написать `tools/measure-link-graph.mjs` (ПРИБОР, не страж): `npm run build` → парс всех
`build/**/*.html`, извлечь внутренние `<a href>`, построить граф. Печатает и пишет в
`researches/34_linkgraph_<дата>.json`: число страниц · число сирот (0 входящих) по сегментам
(лендинг/каталог/документы) · распределение входящих · глубина от `/` (BFS; недостижимые = ∞).

- 🔴 **Контроль прибора (`EXP-0082`):** лендинг ОБЯЗАН показать ≥1 исходящую ссылку, а страница
  каталога — ровно 3 известных (self-lang, `/`, `/profile` ×2 адресно). Пустой граф = ошибка
  прибора, падать громко, не рапортовать «0 ссылок».
- Детерминизм: сортировка полная, повторный прогон = байт-в-байт тот же JSON (канон стиля кода).

**Проверка:** прогон на свежем `build/`; числа записаны приложением в `researches/34` с датой.

### Шаг 2 — Прибор покрытия консолей (сессия владельца; ТОЛЬКО чтение) ✅ 2026-08-14

> Сделано: `tools/console-coverage.mjs` → `researches/34_coverage_2026-08-14.json` + приложение
> §15 `researches/34`. Сессия владельца оказалась живой в профиле chrome-debug (слово владельца в
> чате: «Браузер твой… работай»); съём числами из DOM отчётов, единственный клик — пагинация
> запросов GSC под двойным фильтром. Главное: **Google 1 → 994 в индексе** (срез 07.08; вне —
> 9 240, из них «Обнаружена, не проиндексирована» 9 237) · органика началась: 8 кликов / 149
> показов за 3 мес, запросы каталожные · Bing: обнаружено 10,2 тыс., в индексе 2 · **Яндекс:
> −641 за 6–12.08, «малоценная или маловостребованная», все /en/dimension/***.
> Сходимость причин GSC с итогом — в пределах округления консоли.
> `[TESTED: 2026-08-14 · два полных прогона; при сломе пиннинга падает громко и сбрасывает слепок]`

Написать `tools/console-coverage.mjs` по образцу `tools/wordstat-harvest.mjs` (CDP 9222):

- **Google Search Console:** отчёт «Страницы» (проиндексировано / не проиндексировано + причины:
  «Обнаружена, не проиндексирована», «Просканирована, не проиндексирована»…) + «Эффективность»
  (запросы, показы, клики, позиции, 3 мес.). Снимать ЧИСЛА из ответов внутренних API страницы (как
  Вордстат), не скриншотами.
- **Bing Webmaster / Яндекс.Вебмастер:** аналогичные сводки (Bing: URLs discovered/indexed;
  Яндекс: страницы в поиске, исключённые).
- Запись: `researches/34_coverage_<дата>.json` + человекочитаемый раздел-приложение в
  `researches/34` («Покрытие на <дата>»: таблица per консоль + топ-20 запросов показа).
- 🔴 **Капканы чужого аккаунта (`EXP-0138`-семейство, оплачено в `homeworks/09`):** ни одного
  клика по элементу без двойного фильтра «кандидат ровно один И подпись совпадает точно»; кнопки
  без подписи не нажимать; НИЧЕГО не подавать/не отправлять — только чтение. Иконка «Отправить
  отзыв» в GSC уже нажималась по ошибке — не повторить.
- ⚠️ «Обнаружено 0 / проиндексировано 1» — вероятный честный результат для домена месячного
  возраста: это ДАННЫЕ, не авария и не повод «чинить».

**Проверка:** JSON существует, суммы разбивки сходятся с итогом; в отчёте раздел «чего числа НЕ
доказывают» (минимум: покрытие ≠ трафик; причины Google — самоотчёт чёрного ящика; один замер —
точка, не тренд).

### Шаг 3 — Добор спроса по спискам шага 0 (сессия владельца) ✅ 2026-08-14

> Сделано автономно (сессия в chrome-debug жива): жнецы получили вход `--file`/`--out`
> (общий читатель — `tools/demand-lists.mjs`), собрано **84 RU (Вордстат) + 84 EN (Bing)**;
> сырьё по плановым именам `researches/34_demand_t{0,3,10}.json` (внутри разбивка ru/en — два
> жнеца с разными контрактами пишут каждый своё, склейка `tools/merge-demand.mjs`).
> Контрольная строка: «как найти своих людей» → 4 430 ≈ 4 206 (+5 %, сезон) — съём валиден.
> Итоги и «что меняет» — приложение §16 `researches/34`.
> `[TESTED: 2026-08-14 · контрольная строка сошлась; счёт собранного = счёту списков]`

`node tools/wordstat-harvest.mjs` (RU, все три списка) · `node tools/bing-keywords.mjs` (EN).
Контрольная строка: «как найти своих людей» обязана дать ~4 206 (±сезон) — расхождение на порядок =
ошибка съёма, не «спрос изменился». Результаты — приложением в `researches/34` по группам, с
пометкой [НАШ ЗАМЕР] и оговорками методики (Вордстат = показы с уточнениями; Bing ≠ Google).

**Проверка:** ≥10 запросов с числами в каждой из трёх групп; сырьё в
`researches/34_demand_t{0,3,10}.json`.

### Шаг 4 — Греп справочного стиля EN (автономно) ✅ 2026-08-14

> Сделано: `tools/grep-en-boilerplate.mjs` → `researches/34_en_boilerplate_candidates.md`:
> **279 записей из 5 111** с маркерами («voiced by» 199 · «premiered on» 114 · «aired on … from …
> to» 33 · «totaling N episodes» 2 · «consists of N seasons» 0). Контроль «Гадкие американцы» —
> в списке. Данные не тронуты. `[TESTED: 2026-08-14 · прогон; контроль-предохранитель в скрипте]`

Одноразовый скрипт-файл (не argv!) по `dims-build.json`: маркеры «aired on … from … to»,
«totaling \d+ episodes», «voiced by», «premiered on», «consists of \d+ seasons». Выход —
`researches/34_en_boilerplate_candidates.md`: slug · вид · найденное предложение. **Данные не
менять** — правка ждёт №025 В6 (фаза 3).

**Проверка:** файл существует; «Гадкие американцы» (`ugly-americans-zgji6eoe`) обязан попасть в
список — это известный положительный контроль из §10 `researches/34`.

### Шаг 5 — Критерии вердикта фазы 8: записать ДО данных (автономно) ✅ 2026-08-14

> Зафиксировано: раздел «Критерии вердикта» закоммичен в `75b02f7` (2026-08-14, до первых
> данных), с тех пор не менялся (diff пуст); первый `34_coverage_*.json` снят ПОЗЖЕ, тем же днём.
> Числа задним числом не подставлялись — свидетель история git.

Дописать в этот план раздел «Критерии вердикта» (ниже) — он уже заполнен ПРЕДЛОЖЕНИЕМ агента;
шаг фиксирует: числа шага 2 в него НЕ подставлялись задним числом (свидетель — история git: раздел
закоммичен раньше первого `coverage_*.json`).

### Шаг 6 — Свод и закрытие фазы ✅ 2026-08-14

> Приложения §14–§16 в `researches/34` датированы; `STATUS.md` пополнен; маркеры `[TESTED]`
> проставлены по факту прогонов. Судейская проверка утверждений фазы:
> · «только чтение» — по коду: console-coverage не шлёт ни одного POST/submit, единственный клик —
>   пагинация GSC под двойным фильтром (кандидат ровно один + подпись точная); жнецы — прежние
>   инструменты с прежними ограничениями;
> · контроли приборов ДОКАЗАНЫ мутациями 2026-08-14: (1) фальшивый контрольный slug → греп упал
>   громко, exit 1; (2) ослеплённый парсер `<a href>` → карта ссылок упала на контроле лендинга,
>   exit 1; оба восстановлены, контрольные прогоны зелёные;
> · детерминизм карты ссылок — два прогона байт-в-байт (хэши равны).

Все приложения в `researches/34` датированы · `STATUS.md` пополнен строкой · `/fable-judge` на
утверждения фазы (особенно на «только чтение» и контроль приборов) · маркеры `[TESTED: …]`
проставлены по факту прогонов.

---

## Критерии вердикта фазы 8 (ПРЕДЛОЖЕНИЕ, записано 2026-08-14 — до первых данных)

Через ≥3 месяца и ≥3 замеров покрытия, по сегменту каталога:

- **Зелёный путь (держать и достраивать):** проиндексировано ≥30 % каталога ИЛИ тренд покрытия
  растёт ≥2 замера подряд, И появились показы у ≥5 % проиндексированных.
- **Жёлтый (обогащать, не сокращать):** индексация 5–30 % и стоит; показы околонулевые →
  приоритет фазам 2–3 (архитектура, данные), пересмотр через ещё 2 замера.
- **Красный (обсуждать порог с владельцем):** <5 % и падает ПОСЛЕ построенных хабов и ссылок,
  либо в GSC массово «Crawled — currently not indexed» с ухудшением по НЕкаталожным сегментам
  (признак site-wide давления). Тогда — интервью с вариантами по `researches/34` §5.4.

Пороги — предложение агента по индустриальным ориентирам (<50 % индексации партии = «stop scaling»,
§5.4); владелец вправе переписать их в любой момент ДО вердикта.

## Риски

| Риск | Гашение |
|---|---|
| Сессия владельца не состоится | Шаги 0, 1, 4, 5 автономны и идут первыми; 2–3 паркуются с просьбой в чате |
| Внутренние API консолей изменятся | Прибор падает громко с подсказкой «сними вручную по инструкции» — не выдумывает числа |
| Вордстат-лимиты / капчи | Жатва частями; повторный прогон дописывает, не затирает |
| Соблазн «починить» низкое покрытие | Записано: это данные фазы, решает фаза 8 по критериям выше |
