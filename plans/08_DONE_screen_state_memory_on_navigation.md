# План 08 — Память состояния экранов при навигации

**Статус:** ✅ ИСПОЛНЕН 2026-07-30 (ответ владельца В11=А) · **Когда/контекст:** заведён
2026-07-29, волна владельца `ideas/21` (п. 9) ·
**Тип:** план (механика нужна всему приложению, а не одному экрану)

> ## ✅ Что сделано 2026-07-30
>
> Все пять экранов открываются там, где их оставили. Механика — **тонкий общий слой**
> `src/lib/ui/view-memory.ts` поверх штатных `beforeNavigate`/`afterNavigate`; память живёт в
> кэше сессии под префиксом `view:` (ответ **В11=А**: живая вкладка, F5 обнуляет).
>
> **Страж:** `node tools/verify-plan08.mjs` — **60 зелёных, 0 провалов** (обе темы × 390/1440;
> 4 честных «нечем мерить»: «Связи» и «Меню» на 1440 короче окна). До кода тот же страж давал
> **8 провалов**. Доказан **тремя мутациями** (см. «Проверка»).
>
> **Что закрылось попутно:** часть Б `bugs/74` · шаг 6 сделал признак программной прокрутки
> общим, чем и снят риск «дефект панели рождается заново на каждом экране» (`bugs/75`) ·
> найден и починен `bugs/88` — без него восстанавливать прокрутку было НЕКУДА.
>
> **Что осталось владельцу** (агент выбрал по своей рекомендации и помечает это честно):
> вопросы **Б** (что помнить кроме прокрутки — взято: вкладка да, раскрытая карточка и поиск
> нет), **В** (pull-to-refresh гасит память — взято: да) и **Г** (на всех экранах — взято: да,
> это и слово владельца «по каждой из страниц»). Ответ получен только на **А** (срок жизни).

Это не баг одного экрана. Пункт 9 волны — про **общее свойство приложения**: экран обязан
открываться таким, каким его оставили. Тем же механизмом закрываются часть Б пункта 1 и
пункт 2 той же волны (`bugs/74`, `bugs/75`), поэтому чинить их поодиночке нельзя — получится
три разных приёма для одной механики.

---

## Симптом — слово владельца дословно

> при навигации, страницы открываются вверху, не сохраняются позиции скрола по каждой из
> страниц. Должны сохраняться. Страницы должны открываться в том состоянии, в каком их
> оставили при навигации С ЭТИХ страниц.

Ударение владельца — на второй фразе: не только прокрутка, а **состояние**.

## Замер / воспроизведение

Живой прогон стенда `http://localhost:5173`, прибор `tools/probe-ideas21.mjs`,
протокол — `test-results/ideas21/report.txt` (раздел «ПП. 1 и 9 · память прокрутки при
навигации»). Сценарий: пролистать экран вниз → уйти по нав-панели на другой экран → вернуться.

| Экран | Ушёл с позиции | Вернулся на | Ожидание владельца |
|---|---|---|---|
| «Измерения» | 900 | **0** | 900 |
| «Связи» | 72 | **0** | 72 |
| «Пространство» | 900 | **0** | 900 |

**Три экрана из трёх забывают прокрутку.** Консоль при этом чиста — это не ошибка, это
отсутствие механизма.

Оговорки замера, чтобы следующий агент не принял их за факты:
- «Профиль» и «Меню» прибором **не мерены** — про них знание только из кода (там та же
  природа, но живого числа нет).
- 72px по «Связям» — вероятно, весь ход прокрутки на стенде с четырьмя людьми (**гипотеза**,
  отдельно не проверялась). На боевых данных лента длиннее, и цена забывчивости выше.
- Состояние кроме прокрутки (вкладка, раскрытая карточка, докуда долистана лента) прибором
  **не измерялось** — только прокрутка.

## Причина — по коду

Разведка (`test-results/ideas21/recon.txt`, «=== ПУНКТ 9 ===», вердикт
ПОДТВЕРЖДЁН_КОДОМ) доказывает причину **пустым грепом**: механизма нет вовсе.

По всему `src/` не встречается ни одного вхождения `export const snapshot`, `afterNavigate`,
`beforeNavigate`, `noScroll`, `scrollRestoration`. Из семейства `data-sveltekit-*` в проекте
ровно один атрибут — `data-sveltekit-preload-data="hover"` (`src/app.html:168`), к прокрутке
он отношения не имеет. Единственный вызов истории во всём приложении —
`history.replaceState(null, '', '/profile')` в почтовом флоу (`src/routes/profile/+page.svelte:232`),
записей он не добавляет. Все `window.scrollTo` — локальные жесты внутри `/dims`
(строки 691, 716, 749, 830).

**Половина первая — прокрутка.** Навигация продукта — обычные ссылки:
`src/lib/ui/BottomNav.svelte:37` и `src/lib/ui/SideRail.svelte:44` — `<a href={item.href} …>`.
Каждый тап по нав-панели — это history PUSH, а не BACK. Клиентский роутер SvelteKit
восстанавливает прокрутку на возврате «назад», а на push ставит её в 0. Механика зафиксирована
не «по здравому смыслу», а **в самом репозитории** — комментарий
`src/routes/+layout.svelte:123-125`:

> SvelteKit при переходе ставит прокрутку в 0, а при возврате «назад» восстанавливает
> прежнюю — оба раза программно.

Значит «Профиль → Измерения → Профиль» — это три записи истории, и третья открывается сверху
**по построению платформы**, а не по ошибке в нашем коде.

> **Честная граница (из разведки).** Что SvelteKit на push-переходе ставит `scrollTo(0,0)` —
> поведение фреймворка, а не строка продуктового кода. В репозитории оно записано как принятый
> диагноз (`+layout.svelte:113-130`), и замер выше с ним согласуется, но отдельным
> экспериментом «push против popstate» это в 2.0 **не перепроверялось**.

**Половина вторая — состояние.** Вид экрана живёт в `$state` компонента и умирает вместе с
ним при уходе с маршрута:

| Файл:строка | Что теряется |
|---|---|
| `src/routes/dims/+page.svelte:92` | `let tab = $state<Tab>('all')` — возврат всегда открывает «Все» |
| `src/routes/dims/+page.svelte:664` | `const tabScroll: Record<Tab, number> = { all: 0, mine: 0 }` — обычная переменная внутри компонента |
| `src/routes/dims/+page.svelte:148` | `shown` — сколько карточек ленты уже показано |
| `src/routes/dims/+page.svelte:157` / `:136` / `:768` | `expanded` (раскрытая карточка), `submitted` (отправленный поиск), `searchOpen` |
| `src/routes/relations/+page.svelte:68` | `revealed = $state(REVEAL_PORTION)` — докуда раскрыт топ (порция 24, `:67`) |
| `src/routes/relations/+page.svelte:49` | `expanded` — раскрытая карточка связи |
| `src/routes/profile/+page.svelte:62` | `seeMeOpen` — открытое «Как меня видят» |

Переживают навигацию только **данные** (кэш сессии, `ideas/18`), но не **вид**.

**Третья, скрытая часть причины: одной прокрутки мало.** Вернуть `scrollY` на длинный экран
нельзя, пока документ не набрал прежнюю высоту, — браузер прижмёт прокрутку к текущему концу.
Это не теория: `bugs/64` поймал промах QA-прогоном — «просили 7658px, получали 7191px»
(`src/routes/dims/+page.svelte:680-684`). Значит «докуда долистана лента» — не украшение, а
**условие** работы памяти прокрутки.

## Что даёт платформа (и почему НЕ надо изобретать своё)

В проекте `@sveltejs/kit ^2.63.0` (`package.json:39`). Штатных механизма два, и оба сейчас
не используются ни разу.

**1. `export const snapshot = { capture, restore }` в `+page.svelte`.** Заведомо существующий
штатный API SvelteKit (по разведке — с версии 1.5). Страница объявляет, что́ считать своим
снимком; платформа снимает его перед уходом и возвращает **при возврате на ту же запись
истории** (back/forward). Это ровно наш случай «вернулся системной „Назад“».

**2. Собственное управление прокруткой SvelteKit.** Роутер сам ведёт позиции по записям
истории: на popstate возвращает прежнюю, на push ставит 0. Плюс есть штатные крючки
`beforeNavigate` / `afterNavigate` (`$app/navigation`) и атрибуты ссылок `data-sveltekit-*`,
которыми поведением прокрутки на переходе управляют декларативно.

**Чего платформа НЕ закрывает сама.** Нав-панель продукта — это push-навигация: тап по
«Измерениям» создаёт **новую** запись истории, а не возвращается на прежнюю. Снимок, снятый
у старой записи `/dims`, к новой записи не относится. Поэтому требование владельца — «в том
состоянии, в каком оставили при навигации **С ЭТИХ** страниц» — снимком в чистом виде не
выполняется: нужен **тонкий свой слой** поверх штатного (`beforeNavigate` → запомнить снимок
по маршруту, `afterNavigate` → вернуть).

### ✅ Разведка платформы, снятая на стенде 2026-07-30 (шаг 2 — три факта)

Прибор — `test-results/plan08/probe-platform.mjs` (одноразовый, вне git; полный протокол в
шапке `src/lib/ui/view-memory.ts`). Мерили БЕЗ правок продукта:

| Что мерили | Факт |
|---|---|
| PUSH по нав-панели | прокрутка **0** — как и записано в `+layout.svelte` |
| системная «Назад» | **тоже 0**, а не прежние 900. Платформа честно пытается вернуть позицию, но данные приезжают асинхронно: в тот момент в ленте было **12 карточек вместо 44**, и позиция прижималась к короткому документу |
| после F5 | **0**; в `sessionStorage` при этом лежат `sveltekit:scroll` И `sveltekit:snapshot` |

**Два вывода, изменившие решения.**

1. **На платформенное восстановление опираться нельзя** — «при возврате назад восстанавливает
   прежнюю» верно лишь для документа, который УЖЕ набрал высоту. Диагноз, записанный в
   `+layout.svelte:125-127`, нуждается в этой оговорке.
2. **Штатный `snapshot` не годится вовсе:** он хранится в `sessionStorage`, то есть ПЕРЕЖИВАЕТ
   перезагрузку — прямое противоречие ответу владельца В11=А. Поэтому память вида живёт в кэше
   сессии, а `snapshot` в проекте не используется.

**Почему не своё.** Самодельная память истории — это самодельный роутер: перехват `popstate`,
свои индексы записей, свои гонки с восстановлением прокрутки роутера. Разведка по пункту 3 той
же волны отдельно предупреждает: три похожих оверлея уже просили себе такой перехват, и это
«быстро превратится в самодельный роутер поверх штатного». Наш слой обязан быть **тонким**:
хранение снимков по маршруту + порядок восстановления. Всё, что умеет платформа (back/forward,
позиции по записям истории), забираем у платформы.

## Что именно помнить по каждому экрану

Черновик инвентаря; уточняется шагом 3 и зависит от ответа владельца (вопрос Б).

| Экран | Прокрутка | Докуда долистано | Прочее состояние вида |
|---|---|---|---|
| `/dims` «Измерения» | ✔ по каждой вкладке отдельно (`tabScroll`, `:664`) | `shown` (`:148`; в кэше уже есть ключ `ratings:shown`, `cache.ts:103`) | активная вкладка «Все/Мой NDim ID» (`:92`), раскрытая карточка (`:157`), отправленный поиск (`:136`) — **спорно, см. вопрос Б** |
| `/relations` «Связи» | ✔ | `revealed` (`:68`, порция 24) | раскрытая карточка (`:49`) |
| `/space` «Пространство» | ✔ | — (виджеты, ленты нет) | — |
| `/profile` «Профиль» | ✔ | — | оверлей «Как меня видят» (`:62`) — **скорее нет**: он чинится отдельно, багом по п. 3 волны (там разведка предлагает сделать его настоящим дочерним маршрутом), и помнить его открытым незачем |
| `/menu` «Меню» и дочерние | ✔ | — | — |

Правило по умолчанию, которое агент предлагает: **помним прокрутку и «докуда долистано» —
это положение человека в списке; остальное состояние вида помним только там, где владелец
подтвердит.** Возвращать человека в раскрытую карточку или в отправленный поиск — это уже
решение о поведении продукта, не техника.

## Где это хранить

Три честных варианта, выбор — за владельцем (вопрос А):

1. **Память сеанса приложения** — модуль рядом с кэшем данных. Прецедент в проекте есть:
   кэш живёт в `const store = new Map<string, Entry>()` (`src/lib/data/cache.ts:110`), никакого
   хранилища браузера. Плюс: единая с данными судьба (F5 обнуляет и то и другое — вид не
   разъедется с содержимым). Минус: перезагрузка страницы теряет позицию.
2. **`sessionStorage`** — переживает перезагрузку страницы, умирает с вкладкой. Прецедент
   тоже есть (`src/lib/data/funnel.ts:50-51`). Минус: данные к моменту возврата перечитаны
   заново и лента может быть **другой** (см. риск ниже) — позиция окажется «в никуда».
3. **`localStorage`** — переживает всё, вплоть до перезапуска браузера. Агент против:
   «вернуть человека туда, где он был вчера» — это уже не память состояния, а сюрприз.

**Ключ хранения — путь маршрута** (`/dims`, `/relations`, …), а не запись истории: именно
этого требует формулировка владельца про push-навигацию по нав-панели.

## ⛔ Ждёт владельца → ✅ что взято в работу 2026-07-30

Ответ пришёл на **один** вопрос из четырёх — **А (В11=А)**: память живёт в живой вкладке,
перезагрузка обнуляет. Остальные три агент закрыл СВОЕЙ рекомендацией (она была написана здесь
заранее и владельцу видна), реализовал и помечает это явно — вычитка за владельцем:

| Вопрос | Как сделано | Чьё решение |
|---|---|---|
| **А** срок жизни | кэш сессии, префикс `view:`; F5 обнуляет | **владелец, В11=А** |
| **Б** что помнить кроме прокрутки | вкладка «Все / Мой NDim ID» — **да**; «докуда долистано» — **да** (без него прокрутке некуда возвращаться); раскрытая карточка и отправленный поиск — **нет** | рекомендация агента |
| **В** сброс при pull-to-refresh | **да**, гасим (`invalidate(KEYS.view)` в `invalidateAll`) | рекомендация агента |
| **Г** на всех экранах или только «Измерения» | **на всех пяти** — это и слово владельца «по каждой из страниц» | рекомендация агента |

**Рекомендация агента одной строкой (исполнена): помнить на ВСЕХ экранах, хранить в памяти сеанса
(вариант 1), память умирает вместе с кэшем данных — при F5, при pull-to-refresh и при смене
человека; из состояния вида помним прокрутку, «докуда долистано» и активную вкладку
«Все/Мой NDim ID», остальное — нет.**

Четыре вопроса (все — из разведки, все на одну минуту разговора):

**А. Сколько живёт память состояния и переживает ли перезагрузку страницы?**
Сеанс приложения (F5 обнуляет — вариант 1) · вкладка браузера (переживает F5 — вариант 2) ·
дольше (вариант 3). От ответа зависит хранилище и то, что человек увидит после перезагрузки.

**Б. Что помнить кроме прокрутки?** Активную вкладку «Все / Мой NDim ID», раскрытую карточку,
отправленный поиск — по каждому пункту отдельное «да/нет». Агент рекомендует: вкладку — да,
раскрытую карточку и поиск — нет (возврат в чужое раскрытое состояние дезориентирует сильнее,
чем помогает; это **мнение агента**, замером не подкреплено).

**В. Сбрасывать ли память при pull-to-refresh?** Жест утверждён владельцем (интервью №006) и
означает «дай свежее». Агент рекомендует: да, сбрасывать — обновил ленту, значит начал сверху.

**Г. Помнить вид на всех экранах или только на «Измерениях»?** Владелец в п. 9 сказал «по
каждой из страниц», но это стоит подтвердить: на «Пространстве» и в «Меню» цена памяти — та же
работа, а польза меньше. Агент рекомендует: на всех, механизм всё равно один.

**Пятый вопрос — не к этому плану, но он его блокирует по существу:** развилка кэша из
`bugs/74` (часть А пункта 1). Если «Измерения» продолжат перечитываться с нуля после каждой
моей оценки и каждого pull-to-refresh, лента пересобирается заново и **тасуется**
(`Math.random` в `src/lib/model/feed.ts:63-70`). Тогда идеально восстановленная прокрутка
вернёт человека на 900px **другого** списка. Память вида без тёплого кэша данных — половина
дела и повод для нового бага.

## План шагами

- [x] **0. Развилка закрыта.** Ответ **А** получен (В11=А, интервью №007); Б, В, Г взяты по
      рекомендации агента и помечены как его решение — см. таблицу выше.
- [x] **1. «До» зафиксировано.** Красная база снята НОВЫМ стражем на текущем `main` —
      **8 провалов**: «Измерения» 900→0 (и лента 24→12), «Связи» 32→0, «Пространство» 900→0,
      **«Профиль» 522→0**, **«Меню» 494→0** (эти два измерены впервые), вкладка «Мой NDim ID»
      возврат не переживает.
- [x] **2. Разведка платформы снята** — три факта в разделе выше. Правок продукта не
      потребовалось вовсе: `snapshot` объявлять не понадобилось, потому что уже само его
      хранилище (`sessionStorage`) противоречит ответу В11=А.
- [x] **3. Инвентарь «что помнить» уточнён** по таблице выше и ответу на вопрос Б; для каждого
      экрана выписаны имена переменных и порядок их восстановления.
- [x] **4. Тонкий общий слой** — `src/lib/ui/view-memory.ts` (без `.svelte.ts`: рун внутри нет):
      `beforeNavigate` — снять снимок текущего маршрута, `afterNavigate` — вернуть снимок
      целевого. Ключ — путь маршрута. Хранилище — по ответу А. Экран объявляет только СВОИ
      поля (что снять / что вернуть) — знание о конкретных экранах в общий слой не утекает.
- [x] **5. Порядок восстановления.** `restoreScroll` поднят из `/dims` в общий модуль как
      есть — дедлайн 1200 мс, отступление при первом жесте человека, `instant`. Уточнение
      против плана: первый прыжок делается **синхронно**, в том же кадре, а дальше позиция
      доводится по кадрам, пока документ набирает высоту. Так порядок «сначала высота»
      соблюдён циклом, а кадра «экран сверху» человек не видит (замер в шаге 8).
- [x] **6. Флаг «идёт программная прокрутка»** экспортируется слоем на время восстановления.
      Обработчик прокрутки верхней панели `/dims` (`:778-815`) читает его и в это время ничего
      не решает — только переприсваивает `last`. **Этим закрывается `bugs/75` (п. 2 волны)**:
      панель «Все / Мой NDim ID» прячется именно потому, что программный прыжок читается ею
      как «человек листает вниз».
- [x] **7. Экраны подключены** (`/dims` проверен отдельно, затем остальные четыре): `/dims` → `/relations`
      → `/space` → `/profile` → `/menu`. Внутри `/dims` память вкладок (`tabScroll`, `:664`)
      переезжает в общий слой — двух механизмов памяти прокрутки на одном экране быть не должно.
- [x] **8. Ссылки нав-панели — `data-sveltekit-noscroll` НЕ нужен.** Решено наблюдением
      (`test-results/plan08/probe-frames.mjs`): покадровая трасса даёт переход
      `/profile y=0 → /dims y=900` и **ни одного кадра, где новый экран показан сверху** — на
      390 и на 1440. Синхронный первый прыжок закрыл вопрос без правки ссылок.
- [x] **9. Сброс памяти.** Pull-to-refresh — `invalidate(KEYS.view)` в `invalidateAll`. Смена
      человека и выход НЕ потребовали кода вовсе: память лежит в том же кэше, и её уносят
      `own(uid)` и `forgetAll()` — ровно тот образец, на который ссылался план.
- [x] **10. Страж `tools/verify-plan08.mjs`** — **60/0** (обе темы × 390/1440), доказан тремя
      мутациями (см. «Проверка»).
- [x] **11. Сдача:** живой Chrome, обе темы, 390 и 1440, скриншоты в `test-results/plan08/`,
      консоль чиста. Регрессия: юнит 186/186 · typecheck 0 · svelte-check 0 на 415 ·
      `verify-bug75` 44/0 · `verify-bug76` 64/0 · `verify-back-twins` зелёный ·
      `verify-ideas18` зелёный · `verify-ptr` 53/0 · `verify-bug87` 60/0 · `verify-bug54`
      зелёный. ⚠️ Взгляд владельца-QA всё равно нужен: агент судит числами, человек — глазами.

## Чего НЕ делать

Собрано из заметок разведки (пп. 1, 2, 3, 9) и истории `bugs/64` — это уже пройденные тупики:

- **Не возвращать `scroll-behavior: smooth` на корень.** На программном восстановлении это и
  даёт «съезд», от которого ушли: `src/routes/+layout.svelte:113-130`, `:130 scroll-behavior: auto`.
- **Не городить самодельный роутер** — свой перехват `popstate`, свои индексы истории. Тонкий
  слой поверх штатных `beforeNavigate`/`afterNavigate`/`snapshot`, и не больше.
- **Не восстанавливать прокрутку до того, как документ набрал высоту.** Проверено на живом
  прогоне `bugs/64`: просили 7658px — получали 7191px.
- **Не бороться с рукой человека.** Отступление возврата при `wheel`/`touchstart`/`keydown`/
  `pointerdown` (`dims:700-708`) сохранить при переносе в общий модуль.
- **Не копировать гонку таймеров 1.x** (`setTimeout` 10 мс + 200 мс, `ndim_old/public/scripts/app.js:1157-1161`,
  `:2931-2949`). 1.x добивался порядка задержками; у нас для этого есть явный флаг из шага 6.
- **Не «лечить» панель `/dims` строкой `toolbarHidden = false` в начале `switchTab`**:
  программный прыжок случится ПОСЛЕ неё и спрячет панель снова (разведка, п. 2).
- **Не дублировать `restoreScroll` копипастом по экранам.** Одна механика — одно место;
  иначе следующая правка починит один экран из пяти.
- **Не чинить кэш данных дописыванием значений руками** (`src/lib/data/cache.ts:182-193`,
  `researches/17 §2.4`) — соблазн возникнет на шаге 5, когда захочется «просто подсунуть»
  прежнюю ленту.

## Проверка

**Наблюдение, которым судим починку** (оно же — сценарий владельца): пролистать экран вниз,
уйти по нав-панели на соседний, вернуться — человек видит то же место и тот же вид, без кадра
«сверху» и без рывка.

**Страж `tools/verify-plan08.mjs`** (живой браузер, 390 и 1440, обе темы):

- для каждого из пяти экранов: прокрутить до X → уйти → вернуться → `|scrollY − X| ≤ 2`;
- «Измерения»: вкладка «Мой NDim ID» переживает уход и возврат; у каждой вкладки своя позиция;
- «Измерения» и «Связи»: после возврата в ленте **не меньше** карточек, чем было до ухода
  (иначе позиция вернулась в короткий документ);
- панель «Все / Мой NDim ID» видна после возврата и после смены вкладки туда-обратно
  (общий страж с `bugs/75`);
- консоль чиста.

**Чем доказан сам страж — красная база + три мутации (сделано 2026-07-30).**

| Что делали | Результат |
|---|---|
| прогон на `main` ДО кода | **8 провалов** — красная база (см. шаг 1) |
| мутант 1: вернуть `$state` вместо `$state.raw` (откат `bugs/88`) | **2 провала**: «Измерения» 900→0 и глубина вкладки «Все» 900→473 |
| мутант 2: не запоминать вкладку (`capture` отдаёт `tab: 'all'`) | **1 провал**: «вкладка „Мой NDim ID“ пережила уход и возврат — открыта: Все» |
| мутант 3: честный «вариант Б» — снимок в `sessionStorage` **+** `warm: true` **+** восстановление при `nav.from === null` | **1 провал**: «перезагрузка обнуляет память — до F5 900, после F5 900» |

⚠️ **Урок мутанта 3, записанный честно.** Первые две попытки этой мутации (только
`sessionStorage`; затем `sessionStorage` + `warm: true`) оставили страж **полностью зелёным**:
проверку F5 сегодня держат ТРИ независимых причины (память умирает вместе с кэшем · холодный
экран не восстанавливается вовсе · при `nav.from === null` слой не работает). Значит проверка
охраняет ровно то, что должна, но покраснеть может лишь на целом варианте Б. Это тот же класс,
что EXP-0087: удобство устройства продукта делает проверку вакуумной, если не проверить её
мутацией.

Чего мутации НЕ доказали: проверка «содержимого после возврата не меньше» на мутанте 1 осталась
зелёной — после сброса ленты якорь подгрузки успевает добрать порции до замера. Красную базу
она ловила (24→12), но судить по ней одной нельзя.

## Связанное

- `bugs/74` — п. 1 волны, «Измерения» перечитываются и открываются сверху. **Часть Б
  (позиция прокрутки) закрывается этим планом**, отдельно её не решать; часть А (кэш гасят
  моя оценка и pull-to-refresh) — своя развилка владельца, и она влияет на смысл этого плана
  (см. пятый вопрос выше).
- `bugs/75` — п. 2 волны, панель «Все / Мой NDim ID» прячется при смене вкладки. **Закрывается
  шагом 6** этого плана: тот же корень — программная прокрутка, неотличимая от человеческой.
- `bugs/64_DONE_scroll_memory_and_feed_stall.md` — откуда пришли `switchTab`/`restoreScroll`,
  дедлайн 1200 мс и `scroll-behavior: auto`. Первый заход на ту же боль, но внутри одного экрана.
- `ideas/18` — данные переживают навигацию (кэш сессии); этот план — про то, что вид не переживает.
- `src/routes/+layout.svelte:113-130` — прошлая правка по соседнему симптому и записанный в
  репозитории диагноз платформы.
- `ideas/21` п. 9 — первоисточник · `test-results/ideas21/recon.txt` «=== ПУНКТ 9 ===» —
  разведка · `test-results/ideas21/report.txt` — живой замер.
