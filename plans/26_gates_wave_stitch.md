# План 26 — СШИВКА волны «Ворота»: порядок работ и кто чем владеет

**Выведен:** 2026-08-01 · **Не эпик, а карта волны.** Читается ПЕРЕД `plans/21`–`25`, иначе
исполнитель построит чужую фазу.

> 🆕 **2026-08-14:** к волне добавился метаплан **`plans/40_EPIC_semantic_core_v2.md`**
> (переосмысление SEO по `ideas/34`; поглотил непройденные фазы `plans/34`). Его собственные
> предметы — архитектура веса (хабы, перелинковка, разметка) и доктрина индексации; границы с
> `plans/21`/`24`/`25`/`30`/`39` перечислены в нём разделом «Кто чем владеет» и таблицу ниже НЕ
> меняют.

> **Зачем документ существует.** Четыре метаплана писались **параллельно**, разными агентами,
> которые друг друга не видели. Отдельный проход прочитал их вместе и вынес вердикт:
> **ЕСТЬ ПРОТИВОРЕЧИЯ** — 11 противоречий, 9 дыр на стыках, 7 дублей. Это не брак планирования:
> швы неизбежны, когда пишут параллельно, и дешевле найти их здесь, чем в коде.

---

## 1. Кто чем владеет (спор закрыт, не переоткрывать)

Семь работ были заявлены больше чем одним эпиком. Владелец назначен по правилу «у кого стоят
ворота и кто меряет результат».

| Работа | Хозяин | Почему он, а не сосед |
|---|---|---|
| Поп-апы над персонажами и кнопка [Смотреть больше] | **`plans/23`** | Существуют только ради перехода; без него бессмысленны. `plans/21` даёт **текст** и правило «поп-ап выводится из расчёта, а не из сценария» |
| Четыре макета первого экрана | **`plans/21`** — один документ | Это его экран. `plans/23` и `plans/22` приносят свои элементы В ТОТ ЖЕ набор. **Владелец садится один раз**, а не три |
| Приборный замер анонима (секунды до топа, проценты, цена гостя) | **`plans/23`** фаза 1 | Инструмент уже назван; `plans/22` принимает числа как вход. Два прогона дали бы два несравнимых набора |
| Снятие предохранителя воронки (`funnel.ts`) | **`plans/22`** фаза 1 | Решено в интервью №009: «снимается одной строкой, и я её сниму» — владельца НЕ ждёт. `plans/23` снимает свой вопрос об этом |
| App Check | **`plans/22`** фаза 3 | Только там проработан до ворот |
| Число на витрине (гости в счётчике, множитель ×30) | **`plans/22`** | Там оно исчислимо и там же последствия для сервера синхронизации |
| Отзывы (у кого просим, как подписываем) | **`plans/21`** фаза 7 + **`plans/25`** | Там фаза и правило «нет настоящих — нет секции» |

## 2. Порядок работ волны

Сначала то, что **не ждёт владельца** — иначе волна простоит целиком.

### Идёт сейчас, без ответов

1. **App Check (наблюдение) → снятие предохранителя воронки.** Числа копятся СО ДНЯ ПРАВКИ,
   задним числом их не будет, а на них опираются три развилки владельца. App Check первым:
   комментарий в самом `funnel.ts` называет его условием снятия.
2. **Документы в поиск** (`plans/24` фаза 1) — две строки дают ×13 открытого текста, никого не
   блокируют, а индексация раскручивается неделями. 🔴 **Но не раньше, чем снята строка «Добавлена
   аналитика рекламной атрибуции»** из истории версий: иначе первая же работа волны опубликует в
   поиск документ, противоречащий обещанию «у нас без рекламы» (вопрос Р15).
3. **Консоли поисковиков** — руки владельца, ~17 минут. Сразу после п. 2: DNS расходится часами,
   а без подтверждения прав весь эпик 24 — ставка вслепую.
4. **ОДИН приборный прогон анонима** (`plans/23` фаза 1). До интервью, а не после: от его чисел
   зависят три вопроса владельцу.  <!-- ВОПРОС-ОК: ссылка на вопросы интервью №010, а не сами вопросы -->
5. 🔴 **Починка «Связей»** — `src/lib/data/relations.ts`: `Promise.all` роняет ВЕСЬ экран на первом
   отказе, а чтение чужой карточки гостю запрещено правилами. **Работа была ничья** — её не взял
   ни один из четырёх эпиков, хотя без неё петля владельца приводит человека в «Не удалось
   загрузить связи». Дёшево и разблокирует остальное.
6. ✅ **Интервью №010 — ОТВЕЧЕНО ЦЕЛИКОМ 2026-08-01** (все 15 развилок). Звено, названное здесь
   самым длинным, владелец прошёл за один день; длинным оказалось не оно, а строка «заблокировано»,
   которую никто не снял (разбор и разнос — `plans/50`).

### После ответов

7. **Тексты ворот** (`plans/21` фаза 2) — как только получен ответ Р4 о формулировке «точности».
   Тексты **до** макетов: макет с рыбой неотличим от макета с рыбой.
   🔴 Состав пересобрать по `researches/27`, а не по списку из В6 (см. §3, противоречие 7).
8. **ОДНО сидение по макетам** — композиция ворот, поп-апы, кнопка перехода, секция входа, метка
   гостя. Всё в одном документе.
9. **Свежесть кульминации** (`plans/23` фаза 3) — строго ДО моста: кэш замораживает пустой ответ
   на всю сессию, и любая красота перехода приведёт в замороженную пустоту.
10. **Первый экран и история интерактива** (`plans/21` фазы 4–5) — ДО переезда на языковые адреса.
11. 🔴 **Мост** (`plans/23` фаза 5) — только после пп. 5, 9 и 10. **Соблазн выкатить его первым
    огромен:** это одна строка и буквальный ответ на боль `ideas/09`. Но выкаченный не в свою
    очередь, он заводит каждого нового человека на экран, который у гостя падает, и в кульминацию,
    которая замерзает. Одна строка испортит ровно то впечатление, ради которого волна затевалась.
12. **Уборщик → правила → сервер синхронизации** (`plans/22`), в этом и только этом порядке: правила,
    распахнутые раньше уборщика, дают неделю накопленного мусора.
13. **Ведение на месте → панель → диалог ухода** (`plans/23`). Перед панелью обязателен ответ Р1,
    иначе фазы строят механику страха.
14. **Модуль языка → адреса → вычитка и флип корня** (`plans/24`) — хвостом волны.
15. **Отзывы** — вне очереди, по готовности живых людей.

## 3. Противоречия, которые агент НЕ вправе решить сам

Вынесены в **интервью №010**. Здесь — только адреса, чтобы не искать.

| № | Суть | Куда ушло |
|---|---|---|
| 1 | 🔴 **В4 ампутирован ответом В5**: «упереться» больше не во что, остаётся только страх потери | Р1 |
| 2 | Следствие: панель и диалог строят чистую механику страха — под приговором `GOAL.md` | Р1 |
| 5 | Предохранитель воронки имел три взаимоисключающих статуса | закрыто §1 |
| 6 | Модаль при уходе отменяет решение интервью №007 (была выбрана врезка) | Р14 |
| 7 | Два обещания из В6 (**«симметрично»** и **«математическая точность»**) `researches/27` публиковать ЗАПРЕЩАЕТ | Р4 |
| 9 | Обещание лендинга калибруется числами, которых ещё не мерили | замер до текстов, §2 п. 4 |
| 10 | Ворота двух эпиков требуют противоположного от одной строки лендинга | Р8 |

**Уже исправлено агентом, вопросов не требует:**
· №3 — `plans/21` системно путал номера соседей (22↔24) в шести местах; исполнитель ушёл бы в
чужой файл;
· №4 — три эпика заводили `interview_010` под одним именем; интервью одно, вопросы сведены;
· №8 — страж текстов проверял запретные числа только по `researches/25` §14; список пополнен
восемью из `researches/27` §12;
· №11 — `plans/21` числился заблокированным «ждём `researches/27`», а документ уже написан.

## 4. Дыры на стыках — работа, которую не взял никто

| Дыра | Кому отдана |
|---|---|
| 🔴 «Связи» падают целиком от одного отказа в чтении | вне эпиков, §2 п. 5 — делается первой |
| 🔴 «Инфо-кнопки» (слово владельца, процитировано трижды, не спроектировано нигде) | Р12 |
| 🔴 «Приводили друзей» (В1) — путь приглашения не взял никто | `plans/21`, после Р9 |
| 🔴 Срок «7 дней» рядом со словом «бесплатно» **на самом лендинге** — требование, а не пожелание | `plans/21` фаза 2 |
| Экран входа отдельным маршрутом (`bugs/19`) — кнопка есть, цели у кнопки нет | `plans/23` фаза 5 |
| Суммарная доза напоминаний гостю: после волны их станет **пять** | см. ниже |
| Новые тексты лендинга не вошли в объём английской вычитки | `plans/24` фаза 8 |

🔴 **Про пять напоминаний — отдельно, это вопрос замысла.** После волны гость увидит: карточку
«Сейчас Вы гость» · врезку при выходе · пульсирующую панель под шапкой · модальный диалог на
«Назад» · предупреждение о сроке жизни. Каждый эпик проверяет **свой** элемент на мягкость; сумму
не смотрит никто. **Пять напоминаний об одном и том же — это и есть механика удержания**, как бы
мягок ни был каждый по отдельности. Считать дозу обязан тот, кто делает последнюю фазу эпика 23.

## Ссылки

`interviews/interview_009` (ответы владельца) · `interviews/interview_010` (развилки волны) ·
`plans/21`–`25` · `researches/25`, `26`, `27` · `MASTER_PLAN.md` фаза 7
