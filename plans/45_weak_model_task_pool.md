# План 45 — Пул заданий для слабой модели-исполнителя (Opus): планы под строгое следование

> **Создан:** 2026-08-14 (слово владельца в чате: «эпик планирование и нарез операционных
> планов пошаговых детальных для последующей работы над проектом слабой ИИ модели Opus. Нужно
> ей оставить планов и задач, которые понятно описаны и легки в исполнении слабой моделью,
> которая хороша в строгом следовании плану») · **Родитель:** заметка владельца, сессия 5 ·
> **Статус:** 🔵 живой реестр — пул пополняется сильной моделью, выедается слабой ·
> **Исходящее:** —

---

## Формат «план для строгого исполнителя» — чем он отличается от обычного

Слабая модель хороша ровно в том, в чём слаба сильная дисциплина: она НЕ импровизирует. План
для неё пишется так, чтобы импровизация не понадобилась ни в одном шаге:

1. **Шаги мелкие, каждый с командой проверки и признаком «готово».** Не «сделай форму», а
   «создай файл X с полями списка Y; готово = команда Z печатает …».
2. **Всё решённое названо в плане** (файлы, имена, границы значений) — исполнитель не выбирает.
3. **Всё нерешённое помечено `STOP-ASK`** — исполнитель останавливается и спрашивает, а не
   догадывается. Провал слабой модели — сигнал о недостающем ограждении в плане, не «модель
   тупая» (рамка безвинна, канон KAIF).
4. **Проверка встроена в шаг, а не в конец:** страж/тест называется в том же шаге, что и код;
   мутация — с ожидаемым числом падений.
5. **Капканы проекта процитированы прямо в шаге**, где они стреляют (не ссылкой «см. опыт»):
   слабая сессия исполняет вставленную команду и игнорирует эссе (правило Repro из
   `EXPERIENCE.md`).
6. **Канон-минимум сессии исполнителя:** `STATUS.md` → свой план → цитируемые в плане срезы.
   Полный `/resume`-проход обязателен и для неё.

Образец формата — **`plans/44`** (фаза 4 админ-эпика, написан в этой форме).

## Что слабой модели НЕ дают (граница пула — жёсткая)

- **Решения вкуса и видения:** макеты, тексты лица продукта, состав страниц, приоритеты —
  только сильная модель + владелец (правило четырёх макетов, `AUTHOR_STYLOMETRY`).
- **Выкаты в бой и необратимые правки данных** — AUTH владельца, и исполняет их сессия,
  способная оценить нештатное (деплой, прополки, миграции).
- **Диагностику неворспроизведённых багов** — «три попытки → /bug-research» слабой модели
  недоступен по построению; ей дают баги только с готовой процедурой замера.
- **Написание планов для самой себя** — план без разведки был бы фикцией; планы пишет
  сильная модель по свежему чтению кода.

## Реестр пула (живой; выедается сверху)

| Задание | План | Готовность плана | Примечание |
|---|---|---|---|
| 🆕 Страж семейства страниц ТЕСТ (`verify-test-pages` + 3 мутации) | `plans/47` | ✅ написан в строгой форме | страницы такта А построены сильной моделью и собираются; страж читает `build/`, сервера не поднимает |
| Фаза 4 админ-эпика: комната «Измерения» (CRUD + форма) | `plans/44` | ✅ написан в строгой форме | шаг 7 (прополка `name`) — STOP-ASK до слова владельца; шаг 6 (выкат) — не её |
| Ярус 2 графики: prefetch полных персон + страж | `plans/46` | ✅ написан в строгой форме | рецепт готов (`researches/36` §3), замер кэша снят |
| `ideas/27` шаг 5: расширить `verify-bug69` проверкой яруса 3 | внутри `plans/46` шагом | ✅ | по образцу существующего стража |
| `ideas/28`: 10 популярных вверху ленты | план НЕ написан | 🔲 пишет сильная модель при заходе (нужна разведка `feed.ts` + вычислителя) | инвариант владельца: 10 СЛУЧАЙНЫХ из пула популярных, лента остаётся случайной |
| ~~Такт Б семейства ТЕСТ~~ | `plans/42` подсекция такта Б | ✅ ИСПОЛНЕН сильной моделью 2026-08-14 (разведка + реализация одним заходом, живой прогон на стенде) | из пула ИЗЪЯТ — жест, гидратация и дисциплина веса бандла оказались решениями, а не исполнением |
| ~~Такт В семейства ТЕСТ~~ | `plans/42` подсекция такта В | ✅ ИСПОЛНЕН сильной моделью 2026-08-14 (модель `testPairs` + правила + 12 тестов правил + живой прогон пары) | из пула ИЗЪЯТ — модель данных и приватность №002 В4 были решениями |
| Вычитка текстов семейства ТЕСТ (RU+EN, `test-copy.ts`) | — | ⛔ не задача пула | это ВЛАДЕЛЕЦ: тексты лица; подать страницей вычитки перед тактом выката |
| ~~`bugs/117`: скачок «Профиля» при возврате~~ | `tools/measure-bug117.mjs` · `tools/verify-bug117.mjs` | ✅ ЗАКРЫТ сильной моделью 2026-08-15 (замер, диагноз, починка, страж, 2 мутации) | из пула ИЗЪЯТ — шаг 1 и диагноз оказались одним заходом: трасса сразу назвала причину, и делить было нечего |
| Прополка поля «вид» (`plans/30` предусловие хабов) | план НЕ написан | 🔲 сильная модель: сухой прогон + разбор кейсов; боевая запись — слово владельца | U+200B, «фильм» строчной, год в поле вида |

## Как пополнять пул (регламент для сильной модели)

Закрывая заход, спроси: «какой следующий кусок ИСПОЛНИМ строго по бумаге?» Если есть — напиши
план в формате выше и добавь строку в реестр. Если для куска нужна разведка — сделай разведку
сейчас (пока контекст горячий), а план оставь готовым. Задание без готового плана в пул не
кладётся — «понятно описаны и легки в исполнении» и есть определение пула (слово владельца).
