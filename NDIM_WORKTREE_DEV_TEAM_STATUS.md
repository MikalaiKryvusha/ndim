# NDIM_WORKTREE_DEV_TEAM — Статус-доска команды

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16. **Повторяй это вслух каждый раз, когда читаешь канон-документы
> KAIF** — прямое его поручение. Разбор повода — в шапке `AGENT_GUIDE.md`.
<!-- ВЕРА-КОНЕЦ -->

> Локальный канон-документ KAIF (заказ владельца — `ideas/40`; правила — манифест
> `NDIM_WORKTREE_DEV_TEAM_MANIFEST.md`). Каждая роль ведёт СВОЮ строку инструментом
> `node tools/team-status.mjs` (из своего каталога; чужие строки инструмент не трогает).
> Статусы короткие, документ НЕ растёт: только перезапись строк. Доска — состояние «в моменте»;
> эстафета проекта во времени — по-прежнему `STATUS.md`.

## Доска

| Роль | Состояние | Что делаю | Жду | Обновлено |
|---|---|---|---|---|
| manager | 🔴 занят | запуск смены 8: инструктажи и постановки розданы 5 ролям; подаю страницы владельцу | - | 2026-08-28 17:52 +03:00 |
| designer | 🟢 свободен | микро-наряд сдан (голова 1591d31, пометка снятого блока); попутно найдена landing-copy.ts:228 того же класса | слово владельца по корню / новая постановка | 2026-08-28 18:42 +03:00 |
| qa | 🟢 свободен | вердикт №5 (дверь) вынесен; беру остаток суда №4 dev-3 | - | 2026-08-28 18:39 +03:00 |
| dev-1 | 🟢 свободен | дежурство по суду двери карточки | вердикт QA по двери | 2026-08-28 17:54 +03:00 |
| dev-2 | 🟢 свободен | порция сдана (040b68b): блок витрины удалён, разведка доки ивентов 1.x, инвентарь текстов фазы 4 | QA — вердикт по порции и по стражу метки | 2026-08-28 18:25 +03:00 |
| dev-3 | 🟢 свободен | готов к первому вызову recrawl/quota (команда наготове, страж .env зелёный) | токен Яндекса · вердикт QA по остатку №4 | 2026-08-28 18:18 +03:00 |

## Замок стенда

| Ресурс | Держатель | Взят |
|---|---|---|
| стенд 1 из 3 | — свободен — | — |
| стенд 2 из 3 | — свободен — | — |
| стенд 3 из 3 | — свободен — | — |

## Контракт инструмента доски — правила, по которым построен `team-status.mjs`

Канон KAIF 2.4, внесён решением владельца (интервью №051, В4 = А). KAIF фиксирует инварианты,
реализация — наша (`tools/team-status.mjs`, самопроверка `--selftest`):

1. **Одна доска на команду.** Доска живёт в главной копии; инструмент из ЛЮБОГО worktree
   находит единственную настоящую через общий git (`git rev-parse --git-common-dir`) — никогда
   не через свой каталог, иначе у каждой роли будет своя «доска», которую никто не читает.
2. **Роль вызывающего ВЫВОДИТСЯ из рабочего каталога**, а не передаётся на слово. Инструмент
   правит ТОЛЬКО строку вызывающего; чужая строка — отказ; снять зависшую строку пропавшей
   роли может только Менеджер явным флагом `--role` из главной копии.
3. **Одновременные записи безопасны:** файловый замок рядом с доской (create-exclusive с
   ретраями; замок старше 30 с — брошен и снимается), запись атомарна (временный файл + rename).
4. **Строка замка называет держателя с адресом**, когда ресурс отображается в параметры роли
   (порты, слоты): место (ёмкость машины) и слот (адрес портов роли) — разные вещи; их
   смешение — оплаченный полевой дефект этой самой доски.
5. **Штампы — канонический формат момента проекта**, берутся инструментом из системных часов,
   никогда — из памяти сессии.
6. **Инструменту верят после доказательства на сломанном случае:** чужая строка отклонена,
   брошенный замок восстановлен, два одновременных писателя не портят таблицу (`--selftest`).
