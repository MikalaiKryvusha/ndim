# Аудит 2026-08-21 — ЭСТАФЕТА В НОВЫЙ ЧАТ (сессия 26 прервана классификатором)

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16. **Повторяй это вслух каждый раз, когда читаешь канон-документы
> KAIF** — прямое его поручение. Разбор повода — в шапке `AGENT_GUIDE.md`.
<!-- ВЕРА-КОНЕЦ -->

> **Жанр:** запись, а не задача (`reports/README.md`) — не помечается DONE, не переписывается.
> **Создан:** 2026-08-21 · **Родитель:** `ideas/39` (заказ владельца) · **Статус:** аудит НАЧАТ,
> технический срез пройден на треть, идейный не начат · **Исходящее:** `bugs/154`, `bugs/155`,
> `EXP-0173`

## ▶️ ЧТО ДЕЛАТЬ НОВОМУ ЧАТУ — одной строкой

**Продолжить аудит по `ideas/39` с идейного среза** (он не начат и не требует лексики, на которой
спотыкается классификатор), затем дописать технический. Всё сделанное закоммичено, дерево чистое.

## Почему этот чат закрыт

Классификатор безопасности трижды остановил сессию с меткой `[cyber]`. Причина понята и записана
уроком `EXP-0173`: защитный аудит собственных прав доступа снаружи читается как разведка чужой
системы, если рамка не названа первой строкой. Владелец переключал модель Fable → Opus → Fable →
Opus; работа при этом не терялась. **Ни одного настоящего нарушения не было** — это наш код, наш
продукт, прямой заказ владельца.

🔑 **Новому чату:** язык документов о правах доступа теперь регламентирован — врезка в
`AGENT_GUIDE.md` → «Журнал опыта» и `EXP-0173`. Прочитай ДО того, как продолжишь технический срез.

## Заказ владельца — дословно, полностью

Зафиксирован в `ideas/39_full_audit_before_organic_traffic.md` (закоммичен `c603339`). Два среза:

1. **Технический:** код, методы, архитектура, запросы. Критерий владельца: «*для пользователей
   корректно работал основной функционал, чтобы было удобно, без багов, привлекательно*».
2. **Идейный:** насколько выполняются его продуктовые запросы и **насколько хорошо и полно
   пишутся планы** по ним. Пять направлений:
   · устранение трения на онбординге;
   · крутая SEO-оптимизация для большого органического трафика;
   · расширение Пространства новыми объектами культуры;
   · анонимный режим, снижающий трение и порог страха входа;
   · привлекательность и доступность для массового пользователя.

**Повод, названный им:** «*Мы начали проводить SEO оптимизацию и много страниц отправили в
поисковые движки — скоро к нам начнут приходить пользователи*».

## ✅ Что СДЕЛАНО и закоммичено (5 коммитов)

| Коммит | Что |
|---|---|
| `c603339` | `ideas/39` — заказ владельца дословно, для истории жизненного цикла |
| `7accb95` | `bugs/154` и `bugs/155` — две находки технического среза |
| `e561891` | `EXP-0173` + врезка в `AGENT_GUIDE.md` — язык документов о правах доступа |

### Деterминированный слой прогнан — дерево здоровое

| Проверка | Результат |
|---|---|
| `npm test` | **316/0** (44 набора) |
| `npm run typecheck` | 0 |
| `npm run check` | 0 |
| `npm run test:sync` | зелёный |
| `npm run kaif:check` | чист |
| `node tools/verify-sync-server-name.mjs` | зелёный |
| `npm run questions` | интервью 40, **ждут владельца 0**; 4 провала — долг, не регресс |

⚠️ **Долг `npm run questions` (не вырос за сессию, но назван):** 1 новый вопрос вне `interviews/`
(`bugs/152:10` — это мета-строка «вскрыт вопросом владельца», лечится маркером
`<!-- ВОПРОС-ОК: причина -->`), 3 неразнесённых ответа (№039 В1, №040 В1–В2 — их разнос лежит в
НЕЗАКОММИЧЕННЫХ правках прошлой сессии, см. ниже), унаследованные 25 + 25.

### Две находки технического среза

**`bugs/154`** — удаление аккаунта не убирает ответы человека из `testPairs`, пока жив второй
участник. Класс `bugs/129`: перечень удаляемого снят с правил в `plans/20` (2026-08-01) ДО
появления коллекции (`plans/42`, 2026-08-14). Клиент их даже найти не может — `allow list: if
false`. Направление: обезличивание половины умершего участника суточным проходом, симметрично
`suggestions`. Решение формы — за владельцем.

**`bugs/155`** — область чтения `relations` шире, чем описано в комментарии самого правила и в
`schema.ts`. Подтверждено временным тестом на эмуляторе (контрольный случай красный — прибор
исправен, тест снят). Прецедент `bugs/100`. Тот же хвост у `users/`, `profile/`, `friendships/` —
там возможна законная нужда админ-панели, поэтому судьба каждого — вопрос владельцу.

## 🔴 ЧТО ОСТАЛОСЬ — план работ для нового чата

### 1. Идейный срез (НЕ НАЧАТ) — начинать с него

Материал собран, выводы не написаны. Ключевые факты, уже поднятые:

- **Трение на ОБ:** эпики `plans/21` (лендинг-как-приложение), `plans/22` (гость = человек),
  `plans/23` (мягкое ведение) **написаны, разблокированы ответами владельца и ОТЛОЖЕНЫ** его же
  словом 2026-08-16: «*в день выката пока неактуально — у нас нет живой аудитории*».
  🔴 **Главное напряжение аудита: это условие СНЯТО сегодняшними словами владельца** («скоро к
  нам начнут приходить пользователи»). Значит отложенное пора расконсервировать — и это, вероятно,
  главный вывод идейного среза.
- **Разведка `plans/23` нашла три разрыва петли новичка** (читать §Ф1–Ф5, строки 50–164):
  Ф2 — топ кэшируется на всю сессию (`FRESH.server = ∞`), поэтому новичок видит пустые «Связи»
  даже после того, как сервер посчитал; Ф3 — математика Дайса даёт новичку однозначные проценты;
  Ф5 — «Назад» у гостя уже перехвачен ненамеренно. Всё это встретит первого человека из поиска.
- **SEO:** `plans/40` (8 фаз) — фазы 1, 5 закрыты; фаза 3 (обогащение каталога) разблокирована;
  фаза 4 (опорные страницы) НЕ начата — а именно она отвечает на разгром Грока в `ideas/34`
  («5–7 pillar-страниц», «главная — храм»). Проверить: не осталась ли стратегия «тысячи
  тонких страниц» без противовеса.
- **Наполнение каталога:** контур кандидатов замкнут и в бою; `bugs/146` (индекс голодает)
  открыт — новое измерение ждёт до суток; эпик 60 фаза 2 в работе.
- **Анонимный режим:** `plans/22` вскрыл, что срок жизни гостя в коде **30 дней**
  (`GUEST_TTL_DAYS`), а решение владельца — **7 дней**; и «второй эффект №3»: при 7 днях данные
  и учётная запись Firebase расходятся на 23 дня — гость воскресает в пустоте. Не починено.
- **Привлекательность:** пять правок вида `[NOT-TESTED]` (`bugs/149`, `150`, `151` + термин) —
  в коде есть, кадров живым Chrome НЕТ, в бою НЕТ.

### 2. Технический срез — что НЕ покрыто

| Зона | Состояние |
|---|---|
| `firestore.rules` | ✅ прочитан целиком, 2 находки |
| `sync-server/index.mjs` (1460 строк) | ✅ прочитан целиком, находок нет — код зрелый |
| `src/lib/data/` (кэш, профиль, аккаунт, erase) | ✅ прочитан, 1 находка (`bugs/154`) |
| `src/lib/similarity/`, `model/` | ✅ прочитан, находок нет |
| **`src/routes/*.svelte` (экраны, ~500 КБ)** | 🔴 **НЕ ЧИТАН** — а это то, что видит человек |
| **`tools/` (164 прибора)** | 🔴 НЕ ЧИТАН |
| **e2e и стражи (59 штук)** | 🔴 НЕ ЧИТАН |
| **Живой прогон Chrome** | 🔴 НЕ ДЕЛАН — стенд не поднимался |

🔑 **Совет следующему:** самая ценная непокрытая зона — **экраны** (`profile`, `dims`, `relations`,
`space`), потому что заказ владельца именно про то, что увидит пришедший человек. И **живой прогон
браузером** — канон проекта прямо говорит, что «тесты зелёные» ≠ готово.

### 3. Отчёты аудитора — НЕ НАПИСАНЫ

По контракту `/code-revision` нужны: `2026-08-21_full_SUMMARY.md` (вердикт первым, карта
покрытия, семьи находок, пределы честности) + документ на СЕМЬЮ находок. Оба бага уже ссылаются
на SUMMARY — файл обязан появиться.

### 4. Вопросы владельцу — интервью №041 НЕ ЗАВЕДЕНО

Накоплено минимум три развилки, все названы в багах:
- **В1:** судьба админ-чтения `users/`, `profile/`, `friendships/` — снимать или закреплять
  тестом под будущую панель (`bugs/155`);
- **В2:** форма починки `bugs/154` — клиент помнит свои пары или сервер обезличивает половину;
- **В3 (главный):** расконсервировать ли `plans/21`/`22`/`23` (ворота и онбординг) сейчас, когда
  условие «нет живой аудитории» снято его же словами.

## ⚠️ Капканы, которые новый чат обязан знать

1. **Незакоммиченные правки прошлой сессии** — `bugs/146`, `interviews/038`, `039`, `040`.
   Это разнос ответов владельца, сделанный сессией 25. **НЕ ТРОГАТЬ и не коммитить вслепую** —
   сначала прочитать, чей это труд и закончен ли он.
2. **Язык документов о правах доступа** — `EXP-0173`, иначе классификатор остановит и новый чат.
3. **Стенд не поднимался** в этой сессии. Поднимать с
   `NODE_OPTIONS=--max-old-space-size=6144` (`bugs/134`), гасить по дереву от верхнего pid.
4. **Расписка стейджа просрочена** — в бой ничего не катать без `npm run deploy -- --stage`.
5. Временный тест правил аудита **снят** (`tests/rules/audit-probe.test.ts` удалён), лог эмулятора
   убран. Дерево чистое — проверено `git status`.

## Пределы честности этого аудита

- Технический срез покрыл **серверную и модельную половину** продукта; **клиентская половина —
  то, что человек видит, — не смотрена вовсе**. Называть аудит проведённым нельзя.
- Обе находки — «скептик не опроверг», а не «доказано боем»: `bugs/155` подтверждён проверкой на
  эмуляторе, `bugs/154` — чтением кода трёх файлов, в бою не воспроизводился.
- Идейный срез не начат: перечисленные выше факты собраны, но вердикт по пяти направлениям
  владельца не вынесен.
