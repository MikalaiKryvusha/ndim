/**
 * СОГЛАШЕНИЕ О ТЕГАХ КАТАЛОГА — общий модуль для приборов мастерской.
 *
 * Заказ владельца 2026-08-17, дословно: «*создай наборы обязательных тегов как класс правило для
 * разных измерений — они должны ИИ агентом с высокой гарантией попадать в написанные им кандидаты,
 * как правило*».
 *
 * 🔴 «С ВЫСОКОЙ ГАРАНТИЕЙ» — ЭТО МЕХАНИЗМ, А НЕ ПАМЯТЬ. Правило, записанное только словами в
 * README, держится на внимательности сессии; ровно на ней оно и сломалось (`bugs/144`: агент
 * поставил десяти фильмам теги «фэнтези · приключения · Нолан» и ни одного обязательного). Поэтому
 * соглашение читают ДВОЕ ворот, и оба fail-closed:
 *   · приёмка описаний (`check-candidate-descriptions.mjs`) — говорит, чего не хватает;
 *   · сама выгрузка (`seed-dim-candidates.mjs`) — ОТКАЗЫВАЕТСЯ класть такого кандидата в очередь.
 * Второе важнее первого: приёмку можно не запустить, а мимо выгрузки кандидат в очередь не попадёт.
 *
 * 🔑 ИСТОЧНИК СОГЛАШЕНИЯ — КАТАЛОГ, А НЕ ЭТОТ ФАЙЛ. `candidates/tag-conventions.json` ВЫВЕДЕН
 * замером 5111 боевых записей (`tools/measure-catalog-tags.mjs`, порог 90 % записей вида) и
 * перевыводится одной командой. Список, набранный руками, устарел бы в день, когда владелец заведёт
 * новый вид объектов, и разошёлся бы с каталогом молча — это пара «истина ↔ зеркало», и истина
 * здесь в данных.
 */
import { readFileSync, existsSync } from 'node:fs';

const FILE = 'candidates/tag-conventions.json';

let cache = null;

/** Всё соглашение: `{ "фильм": ["фильм","movie","кино","film"], … }`. Ключи — вид в нижнем регистре. */
export function tagConventions() {
  if (cache !== null) return cache;
  if (!existsSync(FILE)) {
    throw new Error(
      `Соглашение о тегах не найдено: ${FILE}. Перевыведи его замером: node tools/measure-catalog-tags.mjs`,
    );
  }
  cache = JSON.parse(readFileSync(FILE, 'utf8'));
  return cache;
}

/**
 * Обязательные теги для вида. Вид сверяется в нижнем регистре: «Фильм» и «фильм» — один вид.
 * Вид, которого нет в соглашении, обязательных тегов не имеет — и это ЧЕСТНЫЙ ответ, а не «ноль
 * по умолчанию»: у редких видов записей мало, и доля на них ничего не значит.
 */
export function mandatoryTagsFor(kindRu) {
  const kind = String(kindRu ?? '').trim().toLowerCase();
  return tagConventions()[kind] ?? [];
}

/**
 * Каких обязательных тегов не хватает записи. Сравнение регистронезависимое: «Кино» и «кино» —
 * один тег, и требовать второй копии было бы придиркой прибора.
 */
export function missingMandatoryTags(record) {
  const need = mandatoryTagsFor(record?.type?.ru);
  if (need.length === 0) return [];
  const have = new Set(
    (Array.isArray(record?.tags) ? record.tags : []).map((t) => String(t).trim().toLowerCase()),
  );
  return need.filter((t) => !have.has(t));
}

/**
 * ⛔ ИМЁН СОБСТВЕННЫХ В ТЕГАХ НЕТ — правило владельца, подтверждённое замером.
 *
 * Его слова: «*я тегами описывать старался не имена собственные, а суть, что есть данный объект
 * культуры*». Замер 5111 записей согласен с ним: значений тегов с заглавной буквы 681 из 43 011
 * (1,6 %), и это почти целиком аббревиатуры жанров и форматов — RPG, MMORPG, K-pop, TV series.
 * Имён людей в тегах каталога нет.
 *
 * 🔑 Прибор не судит «имя это или не имя» — задача неразрешима машиной без словаря. Он ловит
 * ровно ту форму, которой владелец не пользуется: тег, начинающийся с ЗАГЛАВНОЙ и не входящий в
 * список известных аббревиатур. Это подсказка агенту, а не приговор, и потому она отделена от
 * обязательных тегов: те — отказ, эта — предупреждение.
 */
export const KNOWN_UPPERCASE_TAGS = new Set(
  ['rpg', 'mmorpg', 'mmo', 'moba', 'fps', 'rts', 'jrpg', 'pvp', 'pve', 'vr', 'edm', 'k-pop',
    'r&b', 'pc game', 'tv series', 'tv show', 'latin pop', 'eurodisco', 'christmas',
    'dungeons & dragons', 'world war ii', 'вторая мировая война', 'средневековье', 'rpg elements'],
);

export function looksLikeProperName(tag) {
  const raw = String(tag ?? '').trim();
  if (raw === '') return false;
  if (KNOWN_UPPERCASE_TAGS.has(raw.toLowerCase())) return false;
  const first = raw[0];
  return first === first.toUpperCase() && first !== first.toLowerCase();
}

/*
 * ═══ ПРОВЕРКИ НА КОРПУС, А НЕ НА КАРТОЧКУ (шаг 4 лечения `bugs/178`) ══════════════════════════
 *
 * 🔴 ЗАЧЕМ ОТДЕЛЬНЫЙ КЛАСС ПРОВЕРОК. Ворота приёмки судят ОДНУ карточку и по построению не видят
 * расхождения КОРПУСА: у каждой отдельной записи теги чётны и обязательные на месте, а каталог
 * при этом зовёт одну и ту же вещь двумя разными именами. Живой случай `bugs/178`: у тега
 * «симулятор» английских пар ДВЕ — `simulator` 31 запись против `simulation` 32, плюс 5 записей
 * без пары вовсе. Каждая карточка чиста, соглашения у каталога нет.
 *
 * ✅ ЧТО ЕСТЬ ПАРА — НАЗНАЧЕНО ЗАМЕРОМ, А НЕ ДОГАДКОЙ. Проверено на двух корпусах из git:
 *   · наши партии 1–4 (50 карточек, 568 значений) пишут теги ЧЕРЕДУЯ: «фильм, movie, кино, film»;
 *   · каталог владельца (срез 50 записей) пишет их БЛОКАМИ: «кино, фильм, триллер, movie, film,
 *     thriller».
 * Соседство работает только на первом — 283 пары против 1 промаха у нас и 101 против 116 у него.
 * А ПОЗИЦИЯ работает на обоих: i-й кириллический тег отвечает i-му латинскому. На партиях —
 * 50 записей из 50 с равными блоками, на срезе каталога — 43 из 50.
 *
 * ⚠️ ОСТАТОК ИЗМЕРЕН И НАЗВАН. На срезе каталога правило дало 9 расколов, и они трёх разных
 * пород, а не одной:
 *   · НАСТОЯЩИЕ — «фантастика» → science fiction | sci-fi · «спорт» → skiing | sport;
 *   · НЕВИДИМЫЙ СИМВОЛ — «thriller» и «thriller␡» с `U+200B` на хвосте: на глаз одно слово,
 *     для машины два разных тега. Отдельная находка, заведена багом;
 *   · ПОРЯДОК ВНУТРИ БЛОКА — «кино» → movie ×26 | film ×1 и «фильм» → film ×25 | movie ×3: те же
 *     два тега, записанные в обратном порядке в одной записи. Это ШУМ правила, и он виден по
 *     доле: настоящий раскол делится примерно поровну (31/32), шум даёт 26 к одному.
 * Поэтому `splitTagPairs` возвращает ВСЕ находки со счётом, а решение «раскол это или порядок»
 * оставляет читающему: прибор предъявляет, а порог здесь назначается не мною.
 */

/** Невидимые знаки, которые превращают один тег в два. `U+200B` найден в живом каталоге. */
const НЕВИДИМЫЕ = /[\u200B-\u200F\u202A-\u202E\u2060\uFEFF\u00AD]/u;

const кириллический = (t) => /\p{Script=Cyrillic}/u.test(t);
const латинский = (t) => /\p{Script=Latin}/u.test(t) && !/\p{Script=Cyrillic}/u.test(t);
const чистый = (t) => String(t ?? '').trim().toLowerCase();

/**
 * НЕВИДИМЫЙ ЗНАК В ТЕГЕ — точная проверка без единого ложного срабатывания.
 * Возвращает список значений с кодами найденных знаков; пустой список означает «чисто».
 */
export function invisibleCharTags(record) {
  const найдено = [];
  for (const t of (Array.isArray(record?.tags) ? record.tags : [])) {
    const s = String(t);
    if (!НЕВИДИМЫЕ.test(s)) continue;
    const коды = [...s].filter((ch) => НЕВИДИМЫЕ.test(ch))
      .map((ch) => `U+${ch.codePointAt(0).toString(16).toUpperCase().padStart(4, '0')}`);
    найдено.push({ тег: s, коды: [...new Set(коды)] });
  }
  return найдено;
}

/**
 * ПАРЫ ТЕГОВ ОДНОЙ ЗАПИСИ — i-й кириллический к i-му латинскому.
 * Блоки разной длины паре не поддаются, и это ЧЕСТНЫЙ отказ, а не «пар нет»: возвращается `null`,
 * чтобы вызывающий мог посчитать такие записи отдельно, а не принять их за чистые.
 */
export function tagPairsOf(record) {
  const теги = (Array.isArray(record?.tags) ? record.tags : []).map(чистый).filter(Boolean);
  const ru = теги.filter(кириллический);
  const en = теги.filter(латинский);
  if (!ru.length || ru.length !== en.length) return null;
  return ru.map((r, i) => [r, en[i]]);
}

/**
 * РАСКОЛОТЫЕ ПАРЫ ПО ВСЕМУ КОРПУСУ: русский тег, у которого английских спутников больше одного.
 *
 * Возвращает `{ расколы, записей, безПар }`, где каждый раскол — `{ тег, спутники: [[en, n], …] }`,
 * спутники отсортированы по убыванию счёта: доля и есть то, по чему читающий отличит настоящий
 * раскол (31/32) от шума порядка (26/1).
 */
export function splitTagPairs(записи) {
  const карта = new Map();
  let сПарами = 0;
  let безПар = 0;
  for (const r of записи ?? []) {
    const пары = tagPairsOf(r);
    if (!пары) { безПар += 1; continue; }
    сПарами += 1;
    for (const [ru, en] of пары) {
      if (!карта.has(ru)) карта.set(ru, new Map());
      const м = карта.get(ru);
      м.set(en, (м.get(en) ?? 0) + 1);
    }
  }
  const расколы = [...карта]
    .filter(([, м]) => м.size > 1)
    .map(([тег, м]) => ({ тег, спутники: [...м].sort((a, b) => b[1] - a[1]) }))
    .sort((a, b) => b.спутники.length - a.спутники.length || a.тег.localeCompare(b.тег, 'ru'));
  return { расколы, записей: сПарами, безПар };
}
