# Баг 142 — Нет поля комментария и кнопки «Вернуть на доработку»

> **Заведён:** 2026-08-17, нашёл владелец вычиткой в бою · **Родитель:** `plans/30` фаза 6
> **Статус:** ✅ **ПОЧИНЕН 2026-08-17** — четвёртое действие построено с обеих сторон контура,
> правила 139/0, `verify-admin-dims` 60/0, `verify-candidate-return-loop` 11/0, четыре мутации.
> 🚀 **В БОЮ с 2026-08-17 ≈21:40** (AUTH владельца: «*депрой в прод, админку вижу только я, это не
> заденет пользователей*»). Правила выкачены в `ndim-db-prod` первым шагом двери; в отданном боем
> чанке стоят кнопка возврата, поле комментария, `ownerNote` и статус `returned`.
> **Исходящее:** развилка снята НАХОДКОЙ, а не вопросом — см. раздел «Развилка» ниже

## Симптом — словами владельца

> «*Ты не сделал поле комментария моего и ИИ агента. Я хотел написать комментарий в кандидате и
> нажать кнопку [Вернуть на доработку].*»

В очереди у карточки три действия: «Одобрить», «Правка перед одобрением», «Отклонить». **Четвёртого —
«Вернуть на доработку» — нет, и поля для комментария нет.** Комментарий агента (`agentNote`) в базе
есть и на экране показывается, но владельцу написать свой некуда.

## Форензика — это не описка, а срезанный мною объём

🔴 **Метаплан называл ЧЕТЫРЕ действия, я построил три.** `plans/30`, фаза 6, дословно: «*четыре
действия владельца одной кнопкой каждое, как он и просил: **одобрить · отредактировать · отклонить ·
вернуть с комментарием***». Там же стоит и требование к форме комментария: «*комментарий крепится к
КОНКРЕТНОМУ ПОЛЮ, а не к карточке вообще (практика отрасли: „pins the comment to exactly that
sentence“)*».

Почему срезал: строил контур за один заход и оставил «возврат» как наименее срочное — при пустой
очереди возвращать нечего. **Рассуждение неверно, и он показал это первым же прогоном:** возврат
нужен ровно тогда, когда карточка почти хороша, — то есть в самом частом случае, а не в редком.
Отклонить хороший кандидат из-за одной формулировки значит потерять работу разведки.

⚠️ И структурная причина: у статуса кандидата сегодня три значения (`pending` · `approved` ·
`rejected`), объявленные в `firestore.rules` списком. Возврат требует четвёртого — и правило его
сейчас **отвергнет**. То есть фикс трогает правила вместе с тестами-отказами, а не только экран.

## Что делать (фикс)

1. **Правила Firestore:** словарь статусов пополняется значением возврата (`returned`), тесты-отказы
   на него дописываются в тот же блок «Очередь кандидатов».
2. **Поля комментариев — ДВА и они РАЗНЫЕ, путать нельзя:**
   · `agentNote` — что написал агент владельцу (есть, только для чтения);
   · **`ownerNote`** — что написал владелец агенту (нет, завести).
   Одно поле на двоих затёрло бы одну из сторон и сделало бы диалог невозможным.
3. **Экран:** поле ввода комментария в карточке + кнопка **[Вернуть на доработку]**, активная только
   когда комментарий непуст, — возврат без объяснения не даёт агенту ничего и стоит владельцу
   повторной вычитки.
4. **Агент читает возвращённое запросом по статусу** — это и есть обратное плечо контура: он берёт
   `returned`, правит по комментарию и кладёт обратно в `pending`.

## 🔴 Развилка — СНЯТА НАХОДКОЙ, а не вопросом владельцу

Тикет собирался спросить, к чему крепится комментарий: **A)** один на карточку или **B)** свой у
каждого поля (метаплан `plans/30` требовал второго, ссылаясь на практику отрасли «pins the comment
to exactly that sentence»).

🔑 **Спрашивать было не о чем: владелец это уже выбрал.** Утверждённый им макет комнаты — V1
«Стопка» (`design/dims-manager-mockups.html`) — несёт ровно **одно поле «ваш комментарий агенту» на
карточку** и четвёртую кнопку «Вернуть на доработку». То есть форма была решена в тот же день, когда
он выбирал вид комнаты, а строка метаплана про пофайловое крепление — **довод агента, написанный ДО
показа макетов**. Выбор владельца старше довода агента.

⚠️ **Класс, которого я едва не добавил:** спросить второй раз то, что уже отвечено, — это налог на
внимание владельца, ровно того рода, что и лишнее нажатие из `bugs/141`. Проверка перед вопросом
стоила одного чтения файла макетов. Родня — `EXP-0165`: «нет записи» есть утверждение о ПОИСКЕ, а не
о мире; здесь поиск был проведён и запись нашлась.

📍 Захочет точнее — это одна его строка правки, а не переделка контура: поле у каждого поля
надстраивается над тем же `ownerNote`, став картой «поле → замечание».

## Критерий приёмки — что будет ВИДНО работающим

- Владелец пишет комментарий в карточке, нажимает **[Вернуть на доработку]** — кандидат уходит из
  очереди, в каталог не записано **ничего**, комментарий лежит в `ownerNote`.
- Агент видит возвращённое запросом по статусу и читает комментарий.
- Кнопка неактивна при пустом комментарии.
- Проверка в `tools/verify-admin-dims.mjs`: возврат меняет статус, пишет `ownerNote`, **не создаёт
  измерение** (вердикт снимается с базы), и `agentNote` при этом не затёрт.
- **Мутация:** разрешить возврат с пустым комментарием → проверка краснеет; писать комментарий
  владельца в `agentNote` → краснеет проверка «`agentNote` не затёрт».

## ✅ Исполнено 2026-08-17 — что сделано и чем доказано

**Правки, по обеим сторонам контура:**

| Где | Что |
|---|---|
| `firestore.rules` | четвёртый статус `returned` в словаре; **три новых теста-отказа** (набор 131 → **139**) |
| `src/lib/data/admin-dims.ts` | `returnCandidate(candidate, note)` — статус, `ownerNote`, отказ на пустом комментарии; `agentNote` переживает возврат |
| `src/routes/admin/dims/+page.svelte` | поле «Ваш комментарий агенту» и кнопка **[Вернуть на доработку]**, неактивная при пустом комментарии |
| `tools/read-returned-candidates.mjs` | 🆕 **то, чем агент УЗНАЁТ о замечании** — читает по статусу, ничего не пишет |
| `tools/seed-dim-candidates.mjs` | принимает исправленное обратно и сохраняет замечание владельца |

🔴 **Смежный дефект, найденный чтением кода в тот же заход и починенный вместе.** Прибор выгрузки
считал разобранным всё, что не `pending` (`status !== 'pending'`). С появлением четвёртого статуса
это замкнуло бы контур **в тупик**: возвращённая карточка не вернулась бы к владельцу никогда, а
прибор честно печатал бы «не тронут». Тот же класс, что `bugs/129` и `bugs/140`. Список решённых
статусов теперь назван ПОИМЁННО, а не отрицанием.

🔑 **И половина, без которой кнопка была бы бутафорией.** Владелец пишет замечание в базу, а агент —
сессия с пустым контекстом, которая в базу сама не заглядывает. Замечание, которое некому прочитать,
равносильно ненаписанному. Поэтому читатель заведён В ТОТ ЖЕ ЗАХОД, что и кнопка.

**Проверка.** `verify-admin-dims` **60/0** (девять новых проверок п.9м…п.9у: кнопка есть, при пустом
комментарии неактивна, с непустым оживает, статус `returned`, `ownerNote` записан, `agentNote` не
затёрт, измерение НЕ заведено, карточка ушла из очереди). 🆕 `verify-candidate-return-loop`
**11/0** — круг целиком, без экрана: выгрузил → вернули → поправил → вернулось в очередь, замечание
уцелело, решённое не тронуто, читатель показал. Правила **139/0**.

**Четыре мутации, у каждой адресат назван ДО прогона:**

| Мутация | Предсказание | Результат |
|---|---|---|
| кнопка активна при пустом комментарии | краснеет п.9н | **1** адресный провал при 59 зелёных |
| комментарий владельца пишется в `agentNote` | краснеют п.9р и п.9с | **2** адресных провала при 58 зелёных |
| словарь статусов без `returned` | краснеют три теста правил | **3** провала при 136 зелёных |
| выгрузка считает `returned` решением | краснеет круг возврата | **4** провала при 7 зелёных |

⚠️ **Поймано на себе и названо:** первая редакция проверки «возвращённый ушёл из очереди» была
ГОНКОЙ — экран печатает ответ до того, как перечитает очередь (`saved` ставится перед `refresh()`).
Она покраснела на исправном продукте. Лечение — явное ожидание с потолком (`waitGone`), и та же
лотерея снята у одобрения, где она просто ещё не выстрелила.

## Решения, принятые без владельца

1. **Форма комментария — одно поле на карточку.** Не мой выбор: так устроен утверждённый им макет
   V1 «Стопка» (разбор выше). Строка метаплана про крепление к полю отменена как более ранний довод
   агента.
2. **Возвращённая карточка уходит из очереди владельца.** Очередь отвечает на вопрос «что ждёт
   Вашего решения», а возвращённое ждёт работы АГЕНТА. Оставить её на глазах значило бы показывать
   владельцу то, по чему он уже высказался. Вернётся, когда агент положит исправленное.
3. **Отдельного экрана возвращённых нет.** Их читает прибор агента; заводить владельцу вкладку,
   в которую он не ходит, — лишняя сущность.
4. **Проект эмулятора приборы берут из окружения** (`GCLOUD_PROJECT`), а не константой. Константа
   молча уводила запись в соседнюю базу, и страж круга видел пустоту при исправном приборе.
