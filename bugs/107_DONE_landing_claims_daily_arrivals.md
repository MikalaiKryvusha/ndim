# Bug 107 — лендинг обещает «каждый день приходят новые», а приходит один человек в три недели

**Статус:** ✅ ЗАКРЫТ 2026-08-30 (Дизайнер, смена 14) — симптом починен `413a8a8`, проверено четырьмя способами; разбор в конце документа · найден 2026-08-02 замером боя (`tools/probe-prod-stats.mjs`) ·
**Версия/сборка:** 2.0, БОЙ (`ndimspace.app`) · **Класс:** утверждение о факте без опоры на источник
(`GOAL.md` принцип 8) · **Родня:** прямое продолжение `bugs/07`.

---

## Симптом

Первый экран лендинга — единственная страница, которую видит человек с улицы, — говорит:

> **С нами уже 94 человека — и каждый день приходят новые**

`src/routes/+page.svelte:142–143` (`joinedPre` / `joinedPost`), выводится безусловно (`:259`).
Английская версия утверждает то же: *«— and new ones come every day»*.

**Собственные данные проекта это опровергают.** Снимки дней `space/stats/daily`, прочитанные из боя
2026-08-02:

| Дата снимка | Людей |
|---|---|
| 2026-07-29 | 94 |
| 2026-07-30 | 94 |
| 2026-07-31 | 94 |
| 2026-08-01 | 94 |
| 2026-08-02 | 94 |

Четверо суток подряд не пришёл **никто**. Шире: cutover 2026-07-12 дал **93** человека с оценками
(`plans/04` — 331 учётная запись, 330 точек), сейчас **94**. Это **+1 человек за 21 день**, то есть
темп «новый человек раз в три недели», а не «каждый день».

Соседнее число из того же замера показывает, почему: `activeIn7Days = 2` — за неделю свои оценки
трогали двое из 94.

## Почему это дефект, а не маркетинг

`GOAL.md`, принцип 8 мастер-плана (**«Обещание опирается на источник»**, добавлен ревизией
2026-08-01):

> Каждое утверждение на лице продукта обязано опираться либо на доказанный источником факт, либо
> на то, что мы действительно делаем. **Обещание без опоры — не маркетинг, а враньё**, и оно бьёт
> по первому же пункту нашего же списка («у нас прозрачно и честно»).

Здесь утверждение опровергается не чужим источником, а **нашей собственной базой**, причём тем
самым документом, который для витрины и считается.

⚠️ Отдельная тяжесть: рядом с витриной по решению владельца (интервью №010, Р7) встанут
**настоящие отзывы настоящих людей**. Ложная строка в двух сантиметрах от них обесценивает и их,
и себя — ровно тот довод, которым владелец отменял множитель ×30.

## Родство с `bugs/07` — тот же дефект, недочиненный

`bugs/07` («лендинг врёт: число зашито в код») ловил **всю фразу целиком**, включая хвост про
«каждый день приходят новые». Починили тогда **только число** — оно стало настоящим. Хвост,
который является отдельным и более сильным утверждением, остался.

🔑 **Урок класса:** когда дефект — во ФРАЗЕ, чинить её надо целиком. Число и утверждение вокруг
числа — две разные лжи, и исправление первой маскирует вторую (она выглядит «уже проверенной»).

## Второй, независимый дефект той же строки

Число на витрине — **не живое**, а снимок сборки: `src/lib/content/landing-metric.ts` содержит
`people: 94, takenAt: '2026-07-31'`. Совпадение с боем сегодня — везение, а не механизм: между
выкатами число застывает (это осознанный размен `bugs/81` ради пререндера, цена названа в шапке
`tools/snapshot-landing-metric.mjs`).

При этом решение владельца **Р7** (интервью №010) звучало так: *«Делаем живую настоящую цифру. И
анонимов и полноценных считаем и показываем»*. Не исполнена **ни одна** из двух половин: цифра не
живая, анонимы в счёт не входят (`stats.ts:219` — `!point.anonymous`). Долг записан самим кодом в
`src/lib/data/metrics.ts:53–57`.

## ✅ РЕШЕНИЕ ВЛАДЕЛЬЦА (интервью №014, В1 = вариант **В**, 2026-08-02)

Выбран самый дорогой из четырёх и единственный, который чинит **класс**, а не случай:

> **В) Сделать фразу ВЫВОДИМОЙ из данных.** Показывать хвост только когда он правда: снимки дней
> уже есть, «за неделю пришло N» из них считается. Тогда строка не сможет соврать никогда — она
> либо есть, либо её нет.

Что это значит для реализации:

- Хвост про приток — **не константа в тексте, а вычисляемое утверждение**. Нет притока — нет и
  строки; она не заменяется на «пока никто не приходил» (это уныние на витрине), а просто
  отсутствует.
- Считается из `space/stats/daily`: разность `people` между сегодняшним снимком и снимком
  недельной давности. Механика чтения снимков в проекте уже есть — `snapshotOnOrBefore`
  (`src/lib/model/stats.ts`), на ней стоят тренды экрана «Пространство».
- ⚠️ **Число обязано ехать в ПРЕРЕНДЕР**, как и само `people` (`bugs/81`): иначе строка будет
  доезжать «на горячую» — ровно то, что владелец забраковал в `ideas/21` п. 10. Значит приток
  снимается тем же инструментом, что и витрина (`tools/snapshot-landing-metric.mjs`), и живёт
  рядом в `landing-metric.ts`.
- ⚠️ **Формулировку хвоста при N > 0 владелец пока не называл.** «И каждый день приходят новые»
  не годится даже при N > 0: «за неделю пришло 3» — это не «каждый день». Нужен текст, честный
  при любом N ≥ 1. Это вопрос следующего интервью, а не работа агента.

## Варианты, которые были предложены (для истории решения)

Формулировку витрины агент не сочиняет: это бренд. Варианты, из которых выбирал владелец:

- **А. Убрать хвост.** «С нами уже 94 человека». Правда, ничего не обещает, работает при любом темпе.
- **Б. Сказать правду о темпе иначе** — что-то, что верно при 94 людях и двух активных: например,
  про сам труд («94 человека уже описали себя по измерениям»), а не про приток.
- **В. Сделать утверждение ВЫВОДИМЫМ.** Показывать хвост только когда он правда: снимки дней уже
  есть, «за последние 7 дней пришло N» вычисляется из них. Тогда фраза не может соврать — она
  либо есть, либо её нет. Дороже, но это единственный вариант, который чинит КЛАСС, а не случай.
- **Г. Слово владельца.**

⚠️ Что бы ни выбрали — правку надо делать **в обоих языках сразу** (RU и EN), и EN основной
(интервью №009, В8).

## План фикса

- [x] **Шаг 0 — развилка закрыта владельцем 2026-08-02**: интервью №014, В1 = **вариант В**
      («сделать утверждение ВЫВОДИМЫМ из снимков дней»). Спрашивать больше нечего — исполняем В.
- [x] **Шаг 1** — ✅ СДЕЛАН коммитом `413a8a8` «витрина хвастается размером Пространства, а не считает головы». Фразы нет ни в одном языке.
- [ ] 🔴 **Шаг 2 — ОСТАЁТСЯ ДОЛГОМ и стал важнее, чем был (см. «Класс жив» ниже). Страж на КЛАСС, а не на строку:** проверка «на лендинге нет утверждения о факте,
      которое не выводится из данных». Минимально исполнимая форма — список запрещённых обещаний
      («каждый день», «every day», «постоянно», «growing») в текстах лендинга, красящий сборку.
- [ ] ⚠️ **Шаг 3 — отпал вместе с выбором В** (см. расхождение ниже). Был: при выборе В: считать приток из `space/stats/daily` и отдавать его в пререндер
      тем же путём, что и `landing-metric.ts`.

## Ссылки

`tools/probe-prod-stats.mjs` (замер боя, которым найдено) · `src/routes/+page.svelte:142–143,259` ·
`src/lib/content/landing-metric.ts` · `src/lib/data/metrics.ts:53–57` (записанный долг по Р7) ·
`bugs/07_DONE_landing_fake_people_counter.md` (недочиненная родня) · `GOAL.md` (принцип честности) ·
`MASTER_PLAN.md` принцип 8 · `interviews/interview_010_gates_forks.md` Р7 · `plans/21` (тексты ворот)


---

## ✅ ЗАКРЫТИЕ 2026-08-30 (Дизайнер, смена 14)

Задача пришла ко мне как «первый экран лишается половины фразы — посмотри, что с ним станет».
Рисовать оказалось нечего: **фразы в продукте уже нет.**

### Чем это показано — четырьмя способами, а не одним

| Проверка | Команда | Результат |
|---|---|---|
| Фразы нет в коде | `grep -rn "каждый день\|every day" src/` | на лендинге 0; 4 попадания в `src/lib/content/docs.ts` — описание шкалы оценок, к витрине отношения нет |
| Когда исчезла | `git log -S "new ones come every day" -- src/` | **`413a8a8`** «витрина хвастается размером Пространства, а не считает головы» |
| Нет в стволе | `git grep -c "каждый день приходят новые" main -- src/` | пусто |
| Адрес из документа мёртв | `grep -n "joinedPre\|joinedPost" src/` | не существуют; лендинг переехал на `[lang=lang]/+page.svelte` (`plans/39`) |

Сегодня на первом экране вместо обещания — полоса из четырёх настоящих чисел (снимок
`landing-metric.ts`) и строка «Станьте одним из первых, кого здесь найдут». Число людей —
констатация, оно на месте.

### ⚖️ РАСХОЖДЕНИЕ С ВЫБОРОМ ВЛАДЕЛЬЦА — записано, потому что незаписанное станет «мы это решили»

- **Что выбрал владелец:** интервью №014, В1 = **вариант В** — «сделать утверждение ВЫВОДИМЫМ из
  данных: показывать хвост только когда он правда, считая приток из снимков дней».
- **Что выкачено:** коммитом `413a8a8` — вариант, близкий к **Б**: хвост убран совсем, витрина
  говорит о РАЗМЕРЕ Пространства (измерения · оценки · связи · люди), а не о притоке.
- **Почему разошлось:** по документам не восстанавливается. Ни в коммите, ни в `plans/`, ни в
  интервью объяснения нет; правдоподобно, что вариант В требовал шага 3 (приток в пререндер), а
  витрина чисел закрыла ту же боль дешевле и раньше.
- **Лечить расхождение НЕ предлагается.** Нынешний вид честен, правилу владельца о лице продукта
  не противоречит и не может соврать в принципе: он не утверждает ничего о темпе. Возвращать
  вариант В значило бы вернуть хвост, который нечем наполнить (приток ≈ 1 человек в 3 недели).
- 🔴 **Но это НЕ то, что владелец выбрал**, и до сегодняшнего дня не было записано нигде.
  Нести ли это ему — решение Менеджера, не исполнителя.

### 🔴 КЛАСС ЖИВ — в другом предложении того же первого экрана

Симптом закрыт, **класс — нет**, и второй его экземпляр найден при этой же проверке.
Блок 02 `landingFeatures` (рендерится живьём, `[lang=lang]/+page.svelte:151,259`) звал:

> «Начните общение с теми, кто действительно Вам подходит.» / «Start communicating with those who
> are really right for you.»

**Общения в продукте нет:** единственная кнопка «Написать» (`routes/relations/+page.svelte:532`)
стоит `disabled` с подписью «скоро» (`write` / `soon`), маршрута переписки нет ни одного.

🔑 **И этот экземпляр дороже исходного.** Прежняя фраза врала о ТЕМПЕ — неприятно, но человек
этого не проверит. Эта **звала к действию, которого нет**: человек пришёл бы за общением и упёрся
в серую кнопку — ровно в тот момент, когда мы его наконец заинтересовали. Сломанное обещание,
которое он обнаруживает сам, изнутри.

Исправлено в ту же смену (кандидат К3, оба языка), кадры «до/после» с живого лендинга —
`test-results/owner-reviews/landing-feat02/`. ⚠️ Текст на лице продукта идёт владельцу вычиткой.

🔑 **Вывод для шага 2, который поднимает его цену:** список запрещённых слов («каждый день»,
«every day») ЭТОТ экземпляр не поймал бы — в нём таких слов нет. Страж на класс обязан ловить не
словарь обещаний, а **утверждение о том, что человек может здесь сделать, при отсутствии этого в
продукте**. Это дороже, чем задумывалось, и это надо знать до того, как шаг 2 возьмут в работу.

### Решения, принятые без владельца

1. **Закрыть документ, а не переоткрывать под новый симптом.** Симптом этого документа —
   утверждение о притоке — починен и проверен; найденный класс живёт в другом предложении и
   получил свою правку. Держать баг открытым «за класс» смешало бы две разные вещи в одном номере.
   Альтернатива (оставить открытым до шага 2) отклонена: шаг 2 записан здесь долгом и виден.
2. **Расхождение В↔Б записано, но не вынесено развилкой владельцу** — прямое указание Менеджера;
   моё дело было зафиксировать факт, а не решать его судьбу.
3. **Формулировка К3 при вставке потеряла одно слово.** Дословно К3 звучала «Вы видите ЛЮДЕЙ, с
   которыми у Вас больше всего общего…», но первая половина абзаца уже говорит «находим самых
   похожих на Вас людей» — вышел повтор существительного. Поставлено «Вы видите, С КЕМ у Вас
   больше всего общего, и меру этого общего». Смысл К3 не тронут, снят повтор; отклонить правку
   значило бы выкатить владельцу текст с очевидным заиканием.
4. **Английская половина не перевод, а пара** (правило `plans/51`): «You see who you have the most
   in common with, and how much you share» — «меру этого общего» дословно («the measure of it»)
   в английском звучит канцелярски.
5. **Заголовок блока 02 «настоящая совместимость» НЕ тронут.** Это обещание об ИСХОДЕ отношений,
   и оно принято владельцем сознательно (`plans/29` §3а, цена названа в `landing-copy.ts`). Не
   мой предмет и не мой класс.

### Перепроверка одной командой

```bash
grep -rn "Начните общение\|Start communicating" src/          # пусто
grep -rn "каждый день приходят\|new ones come every day" src/ # пусто
```
