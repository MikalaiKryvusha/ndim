# Bug 46 — Раскрытая связь потеряла человека и параметры пространств

**Статус:** 🟡 ПОЧТИ ЗАКРЫТ (2026-07-30): из шести блоков канона 1.x сделаны **все, кроме
«Был онлайн»**, а он — развилка владельца (в 2.0 присутствие не трекается вовсе). ·
**Источник:** полный проход инвентаря паритета (researches/12, 2026-07-24)

> ## ✅ «Последнее обновление NDim ID» сделано 2026-07-30
>
> Последний блок, который был за агентом. Числа не было у экрана по УСТРОЙСТВУ ПРАВ: `points/{uid}`
> чужого человека зрителю недоступен (его читают только владелец и вычислитель), а в записи топа
> его не лежало. Поэтому правка — в сервере синхронизации, а не в вёрстке:
>
> · `calculator/index.mjs` → `topFor()` кладёт `updated` СОСЕДА в его строку топа. Ни одного нового
>   чтения базы: число уже загружено для статистики Пространства;
> · `schema.ts` → `RelationEntry.updated?: Millis` — поле **необязательное** намеренно: в боевых
>   `relations`, записанных прежними сборками, его нет, и экран тогда просто не показывает строку
>   («неизвестно» на месте даты было бы выдумкой — канон `bugs/86`);
> · экран «Связи» показывает строку ПОСЛЕ персональной информации и ДО «Параметров пространства» —
>   порядок кадра app-16.
>
> **Проверено (test:calc 34 → 37):** новый файл `calculator/relation_row.test.mjs`, свой проект
> эмулятора (канон изоляции; первая редакция его пропустила и уронила `guest_filter.test.mjs`):
> · в строке лежит время ТОГО человека, о котором строка — двумя РАЗНЫМИ числами. Ошибка «положили
>   `updated` владельца в каждую строку» выглядела бы на экране правдоподобно и не поймалась бы
>   ничем другим; мутация ровно с этой ошибкой даёт **3 падения**;
> · поле не подменяется нулём или `null`;
> · точка БЕЗ времени обновления не добавляет поле вовсе (`'updated' in row === false`).
>
> **Живой прогон:** `node tools/verify-tierA.mjs` — зелёный; в него добавлена постоянная проверка
> строки, и она смотрит на ДАТУ рядом с подписью, а не на подпись (подпись без значения — ровно
> тот вид зелёного, из-за которого блок и был потерян). Мутация «спрятать строку» → 2 падения.
> На стенде видно: `«Последнее обновление NDim ID» → 30 июля 2026 г. в 17:12`.
>
> ⚠️ **В бою строка появится только после пересборки образа сервера** (`npm run calc:image` +
> замена контейнера — машина владельца): до этого боевые `relations` пишутся прежней сборкой, поля
> в них нет, и экран честно молчит.
>
> **Что осталось:** «Был онлайн» — развилка владельца, а не работа. Это НОВОЕ публичное поле
> (в 2.0 присутствие не трекается), и оно родня `ideas/23`/`plans/09` про публичный «Статус».

## Симптом
В 1.x раскрытая карточка связи (кадр app-16) — досье знакомства. В 2.0 раскрытие показывает
только общее пространство (размерность · диаметр · расстояние + %) и дату пересчёта.

## Канон 1.x (app-16, полный состав сверху вниз)
1. **Персональная информация человека**: Имя · Гендер · **День рождения с возрастом**
   («13 мая 1986 г. (40 лет)») и другие заполненные поля;
2. «Последнее появление онлайн» (дата+время; род «Была» по гендеру) — ❓ развилка;
3. «Последнее обновление NDim ID» (дата+время);
4. **«Параметры пространства»** (ЕГО): Размерность «396 измерений **(×0.82 моего)**» ·
   Диаметр «199 звёзд **(×0.9 моего)**» — коэффициенты сравнения с моим;
5. **«Параметры нашего общего пространства»**: Размерность «176 измерений **(×0.36 моего,
   ×0.44 хозяина)**» · Диаметр «132.7 звезды (×0.6 моего, ×0.67 хозяина)» ·
   Расстояние «34.15 звезды (26% от диаметра)»;
6. «Связь обновлена» + тройка метрик.

## План фикса
- Персональная информация — читать бакеты по правилам видимости 2.0 (аудитории уважаем;
  скрытое не показываем — это осознанное отличие от 1.x, где видно было всё). Возраст —
  вычислять из даты рождения, если она видна.
- «Последнее обновление NDim ID» — есть в данных точки/relations.
- Коэффициенты ×моего/×хозяина — чистая арифметика на клиенте из уже загруженных чисел
  (моя размерность/диаметр известны из своего relations-документа).
- Размерность/диаметр ЕГО пространства — проверить, что пишутся в relations-запись
  (в 1.x лежали в самой связи); если нет — дописать в вычислитель (поля в топе).
- «Был онлайн» НЕ делать до слова владельца (❓ в STATUS): в 2.0 присутствие пока не
  трекается; завести только после решения.

## План проверки
Стенд: раскрытая связь показывает возраст/поля по видимости (юнит на возраст и на скрытие);
коэффициенты сверены с ручным расчётом (юнит); скрытое поле соседа НЕ утекает (тест правил
уже есть — расширить сценарием раскрытия). QA-прогон живым Chrome, обе темы, скриншоты.

## 🟡 СТАТУС: сделано 2026-07-26 (тир A), две строки остались

Сделано и в бою на стенде (`src/routes/relations/+page.svelte`):

| Блок канона 1.x | Состояние |
|---|---|
| Персональная информация (имя · гендер · день рождения с возрастом · «О себе») | ✅ по видимости 2.0 |
| Последнее появление онлайн | ⏳ развилка владельца (присутствие не трекается) |
| Последнее обновление NDim ID | 🔲 **не сделано** — см. ниже |
| «Параметры пространства» (его): размерность и диаметр + ×моего | ✅ |
| «Параметры нашего общего пространства» + ×моего/×хозяина + % от диаметра | ✅ |
| «Связь обновлена» + тройка метрик | ✅ (было и раньше) |

**Чинить оказалось нечего в математике:** все шесть коэффициентов ядро считает с самого
начала (`similarity.ts:64-72`) и вычислитель кладёт их в КАЖДУЮ запись топа — экран их
просто не показывал. Ни одного нового чтения Firestore, ни строчки в сервере синхронизации.
Тексты — дословно из `app.js:7295-7420` («Параметры пространства», «(×0.82 моего)»,
«(×0.36 моего, ×0.44 хозяина)», «(26% от диаметра)»).

Возраст: `ageAt`/`bornWithAge`/`yearsUnit` в `ui/format.ts` по алгоритму 1.x
(`app.js:7064-7073`, `getAgeWord`), 8 юнитов на границы «вчера/сегодня/завтра» и склонение
11–14. Мутация «убрать поправку на ненаступивший день рождения» роняет 2 теста.

**Проверено:** QA-прогон `node tools/verify-tierA.mjs` — коэффициенты считаются ПОШТУЧНО
(ровно 4 «моего», 2 «хозяина»: короткая подстрока оставалась зелёной при потере целого
блока); Анна показывает возраст, Виктор с одним годом — БЕЗ выдуманного возраста, Мария не
открыла ничего и блока персональной информации не имеет вовсе. Обе темы × 390/1440,
скриншоты `test-results/tierA/relation-expanded-*.png` смотрены глазами.

### ✅ Что было в тире B и сделано 2026-07-30

**«Последнее обновление NDim ID» — требовало правки сервера синхронизации,** и она сделана:
вычислитель кладёт `updated` соседа в строку топа (подробности и мутации — в шапке документа).
Разведка была верна: время чужой точки зрителю по правилам недоступно, поэтому иначе и нельзя.
Формат `relations` расширен НЕОБЯЗАТЕЛЬНЫМ полем — старые боевые документы читаются как прежде.
Осталась только пересборка боевого образа (`npm run calc:image`, машина владельца).

**«Был онлайн» — по-прежнему развилка владельца** (в 2.0 присутствие не трекается вовсе;
это новое публичное поле, а не восстановление старого).

## Решения, принятые без владельца

- **Приватность строже 1.x, осознанно.** В 1.x в чужой карточке было видно всё; здесь —
  только бакет `everyone`. Скрытое поле не даёт строки и не подменяется прочерком: зритель
  не должен уметь отличить «не заполнено» от «скрыто от меня» (`researches/04`).
- **Возраст по неполной дате НЕ достраивается.** 1.x в этом случае считал от декабря
  предыдущего года и врал; мы показываем год без возраста (выдуманное число хуже
  отсутствующего).
- **Заголовки блоков приведены к 1.x** («Наше общее пространство» → «Параметры нашего
  общего пространства»): раз в блоке появились коэффициенты, пара заголовков должна читаться
  как в оригинале. ⏳ на контрольный взгляд владельца.
- **Слово «хозяина» сохранено** как в 1.x, хотя во внутреннем словаре так человека не
  называют: это текст оригинала, а не наш жаргон.
- **Английский «Dimensionality» оставлен как есть** (в 1.x было «Dimension»): текст 2.0 уже
  принят владельцем, менять принятое ради буквы перевода — не паритет, а шум.

## Ссылки
researches/12 §«Связи — РАСКРЫТАЯ карточка» · кадр app-16 · researches/02 (модель связи 1.x) ·
bugs/28 (имена/почта) · src/routes/relations/+page.svelte · plans/07 (тиры A и B) ·
tools/verify-tierA.mjs
