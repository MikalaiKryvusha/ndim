# Bug 38 — виджет манифеста в «Меню» (ЗАКРЫТ УДАЛЕНИЕМ САМОГО МАНИФЕСТА)

> ## ⛔ 2026-07-31 — ЭТОТ БАГ БОЛЬШЕ НЕ ПРИМЕНИМ: манифеста нет вовсе
>
> **Слово владельца:** «Манифест NDim Space - убираем этт виджет, манифест есть в руководсвте
> пользователя» и следом: **«и виджет и страницу удаляем - это была отсебятина ИИ, я этого не
> делал в оригинальном НДим»**.
>
> Удалены: виджет в «Меню» · страница `/menu/manifesto` · модуль `src/lib/content/manifest.ts` ·
> строка-ссылка · стили `.manifest`/`.manifest-link` · все проверки, которые это стерегли.
>
> **Премиса проверена, а не принята на слово:** «1. Манифест» — РАЗДЕЛ руководства пользователя
> (`tools/extract-docs.mjs`, `MANUAL_SECTIONS`), снятый дословно из 1.x в `researches/07`.
> Текст не потерян; дублем были виджет и страница.
>
> **Проверки не ослаблены, а перевёрнуты.** Вместо «виджет виден там-то» теперь стережётся
> ОБРАТНОЕ — что удалённая сущность не вернётся незаметно: `verify-batch4` требует
> `section.manifest` = 0, `a.manifest-link` = 0 и **404 на `/menu/manifesto`**; e2e —
> `toHaveCount(0)` для обоих селекторов. Текст манифеста остаётся под охраной строки
> `{ path: '/menu/manual', marker: 'Манифест' }`, которая была там и раньше.
>
> 🔴 **Урок класса, ради которого этот документ и переименован:** агент завёл сущность,
> которой не было в оригинале и которую владелец не просил. Она обросла страницей, стилями,
> баг-документом и четырьмя проверками — то есть **защитила себя от удаления**: любая попытка
> её убрать выглядела бы как поломка тестов. Прежде чем заводить новый экран «по смыслу»,
> спроси: было ли это в 1.x и просил ли этого владелец.

# Bug 38 — Манифест в мобильном меню слишком развесист

**Статус:** ✅ DONE (2026-07-24, в бою) · ранее: починен (2026-07-17), проверка — QA-прогон verify-batch4 + e2e ·
**Источник:** владелец, чат 2026-07-17

## Симптом (слово владельца)
«В меню сейчас очень сильно развесистый манифест — уберем его для мобильной версии: пусть
в мобильной идут в специальную кнопку полного манифеста. В десктопе пусть этот манифест
будет — в десктопе полно места, чтобы два виджета повесить рядышком — манифест и меню кнопок».

## Решение (задано владельцем — макеты не требуются)
- **Телефон:** вместо манифеста — кнопка-строка «📜 Манифест» (первая карточка меню),
  ведущая на новую полную страницу **`/menu/manifesto`** (пререндерена, как остальные
  страницы раздела).
- **Десктоп (от 1024px):** манифест — полный виджет РЯДОМ с кнопками меню; сетка `/menu`
  стала трёхколоночной: манифест | аккаунт·вид·поделиться | документы·проект·версии.
- Текст манифеста вынесен в общий модуль `src/lib/content/manifest.ts` (один источник для
  виджета и страницы); «Читать дальше» больше не нужен — на десктопе текст целиком.
- Пререндер `/menu` по-прежнему содержит полный текст (виджет скрыт CSS) — «лицо проекта»
  в сыром HTML охраняется e2e без изменений.

## Проверка
`node tools/verify-batch4.mjs` (секция bugs/38): телефон — виджет скрыт, кнопка видна и
ведёт на полный текст; десктоп — виджет виден рядом с колонками кнопок, кнопка скрыта.
`e2e/menu.spec.ts` обновлён (viewport-зависимая проверка + `/menu/manifesto` в списке
страниц). Скриншоты обеих раскладок — глазами.

## Ссылки
src/routes/menu/+page.svelte · src/routes/menu/manifesto/+page.svelte ·
src/lib/content/manifest.ts · bugs/29 (плотность меню — смежный, канон app-08)

## ✅ СТАТУС: DONE (2026-07-24, ревизия беклога)

Что сделано: фикс по журналу выше, давно в main, проверен QA-прогоном на стенде.
Как проверено: боевые выкаты хостинга 2026-07-17 и 2026-07-24 вынесли в прод всё
накопленное в main; регрессия 2026-07-16 зелёная (юнит 149 · rules 87 · e2e 70).
