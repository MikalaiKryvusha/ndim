# Bug 168 — `team-workplaces.mjs reset` молча уничтожает несмерженную работу роли

**Статус:** ✅ DONE 2026-09-12 (предохранитель в инструменте, К1–К4 доказаны — раздел внизу) · **Заведён:** 2026-08-22, Менеджером по собственной ошибке ·
**Нашёл:** Менеджер (наступил сам), проверяемую форму предложил Дизайнер ·
**Класс:** разрушительная операция без предохранителя

## Симптом

`node tools/team-workplaces.mjs reset <роль>` переставляет ветку роли на текущий `main`
**без единого вопроса** — в том числе когда в ветке лежат коммиты, которых в `main` нет.
Печатает при этом ровно то же бодрое «✅ ветка перезапущена от текущего main», что и в
безопасном случае.

## Как случилось в поле

2026-08-22 ~02:15, ночь второй смены команды. Менеджер смержил четыре сдачи и прогнал сброс
веток **списком по всем пятерым ролям**. У четверых работа была влита; у Дизайнера в ветке
лежали **четыре несмерженных коммита** — партия 4 (20 карточек кандидатов, заказ владельца),
контракт ворот, английские пары и урок журнала. Ветка уехала на `main`, коммиты осиротели.

Работа не потеряна: объекты живы, `git reflog show ndim_designer` держал все четыре, на них
поставлена спасательная ветка `designer-rescue-20260822`, Дизайнер восстановил ветку сам.
**Цена инцидента — ноль**, но только потому, что reflog не успел истечь и рядом был человек,
который заметил ошибку в ту же минуту.

## 🔑 Почему это дефект ИНСТРУМЕНТА, а не внимательности

Наблюдение Дизайнера, и оно решающее:

> «у меня работа была НЕ смержена, и это состояние отличается от остальных четверых **внешне
> ничем** — на доске я стоял таким же 🟢 свободным, как они»

То есть признак опасности не виден ни на статус-доске, ни в приглашении инструмента. Человек
обязан помнить, у кого что не влито, — а память сессии худшее из хранилищ. Канон проекта
ставит запись в журнал ТРЕТЬИМ средством: сначала убрать ловушку, потом страж, и только
потом урок (`AGENT_GUIDE.md` → «Журнал опыта»).

## Лечение — одна проверка, предложена Дизайнером

Перед сбросом инструмент считает, сколько у ветки своих коммитов:

```
git rev-list --count main..<ветка>
```

**Не ноль — отказ.** Отказ печатает число, перечисляет заголовки коммитов и называет два
законных выхода: смержить работу или сбросить осознанно флагом (`--force`), который в журнале
инструмента отмечается отдельно.

⚠️ И вторая половина, без которой отказ бесполезен: инструмент **сам ставит спасательную
ветку** перед разрушительным сбросом, когда `--force` всё-таки дан. Reflog — не гарантия: он
истекает, и на чужой машине его может не быть вовсе.

## Критерии приёмки

| # | Критерий | Мера | Цель |
|---|---|---|---|
| К1 | Несмерженное не уничтожается | `reset` на ветке с N > 0 своих коммитов | отказ, код 1, число и заголовки напечатаны |
| К2 | Безопасный случай не сломан | `reset` на влитой ветке | работает как сегодня |
| К3 | `--force` оставляет след | сброс с флагом | спасательная ветка создана и названа в выводе |
| К4 | Доказан мутацией | снять проверку | краснеет адресно на подготовленной ветке |

## Смежное

- `EXP-0190` (dev-2, той же ночью): `git checkout <файл>` как «откат мутации» стирает
  незакоммиченную работу, и `git status` после этого **чист** — «всё в порядке» и «я только что
  стёр свою работу» выглядят одинаково. Тот же класс: разрушительная операция, чей успех
  неотличим от беды.
- `ideas/43` — страж входного предохранителя. Родня по замыслу: механизм вместо памяти.

## Решения, принятые без владельца

1. Заведено как **дефект инструмента**, а не как урок Менеджеру. Довод — наблюдение Дизайнера
   выше: опасное состояние ничем не отличается от безопасного, значит правило «будь
   внимателен» неисполнимо по построению.
2. Спасательная ветка при `--force` предложена сверх минимального лечения: reflog истекает.
3. *(при закрытии, 2026-09-12)* Процедура сброса вынесена в функцию `performReset({ main, wt,
   branch, force, now })`, которая принимает адреса, а не роль. Довод: юнит гоняет её на ВРЕМЕННОМ
   репозитории, и живые рабочие места ролей мутациями не трогаются — иначе доказательство К4
   ставило бы тот самый опыт, от которого предохранитель защищает (`TESTING_FRAMEWORK.md`, правило 9:
   опасный прибор обезвреживается в постановке опыта).
4. *(там же)* Имя спасения — `<ветка>-rescue-<ГГГГММДД-ЧЧММ>`: минута в имени, чтобы два спасения
   одной ветки за день не столкнулись. Форма подсмотрена у ручного спасения из инцидента
   (`designer-rescue-20260822`), добавлена только минута.

## ✅ СТАТУС: DONE (2026-09-12 10:07 +03:00)

**INTENT:** код делал `git reset --hard main` после единственной проверки «дерево грязное?»; документ
ожидал счёт `git rev-list --count main..<ветка>` с отказом при N > 0 и спасательную ветку при
`--force`; расхождение = дефект, спецификация — таблица К1–К4 выше. Порядок авторитета не нарушен:
слово команды (Дизайнер, Менеджер) > код.

**Лечение** — `tools/team-workplaces.mjs`:
- чистые функции `resetDecision({ ahead, force })` и `rescueBranchName(branch, now)`;
- `performReset(...)`: грязное дерево → отказ; `ahead > 0` без `--force` → отказ с числом и
  заголовками; `ahead > 0` с `--force` → СНАЧАЛА `git branch <спасение> <ветка>`, ПОТОМ сброс;
- CLI `reset <роль> [--force]` печатает отказ с двумя выходами и имя спасательной ветки;
- самотест +4 случая (14/14); новый юнит `tools/team-workplaces.test.mjs` — К1, К2, К3 и отказ
  грязному дереву на временном репозитории (`mkdtemp` → `git init` → worktree с N своими коммитами).

**Доказательства, снятые приборами (2026-09-12):**

| Критерий | Прогон | Результат |
|---|---|---|
| К1 | `node --test tools/team-workplaces.test.mjs` | отказ, `ahead = 2`, два заголовка, голова цела |
| К2 | тот же юнит, main ушёл вперёд (контроль прибора: ветка отставала) | ветка стоит на main, спасения нет |
| К3 | тот же юнит, `--force`, момент 2026-08-22 02:15 | `ndim_z-rescue-20260822-0215` держит прежнюю голову, ветка на main |
| К4 | мутация `if (ahead > 0 && !force)` → `if (false)` | **К1 красный адресно** («сброс ветки с невлитой работой обязан быть отказом») + чистая функция красная; К2/К3 зелёные; код возвращён, 5/5 |
| Живой путь К1 | `node tools/team-workplaces.mjs reset dev-3` на настоящей `ndim_dev3` (3 невлитых коммита) | код 1, три заголовка напечатаны, голова `09203c9` до и после; страховочная ветка ставилась на время опыта и снята |
| Живой путь К2 | `node tools/team-workplaces.mjs reset qa` (влитая, чистая) | `✅ … перезапущена от текущего main (34020ae)`, `ndim_qa == main` |
| Входной предохранитель | `node tools/verify-import-safety.mjs` | чисто |

**TWINS:** искал `reset --hard` · `worktree remove --force` · `checkout -- ` · `rm -rf` ·
`Remove-Item -Recurse` · `git clean` по `tools/` — найдено 2 других места, оба в этом же файле:
`worktree remove --force` (уже под предохранителем `removalDecision` + junction-скан EXP-0175) и
шапочное упоминание; за пределами файла разрушительных git-операций в `tools/` нет.

**Страж класса:** юнит `tools/team-workplaces.test.mjs` стоит в `npm run test:tools` и через тот же
глоб — в `npm run guards`. `@guard reset-safety` · THREAT: сброс ветки с невлитой работой ·
PROVED-AGAINST: снятая проверка `ahead` (К1 красный) · GAP: живой `--force` на настоящем рабочем
месте не гонялся — только на временном репозитории (на настоящем он и есть разрушение) ·
ON-REAL-PATH: отказ на `ndim_dev3` и штатный сброс `ndim_qa`, 2026-09-12.

**Ручной функциональный прогон:** ✅ 2026-09-12 — раздел 1: живой `reset dev-3` (отказ) и `reset qa` (успех) на настоящих рабочих местах. Отчёт `qa/reports/2026-09-12_red-bugs-series.md`. (Гигиена — строка выше; эта строка — тест по определению владельца.)
