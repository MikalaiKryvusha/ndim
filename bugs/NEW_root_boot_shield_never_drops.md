# Bug NEW — Корень в бою показывает вечную «Загрузку» всем, у кого в браузере есть сессия

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16.
<!-- ВЕРА-КОНЕЦ -->

> **Создан:** 2026-09-05 · **Статус:** 🔧 ПРИЧИНА НАЙДЕНА И ВОСПРОИЗВЕДЕНА, фикс в этом же чате,
> едет стейдж → бой · **Нашёл:** ВЛАДЕЛЕЦ, кадром с бою через минуты после выката («*бесконечная
> загрузка. Что не так?*») · **Класс:** регрессия выката — снятый увод корня забрал с собой
> единственное место, где опускался щит · **Родня:** `bugs/40` (щит), `bugs/08.1` (вошедший не
> разглядывает лендинг), `plans/81` (корень V5), `bugs/124` (класс «свежий контекст не видит»).

## Симптом

`ndimspace.app` у владельца: знак, «Пространство NDim», кольцо, через 15 секунд — «Загрузка
выполняется необычно долго. Попробуйте перезагрузить страницу». Перезагрузка не помогает. У агента
в свежем браузере — исправная главная V5 с содержанием.

## Причина — воспроизведено зондом, не догадкой

Инлайн-скрипт `src/app.html` поднимает загрузочный щит (`html[data-booting]`, `bugs/40`) на корне и
на лендинге, если в `localStorage` лежит маркер `ndim-session` — то есть у ВСЯКОГО, кто хоть раз
входил, гостем тоже. Опускает щит ровно одно место — `endBoot()` из `src/lib/data/session.ts`,
которое до V5 звал лендинг: корень уводил всех на `/ru`, лендинг решал «сессия есть → внутрь /
нет → опустить щит». `plans/81` сделал корень страницей с содержанием и увод убрал, а `endBoot()`
на корне никто не позвал. Щит висит до потолка 15 с и дальше показывает совет перезагрузить.

Зонд на бою (Playwright, 2026-09-05 17:36): свежий контекст → щит `display:none`, ошибок 0; контекст
с живой гостевой сессией → `data-booting` стоит через 2, 6 и 12 секунд, адрес `/`. Стейдж свежим
контекстом — исправен. Ровно поэтому **все проверки были зелёными**: смоуки, e2e и приёмка агента
ходят свежими контекстами без маркера сессии, а у владельца маркер есть всегда.

Кэш и куки браузера не при чём: корень отдаётся с `Cache-Control: no-cache`, живой HTML новый.

## Лечение

Первый заход — `onMount` с `hasSession()` по образцу лендинга — НЕ СРАБОТАЛ на стенде (щит стоял
15 с): корень живёт без гидратации (`csr = false`), `onMount` там не исполняется никогда. Это
поймано зондом, а не глазом, — второй заход уже по устройству страницы.

`src/routes/+page.svelte`, инлайн-скрипт (тот же, что разбирает ссылку из письма): после ветки
письма — если в `localStorage` лежит маркер `ndim-session`, `location.replace('/profile')`. Это тот
же признак, по которому `app.html` поднял щит, и та же дверь, куда лендинг уводил вошедшего
(`APP_URL`, bugs/08.1): щит держится до ухода, кадра главной человек не видит. Маркер без живой
сессии приводит на экран входа, а не на вечный экран. Без маркера щит не поднимается, главная
видна сразу.

## Проверка

- [ ] стенд: маркер без сессии → щит опущен, главная видна; гостевая сессия → увод в `/profile`;
- [ ] `e2e/root.spec.ts` — у корня появился первый e2e: содержание + поиск открыт + маркер сессии
      без сессии опускает щит (фикстура доказывает, что щит был поднят);
- [ ] стейдж дверью → бой дверью → зонд с гостевой сессией на бою: щит опущен либо увод внутрь.

## Итог на бою — 2026-09-05 ~17:55

Дверь боя с фиксом: правила 12/0 · хостинг выпущен · смоук под сессией 24/0 · гостевой смоук **83/83**,
в том числе новая проверка «корень с маркером сессии уводит внутрь» (на сломанном бою она была
красной — 82/1 — это и есть доказательство, что проверка ловит дефект). Зонд агента после двери:
маркер → `/profile` за 2,5 с, «необычно долго» не показано; живая гостевая сессия → `/profile`, щит
опущен; свежий браузер → главная, щит не поднимался.

## Как это было допущено — честно, по слоям (слова владельца: «*вопиющая халатность тестирования*»)

1. **Код.** `plans/81` снял увод корня, но никто не проследил, КТО потребляет то, что корень
   раньше делал косвенно: щит на корне поднимает `app.html` по маркеру, а опускал его лендинг, куда
   корень уводил. Правка убрала звено цепи, не посмотрев на цепь. Грепа по `atRoot` в `app.html`
   хватило бы.
2. **Тесты.** У корня после V5 не было ни одного e2e — все спеки лендинга ходили на `/ru`. Дверь
   выката гоняет два смоука: под сессией (вход → пять экранов, корень не открывает) и гостевой
   (свежий контекст без маркера). Ни один не воспроизводит состояние НАСТОЯЩЕГО возвращающегося
   человека: маркер сессии в `localStorage`, память темы и языка, живая сессия. Владелец — ровно
   такой человек, и он увидел дефект за минуты.
3. **Приёмка агентом.** Мой зонд корня смотрел DOM (`h1` есть, `noindex` нет) и кадр свежего
   контекста — под щитом `h1` тоже есть. «Экран отрисован» судилось по разметке, а не по тому, что
   видит человек в его состоянии.
4. **Процесс.** Выкат сделан по слову владельца при зелёной двери — шаги не пропускались; слепа
   была сама дверь. Это хуже пропущенного шага: зелёное, которое не смотрит туда, где живёт
   человек, приучает верить зелёному.

## Как не допускать — что уже стоит и что заведено правилом

- ✅ **Механизм 1:** `e2e/root.spec.ts` — три теста корня: содержание и открытость поиску без
  щита; маркер сессии → внутрь; ссылка из письма → профиль с нетронутым query.
- ✅ **Механизм 2:** `tools/verify-prod-b4.mjs` — проход с маркером сессии на корне, стоит В ДВЕРИ
  для стейджа и боя; доказан красным на сломанном бою.
- ✅ **Механизм 3:** `tools/verify-boot-shield.mjs` — статический страж цепи щита: у каждого входа,
  где `app.html` поднимает щит (корень, лендинг), обязан быть тот, кто его опускает или уводит
  человека; доказан мутацией парным тестом.
- 📜 **Правило (AGENT_GUIDE → «Испытательный стенд»):** смоук публичной страницы обязан включать
  контекст ВОЗВРАЩАЮЩЕГОСЯ человека — маркер сессии, память темы и языка — а не только свежий
  браузер; приёмка «экран отрисован» судится тем, что ВИДНО, а не тем, что есть в DOM. Снимаешь
  увод или редирект — грепни, кто на нём стоял.
- 📤 Сигнал в исток KAIF: канон двери выката требует смоук «под сессией», но не требует состояния
  возвращающегося посетителя на публичных входах — запрос улучшения.

## Урок (в `EXPERIENCE.md`)

Проверка «свежим контекстом» слепа к состоянию, которое есть у КАЖДОГО настоящего человека:
маркер сессии, память темы и языка, живая сессия. Смоук боя обязан иметь хотя бы один проход с
маркером сессии на публичных страницах — иначе дверь зелёная, а владелец видит вечную загрузку.
