# Bug 105 — «Как меня видят» открывается отдельным окном поверх приложения, а не в рабочей области

**Статус:** ✅ **ЗАКРЫТ 2026-08-02** — фикс в коде, страж `tools/verify-bug105.mjs` **56/0**,
доказан мутацией (4 адресных провала) и замером «до» на сломанном коде (**32 провала**).
Форма фикса утверждена владельцем (интервью №013 В1 = вариант **А**). Приёмка боем — за владельцем.
**Версия/сборка:** 2.0, бой (`ndimspace.app`) и стенд · **Когда/контекст:** 2026-08-01, брошено
владельцем в чате, пока агент возобновлял сессию по волне «Ворота».

> **Слово владельца дословно:** «превью как меня видят открытается в каком-то отдельном окне, а не в
> обычной рабочей области приложения - нужно внутри приложения открывать».

---

## Симптом

Кнопка **«Как меня видят»** на экране «Профиль» открывает предпросмотр аудиторий **полноэкранным
слоем**: он накрывает всю оболочку продукта — шапку `AppBar`, нижнюю панель `BottomNav`, а на
десктопе ещё и рельс навигации `SideRail`. Человек оказывается «в другом окне»: продукта вокруг
больше нет, есть только слой со своим собственным заголовком и своей собственной стрелкой «Назад».

Ожидание владельца: предпросмотр — **часть приложения**, а не окно поверх него. Он должен
открываться в обычной рабочей области (там же, где живёт содержимое остальных экранов), а оболочка
— шапка, навигация, рельс — обязана остаться на месте.

⚠️ **Это НЕ отмена решения владельца от 2026-07-27** («предпросмотр аудиторий — не таба, а ОКНО за
кнопкой „Как меня видят“»). Кнопка и отдельный вид остаются; спорна была только **форма** этого
вида: сейчас он сделан лайтбоксом, а должен быть экраном продукта. Форму владелец назвал сам
2026-08-02 — вариант **А**, см. «Решения, принятые без владельца» ниже.

## Воспроизведение (детерминированное)

```bash
npm run stand                      # эмуляторы + сервер синхронизации + vite
# http://localhost:5173/profile    — вошедший dev@ndim.space
```

1. Открыть `/profile` на ширине **1440** (десктоп) — виден рельс слева и шапка сверху.
2. Нажать **«Как меня видят»**.
3. Наблюдать: рельса нет, шапки нет, нижней панели нет — весь вьюпорт занят слоем предпросмотра.
4. То же на **430** (телефон): шапка и нижняя панель исчезают.

`[NOT-TESTED]` — живой замер браузером ещё не снят, ниже описано то, что **прочитано в коде**.
Шаг 0 фикса — снять «до» прибором (какие элементы оболочки видимы до и после нажатия), иначе фикс
будет судиться впечатлением, а не числом.

## Криминалистика (прочитано в коде)

Слой объявлен полноэкранным и лежит выше всей оболочки:

| Элемент | Где | `z-index` |
|---|---|---|
| `.seeme` — окно «Как меня видят» | `src/routes/profile/+page.svelte:1577` | **70**, `position: fixed; inset: 0` |
| `.lightbox` — портрет во весь экран | `src/lib/ui/Avatar.svelte:176` | 80 |
| `AppBar` — шапка продукта | `src/lib/ui/AppBar.svelte:128` | 10 |
| `BottomNav` — нижняя панель | `src/lib/ui/BottomNav.svelte:51` | 6 |

```css
/* src/routes/profile/+page.svelte:1576 */
/* ── Окно «Как меня видят»: полноэкранный слой со своим скроллом; шапка прибита. ── */
.seeme {
  position: fixed; inset: 0; z-index: 70;
  background: var(--bg); overflow-y: auto;
}
```

Три признака того, что слой **воспроизводит оболочку вместо того, чтобы жить внутри неё**:

1. `position: fixed; inset: 0` + непрозрачный `--bg` — оболочка не просто перекрыта, она закрашена;
2. у слоя **своя шапка** `.seeme-head` со своим `<h2>` и своей кнопкой «Назад»
   (`+page.svelte:1519–1524`) — дубль того, что уже даёт `AppBar`;
3. прокрутка страницы под слоем блокируется (`document.body.style.overflow = 'hidden'`,
   `+page.svelte:103–110`) — приём **лайтбокса**, а не экрана.

## Корневая причина

Слой был сделан по образцу **лайтбокса фото** (`bugs/36`), хотя по смыслу он — **экран продукта**.
Оба живут в одном файле и в одной ментальной модели «наложение поверх профиля», и предпросмотр
унаследовал чужую форму: `fixed/inset:0` + свой заголовок + скролл-лок.

Историческая нить объясняет, почему этого не поймали раньше:

- **2026-07-27** владелец просит: не таба, а кнопка → окно предпросмотра, «Назад» возвращает в
  профиль. Реализовано полноэкранным слоем.
- **`bugs/76`** — владелец: «с „Как меня видят“ навигация работает некорректно, закрыло приложение,
  словно родительский Профиль не был в стеке навигации». Починено ШТАТНЫМ неглубоким
  маршрутизированием (`pushState` + `page.state`, `+page.svelte:74–100`). Диагноз тогда звучал так:
  «окно **выглядело дочерней страницей** (полный экран, заголовок, стрелка „Назад“) — но записи в
  истории у него не было». 🔴 **Починили запись в истории, а сам вид дочерней страницы оставили** —
  и именно он сейчас назван дефектом.

То есть претензия владельца — прямое продолжение `bugs/76`: тогда разошлись история и вид, теперь
вид расходится с остальным продуктом.

## План фикса

Работа мелкая по диффу, но задевает вид экрана — значит идёт по канону паритета и сдаётся живым
браузером (`AGENT_GUIDE` → «QA-прогон через живой Chrome»).

Цель шагов 2–3 — **вариант А владельца**: предпросмотр рисуется там же, где содержимое «Профиля»;
шапка, нижняя панель и рельс остаются на местах и работают; вторая шапка исчезает, остаётся строка
возврата. Все номера строк ниже — по `src/routes/profile/+page.svelte` (1902 строки на 2026-08-02).

- [x] **Шаг 0 — замер «до».** Прибором снять, какие элементы оболочки видимы при открытом
      предпросмотре (`AppBar`, `BottomNav`, `SideRail`) на 430 и 1440, в обеих темах. Числом, не
      глазами.
- [x] **Шаг 1 — разведка канона 1.x.** Был ли в 1.x предпросмотр аудиторий и как он открывался —
      `researches/12` (инвентарь паритета). Модель видимости 2.0 в 1.x отсутствовала, поэтому,
      скорее всего, прямого канона нет; тогда каноном служит форма СОСЕДНИХ экранов 2.0.
- [x] **Шаг 2 — вернуть предпросмотр в поток `main`-области** (это и есть «рабочая область» из
      ответа владельца):
      · **разметка.** Блок `{#if seeMeOpen && data} … {/if}` (`1517–1557`) сейчас лежит МЕЖДУ
        `</main>` (`1512`) и `<BottomNav …>` (`1559`), то есть прямым ребёнком `.screen`
        (`1075`). Перенести его ВНУТРЬ `<main class="body">` (`1087`) — первой веткой цепочки
        состояний экрана `{#if stand === 'connecting'}` … `{:else if data}` … `{:else if guest}`
        (`1138`, `1180`, `1506`, закрыта на `1511`): при открытом предпросмотре `main` рисует
        предпросмотр ВМЕСТО содержимого профиля, а оболочка вокруг не трогается вовсе.
      · **стиль.** Снять у `.seeme` весь лайтбоксовый набор — `position: fixed; inset: 0;
        z-index: 70;` и `background: var(--bg); overflow-y: auto;` (`1577–1580`): слоя больше нет,
        есть секция в потоке. `z-index: 70` уходит вместе с ним — гонки с `AppBar` (10) и
        `BottomNav` (6) не остаётся.
      · **десктоп.** При ≥1024px `.body` — сетка из четырёх шагов (`1815–1847`), и секция
        предпросмотра обязана занимать всю ширину, как `.intro` / `.guest-card` / `.state`
        (`1858–1862`): добавить её в этот список селекторов, иначе она встанет в один узкий
        столбец из четырёх.
      · **мобилка.** `.seeme-body { max-width: 458px; margin: 0 auto; padding: 14px; }` (`1592`)
        дублирует то, что уже даёт `.body` (`1595–1598`, те же 458px и те же 14px) — снять, иначе
        поля удвоятся.
      · **скролл-лок.** `$effect` с `document.body.style.overflow = 'hidden'` (`103–110`) удалить
        целиком вместе с комментарием «как лайтбокс» (`102`) — экран под собой ничего не блокирует.
- [x] **Шаг 3 — вторая шапка → строка возврата:**
      · `.seeme-head` (`1519–1524`, стили `1581–1586`) перестаёт быть шапкой: снять
        `position: sticky; top: 0; z-index: 1;` (`1582`) и полосу `background: var(--panel-solid);
        border-bottom: 1px solid var(--edge);` (`1584`), удалить `<h2>{t.seeMe[lang]}</h2>`
        (`1523`) вместе с его правилом (`1586`) — настоящая шапка продукта уже стоит выше;
      · кнопка возврата остаётся как есть — `.back-btn` (`1520–1522`, стили `1587–1591`) и есть
        та «строка возврата», о которой сказал владелец, и она же зовёт `closeSeeMe()`.
- [x] **Шаг 4 — не потерять то, что уже оплачено:**
      · 🔑 **инвариант `bugs/76`, ронять нельзя:** `seeMeOpen = page.state.seeMe === true` (`92`),
        `openSeeMe()` = `pushState('', { seeMe: true })` (`94–96`), `closeSeeMe()` = `history.back()`
        (`98–100`). Запись в истории и единственная точка закрытия не трогаются — меняется только
        то, КАК открытое состояние нарисовано;
      · Escape (`1070–1072`) зовёт ту же `closeSeeMe()` — оставить: это не второй путь закрытия, а
        вызов той же точки;
      · `transition:fade` на появлении (`1518`) — оставить, планка анимаций (`EXPERIENCE`, боль
        владельца 2026-07-12);
      · память вида (`plans/08`, `useViewMemory` — `127–133`): предпросмотр сознательно НЕ
        запоминается, так и оставить;
      · кнопка входа `onclick={openSeeMe}` в колонне профиля (`1422`) остаётся на месте — решение
        владельца от 2026-07-27 про кнопку не отменяется.
- [x] **Шаг 5 — страж.** Проверка «оболочка видима при открытом предпросмотре» (`AppBar`,
      `BottomNav`, `SideRail` — по замеру шага 0) + «„Назад“ закрывает предпросмотр, а не
      приложение» на 430/1440 × обе темы. Доказать мутацией (вернуть `position: fixed; inset: 0`
      в `.seeme` → страж обязан покраснеть).

## Результат (2026-08-02)

**Замер «до» на сломанном коде — 32 провала** (`node tools/verify-bug105.mjs`). Числом, а не
глазами: при открытом предпросмотре в точке шапки лежал `h2` слоя, в точке нижней панели — сам
`div.seeme`, на 1440 в точке рельса — он же; `body.overflow` был `hidden`.

**После фикса — 56 проверок, 0 провалов** (обе темы × 430/1440). Доказан мутацией: вернул
`.seeme { position: fixed; inset: 0; z-index: 70 }` → **4 адресных провала** (шапка закрашена,
рельс закрашен, `position: fixed`, ссылка навигации не ловит палец).

🔑 **Проверка видимости оболочки сделана `elementFromPoint`, а не селектором.** Это не педантизм:
шапка при дефекте оставалась в разметке, видимой и с честными координатами — селекторная проверка
была бы ЗЕЛЁНОЙ на сломанном коде. Тот же капкан, что в `verify-bug40` (щит и лендинг лежат в
разметке оба и всегда).

### 🔴 План этого документа ошибся в одном месте, и поймали это ГЛАЗА, а не страж

Шаг 2 требовал дать секции предпросмотра **всю ширину** (`grid-column: 1 / -1`, «как `.intro` /
`.guest-card` / `.state`»). Так и было сделано — и страж стал зелёным. А на кадре 1440 оказалось,
что строки «Имя … Все» растянулись на 1140px: значение уехало к одному краю, чип аудитории к
другому. Это ровно **«пустота ВНУТРИ карточки»**, которой владелец забраковал прежний профиль
(`EXP-0104`), — метрика её не видит, и страж не видел тоже.

Исправлено **по канону соседа, а не по вкусу агента**: `/account` держит содержательный виджет в
ДВУХ шагах сетки и прямо называет полупустую половину «не дырой, а местом будущих виджетов»
(`EXP-0107`). Предпросмотр получил `grid-column: span 2`. Ответ владельца от этого не страдает:
«там же, где содержимое Профиля» — это и есть колонна той же ширины, что у остальных карточек.

**Урок в общем виде:** план, написанный по чтению кода, не заменяет взгляда на кадр. Страж
стерёг то, что было названо (оболочка не закрашена), и не мог знать о том, что названо не было.

### Соседний страж пришлось РАЗВЕРНУТЬ, а не подогнать

`tools/verify-bug76.mjs` требовал `body.overflow === 'hidden'` при открытом предпросмотре — то
есть стерёг скролл-лок как признак исправности. После фикса он дал **4 провала на исправном
коде**: владелец отменил саму форму, которую тот стерёг.

Проверка **не удалена и не ослаблена, а развёрнута**: теперь она требует `!== 'hidden'` и стережёт
живой инвариант «страница не заперта НИКОГДА» (прежняя редакция проверяла это лишь наполовину —
только после закрытия). Причина изменения записана врезкой в шапке самого файла стража, чтобы
следующая сессия не прочитала это как тихую подгонку теста под новое поведение. После разворота —
**64 зелёных, 0 провалов**.

### Что проверено рядом

`verify-bug76` **64/0** (запись в истории и «Назад» — инвариант жив) · `verify-account` **84/0** ·
`verify-plan08` **56 зелёных, 0 провалов** (память вида) · `npm run check` — 0 ошибок,
осиротевших селекторов нет.

**Приёмка боем — за владельцем** (выката не было; фикс лежит в `main`).

## Решения, принятые без владельца

*(остальное заполняется при закрытии)*

✅ **ЗАКРЫТО 2026-08-02: форму назвал владелец, допущения здесь больше нет.**
Источник — интервью №013, В1 (`interviews/interview_013_session_forks.md`, ответ подписан
«Николай Кривуша», 2026-08-01T22:26Z, транспорт — страница вычитки). Выбран **вариант А**:

> «Как обычный экран продукта. Предпросмотр рисуется там же, где содержимое Профиля; шапка, нижняя
> панель и рельс остаются на своих местах и работают. Вторая шапка исчезает, остаётся строка
> возврата.»

Что это снимает. Прежний PENDING читал слова владельца двумя способами — (а) экран продукта в
рабочей области и (б) наложение, заправленное ПОД оболочку. Владелец выбрал (а); (б) отклонён,
хотя переделка по нему была бы меньше. Отклонён и третий вариант из интервью — отдельный адрес
`/profile/preview`: адрес экрана не меняется, предпросмотр остаётся неглубоким состоянием истории
(`bugs/76`). Макеты не рисуем: правило четырёх макетов здесь не срабатывает — форма названа
словами, а не выбрана глазами.

Мелочь, решённая агентом внутри ответа (не развилка, а следствие): заголовок второй шапки
`<h2>{t.seeMe[lang]}</h2>` (`+page.svelte:1523`) удаляется вместе с ней и отдельного места в
`AppBar` не получает — вид называет себя заголовком карточки `t.seenBy` (`+page.svelte:1534`).
Если владелец захочет имя вида в шапке продукта — это отдельная правка `AppBar`, не этого бага.

## Ссылки

`interviews/interview_013_session_forks.md` В1 (ответ владельца: форма = вариант А) · `bugs/76`
(окно получило запись в истории — та же нить) · `bugs/36` (лайтбокс фото: чужая форма, которую
унаследовал этот слой) · `plans/08` (память вида — предпросмотр не помним) · `researches/12`
(инвентарь паритета 1.x) · `ideas/24` (виджетная сетка сводных экранов) ·
`src/routes/profile/+page.svelte` (весь код дефекта)
