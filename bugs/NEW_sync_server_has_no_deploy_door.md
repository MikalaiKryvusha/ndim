# Bug NEW — У сервера синхронизации нет двери выката: 13 коммитов его кода никогда не были в бою

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16.
<!-- ВЕРА-КОНЕЦ -->

> **Создан:** 2026-09-08 · **Статус:** 🔴 ОТКРЫТ, найден при переезде на PM2 (`plans/83`), не чинён ·
> **Нашёл:** агент, сверив коммит живого образа с деревом · **Класс:** у половины продукта нет
> ворот, которые у другой половины есть · **Родня:** `plans/83` (переезд), `plans/53` (контуры),
> `AGENT_GUIDE.md` → «ВЫКАТ В БОЙ».

## Симптом

Живой боевой сервер синхронизации отчитывался версией **2.1.0, сборка 31, собран
2026-08-15T20:08:49Z**. В дереве на 2026-09-08 код `sync-server` ушёл на **13 коммитов вперёд**
(`b095195` … `d404211`, с 2026-08-17 по 2026-08-30). Из них:

| Коммит | Что не доехало в бой |
|---|---|
| `715d561`, `7f558a7` | эпик 60 фаза 1 — прибор замера цены простоя, разбор 2880 холостых операций |
| `874e880`, `c8404c8` | эпик 60 фаза 2 — состояние эскорта на точке, **подписка вместо опроса** |
| `f7f4919` … `1be1d84` | `plans/63` шаги 1–5 — **уборка мёртвых гостей: она УДАЛЯЕТ аккаунты и их следы** |
| `0720d14` | починка гонки с внешней чисткой Auth (обрывала суточный проход) |
| `b095195` | комната измерений панели — один запрос на список |

Три недели работы лежат в стволе и не работают ни у кого.

## Причина

**У веб-приложения дверь есть, у сервера синхронизации — нет.** `npm run deploy` собирает начисто,
проверяет хеш сборки, гоняет смоук под сессией и только потом выкатывает (`AGENT_GUIDE.md` →
«ВЫКАТ В БОЙ», слово владельца 2026-08-15). У сервера синхронизации выкат — это `npm run sync:image`
плюс `docker run` руками, без единых ворот: ни смоука, ни расписки, ни проверки, что выкаченное
вообще поднялось.

Раз ворот нет, нет и напоминания. Никто не забыл — **некому было напомнить**.

🔴 **Второй симптом того же корня, найденный тем же замером:** образ был собран из **несохранённого
дерева** — `builtAt` на семь минут раньше коммита `f6a0c14`, поэтому номер сборки 31 принадлежал
предыдущему коммиту, а код внутри был новее. **Номер сборки врал три недели**, и сверить бой с
историей было нечем. У веб-приложения ровно этот класс закрыт распиской на коммит и отказом
выкатывать грязное дерево (`tools/deploy.mjs:487`, `EXP-0282`).

## Чем это опасно прямо сейчас

Тринадцать коммитов нельзя выкатить «заодно»: среди них код, **удаляющий гостевые аккаунты**, и
переписанный ритм эскорта. Ни то, ни другое в бою не работало ни минуты. Выкатить их вместе с
переездом на PM2 значило бы сделать два изменения одним шагом и потерять ответ на вопрос «что
сломалось». Поэтому `plans/83` поднял бой **старым кодом** — сознательно.

## Направление лечения

1. **Дверь для сервера синхронизации** по образцу двери приложения: расписка на коммит · отказ
   грязному дереву · прогон `npm run test:sync` по тому же коммиту · подъём службы · **проверка
   пульса прибором** (`tools/read-server-pulse.mjs` — он уже есть) · откат одной командой.
   После переезда на PM2 это дешевле, чем было с образом: выкат = сдвинуть замороженную копию на
   новый коммит и перезапустить службу.
2. **Отдельным решением владельца — выкат тринадцати коммитов.** Порциями, а не одним куском:
   первой идёт починка гонки (`0720d14`, чистая), последней — уборка гостей (`plans/63`), у неё
   цена ошибки самая высокая.

## Приёмка

1. Мутация: попытка выката сервера синхронизации на грязном дереве — отказ.
2. Мутация: служба поднялась, но пульс мёртв — дверь красная, а не зелёная.
3. `git -C <замороженная копия> rev-parse HEAD` совпадает с распиской.

## 🟡 СТАТУС 2026-09-12 — ДВЕРЬ ПОСТРОЕНА И ДОКАЗАНА; ПОРЦИЯ 1 В БОЮ С 15:28, ПОРЦИИ 2–4 ВПЕРЕДИ

**Половина первая — ЗАКРЫТА.** `tools/deploy-sync-server.mjs` (`plans/85`): девять шагов, каждый
умеет отказать, расписка на коммит, автоматический откат по мёртвому пульсу, откат одной командой.
Дверь знает оба контура (`--contour prod|stage`), умолчания нет намеренно.

Заодно поднята служба стейджа под PM2 (`ndim-server-stage` + worktree `ndim-server-stage`) — она
и есть место, где дверь доказывается, не трогая бой. Хвост `plans/83` «стейдж под PM2 не поднят»
этим закрыт.

**Приёмка — все три критерия закрыты наблюдением** (отчёт
`qa/reports/2026-09-12_sync-server-deploy-door.md`):

1. ✅ Грязное дерево → отказ, код 1 (плюс ещё четыре отказа: несуществующий коммит, коммит без
   `sync-server/`, занятый порт стенда, контур не назван).
2. ✅ **Пульс мёртв → дверь красная и САМА откатывает.** Доказано не подстроенной мутацией, а
   НАСТОЯЩИМ отказом: первый полный прогон упал на дефекте самой двери (не передавала контур
   прибору пульса), и дверь честно подождала пять тактов, вернула копию на прежний коммит,
   перезапустила службу и отказала кодом 1. Бой при этом не был тронут.
3. ✅ `git -C <копия> rev-parse HEAD` совпадает с распиской.

Главный путь тоже пройден руками: выкат `f6a0c14 → 0720d14` (`npm run test:sync` по выкатываемому
коммиту 112/112, пульс жив, расписка), затем откат обратно.

**Половина вторая — ОТКРЫТА и остаётся поводом этого документа.**

✅ **Порция 1 в бою с 2026-09-12 15:28** — `f6a0c14 → c8404c8` по слову владельца «*кати в бой,
и смотри, как оно работает в бою*». Сверено 2026-09-12 22:51: `git -C D:/work/ai_sandbox/ndim-server-prod
rev-parse --short HEAD` → `c8404c8`, `node tools/read-server-pulse.mjs --contour prod` → жив.
⚠️ Прежняя редакция этого раздела писалась в 15:11 и говорила «в бою по-прежнему `f6a0c14`, все
пятнадцать НЕ ВЫКАЧЕНЫ» — через 17 минут это стало неправдой, а строка осталась. Класс «пункт
„ждём X“ пережил собственное исполнение».

🔴 **Порции 2–4 НЕ выкачены.** Порядок по фактической истории коммитов — в `plans/85`. Порция 2
(`0720d14`) приводит уборку гостей `plans/63`, она удаляет аккаунты; перед ней — сутки наблюдения
за порцией 1, полный проход на стенде, счёт затронутых в боевой базе и отдельное слово владельца
на эту порцию.

📌 Слово на порцию 2 спрашивается страницей интервью, когда выполнены эти предусловия (`plans/85` Ш6). <!-- ВОПРОС-ОК: строка называет, когда и где будет задан вопрос о порции 2; самого вопроса здесь нет, он не может быть задан до суточного наблюдения порции 1 (ворота TESTING_FRAMEWORK перед словом «катить») -->
