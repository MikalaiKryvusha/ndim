# Bug 55 — Внутренние страницы «Меню»: мелкие иконки, нет центровки, «Руководство пользователя» потеряло картинки, кнопки и звёзды

**Статус:** 🔴 OPEN — часть требует **макетов владельцу** (вёрстка страниц) ·
**Когда/контекст:** 2026-07-27, волна 11 владельца (два сообщения + скриншот `/menu/donate`)

## Симптом

Слово владельца дословно, два захода:

> «дизайны внутренних страниц Settings стали отвратительные. Иконки маленькие, центровки
> важных текстов нет. В старом NDim было более красиво» *(скриншот `/menu/donate`)*

> «Вёрстка всей „Руководство пользователя“ отвратительна — потерялись картинки, потерялись
> кнопки, потерялись звёзды — всё, что было в оригинальном старом NDim»

**Волна 12 (2026-07-27, тот же день) — владелец повторил и уточнил:**

> «починить верстку дочерних страниц Settings - особенно Руководство пользователя. В
> оригинальном Ndim оно былт с картинками, с кнопками HTML, с таблицами.»

> «во многих старницах были красивые иконки, если был емодзи - он был красочный. Сейчас там
> все скупое и некрасивое. нет центровок.»

> «в Об Авторе вообще фото потерялось.»

> «В Общем - наводим красоту с стиль.»

Ключевое уточнение волны 12 — **«если был емодзи — он был красочный»**. Разведка это
подтвердила буквально: в 1.x смайлики шкалы красились одиннадцатью CSS-фильтрами от красного
к зелёному, а «десятка» была отдельным многоцветным файлом. Наш `Icon` одноцветен
(`currentColor`) — то есть для шкалы нужен ВТОРОЙ, цветной класс графики, а не тот же набор.

## Что видно на скриншоте `/menu/donate`

1. **Глифы вместо иконок.** В кнопке «Сделать пожертвование» стоит `♡`, в строке «Спасибо за
   Вашу поддержку!» — `❤`. Это те самые юникод-знаки, которые `bugs/17` вычистил из оболочки
   и «Меню», но **до страниц документов не дошёл**: у глифа нет ни сетки, ни веса, а `❤`
   вдобавок рисуется системным шрифтом.
2. **Стрелка «‹ Меню»** — тоже глиф, а не иконка набора.
3. **Разнобой выравнивания.** Заголовок и абзацы — по левому краю, «Прямая ссылка» и обе
   сноски — по центру. Владелец читает это как «центровки важных текстов нет».

## «Руководство пользователя» — отдельная и большая часть

В 1.x руководство было иллюстрированным: **картинки экранов, изображения кнопок и звёзд**
прямо в тексте (человек видел, о чём речь). В 2.0 текст перенесён дословно
(`tools/extract-docs.mjs` из `researches/07`), но **только текст** — вся графика утеряна.

⚠️ Это не «поправить отступы», а восстановление содержимого:
- иллюстрации 1.x лежат в `ndim_old/public/images/` (звёзды `star_*.svg`, кнопки, кадры);
- как именно они стояли в тексте — смотреть в живом 1.x (`index.html` + `app.js`, раздел
  руководства) и в `researches/07_user_manual_1x.md`;
- часть разделов руководства в 2.0 **намеренно** отсутствует (экраны «Дом», расписание
  синхронизации 1.x) — это решение задокументировано в `STATUS.md`, его не отменять молча.

### Разведка выполнена 2026-07-27 — счёт по строкам

Полный инвентарь снят ЧТЕНИЕМ `ndim_old/storage/public/um.html` и записан в
**`researches/12` → «Вёрстка страниц документов 1.x»**. Счёт: **66 `<img>` · 2 `<table>` ·
2 круглые кнопки 80×80 · 9 заголовков · ноль настоящих `<button>`** (кнопки — `div` с
картинкой). Утеряно в 2.0: логотип-шапка · 3 иллюстрации мысленных экспериментов
(остров/самолёт/больница) · финальная `network.webp` · звёзды и цветные смайлики в таблице
шкалы · обе круглые кнопки-образца (💡 и 🔍) · портрет автора и его пять ссылок.

**Ассеты найдены — владельца ждать не нужно.** `homeworks/06` просил у него портрет и ссылки;
и портрет (`Mikalai.webp`), и все пять адресов лежат в архиве 1.x, опубликованные им же.

## Что делать (порядок)

1. **Разведка (до кода, `AGENT_GUIDE` шаг 7а):** снять в `researches/12` вёрстку страниц
   документов 1.x — типографику, выравнивания, какие иллюстрации где стояли.
2. **Механическая часть — агент сам, без макетов:** заменить оставшиеся глифы (`♡ ❤ ‹` и
   прочие) на иконки набора `Icon`, выровнять иконку и текст флексом (тот же класс дефекта,
   что закрыт в профиле волной 11), привести выравнивание блоков к одному правилу.
3. **Вёрстка страниц и возвращение иллюстраций — 4 макета владельцу** (`AGENT_GUIDE` →
   «Дизайн»): это лицо продуктовых документов, агент такое один не решает.

## Инвентарь класса (грепом, ДО правок)

Все страницы под `/menu/*`: `manual`, `terms`, `privacy`, `disclaimer`, `support`, `donate`,
`about`, `author`, `manifesto`, `share` + общая оболочка `src/lib/ui/DocShell.svelte`.
Заполнить таблицей «страница → глифы → выравнивание → потерянная графика» — сдавать по строкам.

## План проверки

QA живым Chrome, обе темы × 390/1440: на страницах документов не осталось юникод-глифов
(греп по знакам, как в `tools/verify-icons.mjs`), иконка и текст в одной линии, выравнивание
единое. Для руководства — сверка по строкам инвентаря с 1.x.

## Сделано 2026-07-27 — механическая часть (шаг 2), коммиты `b3fd192`, `639f254`

| Что | Как |
|---|---|
| «в Об Авторе вообще фото потерялось» | портрет `static/img/author.webp` (из архива 1.x) + пять площадок с НАСТОЯЩИМИ знаками и фирменными цветами — `src/lib/ui/brands.ts`, снято дословно. `homeworks/06` закрыта как ошибка агента |
| глифы на кнопках | `♡` → иконка `donate` (тот же `donate-heart.svg`, что был на этой кнопке в 1.x) · `✉` → `support` · `↗` → `share` · `⧉` → `copy` · `‹` → `back` · монограмма `✉` в сетке соцсетей → `envelope` |
| «если был емодзи — он был красочный» | `❤` → `❤️`: при переносе текстов потерялся селектор начертания **U+FE0F**, и цветной эмодзи 1.x стал чёрным типографским знаком. Вернулся на «Поддержке», «Пожертвовании» и в «О системе» |
| «нет центровок» | канон 1.x: на КОРОТКИХ страницах-действиях весь текст был по центру (`.page_main_text p { text-align: center }`), длинные документы — нет. 2.0 делал ровно наоборот (сноски по центру, абзацы слева). Приведено к канону |
| иллюстрации коротких страниц | `support.png` и `donation.png` вернулись на свои страницы |

**Проверка:** `node tools/verify-wave12.mjs` **108/108**, обе темы × 390/1440, скриншоты
`test-results/wave12/` смотрены глазами. Страж различает два случая: глифов быть НЕ должно,
а цветной ❤️ остаться ОБЯЗАН.

## Осталось — шаг 3: 4 макета владельцу

**`design/manual-mockups.html`** нарисован и ждёт выбора: V1 «Как было» (дословный перенос) ·
V2 «Главы-карточки» · V3 «Живая шкала» · V4 «Документация». Возврат картинок, таблицы со
звёздами и цветными смайликами и круглых кнопок-образцов — общее для всех четырёх, это не
предмет выбора. **Ассеты уже в проекте** (`static/img/docs/`), цвета шкалы замерены (EXP-0057).

## Решения, принятые без владельца

1. **Брендовые знаки автора вынесены в отдельный модуль `brands.ts`, а не добавлены в
   `icons.ts`** — там контракт «одноцветно, `currentColor`, работает в Ч/Б», а фирменный знак
   так не умеет. Это разные классы графики, и смешивать их значило бы сломать контракт набора.
2. **На «Пригласить друзей» настоящие знаки НЕ появились** — только у почты, потому что её
   иконка у нас есть. Остальные сети остались монограммами: оригиналов их логотипов у нас нет,
   а рисовать чужой знак по памяти нельзя (решение bugs/47 не отменяю в чужом баге).
3. **Адреса автора взяты из 1.x без вопроса владельцу** — они были опубликованы им самим на
   его же публичной странице. Спросить стоит только «не изменилось ли что-то с тех пор».
4. **Центровка применена к коротким страницам, но НЕ к длинным документам.** В 1.x центрировано
   было и то и другое? Нет: длинные документы (`um`/`tou`/`pp`) шли `.paragraph` с отступом
   первой строки, без центровки. Проверено чтением `styles.css`, а не по памяти.

## Ссылки

`bugs/17` (набор иконок — до этих страниц не дошёл) · `bugs/31` (документы из Firestore) ·
`bugs/48` (правила оформления) · `researches/07_user_manual_1x.md` ·
`src/lib/ui/DocShell.svelte` · `tools/extract-docs.mjs` · волна 11
