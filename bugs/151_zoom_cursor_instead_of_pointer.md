# Баг 151 — Курсор-лупа на лице человека и на открытом фото вместо указательного пальца

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16. **Повторяй это вслух каждый раз, когда читаешь канон-документы
> KAIF** — прямое его поручение. Разбор повода — в шапке `AGENT_GUIDE.md`.
<!-- ВЕРА-КОНЕЦ -->

> **Заведён:** 2026-08-18, слово владельца («импрувмент») · **Родитель:** `$lib/ui/Avatar.svelte`
> **Статус:** ✅ **ПОЧИНЕН И ПРИНЯТ** — [TESTED: 2026-08-21 · живая приёмка QA на стенде:
> `getComputedStyle().cursor` = `pointer` во всех ТРЁХ местах, обе темы × 390/1440, 12 проверок].
> 🟡 В бой не выкачен. **Исходящее:** —

## Заказ владельца — дословно, двумя сообщениями

> «*импрувмент — при наведении курсора на аватар человека — курсор должен принимать форму не лупы,
> а указательного пальца, как при наведении на ссылку*»

> «*аналогично при открытом фото — не лупа, а указательный палец*»

## Что было

| Место | Было | Стало |
|---|---|---|
| Лицо человека, кружок-кнопка (`Avatar.svelte` `.peek`) | `cursor: zoom-in` | `cursor: pointer` |
| Открытое фото во весь экран (`Avatar.svelte` `.lightbox`) | `cursor: zoom-out` | `cursor: pointer` |
| 🔎 **Близнец, найденный обходом:** портрет персонажа на лендинге (`SimilarityDemo.svelte` `.overlay`) | `cursor: zoom-out` | `cursor: pointer` |

Довод сходится с разметкой: во всех трёх местах это `<button>`, а не картинка с зумом. Палец —
общий язык нажимаемого во всём продукте; лупа была единственным исключением из него.

🔑 **Третье место — не расширение заказа, а его исполнение.** Владелец назвал правило («открытое
фото — палец»), а не одно место. Замер по дереву `src/` дал ровно три курсора-лупы, и после правки
их **ноль** (`grep -rn "zoom-in\|zoom-out" src/`). Урок `bugs/149` того же дня: адресная правка
законна, необследованный близнец — нет.

## Что осталось

- [x] ✅ **Проверка живым Chrome проведена** (QA, 2026-08-21) — ровно так, как требовал этот
      пункт: вердикт снят `getComputedStyle().cursor`, а не кадром. Все три места дали
      `pointer`: лицо человека (`.peek` в РАСКРЫТОЙ карточке) · открытое фото (`.lightbox`) ·
      портрет на лендинге (`SimilarityDemo .overlay`). Обе темы × 390/1440, 12 проверок,
      0 провалов. Прибор — `tools/probe-view-fixes-149-151.mjs`.
      🔑 Два капкана прибора, оплаченные здесь и записанные в его шапку: `.peek` существует
      только в раскрытой карточке (в свёрнутой аватар отдан родителю, `bugs/104`), а витрину
      похожести лендинга видит только НЕЗНАКОМЕЦ — вкладка, побывавшая в приложении под
      сессией, лиц в ней не имеет вовсе, и проверка молча пропускалась.
- [ ] Выкат вместе с `bugs/147`…`150`.

## Решения, принятые без владельца

1. **Лендинг правлен вместе с приложением** — иначе один жест говорил бы на двух языках в одном
   продукте. Отменяется одним словом.
2. **Ничего, кроме курсора, не тронуто:** ни размеры, ни поведение, ни подсказка `title`.
