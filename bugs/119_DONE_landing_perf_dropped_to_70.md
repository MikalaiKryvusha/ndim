# Баг 119 — лендинг просел Lighthouse Perf 100 → 70 (mobile): тяжёлый JS у каждого посетителя

**Заведён:** 2026-08-14 · **Нашёл:** агент, замером перед шагом 2 `ideas/27` (Lighthouse по
канону `EXP-0022`) · **Класс:** перформанс / бюджет лендинга · **Состояние:** ✅ **ЗАКРЫТ
2026-08-14 тем же днём** — фикс «О системе» уехал в бой выкатом `plans/39`, повторный замер:
**Perf 100** (дважды), цель ≥90 перевыполнена. Разбор — секция «Закрытие» внизу

---

## Симптом (числа, не ощущение)

| Замер (mobile-эмуляция Lighthouse 13.4.1, Chromium Playwright) | Perf | FCP | LCP |
|---|---|---|---|
| **Бой** `https://ndimspace.app/` (старый корень, 2026-08-14) | **70** | 4.0 s | 5.4 s |
| Собранный сайт `localhost:4173/ru` (preview, до любых правок графики) | **65** | 4.4 s | 6.9 s |
| Точка отсчёта: бой 2026-07-11 (`EXP-0022`, тогдашний корень) | **100** | — | — |

Обе сегодняшние точки сходятся ⇒ это не артефакт прибора и НЕ следствие переезда на языковые
адреса (бой ещё живёт на старом корне без них). Лендинг потяжелел где-то между 2026-07-11 и
сегодня — за это время на него приехали: полоса из четырёх чисел, полные иллюстрации персон
(3 × ~1,2 МБ, зум), карта демо, гостевой мост, воронка.

⚠️ Замеры лабораторные и на throttling 1.6 Mbps; у preview нет сжатия хостинга — поэтому
сравнивать «до/после» будущего фикса надо ВСЕГДА на одном и том же стенде.

## Что нашлось в отчёте (сырьё для диагностики)

- **554 КБ (High) `_app/immutable/chunks/…js` — внутри Firebase + Firestore** — грузится на
  лендинге КАЖДОМУ посетителю. Это отложенный чанк проверки сессии (`data/session.ts`,
  динамический импорт по канону `EXP-0028`) — вопрос не «почему он есть», а «почему так тяжёл
  и что из Firestore в нём делает на лендинге».
- `unused-javascript`: 152 КиБ / ~860 мс экономии.
- К лендингу подтягивается `apis.google.com/...gapi_iframes` (~104 КБ) — след auth-iframe.
- Лица демо (3 × 144–169 КБ) идут с приоритетом **Low** и метрикам не мешают — прелоад им
  НЕ нужен (замер шага 2 `ideas/27`; см. правку инвентаря там же).
- Полные отчёты: снимались в scratchpad сессии (`lh-before.json`, `lh-prod.json`) — при
  фиксе перегнать заново, канон запуска — `EXP-0022`.

## ✅ Диагностика 2026-08-14 (по vite-манифесту, `.svelte-kit/output/client/.vite/manifest.json`)

Обход графа импортов ответил точно, кто дотягивается до 554-КБ чанка **статически**:

| Узел | Маршрут | Вердикт |
|---|---|---|
| 4–11 | account · admin · auth/action · dims · menu · profile · relations · space | личные экраны — SDK им положен |
| 13 | `[lang=lang]/delete-account` | публичная, но вся её работа — вход и удаление; кандидат на динамизацию, НЕ горит |
| ~~15~~ | ~~`[lang=lang]/menu/about`~~ | 🔴 **УТЕЧКА** — публичная «О системе» возила 554 КБ ради виджета версии сервера, который без входа даже не показывается. Класс `EXP-0136` (публичной странице вшили тяжёлое). **✅ ПОЧИНЕНО тем же днём**: `data/profile`+`data/space` → динамический импорт в `onMount` (канон `EXP-0028`); контроль по манифесту — узел 15 из статического списка ИСЧЕЗ; `npm run check` 0 · юниты 234 · e2e menu+seo 22 passed |

🔑 **Лендинга в статическом списке НЕТ** — его SDK едет динамическим чанком по замыслу
(`EXP-0028`), Lighthouse считает его в весе, потому что `hasSession()` стартует сразу после
гидратации. Отсюда уточнённые направления для самого лендинга (Perf 70):

1. Худеть сам динамический чанк: для `hasSession` достаточно Auth, а в чанке едет и Firestore.
2. Отложить старт проверки сессии до idle (`requestIdleCallback`) — метрики снимаются раньше.
3. FCP 4 s — критический путь на throttling: сравнить состав entry с июльским.

## Чего НЕ делать

- Не «чинить» прелоадом графики — графика в метриках не виновата (Low, ниже фолда).
- Не мерить «после» на другом стенде, чем «до».
- Не выкидывать проверку сессии с лендинга — она продуктовая (`bugs/08.1`, `bugs/10`);
  вопрос в ВЕСЕ и МОМЕНТЕ, а не в существовании.

## ✅ Закрытие 2026-08-14 (после выката `plans/39`)

**Повторный замер боя** (Lighthouse 13.4.1, mobile-эмуляция, симулированный throttling — тот же
прибор): `https://ndimspace.app/ru` → **Perf 100 · FCP 1,2 s · LCP 1,3 s · TBT 0 ms**;
контрольный второй прогон — **Perf 100** те же метрики. Было: бой 70 / FCP 4,0 s / LCP 5,4 s.

**Что именно вылечило.** Утренний фикс «О системе» (`data/profile`+`data/space` → динамический
импорт, коммит `c167472`) вынул 554-КБ Firebase-чанк из СТАТИЧЕСКОГО графа импортов — и выкат
`plans/39` увёз это в бой вместе с пересборкой. Пока чанк был статически достижим, SvelteKit
преподгружал его (`modulepreload`) на страницах, деля канал 1,6 Mbps с критическим путём
лендинга — отсюда FCP 4 s. Теперь SDK едет только динамически после гидратации: он ещё виден в
`unused-javascript` (~105 КиБ экономии — обе точки роста живы, см. ниже), но из метрик ушёл.

**Что осталось честно открытым (НЕ реанимировать баг, это задел):** динамический чанк сессии
всё ещё возит Firestore ради `hasSession` (достаточно Auth) — похудение возможно, но теперь
это оптимизация без симптома; направление записано в `ideas/27` шаг 4 контекстом.

🔧 **Капкан прибора, оплачен этой сессией:** штатный лаунчер Lighthouse в этом окружении падает
(«Protocol error (Page.enable): Session closed») с ЛЮБЫМ браузером. Рабочий путь: поднять Chrome
самому (`--headless=new --remote-debugging-port=9333 --user-data-dir=<пустой>`) и передать
`npx lighthouse <url> --port=9333`. Урок — `EXP-0146`.
