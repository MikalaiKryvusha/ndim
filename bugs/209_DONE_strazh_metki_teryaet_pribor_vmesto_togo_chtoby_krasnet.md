# bugs/NEW — Страж метки ТЕРЯЕТ прибор вместо того, чтобы покраснеть: «опт-ин необратим» неверно для трёх приборов из пятнадцати

> **Создан:** 2026-08-28 (смена 7, суд QA ветки `ndim_dev2`, срез `12cec3b`, фаза 1 `plans/74`) ·
> **Статус:** ✅ ЗАКРЫТ 2026-08-29 (смена 10, dev-3) — лечение в двух редакциях, все четыре
> критерия сняты живыми мутациями, самотест 16 → 20 случаев ·
> **Родня:** `bugs/202` (воронка загрязнена нашими смоуками — ради этого страж и написан) ·
> `tools/verify-probe-mark.mjs` · `EXP-0194` (страж вне ворот) ·
> `AGENT_GUIDE.md` → лестница трёх вопросов, вопрос 2
> **Повод:** суд ветки. Страж сделан хорошо и стоит в воротах; неверно ОДНО его свойство —
> и ровно то, на которое опирается решение №5 сдачи.

## Вывод-вперёд

`tools/verify-probe-mark.mjs` следит, чтобы каждый браузерный прибор, способный дойти до живого
контура, метил свои сессии — иначе наши прогоны считаются людьми и пачкают воронку (`bugs/202`).

🔴 **Но прибор, у которого метку УБРАЛИ, не краснеет — он исчезает из корпуса.** Страж остаётся
зелёным, а число осмотренных молча уменьшается на единицу. Комментарий в коде обещает
обратное: «*Правило простое и не расширяет корпус: **опт-ин необратим**. Прибор, однажды взявший
помощник, судится на ПОЛНОТУ*». Опт-ин обратим удалением одной строки.

## Криминалистика — мутация и контроль, оба на живом дереве

Признак корпуса (`isLiveBrowserProbe`, строки 57–74):

```js
const drivesBrowser = /chromium\.launch\(/.test(source);
const reachesLive = /ndimspace\.app/.test(source) || /ndim-stage\.web\.app/.test(source) || /lib\/contours\.mjs/.test(source);
const knowsMark  = /lib\/probe-mark\.mjs/.test(source);
return drivesBrowser && (reachesLive || knowsMark);
```

Прибор без живого адреса В ТЕКСТЕ попадает в корпус **только через `knowsMark`**. Убрал метку —
`knowsMark` ложь, `reachesLive` ложь, прибор перестал быть «живым» и не судится вовсе.

| мутация (снят импорт `probe-mark`) | осмотрено | вердикт стража |
|---|---:|---|
| `smoke.mjs` (живого адреса в тексте НЕТ) | **15 → 14** | ✅ зелёный, код 0 — 🔴 **дефект** |
| `probe-test-pages-live.mjs` (живой адрес ЕСТЬ) — контроль | 15 | ❌ красный, прибор назван, код 1 ✅ |

Контроль обязателен и приведён намеренно: **для 12 приборов из 15 страж работает верно.**
Слепое пятно ровно у тех, кого держит только собственная метка.

⚠️ Контроль попадания снят по ПРЕДМЕТУ ЧТЕНИЯ, а не по факту правки файла (`EXP-0198`): перед
каждым прогоном печаталось, что видит сам признак стража (`/lib\/probe-mark\.mjs/.test(source)`)
— `true` до, `false` после.

## Радиус поражения — поимённо, признаком САМОГО стража

Замер моим зондом, повторяющим предикаты стража дословно (число сошлось с его собственным: 15):

| прибор | живой адрес в тексте | держится только меткой |
|---|---|---|
| **`smoke.mjs`** | НЕТ | 🔴 ДА |
| **`probe-bridge.mjs`** | НЕТ | 🔴 ДА |
| **`verify-funnel-v2.mjs`** | НЕТ | 🔴 ДА |
| остальные 12 | есть | — |

🔑 **Тяжесть определяется именно этими тремя, а не их числом.** `smoke.mjs` — тот самый прибор,
чьи прогоны и загрязнили воронку (`bugs/202`: «гостевые всплески совпадают с днями выкатов —
свои же смоуки»). `verify-funnel-v2.mjs` — приёмка самой воронки. То есть без охраны остались
ровно те, ради которых страж заведён.

## Почему зелёный тут особенно опасен

У воронки нет громких отказов: `track()` глотает ошибку намеренно. Страж заведён, чтобы поломка
не давала **тихий ноль**. Здесь он сам даёт тихий зелёный — то же самое свойство этажом выше.
Единственный видимый след — число «осмотрено 15», но его не с чем сравнить: ожидаемого значения
нигде нет, и «14» читается как норма.

⚠️ Контроль на ПОЛНУЮ слепоту у стража есть и работает (`judged === 0` → «признак сломан»).
Не защищено именно ЧАСТИЧНОЕ усыхание корпуса.

## Воспроизведение (детерминированное)

```bash
node tools/verify-probe-mark.mjs            # ✅ «осмотрено 15», код 0
# снять строку импорта probe-mark из tools/smoke.mjs, затем:
node tools/verify-probe-mark.mjs            # ✅ «осмотрено 14», код 0 ← дефект
# то же с tools/probe-test-pages-live.mjs:
node tools/verify-probe-mark.mjs            # ❌ красный, прибор назван, код 1 ← контроль
```

## Наблюдаемый критерий приёмки фикса

- [x] Снятие метки с `smoke.mjs`, `probe-bridge.mjs` или `verify-funnel-v2.mjs` даёт **красный
      прогон с именем прибора**, а не молчаливое уменьшение счёта.
- [x] Страж сравнивает число осмотренных с **объявленным ожиданием** (якорь в коде либо
      поимённый список опт-ина), и расхождение краснеет само.
- [x] Контроль на месте: у прибора с живым адресом снятие метки по-прежнему краснеет (не
      сломать работающие 12, чиня 3).
- [x] Доказано мутацией НА КАЖДОМ из трёх приборов, а не на одном.

## Лечение — две редакции, и вторая нужна была потому, что первая закрыла только треть

**Редакция 1 (гипотеза 1, поимённый список).** `LIVE_BY_NAME` — три прибора с причиной у каждой
строки; членство в корпусе даёт ИМЯ, а не метка. Плюс `marksWithoutMembership()`: носит метку и
в корпусе не числится — само по себе нарушение.

🔴 **Редакция 2 (критерий 2, смена 10) — `CORPUS_FLOOR = 15`.** Список закрыл ТРОИХ; остальные
двенадцать держатся живым адресом в тексте, и у них дыра ровно того же класса. Замер на живом
дереве до правки:

| мутация `verify-icons.mjs` (адресный прибор) | осмотрено | вердикт СТАРОГО стража |
|---|---:|---|
| снята ТОЛЬКО метка | 15 | ❌ красный, прибор назван, код 1 ✅ |
| снята метка **И** живой адрес | **15 → 14** | ✅ зелёный, код 0 — 🔴 **та же дыра** |

Лечение — объявленная НИЖНЯЯ ГРАНИЦА корпуса, а не равенство «приборов должно быть N»: разница
в направлении устаревания. Равенство краснеет на каждом честном новом приборе и чинится правкой
числа — на третий раз правка числа становится рефлексом, и проверка выучивает человека себя
обходить. Граница на росте молчит, а устаревает только ВВЕРХ: слабеет, но ложно красной не
бывает. Довод целиком — в шапке `CORPUS_FLOOR`.

Число названо ВМЕСТЕ С КОНТУРОМ (`EXP-0226` — число без контура это слух): замер повторением
предикатов стража поверх `git show <ref>:tools/*.mjs` дал **15 на `ndim_dev3` (dfcb2ba) и 15 на
`main` (4f9b87b)**, списки совпадают поимённо, файл в файл — мерж границу красной не сделает.

## Доказательства лечения — прогоны 2026-08-29 (смена 10, `ndim_dev3`)

**Критерии 1, 3, 4 — мутация на каждом из трёх поимённых + контроль на адресном.** Снят импорт
`probe-mark`; предмет чтения печатался до и после (`EXP-0198`); каждая проба откатана из git:

| прибор | как держится в корпусе | вердикт |
|---|---|---|
| `smoke.mjs` | именем | ❌ красный, назван, код 1 |
| `probe-bridge.mjs` | именем | ❌ красный, назван, код 1 |
| `verify-funnel-v2.mjs` | именем | ❌ красный, назван, код 1 |
| `verify-icons.mjs` — **контроль** | живым адресом | ❌ красный, назван, код 1 |

**Критерий 2 — все три причины усыхания краснеют.** Было «осмотрено 14, код 0, зелёный»,
стало — код 1 с текстом `КОРПУС УСОХ: живых приборов 14, объявленная нижняя граница 15`:

| мутация | вердикт |
|---|---|
| `verify-icons.mjs`: снят адрес И метка (полный отцеп) | ❌ красный, код 1 |
| `probe-ideas21.mjs`: то же на ДРУГОМ адресном приборе | ❌ красный, код 1 |
| `verify-tierA.mjs` переименован | ❌ красный, код 1 |
| `probe-bug40-live.mjs` удалён | ❌ красный, код 1 |

**Самотест — 16 → 20 случаев**, четыре новых (ниже границы · ровно на границе · выше границы ·
ноль со своим диагнозом «признак сломан»). Каждый доказан мутацией САМОГО стража — набор,
неспособный покраснеть, доказывает только себя:

| мутация стража | покраснел случай |
|---|---|
| `corpusFloorFaults` всегда пуста | «ниже границы» + «ноль — свой диагноз» |
| `judged !== floor` вместо `judged < floor` | «выше границы — чисто» |
| `judged <= floor` вместо `judged < floor` | «ровно на границе — чисто» |
| ветка нуля отключена | «ноль — свой диагноз, а не усох» |

🔑 **Ещё одна правка, мелкая на вид:** граница ПЕЧАТАЕТСЯ рядом с числом —
`осмотрено 15 (объявленная нижняя граница 15)`. Разбор выше называл главным симптомом то, что
«осмотрено 14» не с чем сравнить; теперь есть с чем, и глазами тоже.

**Ворота на закрытии (`ndim_dev3`, поверх dfcb2ba):** `npm run guards` — стражей 8, все зелёные ·
`npm run test:tools` — 382/382 · `npm run test` — 416/416 · `npm run check` — 517 файлов, 0 ошибок.

## Гипотезы лечения (ранжированы)

1. **Дешёвая и точная.** Поимённый список опт-ина в самом страже — те три прибора, что живого
   адреса не держат, перечислены с причиной. Список маленький, закрытый и краснеющий: прибор из
   списка без метки — нарушение. Ровно та форма, которую страж УЖЕ применяет к себе (`SELF`).
2. **Механическая.** Считать «живым» и прибор, принимающий `--base`/`PROBE_BASE`: это и есть
   способ дойти до живого контура рукой, названный в комментарии самого стража как повод
   завести третью половину признака. Тогда корпус перестаёт зависеть от метки вовсе.
3. ⛔ **Чего делать НЕ надо:** расширять корпус до всех браузерных приборов. Автор отверг это
   осознанно и прав: прогон на `localhost` пишет в эмулятор, которого никто не читает.

## Решения, принятые без владельца

Одно: считать это дефектом, а не замечанием. Основание — свойство «опт-ин необратим» записано в
код как обоснование решения №5 сдачи («полнота держится машиной, а не списком»), и оно неверно;
решение остаётся правильным, но опирается на гарантию, которой нет.

## Ссылки

`qa/team-verdicts.md` (смена 7, суд №3 — полный замер) · `tools/verify-probe-mark.mjs` ·
`plans/76_funnel_v2_phase1_operational.md` → решение 5 · `bugs/202` · `EXP-0198`

## ✅ СТАТУС: DONE (2026-08-29 12:14 +03:00)

**Чем доказано:** критерий 2 закрыт объявленной границей CORPUS_FLOOR=15: все три причины усыхания корпуса краснеют живой мутацией (полный отцеп адреса+метки на verify-icons.mjs и probe-ideas21.mjs, переименование verify-tierA.mjs, удаление probe-bug40-live.mjs) — было «осмотрено 14, код 0, зелёный»; критерии 1/3/4 — мутация на каждом из трёх поимённых приборов плюс контроль на адресном; самотест 16 → 20 случаев, каждый новый доказан мутацией самого стража; ворота guards 8/8, юниты 382/382 и 416/416, check 517/0

> Закрыт командой `node tools/close-bug.mjs` — шаг механизирован, чтобы документ не отставал от кода.
