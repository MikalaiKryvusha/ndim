# Баг 134 — стенд умирает от нехватки памяти после долгой работы

> **Заведён:** 2026-08-16 · **Нашёл:** агент, при встраивании ворот Smoke в дверь выката (`plans/54`
> К7) · **Статус:** 🔴 **ПРИЧИНА УСТАНОВЛЕНА И ДОКАЗАНА 2026-08-22** (роль `qa`, контрольный опыт
> с правкой конфига и без неё) — виновник `vite dev` и его вотчер над каталогом сборки; лечение
> в три строки конфига проверено, ЧИНИТЬ РАЗРАБОТЧИКУ ·
> **Тяжесть:** средняя — продукт в бою не задет, но ложные диагнозы стоят дорого

## Симптом

Стенд (`npm run stand`), проработавший около 40 минут, умирает сам:

```
FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory
1: 00007FF7D92E403F node::OnFatalError+1343
...
!  Script exited unsuccessfully (code 134)
Error: Script "node tools/seed-dev.mjs && node tools/stand.mjs" exited with code 134
```

Эмуляторы после этого гасятся штатно (`emulators: Shutting down`), но `vite dev` и осиротевшие
процессы остаются — их пришлось гасить руками по дереву.

## Чем это опасно (важнее самого падения)

**Умерший стенд выглядит как сломанный продукт.** Прогон набора Smoke, начавшийся при живом стенде и
доработавший при мёртвом, выдал четыре «провала»: «оценки в базе больше нет — упал: fetch failed»,
«кнопки „Выйти" на экране нет», консоль в `ERR_CONNECTION_RESET`, «след прогона не убран». Ни один
из них не был дефектом продукта. Сессия, доверившаяся этому отчёту, пошла бы чинить исправное — это
ровно тот класс ложного диагноза, которым проект уже платил (`bugs/103`, капканы стенда в
`AGENT_GUIDE.md`).

## Лечение симптома, сделанное сразу

`tools/smoke.mjs` научился отличать **упавшее окружение** от дефекта продукта: перед прогоном и при
каждом обращении к базе проверяется доступность эмуляторов, и при их пропаже кейс помечается как
`ОКРУЖЕНИЕ`, а не как провал продукта. Прогон при этом остаётся КРАСНЫМ (ворота обязаны закрыться),
но причина названа верно.

## Что известно о причине

- `tools/stand.mjs` детей не буферизует — они запущены с `stdio: 'inherit'`, то есть логи копиться
  в его памяти не могут. Простая гипотеза «стенд копит вывод» **не подтверждается чтением кода**.
- Падение случилось не на старте, а после долгой работы и нескольких прогонов подряд (сборка
  начисто → `vite preview` → набор Smoke × 5, каждый со своими браузерными контекстами).
- Под тем же процессом живут `node tools/seed-dev.mjs`, `tools/stand.mjs`, сервер синхронизации с
  циклом 15 с и `vite dev`. Кто из них съел кучу — **не измерено**.

⚠️ **Форензика не снималась** — это одно воспроизведение и трасса из лога. Ставить диагноз по ней
запрещено правилом проекта: меряй прежде, чем поверишь.

## 📏 ЗАМЕР ПРОВЕДЁН 2026-08-16 (сессия 18) — гипотеза «копит стенд» ОПРОВЕРГНУТА

Процедура ниже исполнена буквально: стенд поднят, `node tools/smoke.mjs` прогнан **шесть раз**
подряд, после каждого снят `WorkingSetSize` по процессам. Все шесть прогонов зелёные (код 0,
окно 1.2–1.3 мин из 10).

| Процесс | база | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|---|
| **`vite dev`** | 401,5 | 545,5 | 596,8 | 619,3 | 592,6 | 613,5 | 618,9 |
| `tools/stand.mjs` | 175,4 | 177,1 | 178,0 | 178,2 | 179,2 | 179,2 | 178,9 |
| сервер синхронизации | 118,0 | 103,9 | 103,7 | 105,2 | — | — | — |

*(МБ рабочего множества.)*

**Что это говорит — и чего НЕ говорит.**

1. **Растёт ровно один процесс — `vite dev`.** Стенд и сервер синхронизации стоят плоско
   (±4 МБ за шесть прогонов, то есть шум GC). Гипотеза «стенд копит вывод» была опровергнута
   чтением кода ещё при заведении бага; теперь она опровергнута и числом.
2. 🔑 **Но прирост ЗАТУХАЕТ: +144 → +51 → +23 → далее колебания в обе стороны вокруг ~600–620 МБ.**
   Это форма ПРОГРЕВА КЭША (граф модулей и кэш трансформаций `vite dev`), а не безудержной течи:
   у течи прирост держится, а не гаснет. За шесть прогонов **падение не воспроизведено**.
3. ⚠️ **Значит виновник по-прежнему НЕ НАЗВАН, и диагноз ставить нельзя.** Замер сузил поле, а не
   закрыл вопрос: 620 МБ бесконечно далеко от предела кучи, и сами по себе прогоны набора стенд
   не убивают.

**Куда смотреть следующему заходу — по уликам исходного падения.** В тот раз рядом со стендом шло
то, чего в этом замере не было: **сборка НАЧИСТО и `vite preview`** («сборка начисто → `vite
preview` → набор Smoke × 5»). Сборка тянет каталог **17,5 МБ** (`dims-build.json`) и даёт серверный
чанк 18 МБ — это самое тяжёлое по памяти событие проекта, и происходило оно ПРИ ЖИВОМ стенде.
Проверяемое предположение (не диагноз): умер не стенд от собственной работы, а его дерево — под
одновременной сборкой. Дешёвая проверка: повторить замер, запуская `npm run build` в цикле рядом с
поднятым стендом, и смотреть те же числа.
⚠️ Это ГИПОТЕЗА. Правило проекта: меряй прежде, чем поверишь, — счёт замера против убедительных
планов в этой очереди уже 4:0.

## 🔴🔴 ДИАГНОЗ ПОСТАВЛЕН 2026-08-22 (роль `qa`, команда worktree) — ВИНОВНИК НАЗВАН ЧИСЛОМ

**Виновник — `vite dev`, и он не «течёт»: он ЗАХЛЁБЫВАЕТСЯ ВОТЧЕРОМ на файлах, которые пишет
сборка.** Улика из прошлого замера («в тот раз рядом шла сборка НАЧИСТО») отработана буквально —
прибор `tools/measure-stand-memory.mjs` гоняет шаги рядом с живым стендом и снимает
`WorkingSetSize` после каждого.

### Опыт A — как есть (текущий `main`), стенд с кучей 6336 МБ

| процесс | база | после ОДНОЙ `npm run build` |
|---|---|---|
| **`vite dev`** | **730,3** | **2107,2** (+1377 МБ за одну сборку) |
| сервер синхронизации | 201,2 | 203,0 |
| `firebase-tools` | 168,2 | 168,5 |
| java (эмуляторы) | 336,4 | 339,1 |
| `stand.mjs` | 61,3 | 61,3 |

**Стенд перестал отвечать сразу после шага** (`dev-сервер приложения` не ответил за 3 с) —
прибор это поймал и назвал. Через несколько минут процесс `vite dev` был ЖИВ, слушал 5173 и
весил уже **3653 МБ**: он не умер, он ЗАХЛЕБНУЛСЯ и потом отпустил.

### Механизм — виден в логе стенда, а не выведен

Сборка пишет в `build/` больше десяти тысяч HTML, и `vite dev` СЛЕДИТ за этим каталогом. Лог
стенда в момент сборки — тысячи строк подряд:

```
[vite] (client) page reload build/en/catalog/video-game/17.html
[vite] (client) page reload build/en/catalog/video-game/18.html
… (и так по всему собранному сайту)
```

Каждый файл сборки вотчер считает изменением исходника: перетряхивается граф модулей, летят
перезагрузки, память улетает. В `vite.config.ts` нет ни строки `server.watch.ignored` — каталоги
сборки из-под наблюдения не выведены.

### Опыт B — КОНТРОЛЬНЫЙ, та же машина, тот же сценарий, одна правка конфига

Временно добавлено `server: { watch: { ignored: ['**/build/**', '**/.svelte-kit/output/**'] } }`:

| процесс | база | после 1-й сборки | после 2-й сборки |
|---|---|---|---|
| **`vite dev`** | **272,5** | **178,7** | **185,2** |
| сервер синхронизации | 115,3 | 95,7 | 96,0 |
| `stand.mjs` | 54,3 | 54,3 | 54,3 |

**Рост исчез полностью** — вместо +1377 МБ за сборку память даже снизилась (сборщик мусора
отработал), стенд пережил ДВЕ сборки подряд и ни разу не перестал отвечать. Правка откачена
после опыта: чинит не QA.

### Почему это объясняет исходное падение ДОСЛОВНО

1. **Почему упало именно тогда:** в тот день рядом со стендом шла сборка начисто — то самое
   событие, которое разгоняет вотчер.
2. **Почему `FATAL ERROR … heap out of memory`:** предел кучи на этой машине по умолчанию
   **4288 МБ**, а стенд, поднятый с `--max-old-space-size=6144`, имеет **6336 МБ**. В опыте A
   `vite dev` дорос до 3653 МБ и выжил ТОЛЬКО благодаря флагу; без флага он проходит 4288 МБ и
   падает — ровно с той трассой, что в шапке этого бага.
3. **Почему набор Smoke выдал четыре ложных «провала»:** стенд не был мёртв — dev-сервер
   ЗАХЛЁБЫВАЛСЯ и не отвечал по таймауту, а потом оживал. Отсюда `ERR_CONNECTION_RESET` и
   «кнопки нет» на исправном продукте.
4. **Почему замер 2026-08-16 виновника не нашёл:** в нём гоняли только набор Smoke — сборки
   рядом не было, и вотчеру нечего было переваривать. Прирост `vite dev` до ~620 МБ там — это
   прогрев кэша, как и было написано; настоящая причина в тот сценарий просто не попадала.

### Лечение (проверено опытом B, отдать разработчику)

```ts
// vite.config.ts
export default defineConfig({
  // Вотчер dev-сервера не смотрит на каталоги, куда пишет СБОРКА: она кладёт туда 10 000+
  // файлов, и каждый прилетает в dev-сервер как изменение исходника (bugs/134).
  server: { watch: { ignored: ['**/build/**', '**/.svelte-kit/output/**'] } },
  …
});
```

**Критерий приёмки починки:** `node tools/measure-stand-memory.mjs --steps build,build` при
поднятом стенде — `vite dev` НЕ растёт (в опыте B: 272 → 179 → 185 МБ), стенд отвечает после
каждого шага. Обратная проверка обязательна: без строки конфига тот же прогон обязан показать
скачок в гигабайты и потерю ответа — иначе прибор меряет не то (в опыте A: 730 → 2107 МБ).

⚠️ Обход `--max-old-space-size=6144` после починки останется полезным, но перестанет быть
обязательным: он лечил симптом, а не причину.

## ✅ ПОЧИНЕНО И ПРИНЯТО — 2026-08-22, Менеджер (внедрение) по диагнозу QA

Правка — `vite.config.ts`: `server.watch.ignored: ['**/build/**', '**/.svelte-kit/output/**']`
с комментарием-обоснованием (эти каталоги пишет только сборка, dev-серверу они не входные).

**[TESTED: 2026-08-22 · критерий приёмки исполнен прибором `measure-stand-memory --steps
build,build` при поднятом стенде БЕЗ обхода по памяти: `vite dev` 270,4 → 178,7 → 185,4 МБ —
роста НОЛЬ, стенд отвечал после каждого шага, сумма дерева 934 МБ; числа —
`test-results/stand-memory/measure-2026-08-21T21-50-25-243Z.json`].** Обратная сторона
(«без строки — скачок в гигабайты») доказана опытом A QA той же ночи на этой же машине и том же
приборе (730 → 2107 МБ, потеря ответа) — прогон не повторялся, цитируется его протокол выше.

Следствия для канона сняты этой же починкой: обход `NODE_OPTIONS=--max-old-space-size=6144`
перестал быть обязательным (полезен как страховка); предупреждение «сборка рядом с чужим живым
стендом роняет его» снято.

## 📏 ЗАМЕР ПОДТВЕРЖДЕНИЯ — 2026-08-29, dev-3 (смена 11, вписан сменой 12)

**Зачем секция, если баг закрыт.** Постановка, по которой я пришёл сюда, звучала «диагноз НЕ
поставлен, поставить замером» и опиралась на строку `STATUS.md`, пережившую своё исполнение.
Факт другой: диагноз поставлен, доказан контрольным опытом и ВЫЛЕЧЕН 2026-08-22 — документ несёт
тег `_DONE_`, лечение лежит в `vite.config.ts`. Секция поэтому не переоткрывает баг, а добавляет
то, чего у него не было: **подтверждение лечения РАБОЧЕЙ нагрузкой**, а не лабораторным сценарием.

### Что наблюдалось

Наблюдение ПАССИВНОЕ: стенд не поднимался, конфиг не менялся, чужие процессы не трогались.

| Что | Значение |
|---|---|
| стенд поднят | 2026-08-29 09:32:38 |
| живёт на момент замера | **414 мин** (6 ч 54 мин) |
| `vite dev` (pid 6932) | **853 МБ** |
| `stand.mjs` (pid 13120) | 24 МБ |
| сервер синхронизации | 75 МБ |
| java (эмуляторы) | 278 + 30 МБ |
| файлов в `build/` | 21 058 |
| из них записано ПОСЛЕ подъёма стенда | **21 034** (последняя запись 10:00:49) |
| свободная память машины | 6 480 МБ из 32 660 |

**Против состояния ДО лечения, числами этого же документа:**
· исходное падение — 40 мин жизни, `vite dev` 4093 МБ при потолке 4288 → `FATAL … heap out of memory`;
· опыт A (без строки конфига) — ОДНА сборка → 730 → 2107 МБ, потеря ответа, затем 3653 МБ.

**Что из этого следует.** Полная сборка начисто (21 034 файла) прошла РЯДОМ с живым стендом —
ровно то событие, которое до лечения разгоняло вотчер, — и через 6,9 часа `vite dev` весит 853 МБ,
а стенд отвечает. Нагрузка больше лабораторной, роста нет. По этому классу стенд релизное окно не
блокирует; обход `--max-old-space-size` остаётся страховкой, а не условием работы.

**Второе чтение, 2026-08-29 20:24** (другой стенд, поднят 18:50:16, слот 0): `vite dev` 152,5 МБ
на 94-й минуте, `stand.mjs` 63,7 · 52,9 МБ, сервер синхронизации 95,3 МБ, эмуляторы 292,3 · 18,8 МБ.
⚠️ Ценность этого чтения НИЖЕ первого, и вот почему прямо: я не знаю, шла ли рядом с ним сборка,
а без сборки вотчеру нечего переваривать — то есть оно согласуется с лечением, но само по себе
его не проверяет. Ровно этим был слаб замер 2026-08-16 (см. секцию выше, п. 4 разбора).

### [ЗАМЕР: 2026-08-29 · ПАССИВНОЕ наблюдение dev-3 за ЧУЖОЙ связкой — стенд ГЛАВНОЙ КОПИИ

`D:\work\ai_sandbox\ndim`, слот 0, держатель — Менеджер. Своего стенда я не поднимал, конфиг не
менял, процессов не трогал. Числа сняты `Get-CimInstance Win32_Process` (рабочее множество),
`Get-NetTCPConnection` (кто слушает порт) и временем записи файлов `build/`.
**Чем замер НЕ покрыт:** число сборок рядом со стендом за смену я не считал — доказано, что ОДНА
полная сборка прошла и стенд её пережил. Это ПОДТВЕРЖДЕНИЕ лечения, а не второй контрольный опыт:
правка конфига не снималась, обратная сторона («без строки — скачок в гигабайты») живьём мною не
наблюдалась и цитируется по опыту A QA от 2026-08-22.]

🔑 **Почему маркер называет чужой контур, а не просто дату.** `TESTING_FRAMEWORK.md`, правило 8:
чей это был прогон и из какого контура — часть улики, а не подпись. Молчаливый замер читается
следующей сессией как «автор наблюдал на своём стенде» — и она построит на нём как на своём.
Здесь наблюдение снято со стенда другой роли, поднятого не мной и не под мою задачу; это и
объясняет, почему оно ПОДТВЕРЖДАЕТ, но не ЗАМЕНЯЕТ контрольный опыт.

## Как воспроизводить и мерить (когда дойдут руки)

1. Поднять стенд, запомнить время.
2. Гонять `node tools/smoke.mjs` в цикле, снимая после каждого прогона рабочее множество процессов:
   `Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Select-Object ProcessId, WorkingSetSize, CommandLine`.
3. Тот процесс, чей `WorkingSetSize` растёт от прогона к прогону, и есть виновник. Пока он не
   назван числом, любое «лечение» — угадывание.
4. Дешёвый обход на время расследования: поднять стенд с большей кучей
   (`NODE_OPTIONS=--max-old-space-size=4096`) — он не чинит утечку, но отодвигает падение и
   позволяет снять замер.

## Решения, принятые без владельца

1. **Не чинить вслепую.** Соблазн «поднять лимит кучи и забыть» отклонён: это спрячет утечку,
   а не уберёт её, и следующее падение случится в самый неудобный момент — например, посреди выката,
   потому что ворота Smoke теперь требуют живого стенда.
2. **Лечить сначала ЛОЖНЫЙ ДИАГНОЗ, а не падение.** Падение стенда стоит перезапуска; ложный отчёт
   стоит похода чинить исправный продукт. Поэтому первым сделано различение «окружение ≠ продукт».
3. **Не переоткрывать баг** (решение секции замера 2026-08-29). Постановка утверждала «диагноз НЕ
   поставлен», и буквальное её исполнение означало бы снять тег `_DONE_`. Отклонено: диагноз
   поставлен, доказан контрольным опытом и вылечен — неправдой была ОТСТАВШАЯ СТРОКА `STATUS.md`,
   а не состояние бага. Документ получил датированную секцию подтверждения; тег остался.
   Альтернатива «промолчать, раз всё починилось» отклонена тоже: подтверждение рабочей нагрузкой
   у бага отсутствовало, и его отсутствие однажды снова прочитают как «не проверено».
4. **Второе чтение оставлено с ОБЪЯВЛЕННОЙ низкой ценностью, а не выброшено.** Замер 20:24 сам по
   себе лечения не проверяет (неизвестно, шла ли рядом сборка). Выбор между «убрать» и «оставить,
   назвав границу» решён вторым: чтение с названной границей — это данные, молча выброшенное
   чтение — невидимость. Тот же довод, по которому в секции назван чужой контур прогона.
