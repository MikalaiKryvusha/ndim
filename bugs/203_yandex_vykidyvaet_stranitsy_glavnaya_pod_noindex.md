# Bug 203 — Яндекс выкидывает страницы из поиска: главная сайта стоит под `noindex`

**Статус:** 🔴 ОТКРЫТ, диагностика снята, лечение — развилка владельца · **Заведён:**
2026-08-28, сессия 42 (Менеджер), по письму Яндекс Вебмастера владельцу · **Нашёл:** владелец
(скриншот дайджеста Вебмастера 17–23.08) → диагностика Менеджера curl-фактами ·
**Родитель:** `plans/39` (языковые адреса; корень = распознаватель под `noindex`) ·
**Родня:** `bugs/202` (слепота воронки) · `plans/74` Ф2 (миссия консолей — глаза на Яндекс) ·
`researches/26` §4.2 (отличия Яндекса) · интервью №010 Р5 (решение о корне)

## Слово владельца (чат 2026-08-28, дословно)

> «*смотри, что эти ублюдки присылают мне. Удаляют страницы из индекса!!! Нужно направить туда
> бота через debug chrome, и вернуть им в реаботу всё, что они отклонили*»

## Симптом (скриншот дайджеста Вебмастера, неделя 17–23 августа 2026)

| Строка дайджеста | Значение |
|---|---|
| 🔴 «Главная страница сайта недоступна для робота» | корень `ndimspace.app/` не индексируем |
| «Страницы в поиске — 3» с меткой **−100%** | почти всё выкинуто из поиска Яндекса |
| Удалено: `/ru/dimension/finist-the-first-warrior-2hmbiqls` | причина Яндекса: «Малоценная или маловостребованная страница» |
| Добавлено: `/en/dimension/ship-to-shore-kzuchves`, `/menu/support` | добавления идут, но единицами |

## Диагностика — все замеры 2026-08-28 ~15:50, прибор curl

| Проверка | Результат |
|---|---|
| Главная `/` как YandexBot | **HTTP 200**, 0.46 s |
| Главная `/` обычным UA | HTTP 200, 0.21 s |
| `robots.txt` | чист: `User-agent: * / Disallow:` (пусто) + sitemap |
| Удалённая карточка Финиста как YandexBot | HTTP 200 |
| `https://www.` и `http://` | оба 301 → `https://ndimspace.app/` (правильно) |
| `/ru`, `/en`, карточки | меты `robots` НЕТ, canonical на месте |
| sitemap.xml | 10 460 страниц |
| **Корень `/`** | 🔴 **`<meta name="robots" content="noindex"/>`** |

Снаружи хост здоров: коды 200, редиректы зеркал правильные, robots чист. Единственный красный
факт — **мета `noindex` на корне**, и он НЕ дефект кода: это исполнение решения владельца
(интервью №010 Р5 = В; `plans/39`): лендинги живут на `/ru` и `/en`, «ни один не сидит на голом
корне», корень — JS-распознаватель языка (`src/routes/+page.svelte:64`), в поиске не участвует —
«цена названа и принята».

## Почему Google растёт, а Яндекс казнит — одна и та же мета, разные судьи

- **Google** оценивает страницы по отдельности: корень noindex — ну и ладно, ходит по sitemap,
  индексирует `/ru`, `/en` и карточки. Показы ×30 за месяц (аудит 2026-08-28).
- **Яндекс** считает главную страницу лицом ХОСТА. Недоступная главная — сигнал качества всему
  сайту («Главная страница сайта недоступна для робота» — формулировка их же дайджеста), и
  дальше его алгоритм малоценности (широко известный как «МПК») выкидывает внутренние страницы
  пачками. −100% страниц в поиске при живом самом сайте — типичная картина именно этого класса.

Класс дефекта: **«решение, оптимальное под одного судью, наказано другим судьёй»**. Решение
№010 Р5 принималось с ценой «корень не в поиске» — но цена оказалась не «минус одна страница»,
а «минус доверие Яндекса ко всему хосту». Цена выросла ПОСЛЕ принятия решения — значит, решение
законно перевзвесить, и это право владельца, не агента.

## Что сделано сразу (сессия 42)

- [x] Диагностика снята прибором (таблица выше), не догадкой.
- [x] **Вердикт Яндекса прочитан в Вебмастере под сессией владельца** (CDP 9222, отдельный
      отладочный профиль, владелец авторизовался): «При обращении к главной странице сайта не
      удалось получить документ: „N/a (**URL запрещён к индексированию тегом noindex**)"»,
      проблема известна с 18.08.2026 — диагноз подтверждён их же формулировкой.
- [x] **Масштаб снят выгрузкой CSV** (`test-results/ya-incident/excluded.csv`, 2767 событий):
      ADD 1388 · DELETE 1379 (LOW_DEMAND 1378 + META_NO_INDEX 1 — корень). По последнему
      событию на URL: **удалено сейчас 1352 страницы**; лендинги `/ru`, `/en` живы.
- [x] 🟢 **ПЕРЕОБХОД ОТПРАВЛЕН: 150 адресов из 150 дневного лимита** (2026-08-28 ~16:10,
      8 партий по счётчику Вебмастера «можно отправить ещё 130 → … → 10»; полный список —
      `test-results/ya-incident/recrawl-log.txt`; кадр после — `reindex-after.png`).
      Приоритет очереди — последние удалённые первыми. Корень НЕ отправлялся: до снятия
      `noindex` робот снова упрётся в запрет.
- [ ] **Остаток очереди — 1201 адрес** (`test-results/ya-incident/recrawl-rest.txt`) — по
      150/день; машинный канал разобран ниже и срока НЕ сокращает.

### 🔴 МАШИННЫЙ КАНАЛ ИЗМЕРЕН — ПУЛ ОБЩИЙ, СРОК НЕ СОКРАЩАЕТСЯ (dev-3, 2026-08-28 18:2x)

Первый живой вызов API Вебмастера состоялся в день выдачи токена. **Вопрос был не «работает ли
API», а «одна у интерфейса и API квота или две»** — от этого зависел срок разбора очереди, и
ответ поменял бы план в разы.

| Замер | Значение |
|---|---|
| пользователь · хост | `130280754` · `ndimspace.app`, права подтверждены |
| суточная квота переобхода | **150** |
| остаток на момент опроса | **0** |
| израсходовано | **150** при 150, отправленных руками в этот же день |
| **вердикт** | 🔴 **ПУЛ ОБЩИЙ** — интерфейс и API берут из одной квоты |

**Что из этого следует для плана, прямо:** машинный канал **не ускоряет** разбор очереди —
потолок те же 150 адресов в сутки, то есть остаток 1201 разбирается **примерно за 9 дней** и
машиной, и руками. Ускорения ждать неоткуда: это квота Яндекса на хост, а не наше ограничение.
**Что канал действительно даёт:** снимает ручной труд (восемь партий кликами в интерфейсе
превращаются в один вызов) и делает подачу воспроизводимой и учитываемой.
⛔ Подача переобхода (`recrawl/queue`) — **действие**, а не наблюдение, и требует слова
владельца; прибор наблюдения его не вызывает вовсе (читает только квоту).

#### 📒 ЖУРНАЛ ПОДАЧ И ОЧЕРЕДЬ — В GIT, С 2026-08-28

Очередь и журнал отправленного переехали из `test-results/` (он в `.gitignore`) в
**`reports/YANDEX_RECRAWL/`** — замечание З3 вердикта №7 QA. Довод: здесь память о **необратимом**
действии, подача уходит в чужой индекс и назад не отзывается; без версионируемого журнала на
вопрос «что уже отправлено» ответить нечем, и адреса уходят повторно, съедая суточную квоту.

🔑 **Тождество очереди при переносе доказано, а не заявлено:** 1201 адрес и sha256
`c2817707699026a6` совпали у источника и копии побайтно, множества адресов равны. Это существенно
именно здесь: разрешение владельца дано для очереди ЭТОГО инцидента, и подмена очереди под тем же
разрешением была бы расширением разрешения молчанием.

**Журнал подач** (каждая строка — прогон прибора, числа печатает он сам):

<!-- ЖУРНАЛ-ПОДАЧ: строки добавляются снизу, по одной на прогон -->

- **2026-08-28** — машинных подач НЕ было: суточная квота выбрана ручной отправкой владельца
  (остаток 0, подтверждено живым вызовом). Прибор в этот день отказался слать сам — «квота
  исчерпана», и это правильное поведение, а не простой.

#### 🔴 ПРАВИЛО СУТОК — БЕЗ НЕГО ВЕРДИКТ ПУЛА ЛОЖЕН, И ЛОЖЕН УВЕРЕННО

Вердикт строится сравнением израсходованного (`daily_quota − quota_remainder`) с числом,
отправленным руками, — а квота **суточная и сбрасывается**. Флаг `--sent-by-hand` слова «сегодня»
не знает и знать не может: его называет оператор.

- **Опрос в ТОТ ЖЕ день, что и ручная отправка** → звать
  `node tools/yandex-snapshot.mjs --quota --sent-by-hand <сколько отправлено>`. Сравнение законно.
- **Опрос на следующий день и позже** → звать `--sent-by-hand 0` (чистое чтение квоты), а вопрос
  о пуле решать **контролируемой малой пачкой руками и опросом квоты ТЕМ ЖЕ днём**.
  ⚠️ Иначе прибор увидит нулевой расход по свежей квоте при 150 в аргументе и по собственной
  логике объявит **«раздельный пул»** — уверенно и неверно. Класс — «число верное, но не о том
  дне»; поймано разбором ДО первого вызова, а не после ложного ответа.
- Прибор в этом месте честен и сам: при `--sent-by-hand 0` он говорит «сопоставлять не с чем»,
  а при несходящемся расходе — «не сходится ни с общим пулом, ни с раздельным», вместо догадки.

Правило утверждено Менеджером (смена 8) и записано здесь, а не в переписке, — переписка не
переживает смену.

#### 🔑 API И ИНТЕРФЕЙС НАЗЫВАЮТ ОДНИ И ТЕ ЖЕ СОБЫТИЯ РАЗНЫМИ СЛОВАМИ

Снимок машиной за то же окно, что и выгрузка CSV из интерфейса, дал тот же счёт и **другой
словарь**:

| Источник | Коды причин выпадения |
|---|---|
| выгрузка CSV из интерфейса (2026-08-28 ~15:5x) | `LOW_DEMAND` 1378 · `META_NO_INDEX` 1 |
| API Вебмастера, снимок (2026-08-28 18:39) | `LOW_QUALITY` 1378 · `NO_INDEX` 1 |

Счёт сходится до единицы, слова — нет. **Практическое следствие:** сопоставлять наши документы и
чужие отчёты по СЛОВУ кода нельзя, только по счёту и по адресам. Прибор поэтому складывает коды
как пришли и ничего не переводит — «нормализация» одного словаря в другой вписала бы в данные
выдумку. Машинный снимок: `reports/CONSOLES/2026-08-28_yandex_webmaster.json`.
⚠️ **Граница снимка, названная честно:** он держит сводку, разрез по разделам и **по 20 образцов
адресов на причину** — поимённого списка всех 1352 в нём НЕТ. Очередь на переобход по-прежнему
берётся из `test-results/ya-incident/recrawl-rest.txt`.
- [ ] Повторная отправка главной — ПОСЛЕ исполнения выбранного владельцем варианта лечения.

## Лечение причины — ✅ ВЫБРАНО ВЛАДЕЛЬЦЕМ: вариант А (интервью №052, В1 = А, 2026-08-28 16:24)

**Корень становится видимой страницей-дверью:** `noindex` снимается; на странице — название,
одна строка о Пространстве, две большие кнопки «Русский»/«English»; JS-распознаватель языка
и контракт писем (`mode=signIn&oobCode` → `/profile`) остаются как есть — человек страницы
по-прежнему не видит. Лендинги `/ru`,`/en`, карточки, sitemap не трогаются. Тексты двери —
на вычитку владельцу ДО выката. Путь: 4 макета (Дизайнер) → выбор владельца → код → стейдж →
бой → повторная отправка главной в переобход. Исторические варианты решения (для контекста):

- **А. Серверный 301 с корня на `/en`** (redirect в `firebase.json` вместо JS-распознавателя).
  Яндекс понимает «главная = редирект» и берёт целевую. Цена: распознавание языка по браузеру
  на корне умирает — русский человек, набравший голый домен, попадёт на `/en` (в шапке есть
  переключатель). Контракт писем (`mode=signIn&oobCode` → `/profile`) обязан пережить редирект —
  проверить, что hosting-redirect не съедает query.
- **Б. Корень — настоящая страница x-default без `noindex`**: лёгкий выбор языка (две больших
  двери RU/EN) + hreflang. Яндекс получает живую главную, Google — x-default. Цена: правка
  №010 Р5 «ни один не сидит на голом корне» смягчается до «лендинги на префиксах, корень —
  дверь»; плюс дизайн-работа (4 макета).
- **В. Ничего не менять, только переобход**: слабо — Яндекс продолжит считать главную
  недоступной, класс вернётся.

⚠️ Что бы ни выбрали — Google-динамику (×30) не рисковать: `/ru`, `/en`, карточки, sitemap не
трогаются ни в одном варианте.

## Разрешение владельца — МАШИННАЯ ПОДАЧА ПЕРЕОБХОДА (первоисточник)

> **«агент будет сам отправлять правки в яндекс»**
> — владелец, чат Менеджера, **2026-08-28 ~19:05 +03:00**, транспорт: сообщение в чате.

**На что отвечал.** Развилка была поставлена с честной ценой, дословно из вопроса Менеджера:
«Машина его НЕ ускорит — лимит общий с ручной подачей, 150 в день, ~9 дней в любом случае.
Разница только в труде: **А)** агент шлёт порцию сам каждый день, без Вашего участия ·
**Б)** каждый день отправляете Вы руками в Вебмастере». Ответ владельца = **А**.

**Граница разрешения** (зафиксирована Менеджером при передаче исполнителю, 2026-08-28):
слово покрывает машинную подачу переобхода (`recrawl/queue`) адресов очереди ЭТОГО инцидента
(`test-results/ya-incident/recrawl-rest.txt`) ежедневными порциями в пределах суточной квоты.
Никаких других пишущих действий в Вебмастере оно не открывает — разрешение на действие,
не на область.

**Механика** (решение Менеджера): первым делом каждой смены dev-3, прибором
`tools/yandex-recrawl.mjs`; перед подачей — опрос квоты (машина уступает ручной отправке);
каждая подача — строка в этом документе: отправлено / остаток очереди / остаток квоты.
Шапка прибора обязана ссылаться СЮДА, а не на саму себя (условие вердикта №7 QA).

## Приёмка

Ближняя: страницы переотправлены в переобход (список зафиксирован в этом документе после
захода в Вебмастер). Дальняя (недели): «Страниц в поиске» в Вебмастере растёт от 3; строка
«Главная недоступна» из дайджеста уходит после исполнения выбранного варианта.
