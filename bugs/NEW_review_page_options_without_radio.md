# Bug NEW — страница вычитки подняла вопрос с вариантами без кнопок выбора: варианты абзацами «**А) …**» не узнаются

**Статус:** 🔧 влито в main `2168cf6` (2026-09-26, два суда Менеджера — CONFIRMED с оговорками, затем CONFIRMED;
прогон 15/15 — `qa/reports/2026-09-25_review-radio.md`); ждёт первой страницы владельцу с вариантами — там и проверка
на реальном мире · **Тяжесть:** S1 — пострадало доверие владельца, ответ пришлось писать текстом ·
**Когда/контекст:** 2026-09-25 23:18, интервью №098 «Тест на совместимость для двоих»; нашёл владелец.

## Симптом

Слово владельца на странице, дословно: «*Г, но какого хуя ты опять радиокнопки забыл сделать? у тебя опять баг у этом
контуре блядском!*».

В интервью №098 оба вопроса несли варианты «А)…Д)», но страница показала только поле текста — кнопок выбора не было.
Владелец ответил текстом.

## Корневая причина

`tools/lib/review-core.mjs` узнаёт вариант только по пункту СПИСКА (`OPTION_START`: строка «`- **А) …**`»). Агент
(Менеджер) записал варианты абзацами «`**А) V1 …**`» — так же, как в №097. Разбор вернул 0 вариантов, и страница честно
нарисовала поле текста. Предполётная проверка (`preflight`, `lintSelfContained`) вопрос с вариантами-абзацами НЕ отказала —
класс «страница выглядит целой, а выбора нет» прошёл до владельца.

## План фикса (dev-1, контур вычитки)

1. Разбор узнаёт обе формы варианта: пункт списка и абзац, открывающийся «**А)**»…«**Е)**» (и латиницей A–F).
2. Предполётная проверка: вопрос, в котором есть строки вида «**Б)**» / «Б)» в начале, а разбор вернул меньше двух
   вариантов, — страница НЕ поднимается, код 1, называет вопрос и строку.
3. Юниты на обе формы (включая №097 и №098 как живые фикстуры) + мутант «абзац не узнаётся» → красный адресно.
4. Живой проход: страница №098 (копия в тестовой папке) показывает кнопки у В1 и В2.

## Фикс (2026-09-25, dev-1)

`INTENT: разбор узнавал вариант только пунктом списка, и счётная сверка «разобрано N из N» считала кандидатов той же
строгой регуляркой — неузнанная форма давала «0 из 0», и сверка молчала ровно при полной потере вариантов.`

1. **Разбор знает обе формы** (`tools/lib/review-core.mjs`): пункт списка `- **А) …**` и абзац `**А) …**` с первой колонки,
   продолжение абзаца — строки без отступа до пустой. У вопроса со списком кнопки строятся по списку: так агенты обходили
   дефект в №097 (абзацы-описания и короткий кликабельный список ниже), и абзацы там — описание, а не второй набор кнопок.
   Списка нет — кнопки по абзацам (№098).
2. **Счёт кандидатов широкий:** `optionLines` считает любую строку вида «X) …» (со списком, жирным или без). Неузнанная
   форма даёт «0 из N» — краснеют блок 5в `tools/verify-owner-reviews.mjs` по всем живым интервью и предполёт.
3. **Предполёт** (`tools/review.mjs` → `preflight`, `lintOptionsLost`): вопрос, ждущий ответа, у которого строк-вариантов ≥ 2,
   а разобрано меньше, страницу не поднимает — код 1, вопрос, строка и счёт названы, лечение — в тексте отказа.

`TWINS: искал другие места, где страница строит выбор из разбора, — `buildPage`/`questionCard` (кнопки вопросов) и
`artifactCard` (четыре фиксированные кнопки артефакта, из разбора не строятся). Близнец класса «сверка теми же глазами,
что и разбор» — блок 5в `verify-owner-reviews`: вылечен тем же широким счётом. Близнец ФОРМЫ — «- **A. …**» (буква с
точкой): замер по `interviews/` 23:30:57 — 21 строка в №092, без кнопок; вылечен `80623a1`. Корпус после лечения — 1193
строки-кандидата, 0 расхождений.`

Гигиена: юниты `tools/lib/review-core.test.mjs` 9/9 (новых 6); мутанты M1 «абзац не узнаётся», M2 «счёт кандидатов
строгий» и M3 «точка не узнаётся» красные адресно; урок `EXPERIENCE.md` → EXP-0331; регрессы `verify-owner-reviews` 137/0, `verify-bug110` 25/0. Ручной функциональный прогон —
копии №098 и №097 живой страницей: кнопки у В1 (5) и В2 (4) №098, у В1 №097 — три по списку без дублей; неузнанная форма —
`open` отказал кодом 1. Отчёт `qa/reports/2026-09-25_review-radio.md`.

`РЕАЛЬНЫЙ МИР: накоплено — отвеченные №097 и №098 (переподнимать не нужно); данные и техника — страницы, которые агенты
поднимают владельцу; путь — `npm run review -- open`. Не проверено на реальном мире: страницу владельцу dev-1 не поднимает;
проверка — первой страницей владельцу после мержа (Менеджер).`

### Поправки суда радиокнопок (`d5a8c87`, CONFIRMED с оговорками)

- **Н1** — форма «- **A.** Впишу числа ниже.» (точка сразу перед «**», №092 В6) не узнавалась: одна кнопка из четырёх, а
  отчёт и `@guard` говорили «все узнаются». Шаблоны принимают «.» перед пробелом и перед «**» (`df08caf`); корпус — 1196
  строк-кандидатов, 0 расхождений.
- **Н2** — `batch` строил страницу через `/doc?p=` мимо `preflight`, `queue` ставил без проверки. Теперь `queue` отказывает
  кодом 1 (`df08caf`, код выхода — `351fbc5`), `/doc` отдаёт 409 «Этот документ агент ещё чинит» с вопросом и строкой.
  Юнит `tools/review-options-gate.test.mjs`, живьём РК-04 и РК-05 — 15/15 на `351fbc5`.
- Граница: вопрос с одной строкой-вариантом (№070 В2 — картинки) порог «≥ 2» не судит.

## Решения, принятые без владельца

`[AI]` Лечение — в разборе (обе формы) И в предполётной проверке; одна из двух половин оставила бы класс открытым.

## Ссылки

`interviews/interview_098_compat_test_page_for_two.md` · `bugs/NEW_review_page_stale_tab_accepts_answers.md` (тот же контур, сегодня).
