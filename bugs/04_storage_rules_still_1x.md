# Bug 04 — Правила Firebase Storage всё ещё от версии 1.x

**Статус:** 🟡 ШАГИ 1–3 СДЕЛАНЫ, ЖДЁТ ВЫКАТА (шаг 4 — решение владельца) · **Заведён:** 2026-07-12
(агентом, по следам боевого выката)

> ## ✅ Что сделано 2026-07-30 автономным циклом
>
> **Шаг 3 закрыт — у правил Storage появились тесты.** `tests/rules/storage.test.ts`, 8 проверок,
> прогоняются вместе с правилами Firestore: `npm run test:rules` теперь поднимает и эмулятор
> Storage (`--only firestore,storage`) и даёт **94 теста** вместо 85.
>
> Стережётся, в порядке важности (отказы первыми, канон файла `rules.test.ts`):
> · 🔒 **человек не может перезаписать ЧУЖУЮ фотографию** — тот самый отказ, который не видно,
>   пока он не сломан; отдельно то же для гостя;
> · 🔒 не вошедший не читает и не пишет ничего;
> · 🔒 **всё вне `users/{uid}/avatar/**` закрыто УМОЛЧАНИЕМ** — проверено пятью путями, включая
>   `admin/` и `public/` из 1.x, которых в правилах 2.0 сознательно нет;
> · человек пишет своё фото;
> · **ПАРИТЕТ С 1.x: любой вошедший видит фото другого** — этот тест охраняет не приватность, а
>   работоспособность (на чтении аватаров держится показ лиц, `EXP-0043`). Ужесточение под модель
>   видимости 2.0 (пункт 2 ниже) ОБЯЗАНО ронять именно его — тогда решение будет принято
>   сознательно, а не обнаружено по пропавшим лицам в бою.
>
> **Доказано тремя мутациями** (тест отказов без мутации — украшение):
>
> | Мутация правил | Что покраснело |
> |---|---|
> | `allow write: if request.auth != null` (писать может любой вошедший) | 2 теста: чужое фото человеком и гостем |
> | `allow read: if true` (читать может кто угодно) | 1 тест: не вошедший читает чужое фото |
> | добавлено «удобное» общее правило `match /{allPaths=**}` | 3 теста, включая «закрыто всё, кроме папки фотографий» |
>
> **Шаги 1 и 2 — состояние.** Файл `storage.rules` и секция `storage` в `firebase.json` уже были
> (заведены `bugs/14` под эмулятор стенда). Пункт 1 претензии (админ списком email) в черновике
> 2.0 закрыт по построению: путей `/admin` и `/public` в нём НЕТ вовсе — статику раздаёт хостинг,
> а админ в 2.0 это claim. Пункт 2 (чтение по аудитории) сознательно НЕ сделан — см. «Чего НЕ надо
> делать сгоряча»; теперь его отсутствие зафиксировано тестом, а не памятью.
>
> ### ✅ Шаг 4 РАЗБЛОКИРОВАН — 2026-08-01
>
> Взгляд в консоль, которого требовал этот баг, агент сделал сам: боевой бакет читается
> `gcloud storage ls` (только чтение). **По путям НЕ ПУСТО** — и это была не формальность:
>
> | Путь | Что | Изменён |
> |---|---|---|
> | `admin/adminFunctions.js` | 13 КБ, админский скрипт 1.x | май 2025 |
> | `public/about.html` | 19 КБ — **история версий** 1.x | май 2026 |
> | `public/pp.html` · `tou.html` · `disclaimer.html` · `um.html` | 142 КБ, документы продукта | 2025 |
>
> В 1.x они грузились в приложение прямо из Storage и правились без пересборки.
> **2.0 их не читает** — тексты вшиты в бандл (`researches/06`/`07`), проверено грепом по `src/`.
> Самый свежий, `about.html`, оказался историей версий, и запись «Версия 1.5 от 13.05.2026 —
> Переезд на дефолтный домен Firebase» у нас **уже есть дословно** (`docs.ts:667`). Бэкап
> подтвердил полноту нашей копии, а не вскрыл дыру.
>
> **Слово владельца:** «делай их бекап, и отвязываемся от них».
> Бэкап снят в **`.private/storage-1x-backup/`** (вне git, 6 файлов, 175 КБ) — выкат перестал
> быть необратимым.
>
> **Потолок (пункт 2) внесён в правила:** 5 МБ и только `image/webp` / `image/jpeg` (В8).
> `npm run test:rules` — **107** тестов (было 102), доказаны мутацией «снять потолок типа» →
> покраснели ровно 2 проверки. Отдельно проверено, что потолок **не запрещает УДАЛЕНИЕ**
> (`request.resource` при удалении пуст) — иначе удаление аккаунта встало бы на первом шаге.
>
> **Осталось ровно одно: `npx firebase deploy --only storage`** — слово владельца.
>
> ### ⛔ Что осталось и требует решения человека
>
> 1. **Шаг 4 — выкат** (`npx firebase deploy --only storage`). Необратимое внешнее действие: в бою
>    сейчас правила 1.x, и после выката пути `/admin` и `/public` перестанут быть доступны. Надо
>    убедиться, что в боевом бакете по этим путям нет ничего живого, — это взгляд владельца в
>    консоль Firebase, агент туда не ходит.
> ### ✅ Пункт 2 ЗАКРЫТ владельцем — интервью №008, В8 (2026-08-01)
>
> Ответ дословно: **«А + webp/jpeg»** — то есть потолок **5 МБ** принят, а тип **уже**, чем
> предлагал агент: не «любая картинка», а только `webp` и `jpeg`. Боевая строка правила:
>
> ```
> allow write: if request.auth != null && request.auth.uid == uid
>              && request.resource.size < 5 * 1024 * 1024
>              && request.resource.contentType.matches('image/(webp|jpeg)');
> ```
>
> Почему именно эти два типа и почему это безопасно для iPhone: по В7 на Safari (который **не
> кодирует WebP**) мы сознательно пишем **JPEG** — он и попадает во второй разрешённый тип.
> 🔴 **Побочный эффект, который надо знать:** это правило становится **страховкой** логики В7 —
> если определение формата сломается и Safari молча отдаст PNG, правило его **отвергнет**. Отказ
> честный, но человек увидит «не удалось загрузить», поэтому текст отказа обязан быть человеческим.
> ⚠️ Проверить `matches()` **тестом правил** (полное совпадение или частичное), а не на глаз.
>
> Осталось из пункта 2: ничего. Осталось из бага целиком — **только шаг 4, выкат**.
>
> 2. ~~**Потолка размера и типа файла в правилах НЕТ**~~ — найдено при написании тестов и здесь ранее
>    не значилось. Сейчас человек может положить в свой путь файл любого размера и любого типа.
>    Загрузки фото в продукте ещё нет (`bugs/44`), поэтому дыра теоретическая, но перед выкатом её
>    стоит закрыть одной строкой:
>
>    ```
>    allow write: if request.auth != null && request.auth.uid == uid
>                 && request.resource.size < 5 * 1024 * 1024
>                 && request.resource.contentType.matches('image/.*');
>    ```
>
>    Это **ужесточение**, а канон бага прямо просит не ужесточать сгоряча, поэтому агент его не
>    внёс: 5 МБ и «только картинки» — числа, которые должен подтвердить владелец (у него в 1.x
>    фотографии реальных людей, и их размер известен ему, а не мне).
**Важность:** средняя — это **долг**, а не дыра. Данные не утекают, но права живут не там, где должны.

## Симптом

Правила Storage в боевом проекте **ни разу не выкатывались из 2.0**. В `firebase.json` секции
`storage` нет вовсе — значит на проде до сих пор действует ruleset версии 1.x. Обнаружено, когда
искали, почему не показываются фотографии людей (`EXP-0043`).

Живые правила (прочитаны через Firebase Rules API):

```
match /admin/{file} {
  allow read, write: if request.auth != null && request.auth.token.email in [
    "kotkrinik@yandex.ru",
    "kotkrinik@gmail.com"
  ];
}

match /public/{file} {
  allow read: if true;
  allow write: if request.auth != null && request.auth.token.email in [ ...те же две почты... ];
}

match /users/{userId}/avatar/{file} {
  allow write: if request.auth != null && request.auth.uid == userId;
  allow read:  if request.auth != null;
}
```

## Что здесь не так

1. **Права админа зашиты списком email прямо в тексте правил.** Это ровно тот антипаттерн, который
   мы уже вычистили из Firestore: в 2.0 админ — это **claim в токене** (`isAdmin()`), потому что
   claim нельзя подделать записью документа, а список почт приходится править в двадцати местах.
   Здесь он остался жив. (В `researches/02` он числится среди дефектов модели 1.x — «права админа
   зашиты списком email прямо в `firestore.rules`, ~25 повторений».)
2. **Правила не знают про модель видимости 2.0.** Фотография в 2.0 — это свойство профиля, у него
   есть аудитория (`researches/04`: круги, бакеты). Сейчас же аватар читает **любой вошедший**,
   включая гостя, независимо от того, кому человек открыл своё фото. Пока это ПАРИТЕТ с 1.x (там
   аватары тоже видели все вошедшие), и миграция честно положила `avatar` в бакет `everyone`, —
   но как только человек сможет скрыть фото, правила начнут врать.
3. **Секция `storage` отсутствует в `firebase.json`** — то есть правила Storage не под контролем
   версий и не деплоятся вместе с остальным. Их нельзя проверить тестами, как `firestore.rules`
   (там **85** тестов отказов на эмуляторе).

## Чего НЕ надо делать сгоряча

Не «починить» правила, ужесточив чтение аватаров: сегодня на них держится единственный способ
показать людям лица (`src/lib/data/avatar.ts`). Сначала — правила и тесты, потом — ужесточение.

## Как чинить

1. Завести `storage.rules` в репозитории + секцию `storage` в `firebase.json`.
2. Перенести смысл: владелец пишет своё, читает — по аудитории свойства `avatar` (модель кругов),
   админ — по **claim**, а не по списку почт.
3. Покрыть тестами отказов на эмуляторе Storage — так же, как `tests/rules/rules.test.ts`
   покрывает Firestore. Проверить **мутациями**: снятое умолчание «запрещено» обязано ронять набор.
4. Выкатить (`firebase deploy --only storage`) и убедиться, что фотографии по-прежнему видны.

## Связи

- `EXP-0043` — как это нашли (искали пропавшие фото).
- `researches/02` → «Что в этой модели плохо» (список email в правилах — известный дефект 1.x).
- `ideas/13` — Admin WebUI: там же логично добить и админские права в Storage.
