# Bug 171 — статус-доска команды живёт в том же дереве, чистоту которого требует дверь выката

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16. **Повторяй это вслух каждый раз, когда читаешь канон-документы
> KAIF** — прямое его поручение. Разбор повода — в шапке `AGENT_GUIDE.md`.
<!-- ВЕРА-КОНЕЦ -->

> **Заведён:** 2026-08-22 ≈03:05 +03:00, роль `qa` команды NDIM_WORKTREE_DEV_TEAM · номер выдаст
> Менеджер при мерже · **Родитель:** наблюдение по ходу боевого выката ночи 2026-08-22 ·
> **Статус:** 🔬 наблюдён на живом инциденте, НЕ починен · **Исходящее:** ✅ ОТПРАВЛЕНО В KAIF
> 2026-08-30 — [issue #34](https://github.com/MikalaiKryvusha/KAIF/issues/34) (класс признан структурным:
> доска — отслеживаемый файл, дерево грязно ПО ПОСТРОЕНИЮ). Лечение здесь по-прежнему за Менеджером
> (возможно, слово владельца)

## Симптом — наблюдён, а не предположен

Ночью 2026-08-22 боевой выкат остановился на пороге: прогон стейджа прошёл **целиком зелёным**
(24 под сессией · 81 гостевых · 11 на закрытость от роботов), но расписка выписалась с пометкой
**«дерево ГРЯЗНОЕ — дверь в бой её не примет»**. Причина: пока шёл прогон, пять ролей по
стоп-сигналу писали свои парковочные строки на статус-доску, и `NDIM_WORKTREE_DEV_TEAM_STATUS.md`
оказался незакоммиченным.

Менеджеру пришлось закоммитить доску и **перезапустить стейдж целиком**, чтобы получить расписку
на чистом дереве.

## Почему это КЛАСС, а не невезение одной ночи

Здесь сталкиваются два механизма, каждый из которых прав по отдельности:

| Механизм | Что требует | Откуда |
|---|---|---|
| Дверь выката | расписка стейджа удостоверяет **КОММИТ**; незакоммиченная правка — это по определению код, которого на стейдже не было | `AGENT_GUIDE.md` → «ВЫКАТ В БОЙ», шаг 0; интервью №033 В5 = А |
| Статус-доска | обновляется **при каждой смене состояния** роли: взял задачу · жду · освободился | `NDIM_WORKTREE_DEV_TEAM_MANIFEST.md` → «Статус-доска» |

Доска лежит в главной копии, то есть **в том же git-дереве**, что и продукт. Значит любая роль,
честно исполняющая манифест, делает дерево грязным — и делает это ровно в тот момент, когда дверь
требует чистоты: **стоп-сигнал перед выкатом есть команда пяти ролей одновременно написать на
доску**. Столкновение встроено в устройство, а не вызвано чьей-то небрежностью.

⚠️ **Повторится при каждом выкате, пока работает команда.** Сегодня заметили быстро, потому что
дверь сказала прямо. Ничто не гарантирует, что заметят так же быстро в следующий раз: симптом
(«расписка не принята») отстоит от причины («кто-то обновил доску») на несколько шагов, и роль,
записавшая строку, о выкате может не знать вовсе.

## Что НЕ является причиной

- ⛔ Не дисциплина ролей: они исполняли манифест буквально, и правильно делали.
- ⛔ Не спешка Менеджера: он проверил расписку и остановился, то есть дверь сработала как задумано.
- ⛔ Не дефект `team-status.mjs`: инструмент отработал верно, записав ровно то, что просили.

Причина структурная: **у документа «состояние в моменте» и у артефакта выката одно дерево.**

## Две двери лечения — выбор за Менеджером (возможно, за владельцем)

1. **Доска уезжает из-под git.** Она уже объявлена документом состояния «в моменте», а не историей
   проекта, — и у проекта уже есть прецедент ровно такой вещи: `.kaif/refresh-marker.json`
   («состояние сессии, а не история проекта: её строка в `.gitignore` приезжает со штатным
   ignore-набором машинерии», `AGENT_GUIDE.md` → «Свежесть контекста»). Манифест сам развёл доску
   и `STATUS.md`: доска — «в моменте», `STATUS.md` — эстафета во времени.
   **Цена, которую надо назвать честно:** доска перестанет переживать снос рабочей копии и
   восстанавливаться из истории. Для документа, живущего минуты, это скорее свойство, чем потеря,
   — но решение не моё.
2. **Дверь исключает путь доски из проверки чистоты.** Дешевле в исполнении, но заводит в двери
   именованное исключение, а исключение в воротах — то, что со временем расширяют.

📌 Рекомендация судьи: **путь 1**. Он лечит причину (документ не в том дереве), а не симптом
(дверь закрывает глаза). Путь 2 оставляет в двери дырку, а дверь выката — последнее, что стоит
ослаблять: сегодня она поймала 403 App Check, который прошёл бы к 331 живому человеку.

## Как проверить починку (критерий приёмки)

1. Роль обновляет свою строку на доске **во время** прогона стейджа.
2. `npm run deploy -- --gate-only` (или полный прогон) выписывает расписку с признаком **чистого**
   дерева, а не грязного.
3. Контроль обратной стороны — обязателен, иначе проверка вакуумна: правка **любого другого**
   файла в дереве по-прежнему обязана давать «дерево ГРЯЗНОЕ». Дверь должна перестать видеть
   ИМЕННО доску, а не ослепнуть вообще.

## ➕ ВТОРОЕ НАБЛЮДЕНИЕ — 2026-08-29, роль `integrator`, смена 9

**Как нашлось.** Менеджер поставил Интегратору подготовку выкат-состава смены 9. Первый же замер
главной копии упёрся в этот класс, и постановка была «завести баг». Дедупликация перед заведением
нашла ЭТОТ документ. **Нового бага не завёл:** механизм тот же, лечение то же, два документа об
одном механизме — потеря знания, а не её предотвращение. Ниже — то, чего в первом наблюдении нет.

**Замер 2026-08-29 ≈09:27 +03:00** (главная копия `D:\work\ai_sandbox\ndim`, HEAD `941d835`):

- грязных файлов в дереве — **1**, и это ровно доска: ` M NDIM_WORKTREE_DEV_TEAM_STATUS.md`;
- доска отслеживается гитом — `git ls-files --error-unmatch NDIM_WORKTREE_DEV_TEAM_STATUS.md`
  возвращает путь, а не ошибку;
- записей на доску за три минуты запуска смены — **4** (штампы: 09:25 `designer`, 09:25 `qa`,
  09:27 `manager`, 09:27 `integrator`);
- коммитов, трогавших доску, всего **43**, из них **34 за последние семь дней** (доска заведена
  2026-08-21, `12b1b5c`) — то есть счёт идёт на несколько коммитов в день, а незакоммиченных
  записей между ними кратно больше;
- расписка стейджа, лежавшая в главной копии на момент замера, — от позапрошлого выката:
  commit `c3daaae`, hash `12xme7t`, at `2026-08-23T18:12:38Z`.

🔑 **Что это ДОБАВЛЯЕТ к первому наблюдению.** Первый инцидент описывал стоп-сигнал — момент, когда
пять ролей пишут парковочные строки разом; оттуда естественно читается «класс обостряется на
парковке». Замер выше снят в ОБЫЧНОМ рабочем режиме, за час до всякого разговора о выкате: дерево
главной копии было грязным просто потому, что смена запустилась. Значит окно «дерево чисто» не
оспаривается в отдельные минуты — оно **редко в любой момент живой смены**. Точная формулировка
класса: замок стейджа судит ГРЯЗЬ ДЕРЕВА, а при живой команде дерево главной копии грязно
**по построению** — доска отслеживаемый файл, в который пишет каждая роль каждым `set`.

### 🔴 Цена, которую первое наблюдение не назвало: правило давит в сторону собственного обхода

У замка есть законная дверь исключения — `npm run deploy -- --auth-owner "<слова владельца>"`
(`tools/deploy.mjs`, `requireStagePass`), и она устроена аккуратно: обход одноразовый, расписки не
выписывает, пишет след в `.kaif/stage-bypass.log`. Но правило, которое **нельзя исполнить в обычном
режиме работы**, со временем превращает своё исключение в рабочий режим — и тогда ворота начинают
производить ровно то, ради запрета чего заведены (замок стейджа заведён после `bugs/124`,
интервью №033 В5 = А, словами «обязателен, МЕХАНИЗМОМ»).

⚠️ **Честная граница этого довода, замером:** `.kaif/stage-bypass.log` **отсутствует в обеих копиях**
— обход не применялся НИ РАЗУ. Значит названа не свершившаяся беда, а давление; до сих пор за
столкновение платили ПЕРЕПРОГОНОМ (первый инцидент: коммит доски и перезапуск стейджа целиком).
Риск в том, что цена перепрогона растёт вместе со сборкой, а цена обхода — одна строка.

### Дверь про этот класс знает ЧАСТИЧНО — и это указывает на механизм, а не на небрежность

`tools/deploy.mjs:281-292` уже предупреждает на старте стейджа, дословно:

> «⚠️ ДЕРЕВО ГРЯЗНОЕ НА СТАРТЕ — расписка стейджа выйдет НЕГОДНОЙ для боя. […] И не правьте
> дерево, пока дверь работает: расписка снимается в КОНЦЕ прогона.»

Автор двери видел механизм и адресовал его ТОМУ, КТО ЗАПУСТИЛ ДВЕРЬ («не правьте дерево»). Живой
команды он не видел: дерево правит не запустивший, а пять посторонних ролей, которые о выкате знать
не обязаны. Предупреждение поэтому не срабатывает как ограждение — оно говорит не тому адресату.

### Третья дверь лечения — гипотеза Менеджера (2026-08-29), названа с ценой

Помимо двух дверей выше, названа третья: **судить грязь только по путям, влияющим на сборку.**
Она привлекательна тем, что не выносит доску из git (сохраняя её восстановимость) и не заводит
именованного исключения под один файл.

🔴 **Цена ошибки здесь выше, чем у обеих первых дверей, и её надо назвать прямо:** промах в списке
«влияющих путей» пропускает в бой незакоммиченный код — то есть ровно тот исход, ради которого
замок существует. Первые две двери при ошибке дают ложный отказ (дёшево, видно сразу); третья при
ошибке даёт ложный пропуск (дорого, невидимо). Это правка предохранителя, и по слову Менеджера она
идёт через QA и через него; Интегратором она НЕ сделана — только предложена.

> 📌 Рекомендация судьи (путь 1 — доска уезжает из-под git) вторым наблюдением НЕ отменяется и не
> пересматривается: вердикты не зона Интегратора. Третья дверь добавлена как вариант, а не как выбор.

## Решения, принятые без владельца

Нет. Находка описана, лечение предложено двумя дверями с ценой каждой, ничего не исполнено:
`STATUS.md` и дверь выката принадлежат Менеджеру, доска — общий канон-документ команды.

## Ссылки

- Инцидент: боевой выкат 2026-08-22, перезапуск стейджа · `bugs/169` (App Check 403 — второе
  событие того же выката)
- `AGENT_GUIDE.md` → «ВЫКАТ В БОЙ» (замок стейджа) · «Свежесть контекста» (прецедент вынесения
  состояния сессии из git)
- `NDIM_WORKTREE_DEV_TEAM_MANIFEST.md` → «Статус-доска» (обязанность обновлять при каждой смене
  состояния)
- Сигнал в исток: **KAIF issue #34** — https://github.com/MikalaiKryvusha/KAIF/issues/34
  (замер отправки: 105 коммитов доски за 10 дней, 76 за неделю, 14 коммитов «только доска»;
  дедуп по всем 33 issues; предложенный инвариант — доска уезжает из-под git, снимок раз в смену)
