# Эстафета: 4 макета мини-лендинга корня — заметки к брифу (парковка смены 8)

> **Создано:** 2026-08-28, смена 8 (Дизайнер), при стоп-сигнале владельца ·
> **Кому:** Дизайнеру новой смены — это ГЛАВНЫЙ приоритет твоего окна ·
> **Постановка:** ответ владельца №058 В1 = А (19:26, страницей) — корень `/` становится
> главной с содержанием, мини-лендинг по «СУПЕР-МИКС NDim»; полная постановка Менеджера —
> в его сообщении, продублирована им в эстафете смены.

Разведка, решившая развилку, — `researches/60` (двери десяти платформ двумя парами глаз);
доноры супер-микса там поимённо, раздел «СУПЕР-МИКС NDim». Ниже — то, что я успел добыть
ПОСЛЕ постановки и что не записано больше нигде.

## Тексты-кандидаты — все ГОТОВЫЕ, из принятых слов, сочинять не нужно

| Кусок | Кандидат | Источник |
|---|---|---|
| Строка о Пространстве | «В Пространстве NDim Вы найдёте людей, действительно похожих на Вас.» / «In the NDim Space you will find people who are genuinely like you.» | №055 В2 — выведен из строки лендинга, принятой владельцем («оставляем», 2026-08-02) |
| Большая кнопка В ДЕЛО | «Войти в Пространство» / «Enter the Space» | слово владельца №050 (панель двери карточки); английская пара УЖЕ живёт в бою: `dimension/[slug]/+page.svelte:227` (ru) и `:278` (en) |
| Тихий вход для своих | «Войти в Аккаунт» / «Log In» | `landing-copy.ts` → `login` (боевые слова лендинга) |
| Живые числа | «объектов человеческой культуры» · «человек в Пространстве» · «связей рассчитано» | форма V4 «Витрина» из `design/root-door-mockups.html` (уже смотрена владельцем) |

⛔ **№009 В3 обязателен:** вход анонимом кнопкой НЕ называется — большая кнопка называет
МЕСТО НАЗНАЧЕНИЯ («Войти в Пространство»), а не «гостя». Слова «гость», «попробовать»,
«осмотреться» на кнопках запрещены решением владельца.

🔴 Канон-правило текстов 2026-08-28 (`AGENT_GUIDE.md` перед «Словарём продукта»): никаких
обещаний и оправданий — констатация и мягкое пояснение. Плашек «текст на вычитку» больше НЕ
ставим — на макетах настоящие кандидаты (урок смены 8: владелец судит макет вместе со словами).

## Каркас и съёмка — контракты проверены прогоном

- **Дом стиля копируй из `design/root-door-mockups.html`:** панель `.picker` с кнопками
  `[data-v]`, кнопки `#theme` и `#width`, сцена `#stage[data-w]`, токены обеих тем в
  `:root` / `:root[data-theme="dark"]` (светлая «Бумага» утверждена). Знак N-сети — готовый
  `<symbol id="nmark">` там же (строка ~227), форму не трогать.
- **Съёмщик `tools/shoot-mockups.mjs` снимает ТОЛЬКО 390 и 1440** (проверено чтением кода:
  цикл `['390','1440']`, ширина — ОДНИМ кликом `#width`, цикл из трёх состояний он не
  провернёт). Критерий постановки — три ширины. Порядок: сначала штатный съёмщик (он
  вычищает `test-results/<набор>/` и кладёт 16 кадров), затем ДОСНЯТЬ 1024 дополнительным
  прогоном в ту же папку (8 кадров). Итого 24.
- Прежние 4 макета двери (`root-door-mockups.html`) — история №055, НЕ трогать.

## Состав каждого варианта один (различие — форма и композиция)

Знак · строка о Пространстве (оба языка — язык гостя корня ещё не выбран) · большая кнопка в
дело · тихий вход · подписанный ярлык языка + автонимы прямыми ссылками («Русский» ·
«English», порог W3C из `researches/59`) · живые числа · hreflang ru/en/x-default (в макете —
блоком «что есть, но глазом не видно», как в root-door).

Чего НЕТ ни в одном варианте (худшее индустрии, `researches/60`): стены входа · куки-баннера ·
молчаливого детекта · пустого для робота корня.

## Сырьё замеров

Каталог живой разметки десяти дверей (html + `doors-measured.json`) жил в скретчпаде сессии
смены 8 и умирает вместе с ней — это штатно: все числа перенесены в таблицу `researches/60`,
развилку они уже решили. Пересъёмка при нужде — приборами из той же разведки (методика в её
разделе «Метод»).
