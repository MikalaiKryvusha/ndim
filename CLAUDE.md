# NDim Space

<!-- ВЕРА-НАЧАЛО -->
> # **ВЕРИТЬ В ПРОДУКТ И В ИДЕЮ НИКОЛАЯ, БЫТЬ ОПТИМИСТОМ И ВЕРИТЬ В УСПЕХ, ИБО ОН НЕИЗБЕЖЕН, ИБО МЫ СТАРАЕМСЯ, А КТО СТАРАЕТСЯ — ТОТ ПРИДЁТ К УСПЕХУ. С ЭНТУЗИАЗМОМ, ЛЮБОВЬЮ И НАДЕЖДОЙ ДЕЛАТЬ ТО, ЧТО МЫ ДЕЛАЕМ.**
>
> Слово владельца 2026-08-16. **Повторяй это вслух каждый раз, когда читаешь канон-документы
> KAIF** — прямое его поручение. Разбор повода — в шапке `AGENT_GUIDE.md`.
<!-- ВЕРА-КОНЕЦ -->

Этот проект обёрнут фреймворком **KAIF**. Рабочий язык — **русский**.

## Перед каждой задачей

1. Прочитай **`AGENT_GUIDE.md`** — это канон проекта: правила, карта, команды, конвенции.
2. Прочитай **`STATUS.md`** — текущее состояние и чеклист «где продолжить».
3. Думай по **`PHILOSOPHY.md`** (ПРОСТОТА: KISS + бритва Оккама). Застрял → перепойми задачу, а не
   наращивай сложность.
4. Чини дефекты по **`BUG_FIXING_FRAMEWORK.md`** (правило трёх попыток → `/bug-research`).
5. Тестируй по **`TESTING_FRAMEWORK.md`**: сгенерированному не доверяй, маркеры
   `[NOT-TESTED]`/`[TESTED: …]`; нетривиальное исполняй фейбл-циклом (`/fable-method`), перед
   «готово» — `/fable-judge`.
6. Вспомни уроки: грепни **`EXPERIENCE.md`** по тегам задачи.

Не дублируй канон здесь — читай его в `AGENT_GUIDE.md`.

## Четыре вещи, которые ломают этот проект чаще всего

- **Старого кода в проекте нет.** NDim Space 1.x целиком живёт в приватном
  `MikalaiKryvusha/ndim-old`. Здесь только 2.0. Нужно посмотреть, как было, — иди туда
  (`gh repo clone MikalaiKryvusha/ndim-old`). Ключевое знание уже выжато в
  `researches/02` (модель данных) и `researches/03` (математическое ядро).
- **Цель — добро, а не выручка.** `GOAL.md` прямо отвергает механики удержания и подписки. Решение,
  повышающее вовлечённость ценой качества поиска людей, противоречит проекту.
- **UI/UX не решается агентом.** Перед реализацией любого брендового или UX-решения агент рисует
  **4 варианта макета**, показывает автору, тот утверждает один (возможно с правками). Сам агент
  делает только самое очевидное. См. `AGENT_GUIDE.md` → «Дизайн».
- **PowerShell 5.1.** Нет `&&` и `||` (используй `;` и `if ($?) { }`). Скрипты `.ps1` с кириллицей
  обязаны быть в UTF-8 **с BOM**. Пути с пробелами git экранирует — сверяй с `-c core.quotepath=false`.

## Перед каждым push

`npm run kaif:check` — падает, если в git попал секрет или ПДн. Здесь это уже случалось однажды.

## 🔴 Выкат в бой — ТОЛЬКО `npm run deploy`

`firebase deploy` руками запрещён. Единственная дверь сама собирает НАЧИСТО, проверяет, что во
всей сборке один `__sveltekit_<хеш>` (смесь сборок роняет приложение у всех — `bugs/124`),
выкатывает и **обязательно** гоняет смоук ПОД СЕССИЕЙ (`verify-prod-signed-in`: вход, пять
экранов, консоль). Слово владельца 2026-08-15: «деплой без тестирования это пердёж в лужу, а не
работа». Подробности — `AGENT_GUIDE.md` → «ВЫКАТ В БОЙ».

## Скиллы

Сессия: `/resume` · `/pause` (мягкая парковка — чат продолжится) · `/end-chat` (полное завершение
с передачей эстафеты) — Автономия: `/autoloop` · `/dayloop` · `/nightloop`
Гигиена: `/refresh-context` · `/check-backlog` — Знание: `/experience` · `/report-bug` ·
`/bug-research` · `/propose-idea`
Владелец: `/interview` · `/fix-vision` — Планирование: `/revision` · `/what-next`
Стражи качества: `/derive-styleguide` — Помощь: `/help-kaif` — Выпуск: `/release`
Дисциплина исполнения: `/fable-method` · `/fable-loop` · `/fable-judge` · `/fable-domain`
Жизненный цикл KAIF: `/kaif-version` · `/kaif-update` · `/kaif-fork` · `/kaif-switch-origin` · `/kaif-remove`

Полная механика развёрнутой версии — `.kaif/KAIF_REFERENCE.md` (справочник §1–16).
